# Copy this file to one named exactly:  .env
# Then paste your API key after the = sign (see README.md, Step 4).
# The .env file stays private on your computer — it is never uploaded.

LEADROUTER_API_KEY=paste-your-key-here

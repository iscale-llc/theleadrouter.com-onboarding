openapi: 3.0.3
info:
  title: iSCALE Lead Routing API
  version: 2.0.0
  description: >-
    # Getting Started


    The iSCALE Lead Routing API provides programmatic access to the lead routing platform. Use it to manage verticals,
    campaigns, buyers, contracts, and leads.


    ## Authentication


    Three authentication methods are supported:


    ### Admin API Key (Full Dashboard Parity)

    ```

    Authorization: Bearer lr_xxx

    ```

    An API key with the `admin` scope authenticates against every `/api/admin/*` endpoint

    with the SAME tenant-scoped access as a UI admin session — it resolves the creating

    admin's live permissions, so demoting or deactivating that admin disables the key.

    Excluded from key auth: impersonation and API-key mint/revoke (session-only). Money-mutating

    endpoints additionally require the key's `allowBilling` flag (default on). Create via the

    admin dashboard (Settings > API Keys). Legacy `routing`/empty-scope keys do NOT reach

    `/api/admin` — the `admin` scope is an explicit grant.


    ### Session Cookie (Admin/Portal/Partner)

    ```

    POST /api/v1/auth

    { "email": "user@example.com", "password": "..." }

    ```

    Returns a `lr_session` cookie used for subsequent requests.


    ### Partner API Key (Lead Submission and Partner API)

    ```

    Authorization: Bearer lr_xxx

    ```

    Lead submission (`POST /api/v1/leads/submit`) authenticates ONLY on this header — the key

    must carry the `posting` scope, and it identifies the partner. There is no query-parameter

    or custom-header alternative. Create keys via the admin dashboard (partner detail > API Keys)

    or the partner portal (Settings).


    ## Response Format


    **Success (single):** `{ "data": { ... } }`


    **Success (list):** `{ "data": [...], "pagination": { "total": 100, "limit": 20, "offset": 0, "hasMore": true } }`


    **Error:** `{ "error": { "code": "VALIDATION_ERROR", "message": "...", "details": { ... } } }`


    ## Error Codes


    | Code | HTTP Status | Description |

    |------|-------------|-------------|

    | VALIDATION_ERROR | 400 | Invalid request data |

    | UNAUTHORIZED | 401 | Missing or invalid auth |

    | FORBIDDEN | 403 | Insufficient permissions |

    | NOT_FOUND | 404 | Resource not found |

    | DUPLICATE | 409 | Resource already exists |

    | RATE_LIMITED | 429 | Too many requests |


    ## Rate Limiting


    - **Lead submission** (`POST /api/v1/leads/submit`): rate limited per partner API key

    - **Public endpoints** (`/api/public/*`): 60 requests/minute per IP

    - **Admin API keys** (`Authorization: Bearer` on `/api/admin/*`): 300 requests/minute per key

    - **Admin/Portal endpoints (session)**: no explicit rate limit (session-authenticated)

    - Rate-limited responses return `429` with `{ "error": { "code": "RATE_LIMITED", "message": "..." } }`
  contact:
    name: iSCALE Support
    email: support@iscale.com
  license:
    name: Proprietary
servers:
  - url: https://theleadrouter.com
    description: Production
security:
  - sessionCookie: []
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: lr_session
      description: Session cookie for admin/buyer authentication
    apiKey:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key for programmatic access
    partnerApiKey:
      type: http
      scheme: bearer
      description: "Partner API key (lr_xxx) for lead submission and partner API access. Scopes: `posting` (lead submission
        only), `routing` (full partner portal API access). Create keys via admin dashboard (partner detail > API Keys
        tab) or partner portal (Settings)."
    bearerAuth:
      type: http
      scheme: bearer
      description: Partner API key (lr_xxx) supplied as a Bearer token. Same credential as partnerApiKey.
    adminApiKey:
      type: http
      scheme: bearer
      description: >-
        Admin API key (lr_xxx, scope `admin`) supplied as a Bearer token. Grants the same tenant-scoped access as a UI
        admin session across all `/api/admin/*` endpoints, resolving the creating admin''s live permissions. Excluded:
        impersonation and API-key mint/revoke (session-only). Money-mutating endpoints additionally require the SEPARATE
        `admin_billing` scope — a key with only the `admin` scope cannot perform money mutations. Rate limited to 300
        requests/minute per key. Create via the admin dashboard (Settings > API Keys).


        SCOPE RESOLUTION (documented after the 2026-07-27 security review, finding D — the behaviour is deliberate but
        was previously undocumented, and undocumented auth surface is where leakage hides). Scopes are resolved by
        `hasScope()` in `src/lib/auth/api-key.ts`:

        * `admin` and `admin_billing` are EXPLICIT-ONLY — they are satisfied only by an explicit
          grant on the key, never by the wildcard below.

        * A key with an EMPTY scopes array (`scopes: []` — legacy keys minted before scopes
          existed) acts as a WILDCARD over every non-privileged scope: `routing`, `posting`,
          `messaging`, `calls`, `call_data_write`. Such a key therefore authenticates against
          `/api/v1/*` (which requires `routing`) while still being refused at `/api/admin/*`
          (which requires explicit `admin`). This is the exact combination observed during the
          review — a 200 on `GET /api/v1/leads` alongside 403 scope-walls on `/api/admin/*`.

        * A key holding ONLY `admin` does NOT satisfy `routing`, so it is refused on `/api/v1/*`.
          To use both surfaces a key must carry both scopes explicitly.

        Operationally: prefer minting keys with explicit scopes. An empty-scope key is broader than it looks on the
        `/api/v1` surface.
  parameters:
    limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
      description: Number of results per page
    offset:
      name: offset
      in: query
      schema:
        type: integer
        minimum: 0
        default: 0
      description: Number of results to skip
    search:
      name: search
      in: query
      schema:
        type: string
      description: Free-text search across name/email fields
    dateFrom:
      name: dateFrom
      in: query
      schema:
        type: string
        format: date-time
      description: Filter results created on or after this timestamp
    dateTo:
      name: dateTo
      in: query
      schema:
        type: string
        format: date-time
      description: Filter results created on or before this timestamp
    sortDirection:
      name: sortDirection
      in: query
      schema:
        type: string
        enum:
          - asc
          - desc
        default: desc
      description: Sort direction
    pathId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Resource UUID
    messagingPathId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Resource UUID.
    messagingPageSize:
      name: page[size]
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
      description: JSON:API page size (results per page).
    messagingPageNumber:
      name: page[number]
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1
      description: JSON:API page number (1-based).
  schemas:
    ErrorResponse:
      type: object
      description: |
        Standard error shape returned by every endpoint.

        `READ_ONLY` is returned as a `403` on ANY mutating method (`POST` / `PUT` / `PATCH` / `DELETE`)
        when the request is authenticated with a read-only API key (`apiKey.readOnly = true`).
        `GET` / `HEAD` / `OPTIONS` are unaffected. Session auth is unaffected.
      required:
        - error
      properties:
        error:
          type: object
          required:
            - code
            - message
          properties:
            code:
              type: string
              enum:
                - VALIDATION_ERROR
                - UNAUTHORIZED
                - FORBIDDEN
                - NOT_FOUND
                - DUPLICATE
                - RATE_LIMITED
                - CAP_EXCEEDED
                - INSUFFICIENT_BALANCE
                - FEATURE_DISABLED
                - READ_ONLY
            message:
              type: string
            details:
              type: object
              additionalProperties: true
      example:
        error:
          code: VALIDATION_ERROR
          message: Invalid email format
          details:
            field: email
    PaginationMeta:
      type: object
      required:
        - total
        - limit
        - offset
        - hasMore
      properties:
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
        hasMore:
          type: boolean
    PaginatedResponse:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items: {}
        pagination:
          $ref: "#/components/schemas/PaginationMeta"
    CursorPaginationMeta:
      type: object
      required:
        - total
        - totalIsApproximate
        - hasMore
        - nextCursor
        - prevCursor
      properties:
        total:
          type: integer
          nullable: true
        totalIsApproximate:
          type: boolean
        hasMore:
          type: boolean
        nextCursor:
          type: string
          nullable: true
        prevCursor:
          type: string
          nullable: true
    LeadStatus:
      type: string
      enum:
        - pending
        - matched
        - sold
        - returned
        - rejected
        - scrubbed
        - trashed
        - inactive_campaign
    LeadDispositionType:
      type: string
      enum:
        - pending
        - delivered
        - sold
        - returned
        - scrubbed
        - trashed
    DistributionDisposition:
      type: string
      enum:
        - delivered
        - rejected
        - skipped
        - failed
    DeliveryStatus:
      type: string
      enum:
        - pending
        - delivering
        - delivered
        - failed
    BuyerStatus:
      type: string
      enum:
        - active
        - paused
        - suspended
        - inactive
    PartnerStatus:
      type: string
      enum:
        - active
        - paused
        - inactive
    PaymentCycle:
      type: string
      enum:
        - weekly
        - bimonthly
        - monthly
        - adhoc
    ContractStatus:
      type: string
      enum:
        - active
        - paused
        - expired
    CampaignStatus:
      type: string
      enum:
        - active
        - paused
        - inactive
    TransactionType:
      type: string
      enum:
        - sale
        - refund
        - payment
        - adjustment
        - fee
        - cpa
        - misc_cost
        - referral_commission
        - cpa_reversal
        - misc_cost_reversal
        - dispute
        - dispute_won
        - dispute_lost
    EntityType:
      type: string
      enum:
        - lead
        - call
        - payment
    RefundReason:
      type: string
      enum:
        - duplicate
        - bad_data
        - wrong_vertical
        - out_of_area
        - not_interested
        - other
    RefundStatus:
      type: string
      enum:
        - pending
        - approved
        - rejected
    PayoutStatus:
      type: string
      enum:
        - pending
        - processing
        - paid
        - cancelled
    SuppressionType:
      type: string
      enum:
        - phone
        - email
    SuppressionScope:
      type: string
      enum:
        - global
        - buyer
        - vertical
    SuppressionReason:
      type: string
      enum:
        - dnc_request
        - opt_out
        - bad_data
        - manual
    DataDeletionType:
      type: string
      enum:
        - gdpr
        - ccpa
    DataDeletionStatus:
      type: string
      enum:
        - pending
        - processing
        - completed
        - failed
    WebhookStatus:
      type: string
      enum:
        - active
        - paused
    WebhookDeliveryStatus:
      type: string
      enum:
        - pending
        - success
        - failed
        - retrying
    ScopedWebhookEvent:
      type: string
      description: |
        Events subscribable on partner-, offer-, and campaign-scoped webhooks (matches
        `SCOPED_WEBHOOK_EVENTS`). Global-scope webhooks additionally accept the entity-sync
        events `buyer.created`, `partner.created`, `vertical.created`, `offer.created`.
        The real-time call trigger events (`call.incoming`, `call.dialed`, `call.connected`,
        `call.duration`) mirror the call billing triggers; `call.duration` is gated per
        webhook by `minDurationSec` / `durationAnchor`. `call.billed` fires off the billable
        event — once per billed pricing rule, with `Rule_*` tokens.
      enum:
        - lead.received
        - lead.created
        - lead.sold
        - lead.rejected
        - lead.dq_distributed
        - lead.returned
        - lead.dispositioned
        - lead.converted
        - lead.sale_adjusted
        - call.converted
        - cpa.converted
        - call.incoming
        - call.dialed
        - call.connected
        - call.duration
        - call.billed
        - call.completed
        - call.dispositioned
        - call.sale_adjusted
    WebhookFilterOperator:
      type: string
      enum:
        - equals
        - not_equals
        - contains
        - not_contains
        - greater_than
        - less_than
        - is_empty
        - is_not_empty
    WebhookFilter:
      type: object
      additionalProperties: false
      required:
        - field
        - operator
      properties:
        field:
          type: string
          minLength: 1
          description: Delivery token to evaluate, for example `Intake_Filtered` or `Sale_Price`.
        operator:
          $ref: "#/components/schemas/WebhookFilterOperator"
        value:
          description: Required except for `is_empty` and `is_not_empty`. Amount tokens use decimal amount strings in the tenant
            currency.
          oneOf:
            - type: string
            - type: array
              items:
                type: string
    VerticalStatus:
      type: string
      enum:
        - active
        - inactive
    FieldType:
      type: string
      enum:
        - text
        - number
        - boolean
        - date
        - select
        - email
        - phone
        - url
    UserRole:
      type: string
      enum:
        - admin
        - buyer
        - partner
    UserStatus:
      type: string
      enum:
        - active
        - inactive
    ApiKeyStatus:
      type: string
      enum:
        - active
        - revoked
    PricingModel:
      type: string
      enum:
        - fixed
        - ping_post
    DeliveryMethod:
      type: string
      enum:
        - http_post
        - http_get
        - email
        - ftp
        - google_sheets
    DeliveryFormat:
      type: string
      enum:
        - json
        - xml
        - form
    PingStatus:
      type: string
      enum:
        - active
        - expired
        - converted
    PingMatchStatus:
      type: string
      enum:
        - pending
        - accepted
        - rejected
        - expired
    IdentifierType:
      type: string
      enum:
        - email
        - phone
        - leadId
    CapPeriodType:
      type: string
      enum:
        - daily
        - weekly
        - monthly
        - concurrent
    CapEntityType:
      type: string
      enum:
        - contract
        - campaign
        - buyer
    PersonStatus:
      type: string
      enum:
        - active
        - merged
        - deleted
    DispositionCategory:
      type: string
      enum:
        - working
        - converted
        - rejected
        - returned
    DispositionSource:
      type: string
      enum:
        - system
        - api
        - buyer
    DeliveryOutcome:
      type: string
      enum:
        - success
        - reject
        - no_match
        - duplicate
        - failed_data_validation
        - unknown_response
        - timeout
        - communication_error
    OfferStatus:
      type: string
      enum:
        - active
        - paused
        - inactive
        - setup
    OfferVisibility:
      type: string
      enum:
        - public
        - private
    DistributionType:
      type: string
      enum:
        - exclusive
        - multisell
        - hybrid
    DistributionRule:
      type: string
      enum:
        - price
        - priority
        - weight
        - round_robin
        - scarcity_weighted
        - quality_tier
    CallStatus:
      type: string
      enum:
        - ringing
        - ivr
        - routing
        - holding
        - connected
        - completed
        - missed
        - failed
    CallSaleStatus:
      type: string
      enum:
        - ringing
        - answered
        - declined
        - timeout
        - completed
    CallSaleCpaStatus:
      type: string
      enum:
        - pending
        - converted
        - expired
        - skipped
    TrackingNumberProvider:
      type: string
      enum:
        - twilio
        - telnyx
        - bandwidth
        - signalwire
    TrackingNumberType:
      type: string
      enum:
        - local
        - tollfree
        - mobile
    TrackingNumberStatus:
      type: string
      enum:
        - assigned
        - available
        - released
    IvrGreetingType:
      type: string
      enum:
        - tts
        - audio
    IvrStatus:
      type: string
      enum:
        - active
        - inactive
    EnrollmentStatus:
      type: string
      enum:
        - requested
        - approved
        - rejected
    EnrollmentSource:
      type: string
      enum:
        - partner
        - admin
    OrganizationPlan:
      type: string
      enum:
        - starter
        - growth
        - enterprise
    OrganizationStatus:
      type: string
      enum:
        - active
        - suspended
        - cancelled
    EngagementStatus:
      type: string
      enum:
        - pending
        - in_progress
        - converted
        - disqualified
        - expired
    BillingCycle:
      type: string
      enum:
        - prepay
        - weekly
        - bimonthly
        - monthly
    BuyerCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Acme Insurance Corp
        firstName:
          type: string
          maxLength: 255
          nullable: true
        lastName:
          type: string
          maxLength: 255
          nullable: true
        email:
          type: string
          format: email
          nullable: true
          example: buyer@acme-insurance.com
        phone:
          type: string
          maxLength: 20
          nullable: true
        company:
          type: string
          maxLength: 255
          nullable: true
        address:
          type: string
          maxLength: 500
          nullable: true
        address2:
          type: string
          nullable: true
        city:
          type: string
          maxLength: 100
          nullable: true
        state:
          type: string
          maxLength: 100
          nullable: true
        zipCode:
          type: string
          maxLength: 20
          nullable: true
        country:
          type: string
          maxLength: 100
          nullable: true
        website:
          type: string
          nullable: true
        timezone:
          type: string
          nullable: true
        salesRepId:
          type: string
          format: uuid
          nullable: true
        accountManagerId:
          type: string
          format: uuid
          nullable: true
        billingCycle:
          $ref: "#/components/schemas/BillingCycle"
          default: monthly
        brandName:
          type: string
          nullable: true
        brandLogoUrl:
          type: string
          nullable: true
        brandTcpa:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/BuyerStatus"
          default: active
        pricingModel:
          $ref: "#/components/schemas/PricingModel"
          default: fixed
        defaultPrice:
          type: number
          minimum: 0
          nullable: true
        creditLimit:
          type: string
          nullable: true
        autoRecharge:
          type: boolean
        autoRechargeAmount:
          type: string
          nullable: true
        autoRechargeThreshold:
          type: string
          nullable: true
        dedupEnabled:
          type: boolean
        dedupFields:
          type: array
          items:
            type: string
        dedupWindowDays:
          type: integer
          minimum: 0
        postbackKey:
          type: string
          nullable: true
        acceptsReturns:
          type: boolean
          default: true
        returnWindow:
          type: integer
          minimum: 0
          default: 72
        billingContactName:
          type: string
          maxLength: 255
          nullable: true
        billingContactEmail:
          type: string
          format: email
          nullable: true
        billingContactPhone:
          type: string
          maxLength: 20
          nullable: true
        paymentTerms:
          type: string
          maxLength: 50
          nullable: true
        notes:
          type: string
          nullable: true
        legacyId:
          type: string
          nullable: true
        crmId:
          type: string
          nullable: true
        partnerResponseBuyerName:
          type: string
          maxLength: 255
          nullable: true
          description: Partner-facing buyer name returned on sold lead responses when the campaign enables buyerName.
          example: Howard Snyder
        partnerResponseBuyerPhone:
          type: string
          pattern: ^\+[1-9]\d{1,14}$
          nullable: true
          description: Partner-facing buyer phone in E.164 format returned on sold lead responses when enabled.
          example: "+12131231234"
        partnerResponseBuyerId:
          type: string
          maxLength: 255
          nullable: true
          description: Partner-facing buyer identifier returned on sold lead responses when enabled. Not the internal buyer UUID.
          example: "1972"
        partnerResponseRedirectUrl:
          type: string
          nullable: true
          description: "Redirect URL template returned on sold lead responses when enabled. Allowed tokens: {{leadId}},
            {{partnerId}}."
          example: https://buyer.example/thanks/{{leadId}}?partner={{partnerId}}
    BuyerUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/BuyerCreate"
      x-all-optional: true
    BuyerFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/BuyerStatus"
        pricingModel:
          $ref: "#/components/schemas/PricingModel"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    PartnerCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: LeadGen Partners LLC
        firstName:
          type: string
          maxLength: 255
          nullable: true
        lastName:
          type: string
          maxLength: 255
          nullable: true
        email:
          type: string
          format: email
          nullable: true
          example: ops@leadgen-partners.com
        phone:
          type: string
          maxLength: 20
          nullable: true
        company:
          type: string
          maxLength: 255
          nullable: true
        address:
          type: string
          maxLength: 500
          nullable: true
        address2:
          type: string
          nullable: true
        city:
          type: string
          maxLength: 100
          nullable: true
        state:
          type: string
          maxLength: 100
          nullable: true
        zipCode:
          type: string
          maxLength: 20
          nullable: true
        country:
          type: string
          maxLength: 100
          nullable: true
        website:
          type: string
          nullable: true
        timezone:
          type: string
          nullable: true
        partnerManagerId:
          type: string
          format: uuid
          nullable: true
        status:
          $ref: "#/components/schemas/PartnerStatus"
          default: active
        defaultPrice:
          type: number
          minimum: 0
          nullable: true
        acceptsDuplicates:
          type: boolean
          default: false
        acceptsReturns:
          type: boolean
          default: false
        contractStartDate:
          type: string
          format: date
          nullable: true
        contractEndDate:
          type: string
          format: date
          nullable: true
        billingContactName:
          type: string
          maxLength: 255
          nullable: true
        billingContactEmail:
          type: string
          format: email
          nullable: true
        billingContactPhone:
          type: string
          maxLength: 20
          nullable: true
        paymentTerms:
          type: string
          maxLength: 50
          nullable: true
        notes:
          type: string
          nullable: true
        paymentCycle:
          $ref: "#/components/schemas/PaymentCycle"
          default: monthly
        paymentThreshold:
          type: string
          description: Minimum balance before payout triggers
        legacyId:
          type: string
          nullable: true
        crmId:
          type: string
          nullable: true
    PartnerUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/PartnerCreate"
      x-all-optional: true
    PartnerFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/PartnerStatus"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    GeoTarget:
      type: object
      properties:
        states:
          type: array
          items:
            type: string
            maxLength: 2
          example:
            - CA
            - NY
            - TX
        zipCodes:
          type: array
          items:
            type: string
            maxLength: 10
        cities:
          type: array
          items:
            type: string
            maxLength: 100
        exclude:
          type: boolean
          default: false
    CustomFilter:
      type: object
      required:
        - field
        - operator
        - value
      properties:
        field:
          type: string
          minLength: 1
        operator:
          type: string
          enum:
            - equals
            - not_equals
            - contains
            - in
            - not_in
            - greater_than
            - less_than
        value:
          type: string
    ScheduleDay:
      type: object
      required:
        - day
        - startTime
        - endTime
      properties:
        day:
          type: integer
          minimum: 0
          maximum: 6
        startTime:
          type: string
          pattern: ^\d{2}:\d{2}$
          example: 09:00
        endTime:
          type: string
          pattern: ^\d{2}:\d{2}$
          example: 1020
        active:
          type: boolean
          default: true
    Schedule:
      type: object
      properties:
        timezone:
          type: string
          maxLength: 50
          default: America/New_York
        days:
          type: array
          items:
            type: integer
            minimum: 0
            maximum: 6
        startTime:
          type: string
          pattern: ^\d{2}:\d{2}$
          nullable: true
        endTime:
          type: string
          pattern: ^\d{2}:\d{2}$
          nullable: true
        startDate:
          type: string
          format: date
          nullable: true
        endDate:
          type: string
          format: date
          nullable: true
    CapConfig:
      type: object
      properties:
        daily:
          type: integer
          minimum: 0
          nullable: true
        weekly:
          type: integer
          minimum: 0
          nullable: true
        monthly:
          type: integer
          minimum: 0
          nullable: true
        concurrent:
          type: integer
          minimum: 0
          nullable: true
    ResponseMappingRule:
      type: object
      required:
        - pattern
        - interpretAs
      properties:
        pattern:
          type: string
        definition:
          type: string
          default: ""
        interpretAs:
          type: string
        sortOrder:
          type: integer
          default: 0
    ResponseTokens:
      type: object
      properties:
        leadIdPattern:
          type: string
        pricePattern:
          type: string
        messagePattern:
          type: string
        redirectUrlPattern:
          type: string
        brandsPattern:
          type: string
        dynamicBrands:
          type: boolean
          default: false
    DeliveryAction:
      type: object
      required:
        - id
        - type
      properties:
        id:
          type: string
        type:
          type: string
          enum:
            - http_post
            - http_get
            - email
            - ftp
            - google_sheets
            - sms
        enabled:
          type: boolean
          default: true
        url:
          type: string
        format:
          type: string
          enum:
            - json
            - xml
            - form
        timeout:
          type: integer
          minimum: 1
          maximum: 120
        retries:
          type: integer
          minimum: 0
          maximum: 10
        authType:
          type: string
          enum:
            - none
            - basic
            - bearer
            - api_key
        authCredentials:
          type: object
          properties:
            username:
              type: string
            password:
              type: string
            token:
              type: string
            headerName:
              type: string
        headers:
          type: array
          items:
            type: object
            required:
              - name
              - value
            properties:
              name:
                type: string
              value:
                type: string
        fieldMapping:
          type: array
          items:
            type: object
            required:
              - from
              - to
            properties:
              from:
                type: string
              to:
                type: string
        bodyTemplate:
          type: string
        urlTemplate:
          type: string
        responseMappingRules:
          type: array
          items:
            $ref: "#/components/schemas/ResponseMappingRule"
        responseTokens:
          $ref: "#/components/schemas/ResponseTokens"
        emailTo:
          type: string
        emailCc:
          type: string
        emailSubject:
          type: string
        emailTemplate:
          type: string
        emailBody:
          type: string
          description: Inline email body (interpolated with lead tokens). Read by the delivery-test send path.
        ftpHost:
          type: string
        ftpPath:
          type: string
        ftpUsername:
          type: string
        ftpPassword:
          type: string
        spreadsheetId:
          type: string
        sheetName:
          type: string
        shareWithEmail:
          type: string
        phoneSource:
          type: string
          enum:
            - fixed
            - lead
          default: fixed
          description: |
            SMS recipient selector (only meaningful when `type=sms`):
            - `fixed` (default) — send to `phoneNumber` (e.g. buyer CRM).
            - `lead` — send to the matched lead's own phone (consumer notification).
              `lead.phone` is normalized to E.164 before send. Sends are skipped
              and logged as `PHONE_INVALID` when `lead.phone` is missing or cannot
              be normalized. TCPA consent is enforced upstream at intake — there is
              no per-send TCPA gate in the SMS dispatcher.
        phoneNumber:
          type: string
          pattern: ^\+[1-9]\d{1,14}$
          description: SMS recipient (E.164). Required when `type=sms` AND `phoneSource=fixed`.
        messageBody:
          type: string
          minLength: 1
          maxLength: 1600
          description: SMS body. Required when `type=sms`. Supports `{{token}}` merge fields.
        templateSlug:
          type: string
          description: Optional SMS template slug — used to pre-fill `messageBody` in the UI.
        fromNumberOverride:
          type: string
          pattern: ^\+[1-9]\d{1,14}$
          description: Per-contract sender override (E.164). Falls back to the resolved provider's `fromNumber`.
    ContractCreate:
      type: object
      required:
        - buyerId
        - verticalId
        - name
        - price
      properties:
        ruleType:
          type: string
          default: oneToOne
        contactId:
          type: string
          format: uuid
          nullable: true
        buyerId:
          type: string
          format: uuid
          example: b2c3d4e5-f6a7-8901-b2c3-d4e5f6a78901
        verticalId:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Auto Insurance - California
        priority:
          type: integer
          minimum: 0
          default: 0
        status:
          $ref: "#/components/schemas/ContractStatus"
          default: active
        pricingModel:
          $ref: "#/components/schemas/PricingModel"
          default: fixed
        price:
          type: number
          minimum: 0
          example: 12.5
        exclusiveMultiplier: {}
        allowMultiSale:
          type: boolean
          default: false
        deliveryMethod:
          $ref: "#/components/schemas/DeliveryMethod"
          default: http_post
        deliveryFormat:
          $ref: "#/components/schemas/DeliveryFormat"
          default: json
        deliveryUrl:
          type: string
          format: uri
          nullable: true
        deliveryEmail:
          type: string
          format: email
          nullable: true
        requiresPing:
          type: boolean
          default: false
        pingTimeout:
          type: integer
          minimum: 100
          maximum: 60000
          default: 30000
        acceptsReturns:
          type: boolean
          default: true
        returnWindow:
          type: integer
          minimum: 0
          default: 72
        geo:
          $ref: "#/components/schemas/GeoTarget"
        schedule:
          $ref: "#/components/schemas/Schedule"
        caps:
          $ref: "#/components/schemas/CapConfig"
        filters:
          type: object
          additionalProperties: true
        fieldMapping:
          type: object
          additionalProperties:
            type: string
        geoFilters: {}
        demographicFilters: {}
        customFilters: {}
        scheduleTimezone:
          type: string
        scheduleHours: {}
        expiresAt:
          type: string
          format: date-time
          nullable: true
        deliveryMethodOverride:
          type: string
          nullable: true
        deliveryUrlOverride:
          type: string
          nullable: true
        deliveryConfig:
          type: array
          items:
            $ref: "#/components/schemas/DeliveryAction"
        intakeCenterId:
          type: string
          format: uuid
          nullable: true
        revenueOnAcceptance:
          type: boolean
          default: true
        revenueOnCpa:
          type: boolean
          default: false
        allowZeroCpaConversion:
          type: boolean
          default: false
          description: Allow a $0 CPA to convert as a real conversion (cost-plus deals where the buyer pays the intake center
            direct). Off by default — a $0 CPA is otherwise rejected as a misconfiguration. No balance movement or
            ledger row is written for a $0 conversion.
        cpaAmount: {}
        cpaWindowDays:
          type: integer
          minimum: 1
          nullable: true
        pricePerLead: {}
        dailyLeadCap: {}
        weeklyLeadCap: {}
        monthlyLeadCap: {}
        dailySpendCap: {}
        weeklySpendCap: {}
        monthlySpendCap: {}
        legacyId:
          type: string
          nullable: true
        crmId:
          type: string
          nullable: true
        inboundWebhookKey:
          type: string
          nullable: true
        callEnabled:
          type: boolean
          default: false
        callPhoneNumber:
          type: string
          nullable: true
        callBackupNumber:
          type: string
          nullable: true
        premiumDestinationAck:
          type: boolean
          description: Premium destination guardrail. Must be true when a CHANGED transfer number (callPhoneNumber /
            callBackupNumber) classifies as a premium-rate (high-cost NPA, e.g. 605) destination — otherwise the write
            fails 400 with error code PREMIUM_ACK_REQUIRED. Acknowledging stamps the per-minute surcharge on the
            contract. Not persisted (read by the guard, then stripped). Non-US transfer numbers are rejected with 400
            VALIDATION_ERROR regardless of ack.
        premiumDestinationSurchargeCreditsPerMin:
          type: string
          nullable: true
          readOnly: true
          description: Per-minute surcharge (credits, decimal string) billed on this contract's transferred minutes while a saved
            transfer number classifies premium; null when none. Stamped server-side on ack (default 3.00), cleared
            automatically when the number returns to a normal destination, superadmin-overridable per contract.
        callRingTimeout:
          type: integer
          minimum: 1
          nullable: true
        callWhisperMessage:
          type: string
          nullable: true
        callWhisperRequired:
          type: boolean
          default: false
        callCapMode:
          type: string
          enum:
            - separate
            - shared
          default: separate
        dailyCallCap:
          type: integer
          minimum: 0
          nullable: true
        weeklyCallCap:
          type: integer
          minimum: 0
          nullable: true
        monthlyCallCap:
          type: integer
          minimum: 0
          nullable: true
        aiDialerAgentId:
          type: string
          format: uuid
          nullable: true
          description: ElevenLabs agent that places outbound calls for this contract's leads. Cross-tenant references rejected.
        aiDialerCadenceId:
          type: string
          format: uuid
          nullable: true
          description: Cadence governing dial timing. cadenceType `immediate` dials at sale; `call_assist` dials N days post-sale
            via the `/api/cron/ai-dialer-call-assist` cron. Cross-tenant references rejected.
        aiDialerDelaySeconds:
          type: integer
          minimum: 0
          maximum: 86400
          default: 30
          description: For Immediate cadences, seconds between sale completion and the first dial. Ignored for Call Assist
            cadences (cron-driven).
    ContractUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/ContractCreate"
      x-all-optional: true
    PremiumDestination:
      type: object
      description: May be present on contract create/update responses when the effective transfer number(s) classify as a
        premium-rate destination (premium destination guardrail). Classification is best-effort — a static high-cost NPA
        list plus optional rate-deck / carrier pricing lookups — so absence does not guarantee a low-cost destination,
        and observed-cost sweeps remain the billing corrector.
      properties:
        risk:
          type: string
          enum:
            - premium
        surchargeCreditsPerMin:
          type: string
          nullable: true
          description: Per-minute surcharge (credits, decimal string) stamped on the contract.
    ContractFilter:
      type: object
      properties:
        buyerId:
          type: string
          format: uuid
        verticalId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/ContractStatus"
        pricingModel:
          $ref: "#/components/schemas/PricingModel"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    MiscCostPerEvent:
      type: string
      description: When this misc cost is charged. `cpa` charges on CPA postback; `sold_lead` charges on every sold lead;
        `disposition` charges when the specific mapped disposition named by `triggerDisposition` arrives — used by
        Intake Only campaigns, where intake cost books at transfer and revenue follows later at signing.
      enum:
        - cpa
        - sold_lead
        - disposition
    ContractMiscCost:
      type: object
      description: A configured miscellaneous cost on a contract (e.g. transfer fee, processing fee). Generates a
        `miscCostCharge` row each time the trigger event fires.
      required:
        - id
        - contractId
        - name
        - amount
        - perEvent
        - billClient
        - isActive
        - sortOrder
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          format: uuid
        contractId:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
          example: Transfer Fee
        amount:
          type: string
          description: Decimal amount as string (precision 10, scale 2)
          example: "5.00"
        perEvent:
          $ref: "#/components/schemas/MiscCostPerEvent"
        billClient:
          type: boolean
          description: When true, the cost is billed to the buyer in addition to internal accounting.
          default: false
        isActive:
          type: boolean
          default: true
        sortOrder:
          type: integer
          minimum: 0
          default: 0
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ContractMiscCostCreate:
      type: object
      required:
        - name
        - amount
      properties:
        name:
          type: string
          minLength: 1
          example: Transfer Fee
        amount:
          type: number
          minimum: 0
          example: 5
        perEvent:
          $ref: "#/components/schemas/MiscCostPerEvent"
        billClient:
          type: boolean
          default: false
        isActive:
          type: boolean
          default: true
        sortOrder:
          type: integer
          minimum: 0
          default: 0
    ContractMiscCostUpdate:
      type: object
      description: Partial update. Include `id` of the misc-cost row to update at the top level.
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
        amount:
          type: number
          minimum: 0
        perEvent:
          $ref: "#/components/schemas/MiscCostPerEvent"
        billClient:
          type: boolean
        isActive:
          type: boolean
        sortOrder:
          type: integer
          minimum: 0
    MiscCostCharge:
      type: object
      description: A recorded misc-cost charge against a specific lead/leadSale. Created automatically by the engine when a
        misc cost's trigger event fires.
      required:
        - id
        - miscCostId
        - contractId
        - leadId
        - buyerId
        - miscCostName
        - amount
        - perEvent
        - billClient
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        miscCostId:
          type: string
          format: uuid
        contractId:
          type: string
          format: uuid
        leadSaleId:
          type: string
          format: uuid
          nullable: true
        leadId:
          type: string
          format: uuid
        buyerId:
          type: string
          format: uuid
        miscCostName:
          type: string
          description: Snapshot of the misc cost name at charge time
        amount:
          type: string
          description: Decimal amount as string (precision 10, scale 2)
        perEvent:
          $ref: "#/components/schemas/MiscCostPerEvent"
        billClient:
          type: boolean
        transactionId:
          type: string
          format: uuid
          nullable: true
        reversedAt:
          type: string
          format: date-time
          nullable: true
          description: Set when the charge has been reversed (e.g. CPA reversal)
        reversalTransactionId:
          type: string
          format: uuid
          nullable: true
        tenantId:
          type: string
          format: uuid
          nullable: true
        createdAt:
          type: string
          format: date-time
    CampaignCreate:
      type: object
      required:
        - partnerId
        - name
        - offerId
      properties:
        partnerId:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Solar Leads - Facebook Q1
        verticalId:
          type: string
          format: uuid
          nullable: true
        offerId:
          type: string
          format: uuid
        pricingModel:
          type: string
          enum:
            - fixed
            - revshare
          default: fixed
        costPerLead:
          type: string
          default: "0"
        cpaCost:
          type: string
          default: "0"
        cpaPricingModel:
          type: string
          enum:
            - fixed
            - revshare
          default: fixed
        dailyCap:
          type: integer
          minimum: 1
          nullable: true
        weeklyCap:
          type: integer
          minimum: 1
          nullable: true
        monthlyCap:
          type: integer
          minimum: 1
          nullable: true
        legacyId:
          type: string
          nullable: true
        crmId:
          type: string
          nullable: true
        callEnabled:
          type: boolean
          default: false
        status:
          $ref: "#/components/schemas/CampaignStatus"
          default: active
        sendBuyerNameInPartnerResponse:
          type: boolean
          default: false
          description: Include buyerName in sold partner lead responses.
        sendBuyerPhoneInPartnerResponse:
          type: boolean
          default: false
          description: Include buyerPhone in sold partner lead responses.
        sendBuyerIdInPartnerResponse:
          type: boolean
          default: false
          description: Include the partner-facing buyerId in sold partner lead responses.
        sendRedirectUrlInPartnerResponse:
          type: boolean
          default: false
          description: Include redirectUrl in sold partner lead responses.
        sendBuyerMediaInPartnerResponse:
          type: boolean
          default: false
          description: Include buyerMediaUrl and buyerMediaType in sold partner lead responses.
    CampaignUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/CampaignCreate"
      x-all-optional: true
    CampaignFilter:
      type: object
      properties:
        partnerId:
          type: string
          format: uuid
        verticalId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/CampaignStatus"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    CampaignFieldCreate:
      type: object
      required:
        - name
        - label
        - type
      properties:
        name:
          type: string
          minLength: 1
          pattern: ^[a-z][a-zA-Z0-9]*$
          example: homeownerStatus
        label:
          type: string
          minLength: 1
          example: Homeowner Status
        type:
          $ref: "#/components/schemas/FieldType"
        required:
          type: boolean
          default: false
        options:
          type: array
          items:
            type: string
          nullable: true
        validation:
          type: object
          additionalProperties: true
          nullable: true
        sortOrder:
          type: integer
          default: 0
    CampaignFieldUpdate:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          pattern: ^[a-z][a-zA-Z0-9]*$
        label:
          type: string
          minLength: 1
        type:
          $ref: "#/components/schemas/FieldType"
        required:
          type: boolean
        options:
          type: array
          items:
            type: string
          nullable: true
        validation:
          type: object
          additionalProperties: true
          nullable: true
        sortOrder:
          type: integer
    OfferCreate:
      type: object
      required:
        - name
        - verticalId
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Auto Insurance - National
        description:
          type: string
          nullable: true
        visibility:
          $ref: "#/components/schemas/OfferVisibility"
          default: private
        verticalId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/OfferStatus"
          default: active
        distributionType:
          $ref: "#/components/schemas/DistributionType"
          default: exclusive
        distributionRule:
          $ref: "#/components/schemas/DistributionRule"
          default: priority
        maxSales:
          type: integer
          minimum: 1
          nullable: true
        defaultPayoutModel:
          type: string
          nullable: true
        defaultPayoutAmount:
          type: number
          minimum: 0
          nullable: true
        intakeCenterId:
          type: string
          format: uuid
          nullable: true
        legacyId:
          type: string
          nullable: true
        crmId:
          type: string
          nullable: true
        callEnabled:
          type: boolean
          default: false
        cpaPayoutModel:
          type: string
          enum:
            - fixed
            - revshare
          nullable: true
        cpaPayoutAmount:
          type: number
          minimum: 0
          nullable: true
        callCallerAffinityMode:
          type: string
          enum:
            - false
            - same
            - exclude
          default: same
        callLeadAffinityMode:
          type: string
          enum:
            - false
            - same
            - exclude
          default: false
        callAffinityWindowDays:
          type: integer
          minimum: 1
          default: 30
        callDistributionType:
          type: string
          enum:
            - sequential
            - non_sequential
          default: sequential
        callDistributionRule:
          type: string
          enum:
            - price
            - priority
            - weight
            - round_robin
            - simultaneous
          default: priority
        callAffinityFallback:
          type: string
          enum:
            - none
            - next_match
          default: none
        requireCallContracts:
          type: boolean
          default: false
        balanceCheckEnabled:
          type: boolean
          nullable: true
        billDqLeads:
          type: boolean
          default: true
          description: When false, DQ delivery still runs but DQ sale price/cost, buyer billing, partner payout, and billable misc
            costs stay zero.
        sendBuyerNameInPartnerResponse:
          type: boolean
          default: false
          description: Default campaign setting for including buyerName in sold partner lead responses.
        sendBuyerPhoneInPartnerResponse:
          type: boolean
          default: false
          description: Default campaign setting for including buyerPhone in sold partner lead responses.
        sendBuyerIdInPartnerResponse:
          type: boolean
          default: false
          description: Default campaign setting for including the partner-facing buyerId in sold partner lead responses.
        sendRedirectUrlInPartnerResponse:
          type: boolean
          default: false
          description: Default campaign setting for including redirectUrl in sold partner lead responses.
        sendBuyerMediaInPartnerResponse:
          type: boolean
          default: false
          description: Default campaign setting for including buyerMediaUrl and buyerMediaType in sold partner lead responses.
        messagingEnabled:
          type: boolean
          default: false
          description: Opt this offer into populating the messaging contact database from its leads and calls. Off by default;
            setting it true requires the tenant messaging feature to be enabled.
        fieldSettings:
          type: object
          additionalProperties:
            type: object
            properties:
              hidden:
                type: boolean
              required:
                type: boolean
          nullable: true
        attributionPolicy:
          type: object
          additionalProperties: true
          nullable: true
    OfferUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/OfferCreate"
      x-all-optional: true
    OfferFilter:
      type: object
      properties:
        verticalId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/OfferStatus"
        visibility:
          $ref: "#/components/schemas/OfferVisibility"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    CallBillingRule:
      type: object
      description: Polymorphic call billing rule for multi-tier pricing (Ringba 'Add Campaign Payout Setting' / Retreaver
        'Tag-based conversion criteria')
      properties:
        id:
          type: string
          format: uuid
        parentType:
          type: string
          enum:
            - offer
            - campaign
            - contract
        parentId:
          type: string
          format: uuid
        priority:
          type: integer
        name:
          type: string
        tagMatch:
          type: object
          nullable: true
          additionalProperties:
            type: string
        dispositionMatch:
          type: array
          nullable: true
          items:
            type: string
        callPayoutType:
          type: string
          enum:
            - fixed
            - revshare
            - raw_call
        callPayoutAmount:
          type: string
          format: decimal
          nullable: true
        callRevsharePercent:
          type: string
          format: decimal
          nullable: true
        callMaxPayoutPerCall:
          type: string
          format: decimal
          nullable: true
        callConversionType:
          type: string
          enum:
            - length
            - connected
            - dialed
            - postback
            - contract_billed
        callMinDurationSec:
          type: integer
          nullable: true
          description: Minimum seconds on the selected duration anchor when callConversionType=length. With Connect by default,
            this is connected talk duration.
        callDurationAnchor:
          type: string
          enum:
            - incoming
            - dial
            - connect
          description: Default `connect` starts when the target answers and excludes ringing. `dial` starts when the platform
            dials the target. `incoming` starts when the caller enters the system.
        callDuplicatePayouts:
          type: string
          enum:
            - allow
            - block
            - window
        callDuplicateWindowSec:
          type: integer
          nullable: true
        tenantId:
          type: string
          format: uuid
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    LeadCreate:
      type: object
      required:
        - verticalId
        - firstName
        - lastName
        - email
        - phone
        - tcpaConsent
      properties:
        verticalId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        offerId:
          type: string
          format: uuid
        firstName:
          type: string
          minLength: 1
          maxLength: 100
          example: Jane
        lastName:
          type: string
          minLength: 1
          maxLength: 100
          example: Doe
        email:
          type: string
          format: email
          example: jane.doe@example.com
        phone:
          type: string
          minLength: 10
          maxLength: 20
          example: "+15551234567"
        dateOfBirth:
          type: string
          nullable: true
        address:
          type: string
          nullable: true
        address2:
          type: string
          nullable: true
        city:
          type: string
          nullable: true
        state:
          type: string
          maxLength: 2
          nullable: true
          example: CA
        zipCode:
          type: string
          maxLength: 10
          nullable: true
          example: "90210"
        country:
          type: string
          default: US
        tcpaConsent:
          type: boolean
          example: true
        consentTimestamp:
          type: string
          format: date-time
        consentIp:
          type: string
        consentText:
          type: string
        custom:
          type: object
          additionalProperties: true
        sub1:
          type: string
          nullable: true
        sub2:
          type: string
          nullable: true
        sub3:
          type: string
          nullable: true
        sub4:
          type: string
          nullable: true
        sub5:
          type: string
          nullable: true
        sub6:
          type: string
          nullable: true
        sub7:
          type: string
          nullable: true
        sub8:
          type: string
          nullable: true
        sub9:
          type: string
          nullable: true
        sub10:
          type: string
          nullable: true
        requestId:
          type: string
          nullable: true
        isTest:
          type: boolean
        trustedFormCertUrl:
          type: string
          format: uri
          nullable: true
        jornayaLeadId:
          type: string
          nullable: true
          description: Jornaya LeadiD token (36-char UUID). When the tenant's Jornaya integration is enabled with auto-audit,
            submitting one triggers a billed LeadiD audit query.
        consentCertUrl:
          type: string
          format: uri
          nullable: true
    LeadUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/LeadCreate"
      x-all-optional: true
    LeadFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/LeadStatus"
        verticalId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        buyerId:
          type: string
          format: uuid
        dateFrom:
          type: string
          format: date-time
        dateTo:
          type: string
          format: date-time
        search:
          type: string
        isTrashed:
          type: boolean
        isTest:
          type: boolean
        isPaid:
          type: boolean
        campaignId:
          type: string
          format: uuid
        offerId:
          type: string
          format: uuid
        contractId:
          type: string
          format: uuid
        isSold:
          type: boolean
        isScrubbed:
          type: boolean
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    CherryPick:
      type: object
      required:
        - contractId
      properties:
        contractId:
          type: string
          format: uuid
        price:
          type: number
          minimum: 0
        skipFilters:
          type: boolean
        skipCaps:
          type: boolean
        skipDedup:
          type: boolean
        skipBalanceCheck:
          type: boolean
        adjustPayout:
          type: boolean
    AdjustPayout:
      type: object
      required:
        - newPrice
      properties:
        saleId:
          type: string
          format: uuid
        newPrice:
          type: number
          minimum: 0
        reason:
          type: string
    Distribute:
      type: object
      required:
        - mode
      properties:
        mode:
          type: string
          enum:
            - all
            - exclude_seen
            - include_specific
            - exclude_specific
        contractIds:
          type: array
          items:
            type: string
            format: uuid
    BulkAction:
      type: object
      required:
        - leadIds
        - action
      properties:
        leadIds:
          type: array
          items:
            type: string
            format: uuid
          minItems: 1
        action:
          type: string
          enum:
            - scrub
            - trash
            - toggle_test
            - toggle_paid
    BulkCherryPick:
      type: object
      required:
        - leadIds
        - contractId
      properties:
        leadIds:
          type: array
          items:
            type: string
            format: uuid
          minItems: 1
        contractId:
          type: string
          format: uuid
        price:
          type: number
          minimum: 0
        skipFilters:
          type: boolean
        skipCaps:
          type: boolean
        skipDedup:
          type: boolean
    BulkAdjustPayout:
      type: object
      required:
        - leadIds
        - newPrice
      properties:
        leadIds:
          type: array
          items:
            type: string
            format: uuid
          minItems: 1
        newPrice:
          type: number
          minimum: 0
        reason:
          type: string
    PersonCreate:
      type: object
      properties:
        firstName:
          type: string
          maxLength: 255
          nullable: true
        lastName:
          type: string
          maxLength: 255
          nullable: true
        email:
          type: string
          format: email
          nullable: true
        phone:
          type: string
          maxLength: 30
          nullable: true
        dateOfBirth:
          type: string
          nullable: true
        address:
          type: string
          nullable: true
        address2:
          type: string
          nullable: true
        city:
          type: string
          nullable: true
        state:
          type: string
          maxLength: 2
          nullable: true
        zipCode:
          type: string
          maxLength: 10
          nullable: true
        country:
          type: string
          nullable: true
        tags:
          type: array
          items:
            type: string
          default: []
        custom:
          type: object
          additionalProperties: true
          default: {}
    PersonUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/PersonCreate"
      properties:
        status:
          $ref: "#/components/schemas/PersonStatus"
      x-all-optional: true
    PersonFilter:
      type: object
      properties:
        search:
          type: string
        state:
          type: string
        status:
          $ref: "#/components/schemas/PersonStatus"
        tags:
          type: string
        dateFrom:
          type: string
        dateTo:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    PersonNoteCreate:
      type: object
      required:
        - content
      properties:
        content:
          type: string
          minLength: 1
          maxLength: 10000
    PersonMerge:
      type: object
      required:
        - mergePersonId
      properties:
        mergePersonId:
          type: string
          format: uuid
        reason:
          type: string
    IvrMenuOption:
      type: object
      required:
        - digit
        - label
        - action
      properties:
        digit:
          type: string
          minLength: 1
          maxLength: 1
        label:
          type: string
          minLength: 1
        action:
          type: string
          minLength: 1
        actionTarget:
          type: string
    IvrConfigCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          example: Solar Leads IVR
        greeting:
          type: string
          nullable: true
        greetingType:
          $ref: "#/components/schemas/IvrGreetingType"
          default: tts
        menuOptions:
          type: array
          items:
            $ref: "#/components/schemas/IvrMenuOption"
          default: []
        timeoutSeconds:
          type: integer
          minimum: 1
          default: 5
        maxRetries:
          type: integer
          minimum: 0
          default: 3
        invalidInputMessage:
          type: string
          nullable: true
        timeoutMessage:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/IvrStatus"
          default: active
    IvrConfigUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/IvrConfigCreate"
      x-all-optional: true
    TrackingNumberCreate:
      type: object
      required:
        - provider
        - phoneNumber
      properties:
        provider:
          $ref: "#/components/schemas/TrackingNumberProvider"
        phoneNumber:
          type: string
          minLength: 1
          example: "+18005551234"
        campaignId:
          type: string
          format: uuid
          nullable: true
        offerId:
          type: string
          format: uuid
          nullable: true
        label:
          type: string
          nullable: true
        numberType:
          $ref: "#/components/schemas/TrackingNumberType"
          default: local
        ivrConfigId:
          type: string
          format: uuid
          nullable: true
        whisperEnabled:
          type: boolean
          default: false
        whisperDefault:
          type: string
          nullable: true
        recordCalls:
          type: boolean
          default: true
        renewalDate:
          type: string
          nullable: true
        purchasedAt:
          type: string
          format: date-time
          nullable: true
        poolManaged:
          type: boolean
          default: true
        status:
          $ref: "#/components/schemas/TrackingNumberStatus"
          default: assigned
    TrackingNumberUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/TrackingNumberCreate"
      x-all-optional: true
    NumberSearch:
      type: object
      required:
        - provider
      properties:
        provider:
          $ref: "#/components/schemas/TrackingNumberProvider"
        areaCode:
          type: string
          maxLength: 10
        country:
          type: string
          maxLength: 2
          default: US
        numberType:
          $ref: "#/components/schemas/TrackingNumberType"
          default: local
        limit:
          type: integer
          minimum: 1
          maximum: 50
          default: 10
    CallFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/CallStatus"
        campaignId:
          type: string
          format: uuid
        offerId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        callerNumber:
          type: string
        dateFrom:
          type: string
          format: date-time
        dateTo:
          type: string
          format: date-time
        buyerId:
          type: string
          format: uuid
        trackingNumberId:
          type: string
          format: uuid
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    CallConversion:
      type: object
      properties:
        callSaleId:
          type: string
          format: uuid
        amount:
          type: number
          minimum: 0
    CallDisposition:
      type: object
      required:
        - callSaleId
        - disposition
      properties:
        callSaleId:
          type: string
          format: uuid
        disposition:
          type: string
          minLength: 1
        dispositionReason:
          type: string
          nullable: true
    TransactionFilter:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/TransactionType"
        entityType:
          $ref: "#/components/schemas/EntityType"
        entityId:
          type: string
          format: uuid
        buyerId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        dateFrom:
          type: string
          format: date-time
        dateTo:
          type: string
          format: date-time
        minAmount:
          type: number
        maxAmount:
          type: number
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    RefundCreate:
      type: object
      required:
        - leadId
        - buyerId
        - saleId
        - amount
        - reason
      properties:
        leadId:
          type: string
          format: uuid
        buyerId:
          type: string
          format: uuid
        saleId:
          type: string
          format: uuid
        amount:
          type: string
          example: "12.50"
        reason:
          $ref: "#/components/schemas/RefundReason"
        notes:
          type: string
          nullable: true
    RefundProcess:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum:
            - approved
            - rejected
        adminNotes:
          type: string
          nullable: true
          maxLength: 2000
    RefundFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/RefundStatus"
        reason:
          $ref: "#/components/schemas/RefundReason"
        buyerId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        dateFrom:
          type: string
          format: date-time
        dateTo:
          type: string
          format: date-time
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    Login:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          example: admin@iscale.com
        password:
          type: string
          minLength: 8
      example:
        email: admin@iscale.com
        password: secureP@ss1
    Register:
      type: object
      required:
        - email
        - password
        - name
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 255
        role:
          $ref: "#/components/schemas/UserRole"
          default: buyer
    UserCreate:
      type: object
      required:
        - email
        - password
        - name
        - role
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 255
        role:
          $ref: "#/components/schemas/UserRole"
        status:
          $ref: "#/components/schemas/UserStatus"
          default: active
        buyerId:
          type: string
          format: uuid
          nullable: true
    UserUpdate:
      type: object
      properties:
        email:
          type: string
          format: email
        name:
          type: string
          minLength: 1
          maxLength: 255
        status:
          $ref: "#/components/schemas/UserStatus"
        buyerId:
          type: string
          format: uuid
          nullable: true
        isPartnerManager:
          type: boolean
        isAccountManager:
          type: boolean
        isSalesRep:
          type: boolean
        legacyId:
          type: integer
          nullable: true
        crmId:
          type: string
          nullable: true
    PasswordChange:
      type: object
      required:
        - currentPassword
        - newPassword
      properties:
        currentPassword:
          type: string
        newPassword:
          type: string
          minLength: 8
          maxLength: 100
    ApiKeyCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Production Integration Key
        buyerId:
          type: string
          format: uuid
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
        readOnly:
          type: boolean
          default: false
          description: |
            When true, this key blocks all non-GET requests with `403 READ_ONLY`.
            GET / HEAD / OPTIONS remain allowed.
        piiAccess:
          type: boolean
          default: false
          description: |
            When true, this key returns unmasked PII (names, emails, phones, addresses,
            dates of birth, custom PII-flagged fields). Default keys return masked values.

            Creator-privilege rule: the caller minting the key must have `piiAccess` on
            their own account (or be superadmin). Otherwise the request is rejected with
            `403 FORBIDDEN`.
    ApiKeyFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/ApiKeyStatus"
        buyerId:
          type: string
          format: uuid
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    ContactUpsert:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
        firstName:
          type: string
          maxLength: 255
          nullable: true
        lastName:
          type: string
          maxLength: 255
          nullable: true
        phone:
          type: string
          maxLength: 30
          nullable: true
        dateOfBirth:
          type: string
          nullable: true
        address:
          type: string
          nullable: true
        city:
          type: string
          nullable: true
        state:
          type: string
          maxLength: 2
          nullable: true
        zipCode:
          type: string
          maxLength: 10
          nullable: true
        country:
          type: string
          nullable: true
        properties:
          type: object
          additionalProperties: true
          default: {}
        tags:
          type: array
          items:
            type: string
          default: []
    InboundDisposition:
      type: object
      required:
        - saleId
        - disposition
      properties:
        saleId:
          type: string
          format: uuid
        disposition:
          type: string
          maxLength: 50
        dispoReason:
          type: string
          maxLength: 100
        intakeDispo:
          type: string
          maxLength: 100
        comsCount:
          type: integer
          minimum: 0
        intakeId:
          type: string
          maxLength: 255
          description: "Deprecated: use buyerLeadId instead. Kept for backwards compatibility."
        buyerLeadId:
          type: string
          maxLength: 255
          description: Preferred field for buyer-assigned lead ID. Replaces intakeId.
        notes:
          type: string
          maxLength: 2000
        agentName:
          type: string
          maxLength: 100
        callDuration:
          type: integer
          minimum: 0
        callbackDate:
          type: string
          format: date-time
        externalId:
          type: string
          maxLength: 255
    DispositionCodeCreate:
      type: object
      required:
        - code
        - label
        - category
        - triggersCpa
        - triggersReturn
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 50
          example: SOLD
        label:
          type: string
          minLength: 1
          maxLength: 100
          example: Sale Completed
        category:
          $ref: "#/components/schemas/DispositionCategory"
        triggersCpa:
          type: boolean
        triggersReturn:
          type: boolean
        returnReason:
          $ref: "#/components/schemas/RefundReason"
          nullable: true
        isActive:
          type: boolean
        sortOrder:
          type: integer
    DispositionCodeUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/DispositionCodeCreate"
      x-all-optional: true
    IntakeCenterCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Primary Intake
        status:
          type: string
          enum:
            - active
            - inactive
          default: active
        deliveryMethod:
          type: string
          enum:
            - http_post
            - http_get
            - email
          nullable: true
        deliveryUrl:
          type: string
          nullable: true
        deliveryConfig: {}
        notes:
          type: string
          maxLength: 2000
          nullable: true
        statusMappings:
          type: object
          additionalProperties:
            type: string
        conversionStatuses:
          type: array
          items:
            type: string
        rejectionStatuses:
          type: array
          items:
            type: string
        programMappings:
          type: object
          additionalProperties:
            type: string
    IntakeCenterUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/IntakeCenterCreate"
      x-all-optional: true
    IntakeCenterFilter:
      type: object
      properties:
        search:
          type: string
        status:
          type: string
          enum:
            - active
            - inactive
        sort:
          type: string
          enum:
            - name
            - status
            - deliveryMethod
            - createdAt
          default: createdAt
        sortDirection:
          type: string
          enum:
            - asc
            - desc
          default: desc
        limit:
          type: integer
          minimum: 1
          maximum: 1000
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    IntakeCenterDispositionCreate:
      type: object
      required:
        - intakeCenterId
        - verticalGroupId
        - externalCode
        - verticalGroupDispositionId
      properties:
        intakeCenterId:
          type: string
          format: uuid
        verticalGroupId:
          type: string
          format: uuid
        externalCode:
          type: string
          minLength: 1
          maxLength: 100
        externalLabel:
          type: string
          maxLength: 200
          nullable: true
        verticalGroupDispositionId:
          type: string
          format: uuid
          nullable: true
        overrideTriggersCpa:
          type: boolean
          nullable: true
        overrideTriggersReturn:
          type: boolean
          nullable: true
        overrideReturnReason:
          $ref: "#/components/schemas/RefundReason"
          nullable: true
        isActive:
          type: boolean
          default: true
        sortOrder:
          type: integer
          default: 0
    IntakeCenterDispositionUpdate:
      type: object
      properties:
        externalCode:
          type: string
          minLength: 1
          maxLength: 100
        externalLabel:
          type: string
          maxLength: 200
          nullable: true
        verticalGroupDispositionId:
          type: string
          format: uuid
          nullable: true
        overrideTriggersCpa:
          type: boolean
          nullable: true
        overrideTriggersReturn:
          type: boolean
          nullable: true
        overrideReturnReason:
          $ref: "#/components/schemas/RefundReason"
          nullable: true
        isActive:
          type: boolean
        sortOrder:
          type: integer
    ContractDispositionCreate:
      type: object
      required:
        - contractId
        - externalCode
        - verticalGroupDispositionId
      properties:
        contractId:
          type: string
          format: uuid
        externalCode:
          type: string
          minLength: 1
          maxLength: 100
        externalLabel:
          type: string
          maxLength: 200
          nullable: true
        verticalGroupDispositionId:
          type: string
          format: uuid
          nullable: true
        overrideTriggersCpa:
          type: boolean
          nullable: true
        overrideTriggersReturn:
          type: boolean
          nullable: true
        overrideReturnReason:
          $ref: "#/components/schemas/RefundReason"
          nullable: true
        isActive:
          type: boolean
          default: true
        sortOrder:
          type: integer
          default: 0
    ContractDispositionUpdate:
      type: object
      properties:
        externalCode:
          type: string
          minLength: 1
          maxLength: 100
        externalLabel:
          type: string
          maxLength: 200
          nullable: true
        verticalGroupDispositionId:
          type: string
          format: uuid
          nullable: true
        overrideTriggersCpa:
          type: boolean
          nullable: true
        overrideTriggersReturn:
          type: boolean
          nullable: true
        overrideReturnReason:
          $ref: "#/components/schemas/RefundReason"
          nullable: true
        isActive:
          type: boolean
        sortOrder:
          type: integer
    CloneDispositions:
      type: object
      required:
        - fromVerticalGroupId
        - toVerticalGroupId
      properties:
        fromVerticalGroupId:
          type: string
          format: uuid
        toVerticalGroupId:
          type: string
          format: uuid
    SessionCreateResponse:
      type: object
      required:
        - sessionKey
        - sessionId
        - expiresAt
      properties:
        sessionKey:
          type: string
        sessionId:
          type: string
          format: uuid
        expiresAt:
          type: string
    SessionPatch:
      type: object
      properties:
        data:
          type: object
          additionalProperties: true
        currentStep:
          type: integer
          minimum: 0
        status:
          type: string
          enum:
            - completed
    SessionGetResponse:
      type: object
      required:
        - sessionId
        - campaignId
        - data
        - currentStep
        - totalSteps
        - status
        - primaryLeadId
        - coregCount
        - expiresAt
        - createdAt
        - updatedAt
      properties:
        sessionId:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        data:
          type: object
          additionalProperties: true
        currentStep:
          type: integer
        totalSteps:
          type: integer
          nullable: true
        status:
          type: string
          enum:
            - active
            - submitted
            - completed
            - expired
        primaryLeadId:
          type: string
          format: uuid
          nullable: true
        coregCount:
          type: integer
        expiresAt:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
    CoregRequest:
      type: object
      properties:
        data:
          type: object
          additionalProperties: true
    VerticalGroupCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Auto Insurance Group
        dedupScope:
          type: string
          enum:
            - offer
            - vertical
            - group
          default: offer
        attributionScope:
          type: string
          enum:
            - offer
            - vertical
            - group
          default: offer
        attributionPolicy:
          type: object
          additionalProperties: true
          nullable: true
    VerticalGroupUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/VerticalGroupCreate"
      x-all-optional: true
    VerticalGroupFilter:
      type: object
      properties:
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 1000
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
        sort:
          type: string
          enum:
            - name
            - createdAt
          default: createdAt
        sortDirection:
          type: string
          enum:
            - asc
            - desc
          default: desc
    VerticalGroupDispositionCreate:
      type: object
      required:
        - label
        - category
      properties:
        label:
          type: string
          minLength: 1
          example: Qualified Lead
        category:
          $ref: "#/components/schemas/DispositionCategory"
        triggersCpa:
          type: boolean
          default: false
        triggersReturn:
          type: boolean
          default: false
        returnReason:
          type: string
          nullable: true
        sortOrder:
          type: integer
          default: 0
        isActive:
          type: boolean
          default: true
        slug:
          type: string
    VerticalGroupDispositionUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/VerticalGroupDispositionCreate"
      x-all-optional: true
    EngagementFilter:
      type: object
      properties:
        partnerId:
          type: string
          format: uuid
        buyerId:
          type: string
          format: uuid
        verticalId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/EngagementStatus"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 1000
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
        sort:
          type: string
          enum:
            - originatedAt
            - latestEventAt
            - cpaStatus
          default: latestEventAt
        sortDirection:
          type: string
          enum:
            - asc
            - desc
          default: desc
    HttpHeader:
      type: object
      required:
        - name
        - value
      properties:
        name:
          type: string
        value:
          type: string
    HttpFieldMapping:
      type: object
      required:
        - from
        - to
      properties:
        from:
          type: string
        to:
          type: string
        defaultValue:
          type: string
    HttpResponseFieldMapping:
      type: object
      required:
        - responsePath
        - leadField
      properties:
        responsePath:
          type: string
        leadField:
          type: string
        interpretAs:
          type: string
    HttpAuthCredentials:
      type: object
      properties:
        username:
          type: string
        password:
          type: string
        token:
          type: string
        headerName:
          type: string
    HttpConfig:
      type: object
      properties:
        url:
          type: string
        method:
          type: string
          enum:
            - POST
            - GET
            - PUT
          default: POST
        format:
          type: string
          enum:
            - json
            - xml
            - form
          default: json
        timeout:
          type: integer
          minimum: 1
          maximum: 120
          default: 30
        retries:
          type: integer
          minimum: 0
          maximum: 10
          default: 3
        authType:
          type: string
          enum:
            - none
            - basic
            - bearer
            - api_key
          default: none
        authCredentials:
          $ref: "#/components/schemas/HttpAuthCredentials"
        headers:
          type: array
          items:
            $ref: "#/components/schemas/HttpHeader"
        bodyTemplate:
          type: string
        urlTemplate:
          type: string
        fieldMapping:
          type: array
          items:
            $ref: "#/components/schemas/HttpFieldMapping"
        responseFieldMapping:
          type: array
          items:
            $ref: "#/components/schemas/HttpResponseFieldMapping"
        responseMappingRules:
          type: array
          items:
            $ref: "#/components/schemas/ResponseMappingRule"
        responseTokens:
          $ref: "#/components/schemas/ResponseTokens"
    TelephonyCredential:
      type: object
      properties:
        id:
          type: string
          format: uuid
        scope:
          type: string
          enum:
            - system
            - tenant
        provider:
          $ref: "#/components/schemas/TrackingNumberProvider"
        apiKey:
          type: string
          nullable: true
        apiSecret:
          type: string
          nullable: true
        publicKey:
          type: string
          nullable: true
        connectionId:
          type: string
          nullable: true
        extra:
          type: object
          additionalProperties: true
          nullable: true
        status:
          type: string
          enum:
            - active
            - inactive
    TelephonyCredentialCreate:
      type: object
      required:
        - provider
        - apiKey
      properties:
        provider:
          $ref: "#/components/schemas/TrackingNumberProvider"
        apiKey:
          type: string
          minLength: 1
        apiSecret:
          type: string
          nullable: true
        publicKey:
          type: string
          nullable: true
        connectionId:
          type: string
          nullable: true
        extra:
          type: object
          additionalProperties: true
          nullable: true
    TelephonyCredentialTest:
      type: object
      required:
        - provider
        - apiKey
      properties:
        provider:
          $ref: "#/components/schemas/TrackingNumberProvider"
        apiKey:
          type: string
          minLength: 1
        apiSecret:
          type: string
          nullable: true
        publicKey:
          type: string
          nullable: true
        connectionId:
          type: string
          nullable: true
    TelephonyCredentialMasked:
      type: object
      description: Telephony credential with apiKey/apiSecret masked
      properties:
        id:
          type: string
          format: uuid
        scope:
          type: string
          enum:
            - system
            - tenant
        tenantId:
          type: string
          format: uuid
          nullable: true
        provider:
          type: string
          enum:
            - telnyx
            - twilio
            - bandwidth
        apiKey:
          type: string
          description: Masked API key (e.g. "KEY_...xyz")
        apiSecret:
          type: string
          nullable: true
          description: Always "***" if set, null otherwise
        publicKey:
          type: string
          nullable: true
        connectionId:
          type: string
          nullable: true
        extra:
          type: object
          additionalProperties: true
          nullable: true
        status:
          type: string
          enum:
            - active
            - inactive
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ProviderName:
      type: string
      description: Downstream provider for a credential row.
      enum:
        - twilio
        - telnyx
        - bandwidth
        - sendgrid
        - ses
    ProviderScope:
      type: string
      description: |
        `system` rows belong to the platform catalog (tenantId=null). `tenant`
        rows are owned by a single tenant. Tenant routes can view shareable
        system rows but cannot mutate them.
      enum:
        - system
        - tenant
    ProviderStatus:
      type: string
      enum:
        - active
        - inactive
    ProviderCapability:
      type: string
      description: |
        Routing decision a credential is allowed to serve. Voice is per-provider
        so a `voice_calls_*` resolution always returns the matching provider.
      enum:
        - sms_delivery
        - sms_messaging
        - email_messaging
        - voice_calls_twilio
        - voice_calls_telnyx
        - voice_calls_bandwidth
    ProviderCredentialLimits:
      type: object
      description: Hard caps superadmin sets on tenant overrides.
      properties:
        maxDailySendLimit:
          type: integer
          minimum: 0
          nullable: true
          description: Max value tenants may set in `assignment.overrides.dailySendLimit`.
      additionalProperties: true
    ProviderCredential:
      type: object
      description: |
        Unified provider credential row. Secret values in `credentials` are
        masked in responses (e.g. `accountSid: "ACa1...xyz"`); plaintext is
        only ever stored encrypted at rest.
      properties:
        id:
          type: string
          format: uuid
        scope:
          $ref: "#/components/schemas/ProviderScope"
        tenantId:
          type: string
          format: uuid
          nullable: true
          description: Always null for `scope=system`; required for `scope=tenant`.
        name:
          type: string
          maxLength: 120
        provider:
          $ref: "#/components/schemas/ProviderName"
        capabilities:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ProviderCapability"
        credentials:
          type: object
          description: Per-provider secret bag (masked in responses).
          additionalProperties:
            type: string
        config:
          type: object
          description: Plaintext per-provider config (fromNumber, baseUrl, region, etc.).
          additionalProperties: true
        shareable:
          type: boolean
          description: System rows only — tenants may subscribe via assignments.
        autoEnableForNewTenants:
          type: array
          description: Capabilities auto-assigned to NEW tenants. Voice forbidden.
          items:
            $ref: "#/components/schemas/ProviderCapability"
        limits:
          $ref: "#/components/schemas/ProviderCredentialLimits"
        dailySendLimit:
          type: integer
          minimum: 0
          nullable: true
        status:
          $ref: "#/components/schemas/ProviderStatus"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ProviderCredentialCreate:
      type: object
      required:
        - scope
        - name
        - provider
        - capabilities
        - credentials
      properties:
        scope:
          $ref: "#/components/schemas/ProviderScope"
        tenantId:
          type: string
          format: uuid
          nullable: true
          description: |
            Tenant route forces caller's tenantId; superadmin route forces null.
            Required when `scope=tenant`.
        name:
          type: string
          minLength: 1
          maxLength: 120
        provider:
          $ref: "#/components/schemas/ProviderName"
        capabilities:
          type: array
          minItems: 1
          description: |
            Must be a SUBSET of the provider's default capabilities (you may
            narrow but never widen).
          items:
            $ref: "#/components/schemas/ProviderCapability"
        credentials:
          type: object
          description: Per-provider secret bag (plaintext on the wire — encrypted at rest).
          additionalProperties:
            type: string
        config:
          type: object
          additionalProperties: true
        shareable:
          type: boolean
          default: false
          description: System scope only — must be false for tenant scope.
        autoEnableForNewTenants:
          type: array
          items:
            $ref: "#/components/schemas/ProviderCapability"
        limits:
          $ref: "#/components/schemas/ProviderCredentialLimits"
        dailySendLimit:
          type: integer
          minimum: 0
          nullable: true
        status:
          $ref: "#/components/schemas/ProviderStatus"
          default: active
    ProviderCredentialUpdate:
      type: object
      description: |
        Partial update. `provider`, `scope`, and `tenantId` are immutable post-
        create. Tenant route ignores `shareable` / `autoEnableForNewTenants` /
        `limits` (superadmin-only fields).
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        capabilities:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ProviderCapability"
        credentials:
          type: object
          description: Only provided keys are re-encrypted; omitted keys preserve existing ciphertext.
          additionalProperties:
            type: string
        config:
          type: object
          additionalProperties: true
        shareable:
          type: boolean
        autoEnableForNewTenants:
          type: array
          items:
            $ref: "#/components/schemas/ProviderCapability"
        limits:
          $ref: "#/components/schemas/ProviderCredentialLimits"
        dailySendLimit:
          type: integer
          minimum: 0
          nullable: true
        status:
          $ref: "#/components/schemas/ProviderStatus"
    ProviderAssignmentOverrides:
      type: object
      description: |
        Allowlisted per-tenant overrides on a credential. Provider-specific —
        only these keys are accepted; all others return VALIDATION_ERROR.
        Secret keys (accountSid, authToken, apiKey, etc.) are NEVER allowed.
      properties:
        dailySendLimit:
          type: integer
          minimum: 0
          description: Tenant-specific daily cap; capped by credential.limits.maxDailySendLimit.
        fromNumber:
          type: string
          pattern: ^\+[1-9]\d{1,14}$
          description: E.164 sender override (Twilio, Bandwidth).
        statusCallbackUrl:
          type: string
          format: uri
          description: HTTPS-only Twilio status callback override.
        region:
          type: string
          pattern: ^[a-z]{2}-[a-z-]+-\d+$
          description: AWS region override for SES.
      additionalProperties: false
    ProviderAssignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        providerCredentialId:
          type: string
          format: uuid
        capability:
          $ref: "#/components/schemas/ProviderCapability"
        isDefault:
          type: boolean
          description: |
            Only one active default per (tenantId, capability). Toggling true
            atomically demotes the prior default.
        status:
          $ref: "#/components/schemas/ProviderStatus"
        overrides:
          $ref: "#/components/schemas/ProviderAssignmentOverrides"
        createdBy:
          type: string
          format: uuid
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ProviderAssignmentCreate:
      type: object
      required:
        - capability
      properties:
        capability:
          $ref: "#/components/schemas/ProviderCapability"
        isDefault:
          type: boolean
          default: false
        status:
          $ref: "#/components/schemas/ProviderStatus"
          default: active
        overrides:
          $ref: "#/components/schemas/ProviderAssignmentOverrides"
    ProviderAssignmentUpdate:
      type: object
      properties:
        isDefault:
          type: boolean
        status:
          $ref: "#/components/schemas/ProviderStatus"
        overrides:
          $ref: "#/components/schemas/ProviderAssignmentOverrides"
    ProviderTestSendRequest:
      type: object
      required:
        - to
      properties:
        to:
          type: string
          pattern: ^\+[1-9]\d{1,14}$
          description: Recipient phone in E.164 (e.g. +15555550100).
        body:
          type: string
          minLength: 1
          description: Optional message body. Defaults to a "Test message from iScale" line.
        tenantId:
          type: string
          format: uuid
          description: Superadmin route only — tenant used for delivery-log audit attribution.
        capability:
          $ref: "#/components/schemas/ProviderCapability"
          description: Tenant route only — defaults to first sms_* capability on the credential.
    ProviderTestSendResponse:
      type: object
      properties:
        ok:
          type: boolean
        provider:
          $ref: "#/components/schemas/ProviderName"
        providerMessageId:
          type: string
          nullable: true
        errorMessage:
          type: string
          nullable: true
    Tenant:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        status:
          type: string
          enum:
            - active
            - suspended
            - inactive
        plan:
          type: string
          enum:
            - starter
            - standard
            - enterprise
        domain:
          type: string
          nullable: true
        logoUrl:
          type: string
          nullable: true
        features:
          type: object
          additionalProperties:
            type: boolean
        settings:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    UserSummary:
      type: object
      description: Abbreviated user record returned on create
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        firstName:
          type: string
          nullable: true
        lastName:
          type: string
          nullable: true
        role:
          type: string
        status:
          type: string
          enum:
            - active
            - inactive
        createdAt:
          type: string
          format: date-time
    UserDetail:
      type: object
      description: Full user record with permissions and profile fields
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        firstName:
          type: string
          nullable: true
        lastName:
          type: string
          nullable: true
        jobTitle:
          type: string
          nullable: true
        phone:
          type: string
          nullable: true
        workPhone:
          type: string
          nullable: true
        ext:
          type: string
          nullable: true
        role:
          type: string
        profilePicture:
          type: string
          nullable: true
        isOwner:
          type: boolean
        isAdmin:
          type: boolean
        dataExport:
          type: boolean
        massEmails:
          type: boolean
        notifications:
          type: boolean
        support:
          type: boolean
        billing:
          type: boolean
        newsletters:
          type: boolean
        apiAccess:
          type: boolean
        apiSecret:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - active
            - inactive
        buyerId:
          type: string
          format: uuid
          nullable: true
        tenantId:
          type: string
          format: uuid
          nullable: true
        lastLoginAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
    EnrollmentInvite:
      type: object
      required:
        - offerId
        - partnerId
        - name
      properties:
        offerId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 255
        costPerLead:
          type: string
          default: "0"
        pricingModel:
          type: string
          enum:
            - fixed
            - revshare
          default: fixed
        dailyCap:
          type: integer
          minimum: 1
          nullable: true
        weeklyCap:
          type: integer
          minimum: 1
          nullable: true
        monthlyCap:
          type: integer
          minimum: 1
          nullable: true
        notes:
          type: string
          nullable: true
    EnrollmentApprove:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        costPerLead:
          type: string
          default: "0"
        pricingModel:
          type: string
          enum:
            - fixed
            - revshare
          default: fixed
        dailyCap:
          type: integer
          minimum: 1
          nullable: true
        weeklyCap:
          type: integer
          minimum: 1
          nullable: true
        monthlyCap:
          type: integer
          minimum: 1
          nullable: true
        notes:
          type: string
          nullable: true
    EnrollmentReject:
      type: object
      properties:
        notes:
          type: string
          nullable: true
    EnrollmentFilter:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/EnrollmentStatus"
        offerId:
          type: string
          format: uuid
        partnerId:
          type: string
          format: uuid
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    OrganizationCreate:
      type: object
      required:
        - name
        - slug
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: Acme Corp
        slug:
          type: string
          minLength: 1
          maxLength: 100
          pattern: ^[a-z0-9-]+$
          example: acme-corp
        plan:
          $ref: "#/components/schemas/OrganizationPlan"
          default: starter
        features:
          type: object
          additionalProperties:
            type: boolean
          default: {}
        limits:
          type: object
          properties:
            maxContacts:
              type: integer
              minimum: 1
              default: 50000
            maxEmailsPerMonth:
              type: integer
              minimum: 1
              default: 100000
        settings:
          type: object
          properties:
            timezone:
              type: string
              default: UTC
            branding:
              type: object
              additionalProperties: true
    OrganizationUpdate:
      description: All fields optional (partial update)
      allOf:
        - $ref: "#/components/schemas/OrganizationCreate"
      x-all-optional: true
    OrganizationFilter:
      type: object
      properties:
        plan:
          $ref: "#/components/schemas/OrganizationPlan"
        status:
          $ref: "#/components/schemas/OrganizationStatus"
        search:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 1000
          default: 20
        offset:
          type: integer
          minimum: 0
          default: 0
    ToggleFeature:
      type: object
      required:
        - feature
        - enabled
      properties:
        feature:
          type: string
          minLength: 1
        enabled:
          type: boolean
    SetupTokenCreate:
      type: object
      required:
        - campaignId
      properties:
        campaignId:
          type: string
          format: uuid
    SetupTokenResponse:
      type: object
      required:
        - id
        - token
        - setupUrl
        - campaignId
        - status
        - expiresAt
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        token:
          type: string
        setupUrl:
          type: string
          format: uri
        campaignId:
          type: string
          format: uuid
        status:
          type: string
        expiresAt:
          type: string
        createdAt:
          type: string
    NotificationChannel:
      type: string
      description: Delivery channel for a notification subscription. Slack is deferred.
      enum:
        - email
        - in_app
    NotificationTriggerRegistry:
      type: object
      description: |
        Registry of available notification triggers, keyed by trigger key
        (e.g. `lead.test_submitted`). Each entry describes the trigger and
        its default delivery channels.
      additionalProperties:
        type: object
        required:
          - label
          - description
          - scope
          - roles
          - defaultChannels
        properties:
          label:
            type: string
          description:
            type: string
          scope:
            type: string
            description: Trigger scope (e.g. `partner`, `tenant`).
          roles:
            type: array
            description: Roles permitted to subscribe to this trigger.
            items:
              type: string
              enum:
                - admin
                - buyer
                - partner
                - superadmin
          defaultChannels:
            type: array
            description: Channels selected by default when no explicit subscription row exists.
            items:
              $ref: "#/components/schemas/NotificationChannel"
    NotificationSubscriptionMap:
      type: object
      description: |
        Map of trigger key → user override. Only triggers with an explicit
        subscription row appear here. The UI applies registry `defaultChannels`
        for any trigger missing from this map.
      additionalProperties:
        type: object
        required:
          - channels
          - enabled
        properties:
          channels:
            type: array
            items:
              $ref: "#/components/schemas/NotificationChannel"
          enabled:
            type: boolean
    NotificationSubscriptionUpsert:
      type: object
      description: Request body for upserting a single notification subscription.
      required:
        - triggerKey
        - channels
        - enabled
      properties:
        triggerKey:
          type: string
          description: Trigger registry key (e.g. `lead.test_submitted`).
        channels:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/NotificationChannel"
        enabled:
          type: boolean
    Vertical:
      type: object
      description: A lead category (insurance, solar, etc.) with its own custom fields and settings.
      required:
        - id
        - name
        - slug
        - status
      properties:
        id:
          type: string
          format: uuid
        displayId:
          type: integer
          description: Sequential human-readable identifier.
        legacyId:
          type: string
          nullable: true
        name:
          type: string
        slug:
          type: string
        verticalGroup:
          type: string
          nullable: true
        verticalGroupId:
          type: string
          format: uuid
          nullable: true
        profileKey:
          type: string
          nullable: true
        retentionCadence:
          type: string
          enum:
            - weekly
            - monthly
          nullable: true
        crmId:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        settings:
          type: object
          additionalProperties: true
          nullable: true
        status:
          type: string
          enum:
            - active
            - inactive
        tenantId:
          type: string
          format: uuid
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    BillingAuditIssue:
      type: object
      description: A single billing integrity issue detected by the audit or reconciliation routines.
      required:
        - code
        - severity
      properties:
        code:
          type: string
          description: Machine-readable issue code (e.g. `balance_drift`, `buyer_sku_funding_remaining_drift`).
        severity:
          type: string
          enum:
            - info
            - warning
            - critical
        tenantId:
          type: string
          format: uuid
          nullable: true
        buyerId:
          type: string
          format: uuid
          nullable: true
        buyerSkuId:
          type: string
          format: uuid
          nullable: true
        contractId:
          type: string
          format: uuid
          nullable: true
        paymentId:
          type: string
          format: uuid
          nullable: true
        saleId:
          type: string
          format: uuid
          nullable: true
        details:
          type: object
          additionalProperties: true
          nullable: true
    BillingReconciliationSummary:
      type: object
      required:
        - totalIssues
        - criticalIssues
        - warningIssues
        - infoIssues
        - latestCheckedAt
      properties:
        totalIssues:
          type: integer
        criticalIssues:
          type: integer
        warningIssues:
          type: integer
        infoIssues:
          type: integer
        totalBuyerSkus:
          type: integer
        totalPayments:
          type: integer
        activeFillableLeads:
          type: integer
        ledgerDriftIssues:
          type: integer
        missingStripeChargeIds:
          type: integer
        duplicateStripePointers:
          type: integer
        stateMismatches:
          type: integer
        latestCheckedAt:
          type: string
          format: date-time
    StripeEventLifecycleView:
      type: object
      description: Processing lifecycle view of a received Stripe webhook event. Error/result payloads are sanitized.
      required:
        - id
        - type
        - status
        - attemptCount
        - retryable
        - livemode
        - receivedAt
        - processedAt
        - replayPreview
      properties:
        id:
          type: string
        type:
          type: string
          description: Stripe event type (e.g. `invoice.paid`).
        status:
          type: string
          description: Internal processing status (e.g. `processed`, `failed`, `failed_retryable`).
        attemptCount:
          type: integer
        retryable:
          type: boolean
          description: True when status is `failed_retryable`.
        stripeAccountId:
          type: string
          nullable: true
        livemode:
          type: boolean
        apiVersion:
          type: string
          nullable: true
        requestId:
          type: string
          nullable: true
        receivedAt:
          type: string
          format: date-time
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        processedAt:
          type: string
          format: date-time
        lastError:
          type: object
          additionalProperties: true
          nullable: true
        handlerResult:
          type: object
          additionalProperties: true
          nullable: true
        linkedBillingPaymentId:
          type: string
          format: uuid
          nullable: true
        linkedBuyerSkuId:
          type: string
          format: uuid
          nullable: true
        replayPreview:
          type: object
          required:
            - safe
            - label
            - reason
          properties:
            safe:
              type: boolean
            label:
              type: string
            reason:
              type: string
    SubscriptionPlan:
      type: object
      description: A subscription plan. Money fields are integer USD cents; credit fields are integer credit counts.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        monthlyPriceUsd:
          type: integer
          description: Monthly price in USD cents.
        includedCredits:
          type: integer
        creditDiscountPercent:
          type: number
          description: Percent discount applied to credit-package purchases (0-100).
        unlimitedServices:
          type: array
          items:
            type: string
        entityLimits:
          type: object
          additionalProperties: true
        trialDays:
          type: integer
        trialCredits:
          type: integer
        requiresCard:
          type: boolean
        sortOrder:
          type: integer
    CreditPackage:
      type: object
      description: A purchasable credit package. Price fields are integer USD cents.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        credits:
          type: integer
        priceUsd:
          type: integer
          description: List price in USD cents.
        discountPercent:
          type: number
          description: Package's own discount percent (0-100).
        planDiscountPercent:
          type: number
          description: Additional discount from the tenant's current plan (0-100).
        effectivePrice:
          type: integer
          description: Discounted price in USD cents after combined discounts.
        discountedPriceUsd:
          type: integer
          description: Same value as effectivePrice.
        sortOrder:
          type: integer
        status:
          type: string
    CreditTransaction:
      type: object
      description: An immutable credit ledger entry (tenant-safe projection — internal platform-cost fields are never
        returned). Credit fields are decimal strings (≤4 dp, numeric(12,4)); USD fields are integer cents.
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          enum:
            - purchase
            - deduction
            - platform_deduction
            - refund
            - adjustment
            - auto_recharge
            - plan_reset
        credits:
          type: string
          description: Signed credit delta for this entry — decimal string, ≤4 dp (e.g. '-22.5').
        balanceAfter:
          type: string
          description: Credit balance after applying this entry — decimal string, ≤4 dp.
        service:
          type: string
          nullable: true
        units:
          type: string
          nullable: true
          description: Decimal units string (precision 10, scale 4).
        description:
          type: string
          nullable: true
        relatedEntityType:
          type: string
          nullable: true
        relatedEntityId:
          type: string
          format: uuid
          nullable: true
        packageId:
          type: string
          format: uuid
          nullable: true
        planId:
          type: string
          format: uuid
          nullable: true
        amountUsd:
          type: integer
          nullable: true
          description: Amount charged in USD cents.
        createdAt:
          type: string
          format: date-time
    CreditBalance:
      type: object
      description: Tenant credit balance, plan, auto-recharge, and subscription state. Credit fields are decimal strings (≤4
        dp, numeric(12,4)).
      properties:
        creditBalance:
          type: string
          description: Purchased/topped-up credits — decimal string, ≤4 dp.
        planCreditBalance:
          type: string
          description: Credits granted by the current plan — decimal string, ≤4 dp.
        totalCredits:
          type: string
          description: creditBalance + planCreditBalance (exact credit math) — decimal string, ≤4 dp.
        plan:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/SubscriptionPlan"
        autoRecharge:
          type: object
          properties:
            enabled:
              type: boolean
            threshold:
              type: string
              nullable: true
              description: Credit balance at which auto-recharge triggers — decimal string, ≤4 dp.
            mode:
              type: string
              enum:
                - package
                - target
              description: package = fixed-package top-up; target = variable charge up to the target balance.
            target:
              type: string
              nullable: true
              description: Target balance in credits for target mode (1 credit = $0.01) — decimal string, ≤4 dp.
            packageId:
              type: string
              format: uuid
              nullable: true
            stripePaymentMethodId:
              type: string
              nullable: true
        subscription:
          type: object
          properties:
            stripeSubscriptionId:
              type: string
              nullable: true
            status:
              type: string
              nullable: true
            currentPeriodStart:
              type: string
              format: date-time
              nullable: true
            currentPeriodEnd:
              type: string
              format: date-time
              nullable: true
            hasUsedTrial:
              type: boolean
        billingExempt:
          type: boolean
        billingMode:
          type: string
          example: self
        stripeCustomerId:
          type: string
          nullable: true
    AutoRechargeConfig:
      type: object
      description: Tenant auto-recharge configuration. Credit values are decimal strings (1 credit = $0.01). Responses may
        carry up to 4 dp; request inputs accept at most 2 dp.
      properties:
        autoRechargeEnabled:
          type: boolean
        autoRechargeThreshold:
          type: string
          nullable: true
          description: Decimal credit string, ≤4 dp.
        autoRechargeMode:
          type: string
          enum:
            - package
            - target
          description: package = fixed-package top-up; target = variable top-up-to-target charge.
        autoRechargeTarget:
          type: string
          nullable: true
          description: Target balance in credits for target mode — decimal string, ≤4 dp.
        autoRechargePackageId:
          type: string
          format: uuid
          nullable: true
        stripePaymentMethodId:
          type: string
          nullable: true
    MessagingCampaign:
      type: object
      description: An email/SMS campaign. `revenueTotal` is a decimal money string; count fields are integers.
      required:
        - id
        - orgId
        - name
        - channel
        - status
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          nullable: true
        templateId:
          type: string
          format: uuid
          nullable: true
        segmentId:
          type: string
          format: uuid
          nullable: true
        channel:
          $ref: "#/components/schemas/MessagingChannel"
        status:
          type: string
          enum:
            - draft
            - scheduled
            - sending
            - paused
            - completed
            - cancelled
        scheduledAt:
          type: string
          format: date-time
          nullable: true
        abTestConfig:
          type: object
          additionalProperties: true
          nullable: true
        attributionWindowDays:
          type: integer
        smartSendingEnabled:
          type: boolean
        timezoneSendingEnabled:
          type: boolean
        totalRecipients:
          type: integer
        totalSent:
          type: integer
        totalDelivered:
          type: integer
        totalOpened:
          type: integer
        totalClicked:
          type: integer
        totalBounced:
          type: integer
        totalFailed:
          type: integer
          description: Sends that never reached the provider (distinct from bounced).
        totalComplaints:
          type: integer
        totalUnsubscribed:
          type: integer
        revenueTotal:
          type: string
          description: Decimal money string (precision 12, scale 2).
        sentAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        templateName:
          type: string
          nullable: true
          description: Joined template name (present on list responses).
        segmentName:
          type: string
          nullable: true
          description: Joined segment name (present on list responses).
    MessagingContact:
      type: object
      description: A contact/profile synced from routing leads. `custom` holds arbitrary JSONB properties.
      required:
        - id
        - orgId
        - email
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
          format: uuid
        email:
          type: string
        phone:
          type: string
          nullable: true
        firstName:
          type: string
          nullable: true
        lastName:
          type: string
          nullable: true
        routingLeadId:
          type: string
          nullable: true
        emailStatus:
          $ref: "#/components/schemas/MessagingEmailStatus"
        smsStatus:
          type: string
          enum:
            - active
            - opted_out
            - bounced
        custom:
          type: object
          additionalProperties: true
          nullable: true
        totalSent:
          type: integer
        totalOpened:
          type: integer
        totalClicked:
          type: integer
        lastEngagedAt:
          type: string
          format: date-time
          nullable: true
        lastEmailSentAt:
          type: string
          format: date-time
          nullable: true
        lastSmsSentAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        tags:
          type: array
          items:
            type: string
          description: Contact tags (present on list/detail responses).
    MessagingErrorResponse:
      type: object
      required:
        - errors
      properties:
        errors:
          type: array
          items:
            type: object
            required:
              - id
              - status
              - code
              - title
              - detail
            properties:
              id:
                type: string
                format: uuid
              status:
                type: string
                description: HTTP status as a string, e.g. "404".
              code:
                type: string
                example: not_found
              title:
                type: string
              detail:
                type: string
      example:
        errors:
          - id: 6f5c1b2a-0d3e-4a1b-8c9d-2e4f6a8b0c1d
            status: "404"
            code: not_found
            title: Not Found
            detail: Profile not found
    MessagingEmailStatus:
      type: string
      enum:
        - active
        - bounced
        - complained
        - unsubscribed
    MessagingListType:
      type: string
      enum:
        - static
        - dynamic
    MessagingFlowStatus:
      type: string
      enum:
        - draft
        - active
        - paused
        - archived
    MessagingChannel:
      type: string
      enum:
        - email
        - sms
    MessagingLocation:
      type: object
      description: Location attributes. On write, `region` maps to the contact's state.
      properties:
        city:
          type: string
        region:
          type: string
        zip:
          type: string
        country:
          type: string
    MessagingProfileCreateAttributes:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
        phone_number:
          type: string
          maxLength: 20
        first_name:
          type: string
          maxLength: 100
        last_name:
          type: string
          maxLength: 100
        external_id:
          type: string
          maxLength: 255
          description: External identifier; stored as the contact's routingLeadId.
        location:
          $ref: "#/components/schemas/MessagingLocation"
        properties:
          type: object
          additionalProperties: true
          description: Arbitrary custom properties (merged into the contact's custom JSONB and written to contactProperty rows).
        tags:
          type: array
          maxItems: 20
          items:
            type: string
            maxLength: 100
    MessagingProfileUpdateAttributes:
      type: object
      description: All fields optional. Same shape as create minus `tags`, and `email` is optional.
      properties:
        email:
          type: string
          format: email
        phone_number:
          type: string
          maxLength: 20
        first_name:
          type: string
          maxLength: 100
        last_name:
          type: string
          maxLength: 100
        external_id:
          type: string
          maxLength: 255
        location:
          $ref: "#/components/schemas/MessagingLocation"
        properties:
          type: object
          additionalProperties: true
    MessagingProfileCreateRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - type
            - attributes
          properties:
            type:
              type: string
              enum:
                - profile
            attributes:
              $ref: "#/components/schemas/MessagingProfileCreateAttributes"
    MessagingProfileUpdateRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - type
            - id
            - attributes
          properties:
            type:
              type: string
              enum:
                - profile
            id:
              type: string
              format: uuid
              description: Must be present even though the path `id` identifies the record.
            attributes:
              $ref: "#/components/schemas/MessagingProfileUpdateAttributes"
    MessagingProfileResource:
      type: object
      required:
        - type
        - id
        - attributes
      properties:
        type:
          type: string
          enum:
            - profile
        id:
          type: string
          format: uuid
        attributes:
          type: object
          properties:
            email:
              type: string
              nullable: true
            phone_number:
              type: string
              nullable: true
            first_name:
              type: string
              nullable: true
            last_name:
              type: string
              nullable: true
            external_id:
              type: string
              nullable: true
            location:
              type: object
              properties:
                city:
                  type: string
                  nullable: true
                region:
                  type: string
                  nullable: true
                zip:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
            properties:
              type: object
              additionalProperties: true
            created:
              type: string
              format: date-time
            updated:
              type: string
              format: date-time
    MessagingProfileResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/MessagingProfileResource"
    MessagingPageMeta:
      type: object
      required:
        - page
      properties:
        page:
          type: object
          properties:
            size:
              type: integer
            number:
              type: integer
            total_pages:
              type: integer
            total_count:
              type: integer
    MessagingPageLinks:
      type: object
      properties:
        self:
          type: string
        next:
          type: string
          nullable: true
        prev:
          type: string
          nullable: true
    MessagingProfileListResponse:
      type: object
      required:
        - data
        - meta
        - links
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MessagingProfileResource"
        meta:
          $ref: "#/components/schemas/MessagingPageMeta"
        links:
          $ref: "#/components/schemas/MessagingPageLinks"
    MessagingListRule:
      type: object
      required:
        - field
        - operator
      properties:
        field:
          type: string
          minLength: 1
        operator:
          type: string
          minLength: 1
        value:
          nullable: true
          oneOf:
            - type: string
            - type: number
            - type: boolean
            - type: array
              items:
                type: string
    MessagingListRuleGroup:
      type: object
      description: When present on create, the list is created as a dynamic segment.
      required:
        - logic
        - rules
      properties:
        logic:
          type: string
          enum:
            - and
            - or
        rules:
          type: array
          items:
            $ref: "#/components/schemas/MessagingListRule"
    MessagingListCreateRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - type
            - attributes
          properties:
            type:
              type: string
              enum:
                - list
            attributes:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                rules:
                  $ref: "#/components/schemas/MessagingListRuleGroup"
                properties:
                  type: object
                  additionalProperties: true
    MessagingListUpdateRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - type
            - id
            - attributes
          properties:
            type:
              type: string
              enum:
                - list
            id:
              type: string
              format: uuid
            attributes:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
    MessagingListMembershipRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          minItems: 1
          maxItems: 1000
          items:
            type: object
            required:
              - type
              - id
            properties:
              type:
                type: string
                enum:
                  - profile
              id:
                type: string
                format: uuid
    MessagingListResource:
      type: object
      required:
        - type
        - id
        - attributes
      properties:
        type:
          type: string
          enum:
            - list
        id:
          type: string
          format: uuid
        attributes:
          type: object
          properties:
            name:
              type: string
            profile_count:
              type: integer
            properties:
              type: object
              additionalProperties: true
              nullable: true
            created:
              type: string
              format: date-time
            updated:
              type: string
              format: date-time
    MessagingListResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/MessagingListResource"
    MessagingListListResponse:
      type: object
      required:
        - data
        - meta
        - links
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MessagingListResource"
        meta:
          $ref: "#/components/schemas/MessagingPageMeta"
        links:
          $ref: "#/components/schemas/MessagingPageLinks"
    MessagingFlowStep:
      oneOf:
        - type: object
          required:
            - type
            - config
          properties:
            type:
              type: string
              enum:
                - delay
            config:
              type: object
              required:
                - duration
                - unit
              properties:
                duration:
                  type: integer
                  minimum: 1
                unit:
                  type: string
                  enum:
                    - minutes
                    - hours
                    - days
                    - weeks
        - type: object
          required:
            - type
            - config
          properties:
            type:
              type: string
              enum:
                - send_email
            config:
              type: object
              required:
                - templateId
              properties:
                templateId:
                  type: string
                  format: uuid
        - type: object
          required:
            - type
            - config
          properties:
            type:
              type: string
              enum:
                - send_sms
            config:
              type: object
              required:
                - templateId
              properties:
                templateId:
                  type: string
                  format: uuid
    MessagingFlowCreateRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - type
            - attributes
          properties:
            type:
              type: string
              enum:
                - flow
            attributes:
              type: object
              required:
                - name
                - trigger_type
                - steps
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                trigger_type:
                  type: string
                  enum:
                    - event
                    - segment_entry
                    - manual
                    - date_property
                trigger_segment_id:
                  type: string
                  format: uuid
                  description: Single trigger segment (required for segment_entry unless trigger_segment_ids is given).
                trigger_segment_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                trigger_event:
                  type: string
                  maxLength: 255
                  description: Required when trigger_type is event.
                steps:
                  type: array
                  minItems: 1
                  items:
                    $ref: "#/components/schemas/MessagingFlowStep"
    MessagingFlowPatchRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - type
            - attributes
          properties:
            type:
              type: string
              enum:
                - flow
            attributes:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - archived
                trigger_segment_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
    MessagingFlowResource:
      type: object
      required:
        - type
        - id
        - attributes
      properties:
        type:
          type: string
          enum:
            - flow
        id:
          type: string
          format: uuid
        attributes:
          type: object
          properties:
            name:
              type: string
            status:
              $ref: "#/components/schemas/MessagingFlowStatus"
            trigger_type:
              type: string
            trigger_config:
              type: object
              additionalProperties: true
              nullable: true
            description:
              type: string
              nullable: true
            total_entered:
              type: integer
            total_completed:
              type: integer
            created:
              type: string
              format: date-time
            updated:
              type: string
              format: date-time
    MessagingFlowResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/MessagingFlowResource"
    MessagingFlowWithStepsResource:
      allOf:
        - $ref: "#/components/schemas/MessagingFlowResource"
        - type: object
          properties:
            attributes:
              type: object
              properties:
                steps:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      type:
                        type: string
                      config:
                        type: object
                        additionalProperties: true
                      sort_order:
                        type: integer
                      parent_step_id:
                        type: string
                        format: uuid
                        nullable: true
                      branch_key:
                        type: string
                        nullable: true
    MessagingFlowWithStepsResponse:
      type: object
      required:
        - data
      properties:
        data:
          $ref: "#/components/schemas/MessagingFlowWithStepsResource"
    MessagingFlowListResponse:
      type: object
      required:
        - data
        - meta
        - links
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MessagingFlowResource"
        meta:
          $ref: "#/components/schemas/MessagingPageMeta"
        links:
          $ref: "#/components/schemas/MessagingPageLinks"
    MessagingTriggerSegmentLinkRequest:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          required:
            - attributes
          properties:
            attributes:
              type: object
              required:
                - segment_id
              properties:
                segment_id:
                  type: string
                  format: uuid
    MessagingSendRequest:
      type: object
      description: Provide either contactId or email (at least one is required).
      required:
        - channel
        - templateId
      properties:
        contactId:
          type: string
          format: uuid
          description: Target contact. Provide this or email.
        email:
          type: string
          format: email
          description: Email to look up or create the contact. Provide this or contactId.
        channel:
          $ref: "#/components/schemas/MessagingChannel"
        templateId:
          type: string
          format: uuid
        mergeData:
          type: object
          additionalProperties: true
          default: {}
          description: Template variable overrides.
    MessagingSendResult:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          properties:
            messageId:
              type: string
              format: uuid
              nullable: true
            status:
              type: string
              description: Message status, e.g. sent, queued, suppressed, failed.
            error:
              type: string
              nullable: true
              description: Present on suppressed/failed outcomes.
    MessagingSegmentRuleGroup:
      type: object
      description: |
        Recursive AND/OR rule group defining segment membership (also reused by flow triggerFilters and condition
        steps). Each rule targets a contact field with an operator and comparison value.
      required:
        - logic
        - rules
      properties:
        logic:
          type: string
          enum:
            - and
            - or
        rules:
          type: array
          description: Array of leaf rules and/or nested rule groups.
          items:
            oneOf:
              - $ref: "#/components/schemas/MessagingSegmentRule"
              - $ref: "#/components/schemas/MessagingSegmentRuleGroup"
    MessagingSegmentRule:
      type: object
      required:
        - field
        - operator
        - value
      properties:
        field:
          type: string
          minLength: 1
          description: Contact field path, e.g. "email", "custom.state", "tag", "totalOpened", "lastEngagedAt".
        operator:
          type: string
          enum:
            - equals
            - not_equals
            - contains
            - not_contains
            - starts_with
            - ends_with
            - greater_than
            - less_than
            - between
            - is_set
            - is_not_set
            - in_list
            - not_in_list
            - before
            - after
            - within_last
            - not_within_last
            - has_tag
            - not_has_tag
        value:
          nullable: true
          description: Comparison value. Null for is_set/is_not_set.
          oneOf:
            - type: string
            - type: number
            - type: boolean
            - type: array
              items:
                type: string
    MessagingSegmentCreate:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
          maxLength: 1000
          nullable: true
        type:
          type: string
          enum:
            - dynamic
            - static
          default: dynamic
        source:
          type: string
          enum:
            - contacts
            - leads
          default: contacts
        status:
          type: string
          enum:
            - active
            - archived
          default: active
        rules:
          allOf:
            - $ref: "#/components/schemas/MessagingSegmentRuleGroup"
          description: Required for dynamic segments; ignored for static.
          nullable: true
        contactIds:
          type: array
          items:
            type: string
            format: uuid
          description: Used for static segments — the initial member list.
    MessagingSegmentUpdate:
      description: All fields optional (partial update).
      allOf:
        - $ref: "#/components/schemas/MessagingSegmentCreate"
      x-all-optional: true
    MessagingFlowStepConfig:
      type: object
      additionalProperties: true
      description: |
        Step configuration; the required shape depends on the step `type`:
        - `send_email` / `send_sms`: `{ templateId: uuid }`
        - `delay`: `{ duration: int >= 1, unit: minutes|hours|days|weeks }`
        - `condition`: `{ rules: MessagingSegmentRuleGroup }`
        - `ab_split`: `{ variants: [{ key: string, weight: 0..100 }, ...] }` (min 2 variants)
        - `update_property`: `{ property: string, value: string }`
        - `add_tag` / `remove_tag`: `{ tag: string (1..100) }`
        - `webhook`: `{ url: string, method: GET|POST, headers?: object, body?: string|null }`

        Note: the create/update step handlers accept `config` as a free-form object and only enforce the
        type-specific required keys (e.g. `templateId`, `url`) at flow activation time, not on write.
    ScoringMatchCondition:
      type: object
      description: A band/adjustment match test — reuses the routing filter operator semantics.
      required:
        - operator
        - value
      properties:
        operator:
          type: string
          description: "One of the routing filter operators: equals, not_equals, greater_than, less_than, in, contains, is_empty,
            is_not_empty (+ aliases)."
        value:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
    ScoringBand:
      type: object
      description: A value band for a weighted or gating factor.
      required:
        - match
        - subScore
      properties:
        match:
          $ref: "#/components/schemas/ScoringMatchCondition"
        subScore:
          type: number
          description: 1–10 sub-score awarded when the field value matches this band.
        gateCap:
          type: number
          description: Absolute score ceiling imposed when this band matches. Only permitted on gating factors.
        coarseMapping:
          type: boolean
          description: Marks a low-fidelity band mapping; emits a COARSE_MAPPING flag when matched.
        note:
          type: string
    ScoringFactor:
      type: object
      description: One scoring input. `weighted` factors contribute a weighted sub-score; `gating` factors also contribute a
        sub-score but may impose an absolute `gateCap`; `modifier` factors add/subtract a flat `adjustment` after the
        weighted average.
      required:
        - field
        - role
        - missingData
      properties:
        field:
          type: string
          description: Flat lead field name (standard column or vertical custom field).
        role:
          type: string
          enum:
            - weighted
            - gating
            - modifier
        weight:
          type: number
          description: Required (>= 0) for weighted/gating factors; must be omitted for modifiers.
        missingData:
          type: object
          required:
            - policy
          properties:
            policy:
              type: string
              enum:
                - renormalize
                - fallbackBand
                - skip
              description: weighted → renormalize; gating → fallbackBand (required); modifier → skip.
            fallbackBand:
              type: string
              description: For gating factors, the band `match.value` scored when the field is absent.
            flag:
              type: string
        bands:
          type: array
          description: Required for weighted/gating factors; must be omitted for modifiers.
          items:
            $ref: "#/components/schemas/ScoringBand"
        adjustment:
          type: object
          description: Required for modifier factors only.
          required:
            - type
            - when
            - amount
          properties:
            type:
              type: string
              enum:
                - additive
            when:
              $ref: "#/components/schemas/ScoringMatchCondition"
            amount:
              type: number
    ScoringRoutingBand:
      type: object
      description: Maps a final-score integer range to a machine-key band label. Ranges must not overlap.
      required:
        - min
        - max
        - label
      properties:
        min:
          type: integer
        max:
          type: integer
        label:
          type: string
          pattern: ^[a-z][a-z0-9_]*$
          description: Machine key (lowercase snake_case).
    ScoringConfig:
      type: object
      description: Full scoring model configuration (stored per version). The engine renormalizes weighted/gating factor
        weights over the factors that produced a sub-score, applies modifiers, then gate caps, rounds half-up, and
        floors at 1. See the Lead Scoring section of the user manual.
      required:
        - scale
        - factors
        - collectedNotScored
        - routingBands
      properties:
        scale:
          type: object
          required:
            - min
            - max
          properties:
            min:
              type: number
            max:
              type: number
        factors:
          type: array
          items:
            $ref: "#/components/schemas/ScoringFactor"
        collectedNotScored:
          type: array
          description: Vertical intake fields intentionally collected but excluded from scoring.
          items:
            type: object
            required:
              - field
              - reason
            properties:
              field:
                type: string
              reason:
                type: string
        routingBands:
          type: array
          items:
            $ref: "#/components/schemas/ScoringRoutingBand"
        calibration:
          type: object
          nullable: true
          properties:
            outcome:
              type: string
            persistedWindowDays:
              type: number
            minSamplesPerBand:
              type: number
    ScoringModelVersion:
      type: object
      description: An immutable-once-live snapshot of a model's config.
      properties:
        id:
          type: string
          format: uuid
        scoringModelId:
          type: string
          format: uuid
        versionNumber:
          type: integer
        config:
          $ref: "#/components/schemas/ScoringConfig"
        status:
          type: string
          enum:
            - draft
            - live
            - retired
        createdBy:
          type: string
          format: uuid
          nullable: true
        promotedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
    ScoringModelDetail:
      type: object
      description: A scoring model with its version history and the resolved live + draft versions.
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - active
            - archived
        liveVersionId:
          type: string
          format: uuid
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        versions:
          type: array
          description: All versions, newest versionNumber first.
          items:
            $ref: "#/components/schemas/ScoringModelVersion"
        liveVersion:
          allOf:
            - $ref: "#/components/schemas/ScoringModelVersion"
          nullable: true
        draftVersion:
          allOf:
            - $ref: "#/components/schemas/ScoringModelVersion"
          nullable: true
  responses:
    Unauthorized:
      description: Missing or invalid authentication
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: object
                properties:
                  code:
                    type: string
                    example: UNAUTHORIZED
                  message:
                    type: string
                    example: Authentication required
    Forbidden:
      description: Insufficient permissions
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: object
                properties:
                  code:
                    type: string
                    example: FORBIDDEN
                  message:
                    type: string
                    example: Insufficient permissions
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: object
                properties:
                  code:
                    type: string
                    example: NOT_FOUND
                  message:
                    type: string
                    example: Resource not found
    ValidationError:
      description: Invalid request data
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: object
                properties:
                  code:
                    type: string
                    example: VALIDATION_ERROR
                  message:
                    type: string
                    example: Invalid request data
                  details:
                    type: object
    MessagingError:
      description: |
        Error (JSON:API envelope). Returned by all Flows/Lists/Profiles endpoints and by
        the auth wrapper on every messaging endpoint. `status` mirrors the HTTP status;
        `code` is a machine-readable slug such as `invalid`, `not_authenticated`,
        `not_found`, `conflict`, or `server_error`.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/MessagingErrorResponse"
paths:
  /api/admin/ai-dialer/agents:
    get:
      summary: List AI Dialer agents
      description: Returns a paginated list of ElevenLabs conversational AI agents for the current tenant, newest first.
      operationId: listAiDialerAgents
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by agent status (exact match). Typically `active` or `inactive`.
          schema:
            type: string
        - name: search
          in: query
          description: Filter by agent name (case-insensitive partial match)
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of AI Dialer agents
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    displayId: 12
                    name: Auto Insurance Qualifier
                    verticalId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    elevenLabsAgentId: agent_abc123
                    elevenLabsPhoneId: null
                    outboundTrackingNumberId: null
                    voiceId: null
                    llmModel: glm-4.5-air
                    systemPrompt: You are a qualification agent...
                    firstMessage: Hi, this is Sarah calling about your quote request.
                    transferMode: conference
                    maxDurationSeconds: 300
                    voicemailMessage: null
                    dynamicVariableMapping: []
                    status: active
                    tenantId: c3d4e5f6-a7b8-9012-cdef-123456789012
                    createdAt: 2025-01-15T10:00:00.000Z
                    updatedAt: 2025-01-15T10:00:00.000Z
                pagination:
                  total: 3
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized (no valid session or API key)
        "403":
          description: Forbidden (caller is not an admin or superadmin)
    post:
      summary: Create an AI Dialer agent
      description: |
        Creates an AI Dialer agent record. If an ElevenLabs API key is configured for the tenant,
        the agent is also provisioned on ElevenLabs and its `elevenLabsAgentId` is stored.
        ElevenLabs provisioning is best-effort — the local record is still created if provisioning fails.
      operationId: createAiDialerAgent
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                voiceId:
                  type: string
                  nullable: true
                llmModel:
                  type: string
                  default: glm-4.5-air
                systemPrompt:
                  type: string
                  nullable: true
                firstMessage:
                  type: string
                  nullable: true
                transferMode:
                  type: string
                  enum:
                    - conference
                    - blind
                    - sip
                  default: conference
                maxDurationSeconds:
                  type: integer
                  minimum: 30
                  maximum: 1800
                  default: 300
                voicemailMessage:
                  type: string
                  nullable: true
                outboundTrackingNumberId:
                  type: string
                  format: uuid
                  nullable: true
                elevenLabsPhoneId:
                  type: string
                  nullable: true
                dynamicVariableMapping:
                  type: array
                  description: Maps lead fields to ElevenLabs dynamic conversation variables.
                  default: []
                  items:
                    type: object
                    required:
                      - leadField
                      - elevenLabsVar
                    properties:
                      leadField:
                        type: string
                        minLength: 1
                      elevenLabsVar:
                        type: string
                        minLength: 1
            example:
              name: Auto Insurance Qualifier
              verticalId: b2c3d4e5-f6a7-8901-bcde-f12345678901
              llmModel: glm-4.5-air
              transferMode: conference
              maxDurationSeconds: 300
      responses:
        "201":
          description: |
            Agent created. `elevenLabsProvisioned` reports whether the ElevenLabs agent
            was created (`elevenLabsAgentId` set). When provisioning was attempted and
            failed, `elevenLabsProvisioned` is `false` and a human-readable `warning`
            explains that the admin should retry via Sync. When ElevenLabs is not
            configured at all, `elevenLabsProvisioned` is `false` with no `warning`.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - elevenLabsProvisioned
                properties:
                  data:
                    type: object
                    description: The created agent row.
                  elevenLabsProvisioned:
                    type: boolean
                    description: True when the ElevenLabs agent was created and `elevenLabsAgentId` stored.
                  warning:
                    type: string
                    description: Present only when provisioning was attempted and failed; prompts a Sync retry.
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Auto Insurance Qualifier
                  status: active
                  elevenLabsAgentId: agent_abc123
                elevenLabsProvisioned: true
        "400":
          description: Validation error (invalid or missing JSON body)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin, or superadmin without tenant context)
  /api/admin/ai-dialer/agents/{id}:
    get:
      summary: Get an AI Dialer agent
      description: Returns a single AI Dialer agent by id, scoped to the caller's tenant.
      operationId: getAiDialerAgent
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: AI Dialer agent
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Auto Insurance Qualifier
                  status: active
                  llmModel: glm-4.5-air
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Agent not found
    patch:
      summary: Update an AI Dialer agent
      description: |
        Updates an AI Dialer agent (partial — all fields optional). If the agent is provisioned on
        ElevenLabs, changes to name/voice/model/prompt/first message are synced there (best-effort).
        Setting `status` to `inactive` pauses all of the agent's active contacts (`cadenceStatus` set to
        `paused`, `lastOutcome` set to `agent_deactivated`).
      operationId: updateAiDialerAgent
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update). Same shape as agent create.
              properties:
                name:
                  type: string
                  minLength: 1
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                voiceId:
                  type: string
                  nullable: true
                llmModel:
                  type: string
                systemPrompt:
                  type: string
                  nullable: true
                firstMessage:
                  type: string
                  nullable: true
                transferMode:
                  type: string
                  enum:
                    - conference
                    - blind
                    - sip
                maxDurationSeconds:
                  type: integer
                  minimum: 30
                  maximum: 1800
                voicemailMessage:
                  type: string
                  nullable: true
                outboundTrackingNumberId:
                  type: string
                  format: uuid
                  nullable: true
                elevenLabsPhoneId:
                  type: string
                  nullable: true
                dynamicVariableMapping:
                  type: array
                  items:
                    type: object
                    required:
                      - leadField
                      - elevenLabsVar
                    properties:
                      leadField:
                        type: string
                        minLength: 1
                      elevenLabsVar:
                        type: string
                        minLength: 1
            example:
              name: Auto Insurance Qualifier (v2)
              status: inactive
      responses:
        "200":
          description: Updated agent
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Auto Insurance Qualifier (v2)
                  status: inactive
        "400":
          description: Validation error (invalid or missing JSON body)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Agent not found
    delete:
      summary: Delete (deactivate) an AI Dialer agent
      description: |
        Soft-deletes the agent by setting `status` to `inactive`, and pauses all of its active contacts
        (`cadenceStatus` set to `paused`, `lastOutcome` set to `agent_deactivated`). Returns the updated row.
      operationId: deleteAiDialerAgent
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Agent deactivated
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  status: inactive
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Agent not found
  /api/admin/ai-dialer/agents/{id}/regenerate-prompt:
    post:
      summary: Regenerate an agent's prompt from its vertical
      description: |
        Regenerates the agent's `systemPrompt`, `firstMessage`, and `dynamicVariableMapping` from the
        assigned vertical's fields, then persists them. The agent must have a `verticalId` assigned.
        No request body is required.
      operationId: regenerateAiDialerAgentPrompt
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Agent with regenerated prompt fields
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  systemPrompt: You are a qualification agent for Auto Insurance...
                  firstMessage: Hi, this is Sarah calling about your quote request.
                  dynamicVariableMapping:
                    - leadField: firstName
                      elevenLabsVar: first_name
        "400":
          description: Agent has no vertical assigned — cannot generate prompt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Agent (or its assigned vertical) not found
  /api/admin/ai-dialer/agents/{id}/sync:
    post:
      summary: Regenerate an agent's prompt and provision/push it to ElevenLabs
      description: |
        Regenerates the agent's `systemPrompt`, `firstMessage`, and `dynamicVariableMapping` from the
        assigned vertical's fields (including the configured `voicemailMode` and `voicemailMessage`) and
        persists them first (store-then-push, so the prompt survives an ElevenLabs failure). Then:
        when the agent already has an `elevenLabsAgentId`, the regenerated prompt is pushed to the remote
        ElevenLabs agent; when it does not, a fresh ElevenLabs agent is **provisioned** and the returned
        `agent_id` is stored. An ElevenLabs failure returns a 5xx error (the prompt is already persisted).
        The agent must have a `verticalId` assigned. No request body is required.
      operationId: syncAiDialerAgent
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: |
            Agent with regenerated prompt fields. `pushedToElevenLabs` indicates whether a remote
            create/update ran. `readiness` reports post-call state: `provisioned` (agent now has an
            `elevenLabsAgentId`) and `hasPhoneId` (an `elevenLabsPhoneId` is set — required for outbound calls).
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pushedToElevenLabs
                  - readiness
                properties:
                  data:
                    type: object
                    description: The agent row with regenerated prompt fields (and newly provisioned id, if any).
                  pushedToElevenLabs:
                    type: boolean
                    description: True when a remote ElevenLabs create or update call ran.
                  readiness:
                    type: object
                    required:
                      - provisioned
                      - hasPhoneId
                    properties:
                      provisioned:
                        type: boolean
                        description: True when the agent now has an `elevenLabsAgentId`.
                      hasPhoneId:
                        type: boolean
                        description: True when the agent has an `elevenLabsPhoneId`; false means outbound calls are skipped.
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  systemPrompt: You are a qualification agent for Auto Insurance...
                  firstMessage: Hi, this is Sarah calling about your quote request.
                  elevenLabsAgentId: agent_abc123
                pushedToElevenLabs: true
                readiness:
                  provisioned: true
                  hasPhoneId: false
        "400":
          description: Agent has no vertical assigned — cannot generate prompt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Agent (or its assigned vertical) not found
  /api/admin/ai-dialer/billing-status:
    get:
      summary: Get AI Dialer billing eligibility
      description: |
        Returns whether the current tenant can enable AI Dialer. A tenant is eligible if it is
        `billingExempt` or has a Stripe customer id (payment method). Superadmins are always eligible.
      operationId: getAiDialerBillingStatus
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Billing eligibility for the current tenant
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - canEnable
                    properties:
                      canEnable:
                        type: boolean
                      billingExempt:
                        type: boolean
                      hasPaymentMethod:
                        type: boolean
                      reason:
                        type: string
                        description: Present only when `canEnable` is false
              example:
                data:
                  canEnable: false
                  billingExempt: false
                  hasPaymentMethod: false
                  reason: A payment method is required to enable AI Dialer.
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
  /api/admin/ai-dialer/cadences:
    get:
      summary: List AI Dialer cadences
      description: Returns a paginated list of dialing cadences for the current tenant, newest first.
      operationId: listAiDialerCadences
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by cadence status (exact match). Typically `active` or `inactive`.
          schema:
            type: string
        - name: search
          in: query
          description: Filter by cadence name (case-insensitive partial match)
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of cadences
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: d4e5f6a7-b8c9-0123-def4-56789012abcd
                    name: Standard 3-Attempt
                    cadenceType: immediate
                    triggerAfterDays: null
                    maxAttempts: 3
                    attemptSchedule:
                      - attempt: 1
                        delayMinutes: 0
                        timeStart: 09:00
                        timeEnd: 20:00
                    daysOfWeek:
                      - mon
                      - tue
                      - wed
                      - thu
                      - fri
                    noAnswerAction: retry
                    busyAction: retry_short
                    voicemailAction: leave_message_retry
                    deadNumberAction: remove
                    dncRequestAction: remove
                    status: active
                    tenantId: c3d4e5f6-a7b8-9012-cdef-123456789012
                    createdAt: 2025-01-15T10:00:00.000Z
                    updatedAt: 2025-01-15T10:00:00.000Z
                pagination:
                  total: 2
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
    post:
      summary: Create an AI Dialer cadence
      description: |
        Creates a dialing cadence. When `cadenceType` is `call_assist`, `triggerAfterDays` is required
        (>= 1); it is forced to null for `immediate` cadences.
      operationId: createAiDialerCadence
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - attemptSchedule
              properties:
                name:
                  type: string
                  minLength: 1
                cadenceType:
                  type: string
                  enum:
                    - immediate
                    - call_assist
                  default: immediate
                triggerAfterDays:
                  type: integer
                  minimum: 1
                  maximum: 365
                  nullable: true
                  description: Required (>= 1) when `cadenceType` is `call_assist`; forced to null otherwise.
                maxAttempts:
                  type: integer
                  minimum: 1
                  maximum: 20
                  default: 3
                attemptSchedule:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                      - attempt
                      - delayMinutes
                      - timeStart
                      - timeEnd
                    properties:
                      attempt:
                        type: integer
                        minimum: 1
                      delayMinutes:
                        type: integer
                        minimum: 0
                      timeStart:
                        type: string
                        pattern: ^\d{2}:\d{2}$
                        example: 09:00
                      timeEnd:
                        type: string
                        pattern: ^\d{2}:\d{2}$
                        example: 20:00
                daysOfWeek:
                  type: array
                  minItems: 1
                  default:
                    - mon
                    - tue
                    - wed
                    - thu
                    - fri
                  items:
                    type: string
                    enum:
                      - mon
                      - tue
                      - wed
                      - thu
                      - fri
                      - sat
                      - sun
                noAnswerAction:
                  type: string
                  enum:
                    - retry
                    - stop
                  default: retry
                busyAction:
                  type: string
                  enum:
                    - retry_short
                    - retry_standard
                    - stop
                  default: retry_short
                voicemailAction:
                  type: string
                  enum:
                    - leave_message_retry
                    - hangup_retry
                    - hangup_stop
                  default: leave_message_retry
                deadNumberAction:
                  type: string
                  enum:
                    - remove
                    - retry_once
                  default: remove
                dncRequestAction:
                  type: string
                  enum:
                    - remove
                  default: remove
            example:
              name: Standard 3-Attempt
              cadenceType: immediate
              maxAttempts: 3
              attemptSchedule:
                - attempt: 1
                  delayMinutes: 0
                  timeStart: 09:00
                  timeEnd: 20:00
      responses:
        "201":
          description: Cadence created
          content:
            application/json:
              example:
                data:
                  id: d4e5f6a7-b8c9-0123-def4-56789012abcd
                  name: Standard 3-Attempt
                  maxAttempts: 3
        "400":
          description: Validation error (invalid body, or `triggerAfterDays` missing for a `call_assist` cadence)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin, or superadmin without tenant context)
  /api/admin/ai-dialer/cadences/{id}:
    get:
      summary: Get an AI Dialer cadence
      description: Returns a single dialing cadence by id, scoped to the caller's tenant.
      operationId: getAiDialerCadence
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Cadence
          content:
            application/json:
              example:
                data:
                  id: d4e5f6a7-b8c9-0123-def4-56789012abcd
                  name: Standard 3-Attempt
                  cadenceType: immediate
                  status: active
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Cadence not found
    patch:
      summary: Update an AI Dialer cadence
      description: |
        Updates a dialing cadence (partial — all fields optional; `status` may also be set). After merging
        with the existing record, if the effective `cadenceType` is `call_assist` then `triggerAfterDays`
        must be >= 1; otherwise `triggerAfterDays` is forced to null.
      operationId: updateAiDialerCadence
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update).
              properties:
                name:
                  type: string
                  minLength: 1
                cadenceType:
                  type: string
                  enum:
                    - immediate
                    - call_assist
                triggerAfterDays:
                  type: integer
                  minimum: 1
                  maximum: 365
                  nullable: true
                maxAttempts:
                  type: integer
                  minimum: 1
                  maximum: 20
                attemptSchedule:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                      - attempt
                      - delayMinutes
                      - timeStart
                      - timeEnd
                    properties:
                      attempt:
                        type: integer
                        minimum: 1
                      delayMinutes:
                        type: integer
                        minimum: 0
                      timeStart:
                        type: string
                        pattern: ^\d{2}:\d{2}$
                      timeEnd:
                        type: string
                        pattern: ^\d{2}:\d{2}$
                daysOfWeek:
                  type: array
                  minItems: 1
                  items:
                    type: string
                    enum:
                      - mon
                      - tue
                      - wed
                      - thu
                      - fri
                      - sat
                      - sun
                noAnswerAction:
                  type: string
                  enum:
                    - retry
                    - stop
                busyAction:
                  type: string
                  enum:
                    - retry_short
                    - retry_standard
                    - stop
                voicemailAction:
                  type: string
                  enum:
                    - leave_message_retry
                    - hangup_retry
                    - hangup_stop
                deadNumberAction:
                  type: string
                  enum:
                    - remove
                    - retry_once
                dncRequestAction:
                  type: string
                  enum:
                    - remove
                status:
                  type: string
                  enum:
                    - active
                    - inactive
            example:
              maxAttempts: 5
              status: inactive
      responses:
        "200":
          description: Updated cadence
          content:
            application/json:
              example:
                data:
                  id: d4e5f6a7-b8c9-0123-def4-56789012abcd
                  maxAttempts: 5
                  status: inactive
        "400":
          description: Validation error (invalid body, or `triggerAfterDays` missing for a `call_assist` cadence)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Cadence not found
    delete:
      summary: Delete (deactivate) an AI Dialer cadence
      description: Soft-deletes the cadence by setting `status` to `inactive`. Returns the updated row.
      operationId: deleteAiDialerCadence
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Cadence deactivated
          content:
            application/json:
              example:
                data:
                  id: d4e5f6a7-b8c9-0123-def4-56789012abcd
                  status: inactive
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Cadence not found
  /api/admin/ai-dialer/calls:
    get:
      summary: List AI Dialer calls
      description: |
        Returns a paginated list of outbound AI dialer call attempts for the current tenant, newest first.
        Each row includes the joined `agentName` and `contactToNumber`.
      operationId: listAiDialerCalls
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by call status (exact match), for example `pending`, `queued`, `calling`, `connected`,
            `transfer_pending`, `transferred`, `completed`, `failed`, `no_answer`, `busy`, `voicemail`, `dead`.
          schema:
            type: string
        - name: agentId
          in: query
          description: Filter by AI Dialer agent id
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          description: Filter by offer id
          schema:
            type: string
            format: uuid
        - name: contactId
          in: query
          description: Filter by AI Dialer contact id
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of AI Dialer calls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: e5f6a7b8-c9d0-1234-ef56-789012abcdef
                    contactId: f6a7b8c9-d0e1-2345-f678-90123abcdef0
                    attemptNumber: 1
                    agentId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    offerId: null
                    contractId: null
                    buyerId: null
                    trackingNumberId: null
                    toNumber: "+15551234567"
                    status: completed
                    elevenLabsConversationId: conv_abc123
                    transferCallId: null
                    scheduledAt: 2025-01-15T10:00:00.000Z
                    callStartedAt: 2025-01-15T10:00:05.000Z
                    callEndedAt: 2025-01-15T10:02:05.000Z
                    callDuration: 120
                    qualificationResult: qualified
                    summary: Lead confirmed interest and was transferred.
                    recordingUrl: https://example.com/rec.mp3
                    costMinutes: "2.0000"
                    costAmount: "0.5000"
                    tenantChargeAmount: "0.5000"
                    errorMessage: null
                    errorCode: null
                    tenantId: c3d4e5f6-a7b8-9012-cdef-123456789012
                    createdAt: 2025-01-15T10:00:00.000Z
                    updatedAt: 2025-01-15T10:02:05.000Z
                    agentName: Auto Insurance Qualifier
                    contactToNumber: "+15551234567"
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
  /api/admin/ai-dialer/calls/{id}:
    get:
      summary: Get an AI Dialer call
      description: |
        Returns a single AI dialer call by id, scoped to the caller's tenant. Includes joined
        `agentName`, `contactToNumber`, `contactLeadId`, `contactCadenceStatus`, and `contactCurrentAttempt`.
      operationId: getAiDialerCall
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: AI Dialer call with joined contact/agent fields
          content:
            application/json:
              example:
                data:
                  id: e5f6a7b8-c9d0-1234-ef56-789012abcdef
                  contactId: f6a7b8c9-d0e1-2345-f678-90123abcdef0
                  attemptNumber: 1
                  agentId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  toNumber: "+15551234567"
                  status: completed
                  callDuration: 120
                  transcript:
                    - role: agent
                      text: Hi, this is Sarah...
                  summary: Lead confirmed interest and was transferred.
                  agentName: Auto Insurance Qualifier
                  contactToNumber: "+15551234567"
                  contactLeadId: 0a1b2c3d-4e5f-6789-0abc-def123456789
                  contactCadenceStatus: completed
                  contactCurrentAttempt: 1
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
        "404":
          description: AI Dialer Call not found
  /api/admin/ai-dialer/config:
    get:
      summary: Get AI Dialer (ElevenLabs) config
      description: |
        Returns the tenant's ElevenLabs integration config. The API key is masked (last 4 chars only)
        and the webhook secret is masked as `***`. When no integration exists, returns defaults with
        `configured: false`.
      operationId: getAiDialerConfig
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: AI Dialer config for the current tenant
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - configured
                      - enabled
                      - concurrencyLimit
                    properties:
                      configured:
                        type: boolean
                      enabled:
                        type: boolean
                      apiKey:
                        type: string
                        nullable: true
                        description: Masked API key (last 4 chars, e.g. `...1234`) or null when unset.
                      defaultPhoneNumberId:
                        type: string
                        nullable: true
                      webhookSecret:
                        type: string
                        nullable: true
                        description: Masked as `***` when set, null otherwise.
                      concurrencyLimit:
                        type: integer
              example:
                data:
                  configured: true
                  enabled: true
                  apiKey: ...cd12
                  defaultPhoneNumberId: phnum_abc123
                  webhookSecret: "***"
                  concurrencyLimit: 10
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin, or superadmin without tenant context)
    put:
      summary: Upsert AI Dialer (ElevenLabs) config
      description: |
        Creates or updates the tenant's ElevenLabs integration (provider `elevenlabs`, category `ai_dialer`)
        and enables it. Returns the config view with the API key masked (last 4 chars) and webhook secret
        masked as `***`.
      operationId: updateAiDialerConfig
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - apiKey
              properties:
                apiKey:
                  type: string
                  minLength: 1
                  description: ElevenLabs API key (stored server-side, never returned in full).
                defaultPhoneNumberId:
                  type: string
                  nullable: true
                webhookSecret:
                  type: string
                  nullable: true
                concurrencyLimit:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 10
            example:
              apiKey: sk_elevenlabs_abcd1234
              defaultPhoneNumberId: phnum_abc123
              concurrencyLimit: 10
      responses:
        "200":
          description: Config saved (masked view)
          content:
            application/json:
              example:
                data:
                  configured: true
                  enabled: true
                  apiKey: ...1234
                  defaultPhoneNumberId: phnum_abc123
                  webhookSecret: null
                  concurrencyLimit: 10
        "400":
          description: Validation error (invalid or missing JSON body, or missing `apiKey`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin, or superadmin without tenant context)
  /api/admin/ai-dialer/pricing:
    get:
      summary: Get AI Dialer pricing
      description: |
        Returns the public per-minute AI Dialer rate from the system-level (tenantId null) ElevenLabs
        integration config. When the system integration is missing or disabled, returns
        `{ enabled: false, ratePerMinute: 0 }`. No secret fields are exposed.
      operationId: getAiDialerPricing
      tags:
        - "Admin: AI Dialer"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: AI Dialer pricing
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - enabled
                      - ratePerMinute
                    properties:
                      enabled:
                        type: boolean
                      ratePerMinute:
                        type: number
                        description: USD per minute charged to the tenant. Defaults to 0.25 when enabled but unset.
              example:
                data:
                  enabled: true
                  ratePerMinute: 0.25
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (caller is not an admin or superadmin)
  /api/admin/billing/audit:
    get:
      summary: Run billing audit
      description: |
        Runs the billing integrity audit and returns a paginated list of detected issues
        (ledger drift, orphaned SKUs, missing Stripe pointers, state mismatches, etc.).
        Output is redacted before return. Pagination is applied in memory after the audit runs.

        Tenant admins are always scoped to their own tenant. Superadmins may pass a `tenantId`
        query param to scope to a specific tenant; omitting it audits across all tenants
        (their `auth.tenantId` is null).
      operationId: runBillingAudit
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
        - adminApiKey: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: tenantId
          in: query
          description: Superadmin-only. Scope the audit to a single tenant. Ignored for tenant admins.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Paginated list of billing audit issues
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BillingAuditIssue"
              example:
                data:
                  - code: buyer_sku_funding_remaining_drift
                    severity: critical
                    tenantId: 3f9a1c22-8b1e-4a77-9c0d-1e2f3a4b5c6d
                    buyerId: 8c2d4e6f-0a1b-2c3d-4e5f-6a7b8c9d0e1f
                    buyerSkuId: 5a4b3c2d-1e0f-9a8b-7c6d-5e4f3a2b1c0d
                    details:
                      expected: 100
                      actual: 98
                pagination:
                  total: 3
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required (non-superadmin without a resolved tenant)
          content:
            application/json:
              example:
                error:
                  code: tenant_required
                  message: Tenant context is required
                  details:
                    route: GET /api/admin/billing/audit
  /api/admin/billing/reconciliation:
    get:
      summary: Run billing reconciliation
      description: |
        Runs the reconciliation dashboard: compares SKU ledgers, payments, and Stripe pointers
        and returns a summary plus a paginated slice of the underlying issues. Unlike
        `/billing/audit`, the response `data` is an object (`{ summary, issues }`) while
        `pagination` describes the `issues` slice.

        Tenant admins are scoped to their own tenant; superadmins may pass `tenantId`, or omit
        it to reconcile across all tenants.
      operationId: runBillingReconciliation
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
        - adminApiKey: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: tenantId
          in: query
          description: Superadmin-only. Scope reconciliation to a single tenant.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Reconciliation summary plus a paginated slice of issues
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: object
                    required:
                      - summary
                      - issues
                    properties:
                      summary:
                        $ref: "#/components/schemas/BillingReconciliationSummary"
                      issues:
                        type: array
                        items:
                          $ref: "#/components/schemas/BillingAuditIssue"
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
              example:
                data:
                  summary:
                    totalIssues: 2
                    criticalIssues: 1
                    warningIssues: 1
                    infoIssues: 0
                    totalBuyerSkus: 42
                    totalPayments: 88
                    activeFillableLeads: 1200
                    ledgerDriftIssues: 1
                    missingStripeChargeIds: 0
                    duplicateStripePointers: 0
                    stateMismatches: 1
                    latestCheckedAt: 2026-07-02T10:00:00.000Z
                  issues:
                    - code: balance_drift
                      severity: critical
                      buyerId: 8c2d4e6f-0a1b-2c3d-4e5f-6a7b8c9d0e1f
                      details:
                        expected: "500.00"
                        actual: "480.00"
                pagination:
                  total: 2
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required (non-superadmin without a resolved tenant)
  /api/admin/billing/repair/preview:
    get:
      summary: Preview a billing repair
      description: |
        Returns a dry-run plan for a named billing repair without applying any changes.
        Superadmin only — billing repairs are a cross-tenant global tool, so tenant admins
        receive a 403 from the `withSuperAdmin` wrapper.
      operationId: previewBillingRepair
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - name: repairId
          in: query
          required: true
          description: Identifier of the repair routine to preview.
          schema:
            type: string
      responses:
        "200":
          description: Repair preview plan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    description: Repair-specific preview payload (planned corrections, affected rows, projected deltas).
                    additionalProperties: true
        "400":
          description: repairId missing
          content:
            application/json:
              example:
                error:
                  code: repair_id_required
                  message: repairId is required
                  details:
                    route: GET /api/admin/billing/repair/preview
        "401":
          description: Unauthorized
        "403":
          description: Superadmin access required
  /api/admin/billing/stripe-events:
    get:
      summary: List Stripe webhook events
      description: |
        Returns a paginated list of received Stripe webhook events with their processing
        lifecycle (status, attempt count, timing, last error, handler result). Ordered by
        `processedAt` descending. Sensitive fields in `lastError`/`handlerResult` are sanitized.

        Tenant admins are scoped to their own tenant. Superadmins see all events and may filter
        by `tenantId`.
      operationId: listStripeEvents
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
        - adminApiKey: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: status
          in: query
          description: Filter by processing status (e.g. `processed`, `failed`, `failed_retryable`).
          schema:
            type: string
        - name: type
          in: query
          description: Filter by Stripe event type (e.g. `invoice.paid`, `checkout.session.completed`).
          schema:
            type: string
        - name: tenantId
          in: query
          description: Superadmin-only. Filter events by tenant.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Paginated list of Stripe event lifecycle views
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/StripeEventLifecycleView"
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required (non-superadmin without a resolved tenant)
  /api/admin/billing/stripe-events/{id}:
    get:
      summary: Get a Stripe webhook event
      description: |
        Returns a single Stripe webhook event lifecycle view by id, including a `replayPreview`
        describing whether the event is safe to replay. Tenant admins are scoped to their own
        tenant; superadmins may fetch any event. Uses inline auth (admin or superadmin) rather
        than the `withAdmin` wrapper.
      operationId: getStripeEvent
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
        - adminApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          description: Stripe event id (internal UUID).
          schema:
            type: string
      responses:
        "200":
          description: Stripe event lifecycle view
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/StripeEventLifecycleView"
        "401":
          description: Unauthorized
        "403":
          description: Admin access or tenant context required
        "404":
          description: Stripe event not found
          content:
            application/json:
              example:
                error:
                  code: stripe_event_not_found
                  message: Stripe event not found
                  details:
                    route: GET /api/admin/billing/stripe-events/[id]
                    id: 5a4b3c2d-1e0f-9a8b-7c6d-5e4f3a2b1c0d
  /api/admin/settings/billing:
    get:
      summary: Get tenant billing settings
      description: |
        Returns the current tenant's effective platform fee. `platformFeeBps` is the resolved
        rate (per-tenant override → system default → 500 bps) and `platformFeeIsOverride`
        indicates whether a per-tenant override is set. Read-only for tenant admins — the fee
        is editable only by superadmin via the superadmin billing routes.
      operationId: getTenantBillingSettings
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
        - adminApiKey: []
      responses:
        "200":
          description: Tenant billing settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required:
                      - platformFeeBps
                      - platformFeeIsOverride
                    properties:
                      platformFeeBps:
                        type: integer
                        description: Effective platform fee in basis points (500 = 5%).
                        example: 500
                      platformFeeIsOverride:
                        type: boolean
                        description: True when a per-tenant override is set on the tenant row.
        "401":
          description: Unauthorized
        "403":
          description: Admin access or tenant context required
        "404":
          description: Tenant not found
    put:
      summary: Update tenant billing settings
      description: |
        No-op update endpoint kept for wire stability. Tenant admins currently have no editable
        billing fields — the platform fee is superadmin-only. The request body is validated
        against an empty schema (extra keys ignored) and the response echoes the current
        effective platform fee, unchanged.
      operationId: updateTenantBillingSettings
      tags:
        - "Admin: Billing"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Empty object. Reserved for future tenant-admin-editable billing fields.
              additionalProperties: true
            example: {}
      responses:
        "200":
          description: Current effective platform fee (unchanged)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      platformFeeBps:
                        type: integer
                        example: 500
                      platformFeeIsOverride:
                        type: boolean
        "400":
          description: Body is not valid JSON, or validation error
        "401":
          description: Unauthorized
        "403":
          description: Admin access or tenant context required
        "404":
          description: Tenant not found
  /api/admin/credits/balance:
    get:
      summary: Get tenant credit balance
      description: |
        Returns the current tenant's credit balance, active subscription plan, auto-recharge
        configuration, subscription status, and tenant billing flags. Credit values are decimal
        strings (≤4 dp, numeric(12,4)). `totalCredits` = `creditBalance` + `planCreditBalance`.
      operationId: getCreditBalance
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
        - adminApiKey: []
      responses:
        "200":
          description: Tenant credit balance and subscription state
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/CreditBalance"
              example:
                data:
                  creditBalance: "1500"
                  planCreditBalance: "500.5"
                  totalCredits: "2000.5"
                  plan:
                    id: 7a6b5c4d-3e2f-1a0b-9c8d-7e6f5a4b3c2d
                    name: Growth
                    monthlyPriceUsd: 29900
                    includedCredits: 5000
                    creditDiscountPercent: 10
                    unlimitedServices: []
                    entityLimits: {}
                  autoRecharge:
                    enabled: true
                    threshold: "200"
                    mode: package
                    target: null
                    packageId: 1b2c3d4e-5f6a-7b8c-9d0e-1f2a3b4c5d6e
                    stripePaymentMethodId: pm_123
                  subscription:
                    stripeSubscriptionId: sub_123
                    status: active
                    currentPeriodStart: 2026-06-01T00:00:00.000Z
                    currentPeriodEnd: 2026-07-01T00:00:00.000Z
                    hasUsedTrial: true
                  billingExempt: false
                  billingMode: self
                  stripeCustomerId: cus_123
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required
  /api/admin/credits/packages:
    get:
      summary: List credit packages
      description: |
        Returns active credit packages with pricing after applying the combined package and
        current-plan credit discount. Prices are integer USD cents. `effectivePrice` and
        `discountedPriceUsd` are the discounted price; both are the same value.
      operationId: listCreditPackages
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
        - adminApiKey: []
      responses:
        "200":
          description: Active credit packages with effective pricing
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CreditPackage"
              example:
                data:
                  - id: 1b2c3d4e-5f6a-7b8c-9d0e-1f2a3b4c5d6e
                    name: Starter Pack
                    credits: 1000
                    priceUsd: 10000
                    discountPercent: 0
                    planDiscountPercent: 10
                    effectivePrice: 9000
                    discountedPriceUsd: 9000
                    sortOrder: 0
                    status: active
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required
  /api/admin/credits/plans:
    get:
      summary: List subscription plans
      description: |
        Returns active subscription plans. `monthlyPriceUsd` is integer USD cents,
        `creditDiscountPercent` is a percent number, and `includedCredits`/`trialCredits`
        are integer credit counts. Available to any admin (not tenant-scoped).
      operationId: listSubscriptionPlans
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
        - adminApiKey: []
      responses:
        "200":
          description: Active subscription plans
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/SubscriptionPlan"
              example:
                data:
                  - id: 7a6b5c4d-3e2f-1a0b-9c8d-7e6f5a4b3c2d
                    name: Growth
                    monthlyPriceUsd: 29900
                    includedCredits: 5000
                    creditDiscountPercent: 10
                    unlimitedServices: []
                    entityLimits: {}
                    trialDays: 14
                    trialCredits: 100
                    requiresCard: true
                    sortOrder: 1
        "401":
          description: Unauthorized
  /api/admin/credits/transactions:
    get:
      summary: List credit ledger transactions
      description: |
        Returns a paginated, tenant-scoped slice of the immutable credit ledger, ordered by
        `createdAt` descending. `credits` and `balanceAfter` are decimal credit strings
        (≤4 dp); `amountUsd`, when present, is integer USD cents. Internal platform-cost
        fields are never included in this tenant-facing response.
      operationId: listCreditTransactions
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
        - adminApiKey: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: type
          in: query
          description: Filter by ledger entry type.
          schema:
            type: string
            enum:
              - purchase
              - deduction
              - platform_deduction
              - refund
              - adjustment
              - auto_recharge
              - plan_reset
        - name: service
          in: query
          description: Filter by service that consumed/added credits.
          schema:
            type: string
        - name: startDate
          in: query
          description: Filter to entries created on or after this timestamp.
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          description: Filter to entries created on or before this timestamp.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Paginated credit ledger
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/CreditTransaction"
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required
  /api/admin/credits/auto-recharge:
    get:
      summary: Get auto-recharge configuration
      description: |
        Returns the tenant's auto-recharge configuration: enabled flag, threshold, mode
        (`package` or `target`), target balance, selected package, and saved payment method.
        Returns defaults (disabled, package mode) when no credit balance row exists yet.
      operationId: getAutoRecharge
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
        - adminApiKey: []
      responses:
        "200":
          description: Current auto-recharge configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AutoRechargeConfig"
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required
    put:
      summary: Update auto-recharge configuration
      description: |
        Enables or disables automatic credit top-ups. When `enabled` is true, `threshold`
        (positive decimal credit string, ≤4 dp) is required. In `package` mode (default) `packageId`
        (an active credit package) is required. In `target` mode `target` is required —
        a decimal credit-balance string at least `threshold + 2000` ($20 minimum charge; 1 credit
        = $0.01) — and `packageId` is ignored; each recharge charges the card for exactly
        `target − current balance` cents. Larger target-mode top-ups earn bonus credits from
        the discount of the largest active package priced at or below the charge amount.
        `stripePaymentMethodId` is persisted only when provided in the request. Creates the
        tenant credit balance row if it does not yet exist.
      operationId: updateAutoRecharge
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - enabled
              properties:
                enabled:
                  type: boolean
                threshold:
                  type: string
                  pattern: ^-?\d+(\.\d{1,4})?$
                  nullable: true
                  description: Credit balance at which auto-recharge triggers — positive decimal string, ≤4 dp. Required when enabling.
                mode:
                  type: string
                  enum:
                    - package
                    - target
                  default: package
                  description: package = fixed-package top-up; target = variable top-up-to-target charge.
                target:
                  type: string
                  pattern: ^-?\d+(\.\d{1,4})?$
                  nullable: true
                  description: Target balance in credits — decimal string, ≤4 dp. Required in target mode; must be ≥ threshold + 2000.
                packageId:
                  type: string
                  nullable: true
                  description: Credit package to purchase on trigger. Required when enabling in package mode; ignored in target mode.
                stripePaymentMethodId:
                  type: string
                  nullable: true
                  description: Saved Stripe payment method to charge. Persisted only when present.
            example:
              enabled: true
              threshold: "1000"
              mode: target
              target: "25000"
      responses:
        "200":
          description: Updated auto-recharge configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AutoRechargeConfig"
        "400":
          description: Validation error (missing threshold, missing packageId in package mode, missing or below-floor target in
            target mode, or inactive package)
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required
  /api/admin/credits/purchase:
    post:
      summary: Purchase a credit package
      description: |
        Creates a Stripe Checkout session (mode `payment`) for a one-time credit package
        purchase and returns the checkout URL. Requires the `permBilling` permission. Creates a
        Stripe customer for the tenant if one does not exist, and records a pending
        `checkoutSession` row (best-effort). Credits are granted by the Stripe webhook on
        payment completion, not by this route.
      operationId: purchaseCredits
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - packageId
              properties:
                packageId:
                  type: string
                  minLength: 1
                  description: Active credit package to purchase.
            example:
              packageId: 1b2c3d4e-5f6a-7b8c-9d0e-1f2a3b4c5d6e
      responses:
        "200":
          description: Stripe Checkout session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      checkoutUrl:
                        type: string
                        format: uri
                        nullable: true
              example:
                data:
                  checkoutUrl: https://checkout.stripe.com/c/pay/cs_test_123
        "400":
          description: Validation error, package not found/inactive, or Stripe customer creation failed
        "401":
          description: Unauthorized
        "403":
          description: Tenant context or billing permission required
        "404":
          description: Tenant not found
  /api/admin/credits/subscribe:
    post:
      summary: Subscribe to a plan
      description: |
        Creates a Stripe Checkout session (mode `subscription`) to subscribe the tenant to a
        plan and returns the checkout URL. Requires the `permBilling` permission. Rejects when
        the tenant already has an active/trialing/past_due subscription (use change-plan), when
        the trial was already used, or when a priced plan lacks a Stripe price. Records a pending
        `checkoutSession` row and an audit log entry.
      operationId: subscribeToPlan
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - planId
              properties:
                planId:
                  type: string
                  minLength: 1
                  description: Active subscription plan to subscribe to.
            example:
              planId: 7a6b5c4d-3e2f-1a0b-9c8d-7e6f5a4b3c2d
      responses:
        "200":
          description: Stripe Checkout subscription session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      checkoutUrl:
                        type: string
                        format: uri
                        nullable: true
        "400":
          description: Plan not found/inactive, already subscribed, trial already used, or plan missing Stripe price
        "401":
          description: Unauthorized
        "403":
          description: Tenant context or billing permission required
  /api/admin/credits/change-plan:
    post:
      summary: Change subscription plan
      description: |
        Switches the tenant's existing Stripe subscription to a different plan with prorated
        billing (`create_prorations`). Requires the `permBilling` permission and an existing
        active/trialing/past_due subscription. Rejects when the target plan is missing, inactive,
        lacks a Stripe price, or matches the current plan. Records an audit log entry.
      operationId: changeSubscriptionPlan
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - planId
              properties:
                planId:
                  type: string
                  minLength: 1
                  description: Target subscription plan.
            example:
              planId: 7a6b5c4d-3e2f-1a0b-9c8d-7e6f5a4b3c2d
      responses:
        "200":
          description: Plan changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      planId:
                        type: string
                        format: uuid
                      planName:
                        type: string
                      stripeSubscriptionId:
                        type: string
        "400":
          description: Plan not found/inactive, missing Stripe price, no active subscription, already on plan, or no subscription
            items
        "401":
          description: Unauthorized
        "403":
          description: Tenant context or billing permission required
  /api/admin/credits/cancel-plan:
    post:
      summary: Cancel subscription
      description: |
        Schedules the tenant's Stripe subscription to cancel at the end of the current period
        (`cancel_at_period_end`) and sets the local subscription status to `canceling`. Requires
        the `permBilling` permission and an active/trialing/past_due subscription. Takes no
        request body. Records an audit log entry.
      operationId: cancelSubscription
      tags:
        - "Admin: Credits"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Subscription scheduled to cancel at period end
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      cancelAtPeriodEnd:
                        type: boolean
                      subscriptionStatus:
                        type: string
                        example: canceling
                      currentPeriodEnd:
                        type: string
                        format: date-time
                        nullable: true
        "400":
          description: No active subscription to cancel
        "401":
          description: Unauthorized
        "403":
          description: Tenant context or billing permission required
  /api/admin/campaigns/{id}/call-readiness:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    get:
      summary: Get campaign call readiness
      description: |
        Read-only diagnostic that mirrors the inbound-call runtime gates and reports whether a
        call to this campaign could complete end-to-end. Each check corresponds to a gate that
        would actually drop the call at runtime, so a `ready` verdict never contradicts routing.

        Checks (in order): `campaign_active`, `tracking_number`, `offer_linked`,
        `telephony_credential`, `contracts_call_capable`, `contract_schedule`, `funding`, `caps`,
        and — only when campaign DNI is enabled — `dni_pool`. Each check reports `pass`, `warn`,
        or `fail`. The overall `status` is `blocked` if any check fails, `issues` if any warns,
        else `ready`.
      operationId: getCampaignCallReadiness
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Call readiness verdict with per-gate checks
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - status
                      - checks
                    properties:
                      status:
                        type: string
                        enum:
                          - ready
                          - issues
                          - blocked
                        description: Aggregate verdict. `fail` dominates `warn` dominates `pass`.
                      checks:
                        type: array
                        items:
                          type: object
                          required:
                            - key
                            - label
                            - status
                          properties:
                            key:
                              type: string
                              description: Stable check identifier, e.g. `campaign_active`, `tracking_number`, `funding`.
                            label:
                              type: string
                              description: Human-readable check label.
                            status:
                              type: string
                              enum:
                                - pass
                                - warn
                                - fail
                            message:
                              type: string
                              description: Explanation, present on non-pass checks (and some informational passes).
              example:
                data:
                  status: ready
                  checks:
                    - key: campaign_active
                      label: Campaign active with calls enabled
                      status: pass
                    - key: tracking_number
                      label: Tracking number assigned
                      status: pass
                    - key: offer_linked
                      label: Active offer linked
                      status: pass
                    - key: telephony_credential
                      label: Telephony provider configured
                      status: pass
                      message: Platform-managed twilio credentials
                    - key: contracts_call_capable
                      label: Call-capable contracts
                      status: pass
                    - key: contract_schedule
                      label: Within schedule now
                      status: pass
                    - key: funding
                      label: Funding available
                      status: pass
                    - key: caps
                      label: Daily caps have headroom
                      status: pass
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/routing-readiness:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    get:
      summary: Get campaign lead-routing readiness
      description: |
        Read-only diagnostic that mirrors the lead-routing runtime gates (submit route →
        process-lead → routing engine) and reports whether a lead posted to this campaign
        right now could route and sell end-to-end. Each check corresponds to a gate that
        would actually drop the lead at runtime, so a `ready` verdict never contradicts routing.

        Checks (in order): `campaign_active`, `partner_posting_key`, `platform_credits`,
        `campaign_caps`, `offer_linked`, `contracts_eligible`, `contract_schedule`, `funding`,
        `pricing`, `delivery_config`, `contract_caps`. Each check reports `pass`, `warn`, or
        `fail`. The overall `status` is `blocked` if any check fails, `issues` if any warns,
        else `ready`. Lead-data-level gates (TCPA, suppression, filters, dedup, quality floor)
        are out of scope — they need a concrete lead.
      operationId: getCampaignRoutingReadiness
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Routing readiness verdict with per-gate checks
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - status
                      - checks
                    properties:
                      status:
                        type: string
                        enum:
                          - ready
                          - issues
                          - blocked
                        description: Aggregate verdict. `fail` dominates `warn` dominates `pass`.
                      checks:
                        type: array
                        items:
                          type: object
                          required:
                            - key
                            - label
                            - status
                          properties:
                            key:
                              type: string
                              description: Stable check identifier, e.g. `campaign_active`, `contracts_eligible`, `funding`.
                            label:
                              type: string
                              description: Human-readable check label.
                            status:
                              type: string
                              enum:
                                - pass
                                - warn
                                - fail
                            message:
                              type: string
                              description: Explanation, present on non-pass checks (and some informational passes).
              example:
                data:
                  status: issues
                  checks:
                    - key: campaign_active
                      label: Campaign active
                      status: pass
                    - key: partner_posting_key
                      label: Partner posting key
                      status: pass
                    - key: platform_credits
                      label: Platform credits
                      status: pass
                    - key: campaign_caps
                      label: Campaign caps have headroom
                      status: pass
                    - key: offer_linked
                      label: Active offer linked
                      status: pass
                    - key: contracts_eligible
                      label: Eligible contracts
                      status: pass
                    - key: contract_schedule
                      label: Within schedule now
                      status: warn
                      message: All eligible contracts are outside their configured hours right now
                    - key: funding
                      label: Funding available
                      status: pass
                    - key: pricing
                      label: Pricing resolves revenue
                      status: pass
                    - key: delivery_config
                      label: Delivery configured
                      status: pass
                    - key: contract_caps
                      label: Contract caps have headroom
                      status: pass
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/dni-pool/release:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    post:
      summary: Release reserved DNI pool numbers
      description: |
        Force-releases the DNI pool numbers currently leased to active impressions
        for this campaign, without waiting for TTL/cron. Each released lease flips
        its impression to `expired`, marks the number `available`, clears its
        campaign/offer association, and strips the `dniPool` tag. Omit
        `trackingNumberId` to release every active reservation, or provide it to
        release a single leased number. Numbers owned via `autoBuyCampaignId` stay
        campaign-owned — only the lease is released.
      operationId: releaseCampaignDniPool
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                trackingNumberId:
                  type: string
                  format: uuid
                  description: Optional — scope the release to a single leased number.
      responses:
        "200":
          description: Release counts
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - releasedNumbers
                      - expiredImpressions
                    properties:
                      releasedNumbers:
                        type: integer
                      expiredImpressions:
                        type: integer
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/clone:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Source campaign UUID
    post:
      summary: Clone a campaign
      description: |
        Creates a copy of the source campaign under a target partner. Copies campaign settings,
        custom fields, offer links, and call billing rules. The clone is created with status
        `testing` and no posting key (regenerated on activation via the existing flow).
      operationId: cloneCampaign
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerId
                - name
              properties:
                partnerId:
                  type: string
                  format: uuid
                  description: Partner the cloned campaign is assigned to.
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                  description: Name for the new campaign.
            example:
              partnerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
              name: Solar Leads - Facebook Q2 (copy)
      responses:
        "200":
          description: Cloned campaign
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      displayId:
                        type: string
                      name:
                        type: string
              example:
                data:
                  id: c3d4e5f6-a7b8-9012-cdef-234567890123
                  displayId: "1042"
                  name: Solar Leads - Facebook Q2 (copy)
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/posting-spec/email:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    post:
      summary: Email the campaign posting spec link
      description: |
        Emails the public posting-spec link to selected partner-portal users and/or
        manually-entered addresses. Selected `userIds` are resolved to emails and dropped unless
        they belong to this campaign's partner (via `user.partnerId` or an active `member.partnerId`).
        Recipients are de-duplicated case-insensitively. A spec token is auto-generated if the
        campaign has none. Returns `400` if no valid recipients resolve or every send fails.
      operationId: emailCampaignPostingSpec
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: At least one of `userIds` or `manualEmails` must be non-empty.
              properties:
                userIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                  default: []
                  description: Partner-portal user UUIDs. Non-matching users are silently dropped.
                manualEmails:
                  type: array
                  items:
                    type: string
                    format: email
                  default: []
                customMessage:
                  type: string
                  maxLength: 2000
                  description: Optional message included in the email body.
            example:
              userIds:
                - d4e5f6a7-b8c9-0123-def4-567890123456
              manualEmails:
                - partner-ops@example.com
              customMessage: Here is the posting spec for your new campaign.
      responses:
        "200":
          description: Send result. `sent` counts successful sends; `failed` lists per-address failures.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - sent
                      - failed
                    properties:
                      sent:
                        type: integer
                      failed:
                        type: array
                        items:
                          type: object
                          properties:
                            email:
                              type: string
                            error:
                              type: string
              example:
                data:
                  sent: 2
                  failed: []
        "400":
          description: Validation error, no valid recipients resolved, or all emails failed to send.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/posting-spec/recipients:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    get:
      summary: List posting-spec email recipients
      description: |
        Lists active, non-banned partner-portal users who can receive the public posting-spec
        link for this campaign — both direct partner users (`user.partnerId`) and active members
        linked to the partner (`member.partnerId`). Results are de-duplicated by user and sorted
        by display name.
      operationId: listCampaignPostingSpecRecipients
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Candidate recipients
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                        - id
                        - email
                        - name
                      properties:
                        id:
                          type: string
                          format: uuid
                        email:
                          type: string
                          format: email
                        name:
                          type: string
                          description: Display name, falling back to first/last name then email.
              example:
                data:
                  - id: d4e5f6a7-b8c9-0123-def4-567890123456
                    email: partner-ops@example.com
                    name: Partner Ops
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/spec-token:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    post:
      summary: Rotate the campaign posting-spec token
      description: |
        Generates a new public posting-spec token for the campaign, invalidating any previously
        shared spec link. Returns the new token value.
      operationId: rotateCampaignSpecToken
      tags:
        - Campaigns
      security:
        - sessionCookie: []
      responses:
        "200":
          description: New spec token
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - specToken
                    properties:
                      specToken:
                        type: string
                        format: uuid
              example:
                data:
                  specToken: 9f8e7d6c-5b4a-3210-9876-543210fedcba
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/webhooks:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
    get:
      summary: List campaign webhooks
      description: Returns a paginated list of webhooks scoped to this campaign, newest first.
      operationId: listCampaignWebhooks
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated webhooks
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      summary: Create a campaign webhook
      description: |
        Creates a webhook scoped to this campaign. `GET`/`POST` methods require a `url`;
        the preset integrations `GOOGLE_ADS`, `FACEBOOK_CAPI`, and `AXON` build their endpoint
        server-side and require their respective config object instead. Campaign-scoped webhook
        delivery tokens are filtered to a partner-safe allowlist.
      operationId: createCampaignWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - events
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                  description: Delivery endpoint. Required for `GET`/`POST`; omitted for preset integrations.
                events:
                  type: array
                  minItems: 1
                  items:
                    $ref: "#/components/schemas/ScopedWebhookEvent"
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                    - AXON
                  default: GET
                status:
                  $ref: "#/components/schemas/WebhookStatus"
                  default: active
                googleAdsConfig:
                  type: object
                  description: Required when `method=GOOGLE_ADS`.
                  nullable: true
                facebookCapiConfig:
                  type: object
                  description: Required when `method=FACEBOOK_CAPI`.
                  nullable: true
                axonConfig:
                  type: object
                  description: Required when `method=AXON`.
                  nullable: true
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                  nullable: true
                filters:
                  description: Optional filters evaluated against delivery tokens before firing. Either a flat AND array or a `{ logic,
                    rules }` object.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
            example:
              name: Buyer CRM notify
              url: https://buyer.example/webhook?lead={Lead_ID}
              events:
                - lead.sold
              method: GET
              status: active
      responses:
        "201":
          description: Webhook created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/webhooks/{webhookId}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get a campaign webhook
      description: Returns a single webhook by ID within the caller's tenant.
      operationId: getCampaignWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Webhook detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a campaign webhook
      description: |
        Partial update of a campaign webhook. Reactivating a paused webhook (`status`
        `paused` → `active`) resets its failure count. Switching `method` auto-adjusts the
        content type and default required tokens. The `AXON` method is not accepted on update.
      operationId: updateCampaignWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update).
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  items:
                    $ref: "#/components/schemas/ScopedWebhookEvent"
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                status:
                  $ref: "#/components/schemas/WebhookStatus"
                googleAdsConfig:
                  type: object
                  nullable: true
                facebookCapiConfig:
                  type: object
                  nullable: true
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                  nullable: true
                filters:
                  description: Optional filters evaluated against delivery tokens before firing. Either a flat AND array or a `{ logic,
                    rules }` object.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
            example:
              status: paused
      responses:
        "200":
          description: Updated webhook
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a campaign webhook
      description: Permanently deletes the webhook. Returns the deleted row.
      operationId: deleteCampaignWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Deleted webhook
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/webhooks/{webhookId}/deliveries:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List campaign webhook deliveries
      description: |
        Returns a paginated delivery log for the webhook, newest first, enriched with lead
        short ID, vertical, and offer. Optionally filter by `verticalId` or `offerId`.
      operationId: listCampaignWebhookDeliveries
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: verticalId
          in: query
          description: Filter deliveries to leads in this vertical.
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          description: Filter deliveries to leads in this offer.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Paginated deliveries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/campaigns/{id}/webhooks/{webhookId}/deliveries/{deliveryId}/refire:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: deliveryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Refire a campaign webhook delivery
      description: |
        Re-sends a previous webhook delivery for its original lead/sale, rebuilding tokens from
        current data and logging a new delivery row (event `refired:<original>`). Test deliveries
        (no lead) and skipped deliveries cannot be refired. Returns `422` when the underlying
        lead/sale no longer exists or a required integration (Google Ads / Facebook CAPI) is
        not configured for the tenant.
      operationId: refireCampaignWebhookDelivery
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Refire result
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      deliveryId:
                        type: string
                        format: uuid
                        description: ID of the newly logged delivery row.
                      resolvedUrl:
                        type: string
                      responseStatus:
                        type: integer
                        nullable: true
                      status:
                        type: string
                        enum:
                          - success
                          - failed
                      durationMs:
                        type: integer
              example:
                data:
                  deliveryId: e5f6a7b8-c9d0-1234-ef56-7890abcdef12
                  resolvedUrl: https://buyer.example/webhook?lead=SAMPLE-001
                  responseStatus: 200
                  status: success
                  durationMs: 342
        "400":
          description: Cannot refire a test or skipped delivery.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Lead/sale no longer exists, or the required integration is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/campaigns/{id}/webhooks/{webhookId}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Campaign UUID
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Test fire a campaign webhook
      description: |
        Fires the webhook with sample tokens and logs the result as a `test` delivery. If the
        webhook's filters do not match the sample tokens, the send is skipped (status `skipped`).
        For `GOOGLE_ADS` the call is dispatched with `validateOnly=true`; for `FACEBOOK_CAPI` a
        test event code is used. Returns `422` when a preset integration is not configured for
        the tenant.
      operationId: testCampaignWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Test result (or skip notice when filters do not match sample tokens)
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      resolvedUrl:
                        type: string
                      tokens:
                        type: object
                        additionalProperties:
                          type: string
                      method:
                        type: string
                      originalUrl:
                        type: string
                        nullable: true
                      responseStatus:
                        type: integer
                        nullable: true
                      responseBody:
                        type: string
                      durationMs:
                        type: integer
                      status:
                        type: string
                        enum:
                          - success
                          - failed
                          - skipped
                  message:
                    type: string
                    description: Present when the test was skipped.
              example:
                data:
                  resolvedUrl: https://buyer.example/webhook?lead=SAMPLE-001
                  method: GET
                  originalUrl: https://buyer.example/webhook?lead={Lead_ID}
                  responseStatus: 200
                  responseBody: OK
                  durationMs: 210
                  status: success
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Required preset integration (Google Ads / Facebook CAPI) not configured for the tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/email/inherit:
    get:
      summary: Get SMTP inheritance setting
      description: Returns whether the tenant inherits the platform's system SMTP account for outbound email, along with the
        system default SMTP identity (if one is configured).
      operationId: getEmailInherit
      tags:
        - Settings
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Current inheritance setting and system SMTP identity
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - inheritSmtp
                    properties:
                      inheritSmtp:
                        type: boolean
                        description: When true, the tenant sends via the platform system SMTP account instead of its own.
                      systemSmtpEmail:
                        type: string
                        nullable: true
                        description: From-address of the active system default SMTP account, or null if none.
                      systemEmailAddress:
                        type: string
                        nullable: true
                        description: Alias of systemSmtpEmail (same value).
                      systemProvider:
                        type: string
                        nullable: true
                        description: Provider of the system default SMTP account (e.g. smtp, ses), or null.
              example:
                data:
                  inheritSmtp: true
                  systemSmtpEmail: no-reply@theleadrouter.com
                  systemEmailAddress: no-reply@theleadrouter.com
                  systemProvider: ses
        "401":
          description: Unauthorized
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update SMTP inheritance setting
      description: Toggles whether the tenant inherits the platform system SMTP account. Writes an audit log entry.
      operationId: updateEmailInherit
      tags:
        - Settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - inheritSmtp
              properties:
                inheritSmtp:
                  type: boolean
            example:
              inheritSmtp: false
      responses:
        "200":
          description: Updated inheritance setting
          content:
            application/json:
              example:
                data:
                  inheritSmtp: false
        "400":
          description: Validation error (inheritSmtp missing or not a boolean)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Tenant not found
  /api/admin/integrations/facebook-capi:
    get:
      summary: Get Facebook CAPI integration status
      description: Returns the tenant's Facebook Conversions API connection status, or null if the integration has never been
        configured. The access token is never returned.
      operationId: getFacebookCapiStatus
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Integration status (or null when never configured)
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    nullable: true
                    properties:
                      id:
                        type: string
                        format: uuid
                      connected:
                        type: boolean
                        description: True only when enabled and an access token is stored.
                      mode:
                        type: string
                        enum:
                          - api
                          - manual
                      pixelId:
                        type: string
                        nullable: true
                      connectedAt:
                        type: string
                        format: date-time
                        nullable: true
              example:
                data:
                  id: 3f1c2d4e-5a6b-7c8d-9e0f-1a2b3c4d5e6f
                  connected: true
                  mode: manual
                  pixelId: "1234567890"
                  connectedAt: 2026-06-01T12:00:00.000Z
        "400":
          description: Tenant context required (superadmin without an active tenant)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
    put:
      summary: Save Facebook CAPI credentials
      description: >
        Stores an encrypted Facebook CAPI access token for the tenant. Two modes:

        - `api` — token has `ads_read`; the pixel is selected per-webhook, so `pixelId` is optional.

        - `manual` — a single fixed `pixelId` is required.


        A body with no `mode` field is accepted for backwards compatibility and treated as `manual` (requires
        `pixelId`).
      operationId: saveFacebookCapiCredentials
      tags:
        - Integrations
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required:
                    - mode
                    - accessToken
                  properties:
                    mode:
                      type: string
                      enum:
                        - api
                    accessToken:
                      type: string
                      minLength: 1
                    pixelId:
                      type: string
                      nullable: true
                    testEventCode:
                      type: string
                      nullable: true
                - type: object
                  required:
                    - mode
                    - accessToken
                    - pixelId
                  properties:
                    mode:
                      type: string
                      enum:
                        - manual
                    accessToken:
                      type: string
                      minLength: 1
                    pixelId:
                      type: string
                      minLength: 1
                    testEventCode:
                      type: string
                      nullable: true
                - type: object
                  description: Backwards-compatible manual body (no mode field).
                  required:
                    - accessToken
                    - pixelId
                  properties:
                    accessToken:
                      type: string
                      minLength: 1
                    pixelId:
                      type: string
                      minLength: 1
                    testEventCode:
                      type: string
                      nullable: true
            example:
              mode: manual
              accessToken: EAAG...
              pixelId: "1234567890"
              testEventCode: TEST12345
      responses:
        "200":
          description: Credentials saved
          content:
            application/json:
              example:
                data:
                  connected: true
        "400":
          description: Validation error or missing tenant context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
    delete:
      summary: Disconnect Facebook CAPI integration
      description: Deletes the tenant's stored Facebook CAPI credentials and clears the in-memory ad-account/pixel cache.
      operationId: deleteFacebookCapiIntegration
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Integration disconnected
          content:
            application/json:
              example:
                data:
                  disconnected: true
        "400":
          description: Tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/integrations/facebook-capi/ad-accounts:
    get:
      summary: List Facebook ad accounts
      description: Returns the ad accounts accessible to the tenant's stored Facebook token (via the Graph API
        `/me/adaccounts`). Results are cached in-memory per tenant for 15 minutes.
      operationId: listFacebookCapiAdAccounts
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Ad accounts accessible to the token holder
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                        - id
                        - name
                        - accountId
                      properties:
                        id:
                          type: string
                          description: Ad account id with the `act_` prefix.
                          example: act_1234567890
                        name:
                          type: string
                        accountId:
                          type: string
                          description: Numeric account id without the prefix.
                          example: "1234567890"
              example:
                data:
                  - id: act_1234567890
                    name: Acme Ads
                    accountId: "1234567890"
        "400":
          description: Tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "422":
          description: Facebook CAPI is not configured for this tenant. Error code `FACEBOOK_CAPI_UNAVAILABLE` (not part of the
            standard error-code enum).
          content:
            application/json:
              example:
                error:
                  code: FACEBOOK_CAPI_UNAVAILABLE
                  message: Facebook CAPI not configured
        "502":
          description: Facebook Graph API call failed. Error code `FACEBOOK_CAPI_UNAVAILABLE`.
          content:
            application/json:
              example:
                error:
                  code: FACEBOOK_CAPI_UNAVAILABLE
                  message: Failed to fetch ad accounts
  /api/admin/integrations/facebook-capi/pixels:
    get:
      summary: List Facebook pixels for an ad account
      description: Returns the pixels for a given ad account (via the Graph API `/act_{id}/adspixels`). Results are cached
        in-memory per tenant + ad account for 15 minutes.
      operationId: listFacebookCapiPixels
      tags:
        - Integrations
      security:
        - sessionCookie: []
      parameters:
        - name: adAccountId
          in: query
          required: true
          description: Ad account id (with or without the `act_` prefix).
          schema:
            type: string
          example: act_1234567890
      responses:
        "200":
          description: Pixels for the ad account
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                        - id
                        - name
                      properties:
                        id:
                          type: string
                        name:
                          type: string
              example:
                data:
                  - id: "987654321"
                    name: Website Pixel
        "400":
          description: adAccountId missing, or tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "422":
          description: Facebook CAPI is not configured for this tenant. Error code `FACEBOOK_CAPI_UNAVAILABLE`.
          content:
            application/json:
              example:
                error:
                  code: FACEBOOK_CAPI_UNAVAILABLE
                  message: Facebook CAPI not configured
        "502":
          description: Facebook Graph API call failed. Error code `FACEBOOK_CAPI_UNAVAILABLE`.
          content:
            application/json:
              example:
                error:
                  code: FACEBOOK_CAPI_UNAVAILABLE
                  message: Failed to fetch pixels
  /api/admin/integrations/facebook-capi/test:
    post:
      summary: Test Facebook CAPI connection
      description: |
        Sends a synthetic test `Lead` conversion event using the tenant's stored credentials and
        returns the Graph API result. In `api` mode with no fixed pixel, a synthetic success is
        returned without a live call (pixel selection is per-webhook).
      operationId: testFacebookCapiConnection
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Test event result
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - success
                      - status
                      - responseBody
                    properties:
                      success:
                        type: boolean
                      status:
                        type: integer
                        description: HTTP status returned by the Facebook Graph API.
                      responseBody:
                        type: string
                      requestBody:
                        type: string
                      error:
                        type: string
              example:
                data:
                  success: true
                  status: 200
                  responseBody: '{"events_received":1}'
        "400":
          description: Facebook CAPI not configured, or tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/integrations/google-ads:
    get:
      summary: Get Google Ads integration status
      description: Returns the tenant's Google Ads OAuth connection status, or null if never connected. The refresh token is
        never returned.
      operationId: getGoogleAdsStatus
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Integration status (or null when never connected)
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    nullable: true
                    properties:
                      id:
                        type: string
                        format: uuid
                      connected:
                        type: boolean
                        description: True only when enabled and a refresh token is stored.
                      connectedEmail:
                        type: string
                        nullable: true
                      connectedAt:
                        type: string
                        format: date-time
                        nullable: true
              example:
                data:
                  id: 7a8b9c0d-1e2f-3a4b-5c6d-7e8f9a0b1c2d
                  connected: true
                  connectedEmail: ads@acme.com
                  connectedAt: 2026-06-01T12:00:00.000Z
        "400":
          description: Tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
    delete:
      summary: Disconnect Google Ads integration
      description: Deletes the tenant's stored Google Ads credentials and clears the in-memory account cache.
      operationId: deleteGoogleAdsIntegration
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Integration disconnected
          content:
            application/json:
              example:
                data:
                  deleted: true
        "400":
          description: Tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/integrations/google-ads/accessible-customers:
    get:
      summary: List accessible Google Ads customers
      description: Returns the Google Ads customer accounts the connected user can advertise on (leaf accounts, managers
        filtered out). Cached in-memory per tenant for 15 minutes.
      operationId: listGoogleAdsAccessibleCustomers
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Accessible customer accounts
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                        - id
                        - name
                        - currencyCode
                        - timeZone
                        - loginCustomerId
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        currencyCode:
                          type: string
                        timeZone:
                          type: string
                        loginCustomerId:
                          type: string
                          description: The login-customer-id header to use for this account (equals id for direct access, the MCC id for child
                            accounts).
              example:
                data:
                  - id: "1234567890"
                    name: Acme US
                    currencyCode: USD
                    timeZone: America/Los_Angeles
                    loginCustomerId: "1234567890"
        "400":
          description: Tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "422":
          description: Google Ads is not connected for this tenant. Error code `GOOGLE_ADS_UNAVAILABLE` (not part of the standard
            error-code enum).
          content:
            application/json:
              example:
                error:
                  code: GOOGLE_ADS_UNAVAILABLE
                  message: Google Ads is not connected for this tenant
        "502":
          description: Google Ads API call failed. Error code `GOOGLE_ADS_UNAVAILABLE`.
          content:
            application/json:
              example:
                error:
                  code: GOOGLE_ADS_UNAVAILABLE
                  message: Failed to fetch Google Ads accounts
  /api/admin/integrations/google-ads/connect:
    get:
      summary: Start Google Ads OAuth flow
      description: Returns a Google OAuth authorization URL for the current tenant. The client redirects the user to it; the
        callback stores the resulting refresh token.
      operationId: startGoogleAdsConnect
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Authorization URL to redirect the user to
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - authorizationUrl
                    properties:
                      authorizationUrl:
                        type: string
                        format: uri
              example:
                data:
                  authorizationUrl: https://accounts.google.com/o/oauth2/v2/auth?client_id=...&state=...
        "400":
          description: Tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "422":
          description: Google Ads OAuth client or developer token is not configured on the platform
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/integrations/google-ads/conversion-actions:
    get:
      summary: List Google Ads conversion actions
      description: Lists ENABLED conversion actions for a Google Ads customer account. Cached in-memory per tenant + customer
        for 15 minutes.
      operationId: listGoogleAdsConversionActions
      tags:
        - Integrations
      security:
        - sessionCookie: []
      parameters:
        - name: customerId
          in: query
          required: true
          description: Google Ads customer id. Dashes are stripped; must be numeric after normalization.
          schema:
            type: string
          example: 123-456-7890
        - name: loginCustomerId
          in: query
          required: false
          description: Optional manager (MCC) login-customer-id. Dashes stripped; must be numeric.
          schema:
            type: string
      responses:
        "200":
          description: Enabled conversion actions
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                        - id
                        - name
                        - category
                        - status
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        category:
                          type: string
                        status:
                          type: string
              example:
                data:
                  - id: "987654321"
                    name: Purchase
                    category: PURCHASE
                    status: ENABLED
        "400":
          description: Invalid query params (missing/non-numeric customerId) or tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "422":
          description: Google Ads is not connected for this tenant. Error code `GOOGLE_ADS_UNAVAILABLE`.
          content:
            application/json:
              example:
                error:
                  code: GOOGLE_ADS_UNAVAILABLE
                  message: Google Ads is not connected for this tenant
        "502":
          description: Google Ads API call failed. Error code `GOOGLE_ADS_UNAVAILABLE`.
          content:
            application/json:
              example:
                error:
                  code: GOOGLE_ADS_UNAVAILABLE
                  message: Failed to fetch conversion actions
  /api/admin/stripe/connect/start:
    post:
      summary: Start Stripe Connect onboarding
      description: |
        Ensures the tenant has a Stripe Standard connected account (creating one on first call),
        then returns a Stripe account-onboarding link URL. Writes an audit log entry.
        Requires an admin or superadmin session with an active tenant.
      operationId: startStripeConnect
      tags:
        - Integrations
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                returnPath:
                  type: string
                  description: Path used for both the Stripe refresh and return URLs. Defaults to the internal Stripe Connect return
                    route.
            example:
              returnPath: /admin/settings/billing
      responses:
        "200":
          description: Stripe onboarding link
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - url
                    properties:
                      url:
                        type: string
                        format: uri
              example:
                data:
                  url: https://connect.stripe.com/setup/s/...
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or tenant context missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
  /api/admin/stripe/connect/status:
    get:
      summary: Get Stripe Connect status
      description: Returns the tenant's Stripe Connect onboarding flags and the effective platform fee. Requires an admin or
        superadmin session with an active tenant.
      operationId: getStripeConnectStatus
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Stripe Connect status and platform fee
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      stripeAccountId:
                        type: string
                        nullable: true
                      stripeChargesEnabled:
                        type: boolean
                      stripePayoutsEnabled:
                        type: boolean
                      stripeDetailsSubmitted:
                        type: boolean
                      stripeOnboardedAt:
                        type: string
                        format: date-time
                        nullable: true
                      platformFeeBps:
                        type: integer
                        description: Effective platform fee in basis points.
                      platformFeeIsOverride:
                        type: boolean
                        description: True when the tenant has a per-tenant fee override rather than the platform default.
              example:
                data:
                  stripeAccountId: acct_1234567890
                  stripeChargesEnabled: true
                  stripePayoutsEnabled: true
                  stripeDetailsSubmitted: true
                  stripeOnboardedAt: 2026-06-01T12:00:00.000Z
                  platformFeeBps: 500
                  platformFeeIsOverride: false
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or tenant context missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
  /api/admin/tenant-features/preview-disable:
    post:
      summary: Preview blockers for disabling a tenant feature
      description: |
        Preflight for disabling a gated tenant feature. Returns the count and a capped sample (max 5)
        of blocking rows using the same predicate the actual disable enforces, so the UI warning and
        the server-side soft-block cannot drift. Currently only `feature: 'sku'` is supported.
      operationId: previewTenantFeatureDisable
      tags:
        - Settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - feature
              properties:
                feature:
                  type: string
                  enum:
                    - sku
                  description: The feature to preview disabling. Only `sku` is currently supported.
            example:
              feature: sku
      responses:
        "200":
          description: Blocker count and sample
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - count
                      - sample
                    properties:
                      count:
                        type: integer
                        description: Number of blocking rows found (each underlying query is capped at 10).
                      sample:
                        type: array
                        description: Up to 5 blocking rows, each tagged by source.
                        items:
                          oneOf:
                            - type: object
                              required:
                                - type
                                - id
                              properties:
                                type:
                                  type: string
                                  enum:
                                    - buyerSku
                                id:
                                  type: string
                                  format: uuid
                                status:
                                  type: string
                                  nullable: true
                                routeabilityStatus:
                                  type: string
                                  nullable: true
                                subscriptionPauseStatus:
                                  type: string
                                  nullable: true
                                stripeSubscriptionId:
                                  type: string
                                  nullable: true
                            - type: object
                              required:
                                - type
                                - id
                              properties:
                                type:
                                  type: string
                                  enum:
                                    - checkoutSession
                                id:
                                  type: string
                                  format: uuid
                                mode:
                                  type: string
                                  nullable: true
                                status:
                                  type: string
                                  nullable: true
              example:
                data:
                  count: 2
                  sample:
                    - type: buyerSku
                      id: 2b3c4d5e-6f7a-8b9c-0d1e-2f3a4b5c6d7e
                      status: active
                      routeabilityStatus: routable
                      subscriptionPauseStatus: null
                      stripeSubscriptionId: sub_123
                    - type: checkoutSession
                      id: 3c4d5e6f-7a8b-9c0d-1e2f-3a4b5c6d7e8f
                      mode: sku_subscription
                      status: pending
        "400":
          description: Tenant context required, or unsupported feature
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/tenant/compliance:
    get:
      summary: Get tenant compliance settings
      description: Returns the tenant's compliance/legal settings. Currently just the TCPA terms text shown on public
        posting-spec pages. Requires an admin or superadmin session with an active tenant.
      operationId: getTenantCompliance
      tags:
        - Settings
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Compliance settings
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      tcpaTermsText:
                        type: string
                        nullable: true
              example:
                data:
                  tcpaTermsText: By submitting you consent to be contacted...
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or tenant context missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update tenant compliance settings
      description: Updates the tenant's compliance/legal settings. An empty or whitespace-only `tcpaTermsText` is stored as
        null (hides the section). Writes an audit log entry.
      operationId: updateTenantCompliance
      tags:
        - Settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tcpaTermsText:
                  type: string
                  maxLength: 10000
                  nullable: true
                  description: Plain-text TCPA terms shown on public posting spec pages. Null/empty hides the section.
            example:
              tcpaTermsText: By submitting you consent to be contacted...
      responses:
        "200":
          description: Updated compliance settings
          content:
            application/json:
              example:
                data:
                  tcpaTermsText: By submitting you consent to be contacted...
        "400":
          description: Validation error (body not JSON, or tcpaTermsText exceeds 10000 chars)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or tenant context missing
        "404":
          description: Tenant not found
  /api/admin/bid-signals:
    get:
      summary: Bid signal analysis
      description: |
        Returns bid-management analysis for the tenant's contracts, optionally scoped to a single offer.
        Computed live via `analyzeBids()` — surfaces bid competitiveness signals used by the admin bid tuning UI.
      operationId: getAdminBidSignals
      tags:
        - "Admin: Bid Signals"
      security:
        - sessionCookie: []
      parameters:
        - name: offerId
          in: query
          description: Restrict analysis to a single offer.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bid analysis result (shape produced by the bid-management engine).
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    additionalProperties: true
        "401":
          description: Unauthorized
  /api/admin/dedup:
    get:
      summary: Dedup match analytics
      description: |
        Fuzzy-dedup analytics for the tenant over a trailing window. Returns a match-type breakdown,
        recent matches (with PHI-decrypted lead display name), and a daily match-rate series.
        Requires the `fuzzyDedup` tenant feature — returns `403 FEATURE_DISABLED` when the feature is off.
      operationId: getAdminDedupAnalytics
      tags:
        - "Admin: Dedup"
      security:
        - sessionCookie: []
      parameters:
        - name: days
          in: query
          description: Trailing window in days (capped at 90).
          schema:
            type: integer
            default: 30
            maximum: 90
        - name: limit
          in: query
          description: Max recent matches to return (capped at 200).
          schema:
            type: integer
            default: 50
            maximum: 200
      responses:
        "200":
          description: Dedup analytics.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - breakdown
                      - recent
                      - daily
                    properties:
                      breakdown:
                        type: array
                        items:
                          type: object
                          properties:
                            matchType:
                              type: string
                              example: hash_exact
                            count:
                              type: integer
                      recent:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            matchType:
                              type: string
                            confidence:
                              type: number
                              nullable: true
                            matchedLeadId:
                              type: string
                              format: uuid
                              nullable: true
                            leadId:
                              type: string
                              format: uuid
                              nullable: true
                            buyerId:
                              type: string
                              format: uuid
                              nullable: true
                            buyerName:
                              type: string
                              nullable: true
                            leadName:
                              type: string
                              description: Decrypted "First Last", else email, else "Unknown".
                            createdAt:
                              type: string
                              format: date-time
                      daily:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                              format: date
                            matchType:
                              type: string
                            count:
                              type: integer
        "401":
          description: Unauthorized
        "403":
          description: Fuzzy dedup feature not enabled for this tenant (FEATURE_DISABLED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/delivery-health:
    get:
      summary: Delivery health dashboard
      description: |
        Aggregated delivery-failure analytics over a trailing window, backed by `leadSale`.
        Returns per-contract failure stats (with failure classification from the delivery-health engine),
        a failure-type breakdown, paused-contract count, and a recent-failures timeline.
        Requires the `selfHealingDelivery` tenant feature — returns `403 FEATURE_DISABLED` when off.
      operationId: getAdminDeliveryHealth
      tags:
        - "Admin: Delivery Log"
      security:
        - sessionCookie: []
      parameters:
        - name: hours
          in: query
          description: Trailing window in hours (capped at 168).
          schema:
            type: integer
            default: 24
            maximum: 168
      responses:
        "200":
          description: Delivery health summary.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      summary:
                        type: object
                        properties:
                          totalDeliveries:
                            type: integer
                          totalFailed:
                            type: integer
                          overallFailureRate:
                            type: number
                          contractsPaused:
                            type: integer
                          hoursBack:
                            type: integer
                      contractStats:
                        type: array
                        items:
                          type: object
                          properties:
                            contractId:
                              type: string
                              format: uuid
                              nullable: true
                            buyerId:
                              type: string
                              format: uuid
                            buyerName:
                              type: string
                              nullable: true
                            contractName:
                              type: string
                              nullable: true
                            contractStatus:
                              type: string
                              nullable: true
                            hasBackupUrl:
                              type: boolean
                            total:
                              type: integer
                            failed:
                              type: integer
                            delivered:
                              type: integer
                            failureRate:
                              type: number
                            classification:
                              type: string
                              enum:
                                - timeout
                                - server_error
                                - client_error
                                - connection_refused
                                - ssl_error
                                - response_rejection
                                - unknown
                      failureTypeBreakdown:
                        type: object
                        description: Count of failed deliveries per failure classification.
                        additionalProperties:
                          type: integer
                      recentFailures:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            contractId:
                              type: string
                              format: uuid
                              nullable: true
                            buyerId:
                              type: string
                              format: uuid
                            buyerName:
                              type: string
                              nullable: true
                            response:
                              type: string
                              nullable: true
                            createdAt:
                              type: string
                              format: date-time
        "401":
          description: Unauthorized
        "403":
          description: Self-healing delivery feature not enabled for this tenant (FEATURE_DISABLED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/delivery-log:
    get:
      summary: List delivery log entries
      description: |
        Cursor-paginated outbound delivery log. Default mode reads the `deliveryLog` table; passing
        `legacy=true` returns the pre-cutover `leadSale`-backed response shape (used by the UI "Show
        legacy rows" toggle). Filters and response fields differ between the two modes.

        Response pagination carries both an `offset` (echoed from the query) and an opaque `cursor` for
        forward keyset paging on `(createdAt, id)`. `buyerName`/`contractName` fall back to joined
        buyer/contract names when not stamped on the log row.
      operationId: listAdminDeliveryLog
      tags:
        - "Admin: Delivery Log"
      security:
        - sessionCookie: []
      parameters:
        - name: channel
          in: query
          description: Delivery channel (default mode only).
          schema:
            type: string
            enum:
              - http_post
              - http_get
              - sms
              - email
              - google_sheets
              - webhook
              - messaging_sync
        - name: outcome
          in: query
          description: Delivery outcome (default mode only).
          schema:
            type: string
            enum:
              - success
              - failed
              - rejected
              - test
        - name: isTest
          in: query
          schema:
            type: boolean
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: contractId
          in: query
          schema:
            type: string
            format: uuid
        - name: leadId
          in: query
          schema:
            type: string
            format: uuid
        - name: deliveryStatus
          in: query
          description: "Legacy mode only (`legacy=true`): filter by leadSale delivery status."
          schema:
            type: string
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO). Interpreted in the tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO). Interpreted in the tenant timezone.
          schema:
            type: string
        - name: search
          in: query
          description: Partial match on endpoint (default mode) or deliveryUrl (legacy mode).
          schema:
            type: string
        - name: legacy
          in: query
          description: When true, returns the pre-cutover leadSale-backed rows and filters.
          schema:
            type: boolean
        - name: cursor
          in: query
          description: Opaque keyset cursor from a prior page's `pagination.cursor`.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Cursor-paginated delivery log entries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      offset:
                        type: integer
                      hasMore:
                        type: boolean
                      cursor:
                        type: string
                        nullable: true
        "400":
          description: Invalid filter parameters or cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/delivery-log/{id}:
    get:
      summary: Get a delivery log entry
      description: |
        Returns a single delivery attempt. Resolves the id against the `deliveryLog` table first
        (left-joined to its linked `leadSale` for price/disposition/buyer-response enrichment) and
        falls back to a legacy `leadSale` id for pre-cutover rows. The `source` field indicates which
        table matched (`deliveryLog` or `leadSale`).
      operationId: getAdminDeliveryLog
      tags:
        - "Admin: Delivery Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Delivery log entry with sale enrichment.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      source:
                        type: string
                        enum:
                          - deliveryLog
                          - leadSale
                    additionalProperties: true
        "400":
          description: Invalid id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Delivery log entry not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/disposition-log:
    get:
      summary: List disposition log entries
      description: |
        Cursor-paginated log of disposition transitions (`dispositionLog`), joined to the lead for its
        `shortId`. Ordered by `(createdAt, id)` descending. The next-page cursor is returned as a
        top-level `cursor` field (pipe-delimited `ISO|uuid`); `pagination` carries `total`, `limit`,
        and `hasMore` only.
      operationId: listAdminDispositionLog
      tags:
        - "Admin: Disposition Log"
      security:
        - sessionCookie: []
      parameters:
        - name: entityType
          in: query
          schema:
            type: string
        - name: toDisposition
          in: query
          schema:
            type: string
        - name: triggeredBy
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: search
          in: query
          description: Partial match on reason, toDisposition, or entityId.
          schema:
            type: string
        - name: cursor
          in: query
          description: Pipe-delimited `ISO8601|uuid` cursor from the prior page's `cursor`.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
      responses:
        "200":
          description: Disposition log page.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  cursor:
                    type: string
                    nullable: true
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      hasMore:
                        type: boolean
        "401":
          description: Unauthorized
  /api/admin/distribution-log:
    get:
      summary: List distribution log entries
      description: |
        Cursor-paginated lead-distribution waterfall log (`leadDistribution`), joined to lead/buyer/offer.
        Ordered by `COALESCE(postedAt, createdAt)` descending. The next-page cursor is returned as a
        top-level `cursor` field (pipe-delimited `ISO|uuid`); `pagination` carries `total`, `limit`,
        and `hasMore` only.
      operationId: listAdminDistributionLog
      tags:
        - "Admin: Distribution Log"
      security:
        - sessionCookie: []
      parameters:
        - name: disposition
          in: query
          schema:
            type: string
        - name: decidedBy
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: search
          in: query
          description: Partial match on contractName, dispositionReason, or id.
          schema:
            type: string
        - name: cursor
          in: query
          description: Pipe-delimited `ISO8601|uuid` cursor from the prior page's `cursor`.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
      responses:
        "200":
          description: Distribution log page.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  cursor:
                    type: string
                    nullable: true
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      hasMore:
                        type: boolean
        "401":
          description: Unauthorized
  /api/admin/messaging-log:
    get:
      summary: List messaging (email) log entries
      description: |
        Cursor-paginated transactional email log (`emailLog`). Ordered by `(createdAt, id)` descending.
        The next-page cursor is returned inside `pagination.cursor`.
      operationId: listAdminMessagingLog
      tags:
        - "Admin: Messaging Log"
      security:
        - sessionCookie: []
      parameters:
        - name: toEmail
          in: query
          description: Partial match on recipient email.
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
        - name: templateSlug
          in: query
          schema:
            type: string
        - name: relatedEntityType
          in: query
          schema:
            type: string
        - name: relatedEntityId
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: search
          in: query
          description: Partial match on errorMessage.
          schema:
            type: string
        - name: cursor
          in: query
          description: Opaque keyset cursor from the prior page's `pagination.cursor`.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Email log page.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      offset:
                        type: integer
                      hasMore:
                        type: boolean
                      cursor:
                        type: string
                        nullable: true
        "400":
          description: Invalid cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/outbound-ping-log:
    get:
      summary: List outbound ping (ping-match) log entries
      description: |
        Offset-paginated log of outbound HTTP pings sent to buyers (`pingMatch`), joined to
        contract/buyer/lead/campaign. Default returns all outbound pings (winners and HTTP-excluded);
        `kind` narrows to one class. `partnerPrice` is read from the stamped value, falling back to a
        live compute for pre-stamp legacy rows.
      operationId: listAdminOutboundPingLog
      tags:
        - "Admin: Ping Log"
      security:
        - sessionCookie: []
      parameters:
        - name: kind
          in: query
          description: Restrict to a single ping class. `winner` = accepted; `excluded` = buyer rejected at HTTP. Omit for all.
          schema:
            type: string
            enum:
              - winner
              - excluded
        - name: status
          in: query
          schema:
            type: string
        - name: pingId
          in: query
          schema:
            type: string
            format: uuid
        - name: contractId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Offset-paginated outbound ping log.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "401":
          description: Unauthorized
  /api/admin/ping-log:
    get:
      summary: List inbound ping log entries
      description: |
        Offset-paginated log of inbound pings received from partners (`ping`), joined to campaign/partner.
        Each ping is enriched with its grouped `pingMatch` rows (per-buyer bids) and the actual partner
        response bytes (`partnerResponseBody`) pulled from the correlated `/api/v1/leads/ping` posting-log
        entry. `partnerPrice` on each match is stamped or computed for legacy rows.
      operationId: listAdminPingLog
      tags:
        - "Admin: Ping Log"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Offset-paginated ping log with matches and partner response.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        matches:
                          type: array
                          items:
                            type: object
                            additionalProperties: true
                        partnerResponseBody:
                          nullable: true
                          description: Exact response bytes sent to the partner at ping time.
                      additionalProperties: true
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "401":
          description: Unauthorized
  /api/admin/pings/{id}/distributions:
    get:
      summary: List distributions for a ping
      description: |
        Offset-paginated `pingDistribution` rows for a single ping, joined to buyer and (for rows that
        hit the buyer endpoint) the corresponding `pingMatch` HTTP response. Returns `404` if the ping
        is not visible to the tenant. Ordered by `rank ASC NULLS LAST`, then `createdAt`.
      operationId: listAdminPingDistributions
      tags:
        - "Admin: Distribution Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated ping distributions.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "400":
          description: Invalid ping id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Ping not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/postback-log:
    get:
      summary: List postback log entries
      description: |
        Cursor-paginated log of inbound CPA/disposition postbacks. Backed by `postingLog`, hard-filtered
        to `endpoint = /api/v1/postback`, joined to the lead for its `shortId`. Ordered by `(createdAt, id)`
        descending; next-page cursor is returned inside `pagination.cursor`.
      operationId: listAdminPostbackLog
      tags:
        - "Admin: Postback Log"
      security:
        - sessionCookie: []
      parameters:
        - name: outcome
          in: query
          schema:
            type: string
        - name: httpStatus
          in: query
          schema:
            type: integer
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: search
          in: query
          description: Partial match on errorMessage.
          schema:
            type: string
        - name: cursor
          in: query
          description: Opaque keyset cursor from the prior page's `pagination.cursor`.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Postback log page.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      offset:
                        type: integer
                      hasMore:
                        type: boolean
                      cursor:
                        type: string
                        nullable: true
        "400":
          description: Invalid cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/postback-log/{id}:
    get:
      summary: Get a postback log entry
      description: |
        Returns a single postback log entry (full `postingLog` row). Returns `404` if the id is not a
        postback entry (`endpoint != /api/v1/postback`) or is not visible to the tenant.
      operationId: getAdminPostbackLog
      tags:
        - "Admin: Postback Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Postback log entry.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    additionalProperties: true
        "400":
          description: Invalid id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Postback log entry not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/postback-log/{id}/refire:
    post:
      summary: Refire a postback
      description: |
        Re-processes the disposition recorded on a stored postback entry through `processDisposition`,
        as if the original CPA/disposition postback were received again. Reads `disposition`, `saleId`
        or `callSaleId`, and optional fields (`dispoReason`, `intakeDispo`, `comsCount`, `intakeId`,
        `dispositionDate`) from the original `requestBody`. No request body is required.

        Requires the owner role (`withOwner`) — stricter than the other admin log endpoints. Returns
        `400` if the entry is not a postback, has no request body, or is missing a disposition/sale id;
        `404` if the referenced sale or buyer no longer exists.
      operationId: refireAdminPostback
      tags:
        - "Admin: Postback Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Disposition re-processed.
          content:
            application/json:
              schema:
                type: object
                required:
                  - success
                  - data
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      dispositionId:
                        type: string
                        format: uuid
                        nullable: true
                      cpaTriggered:
                        type: boolean
                      returnTriggered:
                        type: boolean
                      unmapped:
                        type: boolean
        "400":
          description: Not a postback entry, missing request body, or missing disposition/sale id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Requires owner role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Postback entry, sale, or buyer not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/posting-log/{id}/audit:
    get:
      summary: Get posting log audit trail
      description: |
        Returns up to 50 `auditLog` rows recorded against a posting-log entry
        (`entityType = postingLog`, `entityId = {id}`), newest first. Powers the "Audit" tab on the
        posting-log detail view (e.g. resend history).
      operationId: getAdminPostingLogAudit
      tags:
        - "Admin: Posting Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Audit rows for the posting-log entry.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
        "400":
          description: Invalid id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/posting-log/{id}/resend:
    post:
      summary: Resend a posting log lead submission
      description: |
        Re-submits a previously failed/rejected lead-post from its stored `postingLog` entry through
        `processLeadPost`, creating a brand-new posting-log entry and routing the lead. Optionally
        accepts an override body to edit before resending. Resend is blocked for call events, entries
        with no associated campaign, and entries whose original outcome was `success`.

        The response is passed through directly from `processLeadPost` (the standard lead-submission
        response), not the log envelope. An `auditLog` "resend" row is written on success.
      operationId: resendAdminPostingLog
      tags:
        - "Admin: Posting Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                requestBody:
                  type: object
                  additionalProperties: true
                  description: Overrides the stored request body before resending (also persisted onto the original entry).
                skipTrustedFormValidation:
                  type: boolean
                  description: When true, bypasses TrustedForm certificate validation on the resend.
      responses:
        "200":
          description: Lead re-submission result (shape produced by processLeadPost).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        "400":
          description: Cannot resend (call event, no campaign, or original outcome was success).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Posting log entry or campaign not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/webhook-log:
    get:
      summary: List webhook delivery log entries
      description: |
        Cursor-paginated outbound webhook delivery log (`webhookDelivery`), joined to the webhook config
        and (via lead) offer/vertical/partner. Call-event deliveries carry `callId`/`callSaleId` plus a
        joined `callDisplayId`; lead-only rows return them as null. Ordered by `(createdAt, id)`
        descending; next-page cursor is returned inside `pagination.cursor`.
      operationId: listAdminWebhookLog
      tags:
        - "Admin: Webhook Log"
      security:
        - sessionCookie: []
      parameters:
        - name: webhookId
          in: query
          schema:
            type: string
            format: uuid
        - name: event
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
        - name: leadId
          in: query
          schema:
            type: string
            format: uuid
        - name: callId
          in: query
          description: Filter to deliveries linked to this call (call-event webhooks).
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          description: Inclusive start date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Inclusive end date (YYYY-MM-DD or ISO), tenant timezone.
          schema:
            type: string
        - name: search
          in: query
          description: Partial match on requestUrl.
          schema:
            type: string
        - name: cursor
          in: query
          description: Opaque keyset cursor from the prior page's `pagination.cursor`.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Webhook delivery log page.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      offset:
                        type: integer
                      hasMore:
                        type: boolean
                      cursor:
                        type: string
                        nullable: true
        "400":
          description: Invalid cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/webhook-log/{id}:
    get:
      summary: Get a webhook delivery log entry
      description: |
        Returns a single `webhookDelivery` row (including `responseBody`), joined to the webhook config
        and lead-derived offer/vertical/partner context. Call-event deliveries include `callId`/
        `callSaleId` and the joined `callDisplayId` (null on lead-only rows). Returns `404` if not
        visible to the tenant.
      operationId: getAdminWebhookLog
      tags:
        - "Admin: Webhook Log"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Webhook delivery entry.
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    additionalProperties: true
        "400":
          description: Invalid id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Webhook delivery not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/messaging-media:
    get:
      summary: List messaging media
      description: |
        Returns a paginated, tenant-scoped list of images in the messaging
        TemplateBuilder media library, newest first (`createdAt desc`).

        Auth: `lr_session` session cookie, admin or superadmin role (`withAdmin`).
      operationId: listMessagingMedia
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          description: Page size (default 20, max 1000).
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          description: Row offset (default 0).
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated media list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                    tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    url: https://media.theleadrouter.com/messaging-media/a1b2c3d4/1721419200000-banner.png
                    r2Key: messaging-media/a1b2c3d4/1721419200000-banner.png
                    filename: banner.png
                    contentType: image/png
                    sizeBytes: 20481
                    width: null
                    height: null
                    uploadedByUserId: 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                    createdAt: 2026-07-19T18:00:00.000Z
                    updatedAt: 2026-07-19T18:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
    post:
      summary: Upload messaging media
      description: |
        Uploads an image (jpeg/png/webp/gif, ≤4MB — no svg) to R2 and records a
        tenant-scoped row. Returns the created media row.

        Auth: `lr_session` session cookie, admin or superadmin role (`withAdmin`).
      operationId: uploadMessagingMedia
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image (jpeg/png/webp/gif ≤4MB).
      responses:
        "201":
          description: Media stored; the created row is returned.
          content:
            application/json:
              example:
                data:
                  id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  url: https://media.theleadrouter.com/messaging-media/a1b2c3d4/1721419200000-banner.png
                  r2Key: messaging-media/a1b2c3d4/1721419200000-banner.png
                  filename: banner.png
                  contentType: image/png
                  sizeBytes: 20481
                  createdAt: 2026-07-19T18:00:00.000Z
                  updatedAt: 2026-07-19T18:00:00.000Z
        "400":
          description: Missing file, unsupported type, or oversize upload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/messaging-media/{id}:
    delete:
      summary: Delete messaging media
      description: |
        Deletes a tenant-scoped media row and best-effort removes the R2 object.
        Returns 404 when the id is absent or belongs to another tenant.

        Auth: `lr_session` session cookie, admin or superadmin role (`withAdmin`).
      operationId: deleteMessagingMedia
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Media deleted.
          content:
            application/json:
              example:
                data:
                  ok: true
        "401":
          description: Unauthorized
        "404":
          description: Media not found (or not in caller's tenant).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/calls/{id}/annotations:
    post:
      summary: Add a call annotation
      description: Appends a note + tag annotation to a call. Annotations are stored inside the call's
        `custom.callAnnotations` array (each entry records `note`, `tags`, `createdAt`, and the author's `createdBy`
        user id). Existing annotations are preserved.
      operationId: createCallAnnotation
      tags:
        - Calls
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Call UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                note:
                  type: string
                  maxLength: 2000
                  nullable: true
                  description: Free-text annotation note.
                tags:
                  type: object
                  additionalProperties: true
                  default: {}
                  description: Arbitrary key/value tag map attached to this annotation.
            example:
              note: Caller requested a callback next week
              tags:
                priority: high
                topic: billing
      responses:
        "200":
          description: Annotation appended
          content:
            application/json:
              example:
                data:
                  id: 6f1c2d3e-4a5b-6c7d-8e9f-0a1b2c3d4e5f
                  annotation:
                    note: Caller requested a callback next week
                    tags:
                      priority: high
                    createdAt: 2026-07-02T18:00:00.000Z
                    createdBy: 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Call not found (or not in caller's tenant)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/calls/recordings:
    get:
      operationId: listCallRecordings
      tags:
        - Calls
      summary: List the tenant's call recordings (metadata only)
      description: |
        Enumerates calls that currently have a live recording. Metadata only — no
        audio is transferred.

        This is the other half of bulk retrieval: without it a caller must already
        know every `callId`. With `sign=true` each row carries its own signed URL,
        so a bulk pull is one authenticated request followed by N credential-free
        fetches of `/api/v1/public/recordings/{token}`.

        Token minting is capped per page (`Config.recordingManifestMaxSigned`, 500)
        so one request cannot mint an unbounded number of live capabilities; when
        the page exceeds the cap a `warning` field is returned alongside the data.

        **Read-only keys cannot mint.** `sign=true` is refused with `403 READ_ONLY`
        for a read-only caller, matching the mint endpoint — a signed URL is a
        credential-free capability to PHI audio, and issuing one is not a read. The
        unsigned manifest remains available to read-only keys.
      security:
        - sessionCookie: []
        - bearerAuth: []
      parameters:
        - name: sign
          in: query
          required: false
          description: When `true`, include a signed URL and expiry on each row
          schema:
            type: boolean
            default: false
        - name: limit
          in: query
          schema:
            type: integer
        - name: offset
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Paginated recording metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        callId:
                          type: string
                          format: uuid
                        durationSec:
                          type: integer
                          nullable: true
                        createdAt:
                          type: string
                          format: date-time
                        provider:
                          type: string
                          nullable: true
                        url:
                          type: string
                          description: Present only when `sign=true`
                        expiresAt:
                          type: string
                          format: date-time
                          description: Present only when `sign=true`
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
                  warning:
                    type: string
                    description: Present when the page exceeded the signing cap
        "401":
          description: Unauthorized
        "403":
          description: |
            Admin access required, or `READ_ONLY` when a read-only key requested
            `sign=true`.
  /api/admin/calls/{id}/recording/signed-url:
    post:
      operationId: mintCallRecordingSignedUrl
      tags:
        - Calls
      summary: Mint a short-lived signed URL for a call recording
      description: |
        Returns a URL that streams this call's recording without any credential,
        valid for `Config.recordingSignedUrlTtlSeconds` (15 min).

        Exists because the only alternative — handing out an `admin`-scoped API
        key — grants the entire admin surface. The minted token grants exactly one
        recording in one tenant.

        Note the provider context: SignalWire (the current provider) has no
        signed-URL mechanism of its own, and its Media URL Protection is enabled,
        so a shareable link has to be minted here rather than delegated to the
        carrier.

        Returns `404` for a call in another tenant, a call with no recording, and a
        recording already purged by retention — the same response for all three, so
        this cannot be used to probe which call IDs exist.
      security:
        - sessionCookie: []
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Call UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Signed URL minted
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                    example: https://theleadrouter.com/api/v1/public/recordings/eyJ...abc
                  expiresAt:
                    type: string
                    format: date-time
        "400":
          description: Malformed call id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Recording not found (or not in caller's tenant)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/calls/{id}/recording:
    get:
      summary: Stream a call recording
      description: Proxies and streams the audio recording for a call. Only Telnyx-provider calls with a stored `recordingUrl`
        are supported — the handler resolves the tenant's Telnyx credentials, looks up the recording via the Telnyx
        recordings API (falling back to the stored `recordingUrl`), and streams the audio bytes back inline. Responds
        `404` when the call has no Telnyx recording, no tenant context, no configured credentials, or the download
        fails.
      operationId: getCallRecording
      tags:
        - Calls
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Call UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Audio stream. `Content-Type` is echoed from the upstream recording (typically `audio/mpeg`),
            `Content-Disposition: inline`, cached `private, max-age=3600`."
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Call not found, not a Telnyx recording, missing tenant context, no Telnyx credentials, or the recording
            download failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/compliance-floors:
    get:
      summary: Get compliance floors
      description: Returns the platform-wide compliance floor settings (security minimums that tenant admins cannot weaken
        below). Values are the raw `systemSetting` rows in the `compliance_floor` category as a flat key/value map of
        strings. Empty object when no floors are configured.
      operationId: getComplianceFloors
      tags:
        - Compliance
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Map of compliance floor keys to string values
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: string
              example:
                data:
                  twoFactorRequiredForAdmins: "true"
                  sessionIdleTimeoutMinutes: "30"
        "401":
          description: Unauthorized
  /api/admin/contracts:
    get:
      summary: List contracts (admin)
      description: Admin-only alias of `GET /api/v1/contracts`, gated with `withAdmin` (buyer/partner sessions are rejected).
        Returns a paginated list of contracts with the same behavior as the v1 handler, including the conditional
        `buyerSku`/`sku` join when the tenant has the SKU feature enabled and the `funding` filter.
      operationId: listAdminContracts
      tags:
        - Contracts
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by contract status. Accepts a single value or a comma-separated list.
          schema:
            type: string
            example: active,paused
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          description: Filter to contracts linked to this offer.
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          description: Case-insensitive partial match on contract name.
          schema:
            type: string
        - name: funding
          in: query
          description: Filter by funding source. `sku` = contracts backed by a buyer SKU (`buyerSkuId` set); `standalone` =
            contracts with no SKU funding.
          schema:
            type: string
            enum:
              - sku
              - standalone
        - name: sort
          in: query
          schema:
            type: string
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of contracts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/contracts/{id}/clone:
    post:
      summary: Clone a contract
      description: Creates a copy of an existing contract for a target buyer. The clone copies filters, pricing, caps,
        schedule, delivery config, call/AI-dialer settings, and any call billing rules, but is created with status
        `paused` and with no offer links or sub-resources. Both the source contract and target buyer must exist in the
        caller's tenant.
      operationId: cloneContract
      tags:
        - Contracts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Source contract UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - buyerId
                - name
              properties:
                buyerId:
                  type: string
                  format: uuid
                  description: Buyer that will own the cloned contract.
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                  description: Name for the new contract.
            example:
              buyerId: b2c3d4e5-f6a7-8901-b2c3-d4e5f6a78901
              name: Auto Insurance - California (copy)
      responses:
        "200":
          description: Contract cloned
          content:
            application/json:
              example:
                data:
                  id: c3d4e5f6-a7b8-9012-c3d4-e5f6a7b89012
                  displayId: 1042
                  name: Auto Insurance - California (copy)
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Source contract or target buyer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/data-deletion/{id}:
    get:
      summary: Get a data subject request
      description: Returns a single data subject request (GDPR/CCPA deletion, access, correction, or do-not-sell) by id,
        scoped to the caller's tenant. Requires admin or superadmin role.
      operationId: getDataDeletionRequest
      tags:
        - Compliance
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Data subject request UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Data subject request detail
          content:
            application/json:
              example:
                data:
                  id: d4e5f6a7-b8c9-0123-d4e5-f6a7b8c90123
                  requestType: delete
                  identifierType: email
                  identifier: jane.doe@example.com
                  status: pending
                  notes: null
                  createdAt: 2026-07-01T10:00:00.000Z
                  completedAt: null
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Data subject request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Process a data subject request
      description: >-
        Approves, rejects, or retries a data subject request. Requires admin or superadmin role.

        - `approve` — only valid when status is `pending`. For `delete` requests, anonymizes matching
          lead/person PII (redacts name, email, phone, address; clears date of birth) by the request's
          identifier; access/correction/do-not-sell requests are marked `completed` for manual handling.
        - `reject` — only valid when status is `pending`; marks the request `rejected`. - `retry` — only valid when
        status is `failed`; re-runs the deletion.

        On a processing error the request is set to `failed` and the error surfaces via the standard error envelope.
      operationId: processDataDeletionRequest
      tags:
        - Compliance
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Data subject request UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - approve
                    - reject
                    - retry
                reason:
                  type: string
                  maxLength: 1000
                  description: Optional note stored on the request.
            example:
              action: approve
              reason: Verified identity via support ticket
      responses:
        "200":
          description: Request processed
          content:
            application/json:
              example:
                data:
                  success: true
                  status: completed
        "400":
          description: Validation error or illegal status transition (e.g. retrying a non-failed request)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Data subject request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/leads/{id}/disposition-log:
    get:
      summary: Get a lead's disposition log
      description: Returns the paginated disposition-log history for a lead (audit trail of disposition events across the
        lead's sales/distributions). Scoped to the caller's tenant; the lead must exist in that tenant. Ordered
        newest-first.
      operationId: getLeadDispositionLog
      tags:
        - Leads
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Lead UUID
          schema:
            type: string
            format: uuid
        - name: entityType
          in: query
          description: Filter log entries by entity type (e.g. `leadSale`, `callSale`).
          schema:
            type: string
        - name: limit
          in: query
          description: Max results per page (capped at 100).
          schema:
            type: integer
            default: 50
            maximum: 100
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated disposition log entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "400":
          description: Invalid lead ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Lead not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/leads/{id}/dispositions/{dispositionId}/refire:
    post:
      summary: Re-fire a lead disposition
      description: Re-processes a previously recorded disposition for a lead through the disposition engine (source tagged
        `refire`). Useful to re-trigger CPA/return side-effects. The disposition must belong to the lead and must
        reference a lead sale or call sale (`400` otherwise). No request body — everything is derived from the
        disposition row. Auth via `withOwner` (admin/owner session).
      operationId: refireLeadDisposition
      tags:
        - Leads
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Lead UUID
          schema:
            type: string
            format: uuid
        - name: dispositionId
          in: path
          required: true
          description: Disposition UUID to re-fire
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Disposition re-processed
          content:
            application/json:
              example:
                success: true
                data:
                  dispositionId: e5f6a7b8-c9d0-1234-e5f6-a7b8c9d01234
                  cpaTriggered: false
                  returnTriggered: false
                  unmapped: false
        "400":
          description: Invalid IDs, or the disposition has no associated sale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Lead or disposition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/offers/{offerId}/field-override-impact:
    get:
      summary: Preview field-override impact for an offer
      description: Returns the campaigns under an offer that would be affected if the given fields change — i.e. campaigns
        that shadow those fields via `fieldOverrides` or that define custom campaign fields colliding with them. Only
        campaigns in status `testing`, `active`, or `paused` are considered. Used by the offer editor to warn before
        saving field-setting changes.
      operationId: getOfferFieldOverrideImpact
      tags:
        - Offers
      security:
        - sessionCookie: []
      parameters:
        - name: offerId
          in: path
          required: true
          description: Offer UUID
          schema:
            type: string
            format: uuid
        - name: fields
          in: query
          required: true
          description: Comma-separated list of field names being changed.
          schema:
            type: string
            example: homeownerStatus,creditScore
      responses:
        "200":
          description: Impacted campaigns (empty array when nothing is affected)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        campaignId:
                          type: string
                          format: uuid
                        campaignName:
                          type: string
                        displayId:
                          type: integer
                        status:
                          type: string
                        shadowedFields:
                          type: array
                          items:
                            type: string
                        customFieldCollisions:
                          type: array
                          items:
                            type: string
                        updatedAt:
                          type: string
                          format: date-time
              example:
                data:
                  - campaignId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    campaignName: Solar Leads - Facebook Q1
                    displayId: 204
                    status: active
                    shadowedFields:
                      - homeownerStatus
                    customFieldCollisions: []
                    updatedAt: 2026-06-30T12:00:00.000Z
        "400":
          description: Missing `fields` query param
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Offer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/offers/{offerId}/scarcity-state:
    get:
      summary: Get offer DWRR scarcity state
      description: "Returns the dynamic weighted round-robin (DWRR / \"scarcity_weighted\") state for an offer, powering the
        offer detail page's Scarcity Weights tab: per-contract weights, expected vs actual delivery, deficits, top-state
        contributions, snapshot freshness, cron heartbeat, and (when shadow mode is on) a 7-day shadow match-rate
        summary. Returns `400` when the offer is neither `scarcity_weighted` nor shadow-enabled (no DWRR state to
        display)."
      operationId: getOfferScarcityState
      tags:
        - Offers
      security:
        - sessionCookie: []
      parameters:
        - name: offerId
          in: path
          required: true
          description: Offer UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: DWRR scarcity state for the offer
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      offerId:
                        type: string
                        format: uuid
                      distributionRule:
                        type: string
                      shadowScarcityEnabled:
                        type: boolean
                      scarcityConfig:
                        type: object
                        properties:
                          alpha:
                            type: number
                          includeZip:
                            type: boolean
                          paceEnabled:
                            type: boolean
                      snapshotLastUpdatedAt:
                        type: string
                        format: date-time
                        nullable: true
                      cronLastRunAt:
                        type: string
                        format: date-time
                        nullable: true
                      cronLastRunStatus:
                        type: string
                        enum:
                          - ok
                          - error
                        nullable: true
                      cronAgeMinutes:
                        type: integer
                        nullable: true
                      leadsToday:
                        type: integer
                      contracts:
                        type: array
                        items:
                          type: object
                          properties:
                            contractId:
                              type: string
                              format: uuid
                            contractName:
                              type: string
                            scarcityWeight:
                              type: number
                              nullable: true
                            totalCoveredVolume:
                              type: number
                              nullable: true
                            totalCoveredVolumeUpdatedAt:
                              type: string
                              format: date-time
                              nullable: true
                            expectedDelivered:
                              type: number
                            actualDelivered:
                              type: number
                            deficit:
                              type: number
                            topStateContributions:
                              type: array
                              items:
                                type: object
                                properties:
                                  state:
                                    type: string
                                  weight:
                                    type: number
                      shadowSummary:
                        type: object
                        nullable: true
                        properties:
                          last7dRuns:
                            type: integer
                          matchRate:
                            type: number
                          perStateMismatchCount:
                            type: array
                            items:
                              type: object
                              properties:
                                state:
                                  type: string
                                mismatches:
                                  type: integer
                          recentMismatches:
                            type: array
                            items:
                              type: object
        "400":
          description: Offer is not using DWRR and shadow mode is off
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Offer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/offers/{offerId}/scarcity-state/recompute:
    post:
      summary: Recompute offer DWRR scarcity state
      description: Operator-triggered recompute of an offer's scarcity snapshots and per-contract weights. Runs the same
        idempotent single-offer routine the hourly cron uses (DELETE+INSERT of snapshot rows, UPDATE of
        `offerContract`). Empty request body — the offer is derived from the URL. Never throws internally; a compute
        failure surfaces as an `error` string in the result.
      operationId: recomputeOfferScarcityState
      tags:
        - Offers
      security:
        - sessionCookie: []
      parameters:
        - name: offerId
          in: path
          required: true
          description: Offer UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Recompute result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      snapshotsWritten:
                        type: integer
                      weightsUpdated:
                        type: integer
                      durationMs:
                        type: integer
                      error:
                        type: string
                        description: Present only when the recompute failed.
              example:
                data:
                  snapshotsWritten: 42
                  weightsUpdated: 6
                  durationMs: 118
        "400":
          description: Invalid offer ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "404":
          description: Offer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/partners/{id}/payout-view:
    get:
      summary: Get a partner's payout view
      description: 'Returns the canonical "available to be paid" view for a partner: `totalBalance` (perpetual accrual),
        `lockedInOpenPayouts` (sum of payouts in `pending`/`processing`/`failed`), `availableBalance` (total minus
        locked), and the list of open payout rows. Amounts are decimal strings. Admin session only.'
      operationId: getPartnerPayoutView
      tags:
        - Partners
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Partner UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Partner payout view
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      partnerId:
                        type: string
                        format: uuid
                      totalBalance:
                        type: string
                      lockedInOpenPayouts:
                        type: string
                      availableBalance:
                        type: string
                      openPayouts:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            amount:
                              type: string
                            status:
                              type: string
                            createdAt:
                              type: string
                              format: date-time
              example:
                data:
                  partnerId: a7b8c9d0-1234-5678-9abc-def012345678
                  totalBalance: "1250.00"
                  lockedInOpenPayouts: "250.00"
                  availableBalance: "1000.00"
                  openPayouts:
                    - id: b8c9d0e1-2345-6789-abcd-ef0123456789
                      amount: "250.00"
                      status: pending
                      createdAt: 2026-06-28T09:00:00.000Z
        "401":
          description: Unauthorized (non-admin session)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/sms-templates:
    get:
      summary: List SMS templates
      description: Returns a paginated list of SMS templates. Tenant admins see their own tenant's rows plus global (`tenantId
        IS NULL`) rows; superadmin sees all. Ordered newest-first.
      operationId: listSmsTemplates
      tags:
        - "Admin: SMS Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by template status.
          schema:
            type: string
            enum:
              - active
              - inactive
        - name: limit
          in: query
          description: Max results per page (capped at 100).
          schema:
            type: integer
            default: 20
            maximum: 100
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of SMS templates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
    post:
      summary: Create an SMS template
      description: Creates a tenant-scoped SMS template. The `tenantId` is taken from the session and never accepted from the
        body — superadmin must use the superadmin SMS routes to create global templates. Slug must be unique within the
        tenant (`409` on collision).
      operationId: createSmsTemplate
      tags:
        - "Admin: SMS Templates"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - slug
                - name
                - body
              properties:
                slug:
                  type: string
                  maxLength: 100
                  pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
                  description: Lowercase kebab-case identifier, unique within the tenant.
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                description:
                  type: string
                  maxLength: 500
                  nullable: true
                body:
                  type: string
                  minLength: 1
                  maxLength: 1600
                  description: SMS body (max 1600 chars / 10 GSM-7 segments).
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
            example:
              slug: welcome-sms
              name: Welcome SMS
              body: Hi {{firstName}}, thanks for your interest!
              status: active
      responses:
        "201":
          description: SMS template created
          content:
            application/json:
              example:
                data:
                  id: c9d0e1f2-3456-7890-abcd-ef0123456789
                  slug: welcome-sms
                  name: Welcome SMS
                  body: Hi {{firstName}}, thanks for your interest!
                  status: active
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "409":
          description: A template with this slug already exists in the tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/sms-templates/{id}:
    get:
      summary: Get an SMS template
      description: Returns a single SMS template by id. Tenant admins can read their own tenant's row or a global (`tenantId
        IS NULL`) row; superadmin can read any.
      operationId: getSmsTemplate
      tags:
        - "Admin: SMS Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: SMS template UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: SMS template detail
          content:
            application/json:
              example:
                data:
                  id: c9d0e1f2-3456-7890-abcd-ef0123456789
                  slug: welcome-sms
                  name: Welcome SMS
                  body: Hi {{firstName}}, thanks for your interest!
                  status: active
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
        "401":
          description: Unauthorized
        "404":
          description: SMS template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update an SMS template
      description: Updates a tenant-scoped SMS template (partial update). Tenant admins can only mutate their own tenant's
        rows — attempts to edit a global template return `403`. Slug is immutable here.
      operationId: updateSmsTemplate
      tags:
        - "Admin: SMS Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: SMS template UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                description:
                  type: string
                  maxLength: 500
                  nullable: true
                body:
                  type: string
                  minLength: 1
                  maxLength: 1600
                status:
                  type: string
                  enum:
                    - active
                    - inactive
            example:
              name: Welcome SMS (v2)
              status: active
      responses:
        "200":
          description: Updated SMS template
          content:
            application/json:
              example:
                data:
                  id: c9d0e1f2-3456-7890-abcd-ef0123456789
                  slug: welcome-sms
                  name: Welcome SMS (v2)
                  status: active
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Global SMS templates can only be edited by superadmin
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: SMS template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Retire an SMS template
      description: "Soft-retires an SMS template by setting its status to `inactive` (never a hard delete — the template id
        can be embedded in in-flight wizard snapshots). Idempotent: retiring an already `inactive` template is a no-op
        that returns the row. Tenant admins cannot delete global templates (`403`)."
      operationId: deleteSmsTemplate
      tags:
        - "Admin: SMS Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: SMS template UUID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: SMS template retired (or already inactive)
          content:
            application/json:
              example:
                data:
                  id: c9d0e1f2-3456-7890-abcd-ef0123456789
                  slug: welcome-sms
                  name: Welcome SMS
                  status: inactive
        "401":
          description: Unauthorized
        "403":
          description: Global SMS templates can only be deleted by superadmin
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: SMS template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/users/{id}/membership:
    patch:
      summary: Activate or deactivate a user's membership
      description: Activates or deactivates a user's membership(s) in the caller's current tenant. Because a user can hold
        multiple memberships in a tenant (one per `appRole`), all memberships the caller is permitted to manage are
        updated together. Callers cannot deactivate themselves. Requires tenant context (`403` for superadmin with no
        tenant selected).
      operationId: updateUserMembership
      tags:
        - Users
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Target user UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - activate
                    - deactivate
            example:
              action: deactivate
      responses:
        "200":
          description: Membership status updated
          content:
            application/json:
              example:
                data:
                  userId: 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                  status: inactive
                  count: 2
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Tenant context required, attempt to deactivate self, or caller lacks permission to manage one of the target
            roles.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Membership not found in this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/search:
    get:
      summary: Global entity search (command-K palette)
      description: Searches buyers, partners, contracts, campaigns, offers, verticals, people, intake centers, and leads in
        one call for the admin command palette. Business entities match on name (case-insensitive, prefix matches ranked
        above substring, active-status boosted) and exact displayId. Leads match ONLY by exact displayId or exact
        email/phone via hashed lookup columns — PII columns are never pattern-matched. People match by exact displayId
        or exact email/phone via lookup tables. Email/phone lookups require the caller to have piiAccess; lead/person
        names are masked for callers without piiAccess. Results are tenant-scoped, capped at 5 per entity and 20 total
        after relevance merge.
      operationId: adminGlobalSearch
      tags:
        - Search
      security:
        - sessionCookie: []
      parameters:
        - name: q
          in: query
          required: true
          description: Search query. Minimum 2 characters after trimming.
          schema:
            type: string
            minLength: 2
            example: acme
      responses:
        "200":
          description: Merged, relevance-ranked search results (max 20)
          content:
            application/json:
              example:
                data:
                  - entityType: buyer
                    id: 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                    displayId: 142
                    name: Acme Insurance Group
                    sublabel: Acme Holdings LLC
                    href: /admin/buyers/9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                  - entityType: lead
                    id: 1b2c3d4e-5f6a-7b8c-9d0e-1f2a3b4c5d6e
                    displayId: 228904
                    name: J*** D***
                    sublabel: CA
                    href: /admin/leads/1b2c3d4e-5f6a-7b8c-9d0e-1f2a3b4c5d6e
        "400":
          description: Query shorter than 2 characters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized (no session, stale session, or no active membership)
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/notifications:
    get:
      summary: List notifications for the current user
      description: |
        Returns the authenticated user's in-app notifications, newest first, plus an
        `unreadCount` of the user's total unread notifications. Notifications are scoped
        to the user (`notification.userId`), not the tenant. Available to any authenticated
        session role (admin, buyer, partner, superadmin).

        The `pagination.total` field reflects the number of items in the current page
        (`items.length`), NOT the full result count, and `hasMore` is `true` whenever the
        page is full (`items.length === limit`).
      operationId: listNotifications
      tags:
        - "Admin: Notifications"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          description: Page size (max 50).
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: unread
          in: query
          description: When `true`, only unread notifications are returned.
          schema:
            type: boolean
      responses:
        "200":
          description: List of notifications with unread count
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - unreadCount
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        userId:
                          type: string
                          format: uuid
                        type:
                          type: string
                        title:
                          type: string
                        body:
                          type: string
                          nullable: true
                        link:
                          type: string
                          nullable: true
                        read:
                          type: boolean
                        readAt:
                          type: string
                          format: date-time
                          nullable: true
                        createdAt:
                          type: string
                          format: date-time
                  unreadCount:
                    type: integer
                    description: Total unread notifications for the user (not just this page).
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                      offset:
                        type: integer
                      total:
                        type: integer
                        description: Count of items on the current page, not the full result set.
                      hasMore:
                        type: boolean
        "403":
          description: User identity required (session lacks a user id)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/notifications/{id}:
    patch:
      summary: Mark a notification as read
      description: |
        Marks a single notification as read (`read=true`, `readAt=now`). Scoped to the
        authenticated user — a notification owned by another user is silently unaffected.
        No request body. Always returns `{ data: { success: true } }` even when the id
        does not match a row owned by the user.
      operationId: markNotificationRead
      tags:
        - "Admin: Notifications"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Notification UUID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Notification marked read (idempotent)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "403":
          description: User identity required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/notifications/read-all:
    post:
      summary: Mark all notifications as read
      description: |
        Marks every unread notification owned by the authenticated user as read
        (`read=true`, `readAt=now`). No request body. Idempotent.
      operationId: markAllNotificationsRead
      tags:
        - "Admin: Notifications"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: All notifications marked read
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "403":
          description: User identity required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/notifications/subscriptions:
    get:
      summary: List effective notification subscriptions for the current user
      description: |
        Returns the effective per-trigger notification subscriptions for the authenticated
        user. For each trigger key the endpoint emits a single row resolved from either a
        user-level override or the applicable role default, with a `source` discriminator.
        A user-level override always wins over the role default — including an explicit
        opt-out (`enabled=false`). Requires a user-scoped session.
      operationId: listNotificationSubscriptions
      tags:
        - "Admin: Notification Subscriptions"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Effective subscriptions
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        triggerKey:
                          type: string
                        channels:
                          type: array
                          items:
                            type: string
                            enum:
                              - in_app
                              - email
                        enabled:
                          type: boolean
                        source:
                          type: string
                          enum:
                            - user
                            - role
                          description: Whether this row came from a user override or a role default.
              example:
                data:
                  - triggerKey: buyer.approved
                    channels:
                      - in_app
                      - email
                    enabled: true
                    source: user
                  - triggerKey: refund.requested
                    channels:
                      - in_app
                    enabled: true
                    source: role
        "403":
          description: Notification subscriptions require a user-scoped session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Upsert a user-level subscription override
      description: |
        Creates or updates the authenticated user's per-trigger override. Upsert keyed on
        `(tenantId, userId, triggerKey)`. `triggerKey` must be a known trigger; `channels`
        must contain at least one of `in_app` / `email`. Superadmin with no tenant context
        cannot upsert (tenant id is required for the write).
      operationId: upsertNotificationSubscription
      tags:
        - "Admin: Notification Subscriptions"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - triggerKey
                - channels
                - enabled
              properties:
                triggerKey:
                  type: string
                  description: Known notification trigger key.
                channels:
                  type: array
                  minItems: 1
                  items:
                    type: string
                    enum:
                      - in_app
                      - email
                enabled:
                  type: boolean
            example:
              triggerKey: buyer.approved
              channels:
                - in_app
                - email
              enabled: true
      responses:
        "200":
          description: "Subscription upserted (always source: user)"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      triggerKey:
                        type: string
                      channels:
                        type: array
                        items:
                          type: string
                      enabled:
                        type: boolean
                      source:
                        type: string
                        enum:
                          - user
        "400":
          description: Validation error (unknown triggerKey, empty channels)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Notification subscriptions require a user-scoped session, or tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/notifications/subscriptions/{triggerKey}:
    delete:
      summary: Remove a user-level subscription override
      description: |
        Deletes the authenticated user's override row for the given `triggerKey`, so the
        user falls back to the role default on the next read. Idempotent — returns `200`
        even when no override row exists.
      operationId: deleteNotificationSubscription
      tags:
        - "Admin: Notification Subscriptions"
      security:
        - sessionCookie: []
      parameters:
        - name: triggerKey
          in: path
          required: true
          description: Notification trigger key (URL-encoded).
          schema:
            type: string
          example: buyer.approved
      responses:
        "200":
          description: Override removed (idempotent)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
        "403":
          description: User identity required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/notifications/role-defaults:
    get:
      summary: List role-default notification subscriptions
      description: |
        Lists the tenant's role-level default subscriptions (rows with `userId = null`).
        Admin-only — non-admin sessions receive `403`.

        Tests may pass an `X-Test-Role` header to drive a recognizably non-admin claim; the
        effective role is the header value when it differs from the session role, so a test
        cannot escalate to admin from a non-admin session.
      operationId: listNotificationRoleDefaults
      tags:
        - "Admin: Notification Subscriptions"
      security:
        - sessionCookie: []
      parameters:
        - name: X-Test-Role
          in: header
          required: false
          description: Test-only role override. Effective role is the min-privilege of the session role and this header.
          schema:
            type: string
            enum:
              - admin
              - buyer
              - partner
              - superadmin
      responses:
        "200":
          description: Role-default subscriptions
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        triggerKey:
                          type: string
                        role:
                          type: string
                          enum:
                            - admin
                            - buyer
                            - partner
                            - superadmin
                        channels:
                          type: array
                          items:
                            type: string
                            enum:
                              - in_app
                              - email
                        enabled:
                          type: boolean
              example:
                data:
                  - triggerKey: buyer.approved
                    role: admin
                    channels:
                      - in_app
                      - email
                    enabled: true
        "403":
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Upsert a role-default notification subscription
      description: |
        Creates or updates a role-level default subscription for the tenant. Admin-only.
        Upsert keyed on `(tenantId, role, triggerKey)`. `role` is required in the body
        (unlike the user-subscription upsert). Superadmin with no tenant context cannot
        upsert (tenant id is required for the write).
      operationId: upsertNotificationRoleDefault
      tags:
        - "Admin: Notification Subscriptions"
      security:
        - sessionCookie: []
      parameters:
        - name: X-Test-Role
          in: header
          required: false
          description: Test-only role override (see GET).
          schema:
            type: string
            enum:
              - admin
              - buyer
              - partner
              - superadmin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - triggerKey
                - channels
                - enabled
                - role
              properties:
                triggerKey:
                  type: string
                role:
                  type: string
                  enum:
                    - admin
                    - buyer
                    - partner
                    - superadmin
                channels:
                  type: array
                  minItems: 1
                  items:
                    type: string
                    enum:
                      - in_app
                      - email
                enabled:
                  type: boolean
            example:
              triggerKey: buyer.approved
              role: admin
              channels:
                - in_app
                - email
              enabled: true
      responses:
        "200":
          description: Role default upserted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      triggerKey:
                        type: string
                      role:
                        type: string
                      channels:
                        type: array
                        items:
                          type: string
                      enabled:
                        type: boolean
        "400":
          description: Validation error (unknown triggerKey, empty channels, invalid role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Admin role required, or tenant context required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/approvals/pending:
    get:
      summary: Get the approval queue
      description: |
        Returns the admin approval queue: pending partners, pending buyers, and requested
        offer enrollments (each capped at 5 preview items), plus badge counts. Pending
        partner/buyer signup answers are attached per item. The `partners` and `buyers`
        counts come from the `member` table (signup-pending only), so they may differ from
        the number of preview items (which come from entity status). Admin/superadmin only.
      operationId: getPendingApprovals
      tags:
        - "Admin: Approvals"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Approval queue summary
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      partners:
                        $ref: "#/components/schemas/PaginatedResponse"
                      buyers:
                        $ref: "#/components/schemas/PaginatedResponse"
                      enrollments:
                        type: object
                        properties:
                          count:
                            type: integer
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                id:
                                  type: string
                                  format: uuid
                                offerId:
                                  type: string
                                  format: uuid
                                partnerId:
                                  type: string
                                  format: uuid
                                offerName:
                                  type: string
                                  nullable: true
                                partnerName:
                                  type: string
                                  nullable: true
                                createdAt:
                                  type: string
                                  format: date-time
                      refunds:
                        type: object
                        properties:
                          count:
                            type: integer
                      totalPending:
                        type: integer
              example:
                data:
                  partners:
                    count: 2
                    items:
                      - id: 5f2c0a3e-1b7d-4c9a-8e2f-0a1b2c3d4e5f
                        name: LeadGen Partners LLC
                        email: ops@leadgen-partners.com
                        company: LeadGen Partners LLC
                        createdAt: 2026-07-01 10:00:00+00:00
                        answers:
                          - question: Primary traffic source?
                            answer: Paid social
                  buyers:
                    count: 1
                    items: []
                  enrollments:
                    count: 0
                    items: []
                  refunds:
                    count: 3
                  totalPending: 6
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/approvals/buyers/{id}:
    post:
      summary: Approve or reject a pending buyer
      description: |
        Approves or rejects a buyer whose status is `pending`. `approve` transitions the
        buyer (and its linked tenant member rows) to `active`; `reject` transitions to
        `inactive`. A `reason` is required when `action=reject`. The transition uses a
        concurrency guard (`status=pending`), so a buyer already processed returns `400`.
        Sends account-approved / account-denied email to the buyer and posts a Slack
        notification when configured. Admin/superadmin only.
      operationId: approveBuyer
      tags:
        - "Admin: Approvals"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Buyer UUID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - approve
                    - reject
                reason:
                  type: string
                  description: Required when `action=reject`.
            example:
              action: approve
      responses:
        "200":
          description: Buyer approved or rejected
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - active
                          - inactive
                      action:
                        type: string
                        enum:
                          - approve
                          - reject
        "400":
          description: Validation error, missing rejection reason, or buyer already processed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Buyer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/approvals/partners/{id}:
    post:
      summary: Approve or reject a pending partner
      description: |
        Approves or rejects a partner whose status is `pending`. `approve` transitions the
        partner (and its linked tenant member rows) to `active`; `reject` transitions to
        `inactive`. A `reason` is required when `action=reject`. On approval, a posting API
        key is auto-provisioned and a CRM lifecycle event is enqueued (both best-effort — a
        failure there does not fail the approval). The transition uses a concurrency guard,
        so a partner already processed returns `400`. Sends approval/denial email and posts
        a Slack notification when configured. Admin/superadmin only.
      operationId: approvePartner
      tags:
        - "Admin: Approvals"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Partner UUID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - approve
                    - reject
                reason:
                  type: string
                  description: Required when `action=reject`.
            example:
              action: reject
              reason: Incomplete traffic sourcing details
      responses:
        "200":
          description: Partner approved or rejected
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - active
                          - inactive
                      action:
                        type: string
                        enum:
                          - approve
                          - reject
        "400":
          description: Validation error, missing rejection reason, or partner already processed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Partner not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/alerts:
    get:
      summary: List anomaly alerts
      description: |
        Returns a paginated list of anomaly-detection alerts for the tenant, newest first
        (by `detectedAt`). Requires the `anomalyDetection` tenant feature — a tenant without
        it receives `403 FEATURE_DISABLED`. Date filters are interpreted in the tenant
        timezone. Admin only.
      operationId: listAnomalyAlerts
      tags:
        - "Admin: Alerts"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - open
              - acknowledged
              - resolved
              - dismissed
        - name: signal
          in: query
          schema:
            type: string
            enum:
              - acceptance_rate_drop
              - delivery_failure_rate
              - volume_anomaly
              - margin_erosion
              - invalid_field_spike
        - name: severity
          in: query
          schema:
            type: string
            enum:
              - warning
              - critical
        - name: entityType
          in: query
          description: Filter by the entity type the alert is about (free-form string).
          schema:
            type: string
        - name: dateFrom
          in: query
          description: Inclusive lower bound on `detectedAt` (YYYY-MM-DD, tenant timezone).
          schema:
            type: string
            format: date
        - name: dateTo
          in: query
          description: Inclusive upper bound on `detectedAt` (YYYY-MM-DD, tenant timezone).
          schema:
            type: string
            format: date
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated list of anomaly alerts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Anomaly detection feature disabled for this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/alerts/{id}:
    get:
      summary: Get an anomaly alert
      description: Returns a single anomaly alert scoped to the tenant. Admin only.
      operationId: getAnomalyAlert
      tags:
        - "Admin: Alerts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Anomaly alert UUID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Anomaly alert
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "404":
          description: Alert not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update anomaly alert status
      description: |
        Transitions an anomaly alert's status. Only the status transitions permitted by the
        anomaly-alert state machine are accepted; an illegal transition returns `400`.
        Moving to `acknowledged` records `acknowledgedBy`/`acknowledgedAt`; moving to
        `resolved` records `resolvedAt`. Admin only.
      operationId: updateAnomalyAlert
      tags:
        - "Admin: Alerts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Anomaly alert UUID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - open
                    - acknowledged
                    - resolved
                    - dismissed
            example:
              status: acknowledged
      responses:
        "200":
          description: Updated anomaly alert
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          description: Validation error or illegal status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Alert not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/vertical-groups:
    get:
      summary: List vertical groups
      description: Returns a paginated list of vertical groups with vertical counts per group.
      operationId: listVerticalGroups
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: search
          in: query
          description: Filter by name (case-insensitive partial match)
          schema:
            type: string
        - name: sort
          in: query
          description: Sort column
          schema:
            type: string
            enum:
              - name
              - createdAt
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of vertical groups
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Insurance
                    dedupScope: verticalGroup
                    attributionScope: verticalGroup
                    attributionPolicy: null
                    verticalCount: 3
                    createdAt: 2025-01-15 10:00:00+00:00
                    updatedAt: 2025-01-15 10:00:00+00:00
                pagination:
                  total: 5
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid filter parameters
        "401":
          description: Unauthorized
    post:
      summary: Create a vertical group
      description: Creates a new vertical group for organizing verticals and their shared disposition configurations.
      operationId: createVerticalGroup
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                dedupScope:
                  type: string
                  enum:
                    - offer
                    - vertical
                    - group
                  default: offer
                attributionScope:
                  type: string
                  enum:
                    - offer
                    - vertical
                    - group
                  default: offer
                attributionPolicy:
                  type: object
                  additionalProperties: true
                  nullable: true
            example:
              name: Insurance Products
              dedupScope: offer
              attributionScope: offer
      responses:
        "201":
          description: Vertical group created
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Insurance Products
                  dedupScope: verticalGroup
                  attributionScope: verticalGroup
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/admin/vertical-groups/{id}:
    get:
      summary: Get a vertical group
      description: Returns a single vertical group with its associated verticals.
      operationId: getVerticalGroup
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Vertical group with verticals
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Insurance Products
                  verticals:
                    - id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                      name: Auto Insurance
                      slug: auto-insurance
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
    put:
      summary: Update a vertical group
      description: Updates name, dedup scope, attribution scope, or attribution policy of a vertical group.
      operationId: updateVerticalGroup
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              name: Insurance Products (Updated)
              dedupScope: vertical
      responses:
        "200":
          description: Updated vertical group
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
    patch:
      summary: Update a vertical group (diff)
      description: Partial update — sets only the supplied keys (default-stripped update schema). Accepts an optional
        `_snapshot` envelope carrying page-load values of the diffed fields; when present, the server returns 409
        CONFLICT if any submitted field changed since the snapshot (field-level concurrent-edit guard).
      operationId: patchVerticalGroup
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              name: Insurance Products (Updated)
              _snapshot:
                name: Insurance Products
      responses:
        "200":
          description: Updated vertical group
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
        "409":
          description: A submitted field changed since the client snapshot
    delete:
      summary: Delete a vertical group
      description: Deletes a vertical group and unlinks any verticals assigned to it.
      operationId: deleteVerticalGroup
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Vertical group deleted
          content:
            application/json:
              example:
                data:
                  success: true
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
  /api/admin/vertical-groups/{id}/verticals:
    put:
      summary: Update verticals in a group
      description: Replaces the set of verticals assigned to a vertical group.
      operationId: updateVerticalGroupVerticals
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - verticalIds
              properties:
                verticalIds:
                  type: array
                  items:
                    type: string
                    format: uuid
            example:
              verticalIds:
                - b2c3d4e5-f6a7-8901-bcde-f12345678901
                - c3d4e5f6-a7b8-9012-cdef-123456789012
      responses:
        "200":
          description: Updated list of verticals in the group
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
  /api/admin/vertical-groups/{id}/dispositions:
    get:
      summary: List dispositions for a vertical group
      description: Returns a paginated list of dispositions configured for a vertical group.
      operationId: listVerticalGroupDispositions
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of vertical group dispositions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
    post:
      summary: Create a disposition for a vertical group
      description: Adds a new disposition code to a vertical group's configuration.
      operationId: createVerticalGroupDisposition
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              label: Qualified
              slug: qualified
              category: converted
              triggersCpa: true
              triggersReturn: false
              returnReason: null
              sortOrder: 1
      responses:
        "201":
          description: Disposition created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
        "409":
          description: Duplicate slug in this group
  /api/admin/vertical-groups/{id}/dispositions/{dispId}:
    put:
      summary: Update a vertical group disposition
      description: Updates fields on an existing disposition within a vertical group.
      operationId: updateVerticalGroupDisposition
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: dispId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              label: Qualified - Updated
              triggersCpa: false
              isActive: true
      responses:
        "200":
          description: Updated disposition
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Vertical group or disposition not found
        "409":
          description: Duplicate slug in this group
    delete:
      summary: Delete a vertical group disposition
      description: Removes a disposition from a vertical group.
      operationId: deleteVerticalGroupDisposition
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: dispId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Disposition deleted
          content:
            application/json:
              example:
                data:
                  success: true
        "401":
          description: Unauthorized
        "404":
          description: Vertical group or disposition not found
  /api/admin/vertical-groups/{id}/dispositions/clone:
    post:
      summary: Clone dispositions from another vertical group
      description: Copies all dispositions from a source vertical group to the target group, skipping slug conflicts.
      operationId: cloneVerticalGroupDispositions
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Target vertical group ID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - sourceGroupId
              properties:
                sourceGroupId:
                  type: string
                  format: uuid
            example:
              sourceGroupId: d4e5f6a7-b8c9-0123-defg-234567890123
      responses:
        "200":
          description: Clone result
          content:
            application/json:
              example:
                data:
                  clonedCount: 5
        "400":
          description: Validation error (missing or invalid sourceGroupId)
        "401":
          description: Unauthorized
        "404":
          description: Source or target vertical group not found
        "409":
          description: Slug conflicts detected
  /api/admin/vertical-groups/{id}/dispositions/reorder:
    put:
      summary: Reorder dispositions in a vertical group
      description: Updates the sortOrder of multiple dispositions in a single request.
      operationId: reorderVerticalGroupDispositions
      tags:
        - "Admin: Vertical Groups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - items
              properties:
                items:
                  type: array
                  items:
                    type: object
                    required:
                      - id
                      - sortOrder
                    properties:
                      id:
                        type: string
                        format: uuid
                      sortOrder:
                        type: integer
            example:
              items:
                - id: e5f6a7b8-c9d0-1234-efgh-345678901234
                  sortOrder: 0
                - id: f6a7b8c9-d0e1-2345-fghi-456789012345
                  sortOrder: 1
      responses:
        "200":
          description: Reordered dispositions
        "400":
          description: Validation error (duplicate IDs, IDs not in group)
        "401":
          description: Unauthorized
        "404":
          description: Vertical group not found
  /api/admin/intake-centers:
    get:
      summary: List intake centers
      description: Returns a paginated list of intake centers with vertical group counts.
      operationId: listIntakeCenters
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - name
              - status
              - deliveryMethod
              - createdAt
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of intake centers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "400":
          description: Invalid filter parameters
        "401":
          description: Unauthorized
    post:
      summary: Create an intake center
      description: Creates a new intake center for managing buyer lead delivery and disposition mappings.
      operationId: createIntakeCenter
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            example:
              name: Acme Legal Intake
              status: active
              deliveryMethod: http_post
              deliveryUrl: https://api.acme.com/leads
              deliveryConfig: {}
              notes: Primary intake center for Acme
      responses:
        "201":
          description: Intake center created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/admin/intake-centers/{id}:
    get:
      summary: Get an intake center
      description: Returns a single intake center with vertical group statistics.
      operationId: getIntakeCenter
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Intake center with vertical group stats
        "401":
          description: Unauthorized
        "404":
          description: Intake center not found
    put:
      summary: Update an intake center
      description: Updates intake center fields including name, status, delivery config, status mappings, and
        conversion/rejection statuses.
      operationId: updateIntakeCenter
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              name: Acme Legal Intake (Updated)
              status: active
              deliveryMethod: http_post
              statusMappings:
                QUAL: qualified
                DQ: disqualified
              conversionStatuses:
                - qualified
                - retained
              rejectionStatuses:
                - disqualified
                - no_show
      responses:
        "200":
          description: Updated intake center
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Intake center not found
    delete:
      summary: Deactivate an intake center
      description: Soft-deletes an intake center by setting status to inactive.
      operationId: deleteIntakeCenter
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Intake center deactivated
          content:
            application/json:
              example:
                data:
                  success: true
        "401":
          description: Unauthorized
        "404":
          description: Intake center not found
  /api/admin/intake-centers/{id}/dispositions:
    get:
      summary: List intake center disposition mappings
      description: Returns disposition mappings for an intake center, filtered by vertical group. Includes joined disposition
        code and vertical group disposition data.
      operationId: listIntakeCenterDispositions
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: verticalGroupId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of disposition mappings
        "400":
          description: verticalGroupId query param required
        "401":
          description: Unauthorized
    post:
      summary: Create intake center disposition mappings
      description: Creates one or more disposition mappings for an intake center. Accepts a single object or an array. Skips
        duplicates by externalCode.
      operationId: createIntakeCenterDispositions
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              verticalGroupId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              externalCode: QUAL
              externalLabel: Qualified
              verticalGroupDispositionId: b2c3d4e5-f6a7-8901-bcde-f12345678901
              isActive: true
              sortOrder: 0
      responses:
        "201":
          description: Disposition mappings created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
    put:
      summary: Update an intake center disposition mapping
      description: Updates a single disposition mapping by ID (passed in body).
      operationId: updateIntakeCenterDisposition
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              id: d4e5f6a7-b8c9-0123-defg-234567890123
              externalCode: QUAL_V2
              overrideTriggersCpa: true
      responses:
        "200":
          description: Updated mapping
        "400":
          description: Validation error or missing mapping id
        "401":
          description: Unauthorized
        "404":
          description: Disposition mapping not found
    delete:
      summary: Delete an intake center disposition mapping
      description: Deletes a single disposition mapping. ID can be passed as query param `mappingId` or in body as `id`.
      operationId: deleteIntakeCenterDisposition
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: mappingId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Mapping deleted
          content:
            application/json:
              example:
                data:
                  success: true
        "400":
          description: Mapping id required
        "401":
          description: Unauthorized
        "403":
          description: Forbidden — owner access required
        "404":
          description: Disposition mapping not found
  /api/admin/intake-centers/{id}/dispositions/clone:
    post:
      summary: Clone IC disposition mappings between vertical groups
      description: Clones disposition mappings from one vertical group to another within the same intake center. Skips
        duplicates by externalCode.
      operationId: cloneIntakeCenterDispositions
      tags:
        - "Admin: Intake Centers"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fromVerticalGroupId
                - toVerticalGroupId
              properties:
                fromVerticalGroupId:
                  type: string
                  format: uuid
                toVerticalGroupId:
                  type: string
                  format: uuid
            example:
              fromVerticalGroupId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              toVerticalGroupId: b2c3d4e5-f6a7-8901-bcde-f12345678901
      responses:
        "200":
          description: Clone result
          content:
            application/json:
              example:
                data:
                  cloned: 8
        "400":
          description: Validation error (same source/target)
        "401":
          description: Unauthorized
        "404":
          description: Source vertical group mappings not found
  /api/admin/number-pool/assign:
    post:
      summary: Assign a tracking number to a campaign
      description: Assigns an available tracking number from the pool to a campaign. Requires the campaign to be linked to a
        call-enabled offer.
      operationId: assignTrackingNumber
      tags:
        - "Admin: Number Pool"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  type: string
                  format: uuid
            example:
              campaignId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "200":
          description: Number assigned
          content:
            application/json:
              example:
                data:
                  number: "+15551234567"
                  source: pool
        "400":
          description: Validation error (no offer, call not enabled)
        "401":
          description: Unauthorized
        "402":
          description: Insufficient credits to cover the first month of tracking-number rent (INSUFFICIENT_BALANCE)
        "404":
          description: Campaign or offer not found
        "422":
          description: Telephony not configured, area code not configured, or no tracking numbers available
        "500":
          description: Assignment failed (internal error)
        "502":
          description: Telephony provider purchase failed
  /api/admin/number-pool/release:
    post:
      summary: Release a tracking number back to the pool
      description: Unassigns a tracking number from its campaign and returns it to the available pool.
      operationId: releaseTrackingNumberToPool
      tags:
        - "Admin: Number Pool"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - trackingNumberId
              properties:
                trackingNumberId:
                  type: string
                  format: uuid
            example:
              trackingNumberId: e5f6a7b8-c9d0-1234-efgh-345678901234
      responses:
        "200":
          description: Number released
          content:
            application/json:
              example:
                data:
                  released: true
        "400":
          description: trackingNumberId is required
        "401":
          description: Unauthorized
  /api/admin/number-pool/release-to-provider:
    post:
      summary: Release a tracking number back to the telephony provider
      description: Permanently releases a tracking number, removing it from the pool entirely.
      operationId: releaseTrackingNumberToProvider
      tags:
        - "Admin: Number Pool"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - trackingNumberId
              properties:
                trackingNumberId:
                  type: string
                  format: uuid
            example:
              trackingNumberId: e5f6a7b8-c9d0-1234-efgh-345678901234
      responses:
        "200":
          description: Number released to provider
          content:
            application/json:
              example:
                data:
                  released: true
        "400":
          description: trackingNumberId is required
        "401":
          description: Unauthorized
        "404":
          description: Tracking number not found
        "422":
          description: Cannot release — number has active calls
        "502":
          description: Telephony provider release failed
  /api/admin/number-pool/stats:
    get:
      summary: Get number pool statistics
      description: Returns statistics about the tracking number pool (available, assigned, total counts).
      operationId: getNumberPoolStats
      tags:
        - "Admin: Number Pool"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Pool statistics
          content:
            application/json:
              example:
                data:
                  assigned: 20
                  available: 30
                  released: 5
                  estimatedMonthlyCost: 12.5
        "401":
          description: Unauthorized
  /api/admin/telephony/status:
    get:
      summary: Get telephony provider status
      description: Returns per-provider configuration status, showing whether credentials come from own keys, system keys, or
        are unconfigured.
      operationId: getTelephonyStatus
      tags:
        - "Admin: Telephony"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Provider status overview
          content:
            application/json:
              example:
                providers:
                  - name: telnyx
                    source: own
                    configured: true
                  - name: twilio
                    source: system
                    configured: true
                  - name: bandwidth
                    source: none
                    configured: false
                inheritTelephony: true
                hasOwnKeys: true
                systemKeysAvailable: true
        "401":
          description: Unauthorized
  /api/admin/integrations/status:
    get:
      summary: Get integration statuses for the current tenant
      description: Returns per-provider integration status for the current tenant. Used by the admin UI to render reconnect
        banners and dropdown empty states. Only providers with a `thirdPartyIntegration` row are included; missing
        providers should be treated as unknown/connected by the caller.
      operationId: getIntegrationStatuses
      tags:
        - Integrations
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Map of provider key to status row
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: Map keyed by provider (e.g. `facebook_capi`, `google_ads`, `google_sheets`). Each value is an
                      IntegrationStatusEntry.
                    additionalProperties:
                      type: object
                      required:
                        - provider
                        - status
                        - statusReason
                        - statusCode
                        - lastErrorAt
                        - lastSuccessAt
                        - revokedAt
                      properties:
                        provider:
                          type: string
                          example: facebook_capi
                        status:
                          type: string
                          enum:
                            - connected
                            - reconnect_required
                            - error
                            - disconnected
                        statusReason:
                          type: string
                          nullable: true
                        statusCode:
                          type: string
                          nullable: true
                        lastErrorAt:
                          type: string
                          format: date-time
                          nullable: true
                        lastSuccessAt:
                          type: string
                          format: date-time
                          nullable: true
                        revokedAt:
                          type: string
                          format: date-time
                          nullable: true
              example:
                data:
                  facebook_capi:
                    provider: facebook_capi
                    status: reconnect_required
                    statusReason: Facebook access token expired or was revoked.
                    statusCode: oauth.token_revoked
                    lastErrorAt: 2026-05-08T14:22:11.000Z
                    lastSuccessAt: 2026-05-07T09:14:00.000Z
                    revokedAt: 2026-05-08T14:22:11.000Z
                  google_ads:
                    provider: google_ads
                    status: connected
                    statusReason: null
                    statusCode: null
                    lastErrorAt: null
                    lastSuccessAt: 2026-05-08T13:55:02.000Z
                    revokedAt: null
        "401":
          description: Unauthorized
          content:
            application/json:
              example:
                error:
                  code: UNAUTHORIZED
                  message: Login required
  /api/admin/integrations/crm/zoho:
    get:
      operationId: getZohoCrmIntegration
      tags:
        - Integrations
      summary: Get Zoho lifecycle CRM sync status
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Zoho CRM lifecycle sync status and recent sync failures
          content:
            application/json:
              example:
                data:
                  provider: zoho_crm
                  connected: true
                  enabled: true
                  status: connected
                  connectedEmail: admin@example.com
                  apiDomain: https://www.zohoapis.com
                  defaultDealStage: Qualification
                  enabledEvents:
                    - buyer.created
                    - buyer.first_paid
                    - contract.cap_full
                    - contract.sku_exhausted
                    - partner.created
                    - partner.active
                    - campaign.created
                    - campaign.activated
                  failures: []
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: updateZohoCrmIntegrationSettings
      tags:
        - Integrations
      summary: Update Zoho lifecycle CRM sync settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            example:
              defaultDealStage: Qualification
              enabledEvents:
                - buyer.created
                - buyer.first_paid
                - contract.cap_full
                - contract.sku_exhausted
                - partner.created
                - partner.active
                - campaign.created
                - campaign.activated
      responses:
        "200":
          description: Updated Zoho lifecycle CRM sync settings
          content:
            application/json:
              example:
                data:
                  provider: zoho_crm
                  connected: true
                  enabled: true
                  status: connected
                  defaultDealStage: Qualification
                  enabledEvents:
                    - buyer.created
                    - buyer.first_paid
                    - partner.created
                    - partner.active
                  failures: []
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      operationId: disconnectZohoCrmIntegration
      tags:
        - Integrations
      summary: Disconnect Zoho lifecycle CRM sync
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Zoho lifecycle CRM sync disconnected
          content:
            application/json:
              example:
                data:
                  disconnected: true
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/integrations/crm/zoho/connect:
    get:
      operationId: connectZohoCrmIntegration
      tags:
        - Integrations
      summary: Start Zoho lifecycle CRM sync OAuth
      security:
        - sessionCookie: []
      parameters:
        - name: defaultDealStage
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Zoho authorization URL
          content:
            application/json:
              example:
                data:
                  authorizationUrl: https://accounts.zoho.com/oauth/v2/auth?scope=ZohoCRM.modules.ALL
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/ValidationError"
  /api/admin/integrations/crm/zoho/callback:
    get:
      operationId: zohoCrmOauthCallback
      tags:
        - Integrations
      summary: Zoho lifecycle CRM sync OAuth callback
      parameters:
        - name: code
          in: query
          schema:
            type: string
        - name: state
          in: query
          schema:
            type: string
        - name: error
          in: query
          schema:
            type: string
      responses:
        "302":
          description: Redirects back to the admin integrations page
  /api/admin/integrations/crm/zoho/test:
    post:
      operationId: testZohoCrmIntegration
      tags:
        - Integrations
      summary: Test Zoho lifecycle CRM sync connection and Deal stage metadata
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            example:
              defaultDealStage: Qualification
      responses:
        "200":
          description: Connection verified
          content:
            application/json:
              example:
                data:
                  ok: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/integrations/crm/zoho/retry:
    post:
      operationId: retryZohoCrmSyncEvent
      tags:
        - Integrations
      summary: Retry a failed Zoho CRM sync event
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            example:
              eventId: 11111111-2222-3333-4444-555555555555
      responses:
        "200":
          description: Event queued for retry
          content:
            application/json:
              example:
                data:
                  ok: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/cron/crm-sync:
    get:
      operationId: runCrmSyncCron
      tags:
        - Cron
      summary: Process pending CRM sync events
      security:
        - bearerAuth: []
      responses:
        "200":
          description: CRM sync batch result
          content:
            application/json:
              example:
                data:
                  processed: 2
                  succeeded: 2
                  failed: 0
                  blocked: 0
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/engagements:
    get:
      summary: List engagements
      description: Returns a paginated list of engagements with joined person, vertical, buyer, partner, and offer data.
      operationId: listEngagements
      tags:
        - "Admin: Engagements"
      security:
        - sessionCookie: []
      parameters:
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          description: Filter by CPA status
          schema:
            type: string
        - name: search
          in: query
          description: Search by person name, email, or phone
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - originatedAt
              - cpaStatus
              - latestEventAt
            default: latestEventAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of engagements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "400":
          description: Invalid filter params
        "401":
          description: Unauthorized
  /api/admin/engagements/{id}:
    get:
      summary: Get an engagement
      description: Returns a single engagement with full detail including events, cascade chain, and joined entity names.
      operationId: getEngagement
      tags:
        - "Admin: Engagements"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Engagement detail with events and cascade chain
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  cpaStatus: pending
                  originationType: lead
                  personFirstName: John
                  personLastName: Doe
                events:
                  - id: e1e2e3e4-e5e6-e7e8-e9e0-e1e2e3e4e5e6
                    normalizedStatus: qualified
                    rawStatus: QUAL
                    occurredAt: 2025-01-15 14:30:00+00:00
                cascadeChain: []
        "401":
          description: Unauthorized
        "404":
          description: Engagement not found
  /api/admin/consent/accounts:
    get:
      summary: List consent accounts
      description: Lists consent accounts from the consent service, optionally filtered by partner. Auto-creates if none exist.
      operationId: listConsentAccounts
      tags:
        - "Admin: Consent"
      security:
        - sessionCookie: []
      parameters:
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Consent accounts
        "401":
          description: Unauthorized
        "403":
          description: Consent feature not enabled
    post:
      summary: Create a consent account
      description: Creates a new consent account in the consent service for the current tenant.
      operationId: createConsentAccount
      tags:
        - "Admin: Consent"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            example:
              name: Acme Legal Consent
              domain: consent.acme.com
              partnerId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "201":
          description: Consent account created
        "401":
          description: Unauthorized
        "403":
          description: Consent feature not enabled
  /api/admin/consent/certificates:
    get:
      summary: List consent certificates
      description: Lists consent certificates from the consent service, with optional filters for status and date range.
      operationId: listConsentCertificates
      tags:
        - "Admin: Consent"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: List of consent certificates
        "401":
          description: Unauthorized
        "403":
          description: Consent feature not enabled
  /api/admin/consent/certificates/{id}:
    get:
      summary: Get a consent certificate
      description: Returns a single consent certificate from the consent service by its ID.
      operationId: getConsentCertificate
      tags:
        - "Admin: Consent"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Consent certificate detail
        "401":
          description: Unauthorized
        "403":
          description: Consent feature not enabled
        "404":
          description: Certificate not found
  /api/admin/consent/certificates/by-lead/{leadId}:
    get:
      summary: Get consent certificate by lead ID
      description: Looks up the consent certificate associated with a lead via the lead's consentCertUrl field.
      operationId: getConsentCertificateByLead
      tags:
        - "Admin: Consent"
      security:
        - sessionCookie: []
      parameters:
        - name: leadId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Consent certificate for the lead
        "401":
          description: Unauthorized
        "403":
          description: Consent feature not enabled
        "404":
          description: Lead or consent certificate not found
        "422":
          description: Invalid consent certificate URL
  /api/admin/posting-log:
    get:
      summary: List posting log entries
      description: Returns a cursor-paginated list of posting log entries with filters for endpoint, outcome, campaign,
        partner, date range, and more.
      operationId: listPostingLog
      tags:
        - "Admin: Posting Log"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: cursor
          in: query
          description: Cursor for pagination (from previous response)
          schema:
            type: string
        - name: endpoint
          in: query
          schema:
            type: string
        - name: outcome
          in: query
          schema:
            type: string
        - name: httpStatus
          in: query
          schema:
            type: integer
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: type
          in: query
          description: Filter by log entry type (e.g. lead, call)
          schema:
            type: string
        - name: leadId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: search
          in: query
          description: Search error messages
          schema:
            type: string
      responses:
        "200":
          description: Cursor-paginated posting log entries
          content:
            application/json:
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    displayId: PL-001234
                    endpoint: /api/v1/leads
                    method: POST
                    httpStatus: 200
                    durationMs: 142
                    outcome: accepted
                    createdAt: 2025-01-15 10:30:00+00:00
                pagination:
                  hasMore: true
                  cursor: eyJjcmVhdGVkQXQiOiIyMDI1LTAxLTE1VDEwOjMwOjAwLjAwMFoiLCJpZCI6ImExYjJjM2Q0In0=
        "400":
          description: Invalid cursor
        "401":
          description: Unauthorized
  /api/admin/posting-log/{id}:
    get:
      summary: Get a posting log entry
      description: Returns a single posting log entry including request and response bodies.
      operationId: getPostingLogEntry
      tags:
        - "Admin: Posting Log"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Full posting log entry
        "401":
          description: Unauthorized
        "404":
          description: Posting log entry not found
    patch:
      summary: Edit a posting log entry request body
      description: Updates the stored requestBody of a lead posting log entry (e.g. to correct a failed submission before
        replay). Call webhook entries and already-successful submissions cannot be edited.
      operationId: updatePostingLogEntry
      tags:
        - "Admin: Posting Log"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - requestBody
              properties:
                requestBody:
                  type: object
                  additionalProperties: true
            example:
              requestBody:
                firstName: Jane
                email: jane@example.com
      responses:
        "200":
          description: Updated posting log entry
        "400":
          description: Cannot modify call entries or a successful submission
        "401":
          description: Unauthorized
        "404":
          description: Posting log entry not found
  /api/admin/delivery-log/{id}/redeliver:
    post:
      summary: Manually redeliver a lead to a buyer over HTTP
      description: |-
        Admin manually redelivers a lead to a buyer using the source deliveryLog row as the base.
        All body fields are optional — when absent, the original deliveryLog row's value is used.
        Writes a new deliveryLog row with `attemptNumber + 1`. Does NOT create a new leadSale and does
        NOT trigger billing. The action is recorded in the audit log with `action='redeliver'`.
        HTTP-only for v1 — `sms`, `email`, and `google_sheets` channels return 400. `messaging_sync`
        rows are permanently excluded: they record an internal service-to-service POST whose
        Authorization header is not stored.
      operationId: redeliverDeliveryLogEntry
      tags:
        - "Admin: Delivery Log"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: The deliveryLog row to redeliver from.
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                requestBody:
                  type: object
                  description: JSON body to send to the buyer. Defaults to the original delivery's request body.
                  additionalProperties: true
                endpoint:
                  type: string
                  format: uri
                  description: Destination URL. Must be http:// or https://. Defaults to the original delivery's endpoint.
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - PUT
                    - PATCH
                  description: HTTP verb. Defaults to the original delivery's method.
                headers:
                  type: object
                  description: Request headers as a key/value map. Defaults to the original delivery's headers.
                  additionalProperties:
                    type: string
            example:
              endpoint: https://buyer.example.com/leads
              method: POST
              headers:
                Authorization: Bearer new-token
              requestBody:
                firstName: Jane
                lastName: Doe
                email: jane@example.com
      responses:
        "200":
          description: Redelivery attempted (success or failure both return 200 -- inspect the response).
          content:
            application/json:
              example:
                data:
                  newDeliveryLogId: 7c1f9e2a-1b8d-4f3a-9c0a-2f8c1c5a6d10
                  success: true
                  httpStatus: 200
                  error: null
        "400":
          description: Validation error -- invalid input, SSRF-rejected endpoint, or non-redeliverable channel
            (sms/email/google_sheets/messaging_sync).
        "401":
          description: Unauthorized
        "403":
          description: Admin role required
        "404":
          description: Delivery log entry not found in this tenant
  /api/admin/contracts/{id}/dispositions:
    get:
      summary: Get contract disposition overrides
      description: Returns contract-level disposition overrides and the effective merged set (contract overrides merged with
        intake center mappings).
      operationId: getContractDispositions
      tags:
        - "Admin: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Contract ID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Overrides and effective dispositions
          content:
            application/json:
              example:
                data:
                  overrides:
                    - externalCode: QUAL
                      externalLabel: Qualified
                      verticalGroupDispositionId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                      source: contract
                  effective:
                    - externalCode: QUAL
                      source: contract
                    - externalCode: DQ
                      source: intakeCenter
        "401":
          description: Unauthorized
        "404":
          description: Contract not found
    post:
      summary: Create a contract disposition override
      description: Adds a new disposition override at the contract level. Must be unique by externalCode within the contract.
      operationId: createContractDisposition
      tags:
        - "Admin: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              externalCode: CUSTOM_QUAL
              externalLabel: Custom Qualified
              verticalGroupDispositionId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              overrideTriggersCpa: true
              overrideTriggersReturn: false
              isActive: true
              sortOrder: 0
      responses:
        "201":
          description: Override created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "409":
          description: Duplicate externalCode for this contract
    put:
      summary: Update a contract disposition override
      description: Updates a contract disposition override. The override `id` is passed in the request body.
      operationId: updateContractDisposition
      tags:
        - "Admin: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            example:
              id: d4e5f6a7-b8c9-0123-defg-234567890123
              overrideTriggersCpa: false
              isActive: false
      responses:
        "200":
          description: Updated override
        "400":
          description: Override id is required
        "401":
          description: Unauthorized
        "404":
          description: Override not found
    delete:
      summary: Delete a contract disposition override
      description: Deletes a contract disposition override. ID via query param `overrideId` or body `id`. Requires owner role.
      operationId: deleteContractDisposition
      tags:
        - "Admin: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: overrideId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Override deleted
          content:
            application/json:
              example:
                data:
                  success: true
        "400":
          description: Override id required
        "401":
          description: Unauthorized
        "403":
          description: Owner role required
        "404":
          description: Override not found
  /api/admin/contracts/{id}/dispositions/populate:
    post:
      summary: Populate contract dispositions from intake center
      description: Copies disposition mappings from the contract's intake center (via the vertical group) into contract-level
        disposition overrides. Skips existing externalCodes.
      operationId: populateContractDispositions
      tags:
        - "Admin: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Contract ID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Populate result
          content:
            application/json:
              example:
                data:
                  populated: 12
        "400":
          description: No intake center or vertical group associated
        "401":
          description: Unauthorized
        "404":
          description: Contract not found
  /api/admin/events/bulk:
    post:
      summary: Bulk import engagement events
      description: Imports up to 5000 engagement events in a single request. Resolves engagements by lead sale ID (type=lead)
        or caller phone (type=call). Processes in batches with idempotency checks. Normalizes statuses via intake center
        mappings and updates engagement terminal/CPA status.
      operationId: bulkImportEvents
      tags:
        - "Admin: Events"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - events
              properties:
                events:
                  type: array
                  maxItems: 5000
                  items:
                    type: object
                    required:
                      - type
                      - identifier
                      - contractId
                      - status
                    properties:
                      type:
                        type: string
                        enum:
                          - lead
                          - call
                      identifier:
                        type: string
                        description: leadSaleId for type=lead, callerPhone for type=call
                      contractId:
                        type: string
                        format: uuid
                      status:
                        type: string
                      disposition:
                        type: string
                      payout:
                        type: string
                      occurredAt:
                        type: string
                        format: date-time
            example:
              events:
                - type: lead
                  identifier: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  contractId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  status: qualified
                  payout: "25.00"
                  occurredAt: 2025-01-15 14:00:00+00:00
                - type: call
                  identifier: "+15551234567"
                  contractId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  status: scheduled
      responses:
        "200":
          description: Bulk import result
          content:
            application/json:
              example:
                data:
                  processed: 100
                  created: 95
                  skipped: 3
                  errors:
                    - row: 42
                      identifier: bad-id
                      reason: LeadSale not found or has no engagement
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/admin/delivery-test:
    post:
      summary: Test lead delivery configuration
      description: Sends a test lead delivery using the provided action configuration (HTTP POST or GET) with authentication
        and field mappings.
      operationId: testDelivery
      tags:
        - "Admin: Delivery Test"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            example:
              action:
                type: http_post
                url: https://api.example.com/leads
                format: json
                authType: bearer
                authCredentials:
                  token: test-token-12345
                headers:
                  - name: X-Custom-Header
                    value: test
                fieldMapping:
                  - from: firstName
                    to: first_name
                timeout: 30
      responses:
        "200":
          description: Test delivery result
          content:
            application/json:
              example:
                data:
                  success: true
                  httpStatus: 200
                  responseBody: '{"status":"ok"}'
                  durationMs: 245
        "400":
          description: Invalid action configuration
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
  /api/admin/campaigns/{id}/setup-token:
    get:
      summary: List setup tokens for a campaign
      description: Returns all setup tokens for a campaign, with token values truncated for security.
      operationId: listCampaignSetupTokens
      tags:
        - "Admin: Enrollments"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of setup tokens
          content:
            application/json:
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    tokenPrefix: stk_abc123...
                    campaignId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    status: active
                    expiresAt: 2025-01-20 10:00:00+00:00
        "401":
          description: Unauthorized
    post:
      summary: Generate a setup token for a campaign
      description: Creates a new time-limited setup token that a partner can use to activate API access for a campaign.
      operationId: createCampaignSetupToken
      tags:
        - "Admin: Enrollments"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
            format: uuid
      responses:
        "201":
          description: Setup token created
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  token: stk_abcdef1234567890
                  setupUrl: https://theleadrouter.com/api/v1/setup/stk_abcdef1234567890
                  campaignId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  status: active
                  expiresAt: 2025-01-20 10:00:00+00:00
        "401":
          description: Unauthorized
        "404":
          description: Campaign not found
    delete:
      summary: Revoke a setup token
      description: Revokes a setup token and its linked API key (if any).
      operationId: revokeCampaignSetupToken
      tags:
        - "Admin: Enrollments"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tokenId
              properties:
                tokenId:
                  type: string
                  format: uuid
            example:
              tokenId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "200":
          description: Token revoked
          content:
            application/json:
              example:
                data:
                  success: true
        "400":
          description: tokenId is required
        "401":
          description: Unauthorized
        "404":
          description: Setup token not found
  /api/admin/impersonate:
    post:
      summary: Start impersonating a buyer or partner
      description: Sets the admin session to impersonate a buyer or partner user, switching roles and permissions accordingly.
      operationId: startImpersonation
      tags:
        - "Admin: Enrollments"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            example:
              buyerId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "200":
          description: Impersonation started
          content:
            application/json:
              example:
                data:
                  buyerId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Acme Insurance Corp
        "400":
          description: buyerId or partnerId required
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer or partner not found
    get:
      summary: Start impersonating a buyer or partner (redirect)
      description: Redirect-based impersonation — sets the impersonation cookie via a full page navigation and redirects to
        the buyer or partner dashboard. Pass buyerId or partnerId as a query parameter.
      operationId: startImpersonationRedirect
      tags:
        - "Admin: Enrollments"
      security:
        - sessionCookie: []
      parameters:
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "302":
          description: Impersonation cookie set — redirects to /buyer or /partner (redirects to /admin on failure)
  /api/admin/impersonate/exit:
    post:
      summary: Exit impersonation
      description: Restores the original admin session, ending the impersonation.
      operationId: exitImpersonation
      tags:
        - "Admin: Enrollments"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Admin session restored
          content:
            application/json:
              example:
                data:
                  restored: true
        "400":
          description: Not currently impersonating
        "401":
          description: Unauthorized
        "404":
          description: Admin user not found
  /api/admin/people/backfill:
    get:
      summary: Backfill person records for leads
      description: Finds leads without a personId and resolves/creates person records for them.
      operationId: backfillPersonRecords
      tags:
        - "Admin: Organizations"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Backfill result
          content:
            application/json:
              example:
                data:
                  total: 20
                  resolved: 18
                  failed: 2
        "401":
          description: Unauthorized
  /api/admin/offers/{offerId}/filter-fields:
    get:
      summary: Get visible filter fields for an offer
      description: Returns the list of system and vertical fields that are visible for a given offer, respecting the offer's
        field settings.
      operationId: getOfferFilterFields
      tags:
        - "Admin: Engagements"
      security:
        - sessionCookie: []
      parameters:
        - name: offerId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of visible fields
          content:
            application/json:
              example:
                data:
                  - name: firstName
                    label: firstName
                    type: text
                    source: system
                  - name: policyType
                    label: Policy Type
                    type: select
                    source: vertical
        "401":
          description: Unauthorized
        "404":
          description: Offer not found
  /api/admin/owner/transfer:
    post:
      summary: Transfer ownership
      description: Transfers the owner role from the current owner to another active admin user. Requires owner role.
      operationId: transferOwnership
      tags:
        - "Admin: Organizations"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - targetUserId
              properties:
                targetUserId:
                  type: string
                  format: uuid
            example:
              targetUserId: b2c3d4e5-f6a7-8901-bcde-f12345678901
      responses:
        "200":
          description: Ownership transferred
          content:
            application/json:
              example:
                data:
                  success: true
                  newOwnerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
        "400":
          description: Cannot transfer to self, non-admin, or inactive user
        "401":
          description: Unauthorized
        "403":
          description: Owner role required
        "404":
          description: Target user not found
  /api/admin/payouts/{id}:
    get:
      summary: Get a payout
      description: Returns a single payout record by ID.
      operationId: getAdminPayout
      tags:
        - "Admin: Engagements"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Payout detail
        "401":
          description: Unauthorized
        "403":
          description: Forbidden — feature not enabled
        "404":
          description: Payout not found
    patch:
      summary: Update payout status
      description: Updates a payout status (e.g. to 'paid'). When marking as paid, deducts from partner balance and creates a
        transaction record.
      operationId: updatePayoutStatus
      tags:
        - "Admin: Engagements"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
            example:
              status: paid
      responses:
        "200":
          description: Updated payout
        "401":
          description: Unauthorized
        "403":
          description: Forbidden — feature not enabled
        "404":
          description: Payout not found
  /api/admin/tenant-features:
    get:
      summary: Get tenant feature flags
      description: Returns the feature flags for the current tenant.
      operationId: getTenantFeatures
      tags:
        - "Admin: Organizations"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Feature flags map
          content:
            application/json:
              example:
                data:
                  consent: true
                  messaging: false
                  calls: false
        "401":
          description: Unauthorized
    put:
      summary: Update tenant feature flags
      description: Updates the feature flags for the current tenant. Accepts a partial map of boolean flags; returns the
        merged flag map. Disabling a flag that still has active dependents is soft-blocked with a 409.
      operationId: updateTenantFeatures
      tags:
        - "Admin: Organizations"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - features
              properties:
                features:
                  type: object
                  additionalProperties:
                    type: boolean
            example:
              features:
                messaging: true
                calls: false
      responses:
        "200":
          description: Updated feature flags map
          content:
            application/json:
              example:
                data:
                  consent: true
                  messaging: true
                  calls: false
        "400":
          description: Tenant context required or features must be a boolean map
        "401":
          description: Unauthorized
        "409":
          description: Feature cannot be disabled while dependents are active
  /api/buyer/me:
    get:
      summary: Get buyer identity and available endpoints
      description: Returns the authenticated buyer's identity, scopes, and all available API endpoints with descriptions.
      operationId: getBuyerIdentity
      tags:
        - "Portal: Discovery"
      security:
        - bearerAuth: []
        - sessionCookie: []
      responses:
        "200":
          description: Buyer identity with endpoint list
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
  /api/buyer/api-keys:
    get:
      summary: List buyer's API keys
      description: Returns all API keys for the authenticated buyer. Key values are masked.
      operationId: listBuyerApiKeys
      tags:
        - "Portal: API Keys"
      security:
        - bearerAuth: []
        - sessionCookie: []
      responses:
        "200":
          description: List of API keys
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
    post:
      summary: Create a buyer API key
      description: Creates a new API key for the authenticated buyer. The full key value is only returned once.
      operationId: createBuyerApiKey
      tags:
        - "Portal: API Keys"
      security:
        - bearerAuth: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
            example:
              name: My Integration Key
      responses:
        "200":
          description: API key created (full key returned once)
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
  /api/buyer/api-keys/{keyId}:
    delete:
      summary: Revoke a buyer API key
      description: Permanently revokes an API key belonging to the authenticated buyer.
      operationId: revokeBuyerApiKey
      tags:
        - "Portal: API Keys"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: keyId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Key revoked
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required or not your key
        "404":
          description: Key not found
  /api/buyer/contracts/{id}:
    put:
      summary: Update a buyer's own contract (self-service)
      description: |
        Buyer-scoped contract edit. Updates delivery settings (schedule hours + timezone),
        geo state filters, daily lead cap, call transfer numbers, the SMS delivery message,
        and can activate a paused contract. Call transfer numbers (`callPhoneNumber`,
        `callBackupNumber`) are only accepted on call-enabled contracts and are normalized to
        E.164. `scheduleHours` uses the strict engine shape (lowercase weekday keys →
        `{ start, end }` in HH:MM). `smsMessage` edits only the message body of an existing SMS
        delivery action (targeted by its stable `actionId`); an unknown/missing action returns
        `400 "SMS delivery action not found on this contract"`. Scoped to the authenticated
        buyer + tenant.

        The response is allowlisted — it returns only buyer-safe contract fields plus the
        secret-stripped `deliveryDestinations` projection. It never echoes the raw
        `deliveryConfig`, `intakeApiKey`, or `inboundWebhookKey`.
      operationId: updateBuyerContract
      tags:
        - "Portal: Contracts"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                dailyLeadCap:
                  type: integer
                  minimum: 1
                  maximum: 10000
                geoFilters:
                  type: object
                  properties:
                    states:
                      type: array
                      items:
                        type: string
                        minLength: 2
                        maxLength: 2
                    exclude:
                      type: boolean
                demographicFilters:
                  type: object
                  additionalProperties: true
                  description: Free-form demographic filter map (field → filter config).
                customFilters:
                  type: array
                  description: Custom filter rules. Legacy object form is coerced to an array.
                  items:
                    type: object
                    additionalProperties: true
                deliveryMethodOverride:
                  type: string
                  description: Overrides the contract delivery method.
                deliveryUrlOverride:
                  type: string
                  format: uri
                  description: Overrides the contract delivery URL.
                scheduleHours:
                  type: object
                  description: "Engine shape: lowercase weekday keys → { start, end } in HH:MM. Empty object = always-on."
                  additionalProperties:
                    type: object
                    required:
                      - start
                      - end
                    properties:
                      start:
                        type: string
                        example: 09:00
                      end:
                        type: string
                        example: 17:00
                scheduleTimezone:
                  type: string
                  description: IANA timezone identifier.
                  example: America/Chicago
                callPhoneNumber:
                  type: string
                  nullable: true
                  description: Call transfer number (call-enabled contracts only). Normalized to E.164.
                  example: "+14155551234"
                callBackupNumber:
                  type: string
                  nullable: true
                  description: Backup call transfer number (call-enabled contracts only). Normalized to E.164.
                  example: "+14155559876"
                smsMessage:
                  type: object
                  description: Edits the message body of an existing SMS delivery action, targeted by its stable action id. Only the body
                    changes; the destination number, sending number, and template are untouched.
                  required:
                    - actionId
                    - messageBody
                  properties:
                    actionId:
                      type: string
                      minLength: 1
                      description: Stable id of the SMS delivery action to edit.
                    messageBody:
                      type: string
                      minLength: 1
                      maxLength: 1600
                      description: New SMS body. Supports {{firstName}}, {{lastName}}, {{phone}}, {{email}}, {{state}}, {{city}}, {{zip}}
                        tokens interpolated at send time.
                status:
                  type: string
                  enum:
                    - active
            example:
              scheduleHours:
                monday:
                  start: 09:00
                  end: 17:00
              scheduleTimezone: America/Chicago
              geoFilters:
                states:
                  - CA
                  - NY
      responses:
        "200":
          description: |
            Allowlisted contract fields plus the secret-stripped delivery destinations.
            Never includes the raw deliveryConfig, intakeApiKey, or inboundWebhookKey.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                      callPhoneNumber:
                        type: string
                        nullable: true
                      callBackupNumber:
                        type: string
                        nullable: true
                      scheduleHours:
                        type: object
                        additionalProperties: true
                      scheduleTimezone:
                        type: string
                        nullable: true
                      geoFilters:
                        type: object
                        additionalProperties: true
                      demographicFilters:
                        type: object
                        nullable: true
                        additionalProperties: true
                      customFilters:
                        type: array
                        items:
                          type: object
                          additionalProperties: true
                      dailyLeadCap:
                        type: integer
                        nullable: true
                      deliveryMethodOverride:
                        type: string
                        nullable: true
                      deliveryUrlOverride:
                        type: string
                        nullable: true
                      deliveryDestinations:
                        type: array
                        description: Secret-stripped, buyer-safe view of each delivery action. A discriminated union keyed by `kind`.
                        items:
                          type: object
                          required:
                            - kind
                            - enabled
                            - isPrimary
                          properties:
                            kind:
                              type: string
                              enum:
                                - http
                                - email
                                - google_sheets
                                - sms
                                - ftp
                            enabled:
                              type: boolean
                            isPrimary:
                              type: boolean
                            method:
                              type: string
                              enum:
                                - GET
                                - POST
                              description: http only.
                            url:
                              type: string
                              nullable: true
                              description: http only — scheme+host+path, query string stripped. Null when unparseable.
                            emailTo:
                              type: string
                              nullable: true
                              description: email only.
                            emailCc:
                              type: string
                              nullable: true
                              description: email only.
                            label:
                              type: string
                              description: google_sheets only — display name of the sheet.
                            sheetUrl:
                              type: string
                              nullable: true
                              description: google_sheets only — link to the sheet, or null when no spreadsheet id.
                            actionId:
                              type: string
                              description: sms only — stable id used to target message edits.
                            phoneSource:
                              type: string
                              enum:
                                - fixed
                                - lead
                              description: sms only — fixed number vs the lead's own phone.
                            phoneNumber:
                              type: string
                              nullable: true
                              description: sms only — destination number when phoneSource is fixed.
                            messageBody:
                              type: string
                              description: sms only — current message body.
                            hasTemplateFallback:
                              type: boolean
                              description: sms only — whether a template fallback is configured.
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  status: active
                  callPhoneNumber: "+14155551234"
                  scheduleTimezone: America/Chicago
                  deliveryDestinations:
                    - kind: sms
                      enabled: true
                      isPrimary: false
                      actionId: sms-1
                      phoneSource: fixed
                      phoneNumber: "+14155550100"
                      messageBody: Hi {{firstName}}, thanks for your interest!
                      hasTemplateFallback: false
        "400":
          description: Validation error (invalid phone, timezone, schedule shape, call fields on a non-call contract, or SMS
            action not found)
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
        "404":
          description: Contract not found
  /api/buyer/people:
    get:
      summary: List buyer's people
      description: Returns a paginated list of people (contacts) linked to leads from the authenticated buyer. Requires buyer role.
      operationId: listBuyerPeople
      tags:
        - "Portal: People"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: search
          in: query
          description: Search by first name, last name, email, or phone
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of people
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
  /api/buyer/people/{personId}:
    get:
      summary: Get a person detail
      description: Returns person detail with emails and phones. Only accessible if the person is linked to a lead from the
        authenticated buyer.
      operationId: getBuyerPerson
      tags:
        - "Portal: People"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: personId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Person with emails and phones
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  firstName: John
                  lastName: Doe
                  email: john@example.com
                  phone: "+15551234567"
                  emails:
                    - id: e1
                      email: john@example.com
                      isPrimary: true
                  phones:
                    - id: p1
                      phone: "+15551234567"
                      isPrimary: true
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
        "404":
          description: Person not found or not linked to buyer
  /api/buyer/engagements:
    get:
      summary: List buyer engagements
      description: Returns a paginated list of engagements scoped to the authenticated buyer.
      operationId: listBuyerEngagements
      tags:
        - "Portal: Engagements"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by CPA status
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - originatedAt
              - cpaStatus
              - latestEventAt
            default: latestEventAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of engagements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required
  /api/buyer/engagements/{id}:
    get:
      summary: Get a buyer engagement
      description: Returns a single engagement with events, scoped to the authenticated buyer.
      operationId: getBuyerEngagement
      tags:
        - "Portal: Engagements"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Engagement with events
        "401":
          description: Unauthorized
        "403":
          description: Buyer access required or not your engagement
        "404":
          description: Engagement not found
  /api/partner/people:
    get:
      summary: List partner's people
      description: Returns a paginated list of people (contacts) linked to leads from the authenticated partner.
      operationId: listPartnerPeople
      tags:
        - "Partner: People"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: search
          in: query
          description: Search by first name, last name, email, or phone
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of people
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
  /api/partner/people/{personId}:
    get:
      summary: Get a person detail
      description: Returns person detail with emails and phones. Only accessible if the person is linked to a lead from the
        authenticated partner.
      operationId: getPartnerPerson
      tags:
        - "Partner: People"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: personId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Person with emails and phones
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  firstName: John
                  lastName: Doe
                  email: john@example.com
                  phone: "+15551234567"
                  emails:
                    - id: e1
                      email: john@example.com
                      isPrimary: true
                  phones:
                    - id: p1
                      phone: "+15551234567"
                      isPrimary: true
        "400":
          description: Invalid or missing person ID
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
        "404":
          description: Person not found or not linked to partner
  /api/partner/engagements:
    get:
      summary: List partner's engagements
      description: Returns a paginated list of engagements scoped to the authenticated partner.
      operationId: listPartnerEngagements
      tags:
        - "Partner: Engagements"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by CPA status
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - originatedAt
              - cpaStatus
              - latestEventAt
            default: latestEventAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of engagements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
  /api/partner/engagements/{id}:
    get:
      summary: Get partner engagement detail
      description: Returns a single engagement with events, scoped to the authenticated partner.
      operationId: getPartnerEngagement
      tags:
        - "Partner: Engagements"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Engagement with events
        "401":
          description: Unauthorized
        "403":
          description: Partner access required or not your engagement
        "404":
          description: Engagement not found
  /api/partner/posting-log:
    get:
      summary: List partner's posting log
      description: Returns a cursor-paginated posting log scoped to the authenticated partner. Excludes request/response
        bodies and IP/user agent.
      operationId: listPartnerPostingLog
      tags:
        - "Partner: Posting Log"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: cursor
          in: query
          schema:
            type: string
        - name: endpoint
          in: query
          schema:
            type: string
        - name: outcome
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: type
          in: query
          description: Filter by log entry type (e.g. lead, call)
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Cursor-paginated posting log entries
        "400":
          description: Invalid cursor
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
  /api/partner/posting-log/{id}:
    get:
      summary: Get a partner posting log entry
      description: Returns a single posting log entry with request/response bodies. Only accessible if the entry belongs to
        the authenticated partner.
      operationId: getPartnerPostingLogEntry
      tags:
        - "Partner: Posting Log"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Posting log entry ID — a UUID or the numeric displayId.
          schema:
            type: string
      responses:
        "200":
          description: Full posting log entry
        "400":
          description: Invalid posting log id
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
        "404":
          description: Entry not found
  /api/partner/campaigns:
    get:
      summary: List partner's campaigns
      description: Returns all campaigns belonging to the authenticated partner, with vertical name, offer info, and lead counts.
      operationId: listPartnerCampaigns
      tags:
        - "Partner: Campaigns"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 1000
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: List of partner campaigns
          content:
            application/json:
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Auto Insurance Q1
                    verticalName: Auto Insurance
                    pricingModel: cpl
                    costPerLead: "15.00"
                    status: active
                    offerName: Auto Insurance Standard
                    offerStatus: active
                    leadCount: 142
        "401":
          description: Unauthorized
  /api/partner/campaigns/sync:
    get:
      summary: Campaign change detection
      description: Returns a hash and timestamp for lightweight change detection. Poll this endpoint and only re-fetch full
        campaign data when the hash changes.
      operationId: getPartnerCampaignSync
      tags:
        - "Partner: Campaigns"
      security:
        - bearerAuth: []
        - sessionCookie: []
      responses:
        "200":
          description: Campaign sync metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  hash:
                    type: string
                    nullable: true
                    description: 12-char MD5 hash — changes when any campaign config changes
                  lastUpdated:
                    type: string
                    format: date-time
                    nullable: true
                    description: Most recent change timestamp
                  count:
                    type: integer
                    description: Number of campaigns
              example:
                hash: a1b2c3d4e5f6
                lastUpdated: 2026-03-10 14:30:00+00:00
                count: 5
        "403":
          description: Partner access required
  /api/partner/leads:
    get:
      summary: List partner's leads
      description: Returns a paginated list of leads belonging to the authenticated partner, with search and date filters.
      operationId: listPartnerLeads
      tags:
        - "Partner: Leads"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          description: Search by email, phone, name, UUID, or short ID
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date
        - name: dateTo
          in: query
          schema:
            type: string
            format: date
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of leads
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
  /api/partner/leads/{id}:
    get:
      summary: Get a partner lead
      description: Returns a lead with its dispositions, sales (no buyer names), and distributions (no buyer/contract names).
        Accepts a lead UUID or the lead's `shortId`. The numeric `displayId` is NOT accepted — a non-UUID value is
        matched against `shortId` only.
      operationId: getPartnerLead
      tags:
        - "Partner: Leads"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Lead UUID or the lead's `shortId` (8-character nanoid). Not `displayId`.
          schema:
            type: string
      responses:
        "200":
          description: Lead detail with dispositions, sales, and distributions
          content:
            application/json:
              example:
                data:
                  lead:
                    id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    shortId: AB12CD
                    firstName: John
                    lastName: Doe
                    status: sold
                  dispositions:
                    - id: d1
                      disposition: qualified
                      createdAt: 2025-01-15 14:00:00+00:00
                  sales:
                    - id: s1
                      price: "25.00"
                      deliveryStatus: delivered
                  distributions: []
        "401":
          description: Unauthorized
        "404":
          description: Lead not found
  /api/partner/offers:
    get:
      summary: List available offers
      description: Returns a paginated list of public, active/paused offers that the partner is not yet enrolled in. Requires
        offers permission.
      operationId: listPartnerOffers
      tags:
        - "Partner: Offers"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of available offers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Offers permission required
  /api/partner/offers/{id}:
    get:
      summary: Get an offer detail
      description: Returns a single public, active/paused offer. Requires offers permission.
      operationId: getPartnerOffer
      tags:
        - "Partner: Offers"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Offer detail
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Auto Insurance Standard
                  description: Standard auto insurance lead program
                  verticalName: Auto Insurance
                  defaultPayoutAmount: "25.00"
                  defaultPayoutModel: cpl
                  status: active
                  visibility: public
        "401":
          description: Unauthorized
        "403":
          description: Offers permission required
        "404":
          description: Offer not found
  /api/partner/payouts:
    get:
      summary: Get partner payout data
      description: Returns the partner's payout history, current balance, next payout date, and total paid amount.
      operationId: getPartnerPayouts
      tags:
        - "Partner: Payouts"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 1000
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Payout data
          content:
            application/json:
              example:
                data:
                  payouts:
                    - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                      amount: "500.00"
                      status: paid
                      processedAt: 2025-01-15 10:00:00+00:00
                  balance: "1250.00"
                  nextPayoutDate: 2025-02-01
                  totalPaid: "3500.00"
                pagination:
                  total: 12
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Partner access required or finance feature disabled
  /api/partner/settings:
    get:
      summary: Get partner settings
      description: Returns the authenticated partner's profile data.
      operationId: getPartnerSettings
      tags:
        - "Partner: Settings"
      security:
        - bearerAuth: []
        - sessionCookie: []
      responses:
        "200":
          description: Partner profile data
          content:
            application/json:
              example:
                data:
                  partner:
                    id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Acme Leads Inc
                    email: admin@acmeleads.com
                    status: active
        "401":
          description: Unauthorized
    put:
      summary: Update partner settings (deprecated)
      description: |
        Deprecated. Password management was removed — partner-portal users authenticate via
        magic link. This endpoint always returns 410 Gone.
      operationId: updatePartnerSettings
      deprecated: true
      tags:
        - "Partner: Settings"
      security:
        - bearerAuth: []
        - sessionCookie: []
      responses:
        "410":
          description: Gone — password management removed; users log in via magic link.
          content:
            application/json:
              example:
                error:
                  code: DEPRECATED
                  message: Password management removed. Users log in via magic link.
  /api/partner/notification-subscriptions:
    get:
      summary: List partner-portal notification subscriptions
      description: |
        Returns the partner-eligible trigger registry plus the logged-in partner-portal user's
        per-trigger subscriptions. The `subscriptions` map only includes trigger keys for which
        an explicit row exists — the UI applies registry `defaultChannels` for the rest.
      operationId: listPartnerNotificationSubscriptions
      tags:
        - "Partner: Settings"
      security:
        - sessionCookie: []
        - partnerApiKey: []
      responses:
        "200":
          description: Trigger registry and the user's subscription overrides
          content:
            application/json:
              schema:
                type: object
                required:
                  - triggers
                  - subscriptions
                properties:
                  triggers:
                    $ref: "#/components/schemas/NotificationTriggerRegistry"
                  subscriptions:
                    $ref: "#/components/schemas/NotificationSubscriptionMap"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      summary: Upsert a partner-portal notification subscription
      description: |
        Upserts the logged-in partner-portal user's preference for a single trigger.
        Defense-in-depth: the trigger must exist AND its `roles` array must include `partner`.
        Slack is deferred — `channels` is restricted to `email` and `in_app`.
      operationId: upsertPartnerNotificationSubscription
      tags:
        - "Partner: Settings"
      security:
        - sessionCookie: []
        - partnerApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NotificationSubscriptionUpsert"
      responses:
        "200":
          description: Subscription upserted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required:
                      - triggerKey
                      - channels
                      - enabled
                    properties:
                      triggerKey:
                        type: string
                      channels:
                        type: array
                        items:
                          type: string
                          enum:
                            - email
                            - in_app
                      enabled:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/partner/me:
    get:
      summary: Get partner identity
      description: Returns the authenticated partner's identity, available scopes, and endpoint directory.
      operationId: getPartnerMe
      tags:
        - "Partner: Settings"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Partner identity and endpoint directory
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      partnerId:
                        type: string
                        format: uuid
                      partnerName:
                        type: string
                        nullable: true
                      role:
                        type: string
                      authType:
                        type: string
                      scopes:
                        type: array
                        items:
                          type: string
                      endpoints:
                        type: array
                        items:
                          type: object
                          properties:
                            method:
                              type: string
                            path:
                              type: string
                            description:
                              type: string
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
  /api/partner/api-keys:
    get:
      summary: List partner API keys
      description: Returns the authenticated partner's API keys (routing scope only). Key hashes are omitted.
      operationId: listPartnerApiKeys
      tags:
        - "Partner: Settings"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: List of API keys
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        name:
                          type: string
                        keyPrefix:
                          type: string
                        scopes:
                          type: array
                          items:
                            type: string
                        status:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      offset:
                        type: integer
                      hasMore:
                        type: boolean
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
    post:
      summary: Create partner API key
      description: Creates a new API key scoped to routing for the authenticated partner. The full key is returned only once.
      operationId: createPartnerApiKey
      tags:
        - "Partner: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
            example:
              name: My Integration Key
      responses:
        "200":
          description: API key created (includes full key -- store it, it won't be shown again)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      name:
                        type: string
                      key:
                        type: string
                        description: Full API key (shown only once)
                      keyPrefix:
                        type: string
                      scopes:
                        type: array
                        items:
                          type: string
                      status:
                        type: string
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
  /api/partner/api-keys/{keyId}:
    parameters:
      - name: keyId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      summary: Revoke partner API key
      description: Revokes an API key owned by the authenticated partner.
      operationId: revokePartnerApiKey
      tags:
        - "Partner: Settings"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: API key revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
        "404":
          description: API key not found
  /api/partner/campaigns/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: Campaign UUID or numeric display ID
        schema:
          type: string
    get:
      summary: Get campaign detail
      description: Returns campaign detail with posting spec, payout info, lead count, and consent setup.
      operationId: getPartnerCampaign
      tags:
        - "Partner: Campaigns"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Campaign detail with posting spec
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      displayId:
                        type: integer
                      name:
                        type: string
                      specToken:
                        type: string
                        format: uuid
                      verticalId:
                        type: string
                        format: uuid
                      verticalName:
                        type: string
                        nullable: true
                      offerName:
                        type: string
                        nullable: true
                      offerId:
                        type: string
                        format: uuid
                        nullable: true
                      pricingModel:
                        type: string
                      costPerLead:
                        type: string
                        nullable: true
                      dailyCap:
                        type: integer
                        nullable: true
                      weeklyCap:
                        type: integer
                        nullable: true
                      monthlyCap:
                        type: integer
                        nullable: true
                      status:
                        type: string
                      createdAt:
                        type: string
                        format: date-time
                      leadCount:
                        type: integer
                      offerPayout:
                        type: object
                        properties:
                          model:
                            type: string
                            nullable: true
                          amount:
                            type: number
                            nullable: true
                      postingKeyPrefix:
                        type: string
                        nullable: true
                      postingKey:
                        type: string
                        nullable: true
                      postingSpec:
                        type: object
                      cpaEnabled:
                        type: boolean
                      cpaPricingModel:
                        type: string
                        nullable: true
                      cpaCost:
                        type: string
                        nullable: true
                      callEnabled:
                        type: boolean
                      callDniEnabled:
                        type: boolean
                      trackingPhone:
                        type: string
                        nullable: true
                      acceptance:
                        type: object
                        nullable: true
        "400":
          description: Invalid campaign ID
        "401":
          description: Unauthorized
        "403":
          description: Campaign not found or not owned by partner
  /api/partner/campaigns/{id}/dni-pool/release:
    parameters:
      - name: id
        in: path
        required: true
        description: Campaign UUID
        schema:
          type: string
          format: uuid
    post:
      summary: Release reserved DNI pool numbers
      description: |
        Force-releases the DNI pool numbers currently leased to active impressions
        for one of the partner's own campaigns, without waiting for TTL/cron. Omit
        `trackingNumberId` to release every active reservation, or provide it to
        release a single leased number. Response is stripped to the two release counts.
      operationId: releasePartnerCampaignDniPool
      tags:
        - "Partner: Campaigns"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                trackingNumberId:
                  type: string
                  format: uuid
                  description: Optional — scope the release to a single leased number.
      responses:
        "200":
          description: Release counts
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - releasedNumbers
                      - expiredImpressions
                    properties:
                      releasedNumbers:
                        type: integer
                      expiredImpressions:
                        type: integer
        "400":
          description: Invalid request body
        "401":
          description: Unauthorized
        "404":
          description: Campaign not found or not owned by partner
  /api/partner/campaigns/{id}/spec-token:
    parameters:
      - name: id
        in: path
        required: true
        description: Campaign UUID or numeric display ID
        schema:
          type: string
    post:
      summary: Regenerate spec token
      description: Regenerates the spec token for a campaign's public posting spec URL.
      operationId: regenerateSpecToken
      tags:
        - "Partner: Campaigns"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: New spec token
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      specToken:
                        type: string
                        format: uuid
        "401":
          description: Unauthorized
        "403":
          description: Campaign not found or not owned by partner
  /api/partner/offers/{id}/stats:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get offer stats for partner
      description: Returns aggregate lead/call statistics for an offer, scoped to the partner's campaigns.
      operationId: getPartnerOfferStats
      tags:
        - "Partner: Offers"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Offer statistics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      totalLeads:
                        type: integer
                      soldLeads:
                        type: integer
                      rejectedLeads:
                        type: integer
                      returnedLeads:
                        type: integer
                      pendingLeads:
                        type: integer
                      totalCost:
                        type: string
                      totalCpaPayout:
                        type: string
                      conversionRate:
                        type: number
                      returnRate:
                        type: number
                      avgCostPerLead:
                        type: string
                      leadsToday:
                        type: integer
                      leadsThisWeek:
                        type: integer
                      leadsThisMonth:
                        type: integer
        "400":
          description: Invalid offer ID
        "401":
          description: Unauthorized
        "403":
          description: Partner or offers permission required
        "404":
          description: Offer not found
  /api/partner/calls:
    get:
      summary: List partner calls
      description: Returns a paginated list of calls for the partner's campaigns.
      operationId: listPartnerCalls
      tags:
        - "Partner: Campaigns"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: personId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: search
          in: query
          description: Search by caller or called number
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of calls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
  /api/partner/people/{personId}/offers:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List offers for a person
      description: Returns lead and call activity grouped by offer for a person linked to the partner's leads.
      operationId: listPartnerPersonOffers
      tags:
        - "Partner: Leads"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Offers with lead/call counts
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        offerId:
                          type: string
                          format: uuid
                        offerName:
                          type: string
                        verticalName:
                          type: string
                          nullable: true
                        leadCount:
                          type: integer
                        callCount:
                          type: integer
                        totalCost:
                          type: string
                        latestAt:
                          type: string
                          format: date-time
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
        "404":
          description: Person not found
  /api/partner/people/{personId}/leads:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List a person's leads for this partner
      description: |
        Leads this partner submitted for the given person, matched by personId or
        by email/phone hash so orphaned links still appear. Confined to the
        authenticated partner — there is no partnerId parameter.

        Replaces the portal's former use of `/api/v1/people/{personId}/leads`,
        which is admin-scoped: an impersonated session is not honoured on
        `/api/v1`, so that route answered from the underlying admin identity and
        skipped the partner confinement.
      operationId: listPartnerPersonLeads
      tags:
        - "Partner: Leads"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated leads (id, displayId, status, cost, createdAt — no PII)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
        "404":
          description: Person not found
  /api/partner/people/{personId}/timeline:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get person timeline
      description: Returns a merged chronological timeline of leads, calls, and engagement events (last 90 days).
      operationId: getPartnerPersonTimeline
      tags:
        - "Partner: Leads"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Timeline entries sorted newest first
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                          enum:
                            - lead
                            - call
                            - engagement_event
                        id:
                          type: string
                          format: uuid
                        date:
                          type: string
                          format: date-time
                        title:
                          type: string
                          nullable: true
                        detail:
                          type: string
                          nullable: true
                        offerName:
                          type: string
                          nullable: true
                        metadata:
                          type: object
        "401":
          description: Unauthorized
        "403":
          description: Partner access required
        "404":
          description: Person not found
  /api/buyer/posting-log:
    get:
      summary: Buyer posting log (redirect)
      description: Returns 403 -- posting logs are partner-only. Use /api/partner/posting-log.
      operationId: getBuyerPostingLog
      tags:
        - "Portal: Posting Log"
      security:
        - sessionCookie: []
      responses:
        "403":
          description: Posting logs are only available to partners
  /api/buyer/posting-log/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Buyer posting log entry (redirect)
      description: Returns 403 -- posting logs are partner-only. Use /api/partner/posting-log.
      operationId: getBuyerPostingLogEntry
      tags:
        - "Portal: Posting Log"
      security:
        - sessionCookie: []
      responses:
        "403":
          description: Posting logs are only available to partners
  /api/buyer/users:
    get:
      summary: List buyer portal users
      operationId: listBuyerUsers
      tags:
        - "Portal: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: List of buyer portal users
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    post:
      summary: Create buyer portal user (deprecated)
      description: |
        Deprecated. Use `POST /api/v1/invitations` to add users. This endpoint always returns
        410 Gone.
      operationId: createBuyerUser
      deprecated: true
      tags:
        - "Portal: Users"
      security:
        - sessionCookie: []
      responses:
        "410":
          description: Gone — use POST /api/v1/invitations to add users.
          content:
            application/json:
              example:
                error:
                  code: DEPRECATED
                  message: Use POST /api/v1/invitations to add users. See plan.
  /api/buyer/users/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      summary: Update buyer portal user
      operationId: updateBuyerUser
      tags:
        - "Portal: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                jobTitle:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                officePhone:
                  type: string
                  nullable: true
                permAccounts:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permAccounts (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permUsers:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permUsers (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permBilling:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permBilling (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permOffers:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permOffers (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permReports:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permReports (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
      responses:
        "200":
          description: Buyer user updated
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: User not found
    delete:
      summary: Delete buyer portal user
      operationId: deleteBuyerUser
      tags:
        - "Portal: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Buyer user deleted
        "400":
          description: Cannot delete your own account
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: User not found
  /api/partner/consent/accounts:
    get:
      summary: Get partner consent account
      description: Returns the consent account for the authenticated partner. Auto-creates if one does not exist. Requires
        consent feature to be enabled.
      operationId: getPartnerConsentAccount
      tags:
        - "Partner: Consent"
      security:
        - bearerAuth: []
        - sessionCookie: []
      responses:
        "200":
          description: Consent account
        "401":
          description: Unauthorized
        "403":
          description: Consent feature not enabled or no partner associated
  /api/partner/offers/{id}/enroll:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Enroll in an offer
      operationId: enrollInOffer
      tags:
        - "Partner: Offers"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                notes:
                  type: string
                  nullable: true
                  maxLength: 1000
      responses:
        "201":
          description: Enrollment created
        "400":
          description: Validation error (invalid body, or already enrolled / already has a campaign for this offer)
        "401":
          description: Unauthorized
        "403":
          description: Partner access required or offers permission missing
        "404":
          description: Offer not found
  /api/partner/reports/dispositions:
    get:
      summary: Get partner disposition report
      description: |
        In addition to the listed parameters, accepts report-filter params applied as `IN`
        filters: `campaignId`, `verticalId`, `offerId`, `status`, `state`, `city`, and
        `sub1`–`sub10` (each comma-separated multi-value). Drill-down parent filters are passed
        as `pf_<dimensionKey>` (e.g. `pf_offer`, `pf_disposition`) for each parent dimension
        below `level`.
      operationId: getPartnerDispositionReport
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          description: Comma-separated list of 1–3 dimension keys.
          schema:
            type: string
        - name: level
          in: query
          schema:
            type: integer
            default: 0
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: campaignId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: verticalId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: offerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: status
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: state
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: city
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
      responses:
        "200":
          description: Disposition report data
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Partner access required or tenant context missing
  /api/partner/reports/drilldown:
    get:
      summary: Get partner drilldown report
      description: |
        In addition to the listed parameters, accepts report-filter params applied as `IN`
        filters: `campaignId`, `verticalId`, `offerId`, `status`, `state`, `city`, and
        `sub1`–`sub10` (each comma-separated multi-value). Drill-down parent filters are passed
        as `pf_<dimensionKey>` for each parent dimension below `level`.
      operationId: getPartnerDrilldownReport
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          description: Comma-separated list of 1–3 dimension keys.
          schema:
            type: string
        - name: level
          in: query
          schema:
            type: integer
            default: 0
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: campaignId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: verticalId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: offerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: status
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: state
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: city
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
      responses:
        "200":
          description: Drilldown report data
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Partner access required or tenant context missing
  /api/partner/reports/drilldown/distinct:
    get:
      summary: Get partner drilldown distinct values
      operationId: getPartnerDrilldownDistinctValues
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: field
          in: query
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Distinct values for the field
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Partner access required or tenant context missing
  /api/partner/reports/saved:
    get:
      summary: List partner saved reports
      operationId: listPartnerSavedReports
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: reportType
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of saved reports
        "401":
          description: Unauthorized
    post:
      summary: Create partner saved report
      operationId: createPartnerSavedReport
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - reportType
                - config
              properties:
                name:
                  type: string
                  minLength: 1
                reportType:
                  type: string
                config:
                  type: object
                  additionalProperties: true
                  description: Free-form report configuration object.
                isShared:
                  type: boolean
                folderId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "201":
          description: Saved report created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/reports/saved/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      summary: Update partner saved report
      operationId: updatePartnerSavedReport
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                reportType:
                  type: string
                config:
                  type: object
                  additionalProperties: true
                  description: Free-form report configuration object.
                isShared:
                  type: boolean
                folderId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "200":
          description: Saved report updated
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Saved report not found
    delete:
      summary: Delete partner saved report
      operationId: deletePartnerSavedReport
      tags:
        - "Partner: Reports"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Saved report deleted
        "401":
          description: Unauthorized
        "404":
          description: Saved report not found
  /api/partner/users:
    get:
      summary: List partner portal users
      operationId: listPartnerUsers
      tags:
        - "Partner: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: List of partner portal users
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    post:
      summary: Create partner portal user (deprecated)
      description: |
        Deprecated. Use `POST /api/v1/invitations` to add users. This endpoint always returns
        410 Gone.
      operationId: createPartnerUser
      deprecated: true
      tags:
        - "Partner: Users"
      security:
        - sessionCookie: []
      responses:
        "410":
          description: Gone — use POST /api/v1/invitations to add users.
          content:
            application/json:
              example:
                error:
                  code: DEPRECATED
                  message: Use POST /api/v1/invitations to add users. See plan.
  /api/partner/users/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      summary: Update partner portal user
      operationId: updatePartnerUser
      tags:
        - "Partner: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                jobTitle:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                officePhone:
                  type: string
                  nullable: true
                permAccounts:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permAccounts (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permUsers:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permUsers (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permBilling:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permBilling (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permOffers:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permOffers (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
                permReports:
                  type: boolean
                  deprecated: true
                  description: DEPRECATED. Value is written to member.permReports (per-tenant) not user. Will be removed once legacy
                    user-level perms drop ships.
      responses:
        "200":
          description: Partner user updated
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: User not found
    delete:
      summary: Delete partner portal user
      operationId: deletePartnerUser
      tags:
        - "Partner: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Partner user deleted
        "400":
          description: Cannot delete your own account
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: User not found
  /api/partner/campaigns/{id}/webhooks:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List partner campaign webhooks
      operationId: listPartnerCampaignWebhooks
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: List of campaign-scoped webhooks
        "401":
          description: Unauthorized
        "404":
          description: Campaign not found
    post:
      summary: Create partner campaign webhook
      operationId: createPartnerCampaignWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - url
                - events
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  description: Lead/CPA events only; call events are not accepted on partner portal campaign webhooks.
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                  default: GET
                status:
                  type: string
                  enum:
                    - active
                    - paused
                  default: active
                filters:
                  type: array
                  description: Optional AND filters evaluated against webhook delivery tokens before firing.
                  items:
                    $ref: "#/components/schemas/WebhookFilter"
      responses:
        "201":
          description: Campaign webhook created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Campaign not found
  /api/partner/campaigns/{id}/webhooks/{webhookId}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get partner campaign webhook
      operationId: getPartnerCampaignWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Campaign webhook detail
        "401":
          description: Unauthorized
        "404":
          description: Campaign or webhook not found
    put:
      summary: Update partner campaign webhook
      operationId: updatePartnerCampaignWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  description: Lead/CPA events only; call events are not accepted on partner portal campaign webhooks.
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                status:
                  type: string
                  enum:
                    - active
                    - paused
                filters:
                  type: array
                  description: Optional AND filters evaluated against webhook delivery tokens before firing.
                  items:
                    $ref: "#/components/schemas/WebhookFilter"
      responses:
        "200":
          description: Campaign webhook updated
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Campaign or webhook not found
    delete:
      summary: Delete partner campaign webhook
      operationId: deletePartnerCampaignWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Campaign webhook deleted
        "401":
          description: Unauthorized
        "404":
          description: Campaign or webhook not found
  /api/partner/campaigns/{id}/webhooks/{webhookId}/deliveries:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List partner campaign webhook deliveries
      operationId: listPartnerCampaignWebhookDeliveries
      tags:
        - "Partner: Webhooks"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: List of campaign webhook deliveries
        "401":
          description: Unauthorized
        "404":
          description: Campaign or webhook not found
  /api/partner/campaigns/{id}/webhooks/{webhookId}/deliveries/{deliveryId}/refire:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: deliveryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Refire partner campaign webhook delivery
      operationId: refirePartnerCampaignWebhookDelivery
      tags:
        - "Partner: Webhooks"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Delivery refired
        "401":
          description: Unauthorized
        "404":
          description: Campaign, webhook, or delivery not found
  /api/partner/campaigns/{id}/webhooks/{webhookId}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Test partner campaign webhook
      operationId: testPartnerCampaignWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Test delivery sent
        "401":
          description: Unauthorized
        "404":
          description: Campaign or webhook not found
  /api/partner/webhooks:
    get:
      summary: List partner portal webhooks
      operationId: listPartnerPortalWebhooks
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: List of webhooks
        "401":
          description: Unauthorized
    post:
      summary: Create partner portal webhook
      operationId: createPartnerPortalWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - url
                - events
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                  default: GET
                status:
                  type: string
                  enum:
                    - active
                    - paused
                  default: active
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "201":
          description: Webhook created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/webhooks/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get partner portal webhook
      operationId: getPartnerPortalWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Webhook detail
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
    put:
      summary: Update partner portal webhook
      operationId: updatePartnerPortalWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                status:
                  type: string
                  enum:
                    - active
                    - paused
                filters:
                  type: array
                  description: Optional AND filters evaluated against webhook delivery tokens before firing.
                  items:
                    $ref: "#/components/schemas/WebhookFilter"
      responses:
        "200":
          description: Webhook updated
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
    delete:
      summary: Delete partner portal webhook
      operationId: deletePartnerPortalWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Webhook deleted
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
  /api/partner/webhooks/{id}/deliveries:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List partner portal webhook deliveries
      operationId: listPartnerPortalWebhookDeliveries
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: verticalId
          in: query
          schema:
            type: string
        - name: offerId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of webhook deliveries
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
  /api/partner/webhooks/{id}/deliveries/{deliveryId}/refire:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: deliveryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Refire partner portal webhook delivery
      operationId: refirePartnerPortalWebhookDelivery
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Delivery refired
        "400":
          description: Invalid ID, or cannot refire a test or skipped delivery
        "401":
          description: Unauthorized
        "404":
          description: Webhook or delivery not found
        "422":
          description: Lead/sale no longer exists, or integration not configured
  /api/partner/webhooks/{id}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Test partner portal webhook
      operationId: testPartnerPortalWebhook
      tags:
        - "Partner: Webhooks"
      security:
        - partnerApiKey: []
        - sessionCookie: []
      responses:
        "200":
          description: Test delivery sent
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
        "422":
          description: Integration (Google Ads / Facebook CAPI) not configured
  /api/admin/google-sheets/create:
    post:
      summary: Create a Google Spreadsheet
      description: Creates a new Google Spreadsheet and shares it with the specified email.
      operationId: createGoogleSheet
      tags:
        - Integrations
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - title
                - shareWithEmail
              properties:
                title:
                  type: string
                  minLength: 1
                  maxLength: 255
                shareWithEmail:
                  type: string
                  format: email
      responses:
        "200":
          description: Spreadsheet created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      spreadsheetId:
                        type: string
                      url:
                        type: string
                        format: uri
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/admin/google-sheets/test:
    post:
      summary: Test Google Sheets connection
      description: Tests connectivity to a spreadsheet and optionally appends a test row.
      operationId: testGoogleSheetsConnection
      tags:
        - Integrations
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - spreadsheetId
              properties:
                spreadsheetId:
                  type: string
                  minLength: 1
                sheetName:
                  type: string
      responses:
        "200":
          description: Connection test result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      connected:
                        type: boolean
                      title:
                        type: string
                      testRowAppended:
                        type: boolean
                      testRowNumber:
                        type: integer
                      error:
                        type: string
                        nullable: true
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/admin/delivery-spec-import:
    post:
      summary: Import delivery spec from URL
      description: Fetches and parses a delivery specification from a remote URL.
      operationId: importDeliverySpec
      tags:
        - "Admin: Delivery"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
              properties:
                url:
                  type: string
                  format: uri
      responses:
        "200":
          description: Parsed spec
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deliveryAction:
                        type: object
                      rawSpec:
                        type: object
        "400":
          description: Validation error (invalid URL)
        "403":
          description: Admin access required
        "422":
          description: Failed to fetch or parse spec, or AI key not configured
        "429":
          description: Rate limit exceeded (10 imports per minute per user)
  /api/admin/webhooks:
    get:
      summary: List webhooks
      operationId: adminListWebhooks
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated webhooks
        "401":
          description: Unauthorized
    post:
      summary: Create webhook
      operationId: adminCreateWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - events
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                  description: Required for GET/POST webhooks. Omitted for preset integrations (GOOGLE_ADS/FACEBOOK_CAPI/AXON), which
                    build their endpoint server-side.
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                    - AXON
                  default: GET
                status:
                  type: string
                  enum:
                    - active
                    - paused
                  default: active
                googleAdsConfig:
                  type: object
                  description: Required when method is GOOGLE_ADS.
                  required:
                    - customerId
                    - conversionActionId
                  properties:
                    customerId:
                      type: string
                    loginCustomerId:
                      type: string
                    conversionActionId:
                      type: string
                    gclidField:
                      type: string
                      default: Gclid
                    conversionValue:
                      type: string
                      default: Payout
                facebookCapiConfig:
                  type: object
                  description: Required when method is FACEBOOK_CAPI.
                  properties:
                    pixelId:
                      type: string
                    adAccountId:
                      type: string
                    eventName:
                      type: string
                      default: Lead
                    fbclidField:
                      type: string
                      default: Fbclid
                    fbcField:
                      type: string
                      default: Fbc
                    fbpField:
                      type: string
                      default: Fbp
                    conversionValue:
                      type: string
                      default: CPA_Payout
                    currencyCode:
                      type: string
                      description: Optional 3-letter ISO code. Omitted or blank resolves the tenant account currency at fire time.
                    includeUserData:
                      type: boolean
                      default: true
                axonConfig:
                  type: object
                  description: Required when method is AXON.
                  required:
                    - eventKey
                    - conversionApiKey
                    - campaigns
                  properties:
                    eventKey:
                      type: string
                    conversionApiKey:
                      type: string
                    currency:
                      type: string
                      description: Optional 3-letter ISO code. Omitted or blank resolves the tenant account currency at fire time.
                    aleidField:
                      type: string
                      default: sub6
                    axwrtField:
                      type: string
                      default: sub7
                    cuidField:
                      type: string
                      default: sub8
                    fallbackEventSourceUrl:
                      type: string
                      format: uri
                    campaigns:
                      type: array
                      minItems: 1
                      items:
                        type: object
                        required:
                          - campaignId
                        properties:
                          campaignId:
                            type: string
                            format: uuid
                          submit:
                            type: number
                            default: 0
                          qualified:
                            type: number
                            default: 0
                          retainer:
                            type: number
                            default: 0
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "201":
          description: Created
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/admin/webhooks/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get webhook detail
      operationId: adminGetWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Webhook detail
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
    put:
      summary: Update webhook
      operationId: adminUpdateWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Partial update. All fields optional.
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                status:
                  type: string
                  enum:
                    - active
                    - paused
                googleAdsConfig:
                  type: object
                  additionalProperties: true
                facebookCapiConfig:
                  type: object
                  additionalProperties: true
                axonConfig:
                  type: object
                  additionalProperties: true
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "200":
          description: Updated
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
    delete:
      summary: Delete webhook
      operationId: adminDeleteWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Deleted
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
  /api/admin/webhooks/{id}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Test fire webhook
      operationId: adminTestWebhook
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Test result
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
        "422":
          description: Integration (Google Ads / Facebook CAPI) not configured
  /api/admin/webhooks/{id}/deliveries:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List webhook deliveries
      operationId: adminListWebhookDeliveries
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Paginated deliveries
        "401":
          description: Unauthorized
        "404":
          description: Webhook not found
  /api/admin/tenant-settings:
    get:
      summary: Get tenant settings
      operationId: getTenantSettings
      tags:
        - Settings
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Tenant settings (API keys masked)
        "401":
          description: Unauthorized
    put:
      summary: Update tenant settings
      operationId: updateTenantSettings
      tags:
        - Settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              properties:
                aiModel:
                  type: string
                aiAllowedDomains:
                  type: array
                  nullable: true
                  items:
                    type: string
                googleSheets:
                  type: object
                  properties:
                    defaultShareEmails:
                      type: array
                      maxItems: 20
                      items:
                        type: string
                        format: email
                        maxLength: 255
      responses:
        "200":
          description: Updated
        "401":
          description: Unauthorized
  /api/admin/pending-signups:
    get:
      summary: List pending portal signups
      operationId: listPendingSignups
      tags:
        - "Admin: Signups"
      security:
        - sessionCookie: []
      parameters:
        - name: entityType
          in: query
          schema:
            type: string
            enum:
              - buyer
              - partner
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated pending signups
        "401":
          description: Unauthorized
  /api/admin/tracking-numbers/{id}:
    get:
      summary: Get a tracking number
      description: Returns a single tracking number by ID.
      operationId: getAdminTrackingNumber
      tags:
        - Tracking Numbers
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Tracking number detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      phoneNumber:
                        type: string
                      provider:
                        type: string
                      status:
                        type: string
                      campaignId:
                        type: string
                        format: uuid
                        nullable: true
                      offerId:
                        type: string
                        format: uuid
                        nullable: true
                      renewalDate:
                        type: string
                        format: date-time
                        nullable: true
                      poolManaged:
                        type: boolean
        "401":
          description: Unauthorized
        "404":
          description: Tracking number not found
  /api/admin/contracts/{id}/misc-costs:
    parameters:
      - name: id
        in: path
        required: true
        description: Contract ID
        schema:
          type: string
          format: uuid
    get:
      operationId: listContractMiscCosts
      tags:
        - "Admin: Contracts"
      summary: List misc costs for a contract
      description: Returns all configured miscellaneous costs (transfer fees, processing fees, etc.) for the contract, ordered
        by `sortOrder`.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: List of misc costs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ContractMiscCost"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createContractMiscCost
      tags:
        - "Admin: Contracts"
      summary: Create a misc cost on a contract
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContractMiscCostCreate"
            example:
              name: Transfer Fee
              amount: 5
              perEvent: cpa
              billClient: false
              isActive: true
              sortOrder: 0
      responses:
        "201":
          description: Misc cost created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ContractMiscCost"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      operationId: updateContractMiscCost
      tags:
        - "Admin: Contracts"
      summary: Update a misc cost on a contract
      description: Updates an existing misc-cost row. The row `id` is passed in the request body.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContractMiscCostUpdate"
            example:
              id: d4e5f6a7-b8c9-0123-defg-234567890123
              amount: 7.5
              isActive: false
      responses:
        "200":
          description: Misc cost updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ContractMiscCost"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteContractMiscCost
      tags:
        - "Admin: Contracts"
      summary: Delete a misc cost from a contract
      description: Deletes a misc-cost row. ID via query param `miscCostId` or body `id`. Requires owner role.
      security:
        - sessionCookie: []
      parameters:
        - name: miscCostId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Misc cost deleted
          content:
            application/json:
              example:
                data:
                  success: true
        "400":
          description: Misc cost id required
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — owner role required
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/contracts/{id}/cpa-rpl:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getContractCpaRpl
      tags:
        - "Admin: Contracts"
      summary: Get CPA/RPL metrics for contract
      security:
        - sessionCookie: []
      parameters:
        - name: lookbackDays
          in: query
          schema:
            type: integer
        - name: excludeDays
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: CPA/RPL calculation result
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/export/{entity}:
    parameters:
      - name: entity
        in: path
        required: true
        schema:
          type: string
          enum:
            - leads
            - calls
            - people
            - posting-log
            - delivery-log
            - webhook-log
            - ping-log
            - audit-log
    get:
      operationId: exportCsv
      tags:
        - "Admin: Export"
      summary: Export entity data as CSV
      description: Streaming CSV export with filter support. For `calls`, the `Service Fee (USD)` column is per-call and
        emitted on the first row of multi-sale calls only (blank on the other rows), so the column sums to the total
        charged.
      security:
        - sessionCookie: []
      parameters:
        - name: fields
          in: query
          description: "`populated` exports only populated columns; any other value exports all columns."
          schema:
            type: string
            enum:
              - populated
              - all
        - name: search
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: status
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: contractId
          in: query
          schema:
            type: string
            format: uuid
        - name: isTrashed
          in: query
          schema:
            type: string
        - name: isTest
          in: query
          schema:
            type: string
        - name: isPaid
          in: query
          schema:
            type: string
        - name: isSold
          in: query
          schema:
            type: string
        - name: isScrubbed
          in: query
          schema:
            type: string
        - name: consentStatus
          in: query
          schema:
            type: string
        - name: type
          in: query
          description: Log-specific filter (posting-log / ping-log).
          schema:
            type: string
        - name: endpoint
          in: query
          description: Log-specific filter.
          schema:
            type: string
        - name: outcome
          in: query
          description: Log-specific filter.
          schema:
            type: string
        - name: deliveryStatus
          in: query
          description: Delivery-log filter.
          schema:
            type: string
        - name: event
          in: query
          description: Webhook-log filter.
          schema:
            type: string
        - name: entityType
          in: query
          description: Audit-log filter.
          schema:
            type: string
        - name: action
          in: query
          description: Audit-log filter.
          schema:
            type: string
      responses:
        "200":
          description: CSV file stream
          content:
            text/csv:
              schema:
                type: string
        "400":
          description: Invalid export entity
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — data export permission required
  /api/admin/export/call-charges:
    get:
      operationId: exportCallCharges
      tags:
        - "Admin: Export"
      summary: Incremental call-charges feed (cursor-based)
      description: |
        Cursor-paged feed of call-tracking charge events (the service fee a tenant was
        charged by the platform per call, daily-aggregate call-tracking rows, and
        premium-destination surcharge aggregates — `service` is `call_tracking` or
        `call_tracking_premium`). Sourced
        from the append-only credit ledger, so each event is returned exactly once. Rows
        become visible in the feed ~5 seconds after commit (so an in-flight write can never
        be skipped by the cursor). Reachable by a read-only admin API key.

        Pass no `since` for the first pull; then pass the previous response's `X-Next-Cursor`
        (or, for JSON, `pagination.nextCursor`) as `since` to fetch only later events — no
        overlap, no gap. Keep pulling while `X-Has-More` is `true`. `serviceFee` is in USD
        (1 credit = $0.01 peg) with up to 6 decimal places: deductions are positive,
        reversals negative. `platformCostUsd` is never exposed. `callerId` is the caller's
        phone number; masked (`***-***-1234`) unless the key has PII access; blank on
        aggregate rows.

        When a page is empty, keep polling with your previous cursor; an empty `since` is
        treated as from-the-beginning. Refunds: charge reversals appear only if written as
        call-scoped `call_tracking` ledger rows — generic credit adjustments (e.g. superadmin
        balance adjustments) are not attributed to calls and do not appear in this feed or in
        per-call fee surfaces. Precision: this feed emits the full ledger precision (up to 6
        decimal places), while the calls CSV export and UI round to cents.
      security:
        - sessionCookie: []
        - adminApiKey: []
      parameters:
        - name: since
          in: query
          description: Opaque cursor from a prior response. Returns rows strictly after it.
          schema:
            type: string
        - name: limit
          in: query
          description: Max rows per page (default 1000, clamped to 1..5000).
          schema:
            type: integer
            default: 1000
            minimum: 1
            maximum: 5000
        - name: format
          in: query
          description: Response format. `csv` (default, includes a `cursor` column) or `json`.
          schema:
            type: string
            enum:
              - csv
              - json
            default: csv
      responses:
        "200":
          description: |
            A page of charge events. Response headers `X-Next-Cursor` (empty when no rows)
            and `X-Has-More` (`true`/`false`) drive incremental paging.
          headers:
            X-Next-Cursor:
              description: Cursor to pass as `since` on the next pull. Empty if the page was empty.
              schema:
                type: string
            X-Has-More:
              description: "`true` if more rows remain after this page."
              schema:
                type: string
                enum:
                  - "true"
                  - "false"
          content:
            text/csv:
              schema:
                type: string
              example: >
                callId,callerId,chargeType,serviceFee,minutes,description,service,createdAt,currency,cursor

                7a1c...,***-***-4567,deduction,0.05,2.0,Call
                tracking,call_tracking,2026-05-03T18:00:00.123Z,USD,MjAyNi0wNS0wM...
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        callId:
                          type: string
                          nullable: true
                          description: Related call id; blank for daily-aggregate rows.
                        callerId:
                          type: string
                          nullable: true
                          description: Caller number; masked unless the key has PII access; blank on aggregate rows.
                        chargeType:
                          type: string
                        serviceFee:
                          type: string
                          description: USD, up to 6 decimal places; deduction positive, reversal negative.
                        minutes:
                          type: string
                          nullable: true
                        description:
                          type: string
                          nullable: true
                        service:
                          type: string
                          nullable: true
                          description: "`call_tracking` for call fees; `call_tracking_premium` for premium-destination surcharge aggregates."
                        createdAt:
                          type: string
                          format: date-time
                        currency:
                          type: string
                        cursor:
                          type: string
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                      nextCursor:
                        type: string
                        nullable: true
                      hasMore:
                        type: boolean
        "400":
          description: Invalid `since` cursor or `limit`
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — data export permission required
  /api/admin/leads/{id}/force-charge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: forceChargeLead
      tags:
        - Leads
      summary: Force charge a lead to a buyer
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contractId
                - reason
              properties:
                contractId:
                  type: string
                  format: uuid
                price:
                  type: number
                  minimum: 0
                reason:
                  type: string
                  minLength: 1
                  maxLength: 500
      responses:
        "200":
          description: Charge applied
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: SKU inventory unavailable or reservation failed
  /api/admin/tenant/match-priority:
    get:
      operationId: getTenantMatchPriority
      tags:
        - Settings
      summary: Get person dedup match priority
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Match priority setting
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      personMatchPriority:
                        type: string
                        enum:
                          - email
                          - phone
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      operationId: setTenantMatchPriority
      tags:
        - Settings
      summary: Set person dedup match priority (write-once)
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - personMatchPriority
              properties:
                personMatchPriority:
                  type: string
                  enum:
                    - email
                    - phone
      responses:
        "200":
          description: Updated
        "400":
          description: Validation error (missing tenant context or invalid value)
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Match priority already set (write-once)
  /api/admin/partners/{id}/notification-subscriptions:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Partner UUID
    get:
      summary: List partner notification subscriptions (admin)
      description: |
        Returns the partner-scoped trigger registry, the partner's portal-access members
        (used to populate the user dropdown), and — when `userId` is provided — that user's
        per-trigger subscription overrides. Admin-only.
      operationId: getAdminPartnerNotificationSubscriptions
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: userId
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Portal-access member UUID. When supplied, response includes that user's subscription overrides.
      responses:
        "200":
          description: Trigger registry, partner members, and (optional) subscription overrides
          content:
            application/json:
              schema:
                type: object
                required:
                  - users
                  - triggers
                  - subscriptions
                properties:
                  users:
                    type: array
                    items:
                      type: object
                      required:
                        - id
                        - email
                      properties:
                        id:
                          type: string
                          format: uuid
                        email:
                          type: string
                          format: email
                        firstName:
                          type: string
                          nullable: true
                        lastName:
                          type: string
                          nullable: true
                  triggers:
                    $ref: "#/components/schemas/NotificationTriggerRegistry"
                  subscriptions:
                    $ref: "#/components/schemas/NotificationSubscriptionMap"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Upsert a partner user's notification subscription (admin)
      description: |
        Upserts the subscription row for `userId` on `triggerKey`. The target user must be
        a portal-access member of partner `{id}` (enforced server-side). Admin-only.
        Slack is deferred — `channels` is restricted to `email` and `in_app`.
      operationId: upsertAdminPartnerNotificationSubscription
      tags:
        - Webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: userId
          in: query
          required: true
          schema:
            type: string
            format: uuid
          description: UUID of the portal-access partner member to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NotificationSubscriptionUpsert"
      responses:
        "200":
          description: Subscription upserted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required:
                      - triggerKey
                      - channels
                      - enabled
                    properties:
                      triggerKey:
                        type: string
                      channels:
                        type: array
                        items:
                          type: string
                          enum:
                            - email
                            - in_app
                      enabled:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/partners/{id}/webhooks:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listPartnerWebhooks
      tags:
        - Webhooks
      summary: List partner webhooks
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated webhooks
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createPartnerWebhook
      tags:
        - Webhooks
      summary: Create partner webhook
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - events
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                  description: Required for GET/POST webhooks. Omitted for preset integrations (GOOGLE_ADS/FACEBOOK_CAPI/AXON), which
                    build their endpoint server-side.
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                    - AXON
                  default: GET
                status:
                  type: string
                  enum:
                    - active
                    - paused
                  default: active
                googleAdsConfig:
                  type: object
                  description: Required when method is GOOGLE_ADS.
                  required:
                    - customerId
                    - conversionActionId
                  properties:
                    customerId:
                      type: string
                    loginCustomerId:
                      type: string
                    conversionActionId:
                      type: string
                    gclidField:
                      type: string
                      default: Gclid
                    conversionValue:
                      type: string
                      default: Payout
                facebookCapiConfig:
                  type: object
                  description: Required when method is FACEBOOK_CAPI.
                  properties:
                    pixelId:
                      type: string
                    adAccountId:
                      type: string
                    eventName:
                      type: string
                      default: Lead
                    fbclidField:
                      type: string
                      default: Fbclid
                    fbcField:
                      type: string
                      default: Fbc
                    fbpField:
                      type: string
                      default: Fbp
                    conversionValue:
                      type: string
                      default: CPA_Payout
                    currencyCode:
                      type: string
                      description: Optional 3-letter ISO code. Omitted or blank resolves the tenant account currency at fire time.
                    includeUserData:
                      type: boolean
                      default: true
                axonConfig:
                  type: object
                  description: Required when method is AXON.
                  required:
                    - eventKey
                    - conversionApiKey
                    - campaigns
                  properties:
                    eventKey:
                      type: string
                    conversionApiKey:
                      type: string
                    currency:
                      type: string
                      description: Optional 3-letter ISO code. Omitted or blank resolves the tenant account currency at fire time.
                    aleidField:
                      type: string
                      default: sub6
                    axwrtField:
                      type: string
                      default: sub7
                    cuidField:
                      type: string
                      default: sub8
                    fallbackEventSourceUrl:
                      type: string
                      format: uri
                    campaigns:
                      type: array
                      minItems: 1
                      items:
                        type: object
                        required:
                          - campaignId
                        properties:
                          campaignId:
                            type: string
                            format: uuid
                          submit:
                            type: number
                            default: 0
                          qualified:
                            type: number
                            default: 0
                          retainer:
                            type: number
                            default: 0
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "201":
          description: Created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/partners/{id}/webhooks/{webhookId}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getPartnerWebhook
      tags:
        - Webhooks
      summary: Get partner webhook
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Webhook detail
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updatePartnerWebhook
      tags:
        - Webhooks
      summary: Update partner webhook
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Partial update. All fields optional.
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                status:
                  type: string
                  enum:
                    - active
                    - paused
                googleAdsConfig:
                  type: object
                  additionalProperties: true
                facebookCapiConfig:
                  type: object
                  additionalProperties: true
                axonConfig:
                  type: object
                  additionalProperties: true
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "200":
          description: Updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deletePartnerWebhook
      tags:
        - Webhooks
      summary: Delete partner webhook
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/partners/{id}/webhooks/{webhookId}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: testPartnerWebhook
      tags:
        - Webhooks
      summary: Test fire partner webhook
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Test result
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Integration (Google Ads / Facebook CAPI) not configured
  /api/admin/partners/{id}/webhooks/{webhookId}/deliveries:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listPartnerWebhookDeliveries
      tags:
        - Webhooks
      summary: List partner webhook deliveries
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Paginated deliveries
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/offers/{offerId}/webhooks/{webhookId}:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getOfferWebhook
      tags:
        - Webhooks
      summary: Get offer webhook
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Webhook detail
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updateOfferWebhook
      tags:
        - Webhooks
      summary: Update offer webhook
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Partial update. All fields optional.
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                status:
                  type: string
                  enum:
                    - active
                    - paused
                googleAdsConfig:
                  type: object
                  additionalProperties: true
                facebookCapiConfig:
                  type: object
                  additionalProperties: true
                axonConfig:
                  type: object
                  additionalProperties: true
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "200":
          description: Updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteOfferWebhook
      tags:
        - Webhooks
      summary: Delete offer webhook
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/offers/{offerId}/webhooks/{webhookId}/test:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: testOfferWebhook
      tags:
        - Webhooks
      summary: Test fire offer webhook
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Test result
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Integration (Google Ads / Facebook CAPI) not configured
  /api/admin/offers/{offerId}/webhooks/{webhookId}/deliveries:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listOfferWebhookDeliveries
      tags:
        - Webhooks
      summary: List offer webhook deliveries
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Paginated deliveries
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/offers/{offerId}/clone:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: cloneOffer
      tags:
        - Offers
      summary: Clone offer
      description: Creates a setup/private copy of an offer. Same-vertical clones copy offer-contract and DQ-contract links;
        campaigns, webhooks, leads, calls, and sales are not copied.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                verticalId:
                  type: string
                  format: uuid
                  description: Optional destination vertical. Defaults to the source offer vertical.
      responses:
        "200":
          description: Cloned offer
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      displayId:
                        type: integer
                      name:
                        type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/offers/{offerId}/sync-pricing:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: syncOfferPricing
      tags:
        - Offers
      summary: Sync offer pricing to campaigns
      description: Bulk updates all linked campaign pricing to match the offer defaults.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Sync result
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/enrollments:
    get:
      operationId: listEnrollments
      tags:
        - "Admin: Enrollments"
      summary: List enrollment requests
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            default: requested
        - name: search
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated enrollments
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/enrollments/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getEnrollment
      tags:
        - "Admin: Enrollments"
      summary: Get enrollment detail
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Enrollment detail
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updateEnrollment
      tags:
        - "Admin: Enrollments"
      summary: Approve or reject an enrollment
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - approved
                    - rejected
                notes:
                  type: string
                  nullable: true
                  maxLength: 1000
      responses:
        "200":
          description: Updated enrollment
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/ip-rules:
    get:
      operationId: listIpRules
      tags:
        - "Admin: Security"
      summary: List IP rules
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated IP rules
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createIpRule
      tags:
        - "Admin: Security"
      summary: Create an IP rule
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - ipAddress
              properties:
                type:
                  type: string
                  enum:
                    - allow
                    - block
                ipAddress:
                  type: string
                description:
                  type: string
                  nullable: true
                  maxLength: 500
                expiresAt:
                  type: string
                  format: date-time
                  nullable: true
      responses:
        "201":
          description: IP rule created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Owner role required
  /api/admin/ip-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: deleteIpRule
      tags:
        - "Admin: Security"
      summary: Delete an IP rule
      security:
        - sessionCookie: []
      responses:
        "200":
          description: IP rule deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — owner access required
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/offers/{offerId}/webhooks:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listOfferWebhooks
      tags:
        - Webhooks
      summary: List webhooks for an offer
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated webhooks
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createOfferWebhook
      tags:
        - Webhooks
      summary: Create a webhook for an offer
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - events
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                url:
                  type: string
                  format: uri
                  maxLength: 2000
                  description: Required for GET/POST webhooks. Omitted for preset integrations (GOOGLE_ADS/FACEBOOK_CAPI/AXON), which
                    build their endpoint server-side.
                events:
                  type: array
                  items:
                    type: string
                  minItems: 1
                method:
                  type: string
                  enum:
                    - GET
                    - POST
                    - GOOGLE_ADS
                    - FACEBOOK_CAPI
                    - AXON
                  default: GET
                status:
                  type: string
                  enum:
                    - active
                    - paused
                  default: active
                googleAdsConfig:
                  type: object
                  description: Required when method is GOOGLE_ADS.
                  required:
                    - customerId
                    - conversionActionId
                  properties:
                    customerId:
                      type: string
                    loginCustomerId:
                      type: string
                    conversionActionId:
                      type: string
                    gclidField:
                      type: string
                      default: Gclid
                    conversionValue:
                      type: string
                      default: Payout
                facebookCapiConfig:
                  type: object
                  description: Required when method is FACEBOOK_CAPI.
                  properties:
                    pixelId:
                      type: string
                    adAccountId:
                      type: string
                    eventName:
                      type: string
                      default: Lead
                    fbclidField:
                      type: string
                      default: Fbclid
                    fbcField:
                      type: string
                      default: Fbc
                    fbpField:
                      type: string
                      default: Fbp
                    conversionValue:
                      type: string
                      default: CPA_Payout
                    currencyCode:
                      type: string
                      description: Optional 3-letter ISO code. Omitted or blank resolves the tenant account currency at fire time.
                    includeUserData:
                      type: boolean
                      default: true
                axonConfig:
                  type: object
                  description: Required when method is AXON.
                  required:
                    - eventKey
                    - conversionApiKey
                    - campaigns
                  properties:
                    eventKey:
                      type: string
                    conversionApiKey:
                      type: string
                    currency:
                      type: string
                      description: Optional 3-letter ISO code. Omitted or blank resolves the tenant account currency at fire time.
                    aleidField:
                      type: string
                      default: sub6
                    axwrtField:
                      type: string
                      default: sub7
                    cuidField:
                      type: string
                      default: sub8
                    fallbackEventSourceUrl:
                      type: string
                      format: uri
                    campaigns:
                      type: array
                      minItems: 1
                      items:
                        type: object
                        required:
                          - campaignId
                        properties:
                          campaignId:
                            type: string
                            format: uuid
                          submit:
                            type: number
                            default: 0
                          qualified:
                            type: number
                            default: 0
                          retainer:
                            type: number
                            default: 0
                minDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Applies only to the `call.duration` event. Minimum call duration in seconds, measured from
                    `durationAnchor`, before this webhook fires. `null` means `call.duration` fires on every completed
                    call.
                durationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  description: Applies only to the `call.duration` event. Where the duration measurement starts. `null` defaults to
                    `connect` at fire time.
                requiredTokens:
                  type: array
                  items:
                    type: string
                filters:
                  description: |
                    Optional filters evaluated against webhook delivery tokens before firing.
                    Either a plain array of filter rules (AND) or an object with `logic` and `rules`.
                  oneOf:
                    - type: array
                      items:
                        $ref: "#/components/schemas/WebhookFilter"
                    - type: object
                      required:
                        - rules
                      properties:
                        logic:
                          type: string
                          enum:
                            - and
                            - or
                          default: and
                        rules:
                          type: array
                          minItems: 1
                          items:
                            $ref: "#/components/schemas/WebhookFilter"
      responses:
        "201":
          description: Webhook created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/offers/{offerId}/webhooks/{webhookId}/deliveries/{deliveryId}/refire:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: deliveryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: refireOfferWebhookDelivery
      tags:
        - Webhooks
      summary: Refire an offer webhook delivery
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Refire result
        "400":
          description: Cannot refire a test or skipped delivery
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Lead/sale no longer exists, or integration not configured
  /api/admin/partners/{id}/webhooks/{webhookId}/deliveries/{deliveryId}/refire:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: webhookId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: deliveryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: refirePartnerWebhookDelivery
      tags:
        - Webhooks
      summary: Refire a partner webhook delivery
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Refire result
        "400":
          description: Cannot refire a test or skipped delivery
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Lead/sale no longer exists, or integration not configured
  /api/admin/pending-signups/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      operationId: updatePendingSignup
      tags:
        - "Admin: Signups"
      summary: Approve or reject a pending signup
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - approve
                    - reject
      responses:
        "200":
          description: Signup updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/reports/contracts:
    get:
      operationId: getContractSummaryReport
      tags:
        - Reports
      summary: Get contract summary report
      description: |
        Pivot summary of contracts (default), buyers, verticals, offers,
        campaigns, or time buckets. Each row exposes sales, revenue, avgPrice,
        acceptRate, refunds, netRevenue, and (for contract pivot) cap utilization.
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          schema:
            type: string
            enum:
              - contract
              - buyer
              - vertical
              - offer
              - campaign
              - day
              - week
              - month
            default: contract
        - name: sortBy
          in: query
          schema:
            type: string
            enum:
              - revenue
              - sales
              - avgPrice
              - acceptRate
              - capUsed
              - capUsedDaily
              - capUsedMonthly
              - netRevenue
              - leads
              - cpaEvents
              - cpaRatio
              - dqLeads
              - paidLeads
            default: revenue
        - name: sortDir
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: timeAxis
          in: query
          description: |
            Window dimension. `revenue` (default) windows on each row's own time
            (`leadSale.createdAt`, `leadDistribution.createdAt`, `refund.processedAt`,
            `leadSale.cpaConvertedAt`). `lead` windows everything on `lead.createdAt`
            so a lead created in the window appears regardless of when it was
            sold/converted.
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: buyerId
          in: query
          schema:
            type: string
        - name: partnerId
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
        - name: offerId
          in: query
          schema:
            type: string
        - name: contractId
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
        - name: packLeftOnly
          in: query
          schema:
            type: string
            enum:
              - "0"
              - "1"
        - name: contractStatus
          in: query
          schema:
            type: string
            description: Comma-separated subset of active|paused|expired|setup
      responses:
        "200":
          description: Contract summary report data
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pivot
                  - range
                  - total
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        dimId:
                          type: string
                        dimLabel:
                          type:
                            - string
                            - "null"
                        sales:
                          type: integer
                        revenue:
                          type: string
                        avgPrice:
                          type:
                            - string
                            - "null"
                        attempts:
                          type: integer
                        acceptRate:
                          type:
                            - number
                            - "null"
                        capUsedDaily:
                          type:
                            - number
                            - "null"
                        capUsedMonthly:
                          type:
                            - number
                            - "null"
                        refundsCount:
                          type: integer
                        refundsAmount:
                          type: string
                        netRevenue:
                          type: string
                        leads:
                          type: integer
                        cpaEvents:
                          type: integer
                        cpaRatio:
                          type:
                            - number
                            - "null"
                  pivot:
                    type: string
                  range:
                    type: object
                    properties:
                      startDate:
                        type: string
                      endDate:
                        type: string
                      tz:
                        type: string
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/admin/reports/dispositions:
    get:
      operationId: getDispositionReport
      tags:
        - Reports
      summary: Get disposition report
      description: |
        In addition to the listed parameters, accepts report-filter params applied as `IN`
        filters: `buyerId`, `partnerId`, `campaignId`, `verticalId`, `offerId`, `status`,
        `state`, `city`, and `sub1`–`sub10` (each comma-separated multi-value). Drill-down
        parent filters are passed as `pf_<dimensionKey>` (e.g. `pf_offer`, `pf_disposition`)
        for each parent dimension below `level`.
      security:
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          description: Comma-separated list of 1–3 dimension keys.
          schema:
            type: string
        - name: level
          in: query
          schema:
            type: integer
            default: 0
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: buyerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: partnerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: campaignId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: verticalId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: offerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: status
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: state
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: city
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
      responses:
        "200":
          description: Disposition report data
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/admin/reports/drilldown:
    get:
      operationId: getDrilldownReport
      tags:
        - Reports
      summary: Get drilldown report
      description: |
        In addition to the listed parameters, accepts report-filter params applied as `IN`
        filters: `buyerId`, `partnerId`, `campaignId`, `verticalId`, `offerId`, `contractId`,
        `status`, `state`, `city`, `zipCode`, and `sub1`–`sub10` (each comma-separated
        multi-value). Drill-down parent filters are passed as `pf_<dimensionKey>` for each
        parent dimension below `level`. When a `custom.<field>` dimension/column or
        `source=vertical` is requested, `verticalId` must be exactly one UUID.
      security:
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          description: Comma-separated dimension keys (max 8).
          schema:
            type: string
        - name: level
          in: query
          schema:
            type: integer
            default: 0
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: timeBasis
          in: query
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: columns
          in: query
          description: Comma-separated selected column / aggregate keys.
          schema:
            type: string
        - name: source
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          description: Required (exactly one UUID) for Vertical Fields reports.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: buyerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: partnerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: campaignId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: offerId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: contractId
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: status
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: state
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: city
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
        - name: zipCode
          in: query
          description: Comma-separated IN filter.
          schema:
            type: string
      responses:
        "200":
          description: Drilldown report data
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/admin/reports/drilldown/distinct:
    get:
      operationId: getDrilldownDistinctValues
      tags:
        - Reports
      summary: Get distinct values for a drilldown field
      security:
        - sessionCookie: []
      parameters:
        - name: field
          in: query
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Distinct values array
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/admin/saved-reports:
    get:
      operationId: listSavedReports
      tags:
        - Reports
      summary: List saved reports
      security:
        - sessionCookie: []
      parameters:
        - name: reportType
          in: query
          description: Single value or comma-separated family filter.
          schema:
            type: string
        - name: scope
          in: query
          schema:
            type: string
            enum:
              - mine
              - library
              - all
            default: all
        - name: folderId
          in: query
          description: A folder UUID, or the literal string `null` for uncategorized reports.
          schema:
            type: string
      responses:
        "200":
          description: List of saved reports
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createSavedReport
      tags:
        - Reports
      summary: Create a saved report
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - reportType
                - config
              properties:
                name:
                  type: string
                  minLength: 1
                reportType:
                  type: string
                config:
                  type: object
                  additionalProperties: true
                  description: Free-form report configuration object.
                isShared:
                  type: boolean
                folderId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "201":
          description: Saved report created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Folder not found
  /api/admin/saved-reports/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      operationId: updateSavedReport
      tags:
        - Reports
      summary: Update a saved report
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Partial update. All fields optional.
              properties:
                name:
                  type: string
                  minLength: 1
                reportType:
                  type: string
                config:
                  type: object
                  additionalProperties: true
                  description: Free-form report configuration object.
                isShared:
                  type: boolean
                folderId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "200":
          description: Saved report updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteSavedReport
      tags:
        - Reports
      summary: Delete a saved report
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Saved report deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/security-settings:
    get:
      operationId: getSecuritySettings
      tags:
        - "Admin: Security"
      summary: Get security settings
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Security settings
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      operationId: updateSecuritySettings
      tags:
        - "Admin: Security"
      summary: Update security settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                hipaaMode:
                  type: string
                sessionTimeoutMinutes:
                  type: string
                sessionIdleTimeoutMinutes:
                  type: string
                maxConcurrentSessions:
                  type: string
                magicLinkIpMode:
                  type: string
                twoFactorPolicy:
                  type: string
                  description: Legacy 2FA policy (optional/required_admins/required_all). Kept for backwards compatibility — prefer the
                    per-role flags below.
                twoFactorRequiredForAdmins:
                  type: string
                  enum:
                    - true
                    - false
                twoFactorRequiredForBuyers:
                  type: string
                  enum:
                    - true
                    - false
                twoFactorRequiredForPartners:
                  type: string
                  enum:
                    - true
                    - false
                googleOAuthEnabled:
                  type: string
                lockoutThreshold:
                  type: string
                lockoutDurationMinutes:
                  type: string
      responses:
        "200":
          description: Security settings updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/sessions:
    get:
      operationId: listSessions
      tags:
        - "Admin: Security"
      summary: List active sessions
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: q
          in: query
          schema:
            type: string
          description: Search by email
        - name: role
          in: query
          schema:
            type: string
          description: Filter by role
      responses:
        "200":
          description: Paginated sessions
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/webhooks/{id}/deliveries/{deliveryId}/refire:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: deliveryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: refireWebhookDelivery
      tags:
        - Webhooks
      summary: Refire a webhook delivery
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Refire result
        "400":
          description: Cannot refire a test or skipped delivery
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Lead/sale no longer exists, or integration not configured
  /api/admin/referrers:
    get:
      operationId: listReferrers
      tags:
        - "Admin: Referrers"
      summary: List referrers (paginated)
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum:
              - pending
              - active
              - paused
              - inactive
        - name: search
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - displayId
              - name
              - email
              - status
              - balance
              - createdAt
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
      responses:
        "200":
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createReferrer
      tags:
        - "Admin: Referrers"
      summary: Create a new referrer
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - paused
                    - inactive
                  default: pending
                paymentCycle:
                  type: string
                  enum:
                    - weekly
                    - bimonthly
                    - monthly
                    - adhoc
                  nullable: true
                paymentThreshold:
                  type: number
                  minimum: 0
                  nullable: true
                signingBonusOverrideCents:
                  type: integer
                  nullable: true
                revShareOverrideBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
                  nullable: true
                revShareBaseOverride:
                  type: string
                  enum:
                    - gross_revenue
                    - net_of_platform_fee
                  nullable: true
                notes:
                  type: string
                  nullable: true
      responses:
        "201":
          description: Created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Duplicate email
  /api/admin/referrers/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getReferrer
      tags:
        - "Admin: Referrers"
      summary: Get a referrer
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Referrer detail
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateReferrer
      tags:
        - "Admin: Referrers"
      summary: Update a referrer
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - paused
                    - inactive
                paymentCycle:
                  type: string
                  enum:
                    - weekly
                    - bimonthly
                    - monthly
                    - adhoc
                  nullable: true
                paymentThreshold:
                  type: number
                  minimum: 0
                  nullable: true
                signingBonusOverrideCents:
                  type: integer
                  nullable: true
                revShareOverrideBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
                  nullable: true
                revShareBaseOverride:
                  type: string
                  enum:
                    - gross_revenue
                    - net_of_platform_fee
                  nullable: true
                notes:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteReferrer
      tags:
        - "Admin: Referrers"
      summary: Soft-delete (sets status=inactive)
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Soft-deleted
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/referrers/{id}/invite:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: inviteReferrer
      tags:
        - "Admin: Referrers"
      summary: Send a magic-link invitation to the referrer's email
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Invitation sent
        "400":
          description: Referrer has no email
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/referrer/referrals:
    get:
      operationId: listReferrerReferrals
      tags:
        - Referrer Portal
      summary: List buyers attributed to the calling referrer (PII redacted)
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of referred buyers (no PII)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 11111111-2222-3333-4444-555555555555
                    label: Acme Corp
                    status: active
                    createdAt: 2026-04-29T10:00:00Z
                    lifetimeCommissionCents: 0
  /api/referrer/commissions:
    get:
      operationId: listReferrerCommissions
      tags:
        - Referrer Portal
      summary: List referrer commissions
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated commission ledger
  /api/referrer/payouts:
    get:
      operationId: listReferrerPayouts
      tags:
        - Referrer Portal
      summary: List referrer payouts
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated payout history
  /api/referrer/settings:
    get:
      operationId: getReferrerSettings
      tags:
        - Referrer Portal
      summary: Get referrer settings
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Referrer profile + payout config
    put:
      operationId: updateReferrerSettings
      tags:
        - Referrer Portal
      summary: Update referrer settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                payoutMethodPreference:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated profile
  /api/referrer/share-link:
    get:
      operationId: getReferrerShareLink
      tags:
        - Referrer Portal
      summary: Returns the referrer's tenant-scoped share URL
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Share link
          content:
            application/json:
              example:
                data:
                  url: https://tenant.example.com/?ref=83d059fe-d4a2-477d-83e3-6febeda1a3c2
                  referrerId: 83d059fe-d4a2-477d-83e3-6febeda1a3c2
  /api/admin/coupons:
    get:
      operationId: listCoupons
      tags:
        - "Admin: Coupons"
      summary: List coupons (paginated). Joins promotion code + referrer.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          schema:
            type: string
        - name: referrerId
          in: query
          schema:
            type: string
            format: uuid
        - name: includeDeleted
          in: query
          schema:
            type: integer
            enum:
              - 0
              - 1
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
      responses:
        "200":
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createCoupon
      tags:
        - "Admin: Coupons"
      summary: Create a coupon + 1-1 promotion code on Stripe and mirror locally
      description: |
        Creates the Stripe coupon first, then the promotion code. If the promo
        code create fails, the orphan Stripe coupon is rolled back so DB and
        Stripe never diverge. Returns 400 CONNECT_NOT_READY if the tenant has
        no `stripeAccountId`.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
                - duration
              properties:
                name:
                  type: string
                  nullable: true
                code:
                  type: string
                  description: Uppercase A-Z 0-9 _ - only
                amountOffCents:
                  type: integer
                  nullable: true
                  description: XOR with percentOffBps
                percentOffBps:
                  type: integer
                  minimum: 1
                  maximum: 10000
                  nullable: true
                currency:
                  type: string
                  nullable: true
                  description: Required when amountOffCents is set
                duration:
                  type: string
                  enum:
                    - once
                    - repeating
                    - forever
                durationInMonths:
                  type: integer
                  nullable: true
                  description: Required when duration=repeating
                maxRedemptions:
                  type: integer
                  nullable: true
                redeemBy:
                  type: string
                  format: date-time
                  nullable: true
                referrerId:
                  type: string
                  format: uuid
                  nullable: true
                appliesToSkuIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                  nullable: true
                active:
                  type: boolean
                  default: true
                expiresAt:
                  type: string
                  format: date-time
                  nullable: true
                promotionMaxRedemptions:
                  type: integer
                  nullable: true
                perBuyerRedemptionLimit:
                  type: integer
                  enum:
                    - 1
                  nullable: true
                restrictions:
                  type: object
                  nullable: true
                  properties:
                    firstTimeTransaction:
                      type: boolean
                    minimumAmountCents:
                      type: integer
                    minimumAmountCurrency:
                      type: string
                      minLength: 3
                      maxLength: 3
      responses:
        "201":
          description: Coupon + promotion code created
        "400":
          $ref: "#/components/responses/ValidationError"
        "409":
          description: Promotion code already exists
        "422":
          description: SKU sync failed
  /api/admin/coupons/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getCoupon
      tags:
        - "Admin: Coupons"
      summary: Get a coupon
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Coupon detail with promotion code + referrer
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateCoupon
      tags:
        - "Admin: Coupons"
      summary: Update name (Stripe only allows mutating name + metadata)
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  nullable: true
                metadata:
                  type: object
                  additionalProperties:
                    type: string
      responses:
        "200":
          description: Updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          description: Coupon already deleted
    delete:
      operationId: deleteCoupon
      tags:
        - "Admin: Coupons"
      summary: Soft-delete locally + Stripe cascade-delete (also kills promo code)
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/settings/referrer-program:
    get:
      operationId: getReferrerProgramSettings
      tags:
        - Settings
      summary: Read tenant referrer-program defaults
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Settings
          content:
            application/json:
              example:
                data:
                  referrerProgramEnabled: true
                  defaultReferrerSigningBonusCents: 5000
                  defaultReferrerRevSharePctBps: 1000
                  referrerRevShareBase: gross_revenue
                  stripeAccountId: acct_123
    put:
      operationId: updateReferrerProgramSettings
      tags:
        - Settings
      summary: Update tenant referrer-program defaults
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - referrerProgramEnabled
                - defaultReferrerSigningBonusCents
                - defaultReferrerRevSharePctBps
                - referrerRevShareBase
              properties:
                referrerProgramEnabled:
                  type: boolean
                defaultReferrerSigningBonusCents:
                  type: integer
                  minimum: 0
                  maximum: 1000000
                defaultReferrerRevSharePctBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
                referrerRevShareBase:
                  type: string
                  enum:
                    - gross_revenue
                    - net_of_platform_fee
      responses:
        "200":
          description: Updated
        "400":
          $ref: "#/components/responses/ValidationError"
  /api/cron/referrer-commission-digest:
    get:
      operationId: cronReferrerCommissionDigest
      tags:
        - Cron
      summary: Daily digest email of new commission accruals (Bearer CRON_SECRET)
      description: |
        Groups `referrerCommission` rows accrued in the last 24h by referrerId,
        sends one `referrer-commission-accrued` email per active referrer. Auth
        is `Authorization: Bearer $CRON_SECRET`.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Digest results
          content:
            application/json:
              example:
                data:
                  processed: 3
                  sent: 2
                  results: []
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/admin/skus/bulk-import:
    post:
      operationId: bulkImportSkus
      tags:
        - "Admin: SKUs"
      summary: Bulk import SKUs (idempotent on slug, dry-run preview supported)
      description: |
        Imports a batch of up to 100 SKU specs. Idempotent on `(tenant scope, slug)`:
        new slug → `create`, existing slug + diff → `update`, existing slug + no
        diff → `skip`, row-level error → `error` (rest of batch continues).
        `dryRun: true` returns the same response shape with no DB writes.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - items
              properties:
                items:
                  type: array
                  minItems: 1
                  maxItems: 100
                  items:
                    type: object
                    required:
                      - slug
                      - name
                      - priceCents
                      - leadCount
                    properties:
                      slug:
                        type: string
                        description: lowercase kebab-case
                      name:
                        type: string
                      tagline:
                        type: string
                        nullable: true
                      badge:
                        type: string
                        nullable: true
                      priceCents:
                        type: integer
                        minimum: 0
                      leadCount:
                        type: integer
                        minimum: 1
                      verticalId:
                        type: string
                        format: uuid
                        nullable: true
                        description: Single vertical this SKU sells (1:1 SKU↔vertical).
                      isSubscription:
                        type: boolean
                        default: false
                      billingInterval:
                        type: string
                        enum:
                          - week
                          - month
                          - year
                        nullable: true
                      availableUntil:
                        type: string
                        format: date
                        nullable: true
                        description: Promo cutoff date (YYYY-MM-DD, tenant timezone). Hidden from shop after this date.
                      bonusLeads:
                        type: integer
                        nullable: true
                      allowPromoCodes:
                        type: boolean
                        default: false
                      trialLeads:
                        type: integer
                        nullable: true
                      trialDays:
                        type: integer
                        nullable: true
                        maximum: 90
                      productId:
                        type: string
                        format: uuid
                        nullable: true
                      showInBuyerPortal:
                        type: boolean
                        default: false
                      firstTimeBuyerOnly:
                        type: boolean
                        default: false
                      active:
                        type: boolean
                        default: true
                      sortOrder:
                        type: integer
                        default: 0
                dryRun:
                  type: boolean
                  default: false
            example:
              dryRun: false
              items:
                - slug: life-silver
                  name: Silver Life
                  priceCents: 129500
                  leadCount: 35
                  verticalId: 5c5a78f5-1813-4ef6-a8d2-ce7b007eafca
                  isSubscription: false
                  active: true
                  allowPromoCodes: true
      responses:
        "200":
          description: Per-row results plus a summary count
          content:
            application/json:
              example:
                data:
                  dryRun: false
                  results:
                    - slug: life-silver
                      action: create
                      skuId: 1f9c5e4a-…
                      diff: null
                      error: null
                    - slug: life-gold
                      action: update
                      skuId: 2a8b1d3c-…
                      diff:
                        priceCents:
                          from: 269500
                          to: 262500
                      error: null
                    - slug: life-platinum
                      action: skip
                      skuId: 8d4f1c2e-…
                      diff: null
                      error: null
                  summary:
                    created: 1
                    updated: 1
                    skipped: 1
                    errors: 0
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/admin/wizards:
    get:
      operationId: listWizards
      tags:
        - "Admin: Wizards"
      summary: List buyer onboarding wizards for the tenant
      description: |
        Paginated list of wizards (signup flows) owned by the caller's tenant.
        Superadmin sees wizards across all tenants. Filter by `status` to scope
        to drafts, active (published), or archived versions.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum:
              - draft
              - active
              - archived
        - name: wizardType
          in: query
          schema:
            type: string
            enum:
              - lead
              - call
      responses:
        "200":
          description: Paginated wizard list
          content:
            application/json:
              example:
                data:
                  - id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                    tenantId: 11111111-2222-3333-4444-555555555555
                    name: Default Buyer Onboarding
                    slug: test-default-onboarding
                    status: active
                    isDefault: true
                    version: 3
                    createdAt: 2026-04-01T12:00:00.000Z
                    updatedAt: 2026-04-20T18:30:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createWizard
      tags:
        - "Admin: Wizards"
      summary: Create a new draft wizard
      description: |
        Creates a wizard in `draft` status with `version: 1` and zero steps.
        Per decision 10, only one wizard per tenant may have `isDefault: true`;
        the partial unique index enforces this and surfaces 409 on collision.
        Slug must be unique within the tenant.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - slug
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 120
                slug:
                  type: string
                  minLength: 1
                  maxLength: 120
                  pattern: ^[a-z0-9-]+$
                  description: lowercase kebab-case
                wizardType:
                  type: string
                  enum:
                    - lead
                    - call
                  default: lead
                isDefault:
                  type: boolean
                  default: false
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                setupOnComplete:
                  type: boolean
                contractOptions:
                  type: object
                  additionalProperties: true
                  description: Contract defaults applied on completion (contractStatus, sellType, geoFilters, demographicFilters,
                    customFilters, callPhoneNumber, callBackupNumber, scheduleTimezone, scheduleHours).
            example:
              name: Test Buyer Onboarding
              slug: test-buyer-onboarding
              isDefault: false
      responses:
        "201":
          description: Wizard created
          content:
            application/json:
              example:
                data:
                  id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  tenantId: 11111111-2222-3333-4444-555555555555
                  name: Test Buyer Onboarding
                  slug: test-buyer-onboarding
                  status: draft
                  isDefault: false
                  version: 1
                  createdAt: 2026-05-04T12:00:00.000Z
                  updatedAt: 2026-05-04T12:00:00.000Z
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Duplicate wizard slug or tenant already has a default wizard
  /api/admin/wizards/{id}:
    get:
      operationId: getWizard
      tags:
        - "Admin: Wizards"
      summary: Get wizard detail with steps
      description: |
        Returns the wizard meta plus its ordered `steps[]` array. Cross-tenant
        IDs return 404 (tenant filter strips them before existence check).
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Wizard with steps
          content:
            application/json:
              example:
                data:
                  id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  tenantId: 11111111-2222-3333-4444-555555555555
                  name: Test Buyer Onboarding
                  slug: test-buyer-onboarding
                  status: draft
                  isDefault: false
                  version: 1
                  createdAt: 2026-05-04T12:00:00.000Z
                  updatedAt: 2026-05-04T12:00:00.000Z
                  steps:
                    - id: aa11bb22-cc33-dd44-ee55-ff6677889900
                      order: 0
                      type: statePicker
                      config:
                        label: Which states do you cover?
                        required: true
                    - id: bb22cc33-dd44-ee55-ff66-001122334455
                      order: 1
                      type: termsAccept
                      config:
                        documentSlug: tos-v1
                        required: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updateWizard
      tags:
        - "Admin: Wizards"
      summary: Bulk-replace wizard meta and steps
      description: |
        Atomically replaces the wizard's meta (name/slug/isDefault) and the
        full `steps[]` array. Drafts and active wizards are editable here;
        archived wizards return 409 (clone to a draft first). Editing an
        active wizard bumps `version` so new buyer magic-link snapshots are
        attributable to a distinct revision — in-flight buyers keep finishing
        on the frozen snapshot from the version they started with. Each
        step's `config` is narrowed against the per-type schema in
        `stepRegistry`; the first failing step returns a 400 with the
        failing index and field path.
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - steps
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 120
                slug:
                  type: string
                  minLength: 1
                  maxLength: 120
                  pattern: ^[a-z0-9-]+$
                wizardType:
                  type: string
                  enum:
                    - lead
                    - call
                isDefault:
                  type: boolean
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                setupOnComplete:
                  type: boolean
                contractOptions:
                  type: object
                  additionalProperties: true
                  description: Contract defaults applied on completion (contractStatus, sellType, geoFilters, demographicFilters,
                    customFilters, callPhoneNumber, callBackupNumber, scheduleTimezone, scheduleHours).
                steps:
                  type: array
                  description: Full replacement set; order values must be unique.
                  items:
                    type: object
                    required:
                      - type
                      - order
                      - config
                    properties:
                      type:
                        type: string
                        enum:
                          - statePicker
                          - integerCap
                          - deliveryPicker
                          - customQuestion
                          - termsAccept
                      order:
                        type: integer
                        minimum: 0
                      config:
                        type: object
                        description: Per-type config validated by stepRegistry[type].configSchema
            example:
              name: Test Buyer Onboarding
              slug: test-buyer-onboarding
              isDefault: false
              steps:
                - type: statePicker
                  order: 0
                  config:
                    label: Which states do you cover?
                    required: true
                - type: integerCap
                  order: 1
                  config:
                    label: Daily lead cap
                    min: 1
                    max: 500
                    required: true
                - type: termsAccept
                  order: 2
                  config:
                    documentSlug: tos-v1
                    required: true
      responses:
        "200":
          description: Updated wizard with steps
          content:
            application/json:
              example:
                data:
                  id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  tenantId: 11111111-2222-3333-4444-555555555555
                  name: Test Buyer Onboarding
                  slug: test-buyer-onboarding
                  status: draft
                  isDefault: false
                  version: 1
                  createdAt: 2026-05-04T12:00:00.000Z
                  updatedAt: 2026-05-04T12:34:00.000Z
                  steps:
                    - id: aa11bb22-cc33-dd44-ee55-ff6677889900
                      order: 0
                      type: statePicker
                      config:
                        label: Which states do you cover?
                        required: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Wizard is archived and cannot be edited (clone it to a draft first). Active wizards are editable in place;
            the API bumps version on each active save so new buyers start on the new revision while in-flight buyers
            keep finishing on the frozen snapshot from the version they started with.
  /api/admin/wizards/{id}/publish:
    post:
      operationId: publishWizard
      tags:
        - "Admin: Wizards"
      summary: Publish a draft wizard (sets status=active, bumps version)
      description: |
        Transitions the wizard to `status: 'active'` and increments `version`.
        Rejects zero-step wizards and already-active wizards with 400. The
        previously-active default wizard (if any) keeps its snapshot — buyers
        in flight are unaffected (decisions 21/22).
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Wizard published
          content:
            application/json:
              example:
                data:
                  id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  status: active
                  version: 2
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/wizards/{id}/archive:
    post:
      operationId: archiveWizard
      tags:
        - "Admin: Wizards"
      summary: Archive a wizard
      description: |
        Transitions the wizard to `status: 'archived'`. Per decision 10, the
        only `isDefault: true`, `status: 'active'` wizard for a tenant cannot
        be archived — promote another wizard to default first. Existing
        `buyer.wizardSnapshot` rows are intentionally untouched so in-flight
        buyers finish on their original snapshot.
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Wizard archived
          content:
            application/json:
              example:
                data:
                  id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  status: archived
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/products:
    get:
      operationId: listProducts
      tags:
        - "Admin: Products"
      summary: List products for the tenant
      description: |
        Paginated list of products (per-vertical merchandising/inventory grouping
        above SKU). Returns all status values; admin UI filters via the `status`
        query param (Decision 20). Filter by `verticalId` to scope to a vertical,
        or `search` for partial matches across name, slug, and tagline.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
              - archived
              - expired
        - name: productType
          in: query
          schema:
            type: string
            enum:
              - lead
              - call
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Paginated product list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createProduct
      tags:
        - "Admin: Products"
      summary: Create a product
      description: |
        Creates a product under the caller's tenant. `verticalId` is required
        and immutable post-creation (Decision 34). `defaultContractOptions`
        mirrors `wizard.contractOptions` so both creation paths (wizard-driven
        and admin-default) write the same schema (Decision 11). `tenantId` and
        `legacySkuId` are server-controlled and rejected if supplied.
        Call products use `callBillableEvent` to define when prepaid call units
        are consumed. For duration events, `callDurationAnchor` accepts
        `incoming`, `dial`, or `connect` and defaults to `connect`.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - verticalId
                - name
                - slug
              properties:
                verticalId:
                  type: string
                  format: uuid
                productType:
                  type: string
                  enum:
                    - lead
                    - call
                  default: lead
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                slug:
                  type: string
                  minLength: 1
                  maxLength: 100
                  pattern: ^[a-z0-9-]+$
                  description: lowercase kebab-case, unique per tenant
                description:
                  type: string
                  nullable: true
                tagline:
                  type: string
                  maxLength: 512
                  nullable: true
                badge:
                  type: string
                  maxLength: 64
                  nullable: true
                defaultWizardId:
                  type: string
                  format: uuid
                  nullable: true
                defaultContractOptions:
                  type: object
                  nullable: true
                  description: Admin-default contract form prefill (Decision 11). Shape mirrors wizard.contractOptions.
                callBillableEvent:
                  type: string
                  enum:
                    - raw_call
                    - connected
                    - duration
                  nullable: true
                  description: Call products only. Defines when a prepaid call unit is consumed.
                callTerms:
                  type: string
                  maxLength: 4000
                  nullable: true
                callDurationThresholdSec:
                  type: integer
                  minimum: 1
                  nullable: true
                  description: Required when callBillableEvent is duration.
                callDurationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  default: connect
                  description: Start point for duration-threshold call unit consumption. Target must answer before a duration-threshold
                    unit is consumed.
                stripeProductId:
                  type: string
                  pattern: ^prod_
                  nullable: true
                filterAge:
                  type: boolean
                  default: false
                filterCreative:
                  type: boolean
                  default: false
                dayparting:
                  type: boolean
                  default: false
                crmIntegration:
                  type: boolean
                  default: false
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                    - archived
                    - expired
                  default: active
      responses:
        "201":
          description: Product created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Slug already exists for this tenant
  /api/admin/products/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getProduct
      tags:
        - "Admin: Products"
      summary: Get product detail
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Product detail
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updateProduct
      tags:
        - "Admin: Products"
      summary: Update a product
      description: |
        Updates an existing product. `verticalId` is immutable post-creation
        (Decision 34) — supplying it returns 400 with code `IMMUTABLE_FIELD`.
        Slug is editable but must remain unique within the tenant.
        For duration-based call unit consumption, `callDurationAnchor` accepts
        `incoming`, `dial`, or `connect`; omitted/null duration anchors resolve
        to `connect`.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                productType:
                  type: string
                  enum:
                    - lead
                    - call
                slug:
                  type: string
                  minLength: 1
                  maxLength: 100
                  pattern: ^[a-z0-9-]+$
                description:
                  type: string
                  nullable: true
                tagline:
                  type: string
                  maxLength: 512
                  nullable: true
                badge:
                  type: string
                  maxLength: 64
                  nullable: true
                defaultWizardId:
                  type: string
                  format: uuid
                  nullable: true
                defaultContractOptions:
                  type: object
                  nullable: true
                callBillableEvent:
                  type: string
                  enum:
                    - raw_call
                    - connected
                    - duration
                  nullable: true
                callTerms:
                  type: string
                  maxLength: 4000
                  nullable: true
                callDurationThresholdSec:
                  type: integer
                  minimum: 1
                  nullable: true
                callDurationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  nullable: true
                  default: connect
                stripeProductId:
                  type: string
                  pattern: ^prod_
                  nullable: true
                filterAge:
                  type: boolean
                filterCreative:
                  type: boolean
                dayparting:
                  type: boolean
                crmIntegration:
                  type: boolean
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                    - archived
                    - expired
      responses:
        "200":
          description: Product updated
        "400":
          description: Validation error or IMMUTABLE_FIELD (verticalId)
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Slug already exists for this tenant
    delete:
      operationId: archiveProduct
      tags:
        - "Admin: Products"
      summary: Soft-archive a product
      description: |
        Soft-deletes by setting `status='archived'`. Blocked while any active
        SKU references this product (409 `PRODUCT_HAS_SKUS`) or any active
        contract references it (409 `PRODUCT_HAS_CONTRACTS`).
      security:
        - sessionCookie: []
      responses:
        "204":
          description: Product archived
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Product has active SKUs (PRODUCT_HAS_SKUS) or active contracts (PRODUCT_HAS_CONTRACTS)
  /api/admin/verticals/{id}/sheet-templates:
    get:
      operationId: listVerticalSheetTemplates
      tags:
        - Verticals
      summary: List Google Sheets field-mapping templates for a vertical
      description: |
        Returns a paginated list of saved Google Sheets field-mapping templates
        scoped to the given vertical (tenant-isolated). Used by the contract
        editor's Templates dropdown.
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Vertical ID
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated template list
          content:
            application/json:
              example:
                data:
                  - id: a1b2c3d4-1111-2222-3333-444455556666
                    tenantId: 11111111-2222-3333-4444-555555555555
                    verticalId: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                    name: Standard Insurance Columns
                    fieldMapping:
                      - from: firstName
                        to: First Name
                      - from: email
                        to: Email Address
                    createdAt: 2026-05-11T12:00:00.000Z
                    updatedAt: 2026-05-11T12:00:00.000Z
                    createdBy: bb22cc33-dd44-ee55-ff66-001122334455
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: createVerticalSheetTemplate
      tags:
        - Verticals
      summary: Create a Google Sheets field-mapping template under a vertical
      description: |
        Creates a saved field-mapping template tied to (tenant, vertical, name).
        Name uniqueness is enforced per vertical — a 409 DUPLICATE error is
        returned on collision.
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Vertical ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - fieldMapping
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                fieldMapping:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                      - from
                      - to
                    properties:
                      from:
                        type: string
                        minLength: 1
                      to:
                        type: string
                        minLength: 1
                      transformers:
                        type: array
                        items:
                          type: object
                          required:
                            - type
                          properties:
                            type:
                              type: string
                            params:
                              type: object
                              additionalProperties: true
            example:
              name: Standard Insurance Columns
              fieldMapping:
                - from: firstName
                  to: First Name
                - from: email
                  to: Email Address
      responses:
        "201":
          description: Template created
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-1111-2222-3333-444455556666
                  tenantId: 11111111-2222-3333-4444-555555555555
                  verticalId: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  name: Standard Insurance Columns
                  fieldMapping:
                    - from: firstName
                      to: First Name
                  createdAt: 2026-05-11T12:00:00.000Z
                  updatedAt: 2026-05-11T12:00:00.000Z
                  createdBy: bb22cc33-dd44-ee55-ff66-001122334455
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A template with that name already exists for this vertical
  /api/admin/verticals/{id}/sheet-templates/{templateId}:
    get:
      operationId: getVerticalSheetTemplate
      tags:
        - Verticals
      summary: Get a single Google Sheets field-mapping template
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Vertical ID
        - name: templateId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Template detail
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-1111-2222-3333-444455556666
                  tenantId: 11111111-2222-3333-4444-555555555555
                  verticalId: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                  name: Standard Insurance Columns
                  fieldMapping:
                    - from: firstName
                      to: First Name
                  createdAt: 2026-05-11T12:00:00.000Z
                  updatedAt: 2026-05-11T12:00:00.000Z
                  createdBy: bb22cc33-dd44-ee55-ff66-001122334455
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updateVerticalSheetTemplate
      tags:
        - Verticals
      summary: Update a Google Sheets field-mapping template
      description: |
        Updates either name, fieldMapping, or both. Name renames must remain
        unique within the vertical — 409 DUPLICATE on collision.
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Vertical ID
        - name: templateId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                fieldMapping:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                      - from
                      - to
                    properties:
                      from:
                        type: string
                        minLength: 1
                      to:
                        type: string
                        minLength: 1
                      transformers:
                        type: array
                        items:
                          type: object
                          required:
                            - type
                          properties:
                            type:
                              type: string
                            params:
                              type: object
                              additionalProperties: true
            example:
              name: Renamed Template
      responses:
        "200":
          description: Updated template
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-1111-2222-3333-444455556666
                  name: Renamed Template
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A template with that name already exists for this vertical
    delete:
      operationId: deleteVerticalSheetTemplate
      tags:
        - Verticals
      summary: Hard-delete a Google Sheets field-mapping template
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Vertical ID
        - name: templateId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Deleted (no body)
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/admin/sheet-templates:
    get:
      operationId: listAllSheetTemplates
      tags:
        - Verticals
      summary: List Google Sheets field-mapping templates across verticals
      description: |
        Tenant-scoped, cross-vertical list — used by the wizard builder's
        deliveryPicker dropdown. Optional `verticalId` filter narrows results.
      security:
        - sessionCookie: []
      parameters:
        - name: verticalId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated template list (with vertical name joined)
          content:
            application/json:
              example:
                data:
                  - id: a1b2c3d4-1111-2222-3333-444455556666
                    tenantId: 11111111-2222-3333-4444-555555555555
                    verticalId: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                    verticalName: Auto Insurance
                    vertical:
                      id: 7c1f9e2a-3b4d-4e5f-9a8b-1c2d3e4f5a6b
                      name: Auto Insurance
                    name: Standard Insurance Columns
                    fieldMapping:
                      - from: firstName
                        to: First Name
                    createdAt: 2026-05-11T12:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/admin/forecast:
    get:
      summary: Forecast revenue
      description: |
        Projects revenue forward using historical sales trends. Optionally scoped
        to a single offer, buyer, or partner. Requires the `forecasting` tenant
        feature — returns `403 FEATURE_DISABLED` when the feature is off.
      operationId: getRevenueForecast
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: daysAhead
          in: query
          description: Number of days to project forward.
          schema:
            type: integer
            default: 7
        - name: entityType
          in: query
          description: Scope the forecast to a single entity type. Any other value is ignored (tenant-wide forecast).
          schema:
            type: string
            enum:
              - offer
              - buyer
              - partner
        - name: entityId
          in: query
          description: UUID of the entity to scope to (used with `entityType`).
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Forecast result
          content:
            application/json:
              example:
                data:
                  daysAhead: 7
                  projectedRevenue: "12450.00"
                  dailyAverage: "1778.57"
                  confidence: 0.82
        "401":
          description: Unauthorized
        "403":
          description: Forecasting feature not enabled for this tenant (FEATURE_DISABLED)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/admin/optimization:
    get:
      summary: List optimization suggestions
      description: |
        Returns revenue and cap-utilization improvement suggestions for the tenant,
        analyzed over the trailing 30 days and sorted by estimated impact (descending).
        Requires the `optimization` tenant feature — returns `403 FEATURE_DISABLED`
        when the feature is off.
      operationId: listOptimizationSuggestions
      tags:
        - "Admin: Optimization"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Suggestions ordered by estimated impact
          content:
            application/json:
              example:
                data:
                  - type: cap_utilization
                    entityId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    action: raise_daily_cap
                    title: Contract hitting daily cap
                    estimatedImpact: "450.00"
        "401":
          description: Unauthorized
        "403":
          description: Optimization feature not enabled for this tenant (FEATURE_DISABLED)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Apply an optimization suggestion
      description: |
        Applies a single optimization suggestion (e.g. raising a cap or adjusting
        priority) to the referenced entity. The engine performs the underlying update.
        Note: unlike the GET handler, this method does not re-check the `optimization`
        feature flag.
      operationId: applyOptimizationSuggestion
      tags:
        - "Admin: Optimization"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - entityId
                - action
              properties:
                type:
                  type: string
                  description: Suggestion type (matches the `type` returned by the GET endpoint).
                entityId:
                  type: string
                  format: uuid
                action:
                  type: string
                  description: Action key describing the change to apply.
            example:
              type: cap_utilization
              entityId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              action: raise_daily_cap
      responses:
        "200":
          description: Suggestion applied
          content:
            application/json:
              example:
                data:
                  applied: true
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/report-folders:
    get:
      summary: List report folders
      description: |
        Returns all report folders for the tenant, each with a `reportCount` of the
        published (shared) saved reports it contains. Ordered by `sortOrder` then name.
        Requires the `optimization` tenant feature.
      operationId: listReportFolders
      tags:
        - "Admin: Report Folders"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: List of report folders
          content:
            application/json:
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Weekly Ops
                    description: Reports reviewed in the Monday ops meeting
                    sortOrder: 0
                    createdBy: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    createdByName: Jane Admin
                    reportCount: 4
                    createdAt: 2025-01-15T10:00:00.000Z
                    updatedAt: 2025-01-15T10:00:00.000Z
        "401":
          description: Unauthorized
        "403":
          description: Optimization feature not enabled (FEATURE_DISABLED)
    post:
      summary: Create a report folder
      description: |
        Creates a report folder for organizing saved reports. Requires a user-scoped
        admin session (returns `403` otherwise) and the `optimization` tenant feature.
        Folder names are unique per tenant.
      operationId: createReportFolder
      tags:
        - "Admin: Report Folders"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                description:
                  type: string
                  maxLength: 500
                sortOrder:
                  type: integer
            example:
              name: Weekly Ops
              description: Reports reviewed in the Monday ops meeting
              sortOrder: 0
      responses:
        "201":
          description: Folder created
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Weekly Ops
                  description: Reports reviewed in the Monday ops meeting
                  sortOrder: 0
        "400":
          description: Validation error, or a folder with that name already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: User-scoped session required, or optimization feature not enabled
  /api/admin/report-folders/{id}:
    put:
      summary: Update a report folder
      description: Updates name, description, or sort order of a report folder. Requires the `optimization` tenant feature.
        Folder names are unique per tenant.
      operationId: updateReportFolder
      tags:
        - "Admin: Report Folders"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                description:
                  type: string
                  maxLength: 500
                sortOrder:
                  type: integer
            example:
              name: Weekly Ops (Renamed)
      responses:
        "200":
          description: Updated folder
        "400":
          description: Validation error, or a folder with that name already exists
        "401":
          description: Unauthorized
        "404":
          description: Folder not found
    delete:
      summary: Delete a report folder
      description: |
        Deletes a report folder. Saved reports assigned to it are auto-uncategorized
        (the `folderId` FK is set null). Requires the `optimization` tenant feature.
      operationId: deleteReportFolder
      tags:
        - "Admin: Report Folders"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Folder deleted
          content:
            application/json:
              example:
                success: true
        "401":
          description: Unauthorized
        "404":
          description: Folder not found
  /api/admin/reports/buyer-retention:
    get:
      summary: Buyer retention report
      description: |
        Cohort-style buyer retention over a date range, bucketed weekly or monthly.
        Defaults to the trailing 84 days when no dates are provided. Accepts the standard
        report filter params (see the `buyerId`/`partnerId` etc. parameters). Paginated.
      operationId: getBuyerRetentionReport
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          description: Range start (YYYY-MM-DD, tenant timezone). Defaults to 83 days before today.
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          description: Range end (YYYY-MM-DD, tenant timezone). Defaults to today.
          schema:
            type: string
            format: date
        - name: cadence
          in: query
          description: Retention bucket cadence. Defaults to the tenant's configured retention cadence.
          schema:
            type: string
            enum:
              - weekly
              - monthly
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: buyerId
          in: query
          description: "Report filter: comma-separated buyer UUIDs."
          schema:
            type: string
        - name: partnerId
          in: query
          description: "Report filter: comma-separated partner UUIDs."
          schema:
            type: string
      responses:
        "200":
          description: Retention report with pagination
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "400":
          description: Invalid cadence or filter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/reports/buyer-retention/export:
    get:
      summary: Export buyer retention report (CSV)
      description: |
        CSV export of the buyer retention report. Same parameters and filters as
        `GET /api/admin/reports/buyer-retention`, but returns up to 10,000 rows with
        no pagination. Response is `text/csv` with a UTF-8 BOM for Excel compatibility.
      operationId: exportBuyerRetentionReport
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
        - name: cadence
          in: query
          schema:
            type: string
            enum:
              - weekly
              - monthly
      responses:
        "200":
          description: CSV file (filename `buyer-retention-<cadence>-<start>-to-<end>.csv`)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "400":
          description: Invalid cadence
        "401":
          description: Unauthorized
  /api/admin/reports/calls/drilldown:
    get:
      summary: Call analytics drilldown
      description: |
        Grouped call analytics that drill down one dimension at a time. Groups by the
        dimension at `level` within the ordered `dimensions` list, filtering to the
        parent path via `pf_<dimension>` params. Source is `call LEFT JOIN callSale`.
        Excludes test calls. Requires the `permReports` permission (superadmin, org
        admin, owner, or unscoped admin bypass this check). When `piiAccess` is false,
        caller number and state dimension values are masked.

        Each row carries the Ringba-style metric set: `callCount`, `inProgressCount`
        (calls in a live status — not completed/missed/failed), `uniqueCount`
        (distinct caller numbers), `receivableCount` (sales with totalPrice > 0),
        `receivableDuplicateCount` (sales with creditStatus = duplicate),
        `payableCount` (sales with payout > 0 or cpaPayout > 0), `totalRevenue`,
        `totalPayout`, `totalCost` (call cost summed once per distinct call —
        fanout-safe over the callSale join), `epcTotal` (totalRevenue / callCount),
        and `cpcTotal` (totalPayout / callCount).
      operationId: getCallDrilldown
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          description: |
            Ordered, comma-separated group-by dimensions (1–8). Allowed keys: `campaign`,
            `vertical`, `partner`, `offer`, `contract_id`, `contract_name`, `account_name`,
            `intake_center_id`, `intake_center_name`, `is_transferred`, `state`, `status`,
            `hangup_cause`, `day`, `hour_of_day`, `day_of_week`.
          schema:
            type: string
        - name: level
          in: query
          description: Zero-based index into `dimensions` selecting the dimension to group by.
          schema:
            type: integer
            default: 0
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: timeBasis
          in: query
          description: "`revenue` windows by callSale/CPA event date; `lead` windows by call date."
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: dateField
          in: query
          description: Call timestamp column used for the `lead` time basis window.
          schema:
            type: string
            enum:
              - startedAt
              - createdAt
              - endedAt
              - answeredAt
            default: startedAt
        - name: columns
          in: query
          description: Comma-separated extra dimension keys to aggregate as selected columns (max 8).
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 500
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: campaignId
          in: query
          description: "Report filter: comma-separated campaign UUIDs."
          schema:
            type: string
        - name: partnerId
          in: query
          description: "Report filter: comma-separated partner UUIDs."
          schema:
            type: string
        - name: buyerId
          in: query
          description: "Report filter: comma-separated buyer UUIDs."
          schema:
            type: string
        - name: is_transferred
          in: query
          description: Filter to transferred (`true`) or non-transferred (`false`) calls.
          schema:
            type: string
            enum:
              - "true"
              - "false"
      responses:
        "200":
          description: Grouped drilldown rows with pagination
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - dimensionKey: campaign
                    dimensionValue: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    dimensionLabel: Solar Calls - CA
                    callCount: 120
                    transferredCount: 84
                    cpaEvents: 12
                    transferRate: 70
                    inProgressCount: 3
                    uniqueCount: 110
                    receivableCount: 90
                    receivableDuplicateCount: 4
                    payableCount: 82
                    totalRevenue: 4200
                    totalPayout: 1800
                    totalCost: 240
                    epcTotal: 35
                    cpcTotal: 15
                    grossProfit: 2400
                    revenuePerCall: 35
                    hasChildren: true
                pagination:
                  total: 5
                  limit: 100
                  offset: 0
                  hasMore: false
        "400":
          description: Missing/invalid dimensions, level, date range, or dateField
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: permReports permission required
  /api/admin/reports/contracts/export:
    get:
      summary: Export contract summary report (CSV)
      description: |
        CSV export of the contract summary report. Reuses the same auth, tenant
        filtering, validation, and pivot logic as the JSON contract summary endpoint.
        Response is `text/csv` with a UTF-8 BOM.
      operationId: exportContractSummaryReport
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          schema:
            type: string
            enum:
              - contract
              - buyer
              - vertical
              - offer
              - campaign
              - day
              - week
              - month
            default: contract
        - name: sortBy
          in: query
          schema:
            type: string
            enum:
              - revenue
              - sales
              - avgPrice
              - acceptRate
              - capUsed
              - capUsedDaily
              - capUsedMonthly
              - netRevenue
              - leads
              - cpaEvents
              - cpaRatio
              - dqLeads
              - paidLeads
            default: revenue
        - name: sortDir
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: timeAxis
          in: query
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: buyerId
          in: query
          schema:
            type: string
        - name: partnerId
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
        - name: offerId
          in: query
          schema:
            type: string
        - name: contractId
          in: query
          schema:
            type: string
        - name: contractStatus
          in: query
          schema:
            type: string
        - name: packLeftOnly
          in: query
          schema:
            type: string
            enum:
              - "0"
              - "1"
      responses:
        "200":
          description: CSV file
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "400":
          description: Invalid parameters (e.g. range exceeds max days, endDate before startDate)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/reports/custom/sold-leads/export:
    get:
      summary: Export custom sold-leads report (CSV)
      description: |
        Streams a CSV of sold-lead rows for the custom report, batched at 5,000 rows.
        Requires the `permReports` permission. Rows are `leadSale INNER JOIN lead`,
        excluding test/trashed leads. The `status` filter defaults to `sold`; other
        statuses (e.g. `rejected`) are included only when the row earned revenue or a
        payout in range. Selected columns are appended via `columns`. When `piiAccess`
        is false, PII columns are masked. Response is `text/csv` with a UTF-8 BOM.
      operationId: exportCustomSoldLeadsReport
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: timeBasis
          in: query
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: status
          in: query
          description: Comma-separated lead statuses (max 20). Defaults to `sold`.
          schema:
            type: string
            default: sold
        - name: columns
          in: query
          description: Comma-separated selected column keys to append (max 8).
          schema:
            type: string
        - name: buyerId
          in: query
          description: "Filter: comma-separated buyer UUIDs (max 2000 values)."
          schema:
            type: string
        - name: partnerId
          in: query
          description: "Filter: comma-separated partner UUIDs."
          schema:
            type: string
        - name: campaignId
          in: query
          description: "Filter: comma-separated campaign UUIDs."
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
        - name: offerId
          in: query
          schema:
            type: string
        - name: contractId
          in: query
          schema:
            type: string
        - name: state
          in: query
          description: "Filter: comma-separated state codes. Also accepts city, zipCode, and sub1–sub10."
          schema:
            type: string
      responses:
        "200":
          description: Streamed CSV file
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "400":
          description: Missing/invalid date range or too many filter values
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: permReports permission required
  /api/admin/reports/filter-options:
    get:
      summary: Report filter options
      description: |
        Returns selectable options for a single report filter dimension. Handles three
        dimension kinds: entity reference lists (tenant-scoped), static hardcoded lists
        (no DB hit), and distinct lead-column values (tenant-scoped, capped at 500 rows).
      operationId: getReportFilterOptions
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: dimension
          in: query
          required: true
          description: |
            The dimension to list options for. Entity: `buyer`, `partner`, `campaign`,
            `vertical`, `offer`, `contract`. Static: `revenueType`, `leadType`, `status`.
            Distinct lead columns: `state`, `city`, `sub1`–`sub10`, `lpUrl`.
          schema:
            type: string
      responses:
        "200":
          description: Options as `{ id, label }` pairs with pagination
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    label: Acme Insurance Corp
                pagination:
                  total: 1
                  limit: 1
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid or missing dimension, or tenant context required for distinct dims
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/reports/list/rows:
    get:
      summary: Custom report flat list
      description: |
        The custom report drilldown fully expanded — groups by ALL selected `dimensions`
        at once in a single query so numbers reconcile with the tree drilldown. Requires
        the `permReports` permission. Returns rows plus a `totals` block. `sale_id` is
        only valid when `status=sold`. When `piiAccess` is false, PII dimension values
        are masked.
      operationId: getCustomReportListRows
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          description: Ordered comma-separated group-by dimension keys (1–8).
          schema:
            type: string
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: timeBasis
          in: query
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: columns
          in: query
          description: Comma-separated selected column keys to include.
          schema:
            type: string
        - name: status
          in: query
          description: Lead status filter. `sold` is required to use the `sale_id` dimension.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 1000
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: sortBy
          in: query
          description: Metric or dimension key to sort by (validated server-side).
          schema:
            type: string
        - name: sortDir
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
      responses:
        "200":
          description: Flat list rows with pagination and totals
          content:
            application/json:
              example:
                data:
                  - dimensions:
                      - key: partner
                        value: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                        label: LeadGen Partners
                        ref: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    leadCount: 500
                    soldCount: 320
                    cpaEvents: 40
                    totalRevenue: 4800
                    grossProfit: 2100
                pagination:
                  total: 12
                  limit: 100
                  offset: 0
                  hasMore: false
                totals:
                  leadCount: 5000
                  soldCount: 3200
                  grossProfit: 21000
        "400":
          description: Missing/invalid dimensions, date range, or `sale_id` used without `status=sold`
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: permReports permission required
  /api/admin/reports/list/export:
    get:
      summary: Export custom report flat list (CSV)
      description: |
        Streams a CSV of the custom report flat list, batched at 2,000 rows, with a
        trailing TOTAL row. Columns are the selected dimensions, visible field columns,
        and visible metric columns (controlled by `cols`). Requires the `permReports`
        permission. Response is `text/csv` with a UTF-8 BOM.
      operationId: exportCustomReportList
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: dimensions
          in: query
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: timeBasis
          in: query
          schema:
            type: string
            enum:
              - revenue
              - lead
            default: revenue
        - name: source
          in: query
          description: Report source, used to resolve metric column labels.
          schema:
            type: string
            enum:
              - leads
              - sold
              - calls
            default: leads
        - name: columns
          in: query
          description: Comma-separated selected field column keys.
          schema:
            type: string
        - name: cols
          in: query
          description: Comma-separated metric column keys to include. Defaults to the non-hidden metric set.
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Streamed CSV file (filename `custom-list-<start>-to-<end>.csv`)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "400":
          description: Missing/invalid dimensions or date range
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: permReports permission required
  /api/admin/reports/revenue/day:
    get:
      summary: Revenue report day detail
      description: |
        Detail rows contributing to a single day on the Time of Revenue report:
        credited lead sales, credited call sales, and lead/call CPA conversions with
        their event date on that day. Applies the same report filters as the revenue
        page. Each section is capped at 500 rows. Date boundaries use the tenant timezone.
      operationId: getRevenueDayDetail
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: date
          in: query
          required: true
          description: The day to expand (YYYY-MM-DD, tenant timezone).
          schema:
            type: string
            format: date
        - name: buyerId
          in: query
          schema:
            type: string
        - name: partnerId
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
        - name: offerId
          in: query
          schema:
            type: string
        - name: contractId
          in: query
          schema:
            type: string
        - name: leadType
          in: query
          description: Query-branch toggle. Invalid values fall back to both.
          schema:
            type: string
        - name: revenueType
          in: query
          description: Query-branch toggle (CPL vs CPA). Invalid values fall back to both.
          schema:
            type: string
      responses:
        "200":
          description: Day detail sections
          content:
            application/json:
              example:
                data:
                  leadSales:
                    total: 12
                    limit: 500
                    truncated: false
                    rows:
                      - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                        leadId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                        leadShortId: LD-10231
                        leadCreatedAt: 2026-01-15T10:00:00.000Z
                        buyerName: Acme Insurance
                        contractName: Auto - CA
                        partnerName: LeadGen Partners
                        price: "12.50"
                        miscCost: "0"
                  callSales:
                    total: 3
                    limit: 500
                    truncated: false
                    rows: []
                  cpaConversions:
                    total: 2
                    limit: 1000
                    truncated: false
                    rows: []
        "400":
          description: Missing/invalid date or filter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/reports/revenue/export:
    get:
      summary: Export revenue line items (CSV)
      description: |
        Streams a CSV of every line item contributing to the Time of Revenue report for
        the date range and filters: lead sales, call sales, lead CPA conversions, and
        call CPA conversions (batched at 5,000, no per-section cap). Requires a
        user-scoped session. Sorted by event date descending. Response is `text/csv`
        with a UTF-8 BOM.
      operationId: exportRevenueLineItems
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: buyerId
          in: query
          schema:
            type: string
        - name: partnerId
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
        - name: offerId
          in: query
          schema:
            type: string
        - name: contractId
          in: query
          schema:
            type: string
        - name: leadType
          in: query
          schema:
            type: string
        - name: revenueType
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Streamed CSV file (filename `time-of-revenue-line-items-<start>-to-<end>.csv`)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "400":
          description: Missing/invalid date range or filter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: User-scoped session required
  /api/admin/reports/revenue/pivot:
    get:
      summary: Revenue report pivot detail
      description: |
        Detail rows for a single pivot dimension value within a date range. Same response
        shape as the day detail endpoint. Sub-dimension pivots (`sub1`–`sub3`) return lead
        rows only (calls have no sub fields). Each section is capped at 500 rows.
      operationId: getRevenuePivotDetail
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: pivot
          in: query
          required: true
          description: Pivot dimension.
          schema:
            type: string
            enum:
              - buyer
              - contract
              - partner
              - campaign
              - sub1
              - sub2
              - sub3
        - name: key
          in: query
          required: true
          description: The pivot value to expand (entity name, or sub-field value; `(empty)` for blank sub values).
          schema:
            type: string
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: mode
          in: query
          description: "`revenue` windows by sale/CPA event date; `time-of-lead` windows by lead/call creation date."
          schema:
            type: string
            enum:
              - revenue
              - time-of-lead
            default: revenue
        - name: buyerId
          in: query
          schema:
            type: string
        - name: partnerId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Pivot detail sections (same shape as revenue day detail)
          content:
            application/json:
              example:
                data:
                  leadSales:
                    total: 8
                    limit: 500
                    truncated: false
                    rows: []
                  callSales:
                    total: 0
                    limit: 500
                    truncated: false
                    rows: []
                  cpaConversions:
                    total: 1
                    limit: 1000
                    truncated: false
                    rows: []
        "400":
          description: Missing/invalid pivot, key, date range, mode, or filter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
  /api/admin/saved-reports/default:
    get:
      summary: Get default saved report
      description: |
        Returns the current user's default saved report for a report type, or `null` if
        none is set or the stored report is no longer visible (stale defaults are cleaned
        up). Requires a user-scoped session and the `optimization` tenant feature.
      operationId: getDefaultSavedReport
      tags:
        - "Admin: Saved Reports"
      security:
        - sessionCookie: []
      parameters:
        - name: reportType
          in: query
          required: true
          description: Report type key the default applies to.
          schema:
            type: string
      responses:
        "200":
          description: The default saved report, or null
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: My Weekly View
                  reportType: revenue
                  isShared: false
        "400":
          description: reportType is required
        "401":
          description: Unauthorized
        "403":
          description: User-scoped session required, or optimization feature not enabled
    put:
      summary: Set default saved report
      description: |
        Sets the current user's default saved report for a report type. The report must
        be visible to the user (shared, or owned). Requires a user-scoped session and the
        `optimization` tenant feature.
      operationId: setDefaultSavedReport
      tags:
        - "Admin: Saved Reports"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reportType
                - savedReportId
              properties:
                reportType:
                  type: string
                  minLength: 1
                savedReportId:
                  type: string
                  format: uuid
            example:
              reportType: revenue
              savedReportId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "200":
          description: Default set; returns the saved report
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: User-scoped session required, or optimization feature not enabled
        "404":
          description: Saved report not found or not visible to the user
    delete:
      summary: Clear default saved report
      description: |
        Removes the current user's default saved report for a report type. Requires a
        user-scoped session and the `optimization` tenant feature.
      operationId: clearDefaultSavedReport
      tags:
        - "Admin: Saved Reports"
      security:
        - sessionCookie: []
      parameters:
        - name: reportType
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Default cleared
          content:
            application/json:
              example:
                success: true
        "400":
          description: reportType is required
        "401":
          description: Unauthorized
        "403":
          description: User-scoped session required, or optimization feature not enabled
  /api/admin/saved-reports/{id}/publish:
    post:
      summary: Publish a saved report
      description: |
        Marks a saved report as shared (published), optionally moving it into a folder.
        Requires a user-scoped session and the `optimization` tenant feature. General
        edit rights are enforced; publishing a private draft is owner-only. A provided
        `folderId` must belong to the same tenant.
      operationId: publishSavedReport
      tags:
        - "Admin: Saved Reports"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                folderId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Folder to file the report under. Omit to leave folder unchanged; null to uncategorize.
            example:
              folderId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "200":
          description: Report published; returns the updated saved report
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: User-scoped session required, insufficient edit rights, or owner-only publish violation
        "404":
          description: Saved report not found
  /api/admin/scoring-models:
    get:
      operationId: listScoringModels
      tags:
        - "Admin: Scoring Models"
      summary: List scoring models
      description: Returns the tenant's scoring models (newest first), paginated.
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated scoring model list
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ScoringModelDetail"
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createScoringModel
      tags:
        - "Admin: Scoring Models"
      summary: Create scoring model
      description: Creates a scoring model together with its version 1 draft. The `config` is validated against the scoring
        config schema (factor roles, gate-cap placement, non-overlapping routing bands). Promote the draft to make it
        live.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - config
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                config:
                  $ref: "#/components/schemas/ScoringConfig"
      responses:
        "201":
          description: Scoring model created (with version 1 draft)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/admin/scoring-models/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getScoringModel
      tags:
        - "Admin: Scoring Models"
      summary: Get scoring model
      description: Returns a scoring model with its version history and resolved live + draft versions.
      responses:
        "200":
          description: Scoring model detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateScoringModel
      tags:
        - "Admin: Scoring Models"
      summary: Update scoring model
      description: "Updates a model's name/description/status. Sending `config` upserts the DRAFT version: if the latest
        version is a draft it is edited in place, otherwise a new draft version is opened above the current live/retired
        version. Does not promote — use the promote endpoint to go live."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - archived
                config:
                  $ref: "#/components/schemas/ScoringConfig"
      responses:
        "200":
          description: Scoring model updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: patchScoringModel
      tags:
        - "Admin: Scoring Models"
      summary: Patch scoring model (with concurrent-edit guard)
      description: Same update semantics as PUT, plus the Wave-1 `_snapshot` field-conflict guard. Send `_snapshot` = the
        field values loaded at page open; the route returns `409` with code `CONFLICT` when a submitted scalar's current
        DB value differs from its snapshot, or when a `config` diff carries a stale draft `versionNumber` in `_snapshot`
        (the draft moved on since load).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - archived
                config:
                  $ref: "#/components/schemas/ScoringConfig"
                _snapshot:
                  type: object
                  description: Field values as loaded at page open. Compared per-key against the current DB row for submitted scalar
                    fields; may carry `versionNumber` to guard a `config` diff against a superseded draft.
                  additionalProperties: true
      responses:
        "200":
          description: Scoring model updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A submitted field (or the edited draft) changed since page load
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: CONFLICT
                  message: "Changed by someone else since you loaded this page: name. Reload to get the latest values."
                  details:
                    conflictFields:
                      - name
      security:
        - sessionCookie: []
    delete:
      operationId: archiveScoringModel
      tags:
        - "Admin: Scoring Models"
      summary: Archive scoring model
      description: Soft-archives the model (`status = archived`). Returns `409` with code `VALIDATION_ERROR` when the model is
        still attached to any vertical or offer — detach it first.
      responses:
        "200":
          description: Scoring model archived
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Model is attached to a vertical or offer and cannot be archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: VALIDATION_ERROR
                  message: Scoring model is attached to a vertical or offer and cannot be archived
                  details:
                    verticals: 1
                    offers: 0
      security:
        - sessionCookie: []
  /api/admin/scoring-models/{id}/promote:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: promoteScoringModelVersion
      tags:
        - "Admin: Scoring Models"
      summary: Promote a version to live
      description: Promotes a draft version to `live` (retiring any prior live version) and points the model's `liveVersionId`
        at it. `versionId` may be a concrete version UUID (must belong to this model) or a non-UUID sentinel to promote
        the model's latest draft. Audit-logged.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - versionId
              properties:
                versionId:
                  type: string
                  description: A version UUID belonging to this model, or a non-UUID sentinel to promote the latest draft.
      responses:
        "200":
          description: Version promoted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Scoring model or version not found (or version not in this model)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/admin/scoring-models/{id}/versions:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: limit
        in: query
        schema:
          type: integer
          default: 20
      - name: offset
        in: query
        schema:
          type: integer
          default: 0
    get:
      operationId: listScoringModelVersions
      tags:
        - "Admin: Scoring Models"
      summary: List scoring model versions
      description: Returns a model's version history (newest versionNumber first), paginated.
      responses:
        "200":
          description: Paginated version list
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ScoringModelVersion"
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/admin/scoring-models/{id}/validate:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: validateScoringModelForVertical
      tags:
        - "Admin: Scoring Models"
      summary: Validate a model against a vertical
      description: "Dry-run: validates the model's live (or latest draft) config against a vertical's intake fields and
        returns blocking `errors` plus non-blocking `warnings`. This does NOT attach the model — it is a pre-attach
        lint. A dangling intake field (a vertical field no factor maps and not listed in `collectedNotScored`) is a
        blocking error; a factor referencing a field the vertical does not collect is demoted to a warning here."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - verticalId
              properties:
                verticalId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      errors:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                            field:
                              type: string
                              nullable: true
                            message:
                              type: string
                      warnings:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                            field:
                              type: string
                              nullable: true
                            message:
                              type: string
              example:
                data:
                  errors:
                    - code: DANGLING_INTAKE_FIELD
                      field: householdSize
                      message: Vertical intake field 'householdSize' is neither scored by a factor nor listed in collectedNotScored
                  warnings:
                    - code: UNKNOWN_FIELD
                      field: creditBand
                      message: Factor field 'creditBand' is not collected by this vertical; it will always be missing at score time
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Scoring model or vertical not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/admin/scoring-models/{id}/export:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: exportScoringModelXlsx
      tags:
        - "Admin: Scoring Models"
      summary: Export a model config as XLSX
      description: "Streams the model config as an .xlsx workbook (three sheets: Lookups, Config, Routing Bands). Without
        `versionId` the live version is exported, or the latest draft when no version is live. The workbook round-trips
        through the import endpoint."
      parameters:
        - name: versionId
          in: query
          required: false
          description: Export a specific version instead of the default (live, else latest draft).
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: XLSX workbook download
          headers:
            Content-Disposition:
              description: attachment; filename="<model>-v<n>.xlsx"
              schema:
                type: string
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/admin/scoring-models/{id}/import:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: importScoringModelXlsx
      tags:
        - "Admin: Scoring Models"
      summary: Import an XLSX config into a NEW draft
      description: Parses an uploaded .xlsx workbook (matching the export layout) into a config, validates it, and writes it
        as a NEW draft version via the shared config path. Never mutates the live version. A structurally-invalid
        workbook returns `400` with code `VALIDATION_ERROR` and row-addressed `details` (the offending sheet + row).
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: The .xlsx workbook to import.
      responses:
        "201":
          description: New draft version created from the imported workbook
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ScoringModelDetail"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/admin/skus:
    get:
      summary: List SKUs
      description: |
        Returns a paginated list of SKUs (purchasable lead/call packages) for the tenant, enriched
        with the linked wizard name, offer name, product type, and computed unit labels. Superadmin
        sees all tenants' SKUs.
      operationId: listSkus
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      parameters:
        - name: active
          in: query
          description: Filter by active flag.
          schema:
            type: string
            enum:
              - "true"
              - "false"
        - name: isSubscription
          in: query
          description: Filter by subscription flag.
          schema:
            type: string
            enum:
              - "true"
              - "false"
        - name: productId
          in: query
          description: Filter by linked product UUID.
          schema:
            type: string
            format: uuid
        - name: productType
          in: query
          description: Filter by product type. `lead` includes SKUs with no linked product.
          schema:
            type: string
            enum:
              - lead
              - call
        - name: verticalId
          in: query
          description: Filter by SKU vertical UUID.
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          description: Case-insensitive partial match on SKU name.
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of SKUs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    slug: solar-100
                    name: Solar Starter — 100 Leads
                    priceCents: 50000
                    leadCount: 100
                    bonusLeads: 10
                    isSubscription: false
                    billingInterval: null
                    active: true
                    verticalId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    wizardName: Solar Onboarding
                    offerName: Solar - National
                    productType: lead
                    unitType: lead
                    unitLabel: lead
                    unitLabelPlural: leads
                    unitCount: 110
                    unitsTotal: 110
                pagination:
                  total: 12
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
    post:
      summary: Create a SKU
      description: |
        Creates a SKU. Enforces a per-tenant unique slug, tenant-scope guards on `wizardId`/`offerId`/`productId`,
        and vertical-match between the SKU and its linked offer/product. Active SKUs require a `verticalId`;
        drafts (`status: draft` or `active: false`) may omit it. All monetary fields are integer cents.
      operationId: createSku
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - slug
                - name
                - priceCents
                - leadCount
              properties:
                slug:
                  type: string
                  minLength: 1
                  maxLength: 64
                  pattern: ^[a-z0-9-]+$
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                tagline:
                  type: string
                  maxLength: 512
                  nullable: true
                badge:
                  type: string
                  maxLength: 64
                  nullable: true
                priceCents:
                  type: integer
                  minimum: 0
                  description: Package price in integer cents.
                leadCount:
                  type: integer
                  minimum: 0
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Required for active SKUs; drafts may omit.
                wizardId:
                  type: string
                  format: uuid
                  nullable: true
                offerId:
                  type: string
                  format: uuid
                  nullable: true
                productId:
                  type: string
                  format: uuid
                  nullable: true
                isSubscription:
                  type: boolean
                  default: false
                billingInterval:
                  type: string
                  nullable: true
                  description: Required when `isSubscription` is true.
                availableUntil:
                  type: string
                  format: date
                  nullable: true
                bonusLeads:
                  type: integer
                  minimum: 0
                  nullable: true
                allowPromoCodes:
                  type: boolean
                  default: false
                trialLeads:
                  type: integer
                  minimum: 1
                  nullable: true
                  description: Subscription SKUs only.
                trialDays:
                  type: integer
                  minimum: 1
                  maximum: 90
                  nullable: true
                  description: Subscription SKUs only.
                active:
                  type: boolean
                  default: true
                showInBuyerPortal:
                  type: boolean
                  default: false
                firstTimeBuyerOnly:
                  type: boolean
                  default: false
                status:
                  type: string
                  enum:
                    - active
                    - draft
                  description: Soft lifecycle hint (no DB column). Drafts may omit verticalId.
                sortOrder:
                  type: integer
                  default: 0
            example:
              slug: solar-100
              name: Solar Starter — 100 Leads
              priceCents: 50000
              leadCount: 100
              verticalId: b2c3d4e5-f6a7-8901-bcde-f12345678901
              isSubscription: false
              bonusLeads: 10
              active: true
      responses:
        "201":
          description: SKU created
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  slug: solar-100
                  name: Solar Starter — 100 Leads
                  priceCents: 50000
                  leadCount: 100
                  active: true
        "400":
          description: Validation error (bad body, wizard/offer/product not in tenant, vertical mismatch, incompatible SKU/product
            type)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or SKU feature disabled for this tenant (`FEATURE_DISABLED`)
        "409":
          description: A SKU with this slug already exists for the tenant (`DUPLICATE`)
  /api/admin/skus/{id}:
    get:
      summary: Get a SKU
      description: Returns a single SKU by UUID (tenant-scoped).
      operationId: getSku
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: SKU record
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  slug: solar-100
                  name: Solar Starter — 100 Leads
                  priceCents: 50000
                  leadCount: 100
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: SKU not found
    put:
      summary: Update a SKU
      description: |
        Partial update. Several fields are immutable once a non-refunded/non-cancelled buyerSku has been
        issued from this SKU: `priceCents` is frozen, `verticalId` cannot change, and the derived product
        type cannot change (returns `IMMUTABLE_FIELD`). If the SKU has a linked Stripe price it is reconciled
        to Stripe on every save (a shape change recreates the Stripe Price and re-swaps `stripePriceId`); a
        Stripe sync failure rolls back the DB update and returns `STRIPE_ERROR` (400). All monetary fields
        are integer cents.
      operationId: updateSku
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        description: Partial SKU update — all fields optional. Same field set as create (minus `status`).
        content:
          application/json:
            schema:
              type: object
              properties:
                slug:
                  type: string
                  minLength: 1
                  maxLength: 64
                  pattern: ^[a-z0-9-]+$
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                tagline:
                  type: string
                  maxLength: 512
                  nullable: true
                badge:
                  type: string
                  maxLength: 64
                  nullable: true
                priceCents:
                  type: integer
                  minimum: 0
                leadCount:
                  type: integer
                  minimum: 0
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                wizardId:
                  type: string
                  format: uuid
                  nullable: true
                offerId:
                  type: string
                  format: uuid
                  nullable: true
                productId:
                  type: string
                  format: uuid
                  nullable: true
                isSubscription:
                  type: boolean
                billingInterval:
                  type: string
                  nullable: true
                availableUntil:
                  type: string
                  format: date
                  nullable: true
                bonusLeads:
                  type: integer
                  minimum: 0
                  nullable: true
                allowPromoCodes:
                  type: boolean
                trialLeads:
                  type: integer
                  minimum: 1
                  nullable: true
                trialDays:
                  type: integer
                  minimum: 1
                  maximum: 90
                  nullable: true
                active:
                  type: boolean
                showInBuyerPortal:
                  type: boolean
                firstTimeBuyerOnly:
                  type: boolean
                sortOrder:
                  type: integer
            example:
              name: Solar Starter — 100 Leads (v2)
              priceCents: 55000
      responses:
        "200":
          description: Updated SKU
        "400":
          description: Validation error, immutable field, incompatible type, or Stripe price sync failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or SKU feature disabled (`FEATURE_DISABLED`)
        "404":
          description: SKU not found
    delete:
      summary: Delete a SKU
      description: |
        Hard-deletes a SKU. Blocked with a `400` when any `active` buyerSku references it —
        set `active: false` instead to retire it from sale.
      operationId: deleteSku
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: SKU deleted
          content:
            application/json:
              example:
                data:
                  success: true
        "400":
          description: SKU has active buyer SKUs and cannot be deleted
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or SKU feature disabled (`FEATURE_DISABLED`)
        "404":
          description: SKU not found
  /api/admin/skus/{id}/sync-stripe:
    post:
      summary: Force-sync a SKU to Stripe
      description: |
        Re-creates the SKU's Stripe Price (forcing a fresh price object) and pushes current Product
        metadata (name + description) to Stripe, then stores the resulting `stripePriceId`/`stripeProductId`.
        Requires the tenant to have a connected Stripe account. No request body.
      operationId: syncSkuStripe
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Updated SKU with fresh Stripe identifiers
          content:
            application/json:
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  stripePriceId: price_1Q...
                  stripeProductId: prod_Q...
        "400":
          description: Tenant has not connected a Stripe account
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, SKU feature disabled, or missing tenant context
        "404":
          description: SKU not found
  /api/admin/skus/{id}/share:
    post:
      summary: Share a SKU link via email or SMS
      description: |
        Builds a shareable deep link to this SKU — either the buyer-portal link
        (`/buyer/billing?sku=<slug>`) or the public checkout link
        (`/checkout/<slug>?tenant=<slug>`) — optionally with a promotion code, and
        sends it to a recipient by email or SMS via the SYSTEM (platform-default)
        email/SMS providers. The coupon (when supplied) is validated against this
        SKU exactly as checkout would validate it. The recipient is stored MASKED
        in the audit log (`sku.share_sent`); no raw PII is persisted. Requires the
        `sku` tenant feature.
      operationId: shareSku
      tags:
        - "Admin: SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - channel
                - recipient
              properties:
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                recipient:
                  type: string
                  description: Email address (channel=email) or E.164 phone (channel=sms).
                linkType:
                  type: string
                  enum:
                    - portal
                    - checkout
                  default: portal
                couponCode:
                  type: string
                  nullable: true
                  description: Optional promotion code to attach; validated against this SKU.
                message:
                  type: string
                  nullable: true
                  maxLength: 500
                  description: Optional personal note included with the link.
            example:
              channel: email
              recipient: prospect@example.com
              linkType: portal
              couponCode: SAVE20
              message: Here's the deal we discussed
      responses:
        "200":
          description: Link sent
          content:
            application/json:
              example:
                data:
                  sent: true
                  channel: email
                  link: https://theleadrouter.com/buyer/billing?sku=solar-100&coupon=SAVE20
        "400":
          description: Invalid body, bad recipient, invalid/expired/inapplicable coupon, or the system provider for the channel is
            not configured (`VALIDATION_ERROR`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required or SKU feature disabled
        "404":
          description: SKU not found in this tenant
  /api/admin/subscriptions:
    get:
      summary: List subscription buyer SKUs
      description: |
        Returns a paginated list of buyerSku rows that have a Stripe subscription
        (`stripeSubscriptionId IS NOT NULL`), enriched with buyer/SKU/vertical names, pause + renewal
        status, and a `totalBilledCents` aggregate per subscription. Canceled subscriptions are excluded
        unless `pauseStatus=canceled` is passed. Requires the `finance` tenant feature. Monetary fields
        are integer cents.
      operationId: listSubscriptions
      tags:
        - "Admin: Subscriptions"
      security:
        - sessionCookie: []
      parameters:
        - name: buyerId
          in: query
          description: Filter by buyer UUID.
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          description: Filter by buyerSku status (e.g. `active`, `exhausted`, `refunded`).
          schema:
            type: string
        - name: pauseStatus
          in: query
          description: Filter by subscription pause status (`active`, `paused`, `canceled`). Omit to exclude canceled.
          schema:
            type: string
        - name: renewalStatus
          in: query
          description: Filter by renewal payment status.
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of subscription buyer SKUs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: c3d4e5f6-a7b8-9012-cdef-123456789012
                    buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    buyerName: Acme Insurance Corp
                    skuName: Solar Monthly — 500 Leads
                    stripeSubscriptionId: sub_1Q...
                    status: active
                    subscriptionPauseStatus: active
                    renewalPaymentStatus: paid
                    billingInterval: month
                    unitPriceCents: 500
                    purchasedPriceCents: 250000
                    leadsTotal: 500
                    leadsRemaining: 342
                    totalBilledCents: 500000
                    productType: lead
                    unitType: lead
                pagination:
                  total: 3
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or `finance` feature not enabled
  /api/admin/buyer-skus/{id}:
    get:
      summary: Get a buyer SKU
      description: Returns a single buyerSku (purchase receipt) by UUID, tenant-scoped.
      operationId: getBuyerSku
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Buyer SKU record
          content:
            application/json:
              example:
                data:
                  id: c3d4e5f6-a7b8-9012-cdef-123456789012
                  buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  skuId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  status: active
                  leadsTotal: 110
                  leadsRemaining: 42
                  purchasedPriceCents: 50000
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU not found
    patch:
      summary: Update a buyer SKU
      description: |
        A buyerSku is largely an immutable receipt — only `leadsRemaining` and `status` may be patched.
        `verticalId` and `skuId` are rejected (use Reissue instead). Status changes are gated by the
        buyerSku state machine, so terminal states (refunded/cancelled/expired) cannot be re-opened.
        Raising `leadsRemaining` above the current value records a `manual_credit` funding row and bumps
        `leadsTotal` by the delta.
      operationId: updateBuyerSku
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                leadsRemaining:
                  type: integer
                  minimum: 0
                status:
                  type: string
                  enum:
                    - active
                    - exhausted
                    - expired
                    - cancelled
                    - refunded
                    - disputed
                    - reissued
            example:
              leadsRemaining: 50
      responses:
        "200":
          description: Updated buyer SKU
        "400":
          description: Invalid body, immutable field (`verticalId`/`skuId`), or illegal status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required, or SKU feature disabled (`FEATURE_DISABLED`)
        "404":
          description: Buyer SKU not found
  /api/admin/buyer-skus/{id}/manual-credit:
    post:
      summary: Add free lead credits to a buyer SKU
      description: |
        Adds `leads` free lead credits (unit price 0) to an `active` or `exhausted` buyerSku, records a
        `manual_credit` funding event, and restores an exhausted SKU to `active`/`routeable`. Rejected for
        SKUs in any other status.
      operationId: buyerSkuManualCredit
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - leads
              properties:
                leads:
                  type: integer
                  minimum: 1
                  maximum: 100000
                reason:
                  type: string
                  maxLength: 500
            example:
              leads: 25
              reason: Goodwill credit for delivery outage
      responses:
        "200":
          description: Buyer SKU credited
          content:
            application/json:
              example:
                data:
                  buyerSku:
                    id: c3d4e5f6-a7b8-9012-cdef-123456789012
                    leadsTotal: 135
                    leadsRemaining: 67
                    status: active
                  fundingEvent:
                    id: d4e5f6a7-b8c9-0123-def4-234567890123
                    eventType: manual_credit
                    leadDelta: 25
        "400":
          description: Invalid body, or SKU status is not creditable (only `active`/`exhausted`)
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU not found
  /api/admin/buyer-skus/{id}/refund:
    post:
      summary: Refund a buyer SKU to Stripe
      description: |
        Issues a Stripe refund against the buyerSku's payment intent (subscription SKUs target the latest
        paid renewal invoice). `mode: full` refunds the whole charge; `mode: partial` prorates a refund for
        the currently-unused entitlement (`leadsRemaining`). Validates the transition to `refunded` before
        calling Stripe. Optionally cancels the linked Stripe subscription (`cancelSubscription`, default
        true). The buyerSku entitlement mutation happens asynchronously via the `charge.refunded` webhook.
        Amounts are integer cents.
      operationId: refundBuyerSku
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mode
              properties:
                mode:
                  type: string
                  enum:
                    - full
                    - partial
                reason:
                  type: string
                  maxLength: 500
                cancelSubscription:
                  type: boolean
                  default: true
            example:
              mode: partial
              reason: Buyer downsizing
              cancelSubscription: true
      responses:
        "200":
          description: Refund created
          content:
            application/json:
              example:
                data:
                  buyerSku:
                    id: c3d4e5f6-a7b8-9012-cdef-123456789012
                    subscriptionPauseStatus: canceled
                  refundId: re_1Q...
                  subscriptionCanceled: true
                  subscriptionCancelError: null
        "400":
          description: Invalid body, no payment intent/subscription to refund, illegal transition, or tenant has no Stripe account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU or SKU not found
  /api/admin/buyer-skus/{id}/refund-to-credit:
    post:
      summary: Convert remaining buyer SKU leads to account credit
      description: >
        Converts the buyerSku's `leadsRemaining` into buyer account balance instead of a Stripe refund.

        Credits `leadsRemaining * unitPriceCents` to `buyer.balance`, records a `refund` ledger transaction

        (a decimal amount in the tenant currency) and a `refund_reversal` funding row, marks the buyerSku `refunded`,
        and pauses any

        active contracts spawned from it. Blocked when the SKU has open lead reservations. Optionally cancels

        the linked Stripe subscription (`cancelSubscription`, default true). `unitPriceCents` may be supplied

        to override the resolved per-lead price (integer cents).
      operationId: refundBuyerSkuToCredit
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                unitPriceCents:
                  type: integer
                  minimum: 1
                  maximum: 10000000
                reason:
                  type: string
                  maxLength: 500
                cancelSubscription:
                  type: boolean
                  default: true
            example:
              reason: Buyer requested account credit
      responses:
        "200":
          description: Remaining leads converted to buyer credit
          content:
            application/json:
              example:
                data:
                  buyerSku:
                    id: c3d4e5f6-a7b8-9012-cdef-123456789012
                    status: refunded
                  amountCents: 21000
                  creditLeads: 42
                  unitPriceCents: 500
                  buyerBalance: "210.00"
                  pausedContractIds:
                    - e5f6a7b8-c9d0-1234-ef56-345678901234
                  subscriptionCanceled: true
                  subscriptionCancelError: null
        "400":
          description: Invalid body, no leads remaining, missing unit price, open reservations, or invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU or buyer not found
        "409":
          description: Buyer SKU inventory changed during the operation — reload and retry (`CONFLICT`)
  /api/admin/buyer-skus/{id}/reissue:
    post:
      summary: Reissue a buyer SKU to a different SKU
      description: |
        Retires the source buyerSku and issues a replacement bound to `targetSkuId`, spawning a new contract.
        Use this instead of mutating the immutable `verticalId`/`skuId`. When `carryConsumed` is true, the
        consumed-lead count carries over to the new receipt.
      operationId: reissueBuyerSku
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - targetSkuId
                - carryConsumed
              properties:
                targetSkuId:
                  type: string
                  format: uuid
                carryConsumed:
                  type: boolean
            example:
              targetSkuId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              carryConsumed: false
      responses:
        "200":
          description: Buyer SKU reissued
          content:
            application/json:
              example:
                data:
                  newBuyerSku:
                    id: f6a7b8c9-d0e1-2345-f678-456789012345
                  newContract:
                    id: e5f6a7b8-c9d0-1234-ef56-345678901234
        "400":
          description: Invalid body, missing tenant context, or reissue validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
  /api/admin/buyer-skus/{id}/pause-subscription:
    post:
      summary: Pause a buyer SKU's Stripe subscription
      description: |
        Pauses the buyerSku's Stripe subscription and voids any already-open invoices (pausing only stops
        future invoices; Stripe dunning keeps retrying open ones). When `alsoPauseDelivery` is true (default),
        the linked buyer contracts are also flipped to `paused`. Requires the tenant to have Stripe Connect
        charges enabled. Idempotent: already-paused subscriptions return `409`.
      operationId: pauseBuyerSkuSubscription
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                alsoPauseDelivery:
                  type: boolean
                  default: true
            example:
              alsoPauseDelivery: true
      responses:
        "200":
          description: Subscription paused
          content:
            application/json:
              example:
                data:
                  buyerSkuId: c3d4e5f6-a7b8-9012-cdef-123456789012
                  subscriptionPauseStatus: paused
                  deliveryPaused: true
                  contractsPaused: 1
        "400":
          description: Invalid body, or tenant Stripe Connect not ready (`CONNECT_NOT_READY`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU, tenant, or subscription not found in Stripe
        "409":
          description: Subscription already paused, or cannot pause from current state (`DUPLICATE`)
        "422":
          description: Buyer SKU has no recurring subscription, or Stripe rejected the pause (`UNPROCESSABLE`)
  /api/admin/buyer-skus/{id}/resume-subscription:
    post:
      summary: Resume a buyer SKU's Stripe subscription
      description: |
        Resumes a paused Stripe subscription and clears `subscriptionPauseStatus` back to `active`. Delivery
        is NOT auto-resumed (`deliveryAutoResumed` is always false). Requires Stripe Connect charges enabled.
        Canceled subscriptions cannot be resumed. No request body.
      operationId: resumeBuyerSkuSubscription
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Subscription resumed
          content:
            application/json:
              example:
                data:
                  buyerSkuId: c3d4e5f6-a7b8-9012-cdef-123456789012
                  subscriptionPauseStatus: active
                  deliveryAutoResumed: false
        "400":
          description: Tenant Stripe Connect not ready (`CONNECT_NOT_READY`)
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU, tenant, or subscription not found in Stripe
        "409":
          description: Subscription already active, or cannot resume from current state (`DUPLICATE`)
        "422":
          description: No recurring subscription, subscription was canceled, or Stripe rejected the resume (`UNPROCESSABLE`)
  /api/admin/buyer-skus/{id}/upgrade-subscription:
    post:
      summary: Upgrade a buyer SKU subscription to a new SKU
      description: |
        Upgrades the buyerSku's subscription to `targetSkuId`, optionally applying a coupon code and an
        idempotency key (`upgradeAttemptId`). Delegates to the upgrade action which handles Stripe
        proration + entitlement changes. Note the validation-failure path returns `422 UNPROCESSABLE` for
        a malformed body but `400 VALIDATION_ERROR` for domain errors raised by the upgrade action.
      operationId: upgradeBuyerSkuSubscription
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - targetSkuId
              properties:
                targetSkuId:
                  type: string
                  format: uuid
                upgradeAttemptId:
                  type: string
                  format: uuid
                  description: Idempotency key for the upgrade attempt.
                couponCode:
                  type: string
                  minLength: 1
                  maxLength: 128
            example:
              targetSkuId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              couponCode: LOYAL10
      responses:
        "200":
          description: Subscription upgraded
        "400":
          description: Domain validation error from the upgrade action, or missing tenant context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "422":
          description: Malformed request body (`UNPROCESSABLE`)
  /api/admin/buyer-skus/{id}/upgrade-subscription/preview:
    post:
      summary: Preview a buyer SKU subscription upgrade
      description: |
        Computes the proration/price impact of upgrading the buyerSku's subscription to `targetSkuId`
        (optionally with a coupon) without applying any change. Read-only against Stripe.
      operationId: previewBuyerSkuSubscriptionUpgrade
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - targetSkuId
              properties:
                targetSkuId:
                  type: string
                  format: uuid
                couponCode:
                  type: string
                  minLength: 1
                  maxLength: 128
            example:
              targetSkuId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
      responses:
        "200":
          description: Upgrade preview (proration + resulting price)
        "400":
          description: Domain validation error, or missing tenant context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "422":
          description: Malformed request body (`UNPROCESSABLE`)
  /api/admin/buyer-skus/{id}/verticals:
    patch:
      summary: (Removed) Change a buyer SKU vertical
      description: |
        Deprecated. `buyerSku.verticalId` is immutable post-purchase — use Reissue
        (`POST /api/admin/buyer-skus/{id}/reissue`) to move a buyer to a different vertical. This endpoint
        now always returns a `400 VALIDATION_ERROR` explaining the removal (the source comment references a
        410, but the handler returns a validation error).
      operationId: updateBuyerSkuVerticals
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "400":
          description: Endpoint removed — buyerSku vertical is immutable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: SKU feature disabled for this tenant (`FEATURE_DISABLED`)
  /api/admin/buyer-sku-funding/{id}/refund:
    post:
      summary: Refund a buyer SKU funding purchase
      description: |
        Refunds a single `initial_purchase` or `renewal` funding event on a buyerSku via Stripe, then
        reverses the corresponding lead entitlement locally (writes a `refund_reversal` funding row, adjusts
        `leadsTotal`/`leadsRemaining`, and may transition the buyerSku to `refunded`). The path `{id}` is the
        `buyerSkuFunding` row UUID. Requires the tenant to have Stripe Connect charges enabled. Amounts are
        integer cents.
      operationId: refundBuyerSkuFunding
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Buyer SKU funding row UUID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 500
            example:
              reason: Duplicate charge
      responses:
        "200":
          description: Funding purchase refunded
          content:
            application/json:
              example:
                data:
                  refundId: re_1Q...
                  buyerSkuFundingId: d4e5f6a7-b8c9-0123-def4-234567890123
                  buyerSkuId: c3d4e5f6-a7b8-9012-cdef-123456789012
                  billingPaymentId: e5f6a7b8-c9d0-1234-ef56-345678901234
                  leadDelta: -50
        "400":
          description: Invalid body, non-positive funding row, no linked Stripe payment, no payment intent, or Stripe Connect not
            ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Buyer SKU funding purchase not found
        "409":
          description: Funding purchase already fully refunded (`DUPLICATE`)
  /api/admin/buyers/{id}/skus:
    get:
      summary: List a buyer's SKUs
      description: |
        Returns a paginated list of the buyer's purchased SKUs (buyerSku rows) enriched with SKU/product/
        vertical metadata, current contract linkage, per-SKU sale usage (up to 5 recent sales), and the
        funding-event ledger (up to 10 events). Monetary fields are integer cents. The path `{id}` is the
        buyer UUID.
      operationId: listBuyerSkus
      tags:
        - "Admin: Buyer SKUs"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Buyer UUID.
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of the buyer's SKUs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: c3d4e5f6-a7b8-9012-cdef-123456789012
                    buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    skuId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    skuName: Solar Starter — 100 Leads
                    contractName: Solar - CA
                    status: active
                    leadsTotal: 110
                    leadsRemaining: 42
                    purchasedPriceCents: 50000
                    unitPriceCents: 500
                    unitType: lead
                    unitsRemaining: 42
                    saleUsage:
                      totalSales: 68
                      deliveredSales: 66
                      returnedSales: 2
                      totalPriceCents: 34000
                      recentSales: []
                    fundingEvents:
                      - id: d4e5f6a7-b8c9-0123-def4-234567890123
                        eventType: initial_purchase
                        leadDelta: 110
                        leadsConsumed: 68
                        grossAmountCents: 50000
                pagination:
                  total: 4
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
  /api/admin/buyers/budgets:
    get:
      summary: Buyer delivery budget report
      description: |
        Returns per-contract delivery budget rows plus a state-level pivot and totals, used by the
        `/admin/reports/buyers/budgets` page. The response is NOT the standard paginated envelope — it is
        `{ data: BudgetRow[], byState: StateRow[], totals: BudgetTotals }`. DQ sales are excluded unless
        `dq=include`; zero-budget rows (`tomorrowToDeliver === 0`) are hidden unless `budget=all`.
      operationId: getBuyerBudgets
      tags:
        - Reports
      security:
        - sessionCookie: []
      parameters:
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: productId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: dq
          in: query
          description: Set to `include` to include DQ sales; any other value (or absence) excludes them.
          schema:
            type: string
            enum:
              - include
        - name: budget
          in: query
          description: Set to `all` to show rows with `tomorrowToDeliver === 0`.
          schema:
            type: string
            enum:
              - all
      responses:
        "200":
          description: Buyer budget report
          content:
            application/json:
              example:
                data:
                  - contractId: e5f6a7b8-c9d0-1234-ef56-345678901234
                    contractName: Solar - CA
                    buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    buyerName: Acme Insurance Corp
                    verticalName: Solar
                    dailyLeadCap: 50
                    leadsRemaining: 42
                    deliveredToday: 12
                    tomorrowToDeliver: 38
                    acceptedStates:
                      - CA
                byState:
                  - state: CA
                    stateLabel: California
                    tomorrowToDeliver: 38
                    contractCount: 1
                    deliveredToday: 12
                    contributingContracts: []
                totals:
                  rowCount: 1
                  tomorrowToDeliver: 38
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
  /api/admin/buyers/contracts/{id}/resend-welcome:
    post:
      summary: Resend a buyer's welcome / magic-link email
      description: |
        Regenerates the magic-link token on the buyer's most recent completed checkout session and
        (by default) resends the welcome email, so a buyer can complete the onboarding wizard. Requires
        session authentication (not an API key). Blocked once the buyer has already completed the wizard.
        The path `{id}` is the contract UUID.
      operationId: resendBuyerWelcome
      tags:
        - "Admin: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Contract UUID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                sendEmail:
                  type: boolean
                  default: true
                  description: Set false to regenerate the link without sending the email.
            example:
              sendEmail: true
      responses:
        "200":
          description: Welcome link regenerated
          content:
            application/json:
              example:
                data:
                  checkoutSessionId: a7b8c9d0-e1f2-3456-a789-567890123456
                  buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  userId: c9d0e1f2-a3b4-5678-c901-789012345678
                  emailSent: true
        "400":
          description: Missing contract id, invalid body, wizard already completed, no completed checkout session, or missing
            buyer email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Session authentication required (API keys rejected)
        "404":
          description: Contract not found
  /api/admin/buyers/contracts/{id}/delivery/{index}/test:
    post:
      summary: Test-send a contract delivery action
      description: |
        Sends a test SMS or email for the delivery action at position `{index}` in the contract's
        `deliveryConfig`, using sample lead tokens and the tenant's real provider credentials. Writes a
        `deliveryLog` row with `isTest: true`. Only `sms` and `email` action types are supported.
        Rate-limited to 10 requests/minute per user (in-memory). The path `{id}` is the contract UUID.
      operationId: testContractDelivery
      tags:
        - "Admin: Delivery Test"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Contract UUID.
          schema:
            type: string
            format: uuid
        - name: index
          in: path
          required: true
          description: Zero-based index into the contract's `deliveryConfig` array.
          schema:
            type: integer
            minimum: 0
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                recipientOverride:
                  type: string
                  minLength: 1
                  maxLength: 254
                  description: Override recipient — E.164 phone for SMS, or email address for email. Defaults to the action's configured
                    recipient.
            example:
              recipientOverride: "+15555550100"
      responses:
        "200":
          description: Test send succeeded
          content:
            application/json:
              example:
                data:
                  success: true
                  providerId: SM123abc
                  renderedBody: Hi Test, your Solar quote is ready.
                  encoding: GSM-7
                  segments: 1
        "400":
          description: Invalid delivery index, invalid body/recipient, or unsupported action type (only sms/email)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Contract or delivery action not found
        "422":
          description: SMS or email send failed (`UNPROCESSABLE`)
        "429":
          description: Test-send rate limit exceeded (10/min per user)
  /api/admin/coupons/{id}/active:
    post:
      summary: Toggle a coupon's active flag
      description: |
        Reversible alternative to deleting a coupon. Sets `active` on each of the coupon's linked Stripe
        promotion codes (via `promotion_codes.update`), then mirrors the value to the local DB. Soft-deleted
        coupons cannot be reactivated. Requires Stripe Connect. The path `{id}` is the coupon UUID.
      operationId: toggleCouponActive
      tags:
        - "Admin: Coupons"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - active
              properties:
                active:
                  type: boolean
            example:
              active: false
      responses:
        "200":
          description: Coupon promotion codes updated
          content:
            application/json:
              example:
                data:
                  active: false
                  promotionCodesUpdated: 2
        "400":
          description: Invalid body, or tenant Stripe Connect not ready (`CONNECT_NOT_READY`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Admin access required
        "404":
          description: Coupon not found, or no promotion codes linked to it
        "410":
          description: Coupon is soft-deleted and cannot be reactivated (`GONE`)
  /api/buyer/call-availability:
    get:
      summary: Get the buyer's call availability state
      description: |
        Returns the buyer's current Agent Connect availability, derived and
        TTL-aware. `available` is `true` only while `availableUntil` is in the
        future; once the TTL lapses the buyer is treated as unavailable and the
        routing engine skips presence-gated contracts (`agent_unavailable`).

        Availability is a single buyer-level flag shared across every surface
        (portal + mobile). `surface` reports which surface last set it, or `null`
        when not currently available.
      operationId: getBuyerCallAvailability
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Current availability state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  available:
                    type: boolean
                  availableUntil:
                    type: string
                    format: date-time
                    nullable: true
                    description: When availability auto-expires. `null` when unavailable.
                  surface:
                    type: string
                    nullable: true
                    description: Surface that last set availability (e.g. `portal`, `mobile`).
              example:
                available: true
                availableUntil: 2026-07-04T21:30:00.000Z
                surface: portal
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no buyer context. The buyer must also hold call contracts and have the Calls
            section enabled in their portal menu (per-buyer contract-mix gate + admin menu cascade; applies to API keys
            too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Buyer not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Set the buyer's call availability
      description: |
        Toggles the buyer's Agent Connect availability. Setting `available: true`
        opens a window of `durationMinutes` (15–720, default 480) after which the
        flag auto-expires. Setting `available: false` clears it immediately.

        There is ONE availability flag per buyer, shared across portal and mobile
        — last write wins. When another surface had it set, the response reports
        the superseded surface. Requires the `calls` tenant feature.
      operationId: setBuyerCallAvailability
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - available
              properties:
                available:
                  type: boolean
                durationMinutes:
                  type: integer
                  minimum: 15
                  maximum: 720
                  default: 480
                  description: Minutes the availability window stays open. Ignored when `available` is false.
            example:
              available: true
              durationMinutes: 240
      responses:
        "200":
          description: Availability updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  available:
                    type: boolean
                  availableUntil:
                    type: string
                    format: date-time
                    nullable: true
                  surface:
                    type: string
                    description: Always `portal` for this endpoint.
                  supersededSurface:
                    type: string
                    nullable: true
                    description: Surface whose availability this write replaced, if any.
              example:
                available: true
                availableUntil: 2026-07-04T21:30:00.000Z
                surface: portal
                supersededSurface: null
        "400":
          description: Validation error (e.g. `durationMinutes` out of range).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or the `calls` feature is not enabled for the tenant. The buyer must also hold call
            contracts and have the Calls section enabled in their portal menu (per-buyer contract-mix gate + admin menu
            cascade; applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/webhook-endpoints:
    get:
      summary: List Agent Connect webhook subscriptions
      description: |
        Buyer-scoped, paginated list of webhook destinations that receive Agent
        Connect events. The HMAC signing secret is server-generated and shown
        exactly once at creation — it is NEVER returned here.
      operationId: listBuyerWebhookEndpoints
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          description: Page size. Clamped to the configured maximum.
          schema:
            type: integer
            minimum: 1
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated webhook endpoints.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        url:
                          type: string
                          format: uri
                        eventPattern:
                          type: string
                        disabledAt:
                          type: string
                          format: date-time
                          nullable: true
                        createdAt:
                          type: string
                          format: date-time
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
              example:
                data:
                  - id: 3f2b1a0c-9d8e-7f6a-5b4c-3d2e1f0a9b8c
                    url: https://buyer.example.com/hooks/agent-connect
                    eventPattern: call.*
                    disabledAt: null
                    createdAt: 2026-07-04T18:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no buyer / tenant context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create an Agent Connect webhook subscription
      description: |
        Registers an https destination for Agent Connect events. The `hmacSecret`
        in the response is returned ONLY here, exactly once — store it now; it
        cannot be retrieved again. Sign verification uses `sha256` HMAC over the
        raw request body (see the Agent Connect webhooks section of the user
        manual). Requires the `calls` tenant feature.
      operationId: createBuyerWebhookEndpoint
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
                - eventPattern
              properties:
                url:
                  type: string
                  format: uri
                  description: Destination URL. Must be `https://`.
                eventPattern:
                  type: string
                  minLength: 1
                  maxLength: 120
                  description: Event match pattern (e.g. `call.*`, `call.agent.ringing`).
            example:
              url: https://buyer.example.com/hooks/agent-connect
              eventPattern: call.*
      responses:
        "201":
          description: Endpoint created. `hmacSecret` is shown once and never again.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      url:
                        type: string
                        format: uri
                      eventPattern:
                        type: string
                      hmacSecret:
                        type: string
                        description: Signing secret — shown ONCE. Store it immediately.
                      createdAt:
                        type: string
                        format: date-time
              example:
                data:
                  id: 3f2b1a0c-9d8e-7f6a-5b4c-3d2e1f0a9b8c
                  url: https://buyer.example.com/hooks/agent-connect
                  eventPattern: call.*
                  hmacSecret: 8vQ2m0Xk9f1sJ4pR7wZ3nB6dL0cH5tG2yU8aE1oI9s
                  createdAt: 2026-07-04T18:00:00.000Z
        "400":
          description: Validation error (non-https URL, missing pattern).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or the `calls` feature is not enabled for the tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/webhook-endpoints/{id}:
    delete:
      summary: Disable an Agent Connect webhook subscription
      description: |
        Soft-disables the endpoint (sets `disabledAt`); the emitter then skips it.
        Ownership-scoped — an endpoint belonging to another buyer or tenant, or
        one already disabled, returns 404 without leaking existence.
      operationId: deleteBuyerWebhookEndpoint
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Webhook endpoint id.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Endpoint disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      disabled:
                        type: boolean
              example:
                data:
                  id: 3f2b1a0c-9d8e-7f6a-5b4c-3d2e1f0a9b8c
                  disabled: true
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no buyer / tenant context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Endpoint not found (or not owned by this buyer, or already disabled).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/webhook-endpoints/{id}/test:
    post:
      summary: Send a test delivery to a webhook endpoint
      description: |
        Sends one HMAC-signed test envelope to the endpoint URL and reports the
        attempt synchronously. The envelope and signature headers (`X-LR-Event`,
        `X-LR-Delivery-Id`, `X-LR-Signature`) match the real emitter exactly, so
        consumers can verify their signature handling end-to-end. The payload
        `data` carries `test: true`.

        A failed test is reported in the response and is NEVER queued into the
        delivery retry ladder. Ownership-scoped — an endpoint belonging to
        another buyer or tenant, or one already disabled, returns 404 without
        leaking existence.
      operationId: testBuyerWebhookEndpoint
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Webhook endpoint id.
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                event:
                  type: string
                  minLength: 1
                  maxLength: 120
                  description: Event name stamped on the test envelope. Defaults to `webhook.test`.
            example:
              event: call.agent.ringing
      responses:
        "200":
          description: Test attempt result (success or failure — both are 200).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      endpointId:
                        type: string
                        format: uuid
                      deliveryId:
                        type: string
                        format: uuid
                      event:
                        type: string
                      success:
                        type: boolean
                      statusCode:
                        type: integer
                        nullable: true
                      durationMs:
                        type: integer
                      error:
                        type: string
                        nullable: true
                      responseBody:
                        type: string
                        nullable: true
                        description: Endpoint response body, truncated to 2048 chars.
              example:
                data:
                  endpointId: 3f2b1a0c-9d8e-7f6a-5b4c-3d2e1f0a9b8c
                  deliveryId: 9c8b7a6f-5e4d-3c2b-1a09-f8e7d6c5b4a3
                  event: webhook.test
                  success: true
                  statusCode: 200
                  durationMs: 143
                  error: null
                  responseBody: ok
        "400":
          description: Invalid request body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no buyer / tenant context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Endpoint not found (or not owned by this buyer, or disabled).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/call-events:
    get:
      summary: Poll the buyer's Agent Connect event feed
      description: |
        Cursor-tailed polling feed of Agent Connect events for the authenticated
        buyer — the fallback transport when the SSE stream (`/api/buyer/call-events/stream`)
        can't be held open. Pass `after` with the last `cursor` the client has
        seen; only strictly-newer rows return, oldest first (`cursor` ascending).

        Redaction rule: `ringing` and `missed` events carry only a redacted
        consumer preview (never the consumer phone or last name). `accepted` and
        `completed` events carry the full contact once the buyer has taken the
        call.
      operationId: listBuyerCallEvents
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      parameters:
        - name: after
          in: query
          description: Return only events with `cursor` strictly greater than this value.
          schema:
            type: integer
            minimum: 0
        - name: limit
          in: query
          description: Page size. Clamped to the configured maximum.
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Events newer than `after`, oldest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        cursor:
                          type: integer
                          description: Strictly-increasing feed position. Pass back as `after`.
                        buyerId:
                          type: string
                          format: uuid
                        callId:
                          type: string
                          format: uuid
                          nullable: true
                        event:
                          type: string
                          description: Event name (e.g. `call.agent.ringing`).
                        payload:
                          type: object
                          description: Event payload — redacted preview pre-accept, full contact post-accept.
                        createdAt:
                          type: string
                          format: date-time
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
              example:
                data:
                  - cursor: 1042
                    buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    callId: 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                    event: call.agent.ringing
                    payload:
                      firstName: Jordan
                      lastInitial: M
                      age: 42
                      product: Auto Insurance
                      state: CA
                      city: San Diego
                    createdAt: 2026-07-04T18:05:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no buyer context. The buyer must also hold call contracts and have the Calls
            section enabled in their portal menu (per-buyer contract-mix gate + admin menu cascade; applies to API keys
            too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/calls/{id}/disposition:
    post:
      summary: Set the disposition on a taken call
      description: |
        Records the buyer's outcome for a connected call. Only post-connect
        dispositions are accepted (`no_answer` is excluded — a connected call
        cannot be a no-answer). Ownership-scoped to the buyer's own sale on the
        call; a call the buyer has no sale on returns 404. Idempotent — re-posting
        the current disposition is a no-op (`changed: false`).

        Writes through the unified disposition system (`dispositionLog`), never a
        parallel path. `{id}` is the call id.
      operationId: setBuyerCallDisposition
      tags:
        - "Portal: Agent Connect"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Call id.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - disposition
              properties:
                disposition:
                  type: string
                  enum:
                    - voicemail
                    - call_back
                    - quoted
                    - sold
                    - not_interested
                    - dnc
            example:
              disposition: sold
      responses:
        "200":
          description: Disposition recorded (or already current).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      callId:
                        type: string
                        format: uuid
                      disposition:
                        type: string
                      changed:
                        type: boolean
                        description: "`false` when the disposition already matched (idempotent no-op)."
              example:
                data:
                  callId: 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                  disposition: sold
                  changed: true
        "400":
          description: Validation error (disposition not in the accepted enum).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no buyer / tenant context. The buyer must also hold call contracts and have
            the Calls section enabled in their portal menu (per-buyer contract-mix gate + admin menu cascade; applies to
            API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Call not found, or the buyer has no sale on it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/buyer/me/call-availability:
    post:
      summary: Set buyer call availability (service token)
      description: |
        Agent Connect external tier. Service-token variant of the portal
        availability toggle — same single buyer-level flag, last write wins.
        Auth: `Authorization: Bearer lrsk_…` + `X-Tenant: <tenant slug>` +
        `X-Acting-Buyer-Id: <buyer uuid>`. `surface` identifies the consumer:
        `mobile` (app servers driving a phone UX) or `api` (CRM adapters,
        default). `portal` is reserved for the session surface.
      operationId: postV1BuyerCallAvailability
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
        - name: X-Acting-Buyer-Id
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - available
              properties:
                available:
                  type: boolean
                durationMinutes:
                  type: integer
                  minimum: 15
                  maximum: 720
                  default: 480
                surface:
                  type: string
                  enum:
                    - mobile
                    - api
                  default: api
      responses:
        "200":
          description: Availability updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  available:
                    type: boolean
                  availableUntil:
                    type: string
                    format: date-time
                    nullable: true
                  surface:
                    type: string
                  supersededSurface:
                    type: string
                    nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/calls/{callId}/disposition:
    post:
      summary: Write a call disposition (service token)
      description: |
        Agent Connect external tier disposition write-back. Canonical
        post-connect enum; `no_answer` is rejected (a connected call cannot be
        a no-answer). Ownership scoped to the acting buyer. Same auth headers
        as the availability endpoint. Idempotent on re-post.
      operationId: postV1CallDisposition
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
        - name: X-Acting-Buyer-Id
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - disposition
              properties:
                disposition:
                  type: string
                  enum:
                    - voicemail
                    - call_back
                    - quoted
                    - sold
                    - not_interested
                    - dnc
      responses:
        "200":
          description: Disposition applied (or no-op when unchanged).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      callId:
                        type: string
                        format: uuid
                      disposition:
                        type: string
                      changed:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/buyer/me/lookup-by-phone:
    post:
      summary: Look up a buyer by phone (service token)
      description: |
        Mobile Phase 0. Resolve a buyer by E.164 phone within the token's
        tenant, BEFORE an acting buyer is known — so it takes token + X-Tenant
        only (NO X-Acting-Buyer-Id). 404 on a miss AND on a cross-tenant match
        (no existence leak). `name` is split into first token / remainder.
      operationId: postV1BuyerLookupByPhone
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
              properties:
                phone:
                  type: string
                  description: E.164
                  e.g. +15551230001: null
      responses:
        "200":
          description: Buyer found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  buyerId:
                    type: string
                    format: uuid
                  firstName:
                    type: string
                  lastName:
                    type: string
                  email:
                    type: string
                    nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/buyer/me/provision:
    post:
      summary: Provision a buyer from mobile signup (service token)
      description: |
        Mobile Phase 0. Create a new buyer from the mobile signup flow. Token +
        X-Tenant only (NO X-Acting-Buyer-Id). 409 on an existing (tenant, phone).
        Records a TCPA consent snapshot + consent-log row, then applies the
        tenant's signup promo SKU: a $0 SKU auto-grants `leadCount + bonusLeads`
        (one-time-per-phone); a paid SKU is NOT granted (reason
        `requires_purchase` + a `signupPromoSku` checkout deep-link). All paths
        return 201.
      operationId: postV1BuyerProvision
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
                - firstName
                - lastName
                - email
                - tcpaConsent
                - license
              properties:
                phone:
                  type: string
                  description: E.164
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                tcpaConsent:
                  type: boolean
                  enum:
                    - true
                license:
                  type: object
                  required:
                    - state
                    - number
                  properties:
                    state:
                      type: string
                    number:
                      type: string
      responses:
        "201":
          description: Buyer provisioned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  buyerId:
                    type: string
                    format: uuid
                  tenantId:
                    type: string
                    format: uuid
                  firstName:
                    type: string
                  lastName:
                    type: string
                  email:
                    type: string
                  createdAt:
                    type: string
                    format: date-time
                  signupBonus:
                    type: object
                    properties:
                      creditedLeads:
                        type: integer
                      appliedAt:
                        type: string
                        format: date-time
                        nullable: true
                      reason:
                        type: string
                        nullable: true
                        description: null when granted; else not_configured | requires_purchase | already_applied
                  signupPromoSku:
                    type: object
                    nullable: true
                    description: Present only when a paid promo SKU should be surfaced for checkout.
                    properties:
                      id:
                        type: string
                        format: uuid
                      name:
                        type: string
                      leadCount:
                        type: integer
                      bonusLeads:
                        type: integer
                      priceCents:
                        type: integer
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A buyer already exists for this (tenant, phone).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/buyer/me/filters:
    get:
      summary: Get the buyer's targeting filters (service token)
      description: |
        Mobile Phase 0. Returns the acting buyer's targeting, mapped onto their
        contracts. `products` = distinct vertical slugs across non-terminal
        contracts; states/age/income come from the most-recently-updated
        active|setup contract. Zero contracts → empty products + null fields.
      operationId: getV1BuyerFilters
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
        - name: X-Acting-Buyer-Id
          in: header
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Current filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  products:
                    type: array
                    items:
                      type: string
                  states:
                    type: array
                    items:
                      type: string
                    nullable: true
                  ageMin:
                    type: integer
                    nullable: true
                  ageMax:
                    type: integer
                    nullable: true
                  incomeMin:
                    type: integer
                    nullable: true
                  incomeMax:
                    type: integer
                    nullable: true
                  updatedAt:
                    type: string
                    format: date-time
                    nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      summary: Update the buyer's targeting filters (service token)
      description: |
        Mobile Phase 0. Applies states (uppercase-normalized) / age / income
        {min,max} to ALL of the buyer's active+setup contracts. A `null` bound
        clears just that bound. `min > max` → 400. `products` in the body → 400
        (product selection is managed via purchases). Zero contracts → 404.
      operationId: patchV1BuyerFilters
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
        - name: X-Acting-Buyer-Id
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                states:
                  type: array
                  items:
                    type: string
                  nullable: true
                ageMin:
                  type: integer
                  nullable: true
                ageMax:
                  type: integer
                  nullable: true
                incomeMin:
                  type: integer
                  nullable: true
                incomeMax:
                  type: integer
                  nullable: true
      responses:
        "200":
          description: Updated filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  products:
                    type: array
                    items:
                      type: string
                  states:
                    type: array
                    items:
                      type: string
                    nullable: true
                  ageMin:
                    type: integer
                    nullable: true
                  ageMax:
                    type: integer
                    nullable: true
                  incomeMin:
                    type: integer
                    nullable: true
                  incomeMax:
                    type: integer
                    nullable: true
                  updatedAt:
                    type: string
                    format: date-time
                    nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/buyer/me/dnd:
    get:
      summary: Read the buyer's do-not-disturb state (service token)
      description: TTL-derived — an expired window reads as inactive with dndUntil null.
      operationId: getV1BuyerDnd
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
        - name: X-Acting-Buyer-Id
          in: header
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Current dnd state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  dndUntil:
                    type: string
                    format: date-time
                    nullable: true
                  active:
                    type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      summary: Set the buyer's do-not-disturb window (service token)
      description: |
        Mobile Phase 0. Time-boxes the acting buyer's do-not-disturb window
        (buyer.dndUntil, TTL semantics). A future `until` gates LEADS in the
        routing engine (calls are unaffected — they use call availability). A
        `null` or past `until` resumes. Idempotent: an audit row is written only
        on an actual change.
      operationId: postV1BuyerDnd
      tags:
        - "Portal: Agent Connect"
      security: []
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
        - name: X-Acting-Buyer-Id
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - until
              properties:
                until:
                  type: string
                  format: date-time
                  nullable: true
      responses:
        "200":
          description: DND state after the write.
          content:
            application/json:
              schema:
                type: object
                properties:
                  dndUntil:
                    type: string
                    format: date-time
                    nullable: true
                  active:
                    type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/buyer/available-skus:
    get:
      summary: List purchasable SKUs (buyer catalog)
      description: |
        Returns the SKUs the authenticated buyer can purchase from the portal
        catalog. Tenant-scoped; only `active`, `showInBuyerPortal` SKUs whose
        `availableUntil` (if set) is today or later in the tenant timezone are
        returned. Each SKU is 1:1 with a vertical, so `verticalId` / `verticalName`
        come back on the row for direct display. First-time-buyer-only SKUs are
        filtered out once the buyer has any prior SKU purchase.

        Each row is decorated with unit labels derived from the SKU's product
        type (`unitType`, `unitLabel`, `unitLabelPlural`) and a computed
        `unitCount` / `unitsTotal` (`leadCount + bonusLeads`).
      operationId: listBuyerAvailableSkus
      tags:
        - "Portal: SKUs & Checkout"
      security:
        - sessionCookie: []
      parameters:
        - name: productType
          in: query
          description: |
            Restrict the catalog to a single product family. `call` returns only
            call SKUs; `lead` returns lead SKUs (including SKUs whose product has a
            null product type). Any other value applies no product-type filter.
          schema:
            type: string
            enum:
              - lead
              - call
      responses:
        "200":
          description: Purchasable SKU catalog for this buyer.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      skus:
                        type: array
                        items:
                          type: object
              example:
                data:
                  skus:
                    - id: 7c1d2e3f-4a5b-6c7d-8e9f-0a1b2c3d4e5f
                      slug: solar-500
                      name: Solar Starter — 500 leads
                      tagline: Best for new advertisers
                      badge: Popular
                      priceCents: 250000
                      leadCount: 500
                      verticalId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                      verticalName: Residential Solar
                      productType: lead
                      unitType: lead
                      unitLabel: lead
                      unitLabelPlural: leads
                      unitCount: 525
                      unitsTotal: 525
                      isSubscription: false
                      billingInterval: null
                      bonusLeads: 25
                      trialLeads: 0
                      trialDays: 0
                      availableUntil: null
                      firstTimeBuyerOnly: false
                      sortOrder: 0
        "403":
          description: Caller is not a buyer, or has no tenant context. Also returned when the Billing or SKUs section is hidden
            for this buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/balance:
    get:
      summary: Read the buyer's prepaid balance
      description: |
        The authenticated buyer's prepaid balance and credit summary. Mirrors
        the admin balance read (`GET /api/v1/buyers/{buyerId}/balance`) — money
        values are decimal strings. `availableCredit` is `balance + creditLimit`
        computed with integer-cents arithmetic.
      operationId: getBuyerBalance
      tags:
        - "Portal: Billing"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Prepaid balance + credit summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      buyerId:
                        type: string
                        format: uuid
                      balance:
                        type: string
                        description: Prepaid balance as a decimal string.
                      creditLimit:
                        type: string
                        nullable: true
                        description: Post-pay credit limit as a decimal string.
                      availableCredit:
                        type: string
                        description: balance + creditLimit (decimal string).
              example:
                data:
                  buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  balance: "125.50"
                  creditLimit: "100.00"
                  availableCredit: "225.50"
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer. Also returned when the Billing or SKUs section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Buyer record not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/calls:
    get:
      summary: List the buyer's revenue-bearing calls
      description: |
        Buyer-scoped, paginated list of call sales. Scope is server-enforced —
        a buyer only ever sees their own tenant + buyer rows, and only
        revenue-bearing sales: `creditStatus IN ('credited','assist')` AND
        (`totalPrice > 0` OR `cpaConvertedAt IS NOT NULL`). Duplicate / pending-CPA
        sales are excluded.

        `callerNumber` and `calledNumber` are masked for buyers without PII access
        (same contract as `/api/v1/calls`). Ordered newest-first by sale
        `createdAt`.
      operationId: listBuyerCalls
      tags:
        - "Portal: Calls"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          description: Page size. Clamped to the configured maximum.
          schema:
            type: integer
            minimum: 1
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: status
          in: query
          description: Filter by call-sale status.
          schema:
            $ref: "#/components/schemas/CallSaleStatus"
        - name: startDate
          in: query
          description: Inclusive lower bound (YYYY-MM-DD), interpreted in the tenant timezone.
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          description: Inclusive upper bound (YYYY-MM-DD), interpreted in the tenant timezone.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Paginated call list with the standard envelope.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - callId: 1a2b3c4d-5e6f-7081-92a3-b4c5d6e7f809
                    saleId: 9f8e7d6c-5b4a-3021-fedc-ba9876543210
                    callerNumber: +1512•••1234
                    calledNumber: "+18005550100"
                    callStatus: completed
                    saleStatus: completed
                    hangupCause: normal_clearing
                    duration: 212
                    talkDuration: 180
                    billableDuration: 180
                    totalPrice: "45.00"
                    startedAt: 2026-06-01T14:00:00.000Z
                    answeredAt: 2026-06-01T14:00:12.000Z
                    createdAt: 2026-06-01T14:03:32.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "403":
          description: Caller is not a buyer, has no buyer / tenant context, or the buyer holds no call contracts (per-buyer
            contract-mix gate). Also returned when the Calls section is hidden for this buyer by the admin portal-menu
            cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/checkout/sku:
    post:
      summary: Create a Stripe Checkout session for a SKU purchase
      description: |
        Creates a Stripe Checkout session (on the tenant's connected account) for
        the buyer to purchase a SKU, and returns the hosted checkout URL. Supports
        one-time and subscription SKUs, coupon codes, account-balance credit, and
        a purchase quantity (one-time SKUs only).

        Requires the `permBilling` permission. The SKU must be tenant-scoped,
        `active`, and `showInBuyerPortal`. The bound `verticalId` is authoritative:
        `verticalIds` must be exactly `[sku.verticalId]`. Subscription SKUs force
        `quantity = 1`.

        Balance credit and coupon reservations are made before the Stripe call and
        released/reversed if session creation or the local `checkoutSession` insert
        fails. If the buyer already has a reusable contract for the SKU's product,
        success returns to Billing; otherwise it routes to the onboarding wizard
        via a magic-link `/welcome` URL.
      operationId: createBuyerSkuCheckout
      tags:
        - "Portal: SKUs & Checkout"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - skuSlug
                - verticalIds
              properties:
                skuSlug:
                  type: string
                  minLength: 1
                  maxLength: 64
                verticalIds:
                  type: array
                  description: Must be exactly one element equal to the SKU's bound verticalId.
                  items:
                    type: string
                    minLength: 1
                  minItems: 1
                  maxItems: 10
                couponCode:
                  type: string
                  minLength: 1
                  maxLength: 50
                applyBalanceCredit:
                  type: boolean
                  default: false
                  description: Apply available account balance as a discount toward this purchase.
                quantity:
                  type: integer
                  minimum: 1
                  maximum: 100000
                  default: 1
                  description: One-time SKUs only; subscription SKUs must use quantity 1.
            example:
              skuSlug: solar-500
              verticalIds:
                - b2c3d4e5-f6a7-8901-bcde-f12345678901
              couponCode: LAUNCH20
              applyBalanceCredit: true
              quantity: 1
      responses:
        "200":
          description: Checkout session created; redirect the buyer to `data.url`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        format: uri
                        nullable: true
              example:
                data:
                  url: https://checkout.stripe.com/c/pay/cs_test_a1b2c3
        "400":
          description: |
            Validation error (bad body, `verticalIds` not `[sku.verticalId]`,
            quantity on a subscription SKU, invalid quantity-adjusted price), or
            the merchant's Stripe Connect account cannot accept payments
            (`CONNECT_NOT_READY`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Account balance was insufficient to cover the requested balance credit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, lacks `permBilling`, or has no tenant context. Also returned when the Billing
            section is hidden for this buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant, SKU, or buyer not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: SKU is past its `availableUntil` cutoff (`GONE`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/checkout/sku/preview:
    post:
      summary: Preview a coupon against a SKU price
      description: |
        Validates a coupon code against a SKU and returns the resolved discount
        without creating a checkout session. Requires the `permBilling` permission.
        Coupon-resolution failures (not found, inactive, expired, redeemed out,
        SKU mismatch, invalid discount) are returned as `400 VALIDATION_ERROR`
        with a machine-readable `details.status`.
      operationId: previewBuyerSkuCheckout
      tags:
        - "Portal: SKUs & Checkout"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - skuSlug
                - couponCode
              properties:
                skuSlug:
                  type: string
                  minLength: 1
                  maxLength: 64
                couponCode:
                  type: string
                  minLength: 1
                  maxLength: 50
                quantity:
                  type: integer
                  minimum: 1
                  maximum: 100000
                  default: 1
            example:
              skuSlug: solar-500
              couponCode: LAUNCH20
              quantity: 1
      responses:
        "200":
          description: Resolved SKU pricing and coupon discount.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      sku:
                        type: object
                        properties:
                          slug:
                            type: string
                          priceCents:
                            type: integer
                            description: Quantity-adjusted list price in cents.
                          unitPriceCents:
                            type: integer
                          quantity:
                            type: integer
                          isSubscription:
                            type: boolean
                      coupon:
                        type: object
                        properties:
                          code:
                            type: string
                          amountOffCents:
                            type: integer
                            nullable: true
                          percentOffBps:
                            type: integer
                            nullable: true
                          duration:
                            type: string
                          durationInMonths:
                            type: integer
                            nullable: true
                          discountCents:
                            type: integer
                          discountedPriceCents:
                            type: integer
              example:
                data:
                  sku:
                    slug: solar-500
                    priceCents: 250000
                    unitPriceCents: 250000
                    quantity: 1
                    isSubscription: false
                  coupon:
                    code: LAUNCH20
                    amountOffCents: null
                    percentOffBps: 2000
                    duration: once
                    durationInMonths: null
                    discountCents: 50000
                    discountedPriceCents: 200000
        "400":
          description: |
            Validation error, coupon resolution failure (`details.status` is one of
            `empty_code` / `not_found` / `inactive` / `expired` / `redeemed_out` /
            `sku_mismatch` / `invalid_discount`), or `CONNECT_NOT_READY`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, lacks `permBilling`, or has no tenant context. Also returned when the Billing
            section is hidden for this buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant or SKU not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: SKU is past its `availableUntil` cutoff (`GONE`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/checkout/topup:
    post:
      summary: Create a Stripe Checkout session for an account top-up
      description: |
        Creates a Stripe Checkout session (on the tenant's connected account) to
        add prepaid balance to the buyer's account, and returns the hosted checkout
        URL. Requires the `permBilling` permission. `amountCents` must be an integer
        between the configured min and max top-up amounts.
      operationId: createBuyerTopupCheckout
      tags:
        - "Portal: SKUs & Checkout"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amountCents
              properties:
                amountCents:
                  type: integer
                  description: Top-up amount in cents. Must be within the configured min/max bounds.
            example:
              amountCents: 50000
      responses:
        "200":
          description: Checkout session created; redirect the buyer to `data.url`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        format: uri
                        nullable: true
              example:
                data:
                  url: https://checkout.stripe.com/c/pay/cs_test_topup123
        "400":
          description: |
            Invalid body, `amountCents` not an integer, below minimum, above
            maximum, or `CONNECT_NOT_READY`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, lacks `permBilling`, or has no tenant context. Also returned when the Billing
            section is hidden for this buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant or buyer not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/contracts:
    get:
      summary: List the buyer's contracts
      description: |
        Buyer-scoped, paginated list of contracts. Scope is server-enforced —
        a buyer only ever sees their own tenant + buyer rows. The response is
        an allowlist of buyer-safe fields; delivery credentials and intake keys
        are never included.
      operationId: listBuyerContracts
      tags:
        - "Portal: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by contract status.
          schema:
            type: string
            enum:
              - setup
              - active
              - paused
              - expired
              - archived
        - name: limit
          in: query
          description: Page size. Clamped to the configured maximum.
          schema:
            type: integer
            minimum: 1
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Contracts with the standard envelope.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: c3d4e5f6-a7b8-9012-cdef-123456789012
                    name: Auto Insurance - CA
                    status: active
                    verticalId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    verticalName: Auto Insurance
                    pricingModel: fixed
                    pricePerLead: "25.00"
                    dailyLeadCap: 100
                    weeklyLeadCap: null
                    monthlyLeadCap: null
                    geoFilters:
                      states:
                        - CA
                    scheduleTimezone: America/Los_Angeles
                    createdAt: 2026-06-01T14:03:32.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid query parameters (e.g. unknown status).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer. Also returned when the Contracts section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/contracts/pause-all:
    post:
      summary: Pause all of the buyer's contracts
      description: |
        Self-service pause of every active contract for the authenticated buyer
        (delivery stops). Shared implementation with the per-contract pause via
        `pauseContractsForBuyer`.

        When `alsoPauseSubscription` is true, a best-effort cascade also pauses the
        buyer's Stripe subscriptions and voids their open invoices. The cascade
        never fails the primary pause — a cascade error surfaces in `cascade.ok`
        / `cascade.reason` while `paused` still reflects the contracts stopped.
      operationId: pauseAllBuyerContracts
      tags:
        - "Portal: Contracts"
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                alsoPauseSubscription:
                  type: boolean
                  default: false
                  description: Also pause Stripe subscriptions and void their open invoices.
            example:
              alsoPauseSubscription: true
      responses:
        "200":
          description: Contracts paused; subscription cascade result reported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      paused:
                        type: integer
                        description: Number of contracts transitioned to paused.
                      subscriptionsPaused:
                        type: integer
                      voidedInvoiceIds:
                        type: array
                        items:
                          type: string
                      cascade:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          reason:
                            type: string
              example:
                data:
                  paused: 3
                  subscriptionsPaused: 1
                  voidedInvoiceIds:
                    - in_1Pabc123
                  cascade:
                    ok: true
        "400":
          description: Invalid body (unknown keys rejected — schema is strict).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer. Also returned when the Contracts or Billing section is hidden for this buyer by the
            admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/contracts/resume-all:
    post:
      summary: Resume all of the buyer's contracts
      description: |
        Self-service resume of the buyer's paused contracts (delivery restarts).
        Shared implementation with the per-contract resume via
        `resumeContractsForBuyer`.

        When `alsoResumeSubscription` is true, a best-effort cascade also resumes
        the buyer's Stripe subscriptions — this RE-BILLS via Stripe, so it only
        runs on explicit opt-in. A cascade failure never fails the primary resume;
        it surfaces in `cascade.ok` / `cascade.reason`.
      operationId: resumeAllBuyerContracts
      tags:
        - "Portal: Contracts"
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                alsoResumeSubscription:
                  type: boolean
                  default: false
                  description: Also resume Stripe subscriptions (re-bills via Stripe).
            example:
              alsoResumeSubscription: false
      responses:
        "200":
          description: Contracts resumed; subscription cascade result reported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      resumed:
                        type: integer
                      subscriptionsResumed:
                        type: integer
                      cascade:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          reason:
                            type: string
              example:
                data:
                  resumed: 3
                  subscriptionsResumed: 0
                  cascade:
                    ok: true
        "400":
          description: Invalid body (unknown keys rejected — schema is strict).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer. Also returned when the Contracts or Billing section is hidden for this buyer by the
            admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/contracts/{id}/pause:
    post:
      summary: Pause a single contract
      description: |
        Pauses one of the buyer's contracts by id. The contract must belong to the
        authenticated buyer (404 otherwise). If already paused, the transition is
        skipped and `alreadyPaused` is true. The state transition is validated via
        the contract state machine; an illegal source state returns
        `400 VALIDATION_ERROR`.

        When `alsoPauseSubscription` is true, a best-effort cascade pauses the
        Stripe subscriptions scoped to THIS contract's `buyerSku` rows (never
        sibling contracts) and voids their open invoices. Cascade failures surface
        in `cascade` and never fail the pause.
      operationId: pauseBuyerContract
      tags:
        - "Portal: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                alsoPauseSubscription:
                  type: boolean
                  default: false
            example:
              alsoPauseSubscription: true
      responses:
        "200":
          description: Contract paused (or already paused); cascade result reported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      status:
                        type: string
                        enum:
                          - paused
                      alreadyPaused:
                        type: boolean
                      subscriptionsPaused:
                        type: integer
                      voidedInvoiceIds:
                        type: array
                        items:
                          type: string
                      cascade:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          reason:
                            type: string
              example:
                data:
                  status: paused
                  alreadyPaused: false
                  subscriptionsPaused: 1
                  voidedInvoiceIds: []
                  cascade:
                    ok: true
        "400":
          description: Invalid body, or an illegal contract state transition to `paused`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer. Also returned when the Contracts or Billing section is hidden for this buyer by the
            admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contract not found for this buyer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/contracts/{id}/resume:
    post:
      summary: Resume a single contract
      description: |
        Resumes one of the buyer's contracts by id. The contract must belong to the
        authenticated buyer (404 otherwise). If already active, the transition is
        skipped and `alreadyActive` is true. The transition is validated via the
        contract state machine; an illegal source state returns
        `400 VALIDATION_ERROR`.

        When `alsoResumeSubscription` is true, a best-effort cascade resumes the
        Stripe subscriptions scoped to THIS contract (never siblings) — this
        RE-BILLS via Stripe and is opt-in only. Cascade failures surface in
        `cascade` and never fail the resume.
      operationId: resumeBuyerContract
      tags:
        - "Portal: Contracts"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                alsoResumeSubscription:
                  type: boolean
                  default: false
            example:
              alsoResumeSubscription: false
      responses:
        "200":
          description: Contract resumed (or already active); cascade result reported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      status:
                        type: string
                        enum:
                          - active
                      alreadyActive:
                        type: boolean
                      subscriptionsResumed:
                        type: integer
                      cascade:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          reason:
                            type: string
              example:
                data:
                  status: active
                  alreadyActive: false
                  subscriptionsResumed: 0
                  cascade:
                    ok: true
        "400":
          description: Invalid body, or an illegal contract state transition to `active`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer. Also returned when the Contracts or Billing section is hidden for this buyer by the
            admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contract not found for this buyer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/delivery-log:
    get:
      summary: List the buyer's delivery log
      description: |
        Buyer-scoped delivery log. Scope is server-enforced: only this buyer's
        non-test rows that correspond to a sold/returned, buyer-visible lead sale.
        Raw request/response payloads are admin-only and omitted. `endpoint` is
        masked for phone/email channels.

        Note: `pagination.total` reflects the number of rows returned on this page
        (bounded by `limit`), not a full count; `hasMore` is true when the page is
        full.
      operationId: listBuyerDeliveryLog
      tags:
        - "Portal: Delivery Log"
      security:
        - sessionCookie: []
      parameters:
        - name: channel
          in: query
          description: Filter by delivery channel.
          schema:
            type: string
            enum:
              - http_post
              - http_get
              - sms
              - email
              - google_sheets
              - webhook
        - name: outcome
          in: query
          description: Filter by delivery outcome.
          schema:
            type: string
            enum:
              - success
              - failed
              - rejected
              - test
        - name: dateFrom
          in: query
          description: Inclusive lower bound (YYYY-MM-DD), interpreted in the tenant timezone.
          schema:
            type: string
            format: date
        - name: dateTo
          in: query
          description: Inclusive upper bound (YYYY-MM-DD), interpreted in the tenant timezone.
          schema:
            type: string
            format: date
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: Delivery-log rows with the standard envelope.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 3f2e1d0c-9b8a-7654-3210-fedcba987654
                    displayId: 10432
                    tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    contractId: c3d4e5f6-a7b8-9012-cdef-123456789012
                    leadId: d4e5f6a7-b8c9-0123-def0-234567890123
                    leadSaleId: e5f6a7b8-c9d0-1234-ef01-345678901234
                    channel: http_post
                    endpoint: https://buyer.example/intake
                    method: POST
                    isTest: false
                    attemptNumber: 1
                    durationMs: 342
                    outcome: success
                    httpStatus: 200
                    errorMessage: null
                    errorCode: null
                    contractName: Auto Insurance - CA
                    createdAt: 2026-06-01T14:03:32.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid query parameters.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, has no buyer / tenant context, or the buyer holds no lead contracts (per-buyer
            contract-mix gate). Also returned when the Delivery Log section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/partner-response:
    put:
      summary: Update buyer lead-completion partner-response fields
      description: |
        Self-service update of the fields returned to a partner lander on a sold
        lead: the buyer's completion phone and redirect URL. Both are optional;
        sending an empty string clears (nulls) the field. Whether these values are
        actually returned to partners is still governed by per-campaign toggles
        (`sendBuyerPhoneInPartnerResponse` / `sendRedirectUrlInPartnerResponse`)
        controlled by the buyer's account manager.

        `partnerResponseRedirectUrl` may be a plain branded URL or a scheduling
        link (Calendly, Cal.com, Google appointments); scheduling links are
        auto-detected at response-build time and surfaced to landers as embeddable
        calendar metadata. `{{leadId}}` and `{{partnerId}}` tokens are supported in
        the URL; any other `{{token}}` is rejected. A non-http(s) URL or a malformed
        E.164 phone returns `400 VALIDATION_ERROR`. readOnly members are blocked
        with `403 READ_ONLY`. The response returns only these four fields — never the
        full buyer row.
      operationId: updateBuyerPartnerResponse
      tags:
        - "Portal: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                partnerResponseBuyerPhone:
                  type: string
                  nullable: true
                  description: Completion phone in E.164 format, or empty string / null to clear.
                partnerResponseRedirectUrl:
                  type: string
                  nullable: true
                  description: Branded or scheduling redirect URL, or empty string / null to clear. Supports {{leadId}} and {{partnerId}}
                    tokens.
            example:
              partnerResponseBuyerPhone: "+12131231234"
              partnerResponseRedirectUrl: https://calendly.com/buyer-acme/intro
      responses:
        "200":
          description: Updated partner-response fields for this buyer.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      partnerResponseBuyerPhone:
                        type: string
                        nullable: true
                      partnerResponseRedirectUrl:
                        type: string
                        nullable: true
                      partnerResponseBuyerMediaUrl:
                        type: string
                        nullable: true
                      partnerResponseBuyerMediaType:
                        type: string
                        enum:
                          - photo
                          - video
                        nullable: true
              example:
                data:
                  partnerResponseBuyerPhone: "+12131231234"
                  partnerResponseRedirectUrl: https://calendly.com/buyer-acme/intro
                  partnerResponseBuyerMediaUrl: null
                  partnerResponseBuyerMediaType: null
        "400":
          description: Invalid phone, non-http(s) URL, or unknown redirect token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Caller is not a buyer, is a readOnly member (`READ_ONLY`), or the buyer holds no lead contracts (per-buyer
            contract-mix gate). Also returned when the Settings section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/partner-response/media:
    post:
      summary: Upload buyer partner-response photo or video
      description: |
        Uploads the buyer's completion-page media (shown to consumers on the
        partner lander when the campaign enables buyer media). Multipart form with a
        single `file` field. Photos accept jpeg / png / webp up to 10MB; videos
        accept mp4 / webm up to 50MB. Replaces any prior media for this buyer.
        Media type is inferred from the file and returned as `mediaType`. readOnly
        members are blocked with `403 READ_ONLY`.
      operationId: uploadBuyerPartnerResponseMedia
      tags:
        - "Portal: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Photo (jpeg/png/webp ≤10MB) or video (mp4/webm ≤50MB).
      responses:
        "200":
          description: Media stored; public URL and detected type returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                      mediaType:
                        type: string
                        enum:
                          - photo
                          - video
              example:
                data:
                  url: https://media.theleadrouter.com/buyer-media/example.jpg
                  mediaType: photo
        "400":
          description: Missing file, unsupported type, or file exceeds the size limit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Caller is not a buyer, is a readOnly member (`READ_ONLY`), or the buyer holds no lead contracts (per-buyer
            contract-mix gate). Also returned when the Settings section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Remove buyer partner-response media
      description: |
        Clears the buyer's completion-page media, nulling both
        `partnerResponseBuyerMediaUrl` and `partnerResponseBuyerMediaType`.
        readOnly members are blocked with `403 READ_ONLY`.
      operationId: deleteBuyerPartnerResponseMedia
      tags:
        - "Portal: Settings"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Media removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      partnerResponseBuyerMediaUrl:
                        type: string
                        nullable: true
                      partnerResponseBuyerMediaType:
                        type: string
                        nullable: true
              example:
                data:
                  partnerResponseBuyerMediaUrl: null
                  partnerResponseBuyerMediaType: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Caller is not a buyer, is a readOnly member (`READ_ONLY`), or the buyer holds no lead contracts (per-buyer
            contract-mix gate). Also returned when the Settings section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/refunds/{refundId}/acknowledge:
    parameters:
      - name: refundId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: acknowledgeBuyerRefund
      tags:
        - "Portal: Billing"
      summary: Acknowledge a refund decision (buyer portal)
      description: |
        Marks a processed (approved/rejected) refund decision as seen. Buyer-only
        and confined to the caller's own refunds in the WHERE clause. Idempotent —
        a repeat call returns the existing row without changing acknowledgedAt.

        Replaces the portal's former use of `/api/v1/refunds/{refundId}/acknowledge`,
        which applies buyer confinement conditionally (`auth.role === 'buyer' && …`)
        and so did not confine an impersonated admin at all — `/api/v1` does not
        honour an impersonation cookie. The v1 route remains for admins and API keys.
      responses:
        "200":
          description: Refund acknowledged (or already acknowledged)
        "400":
          description: Refund is still pending
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Buyer access required Also returned when the Settings section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/buyer/settings:
    patch:
      summary: Update buyer self-serve settings (concurrency cap)
      description: |
        Self-service update of the buyer's own call concurrency cap
        (`maxConcurrentCalls`) — the most live calls the routing engine will
        send this buyer at once, shared across all of the buyer's contracts.
        `null` (or omitting the field) means unlimited; `0` blocks new calls.

        Requires the admin-granted `canSetConcurrencyCap` permission —
        without it every request is `403 FORBIDDEN`. When the account manager
        has set a floor (`concurrencyCapFloor`), the value must be greater
        than or equal to that floor; `null` (unlimited) is always allowed.
        A below-floor value returns `400 VALIDATION_ERROR`.

        The buyer can only edit their own record: admin-owned keys
        (`concurrencyCapFloor`, `canSetConcurrencyCap`), contract-level caps,
        and any `buyerId` targeting are silently ignored. Every accepted
        change writes an audit log row naming the acting portal user.
      operationId: updateBuyerSelfServeSettings
      tags:
        - "Portal: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                maxConcurrentCalls:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Max simultaneous live calls, or null for unlimited. Must be >= the admin-set concurrencyCapFloor when one
                    exists.
            example:
              maxConcurrentCalls: 5
      responses:
        "200":
          description: Current self-serve call settings after the update.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      maxConcurrentCalls:
                        type: integer
                        nullable: true
                      concurrencyCapFloor:
                        type: integer
                        nullable: true
                        description: Admin-set minimum for the self-serve cap (read-only here).
                      canSetConcurrencyCap:
                        type: boolean
                        description: Admin-granted permission to use this endpoint (read-only here).
              example:
                data:
                  maxConcurrentCalls: 5
                  concurrencyCapFloor: 2
                  canSetConcurrencyCap: true
        "400":
          description: Non-integer / negative value, or a cap below the admin-set concurrencyCapFloor (`VALIDATION_ERROR`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Caller is not a buyer, lacks the `canSetConcurrencyCap` permission, or holds no call contracts (per-buyer
            contract-mix gate). Also returned when the Settings section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/skus:
    get:
      summary: List the buyer's purchased SKUs
      description: |
        Buyer-scoped, paginated list of the buyer's purchased SKUs (`buyerSku`
        rows). Stripe-side identifiers (`stripeInvoiceId`, `stripePaymentIntentId`,
        `stripeChargeId`, `billingPaymentId`, `stripeSubscriptionId`) are stripped
        from the response. Each row resolves exactly one linked contract
        (`currentContractId` / `contractName` / `contractStatus`), is decorated
        with unit labels + counts, and carries up to 10 recent buyer-safe
        `fundingEvents`.
      operationId: listMyBuyerSkus
      tags:
        - "Portal: SKUs & Checkout"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: status
          in: query
          description: Filter by `buyerSku` status (e.g. `active`, `depleted`, `refunded`).
          schema:
            type: string
      responses:
        "200":
          description: Purchased-SKU rows with the standard envelope.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: f6a7b8c9-d0e1-2345-f012-456789012345
                    tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    buyerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    skuId: 7c1d2e3f-4a5b-6c7d-8e9f-0a1b2c3d4e5f
                    contractId: c3d4e5f6-a7b8-9012-cdef-123456789012
                    leadsTotal: 525
                    leadsRemaining: 300
                    status: active
                    subscriptionPauseStatus: active
                    purchasedPriceCents: 250000
                    refundedAmountCents: 0
                    purchasedAt: 2026-05-15T10:00:00.000Z
                    skuName: Solar Starter — 500 leads
                    skuSlug: solar-500
                    productType: lead
                    unitType: lead
                    unitLabel: lead
                    unitLabelPlural: leads
                    unitCount: 525
                    unitsTotal: 525
                    unitsRemaining: 300
                    currentContractId: c3d4e5f6-a7b8-9012-cdef-123456789012
                    contractName: Residential Solar - CA
                    contractStatus: active
                    fundingEvents:
                      - eventType: purchase
                        leadDelta: 525
                        servicePeriodStart: null
                        servicePeriodEnd: null
                        createdAt: 2026-05-15T10:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "403":
          description: Caller is not a buyer. Also returned when the Billing or SKUs section is hidden for this buyer by the admin
            portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/subscriptions/{id}/pause:
    post:
      summary: Pause a subscription (buyerSku)
      description: |
        Pauses the Stripe subscription backing a purchased SKU (`buyerSku` id in
        the path). The row must belong to the authenticated buyer + tenant (404
        otherwise). Requires an existing `stripeSubscriptionId` (422 otherwise) and
        a merchant with Stripe Connect enabled (`CONNECT_NOT_READY` otherwise). The
        pause state transition is guarded by the buyer-SKU pause state machine.

        After pausing in Stripe, already-open invoices on the subscription are
        voided (best-effort) — pausing only stops FUTURE invoices, so open ones
        would otherwise keep retrying via Stripe dunning. When `alsoPauseDelivery`
        is true, the buyer's contracts are cascade-paused via the shared
        `pauseContractsForBuyer` helper.
      operationId: pauseBuyerSubscription
      tags:
        - "Portal: Subscriptions"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: The `buyerSku` id whose subscription to pause.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - alsoPauseDelivery
              properties:
                alsoPauseDelivery:
                  type: boolean
                  description: Also cascade-pause the buyer's contracts. Required (no implicit default).
            example:
              alsoPauseDelivery: true
      responses:
        "200":
          description: Subscription paused.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      buyerSkuId:
                        type: string
                        format: uuid
                      subscriptionPauseStatus:
                        type: string
                        enum:
                          - paused
                      deliveryPaused:
                        type: boolean
              example:
                data:
                  buyerSkuId: f6a7b8c9-d0e1-2345-f012-456789012345
                  subscriptionPauseStatus: paused
                  deliveryPaused: true
        "400":
          description: Invalid body (unknown keys rejected, `alsoPauseDelivery` required), or `CONNECT_NOT_READY`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no tenant context. Also returned when the Billing section is hidden for this
            buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Subscription (buyerSku) or tenant not found, or the subscription is missing in Stripe.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Subscription already paused, or the pause transition is not allowed from the current state (`DUPLICATE`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The purchase has no recurring subscription, or Stripe rejected / returned an unexpected pause status
            (`UNPROCESSABLE`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/subscriptions/{id}/resume:
    post:
      summary: Resume a subscription (buyerSku)
      description: |
        Resumes the Stripe subscription backing a purchased SKU (`buyerSku` id in
        the path). The row must belong to the authenticated buyer + tenant (404
        otherwise). Requires an existing `stripeSubscriptionId` (422 otherwise) and
        a Stripe-Connect-enabled merchant (`CONNECT_NOT_READY` otherwise). A
        canceled subscription cannot be resumed (422). The resume transition is
        guarded by the buyer-SKU pause state machine.

        When `alsoResumeDelivery` is true, the buyer's paused contracts are
        cascade-resumed via the shared `resumeContractsForBuyer` helper. When
        false/omitted, delivery stays paused and must be resumed separately.
      operationId: resumeBuyerSubscription
      tags:
        - "Portal: Subscriptions"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: The `buyerSku` id whose subscription to resume.
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                alsoResumeDelivery:
                  type: boolean
                  default: false
                  description: Also cascade-resume the buyer's paused contracts.
            example:
              alsoResumeDelivery: true
      responses:
        "200":
          description: Subscription resumed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      buyerSkuId:
                        type: string
                        format: uuid
                      subscriptionPauseStatus:
                        type: string
                        enum:
                          - active
                      deliveryResumed:
                        type: boolean
                      contractsResumed:
                        type: integer
              example:
                data:
                  buyerSkuId: f6a7b8c9-d0e1-2345-f012-456789012345
                  subscriptionPauseStatus: active
                  deliveryResumed: true
                  contractsResumed: 2
        "400":
          description: Invalid body (unknown keys rejected), or `CONNECT_NOT_READY`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a buyer, or has no tenant context. Also returned when the Billing section is hidden for this
            buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Subscription (buyerSku) or tenant not found, or the subscription is missing in Stripe.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Subscription already active, or the resume transition is not allowed from the current state (`DUPLICATE`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The purchase has no recurring subscription, was canceled and cannot be resumed, or Stripe rejected /
            returned an unexpected resume status (`UNPROCESSABLE`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/wizard:
    get:
      summary: Get the buyer's onboarding wizard snapshot
      description: |
        Returns the buyer's frozen onboarding-wizard snapshot plus server-held
        progress so the client can rehydrate and resume. The snapshot is returned
        exactly as stored (no join to live `wizard` / `wizardStep` tables — live
        admin edits never retroactively change a mid-flow buyer). Admin-only fields
        (per-step SMS/email overrides) are stripped from the returned snapshot.

        Auth: requires the `lr_wizard_session` cookie (minted during onboarding
        checkout), which is separate from the dashboard `lr_session`. Returns 401
        on absent / invalid / expired wizard session. This scheme is NOT one of the
        documented `securitySchemes` — see the empty `security` list.
      operationId: getBuyerWizard
      tags:
        - "Portal: Wizard"
      security: []
      responses:
        "200":
          description: Buyer's sanitized wizard snapshot + progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      snapshot:
                        type: object
                        nullable: true
                      progress:
                        type: object
              example:
                data:
                  snapshot:
                    wizardId: 8a7b6c5d-4e3f-2109-8a7b-6c5d4e3f2109
                    version: 3
                    steps: []
                  progress:
                    step-1:
                      answer: yes
        "401":
          description: Missing, invalid, or expired `lr_wizard_session` cookie.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Buyer not found for the wizard session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/wizard/complete:
    post:
      summary: Complete the onboarding wizard
      description: |
        Single-flight, idempotent completion of the buyer onboarding wizard. Runs
        every step writer inside one row-locked transaction; any writer throw rolls
        the whole thing back. Resolves (or creates, when product-based contracts
        are enabled) the buyer's contract, applies the snapshot's target contract
        status via the contract state machine, syncs the purchased SKU, and marks
        `wizardCompletedAt`.

        On success it mints the dashboard `lr_session` cookie and clears the
        `lr_wizard_session` cookie (the response carries both `Set-Cookie` headers),
        so the buyer lands authenticated on the dashboard. Idempotent replays (tab
        refresh, retry) short-circuit to `{ ok: true, idempotent: true }` without
        re-writing or consuming rate limit.

        Auth: requires the `lr_wizard_session` cookie (not `lr_session`) plus a
        same-origin request (CSRF guard). Rate-limited to 5 requests/minute per
        buyer. This scheme is NOT one of the documented `securitySchemes` — see the
        empty `security` list.
      operationId: completeBuyerWizard
      tags:
        - "Portal: Wizard"
      security: []
      requestBody:
        required: false
        description: No request body is read; completion works entirely from server-held snapshot + progress.
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: |
            Wizard completed (or idempotent replay). On first completion the
            response also sets `lr_session` and clears `lr_wizard_session`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      contractId:
                        type: string
                        format: uuid
                      idempotent:
                        type: boolean
                        description: Present and true when this was an already-completed replay.
              example:
                data:
                  ok: true
                  contractId: c3d4e5f6-a7b8-9012-cdef-123456789012
        "400":
          description: Validation error — missing step answers, unresolved verticalId, or missing snapshot/contract/email.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired `lr_wizard_session` cookie.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: CSRF check failed (request was not same-origin). Also returned when the Billing section is hidden for this
            buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Buyer not found for the wizard session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: |
            `INVALID_STATE` — the contract's current status cannot transition to the
            snapshot's target status. (Hand-built error code; not in the standard
            ErrorResponse code enum.)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded (5 requests/minute per buyer).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/buyer/wizard/step/{stepId}:
    post:
      summary: Submit a single wizard step answer
      description: |
        Validates a per-step answer against the FROZEN snapshot config for the step
        and writes it to `buyer.wizardProgress[stepId]` via a concurrency-safe
        `jsonb_set` (tab-safe, not read-modify-write). Refreshes the sliding 60-min
        wizard session on success.

        The request body may be the raw answer, or `{ "answer": <value> }` — the
        handler unwraps `answer` when present. The answer is validated against the
        step type's schema resolved from the snapshot config.

        Auth: requires the `lr_wizard_session` cookie (not `lr_session`) plus a
        same-origin request (CSRF guard). Rate-limited to 30 requests/minute per
        buyer. This scheme is NOT one of the documented `securitySchemes` — see the
        empty `security` list.
      operationId: submitBuyerWizardStep
      tags:
        - "Portal: Wizard"
      security: []
      parameters:
        - name: stepId
          in: path
          required: true
          description: The step id, which must exist in the buyer's frozen snapshot.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: "Either the raw answer object, or `{ answer: <value> }`."
              properties:
                answer:
                  description: The step answer. Validated against the step type's snapshot schema.
            example:
              answer:
                selected:
                  - CA
                  - TX
      responses:
        "200":
          description: Step answer accepted and stored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      stepId:
                        type: string
                      ok:
                        type: boolean
              example:
                data:
                  stepId: step-geo
                  ok: true
        "400":
          description: |
            Validation error — non-object body, wizard already completed, no
            snapshot, step not in snapshot, or the answer failed the step schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing, invalid, or expired `lr_wizard_session` cookie.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: CSRF check failed (request was not same-origin). Also returned when the Billing section is hidden for this
            buyer by the admin portal-menu cascade (applies to API keys too).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Buyer not found for the wizard session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded (30 requests/minute per buyer).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/admin/certificates:
    get:
      operationId: listConsentCertificatesAdmin
      tags:
        - "Consent: Certificates"
      summary: List consent certificates (admin session)
      description: |
        Lists TCPA consent certificates for the current tenant, newest first.
        Served by the consent app at `theleadrouter.com/consent/*` via a routing-app rewrite.

        Auth: reads the `lr_session` cookie and derives `{ tenantId, role }` from the JWT.
        **Known gap — this route does NOT reject unauthenticated requests.** When the cookie is
        missing or the JWT fails to verify, the handler falls back to `tenantId: null` (superadmin),
        and `withTenant` then returns rows across ALL tenants. There is no 401 path. Treat the
        `sessionCookie` scheme below as the intended auth, not the enforced one.
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - claimed
              - expired
              - revoked
          description: Filter by certificate lifecycle status.
        - name: accountId
          in: query
          schema:
            type: string
            format: uuid
          description: Filter by the consent account that owns the certificate.
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
          description: Filter to certificates created on or after this timestamp (matched against `createdAt`).
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
          description: Filter to certificates created on or before this timestamp (matched against `createdAt`).
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          description: Page size (values above 100 are clamped to 100).
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          description: Number of results to skip.
      responses:
        "200":
          description: Paginated list of certificates
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        accountId:
                          type: string
                          format: uuid
                        tenantId:
                          type: string
                          nullable: true
                        token:
                          type: string
                        pageUrl:
                          type: string
                        pageTitle:
                          type: string
                          nullable: true
                        referrer:
                          type: string
                          nullable: true
                        ip:
                          type: string
                        userAgent:
                          type: string
                          nullable: true
                        geo:
                          type: object
                          nullable: true
                          additionalProperties: true
                        eventCount:
                          type: integer
                        batchCount:
                          type: integer
                        hasSnapshot:
                          type: boolean
                        durationMs:
                          type: integer
                          nullable: true
                        interactionSummary:
                          type: object
                          nullable: true
                          additionalProperties: true
                        formData:
                          type: object
                          nullable: true
                          additionalProperties: true
                        status:
                          type: string
                          enum:
                            - active
                            - claimed
                            - expired
                            - revoked
                        claimedAt:
                          type: string
                          format: date-time
                          nullable: true
                        claimedBy:
                          type: string
                          nullable: true
                        claimIp:
                          type: string
                          nullable: true
                        expiresAt:
                          type: string
                          format: date-time
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "400":
          description: Unexpected error. This handler returns a `VALIDATION_ERROR` (HTTP 400) for any caught exception, not a 500.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/admin/certificates/{id}:
    get:
      operationId: getConsentCertificateAdmin
      tags:
        - "Consent: Certificates"
      summary: Get consent certificate detail (admin session)
      description: |
        Returns a single certificate for the current tenant plus its event batches (ordered by
        `batchIndex`), snapshots, and claim history. Served by the consent app via the
        `theleadrouter.com/consent/*` rewrite.

        Auth: same `lr_session` cookie derivation as the list endpoint, including the same gap — an
        unauthenticated request falls back to `tenantId: null` (cross-tenant read) rather than 401.
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Certificate UUID.
      responses:
        "200":
          description: Certificate detail with related records
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    description: The certificate row plus related records.
                    properties:
                      id:
                        type: string
                        format: uuid
                      accountId:
                        type: string
                        format: uuid
                      tenantId:
                        type: string
                        nullable: true
                      token:
                        type: string
                      pageUrl:
                        type: string
                      status:
                        type: string
                        enum:
                          - active
                          - claimed
                          - expired
                          - revoked
                      expiresAt:
                        type: string
                        format: date-time
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
                      eventBatches:
                        type: array
                        description: Event batches ordered by ascending batchIndex.
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            certificateId:
                              type: string
                              format: uuid
                            events:
                              type: array
                              items:
                                type: object
                                additionalProperties: true
                            eventCount:
                              type: integer
                            batchIndex:
                              type: integer
                            createdAt:
                              type: string
                              format: date-time
                      snapshots:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            certificateId:
                              type: string
                              format: uuid
                            type:
                              type: string
                              enum:
                                - page_load
                                - form_submit
                            content:
                              type: string
                            viewport:
                              type: object
                              nullable: true
                              additionalProperties: true
                            scrollPosition:
                              type: object
                              nullable: true
                              additionalProperties: true
                            createdAt:
                              type: string
                              format: date-time
                      claimHistory:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            certificateId:
                              type: string
                              format: uuid
                            claimedBy:
                              type: string
                            claimIp:
                              type: string
                              nullable: true
                            success:
                              type: boolean
                            failureReason:
                              type: string
                              nullable: true
                            certificateSnapshot:
                              type: object
                              nullable: true
                              additionalProperties: true
                            createdAt:
                              type: string
                              format: date-time
        "400":
          description: Unexpected error (returned as `VALIDATION_ERROR`, HTTP 400).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Certificate not found for this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/accounts:
    get:
      operationId: listConsentAccountsService
      tags:
        - "Consent: Accounts"
      summary: List consent accounts (service key)
      description: |
        Lists consent accounts, optionally filtered by `tenantId` and/or `partnerId`.
        Returns an unwrapped array under `data` — this endpoint has NO pagination envelope.

        Auth: a consent service key supplied as `Authorization: Bearer <key>`, validated against the
        `CONSENT_SERVICE_KEYS` env allowlist. This is a shared service-to-service key, NOT the partner
        `lr_xxx` credential the `bearerAuth` scheme documents (see report — a dedicated
        `consentServiceKey` scheme should be added).
      security:
        - bearerAuth: []
      parameters:
        - name: tenantId
          in: query
          schema:
            type: string
          description: Filter accounts by tenant.
        - name: partnerId
          in: query
          schema:
            type: string
          description: Filter accounts by partner.
      responses:
        "200":
          description: Accounts (no pagination envelope)
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        name:
                          type: string
                        accountKey:
                          type: string
                          description: Account key used by the tracking script to create certificates.
                        status:
                          type: string
                          enum:
                            - active
                            - suspended
                            - inactive
                        tenantId:
                          type: string
                          nullable: true
                        partnerId:
                          type: string
                          nullable: true
                        settings:
                          type: object
                          nullable: true
                          additionalProperties: true
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        "400":
          description: Unexpected error (returned as `VALIDATION_ERROR`, HTTP 400).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createConsentAccountService
      tags:
        - "Consent: Accounts"
      summary: Create consent account (service key)
      description: |
        Creates a consent account and generates a random `accountKey` (used by the browser tracking
        script to create certificates). Enforces a uniqueness constraint on the `(tenantId, partnerId)`
        pair — a null `partnerId` is treated as a distinct slot.

        Auth: consent service key via `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
        Body validation is manual (`name` and `tenantId` required); there is no Zod schema on this route.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - tenantId
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Human-readable account name.
                tenantId:
                  type: string
                  description: Owning tenant id.
                partnerId:
                  type: string
                  nullable: true
                  description: Optional partner id. Null occupies its own uniqueness slot per tenant.
            example:
              name: Acme Landing Pages
              tenantId: 11111111-1111-1111-1111-111111111111
              partnerId: null
      responses:
        "201":
          description: Account created
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      name:
                        type: string
                      accountKey:
                        type: string
                        description: Account key used by the tracking script to create certificates.
                      status:
                        type: string
                        enum:
                          - active
                          - suspended
                          - inactive
                      tenantId:
                        type: string
                        nullable: true
                      partnerId:
                        type: string
                        nullable: true
                      settings:
                        type: object
                        nullable: true
                        additionalProperties: true
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        "400":
          description: Validation failed (`name is required` or `tenantId is required`) or unexpected error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: An account already exists for this tenant/partner combination
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/accounts/by-tenant/{tenantId}:
    get:
      operationId: getConsentAccountByTenant
      tags:
        - "Consent: Accounts"
      summary: Get consent account by tenant (service key)
      description: |
        Returns the first consent account matching the given tenant. Auth: consent service key via
        `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
      security:
        - bearerAuth: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
          description: Tenant id.
      responses:
        "200":
          description: Account
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      name:
                        type: string
                      accountKey:
                        type: string
                        description: Account key used by the tracking script to create certificates.
                      status:
                        type: string
                        enum:
                          - active
                          - suspended
                          - inactive
                      tenantId:
                        type: string
                        nullable: true
                      partnerId:
                        type: string
                        nullable: true
                      settings:
                        type: object
                        nullable: true
                        additionalProperties: true
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        "400":
          description: Unexpected error (returned as `VALIDATION_ERROR`, HTTP 400).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: No account for this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/accounts/by-tenant/{tenantId}/partner/{partnerId}:
    get:
      operationId: getConsentAccountByTenantPartner
      tags:
        - "Consent: Accounts"
      summary: Get consent account by tenant and partner (service key)
      description: |
        Returns the consent account matching both tenant and partner. Auth: consent service key via
        `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
      security:
        - bearerAuth: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
          description: Tenant id.
        - name: partnerId
          in: path
          required: true
          schema:
            type: string
          description: Partner id.
      responses:
        "200":
          description: Account
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      name:
                        type: string
                      accountKey:
                        type: string
                        description: Account key used by the tracking script to create certificates.
                      status:
                        type: string
                        enum:
                          - active
                          - suspended
                          - inactive
                      tenantId:
                        type: string
                        nullable: true
                      partnerId:
                        type: string
                        nullable: true
                      settings:
                        type: object
                        nullable: true
                        additionalProperties: true
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: No account for this tenant/partner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/admin/certificates:
    get:
      operationId: listConsentCertificatesService
      tags:
        - "Consent: Certificates"
      summary: List consent certificates (service key)
      description: |
        Service-to-service certificate listing scoped to a required `tenantId` query param, newest
        first. Unlike the admin-session list, `tenantId` is mandatory here and tenant scoping is explicit.

        Auth: consent service key via `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
      security:
        - bearerAuth: []
      parameters:
        - name: tenantId
          in: query
          required: true
          schema:
            type: string
          description: Tenant to scope results to (required).
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - claimed
              - expired
              - revoked
          description: Filter by certificate lifecycle status.
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
          description: Filter to certificates created on or after this timestamp.
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
          description: Filter to certificates created on or before this timestamp.
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          description: Page size (clamped to 100).
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          description: Number of results to skip.
      responses:
        "200":
          description: Paginated list of certificates
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - pagination
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: Certificate row (see the admin list endpoint for the full field set).
                      properties:
                        id:
                          type: string
                          format: uuid
                        accountId:
                          type: string
                          format: uuid
                        tenantId:
                          type: string
                          nullable: true
                        token:
                          type: string
                        pageUrl:
                          type: string
                        status:
                          type: string
                          enum:
                            - active
                            - claimed
                            - expired
                            - revoked
                        expiresAt:
                          type: string
                          format: date-time
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
        "400":
          description: Missing `tenantId` query param, or unexpected error (both returned as `VALIDATION_ERROR`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /consent/api/v1/admin/certificates/{id}:
    get:
      operationId: getConsentCertificateService
      tags:
        - "Consent: Certificates"
      summary: Get consent certificate detail (service key)
      description: |
        Returns a certificate (looked up by UUID, not tenant-scoped) plus its event batches (ordered
        by `batchIndex`), snapshots, and claim history.

        Auth: consent service key via `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Certificate UUID.
      responses:
        "200":
          description: Certificate detail with related records (same shape as the admin-session detail endpoint).
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    additionalProperties: true
                    description: Certificate row plus `eventBatches`, `snapshots`, and `claimHistory` arrays.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Certificate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/certificates:
    post:
      operationId: createConsentCertificate
      tags:
        - "Consent: Certificates"
      summary: Create a consent certificate
      description: |
        Public endpoint called by the browser consent tracking script to open a new certificate
        recording session. Returns the certificate id, its token, and the hosted certificate URL.
        The captured client IP (from `x-forwarded-for`) is recorded on the certificate.

        Auth: no header credential. The caller is authenticated by the `accountKey` in the request
        body, which must map to an `active` consent account. Rate limited by IP (10/min) and by
        `accountKey` (1000/min); either limit returns 429. An `OPTIONS` preflight returns 204.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - accountKey
                - pageUrl
              properties:
                accountKey:
                  type: string
                  minLength: 1
                  description: Consent account key identifying the account (acts as the credential).
                pageUrl:
                  type: string
                  format: uri
                  description: URL of the page where consent is being captured.
                referrer:
                  type: string
                  default: ""
                  description: Referring URL, if any.
                userAgent:
                  type: string
                  default: ""
                  description: Client user-agent string.
            example:
              accountKey: 3f8c1e6a-9b2d-4c7e-a1f0-5d6b8e2c9a44
              pageUrl: https://example.com/quote?utm_source=fb
              referrer: https://facebook.com/
              userAgent: Mozilla/5.0
      responses:
        "201":
          description: Certificate created
          content:
            application/json:
              schema:
                type: object
                required:
                  - certificateId
                  - token
                  - certUrl
                properties:
                  certificateId:
                    type: string
                    format: uuid
                  token:
                    type: string
                    description: Opaque certificate token used in the hosted URL and by the tracking script.
                  certUrl:
                    type: string
                    format: uri
                    description: Hosted certificate URL (`${CONSENT_HOST}/c/{token}`).
        "400":
          description: Invalid request data (Zod validation) or unexpected error, both as `VALIDATION_ERROR`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Invalid account key (no active account matches `accountKey`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded (per-IP or per-accountKey)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/certificates/{id}:
    get:
      operationId: getConsentCertificatePublic
      tags:
        - "Consent: Certificates"
      summary: Verify / retrieve a consent certificate (service key)
      description: |
        Retrieves and verifies a certificate. The `{id}` segment accepts either the certificate UUID
        or its token (the underlying lookup resolves both). If an `active` certificate is past its
        `expiresAt`, it is transitioned to `expired` and returned with that status.

        Auth: consent service key via `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Certificate UUID or token.
      responses:
        "200":
          description: Verified certificate
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    additionalProperties: true
                    description: Full certificate row.
                    properties:
                      id:
                        type: string
                        format: uuid
                      token:
                        type: string
                      status:
                        type: string
                        enum:
                          - active
                          - claimed
                          - expired
                          - revoked
                      expiresAt:
                        type: string
                        format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Certificate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/certificates/{id}/claim:
    post:
      operationId: claimConsentCertificate
      tags:
        - "Consent: Certificates"
      summary: Claim a consent certificate (service key)
      description: |
        Claims a certificate, transitioning it from `active` to `claimed` and recording who claimed it.
        The `{id}` segment accepts the certificate UUID or token. When `tenantId` is supplied in the
        body, ownership is enforced — a mismatch returns 403. Every attempt (success or failure) writes
        a `claimLog` row.

        Auth: consent service key via `Authorization: Bearer <key>` (`CONSENT_SERVICE_KEYS`).
        Note: this route does not wrap body parsing in try/catch, so a malformed body (Zod failure)
        surfaces as an unhandled 500 rather than a 400 (see report).
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Certificate UUID or token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - claimedBy
              properties:
                claimedBy:
                  type: string
                  minLength: 1
                  description: Identifier of the entity claiming the certificate (e.g. leadId).
                tenantId:
                  type: string
                  description: Optional. When present, the certificate's tenant must match or the claim is rejected with 403.
            example:
              claimedBy: lead_9f2a
              tenantId: 11111111-1111-1111-1111-111111111111
      responses:
        "200":
          description: Certificate claimed
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    additionalProperties: true
                    description: The updated certificate row (status `claimed`).
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - claimed
                      claimedAt:
                        type: string
                        format: date-time
                      claimedBy:
                        type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Certificate belongs to a different tenant (code `FORBIDDEN`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Certificate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Certificate already claimed (code `DUPLICATE`)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: |
            Certificate is gone — expired or revoked. Returns a non-standard error `code: GONE`, which
            is NOT in the shared `ErrorResponse` code enum (see report).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: object
                    properties:
                      code:
                        type: string
                        example: GONE
                      message:
                        type: string
  /consent/api/v1/certificates/{id}/events:
    post:
      operationId: ingestConsentCertificateEvents
      tags:
        - "Consent: Certificates"
      summary: Ingest a certificate event batch
      description: |
        Public endpoint called by the browser consent tracking script to append a batch of interaction
        events (key/mouse/click/scroll/focus/other) to an `active` certificate. Returns 204 on success.
        Rate limited to 100 batches/hour per certificate. An `OPTIONS` preflight returns 204.

        Auth: none — the caller is scoped only by possession of the certificate `{id}`. There is no
        header or body credential (see report).
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Certificate UUID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - events
                - batchIndex
              properties:
                events:
                  type: array
                  maxItems: 200
                  description: Compact interaction events for this batch.
                  items:
                    type: object
                    required:
                      - t
                      - ts
                      - d
                    properties:
                      t:
                        type: string
                        enum:
                          - k
                          - m
                          - c
                          - s
                          - f
                          - d
                        description: Event type tag (k=key, m=mouse, c=click, s=scroll, f=focus, d=other).
                      ts:
                        type: number
                        description: Event timestamp (epoch ms).
                      d:
                        description: Event payload (shape varies by event type).
                batchIndex:
                  type: integer
                  minimum: 0
                  description: Zero-based index of this batch in the certificate's event stream.
      responses:
        "204":
          description: Batch accepted (no body)
        "400":
          description: Invalid event data (Zod) or certificate not active (`Certificate not active`), or unexpected error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Certificate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded (100 batches/hour per certificate)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /consent/api/v1/certificates/{id}/snapshot:
    post:
      operationId: ingestConsentCertificateSnapshot
      tags:
        - "Consent: Certificates"
      summary: Ingest a certificate DOM snapshot
      description: |
        Public endpoint called by the browser consent tracking script to attach a DOM snapshot
        (page load or form submit) to an `active` certificate. Content is capped at 1MB. Returns 204 on
        success. An `OPTIONS` preflight returns 204.

        Auth: none — scoped only by possession of the certificate `{id}` (see report).
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Certificate UUID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - content
              properties:
                type:
                  type: string
                  enum:
                    - page_load
                    - form_submit
                  description: Snapshot trigger.
                content:
                  type: string
                  maxLength: 1048576
                  description: Serialized DOM snapshot (max 1MB).
                viewport:
                  type: object
                  properties:
                    w:
                      type: number
                    h:
                      type: number
                  description: Viewport dimensions.
                scrollPosition:
                  type: object
                  properties:
                    x:
                      type: number
                    y:
                      type: number
                  description: Scroll offset at capture time.
      responses:
        "204":
          description: Snapshot accepted (no body)
        "400":
          description: Invalid snapshot data (Zod), certificate not active, or processing failure — all `VALIDATION_ERROR`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Certificate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "413":
          description: Snapshot content exceeds 1MB
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics:
    get:
      summary: Messaging analytics overview
      description: |
        Aggregated send/engagement/revenue totals plus a grouped daily breakdown, sourced from the
        `analyticsDaily` rollup table. Defaults to the last 30 days when no date range is supplied.

        Served by the messaging app (`apps/messaging`) at `/messaging/*` via routing-app rewrites.
        Auth is the shared `lr_session` session cookie (forwarded through the rewrite); the handler
        requires an `admin` or `superadmin` role and scopes every query to the caller's active
        organization.
      operationId: getMessagingAnalyticsOverview
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          description: Lower bound on the rollup date (ISO date/date-time string). Defaults to 30 days ago.
          schema:
            type: string
        - name: dateTo
          in: query
          description: Upper bound on the rollup date (ISO date/date-time string). Defaults to now.
          schema:
            type: string
        - name: channel
          in: query
          description: Restrict to a single channel.
          schema:
            type: string
            enum:
              - email
              - sms
        - name: groupBy
          in: query
          description: Dimension for the daily breakdown grouping.
          schema:
            type: string
            enum:
              - date
              - channel
              - sourceType
            default: date
      responses:
        "200":
          description: Summary totals and grouped breakdown
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    type: object
                    properties:
                      totalSent:
                        type: integer
                      totalDelivered:
                        type: integer
                      totalOpened:
                        type: integer
                      totalClicked:
                        type: integer
                      totalBounced:
                        type: integer
                      totalRevenue:
                        type: string
                        description: Decimal revenue as a string.
                      openRate:
                        type: number
                      clickRate:
                        type: number
                      bounceRate:
                        type: number
                  daily:
                    type: array
                    items:
                      type: object
                      properties:
                        groupKey:
                          nullable: true
                          description: Value of the grouped dimension (date, channel, or sourceType).
                        sent:
                          type: integer
                        delivered:
                          type: integer
                        opened:
                          type: integer
                        clicked:
                          type: integer
                        bounced:
                          type: integer
                        complained:
                          type: integer
                        revenue:
                          type: string
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized (missing/invalid session or no active organization)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/campaigns:
    get:
      summary: Per-campaign analytics
      description: |
        Send/engagement/revenue totals grouped by campaign (`analyticsDaily.sourceType = 'campaign'`),
        joined to campaign names. Not paginated. Defaults to the last 30 days.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingCampaignAnalytics
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
      responses:
        "200":
          description: Campaign analytics rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        campaignId:
                          type: string
                          format: uuid
                          nullable: true
                        name:
                          type: string
                        sent:
                          type: integer
                        delivered:
                          type: integer
                        opened:
                          type: integer
                        clicked:
                          type: integer
                        bounced:
                          type: integer
                        revenue:
                          type: string
                        openRate:
                          type: number
                        clickRate:
                          type: number
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/campaigns/{id}:
    get:
      summary: Single-campaign analytics detail
      description: |
        Summary totals plus a per-day breakdown for one campaign. Defaults to the last 30 days.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingCampaignAnalyticsDetail
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
      responses:
        "200":
          description: Campaign analytics summary and daily rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    type: object
                    properties:
                      totalSent:
                        type: integer
                      totalDelivered:
                        type: integer
                      totalOpened:
                        type: integer
                      totalClicked:
                        type: integer
                      totalBounced:
                        type: integer
                      totalRevenue:
                        type: string
                      openRate:
                        type: number
                      clickRate:
                        type: number
                      bounceRate:
                        type: number
                  daily:
                    type: array
                    items:
                      type: object
                      properties:
                        date:
                          type: string
                          format: date-time
                        channel:
                          type: string
                        sent:
                          type: integer
                        delivered:
                          type: integer
                        opened:
                          type: integer
                        clicked:
                          type: integer
                        bounced:
                          type: integer
                        complained:
                          type: integer
                        revenue:
                          type: string
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/deliverability:
    get:
      summary: Deliverability analytics
      description: |
        Delivery/bounce/complaint rates computed live from the `message` table, with per-provider
        breakdown, a daily time series, and threshold alerts (complaint > 0.1%, hard bounce > 5%,
        delivery < 90%). Defaults to the last 30 days. Only `dateFrom`/`dateTo` are honored.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingDeliverabilityAnalytics
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Deliverability metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deliveryRate:
                        type: number
                      hardBounceRate:
                        type: number
                      softBounceRate:
                        type: number
                      complaintRate:
                        type: number
                      totalSent:
                        type: integer
                      totalDelivered:
                        type: integer
                      totalBounced:
                        type: integer
                      hardBounceCount:
                        type: integer
                      softBounceCount:
                        type: integer
                      pendingRetryCount:
                        type: integer
                      totalComplaints:
                        type: integer
                      totalOpened:
                        type: integer
                      uniqueOpenRate:
                        type: number
                      alerts:
                        type: array
                        items:
                          type: string
                      providerBreakdown:
                        type: array
                        items:
                          type: object
                          properties:
                            provider:
                              type: string
                            sent:
                              type: integer
                            delivered:
                              type: integer
                            bounced:
                              type: integer
                            complained:
                              type: integer
                      timeSeries:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                            deliveryRate:
                              type: number
                            bounceRate:
                              type: number
                            complaintRate:
                              type: number
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/export:
    get:
      summary: Export analytics as CSV
      description: |
        Streams the raw `analyticsDaily` rows for the selected range as a CSV attachment
        (`Content-Type: text/csv`, `Content-Disposition: attachment; filename="analytics-export.csv"`).
        Defaults to the last 30 days.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: exportMessagingAnalytics
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
      responses:
        "200":
          description: CSV export
          content:
            text/csv:
              schema:
                type: string
              example: |
                Date,Channel,Source Type,Source ID,Sent,Delivered,Opened,Clicked,Bounced,Complained,Unsubscribed,Revenue
                2026-06-01,email,campaign,3f8a...,1200,1180,540,120,15,1,0,320.00
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/flows:
    get:
      summary: Per-flow analytics
      description: |
        Send/engagement/revenue totals grouped by flow (`analyticsDaily.sourceType = 'flow'`),
        joined to flow names. Not paginated. Defaults to the last 30 days.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingFlowAnalytics
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
      responses:
        "200":
          description: Flow analytics rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        flowId:
                          type: string
                          format: uuid
                          nullable: true
                        name:
                          type: string
                        sent:
                          type: integer
                        delivered:
                          type: integer
                        opened:
                          type: integer
                        clicked:
                          type: integer
                        bounced:
                          type: integer
                        revenue:
                          type: string
                        openRate:
                          type: number
                        clickRate:
                          type: number
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/flows/{id}:
    get:
      summary: Single-flow analytics detail
      description: |
        Summary totals plus a per-day breakdown for one flow. Defaults to the last 30 days.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingFlowAnalyticsDetail
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
      responses:
        "200":
          description: Flow analytics summary and daily rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    type: object
                    properties:
                      totalSent:
                        type: integer
                      totalDelivered:
                        type: integer
                      totalOpened:
                        type: integer
                      totalClicked:
                        type: integer
                      totalBounced:
                        type: integer
                      totalRevenue:
                        type: string
                      openRate:
                        type: number
                      clickRate:
                        type: number
                      bounceRate:
                        type: number
                  daily:
                    type: array
                    items:
                      type: object
                      properties:
                        date:
                          type: string
                          format: date-time
                        channel:
                          type: string
                        sent:
                          type: integer
                        delivered:
                          type: integer
                        opened:
                          type: integer
                        clicked:
                          type: integer
                        bounced:
                          type: integer
                        complained:
                          type: integer
                        revenue:
                          type: string
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/mailbox-providers:
    get:
      summary: Mailbox provider (ISP) analytics
      description: |
        Deliverability rolled up by mailbox provider. Recipient domains from the `message` table are
        classified into ISPs (Gmail, Yahoo, etc.) in application code, then aggregated into a summary,
        a bounce/block comparison vs. the previous equal-length period, and daily delivery and open
        trends. Defaults to the last 30 days. Only `dateFrom`/`dateTo` are honored.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingMailboxProviderAnalytics
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Provider-level deliverability metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      summary:
                        type: array
                        items:
                          type: object
                          properties:
                            provider:
                              type: string
                            sent:
                              type: integer
                            delivered:
                              type: integer
                            bounced:
                              type: integer
                            blocked:
                              type: integer
                            opened:
                              type: integer
                            complained:
                              type: integer
                            deliveryPct:
                              type: number
                            bouncePct:
                              type: number
                            blockPct:
                              type: number
                            openPct:
                              type: number
                      bounceByProvider:
                        type: array
                        items:
                          type: object
                          properties:
                            provider:
                              type: string
                            bounceRate:
                              type: number
                            blockRate:
                              type: number
                            previousBounceRate:
                              type: number
                            previousBlockRate:
                              type: number
                      deliveryTrend:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                            providers:
                              type: object
                              additionalProperties:
                                type: number
                      openTrend:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                            providers:
                              type: object
                              additionalProperties:
                                type: number
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/analytics/revenue:
    get:
      summary: Revenue analytics
      description: |
        Attributed revenue from the `message` table: totals by channel, conversion count, top 10
        campaigns and top 10 flows by revenue, and a daily revenue time series over days with
        conversions. Defaults to the last 30 days. Only `dateFrom`/`dateTo` are honored.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingRevenueAnalytics
      tags:
        - "Messaging: Analytics"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Revenue metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      totalRevenue:
                        type: number
                      emailRevenue:
                        type: number
                      smsRevenue:
                        type: number
                      conversionCount:
                        type: integer
                      topCampaigns:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                              nullable: true
                            name:
                              type: string
                            revenue:
                              type: number
                            conversions:
                              type: integer
                      topFlows:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                              nullable: true
                            name:
                              type: string
                            revenue:
                              type: number
                            conversions:
                              type: integer
                      timeSeries:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                            revenue:
                              type: number
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns:
    get:
      summary: List messaging campaigns
      description: |
        Paginated list of email/SMS campaigns with template and segment names joined in. Ordered by
        `createdAt` descending.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: listMessagingCampaigns
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
        - name: status
          in: query
          schema:
            type: string
            enum:
              - draft
              - scheduled
              - sending
              - paused
              - completed
              - cancelled
        - name: segmentId
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          description: Partial match on campaign name.
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated campaign list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a messaging campaign
      description: |
        Creates a campaign in `draft` status. Both `templateId` and `segmentId` must reference rows in
        the caller's org or a `VALIDATION_ERROR` is returned.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: createMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - channel
                - templateId
                - segmentId
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                description:
                  type: string
                  maxLength: 1000
                  nullable: true
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                templateId:
                  type: string
                  format: uuid
                segmentId:
                  type: string
                  format: uuid
                scheduledAt:
                  type: string
                  format: date-time
                  nullable: true
                abTestConfig:
                  type: object
                  nullable: true
                  required:
                    - variants
                  properties:
                    testType:
                      type: string
                      enum:
                        - template
                        - subject_only
                      default: template
                    variants:
                      type: array
                      minItems: 2
                      items:
                        type: object
                        required:
                          - name
                          - weight
                        properties:
                          name:
                            type: string
                            minLength: 1
                            maxLength: 100
                          templateId:
                            type: string
                            format: uuid
                          weight:
                            type: number
                            minimum: 0
                            maximum: 100
                          subject:
                            type: string
                    testPercent:
                      type: integer
                      minimum: 1
                      maximum: 100
                      default: 20
                    winnerMetric:
                      type: string
                      enum:
                        - open_rate
                        - click_rate
                        - revenue
                      default: open_rate
                    winnerAfterHours:
                      type: integer
                      minimum: 1
                      maximum: 168
                      default: 4
                attributionWindowDays:
                  type: integer
                  minimum: 1
                  maximum: 90
                  default: 7
                smartSendingEnabled:
                  type: boolean
                  default: true
                timezoneSendingEnabled:
                  type: boolean
                  default: false
      responses:
        "201":
          description: Campaign created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Validation error (invalid body, template not found, or segment not found)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns/{id}:
    get:
      summary: Get a messaging campaign
      description: |
        Returns one campaign (template/segment names joined) plus a `sendStats` map of `campaignSend`
        row counts keyed by send status.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Campaign detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/MessagingCampaign"
                      - type: object
                        properties:
                          sendStats:
                            type: object
                            additionalProperties:
                              type: integer
                            description: Count of campaignSend rows per status (queued, sent, failed, skipped, on_hold).
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update a messaging campaign
      description: |
        Partial update. Only allowed while the campaign is in `draft` or `scheduled` status; any other
        status returns a `VALIDATION_ERROR`. Provided `templateId`/`segmentId` are validated against
        the caller's org.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: updateMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        description: All campaign create fields, all optional (partial update).
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                description:
                  type: string
                  maxLength: 1000
                  nullable: true
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                templateId:
                  type: string
                  format: uuid
                segmentId:
                  type: string
                  format: uuid
                scheduledAt:
                  type: string
                  format: date-time
                  nullable: true
                abTestConfig:
                  type: object
                  nullable: true
                attributionWindowDays:
                  type: integer
                  minimum: 1
                  maximum: 90
                smartSendingEnabled:
                  type: boolean
                timezoneSendingEnabled:
                  type: boolean
      responses:
        "200":
          description: Updated campaign
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Validation error (bad body, wrong status, template/segment not found)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a messaging campaign
      description: |
        Hard-deletes a campaign. Only allowed while in `draft` or `cancelled` status; any other status
        returns a `VALIDATION_ERROR`.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: deleteMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Deletion confirmation
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deleted:
                        type: boolean
        "400":
          description: Validation error (campaign not in draft/cancelled status)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns/{id}/cancel:
    post:
      summary: Cancel a campaign
      description: |
        Transitions the campaign to `cancelled` (valid from `scheduled`, `sending`, or `paused`) and
        marks any remaining `queued` sends as `skipped`. Invalid transitions return a `VALIDATION_ERROR`.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: cancelMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Updated campaign
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns/{id}/pause:
    post:
      summary: Pause a campaign
      description: |
        Transitions the campaign to `paused` (valid only from `sending`). Invalid transitions return a
        `VALIDATION_ERROR`.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: pauseMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Updated campaign
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns/{id}/resume:
    post:
      summary: Resume a campaign
      description: |
        Transitions the campaign back to `sending` (valid from `paused`, or a fresh start from `draft`).
        Invalid transitions return a `VALIDATION_ERROR`. Note: the handler only flips status; it does
        not re-queue sends.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: resumeMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Updated campaign
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns/{id}/schedule:
    post:
      summary: Schedule a campaign
      description: |
        Sets `scheduledAt` and transitions the campaign to `scheduled` (valid from `draft`).
        `scheduledAt` must be a future ISO date-time; otherwise a `VALIDATION_ERROR` is returned.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: scheduleMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - scheduledAt
              properties:
                scheduledAt:
                  type: string
                  format: date-time
                  description: Future send time (ISO8601).
      responses:
        "200":
          description: Updated campaign
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingCampaign"
        "400":
          description: Validation error (invalid body, non-future time, or invalid transition)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/campaigns/{id}/send:
    post:
      summary: Send a campaign now
      description: |
        Evaluates the campaign's segment, filters out suppressed contacts, and creates `campaignSend`
        rows (splitting into a test/hold group when an A/B config is present), then transitions the
        campaign to `sending`. Requires the campaign to have a segment and template. Returns send counts
        (this endpoint does not use the standard `data` envelope). An empty segment returns all-zero
        counts without changing status.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: sendMessagingCampaign
      tags:
        - "Messaging: Campaigns"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Send counts
          content:
            application/json:
              schema:
                type: object
                properties:
                  recipients:
                    type: integer
                    description: Eligible contacts after suppression filtering.
                  queued:
                    type: integer
                    description: Sends queued immediately (test group for A/B, else all eligible).
                  onHold:
                    type: integer
                    description: Sends held for A/B winner selection (0 when no A/B config).
                  suppressed:
                    type: integer
                    description: Contacts skipped due to the suppression list.
        "400":
          description: Validation error (missing segment/template or invalid transition)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/contacts:
    get:
      summary: List messaging contacts
      description: |
        Paginated list of contacts with their tags attached. Supports exact email/phone lookup, status
        filters, a tag filter, and free-text search across email, name, and phone. Ordered by
        `createdAt` descending.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: listMessagingContacts
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - name: email
          in: query
          description: Exact email match.
          schema:
            type: string
        - name: phone
          in: query
          description: Exact phone match.
          schema:
            type: string
        - name: emailStatus
          in: query
          schema:
            type: string
            enum:
              - active
              - bounced
              - complained
              - unsubscribed
        - name: smsStatus
          in: query
          schema:
            type: string
            enum:
              - active
              - opted_out
        - name: tag
          in: query
          description: Only contacts carrying this tag.
          schema:
            type: string
        - name: search
          in: query
          description: Free-text search across email, first name, last name, phone.
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated contact list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MessagingContact"
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a messaging contact
      description: |
        Creates a contact (email is required at runtime despite being optional in the shared create
        schema — the DB column is NOT NULL), inserts any tags and properties, auto-detects timezone from
        state/zip in properties, and logs a timeline event.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: createMessagingContact
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                externalId:
                  type: string
                  maxLength: 255
                  nullable: true
                  description: Stored as routingLeadId.
                email:
                  type: string
                  format: email
                  maxLength: 255
                  nullable: true
                  description: Required at runtime.
                phone:
                  type: string
                  maxLength: 20
                  nullable: true
                firstName:
                  type: string
                  maxLength: 100
                  nullable: true
                lastName:
                  type: string
                  maxLength: 100
                  nullable: true
                emailStatus:
                  type: string
                  enum:
                    - active
                    - bounced
                    - complained
                    - unsubscribed
                  default: active
                smsStatus:
                  type: string
                  enum:
                    - active
                    - opted_out
                  default: active
                properties:
                  type: object
                  additionalProperties: true
                  default: {}
                tags:
                  type: array
                  items:
                    type: string
                    maxLength: 100
                  default: []
                source:
                  type: string
                  maxLength: 100
                  nullable: true
      responses:
        "201":
          description: Contact created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingContact"
        "400":
          description: Validation error (invalid body or missing email)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/contacts/import:
    post:
      summary: Import contacts from CSV
      description: |
        Multipart upload of a `.csv` file. The header must include an `email` column. Known columns
        (`email`, `phone`, `firstName`/`first_name`, `lastName`/`last_name`, `tags`) map to contact
        fields; `tags` is comma-separated; all other columns become contact properties. Rows upsert by
        `(orgId, email)`. Returns import counts and up to 20 per-row errors.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: importMessagingContacts
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV file with an `email` header column.
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      imported:
                        type: integer
                      updated:
                        type: integer
                      errors:
                        type: integer
                      total:
                        type: integer
                  errorDetails:
                    type: array
                    description: Present only when there were row errors (first 20).
                    items:
                      type: object
                      properties:
                        row:
                          type: integer
                        error:
                          type: string
        "400":
          description: Validation error (no file, non-CSV, missing header/email column, empty file)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/contacts/{id}:
    get:
      summary: Get a messaging contact
      description: |
        Returns one contact with its tags and a `properties` map (from the `contactProperty` table).

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: getMessagingContact
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Contact detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingContact"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contact not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update a messaging contact
      description: |
        Partial update. `externalId` maps to `routingLeadId`. When `properties` are supplied, tags are
        replaced wholesale and properties are upserted; timezone is re-detected from state/zip. Logs a
        timeline event.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: updateMessagingContact
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        description: All contact create fields, all optional (partial update).
        content:
          application/json:
            schema:
              type: object
              properties:
                externalId:
                  type: string
                  maxLength: 255
                  nullable: true
                email:
                  type: string
                  format: email
                  maxLength: 255
                  nullable: true
                phone:
                  type: string
                  maxLength: 20
                  nullable: true
                firstName:
                  type: string
                  maxLength: 100
                  nullable: true
                lastName:
                  type: string
                  maxLength: 100
                  nullable: true
                emailStatus:
                  type: string
                  enum:
                    - active
                    - bounced
                    - complained
                    - unsubscribed
                smsStatus:
                  type: string
                  enum:
                    - active
                    - opted_out
                properties:
                  type: object
                  additionalProperties: true
                tags:
                  type: array
                  items:
                    type: string
                    maxLength: 100
      responses:
        "200":
          description: Updated contact
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MessagingContact"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contact not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a messaging contact
      description: |
        Hard-deletes a contact.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: deleteMessagingContact
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Deletion confirmation
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deleted:
                        type: boolean
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contact not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/contacts/{id}/messages:
    get:
      summary: List a contact's messages
      description: |
        Paginated message history for one contact, with source (campaign/flow/API) name resolved and
        subject falling back to the template subject/name. Ordered by `createdAt` descending.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: listMessagingContactMessages
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated message list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            channel:
                              type: string
                            subject:
                              type: string
                              nullable: true
                            status:
                              type: string
                            sourceType:
                              type: string
                              nullable: true
                            sourceName:
                              type: string
                              nullable: true
                            sentAt:
                              type: string
                              format: date-time
                              nullable: true
                            deliveredAt:
                              type: string
                              format: date-time
                              nullable: true
                            openedAt:
                              type: string
                              format: date-time
                              nullable: true
                            clickedAt:
                              type: string
                              format: date-time
                              nullable: true
                            bouncedAt:
                              type: string
                              format: date-time
                              nullable: true
                            createdAt:
                              type: string
                              format: date-time
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/contacts/{id}/tags:
    get:
      summary: List a contact's tags
      description: |
        Returns tag rows for a contact. Returns 404 if the contact does not exist in the org.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: listMessagingContactTags
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      responses:
        "200":
          description: Tag rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        tag:
                          type: string
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contact not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Add a tag to a contact
      description: |
        Adds a single tag. Returns a `VALIDATION_ERROR` if the tag already exists on the contact, or 404
        if the contact does not exist in the org.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: addMessagingContactTag
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tag
              properties:
                tag:
                  type: string
                  minLength: 1
                  maxLength: 100
      responses:
        "201":
          description: Tag added
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      contactId:
                        type: string
                        format: uuid
                      tag:
                        type: string
        "400":
          description: Validation error (bad body or duplicate tag)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contact not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Remove a tag from a contact
      description: |
        Removes a tag identified by the `tag` query parameter. Returns 404 if the contact or the tag
        does not exist.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: removeMessagingContactTag
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
        - name: tag
          in: query
          required: true
          description: Tag to remove.
          schema:
            type: string
      responses:
        "200":
          description: Deletion confirmation
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deleted:
                        type: boolean
        "400":
          description: Missing `tag` query parameter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Contact or tag not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/contacts/{id}/timeline:
    get:
      summary: List a contact's timeline
      description: |
        Paginated timeline of contact events (created, updated, engagement, etc.) from the
        `contactTimeline` table. Ordered by `createdAt` descending.

        Messaging app endpoint; `lr_session` cookie auth, admin/superadmin only, org-scoped.
      operationId: listMessagingContactTimeline
      tags:
        - "Messaging: Contacts"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/pathId"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated timeline events
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            eventType:
                              type: string
                            title:
                              type: string
                              nullable: true
                            detail:
                              type: string
                              nullable: true
                            metadata:
                              type: object
                              additionalProperties: true
                              nullable: true
                            createdAt:
                              type: string
                              format: date-time
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/flows:
    get:
      summary: List messaging flows
      description: |
        Returns a paginated list of automation flows for the authenticated org, with a `stepCount` per flow.

        Served by the messaging app (`apps/messaging`) under its `/messaging` basePath and reached externally at
        `theleadrouter.com/messaging/...` via routing-app rewrites. Auth is the shared `lr_session` session cookie,
        forwarded through the rewrite; the handler requires an `admin` or `superadmin` role. Org scoping is derived
        from the session (`orgId`), so there is no org path/query parameter.
      operationId: listMessagingFlows
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by flow status
          schema:
            type: string
            enum:
              - draft
              - active
              - paused
              - archived
        - name: triggerType
          in: query
          description: Filter by trigger type
          schema:
            type: string
            enum:
              - event
              - segment_entry
              - manual
              - date_property
        - name: search
          in: query
          description: Case-insensitive partial match on flow name
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of flows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 6f9b2c1a-1111-2222-3333-444455556666
                    orgId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Welcome Series
                    description: Onboarding drip for new contacts
                    triggerType: segment_entry
                    triggerConfig:
                      segmentIds:
                        - 7a8b9c0d-1111-2222-3333-444455556666
                    status: active
                    attributionWindowDays: 7
                    totalEntered: 120
                    totalCompleted: 84
                    totalRevenue: "0.00"
                    stepCount: 4
                    createdAt: 2025-01-15 10:00:00+00:00
                    updatedAt: 2025-01-15 10:00:00+00:00
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized (missing or invalid session)
        "403":
          description: Forbidden (non-admin role)
    post:
      summary: Create a messaging flow
      description: |
        Creates a new automation flow. Optionally pass `templateId` to seed the flow (and its steps) from a built-in
        flow template; template fields fill any omitted body fields, and body fields override template defaults.
        New flows are always created with `status: draft` regardless of any status supplied. Trigger fields
        (`triggerEvent`, `triggerSegmentIds`/`triggerSegmentId`, `triggerDateProperty`, etc.) are folded into the
        stored `triggerConfig` object.
      operationId: createMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - triggerType
              properties:
                templateId:
                  type: string
                  description: Optional built-in flow template id to seed name/description/trigger/steps. When present, template values
                    fill omitted fields.
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                  description: Required unless supplied by the selected template.
                description:
                  type: string
                  maxLength: 1000
                  nullable: true
                triggerType:
                  type: string
                  enum:
                    - event
                    - segment_entry
                    - manual
                    - date_property
                triggerEvent:
                  type: string
                  maxLength: 255
                  nullable: true
                  description: Event name that starts the flow (triggerType=event).
                triggerSegmentId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Legacy single segment id (triggerType=segment_entry). Prefer triggerSegmentIds.
                triggerSegmentIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                  description: Segment ids that trigger on entry (triggerType=segment_entry).
                triggerDateProperty:
                  type: string
                  maxLength: 255
                  nullable: true
                  description: Contact date property for date-triggered flows (triggerType=date_property).
                triggerDateOffset:
                  type: integer
                  nullable: true
                  description: Offset from the trigger date, e.g. -1 for one unit before.
                triggerDateUnit:
                  type: string
                  enum:
                    - minutes
                    - hours
                    - days
                    - weeks
                  nullable: true
                triggerFilters:
                  $ref: "#/components/schemas/MessagingSegmentRuleGroup"
                attributionWindowDays:
                  type: integer
                  minimum: 0
                  maximum: 365
                  description: Days after completion to attribute conversions. Defaults to 7 at insert time.
            example:
              name: Welcome Series
              description: Onboarding drip for new contacts
              triggerType: segment_entry
              triggerSegmentIds:
                - 7a8b9c0d-1111-2222-3333-444455556666
      responses:
        "201":
          description: Flow created (status=draft)
          content:
            application/json:
              example:
                data:
                  id: 6f9b2c1a-1111-2222-3333-444455556666
                  orgId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Welcome Series
                  triggerType: segment_entry
                  triggerConfig:
                    segmentIds:
                      - 7a8b9c0d-1111-2222-3333-444455556666
                  status: draft
                  attributionWindowDays: 7
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Referenced flow template not found
  /messaging/api/admin/flows/templates:
    get:
      summary: List flow templates
      description: |
        Returns the built-in flow templates (bundled JSON, not org-scoped) that can be used to seed a new flow via the
        `templateId` field on flow creation. Response is an unpaginated data array (not the paginated envelope).
      operationId: listMessagingFlowTemplates
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                        triggerType:
                          type: string
                        triggerConfig:
                          type: object
                          additionalProperties: true
                        steps:
                          type: array
                          items:
                            type: object
                            additionalProperties: true
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/flows/{id}:
    parameters:
      - $ref: "#/components/parameters/pathId"
    get:
      summary: Get a messaging flow
      description: Returns a single flow (org-scoped) with its ordered steps.
      operationId: getMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow with steps
          content:
            application/json:
              example:
                data:
                  id: 6f9b2c1a-1111-2222-3333-444455556666
                  orgId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Welcome Series
                  triggerType: segment_entry
                  status: active
                  steps:
                    - id: 11112222-3333-4444-5555-666677778888
                      flowId: 6f9b2c1a-1111-2222-3333-444455556666
                      stepType: send_email
                      config:
                        templateId: aaaa1111-2222-3333-4444-555566667777
                      sortOrder: 0
                      parentStepId: null
                      branchKey: null
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
    put:
      summary: Update a messaging flow
      description: |
        Updates a flow. Only flows in `draft` or `paused` status may be updated — any other status returns a 400
        `VALIDATION_ERROR`. Trigger fields provided in the body rebuild the stored `triggerConfig`.
      operationId: updateMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update).
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                description:
                  type: string
                  maxLength: 1000
                  nullable: true
                status:
                  type: string
                  enum:
                    - draft
                    - active
                    - paused
                    - archived
                triggerType:
                  type: string
                  enum:
                    - event
                    - segment_entry
                    - manual
                    - date_property
                triggerEvent:
                  type: string
                  maxLength: 255
                  nullable: true
                triggerSegmentId:
                  type: string
                  format: uuid
                  nullable: true
                triggerSegmentIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                triggerDateProperty:
                  type: string
                  maxLength: 255
                  nullable: true
                triggerDateOffset:
                  type: integer
                  nullable: true
                triggerDateUnit:
                  type: string
                  enum:
                    - minutes
                    - hours
                    - days
                    - weeks
                  nullable: true
                triggerFilters:
                  $ref: "#/components/schemas/MessagingSegmentRuleGroup"
                attributionWindowDays:
                  type: integer
                  minimum: 0
                  maximum: 365
            example:
              name: Welcome Series v2
              attributionWindowDays: 14
      responses:
        "200":
          description: Flow updated
        "400":
          description: Validation error or flow not in draft/paused status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
    delete:
      summary: Delete a messaging flow
      description: |
        Deletes a flow and cascades to its steps, flow executions, and step executions. Only flows in `draft` or
        `archived` status may be deleted — any other status returns a 400 `VALIDATION_ERROR`.
      operationId: deleteMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow deleted
          content:
            application/json:
              example:
                data:
                  deleted: true
        "400":
          description: Flow not in draft/archived status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/activate:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Activate a messaging flow
      description: |
        Transitions a flow to `active`. Validated by the flow state machine plus completeness checks: the flow must
        have at least one step, `segment_entry` triggers must reference at least one existing segment, `event`
        triggers require an event name, `send_email`/`send_sms` steps require a `templateId`, `delay` steps require
        delay config, `webhook` steps require a url, and sibling steps must have unique `sortOrder`. Any failure
        returns a 400 `VALIDATION_ERROR`.
      operationId: activateMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow activated
        "400":
          description: Illegal transition or incomplete configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/archive:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Archive a messaging flow
      description: |
        Transitions a flow to `archived` (validated by the flow state machine) and cancels all of its `active` and
        `waiting` executions.
      operationId: archiveMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow archived
        "400":
          description: Illegal transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/pause:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Pause a messaging flow
      description: Transitions a flow to `paused` (validated by the flow state machine). In-flight executions are left intact.
      operationId: pauseMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow paused
        "400":
          description: Illegal transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/executions:
    parameters:
      - $ref: "#/components/parameters/pathId"
    get:
      summary: List flow executions
      description: |
        Returns a paginated list of contact executions for a flow, joined with basic contact fields and the current
        step. `search` matches contact email/first/last name (case-insensitive).
      operationId: listMessagingFlowExecutions
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by execution status
          schema:
            type: string
            enum:
              - active
              - waiting
              - completed
              - failed
              - cancelled
        - name: search
          in: query
          description: Case-insensitive match on contact email/first name/last name
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of executions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 22223333-4444-5555-6666-777788889999
                    orgId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    flowId: 6f9b2c1a-1111-2222-3333-444455556666
                    contactId: 9c0d1e2f-3333-4444-5555-666677778888
                    currentStepId: 11112222-3333-4444-5555-666677778888
                    status: active
                    startedAt: 2025-01-16 09:00:00+00:00
                    completedAt: null
                    contact:
                      email: jane@example.com
                      firstName: Jane
                      lastName: Doe
                    currentStep:
                      stepType: send_email
                      sortOrder: 0
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/steps:
    parameters:
      - $ref: "#/components/parameters/pathId"
    get:
      summary: List flow steps
      description: Returns all steps for a flow ordered by sortOrder. Response is an unpaginated data array (not the paginated
        envelope).
      operationId: listMessagingFlowSteps
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow steps
          content:
            application/json:
              example:
                data:
                  - id: 11112222-3333-4444-5555-666677778888
                    flowId: 6f9b2c1a-1111-2222-3333-444455556666
                    stepType: send_email
                    config:
                      templateId: aaaa1111-2222-3333-4444-555566667777
                    sortOrder: 0
                    parentStepId: null
                    branchKey: null
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
    post:
      summary: Create a flow step
      description: |
        Appends a step to a flow. The request `type` field selects the step kind and is stored as `stepType` on the
        row. If `sortOrder` is omitted or `0`, it is auto-assigned to the current max sibling order + 1; otherwise it
        must be unique among sibling steps (same `parentStepId` and `branchKey`) or a 400 `VALIDATION_ERROR` is
        returned. `parentStepId`, if provided, must reference a step in the same flow. `config` shape depends on
        `type` (see MessagingFlowStepConfig).
      operationId: createMessagingFlowStep
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - config
              properties:
                type:
                  type: string
                  enum:
                    - send_email
                    - send_sms
                    - delay
                    - condition
                    - ab_split
                    - update_property
                    - add_tag
                    - remove_tag
                    - webhook
                  description: Step kind. Persisted as `stepType`.
                sortOrder:
                  type: integer
                  minimum: 0
                  default: 0
                  description: Position among siblings. 0 (or omitted) auto-appends to the end.
                parentStepId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Parent step for branching (condition/ab_split children). Must belong to this flow.
                branchKey:
                  type: string
                  maxLength: 50
                  nullable: true
                  description: Branch key for condition (yes/no) or A/B split variant.
                config:
                  $ref: "#/components/schemas/MessagingFlowStepConfig"
            example:
              type: send_email
              config:
                templateId: aaaa1111-2222-3333-4444-555566667777
      responses:
        "201":
          description: Step created
        "400":
          description: Validation error, duplicate sibling sortOrder, or parent step not in flow
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/steps/reorder:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Reorder flow steps
      description: |
        Batch-updates `sortOrder` (and optionally `parentStepId`/`branchKey`) for a set of steps. `sortOrder` must be
        unique within each sibling group (same parent and branch key) or a 400 `VALIDATION_ERROR` is returned.
        Returns the flow's steps ordered by `sortOrder`.
      operationId: reorderMessagingFlowSteps
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - steps
              properties:
                steps:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                      - id
                      - sortOrder
                    properties:
                      id:
                        type: string
                        format: uuid
                      sortOrder:
                        type: integer
                        minimum: 0
                      parentStepId:
                        type: string
                        format: uuid
                        nullable: true
                      branchKey:
                        type: string
                        maxLength: 50
                        nullable: true
            example:
              steps:
                - id: 11112222-3333-4444-5555-666677778888
                  sortOrder: 1
                - id: 33334444-5555-6666-7777-888899990000
                  sortOrder: 0
      responses:
        "200":
          description: Steps reordered; returns updated ordered steps
        "400":
          description: Validation error or duplicate sibling sortOrder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/flows/{id}/steps/{stepId}:
    parameters:
      - $ref: "#/components/parameters/pathId"
      - name: stepId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Flow step UUID
    get:
      summary: Get a flow step
      description: Returns a single step scoped to the flow and org.
      operationId: getMessagingFlowStep
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Flow step
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow or step not found
    put:
      summary: Update a flow step
      description: |
        Partially updates a step. `type` (if present) is stored as `stepType`. `config` shape depends on the step
        type (see MessagingFlowStepConfig). Note the handler does not re-check sibling `sortOrder` uniqueness on
        update.
      operationId: updateMessagingFlowStep
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update).
              properties:
                type:
                  type: string
                  enum:
                    - send_email
                    - send_sms
                    - delay
                    - condition
                    - ab_split
                    - update_property
                    - add_tag
                    - remove_tag
                    - webhook
                sortOrder:
                  type: integer
                  minimum: 0
                parentStepId:
                  type: string
                  format: uuid
                  nullable: true
                branchKey:
                  type: string
                  maxLength: 50
                  nullable: true
                config:
                  $ref: "#/components/schemas/MessagingFlowStepConfig"
            example:
              config:
                templateId: bbbb2222-3333-4444-5555-666677778888
      responses:
        "200":
          description: Step updated
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow or step not found
    delete:
      summary: Delete a flow step
      description: |
        Deletes a step, removes its step executions, and decrements `sortOrder` for later siblings to close the gap.
      operationId: deleteMessagingFlowStep
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Step deleted
          content:
            application/json:
              example:
                data:
                  deleted: true
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow or step not found
  /messaging/api/admin/flows/{id}/trigger:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Manually trigger a flow for contacts
      description: |
        Enters the given contacts into an `active` flow. The flow must be `active` or a 400 `VALIDATION_ERROR` is
        returned. Contacts already in the flow are skipped (counted in `skipped`); each newly entered contact yields
        an execution. Individual contacts that fail to enter (e.g. a race on the unique constraint) are silently
        skipped and not counted in `triggered`.
      operationId: triggerMessagingFlow
      tags:
        - "Messaging: Flows"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contactIds
              properties:
                contactIds:
                  type: array
                  minItems: 1
                  maxItems: 1000
                  items:
                    type: string
                    format: uuid
            example:
              contactIds:
                - 9c0d1e2f-3333-4444-5555-666677778888
      responses:
        "200":
          description: Trigger result
          content:
            application/json:
              example:
                data:
                  triggered: 1
                  skipped: 0
                  executions:
                    - contactId: 9c0d1e2f-3333-4444-5555-666677778888
                      executionId: 22223333-4444-5555-6666-777788889999
        "400":
          description: Validation error or flow not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Flow not found
  /messaging/api/admin/messages/{id}:
    parameters:
      - $ref: "#/components/parameters/pathId"
    get:
      summary: Get a message with its events
      description: |
        Returns a single sent message (org-scoped) together with its delivery/engagement events (sent, delivered,
        opened, clicked, bounced, complained, unsubscribed) ordered oldest-first.
      operationId: getMessagingMessage
      tags:
        - "Messaging: Messages"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Message with events
          content:
            application/json:
              example:
                data:
                  id: c1d2e3f4-5555-6666-7777-888899990000
                  orgId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  contactId: 9c0d1e2f-3333-4444-5555-666677778888
                  channel: email
                  status: delivered
                  events:
                    - id: e1f2a3b4-1111-2222-3333-444455556666
                      messageId: c1d2e3f4-5555-6666-7777-888899990000
                      eventType: delivered
                      createdAt: 2025-01-16 09:01:00+00:00
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Message not found
  /messaging/api/admin/segments:
    get:
      summary: List segments
      description: |
        Returns a paginated list of contact/lead segments for the org. Filterable by `type`, `source`, `status`, and
        `search` (case-insensitive name match).
      operationId: listMessagingSegments
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      parameters:
        - name: type
          in: query
          schema:
            type: string
            enum:
              - dynamic
              - static
        - name: source
          in: query
          schema:
            type: string
            enum:
              - contacts
              - leads
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - archived
        - name: search
          in: query
          description: Case-insensitive partial match on segment name
          schema:
            type: string
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of segments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 7a8b9c0d-1111-2222-3333-444455556666
                    name: Engaged - Last 30d
                    description: Opened or clicked in last 30 days
                    type: dynamic
                    source: contacts
                    contactCount: 342
                    lastEvaluatedAt: 2025-01-16 08:00:00+00:00
                    status: active
                    createdAt: 2025-01-10 10:00:00+00:00
                    updatedAt: 2025-01-16 08:00:00+00:00
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
    post:
      summary: Create a segment
      description: |
        Creates a segment. For `static` segments, any supplied `contactIds` are inserted as members and
        `contactCount` is set accordingly. For `dynamic` segments, if `rules` are supplied the segment is evaluated
        immediately and `contactCount` reflects the match count.
      operationId: createMessagingSegment
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingSegmentCreate"
            example:
              name: Engaged - Last 30d
              type: dynamic
              source: contacts
              rules:
                logic: and
                rules:
                  - field: lastEngagedAt
                    operator: within_last
                    value: "30"
      responses:
        "201":
          description: Segment created
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/segments/{id}:
    parameters:
      - $ref: "#/components/parameters/pathId"
    get:
      summary: Get a segment
      description: Returns a single segment (org-scoped).
      operationId: getMessagingSegment
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Segment
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Segment not found
    put:
      summary: Update a segment
      description: |
        Partially updates a segment. If the effective type is `dynamic` and `rules` are provided, the segment is
        re-evaluated and `contactCount` is refreshed. If the effective type is `static` and `contactIds` are provided,
        the member set is fully replaced and `contactCount` updated.
      operationId: updateMessagingSegment
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingSegmentUpdate"
            example:
              name: Engaged - Last 14d
              rules:
                logic: and
                rules:
                  - field: lastEngagedAt
                    operator: within_last
                    value: "14"
      responses:
        "200":
          description: Segment updated
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Segment not found
    delete:
      summary: Delete a segment
      description: |
        Deletes a segment (cascades to its members). Refused with a 400 `VALIDATION_ERROR` if any messaging campaign
        still references the segment.
      operationId: deleteMessagingSegment
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Segment deleted
          content:
            application/json:
              example:
                data:
                  deleted: true
        "400":
          description: Segment referenced by one or more campaigns
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Segment not found
  /messaging/api/admin/segments/{id}/members:
    parameters:
      - $ref: "#/components/parameters/pathId"
    get:
      summary: List segment members
      description: Returns a paginated list of the segment's members joined with basic contact fields, newest first.
      operationId: listMessagingSegmentMembers
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated list of members
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: aa11bb22-3333-4444-5555-666677778888
                    contactId: 9c0d1e2f-3333-4444-5555-666677778888
                    addedAt: 2025-01-16 09:00:00+00:00
                    email: jane@example.com
                    firstName: Jane
                    lastName: Doe
                    phone: "+15551234567"
                    emailStatus: active
                    smsStatus: active
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Segment not found
    post:
      summary: Add members to a static segment
      description: |
        Adds contacts to a `static` segment (idempotent; duplicates are ignored). Non-static segments are rejected
        with a 400 `VALIDATION_ERROR`. Recounts `contactCount`, triggers `segment_entry` flows for the added contacts,
        and logs timeline events.
      operationId: addMessagingSegmentMembers
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contactIds
              properties:
                contactIds:
                  type: array
                  minItems: 1
                  items:
                    type: string
                    format: uuid
            example:
              contactIds:
                - 9c0d1e2f-3333-4444-5555-666677778888
      responses:
        "201":
          description: Members added
          content:
            application/json:
              example:
                data:
                  added: 1
                  contactCount: 343
        "400":
          description: Validation error or segment is not static
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Segment not found
  /messaging/api/admin/segments/{id}/preview:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Preview segment rule matches
      description: |
        Evaluates a rule group without persisting anything and returns the match count plus up to 5 sample contacts.
        The `{id}` path segment is ignored — the preview runs entirely off the posted `rules`/`source`. For
        `source: leads`, the count is delegated to the routing app's internal lead-query endpoint.
      operationId: previewMessagingSegment
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - rules
              properties:
                rules:
                  $ref: "#/components/schemas/MessagingSegmentRuleGroup"
                source:
                  type: string
                  enum:
                    - contacts
                    - leads
                  default: contacts
            example:
              source: contacts
              rules:
                logic: and
                rules:
                  - field: custom.state
                    operator: equals
                    value: CA
      responses:
        "200":
          description: Preview result
          content:
            application/json:
              example:
                data:
                  contactCount: 42
                  sampleContacts:
                    - id: 9c0d1e2f-3333-4444-5555-666677778888
                      email: jane@example.com
                      firstName: Jane
                      lastName: Doe
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/segments/{id}/refresh:
    parameters:
      - $ref: "#/components/parameters/pathId"
    post:
      summary: Refresh a dynamic segment
      description: |
        Re-evaluates a `dynamic` segment's rules, updating its membership and `contactCount`, and triggers
        `segment_entry` flows for any newly added contacts. Non-dynamic segments are rejected with a 400
        `VALIDATION_ERROR`.
      operationId: refreshMessagingSegment
      tags:
        - "Messaging: Segments"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Refresh result
          content:
            application/json:
              example:
                data:
                  contactCount: 350
                  newContacts: 8
        "400":
          description: Segment is not dynamic
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Segment not found
  /messaging/api/admin/templates:
    get:
      summary: List messaging templates
      description: |
        Returns a paginated list of email/SMS templates scoped to the caller's organization.

        Served by the messaging app (`apps/messaging`) under basePath `/messaging`, proxied
        from `theleadrouter.com/messaging/*` via routing-app `beforeFiles` rewrites.

        Auth: `lr_session` session cookie, admin or superadmin role only (`withAdmin`).
        A missing/invalid session returns `401`; a non-admin role returns `403`.
      operationId: listMessagingTemplates
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: channel
          in: query
          description: Filter by channel
          schema:
            type: string
            enum:
              - email
              - sms
        - name: status
          in: query
          description: Filter by template status
          schema:
            type: string
            enum:
              - draft
              - active
              - archived
        - name: search
          in: query
          description: Case-insensitive partial match on template name
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated list of templates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                    orgId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Welcome Email
                    channel: email
                    subject: Welcome, {{firstName}}!
                    preheaderText: Thanks for joining
                    status: active
                    currentVersion: 3
                    mergeTags:
                      - firstName
                    createdAt: 2025-01-15T10:00:00.000Z
                    updatedAt: 2025-01-16T12:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized (missing/invalid session)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden (non-admin role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a messaging template
      description: |
        Creates an email or SMS template. If `starterId` is supplied, defaults (subject,
        preheaderText, designJson, channel) are merged from the matching built-in email
        starter before validation. For email templates an unsubscribe footer block is
        auto-appended to `designJson` when absent. Auto-creates version 1.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: createMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - channel
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                status:
                  type: string
                  enum:
                    - draft
                    - active
                    - archived
                  default: draft
                subject:
                  type: string
                  maxLength: 500
                  nullable: true
                  description: Email subject line (email only). Supports merge tags.
                preheaderText:
                  type: string
                  maxLength: 255
                  nullable: true
                designJson:
                  type: array
                  nullable: true
                  description: Block-based email builder tree. Each block has id, type, props, optional children and condition.
                  items:
                    type: object
                htmlContent:
                  type: string
                  nullable: true
                  description: Raw HTML override (email only). Stored on the `html` column.
                textContent:
                  type: string
                  nullable: true
                  description: SMS body or email text fallback.
                mergeTags:
                  type: array
                  items:
                    type: string
                  default: []
                starterId:
                  type: string
                  description: Optional built-in email starter id; merges starter defaults, then removed from the payload.
            example:
              name: Welcome Email
              channel: email
              subject: Welcome, {{firstName}}!
              status: draft
      responses:
        "201":
          description: "Template created (envelope `{ data: template }`)"
          content:
            application/json:
              example:
                data:
                  id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                  name: Welcome Email
                  channel: email
                  currentVersion: 1
                  status: draft
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/templates/import:
    post:
      summary: Import a template from raw HTML
      description: |
        Creates a draft email template from a raw HTML document. HTML is sanitized before
        storage (`sanitizeEmailHtml`). No `designJson` is produced — the template is stored
        as raw HTML only. Auto-creates version 1.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: importMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - html
              properties:
                html:
                  type: string
                  minLength: 1
                  description: Raw email HTML to sanitize and store.
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                  default: Imported Template
                subject:
                  type: string
                  maxLength: 500
                  nullable: true
      responses:
        "201":
          description: "Template created from HTML (envelope `{ data: template }`)"
        "400":
          description: Validation error (HTML required)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/templates/import/convert:
    post:
      summary: Convert raw HTML to builder blocks
      description: |
        Sanitizes the supplied HTML and converts it into the block-based builder
        representation (`convertHtmlToBlocks`, AI-assisted). Does NOT persist anything —
        returns the block tree for the caller to use in the builder.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: convertMessagingTemplateHtml
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - html
              properties:
                html:
                  type: string
                  minLength: 1
      responses:
        "200":
          description: "Converted block tree (envelope `{ data: { blocks } }`)"
          content:
            application/json:
              example:
                data:
                  blocks:
                    - id: block-1
                      type: text
                      props:
                        content: Hello
        "400":
          description: Validation error (HTML required)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/templates/starters:
    get:
      summary: List email starter templates
      description: |
        Returns the built-in email starter templates (static JSON bundle) used to seed new
        template creation. Not paginated — envelope is `{ data: [...] }`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: listMessagingTemplateStarters
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: List of email starters
          content:
            application/json:
              example:
                data:
                  - id: newsletter
                    channel: email
                    subject: This week at {{company}}
                    preheaderText: Your weekly update
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/templates/{id}:
    get:
      summary: Get a messaging template
      description: |
        Returns a single template by id, scoped to the caller's organization. Envelope
        `{ data: template }`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: getMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Template detail
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update a messaging template
      description: |
        Partial update of a template (all fields optional). Every update increments
        `currentVersion` and snapshots a new `templateVersion` row. For email templates an
        unsubscribe footer is auto-appended to `designJson` when absent.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: updateMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update).
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                status:
                  type: string
                  enum:
                    - draft
                    - active
                    - archived
                subject:
                  type: string
                  maxLength: 500
                  nullable: true
                preheaderText:
                  type: string
                  maxLength: 255
                  nullable: true
                designJson:
                  type: array
                  nullable: true
                  items:
                    type: object
                htmlContent:
                  type: string
                  nullable: true
                textContent:
                  type: string
                  nullable: true
                mergeTags:
                  type: array
                  items:
                    type: string
      responses:
        "200":
          description: "Template updated (envelope `{ data: template }`)"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
    delete:
      summary: Delete a messaging template
      description: |
        Deletes a template (cascades to its versions). Rejected with `400 VALIDATION_ERROR`
        if the template is still referenced by any messaging campaign.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: deleteMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Template deleted (envelope `{ data: { deleted: true } }`)"
        "400":
          description: Template referenced by campaigns
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
  /messaging/api/admin/templates/{id}/duplicate:
    post:
      summary: Duplicate a messaging template
      description: |
        Clones a template into a new draft named `<name> (Copy)` at version 1.
        Envelope `{ data: template }`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: duplicateMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "201":
          description: Template duplicated
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
  /messaging/api/admin/templates/{id}/preview:
    post:
      summary: Preview a template with merge data
      description: |
        Renders a template's subject, HTML, and text with merge tags resolved. Uses a real
        contact when `contactId` is supplied (org-scoped), otherwise sample data (defaults
        overlaid with `sampleData`). Does not send or persist. Render warnings are returned
        in `errors`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: previewMessagingTemplate
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                contactId:
                  type: string
                  format: uuid
                  description: Resolve merge tags against this org-scoped contact. Falls back to sample data if not found.
                sampleData:
                  type: object
                  additionalProperties:
                    type: string
                  description: Custom field sample values used when no contactId is given.
      responses:
        "200":
          description: Rendered preview
          content:
            application/json:
              example:
                data:
                  subject: Welcome, John!
                  html: <html>...</html>
                  textContent: Welcome, John!
                  errors: []
        "400":
          description: Invalid preview request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
  /messaging/api/admin/templates/{id}/send-test:
    post:
      summary: Send a test message for a template
      description: |
        Renders the template with the supplied merge data and sends a single test message
        to `to` via the org's default active provider for the template channel. Logs a
        `message` row and returns `{ data: { success: true } }` on success or
        `{ data: { success: false, error } }` when the provider send fails (still HTTP 200).
        Returns `400 VALIDATION_ERROR` when no active provider is configured for the channel.

        DEPRECATED (Phase 5, unified-provider-credentials): transitional route. Credentials
        resolve via the routing DB when the local provider row carries a
        `routingProviderCredentialId`, else the legacy decrypt path. Phase 6 rewrites the
        body against `resolveMessagingCredential`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: sendTestMessagingTemplate
      tags:
        - "Messaging: Templates"
      deprecated: true
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - to
              properties:
                to:
                  type: string
                  minLength: 1
                  description: Recipient email (email channel) or phone number (SMS channel).
                mergeData:
                  type: object
                  additionalProperties:
                    type: string
                  description: Merge overrides. firstName/lastName/email/phone map to contact fields; other keys (or `custom.*`) become
                    custom fields.
      responses:
        "200":
          description: Send attempted. `data.success` indicates provider outcome.
          content:
            application/json:
              example:
                data:
                  success: true
        "400":
          description: Validation error or no active provider configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
  /messaging/api/admin/templates/{id}/versions:
    get:
      summary: List template versions
      description: |
        Returns all stored version snapshots for a template, newest version first. Not
        paginated — envelope `{ data: [...] }`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: listMessagingTemplateVersions
      tags:
        - "Messaging: Templates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Version snapshots (newest first)
          content:
            application/json:
              example:
                data:
                  - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
                    templateId: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                    version: 3
                    subject: Welcome, {{firstName}}!
                    html: <html>...</html>
                    textContent: null
                    designJson: []
                    createdAt: 2025-01-16T12:00:00.000Z
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Template not found
  /messaging/api/admin/providers:
    get:
      summary: List messaging providers
      description: |
        Returns a paginated list of sending providers (email/SMS) scoped to the caller's
        organization. Encrypted credentials are never included in the list response.

        DEPRECATED (Phase 5, unified-provider-credentials): use the routing-app
        `/admin/system/providers` UI and `GET /api/v1/providers`. Live during the 30-day
        soak; Phase 6 returns `410 Gone`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: listMessagingProviders
      tags:
        - "Messaging: Providers"
      deprecated: true
      security:
        - sessionCookie: []
      parameters:
        - name: type
          in: query
          description: Filter by provider type
          schema:
            type: string
            enum:
              - email
              - sms
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated list of providers (no credentials)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 9b2f5c11-1111-2222-3333-444455556666
                    name: Primary Resend
                    type: email
                    providerKey: resend
                    config:
                      fromEmail: hello@example.com
                    isDefault: true
                    dailySendLimit: null
                    dailySendCount: 0
                    status: active
                    createdAt: 2025-01-15T10:00:00.000Z
                    updatedAt: 2025-01-15T10:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
    post:
      summary: Create a messaging provider
      description: |
        Creates a sending provider. `credentials` are encrypted at rest and never echoed in
        the response (the response strips `credentialsEncrypted`). Becomes the default for
        its type when it is the first provider of that type or `isDefault` is true.

        DEPRECATED (Phase 5): use routing-app `POST /api/v1/providers`. Phase 6 returns `410 Gone`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: createMessagingProvider
      tags:
        - "Messaging: Providers"
      deprecated: true
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - type
                - providerKey
                - credentials
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                type:
                  type: string
                  enum:
                    - email
                    - sms
                providerKey:
                  type: string
                  enum:
                    - ses
                    - sendgrid
                    - resend
                    - twilio
                    - bandwidth
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
                credentials:
                  type: object
                  writeOnly: true
                  additionalProperties: true
                  description: Provider auth credentials (API keys, access keys). Encrypted at rest; never returned.
                config:
                  type: object
                  additionalProperties: true
                  default: {}
                  description: Non-secret provider config (region, sending domain, fromEmail, etc.).
                dailySendLimit:
                  type: integer
                  minimum: 0
                  nullable: true
                isDefault:
                  type: boolean
                  default: false
      responses:
        "201":
          description: Provider created (credentials omitted from response)
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/providers/{id}:
    get:
      summary: Get a messaging provider
      description: |
        Returns a single provider. Credentials are decrypted and MASKED (all but the last 4
        characters replaced with `*`) under a `credentials` object — plaintext secrets are
        never returned.

        DEPRECATED (Phase 5): use routing-app `GET /api/v1/providers/{id}`. Phase 6 returns `410 Gone`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: getMessagingProvider
      tags:
        - "Messaging: Providers"
      deprecated: true
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Provider detail with masked credentials
          content:
            application/json:
              example:
                data:
                  id: 9b2f5c11-1111-2222-3333-444455556666
                  name: Primary Resend
                  type: email
                  providerKey: resend
                  isDefault: true
                  status: active
                  credentials:
                    apiKey: "****************abcd"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Provider not found
    put:
      summary: Update a messaging provider
      description: |
        Partial update. When `credentials` is supplied it is re-encrypted; the response
        strips `credentialsEncrypted`. Setting `isDefault` true (with `type`) clears other
        defaults of that type.

        DEPRECATED (Phase 5): use routing-app `PUT /api/v1/providers/{id}`. Phase 6 returns `410 Gone`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: updateMessagingProvider
      tags:
        - "Messaging: Providers"
      deprecated: true
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial update).
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                type:
                  type: string
                  enum:
                    - email
                    - sms
                providerKey:
                  type: string
                  enum:
                    - ses
                    - sendgrid
                    - resend
                    - twilio
                    - bandwidth
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                credentials:
                  type: object
                  writeOnly: true
                  additionalProperties: true
                  description: Replacement credentials; re-encrypted at rest, never returned.
                config:
                  type: object
                  additionalProperties: true
                dailySendLimit:
                  type: integer
                  minimum: 0
                  nullable: true
                isDefault:
                  type: boolean
      responses:
        "200":
          description: Provider updated (credentials omitted from response)
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Provider not found
    delete:
      summary: Delete a messaging provider
      description: |
        Deletes a provider. Rejected with `400 VALIDATION_ERROR` if it is the default
        provider or if any `message` rows reference it.

        DEPRECATED (Phase 5): use routing-app `DELETE /api/v1/providers/{id}`. Phase 6 returns `410 Gone`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: deleteMessagingProvider
      tags:
        - "Messaging: Providers"
      deprecated: true
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Provider deleted (envelope `{ data: { deleted: true } }`)"
        "400":
          description: Provider is default or referenced by messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Provider not found
  /messaging/api/admin/providers/{id}/test:
    post:
      summary: Send a test message through a provider
      description: |
        Sends a canned test message through the provider's decrypted credentials to
        `toEmail` (email providers) or `toPhone` (SMS providers). Returns
        `{ data: { success, providerMessageId, error } }` — HTTP 200 even when the provider
        reports failure. Does not persist a `message` row.

        DEPRECATED (Phase 5): use routing-app `POST /api/v1/providers/{id}/test`. Phase 6 returns `410 Gone`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: testMessagingProvider
      tags:
        - "Messaging: Providers"
      deprecated: true
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        description: One of toEmail / toPhone is required (matching the provider type).
        content:
          application/json:
            schema:
              type: object
              properties:
                toEmail:
                  type: string
                  format: email
                  description: Recipient for email providers.
                toPhone:
                  type: string
                  maxLength: 20
                  description: Recipient for SMS providers.
      responses:
        "200":
          description: Test attempted. `data.success` reflects provider outcome.
          content:
            application/json:
              example:
                data:
                  success: true
                  providerMessageId: msg_abc123
                  error: null
        "400":
          description: Validation error (missing recipient, undecryptable credentials, or wrong recipient for type)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Provider not found
  /messaging/api/admin/settings:
    get:
      summary: Get messaging settings for a category
      description: |
        Returns the org's messaging settings for a category as an array of `{ key, value }`.
        Envelope `{ data: [...] }`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: getMessagingSettings
      tags:
        - "Messaging: Settings"
      security:
        - sessionCookie: []
      parameters:
        - name: category
          in: query
          description: Settings category (defaults to `general`)
          schema:
            type: string
            enum:
              - general
              - tracking
              - notifications
              - apikeys
            default: general
      responses:
        "200":
          description: Settings key/value pairs
          content:
            application/json:
              example:
                data:
                  - key: defaultSenderEmail
                    value: hello@example.com
                  - key: defaultSenderName
                    value: Example Co
        "400":
          description: Invalid category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
    put:
      summary: Upsert messaging settings for a category
      description: |
        Upserts a batch of key/value settings within a category (conflict on
        org+category+key updates the value). Returns the full updated category as
        `{ data: [{ key, value }] }`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: updateMessagingSettings
      tags:
        - "Messaging: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - category
                - settings
              properties:
                category:
                  type: string
                  enum:
                    - general
                    - tracking
                    - notifications
                    - apikeys
                settings:
                  type: array
                  items:
                    type: object
                    required:
                      - key
                      - value
                    properties:
                      key:
                        type: string
                        minLength: 1
                      value:
                        type: string
                        nullable: true
            example:
              category: general
              settings:
                - key: defaultSenderEmail
                  value: hello@example.com
      responses:
        "200":
          description: Updated settings for the category
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/suppression:
    get:
      summary: List suppression entries
      description: |
        Returns a paginated list of suppressed email addresses / phone numbers scoped to the
        caller's organization.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: listMessagingSuppression
      tags:
        - "Messaging: Suppression"
      security:
        - sessionCookie: []
      parameters:
        - name: channel
          in: query
          schema:
            type: string
            enum:
              - email
              - sms
        - name: search
          in: query
          description: Case-insensitive partial match on the suppressed value
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated suppression list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: 5a1c2b3d-4e5f-6789-0abc-def123456789
                    channel: email
                    value: blocked@example.com
                    reason: bounce
                    createdAt: 2025-01-15T10:00:00.000Z
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "400":
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
    post:
      summary: Add a suppression entry
      description: |
        Suppresses an email address or phone number for the org. Returns `409 DUPLICATE` if
        the value is already suppressed for that channel.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: createMessagingSuppression
      tags:
        - "Messaging: Suppression"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - channel
                - value
              properties:
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                value:
                  type: string
                  minLength: 1
                  maxLength: 255
                reason:
                  type: string
                  enum:
                    - bounce
                    - complaint
                    - unsubscribe
                    - manual
                  default: manual
            example:
              channel: email
              value: blocked@example.com
              reason: manual
      responses:
        "201":
          description: "Suppression entry created (envelope `{ data: entry }`)"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "409":
          description: Value already suppressed for this channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/suppression/import:
    post:
      summary: Bulk import suppression entries from CSV
      description: |
        Uploads a CSV file (multipart form-data) of suppression entries. Accepts either
        `channel,value` rows or single `value` rows when a `channel` form field provides the
        default. A header row (`channel,value`, `value`, or any row containing `channel`) is
        auto-skipped. Existing entries are skipped (`onConflictDoNothing`). Returns per-run
        counts and row-level errors. Reason defaults to `manual`.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: importMessagingSuppression
      tags:
        - "Messaging: Suppression"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV file. Rows are `channel,value` or `value` (with the `channel` field as default).
                channel:
                  type: string
                  enum:
                    - email
                    - sms
                  description: Default channel applied to single-column rows.
      responses:
        "200":
          description: Import summary
          content:
            application/json:
              example:
                data:
                  imported: 12
                  skipped: 3
                  errors:
                    - 'Row 4: invalid channel "voice"'
        "400":
          description: File is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
  /messaging/api/admin/suppression/{id}:
    delete:
      summary: Remove a suppression entry
      description: |
        Deletes a suppression entry by id (org-scoped). Returns `{ success: true }` (note:
        this route does NOT use the `{ data: ... }` envelope). Returns `404` when the entry
        does not exist in the org.

        Auth: `lr_session` session cookie, admin/superadmin (`withAdmin`).
      operationId: deleteMessagingSuppression
      tags:
        - "Messaging: Suppression"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Suppression entry removed (envelope `{ success: true }`)"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (non-admin role)
        "404":
          description: Suppression entry not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/api-keys:
    get:
      summary: List messaging API keys (decommissioned)
      description: |
        DECOMMISSIONED. Always returns `410 Gone` with code `GONE`. API keys are managed via
        the routing-app Settings > API Keys UI. No auth is performed — the handler responds
        `410` unconditionally.
      operationId: listMessagingApiKeys
      tags:
        - "Messaging: Settings"
      deprecated: true
      responses:
        "410":
          description: Gone — manage API keys via Settings > API Keys
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: GONE
                  message: Use Settings > API Keys to manage API keys
    post:
      summary: Create a messaging API key (decommissioned)
      description: |
        DECOMMISSIONED. Always returns `410 Gone` with code `GONE`. No auth is performed.
      operationId: createMessagingApiKey
      tags:
        - "Messaging: Settings"
      deprecated: true
      responses:
        "410":
          description: Gone — manage API keys via Settings > API Keys
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/admin/api-keys/{id}:
    delete:
      summary: Revoke a messaging API key (decommissioned)
      description: |
        DECOMMISSIONED. Always returns `410 Gone` with code `GONE`. No auth is performed.
      operationId: deleteMessagingApiKey
      tags:
        - "Messaging: Settings"
      deprecated: true
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "410":
          description: Gone — manage API keys via Settings > API Keys
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /messaging/api/v1/profiles:
    post:
      operationId: messagingCreateProfile
      tags:
        - "Messaging: Profiles"
      summary: Create or upsert a profile
      description: |
        Create a profile, or upsert an existing one matched by `email` within the org.
        Klaviyo-style JSON:API endpoint served by the messaging service at
        `theleadrouter.com/messaging/...`.

        **Auth:** Send an API key (`lr_xxx`) as a Bearer token in the `Authorization`
        header. The key MUST carry the `messaging` scope (keys are validated against the
        routing service). Missing, invalid, or non-`messaging`-scoped keys return `401`
        with the JSON:API error envelope (`{ "errors": [...] }`).

        When a profile with the same email already exists, its `custom` JSONB is merged
        (email is never overwritten) and the response status is `200`; a brand-new profile
        returns `201`. Extra `properties` and `tags` are also written to the
        `contactProperty` / `contactTag` tables for segmentation.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingProfileCreateRequest"
      responses:
        "200":
          description: Existing profile upserted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingProfileResponse"
        "201":
          description: New profile created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingProfileResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    get:
      operationId: messagingListProfiles
      tags:
        - "Messaging: Profiles"
      summary: List profiles
      description: |
        Paginated list of profiles for the authenticated org. Uses JSON:API page-based
        pagination (`page[size]`, `page[number]`) and returns `data`, `meta.page`, and
        `links`. See the Create Profile endpoint for auth details.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/messagingPageSize"
        - $ref: "#/components/parameters/messagingPageNumber"
        - name: filter[email]
          in: query
          schema:
            type: string
          description: Exact-match filter on profile email.
        - name: filter[email_status]
          in: query
          schema:
            $ref: "#/components/schemas/MessagingEmailStatus"
          description: Filter by email deliverability status.
      responses:
        "200":
          description: Paginated profiles
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingProfileListResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/profiles/{id}:
    parameters:
      - $ref: "#/components/parameters/messagingPathId"
    get:
      operationId: messagingGetProfile
      tags:
        - "Messaging: Profiles"
      summary: Get a profile
      description: Fetch a single profile by UUID (scoped to the authenticated org).
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingProfileResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    patch:
      operationId: messagingUpdateProfile
      tags:
        - "Messaging: Profiles"
      summary: Update a profile
      description: |
        Partial update of a profile. The mutated record is identified by the path `id`;
        the request body must still include `data.id` (a UUID) and `data.type: "profile"`
        or validation fails with `400`. `custom` JSONB is merged, not replaced.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingProfileUpdateRequest"
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingProfileResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/lists:
    post:
      operationId: messagingCreateList
      tags:
        - "Messaging: Lists"
      summary: Create a list
      description: |
        Create a list (segment). If `rules` is provided the list is created as a
        **dynamic** segment and evaluated immediately; otherwise it is a **static** list
        that you populate via the relationships endpoint. See the Create Profile endpoint
        for auth details.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingListCreateRequest"
      responses:
        "201":
          description: List created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingListResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    get:
      operationId: messagingListLists
      tags:
        - "Messaging: Lists"
      summary: List lists
      description: Paginated list of lists/segments for the org (JSON:API pagination).
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/messagingPageSize"
        - $ref: "#/components/parameters/messagingPageNumber"
        - name: filter[type]
          in: query
          schema:
            $ref: "#/components/schemas/MessagingListType"
          description: Filter by list type (static or dynamic).
      responses:
        "200":
          description: Paginated lists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingListListResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/lists/{id}:
    parameters:
      - $ref: "#/components/parameters/messagingPathId"
    get:
      operationId: messagingGetList
      tags:
        - "Messaging: Lists"
      summary: Get a list
      description: Fetch a single list (static or dynamic) by UUID.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingListResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    patch:
      operationId: messagingUpdateList
      tags:
        - "Messaging: Lists"
      summary: Update a list
      description: |
        Rename a **static** list. Only static lists are updatable — dynamic lists (or a
        missing list) return `404`. The body must include `data.id` (UUID) and
        `data.type: "list"`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingListUpdateRequest"
      responses:
        "200":
          description: Updated list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingListResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    delete:
      operationId: messagingDeleteList
      tags:
        - "Messaging: Lists"
      summary: Delete a list
      description: |
        Delete a **static** list and its memberships. Only static lists are deletable —
        dynamic lists (or a missing list) return `404`. A list still referenced by any
        campaign returns `409`.
      security:
        - bearerAuth: []
      responses:
        "204":
          description: List deleted
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "409":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/lists/{id}/relationships/profiles:
    parameters:
      - $ref: "#/components/parameters/messagingPathId"
    post:
      operationId: messagingAddListProfiles
      tags:
        - "Messaging: Lists"
      summary: Add profiles to a list
      description: |
        Add one or more profiles to a **static** list. The list `profile_count` is
        recomputed after the change. Non-static or missing lists return `404`. Membership
        inserts are idempotent (existing memberships are skipped).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingListMembershipRequest"
      responses:
        "204":
          description: Profiles added (list count updated)
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    delete:
      operationId: messagingRemoveListProfiles
      tags:
        - "Messaging: Lists"
      summary: Remove profiles from a list
      description: |
        Remove one or more profiles from a **static** list. The list `profile_count` is
        recomputed. Non-static or missing lists return `404`. The profile IDs to remove
        are supplied in the request body (JSON:API resource identifiers).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingListMembershipRequest"
      responses:
        "204":
          description: Profiles removed (list count updated)
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/flows:
    post:
      operationId: messagingCreateFlow
      tags:
        - "Messaging: Flows API"
      summary: Create a flow
      description: |
        Create an automation flow. The flow is inserted as `draft`, its steps are written
        with sequential sort order, then it is immediately transitioned to `active` and
        returned in that state (`201`).

        Validation:
        - `trigger_type: segment_entry` requires `trigger_segment_id` or a non-empty
          `trigger_segment_ids`; every referenced segment must exist or the call returns `404`.
        - `trigger_type: event` requires `trigger_event`.
        - Every `send_email` / `send_sms` step's `templateId` must reference an existing
          template, otherwise `400`.

        See the Create Profile endpoint for auth details.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingFlowCreateRequest"
      responses:
        "201":
          description: Flow created and activated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingFlowResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    get:
      operationId: messagingListFlows
      tags:
        - "Messaging: Flows API"
      summary: List flows
      description: Paginated list of flows for the org (JSON:API pagination).
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/messagingPageSize"
        - $ref: "#/components/parameters/messagingPageNumber"
        - name: filter[status]
          in: query
          schema:
            $ref: "#/components/schemas/MessagingFlowStatus"
          description: Filter by flow status.
      responses:
        "200":
          description: Paginated flows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingFlowListResponse"
        "401":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/flows/{id}:
    parameters:
      - $ref: "#/components/parameters/messagingPathId"
    get:
      operationId: messagingGetFlow
      tags:
        - "Messaging: Flows API"
      summary: Get a flow with steps
      description: Fetch a single flow by UUID, including its ordered steps.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Flow with embedded steps
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingFlowWithStepsResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    patch:
      operationId: messagingUpdateFlow
      tags:
        - "Messaging: Flows API"
      summary: Update a flow
      description: |
        Update a flow's `name`, `status`, and/or `trigger_segment_ids`. Status changes are
        validated against the flow state machine (allowed targets: `active`, `paused`,
        `archived`) — an illegal transition returns `400`. Setting `status: archived`
        cancels any active/waiting executions. Referenced segments must exist (`404`).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingFlowPatchRequest"
      responses:
        "200":
          description: Updated flow
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingFlowResponse"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    delete:
      operationId: messagingDeleteFlow
      tags:
        - "Messaging: Flows API"
      summary: Delete a flow
      description: |
        Delete a flow. Only `draft` or `archived` flows may be deleted — `active` or
        `paused` flows return `400`. Deletion cascades to steps, executions, and step
        executions.
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Flow deleted
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/flows/{id}/trigger-segments:
    parameters:
      - $ref: "#/components/parameters/messagingPathId"
    get:
      operationId: messagingListFlowTriggerSegments
      tags:
        - "Messaging: Flows API"
      summary: List a flow's trigger segments
      description: |
        List the segments currently linked to this flow's `segment_entry` trigger. Each
        entry is serialized as a list resource. Returns an empty `data` array when no
        segments are linked.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Linked trigger segments
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MessagingListResource"
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
    post:
      operationId: messagingLinkFlowTriggerSegment
      tags:
        - "Messaging: Flows API"
      summary: Link a segment to a flow trigger
      description: |
        Add a segment to this flow's `segment_entry` trigger. The flow's `trigger_type`
        must be `segment_entry` (otherwise `400`); the segment must exist (`404`); and a
        segment already linked returns `400`.

        Note: on success this returns a bare `200` with a non-JSON:API body
        (`{ "data": { "segment_id": "...", "flow_id": "..." } }`) — unlike the resource
        envelopes used elsewhere in this API.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingTriggerSegmentLinkRequest"
      responses:
        "200":
          description: Segment linked
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      segment_id:
                        type: string
                        format: uuid
                      flow_id:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/flows/{id}/trigger-segments/{segmentId}:
    parameters:
      - $ref: "#/components/parameters/messagingPathId"
      - name: segmentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: UUID of the segment to unlink.
    delete:
      operationId: messagingUnlinkFlowTriggerSegment
      tags:
        - "Messaging: Flows API"
      summary: Unlink a segment from a flow trigger
      description: |
        Remove a segment from this flow's `segment_entry` trigger. A segment that is not
        currently linked returns `404`.
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Segment unlinked
        "400":
          $ref: "#/components/responses/MessagingError"
        "401":
          $ref: "#/components/responses/MessagingError"
        "404":
          $ref: "#/components/responses/MessagingError"
        "500":
          $ref: "#/components/responses/MessagingError"
  /messaging/api/v1/send:
    post:
      operationId: messagingSend
      tags:
        - "Messaging: Send"
      summary: Send a transactional message
      description: |
        Send a single email or SMS to a contact using a template. Provide either
        `contactId` or `email` (not both required) — when only `email` is given the
        contact is looked up or created within the org.

        **Auth:** Same as the other messaging endpoints — a `messaging`-scoped API key
        (`lr_xxx`) as a Bearer token. Auth failures (`401`) use the JSON:API error
        envelope.

        **Error envelope differs from the rest of this API:** unlike Flows/Lists/Profiles,
        the body-level responses from this handler use the standard routing error shape
        (`{ "error": { "code", "message", "details" } }`), not the JSON:API `errors[]`
        array. Validation failures return `400`; unexpected failures return `500` with
        `code: INTERNAL_ERROR`.

        **Send outcomes:**
        - `201` — message accepted/sent (`{ data: { messageId, status } }`).
        - `200` — send was suppressed (e.g. opted-out / bounced / frequency cap), with
          `status: "suppressed"` and an `error` reason.
        - `422` — send failed for another reason, with `status` and `error`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessagingSendRequest"
      responses:
        "200":
          description: Send suppressed (no message delivered)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingSendResult"
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingSendResult"
        "400":
          description: Validation error (standard routing error shape)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/MessagingError"
        "422":
          description: Send failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessagingSendResult"
        "500":
          description: Internal error (standard routing error shape)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/onboarding/status:
    get:
      summary: Get onboarding status
      description: Returns the authenticated user's onboarding status and saved wizard state, plus role, tenant name, and
        owner flag. Requires an authenticated session.
      operationId: getOnboardingStatus
      tags:
        - Onboarding
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Onboarding status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      status:
                        type: string
                        description: e.g. not_started, in_progress, completed, skipped
                      state:
                        type: object
                        additionalProperties: true
                      role:
                        type: string
                      tenantName:
                        type: string
                        nullable: true
                      isOwner:
                        type: boolean
              example:
                data:
                  status: in_progress
                  state:
                    currentStep: 2
                    stepData: {}
                  role: admin
                  tenantName: Acme Corp
                  isOwner: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: User identity required (session lacks a userId)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/onboarding/progress:
    put:
      summary: Save onboarding progress
      description: Persists the current onboarding step and per-step data, setting status to in_progress. Requires an
        authenticated session.
      operationId: saveOnboardingProgress
      tags:
        - Onboarding
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentStep
                - stepData
              properties:
                currentStep:
                  type: integer
                stepData:
                  type: object
                  additionalProperties: true
            example:
              currentStep: 2
              stepData:
                companyName: Acme Corp
      responses:
        "200":
          description: Progress saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: User identity required (session lacks a userId)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/onboarding/complete:
    put:
      summary: Complete or skip onboarding
      description: Marks onboarding as completed or skipped and clears saved wizard state. Requires an authenticated session.
      operationId: completeOnboarding
      tags:
        - Onboarding
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - completed
                    - skipped
            example:
              status: completed
      responses:
        "200":
          description: Onboarding finalized
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: User identity required (session lacks a userId)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/public/dni/impressions:
    post:
      summary: Record DNI impression
      description: >
        Public, unauthenticated endpoint for dynamic number insertion (DNI) tracking scripts. Records a call impression
        for a visitor against a campaign spec token and returns the number to display. CORS-enabled (allows any origin).
        Rate-limited to 120 requests per minute per IP + spec token. If the number pool is exhausted for the visitor, a
        replacement number is auto-provisioned after the response.
      operationId: recordDniImpression
      tags:
        - Public
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - specToken
              properties:
                specToken:
                  type: string
                  minLength: 1
                visitorId:
                  type: string
                  maxLength: 120
                  nullable: true
                sessionId:
                  type: string
                  maxLength: 120
                  nullable: true
                impressionId:
                  type: string
                  format: uuid
                  nullable: true
                linkedLeadId:
                  type: string
                  format: uuid
                  nullable: true
                url:
                  type: string
                  maxLength: 2048
                  nullable: true
                referrer:
                  type: string
                  maxLength: 2048
                  nullable: true
                landingPage:
                  type: string
                  maxLength: 2048
                  nullable: true
                closing:
                  type: boolean
                  nullable: true
                tags:
                  type: object
                  additionalProperties: true
                  nullable: true
            example:
              specToken: spec_abc123
              visitorId: v-98765
              url: https://example.com/landing
      responses:
        "200":
          description: Impression recorded; returns the number to display for this visitor
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          description: DNI campaign not found for the spec token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Too many DNI impression requests (rate limited); includes Retry-After header
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/partner/reports/filter-options:
    get:
      summary: Partner filter-option lookups
      description: Returns selectable filter options for one dimension, scoped to the authenticated partner. Entity dims
        (campaign/vertical/offer) are limited to the partner's own records; static dims return a hardcoded list; the
        distinct dims (state, city, sub1..sub10, lpUrl) SELECT DISTINCT from the partner's own leads.
        Buyer/contract/partner dims are always rejected.
      operationId: getPartnerReportFilterOptions
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: dimension
          in: query
          required: true
          description: "Which dimension to enumerate. Entity: campaign, vertical, offer. Static: revenueType, leadType, status.
            Distinct (from partner's leads): state, city, sub1..sub10, lpUrl. Passing buyer, contract, or partner
            returns 400."
          schema:
            type: string
            enum:
              - campaign
              - vertical
              - offer
              - revenueType
              - leadType
              - status
              - state
              - city
              - sub1
              - sub2
              - sub3
              - sub4
              - sub5
              - sub6
              - sub7
              - sub8
              - sub9
              - sub10
              - lpUrl
      responses:
        "200":
          description: Options for the requested dimension (distinct dims capped at 500).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        label:
                          type: string
                  pagination:
                    type: object
                    properties:
                      total:
                        type: integer
                      limit:
                        type: integer
                      offset:
                        type: integer
                      hasMore:
                        type: boolean
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    label: Auto Insurance Q1
                pagination:
                  total: 1
                  limit: 1
                  offset: 0
                  hasMore: false
        "400":
          description: Validation error, forbidden dimension (buyer/contract/partner), or missing partner context.
        "401":
          description: Unauthorized
  /api/partner/reports/leads/day:
    get:
      summary: Time-of-Lead day drill-down
      description: Returns every lead and call the partner received on a single calendar day (tenant timezone), bucketed by
        intake date (lead.createdAt / call.createdAt) regardless of payout status. Each row includes a status badge and
        an informational partner payout. Never returns buyer-side identity or revenue columns. Rows are capped at 500
        per section.
      operationId: getPartnerLeadsDay
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: date
          in: query
          required: true
          description: Day to drill into (YYYY-MM-DD, tenant timezone).
          schema:
            type: string
            format: date
        - name: campaignId
          in: query
          description: Comma-separated campaign IDs to filter by.
          schema:
            type: string
        - name: sub1
          in: query
          description: Comma-separated sub1 values to filter by.
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Leads and calls for the day.
          content:
            application/json:
              schema:
                type: object
                properties:
                  leads:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        sub1:
                          type: string
                          nullable: true
                        sub2:
                          type: string
                          nullable: true
                        sub3:
                          type: string
                          nullable: true
                        status:
                          type: string
                        cplPayout:
                          type: number
                          description: Partner payout (lead cost + DQ payout).
                  calls:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        durationSeconds:
                          type: integer
                        status:
                          type: string
                        payout:
                          type: number
                          description: Partner call payout (call cost + CPA payout).
                  truncated:
                    type: boolean
                    description: True if either section hit the 500-row cap.
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/reports/leads/pivot:
    get:
      summary: Time-of-Lead pivot
      description: Groups leads by offer or sub value and returns status counts per group, bucketed by lead intake date
        (lead.createdAt) in the tenant timezone. Never returns buyer-side columns.
      operationId: getPartnerLeadsPivot
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          description: Range start (YYYY-MM-DD, inclusive, tenant timezone).
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Range end (YYYY-MM-DD, inclusive). Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          required: true
          description: Grouping dimension. campaign groups by offer (labelled by offer name); sub1..sub5 group by that sub value.
          schema:
            type: string
            enum:
              - campaign
              - sub1
              - sub2
              - sub3
              - sub4
              - sub5
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Status counts per group, sorted by total descending.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                        label:
                          type: string
                        sold:
                          type: integer
                        pending:
                          type: integer
                        returned:
                          type: integer
                        rejected:
                          type: integer
                        total:
                          type: integer
              example:
                data:
                  - key: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    label: Auto Insurance Standard
                    sold: 40
                    pending: 5
                    returned: 2
                    rejected: 3
                    total: 50
        "400":
          description: Validation error or invalid pivot dimension.
        "401":
          description: Unauthorized
  /api/partner/reports/leads/timeseries:
    get:
      summary: Time-of-Lead daily counts
      description: Daily lead counts bucketed by status (sold / pending / returned / rejected) across a date range in the
        tenant timezone. Every calendar day in range is returned, zero-filled. Never returns buyer-side columns.
      operationId: getPartnerLeadsTimeseries
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          description: Range start (YYYY-MM-DD, inclusive, tenant timezone).
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Range end (YYYY-MM-DD, inclusive). Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: One row per calendar day in range.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        date:
                          type: string
                          format: date
                        sold:
                          type: integer
                        pending:
                          type: integer
                        returned:
                          type: integer
                        rejected:
                          type: integer
                        total:
                          type: integer
              example:
                data:
                  - date: 2026-07-01
                    sold: 12
                    pending: 1
                    returned: 0
                    rejected: 2
                    total: 15
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/reports/revenue/day:
    get:
      summary: Time-of-Revenue day drill-down
      description: "Returns three drill-down sections for a single day (tenant timezone): paid CPL leads bucketed by
        lead.createdAt, paid calls bucketed by callSale.createdAt, and CPA conversions bucketed by their conversion
        event date. Only billable, delivered, non-returned sales are counted so totals reconcile with the partner
        summary. Never exposes buyer-side columns (price, totalPrice, cpaAmount, buyerId). Capped at 500 rows per
        section."
      operationId: getPartnerRevenueDay
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: date
          in: query
          required: true
          description: Day to drill into (YYYY-MM-DD, tenant timezone).
          schema:
            type: string
            format: date
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Lead, call, and CPA-conversion rows for the day.
          content:
            application/json:
              schema:
                type: object
                properties:
                  leads:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        sub1:
                          type: string
                          nullable: true
                        sub2:
                          type: string
                          nullable: true
                        sub3:
                          type: string
                          nullable: true
                        cplPayout:
                          type: number
                  calls:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        durationSeconds:
                          type: integer
                        payout:
                          type: number
                  cpaConversions:
                    type: array
                    items:
                      type: object
                      properties:
                        source:
                          type: string
                          enum:
                            - lead
                            - call
                        entityId:
                          type: string
                        originalCreatedAt:
                          type: string
                          format: date-time
                          description: Underlying lead/call intake date.
                        convertedAt:
                          type: string
                          format: date-time
                          description: CPA conversion event date (the revenue-timing date).
                        offerName:
                          type: string
                          nullable: true
                        cpaPayout:
                          type: number
                  truncated:
                    type: boolean
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/reports/revenue/pivot:
    get:
      summary: Time-of-Revenue pivot
      description: Partner payout roll-up grouped by offer or sub value over a date range (tenant timezone). Returns CPL / CPA
        / call payout plus paid counts per group. Only billable, delivered, non-returned sales are counted. Never
        returns buyer-side columns.
      operationId: getPartnerRevenuePivot
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          required: true
          description: Grouping dimension. campaign groups by offer; sub1..sub5 group by that sub value (call payout only
            contributes to the campaign pivot).
          schema:
            type: string
            enum:
              - campaign
              - sub1
              - sub2
              - sub3
              - sub4
              - sub5
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Payout roll-up per group, sorted by total descending.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                        label:
                          type: string
                        cpl:
                          type: number
                        cpa:
                          type: number
                        cpcall:
                          type: number
                        total:
                          type: number
                        paidLeads:
                          type: integer
                        paidCalls:
                          type: integer
                        paidCpa:
                          type: integer
        "400":
          description: Validation error or invalid pivot dimension.
        "401":
          description: Unauthorized
  /api/partner/reports/revenue/timeseries:
    get:
      summary: Time-of-Revenue daily payout
      description: Daily partner payout across a date range (tenant timezone), split into CPL / CPA / call payout with paid
        lead / call / CPA counts per bucket. Every calendar day in range is returned, zero-filled. Non-partner-safe
        filter params are silently stripped. Never returns buyer-side columns.
      operationId: getPartnerRevenueTimeseries
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: One payout row per calendar day in range.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        date:
                          type: string
                          format: date
                        cpl:
                          type: number
                        cpa:
                          type: number
                        cpcall:
                          type: number
                        total:
                          type: number
                        paidLeads:
                          type: integer
                        paidCalls:
                          type: integer
                        paidCpa:
                          type: integer
              example:
                data:
                  - date: 2026-07-01
                    cpl: 450
                    cpa: 120
                    cpcall: 60
                    total: 630
                    paidLeads: 30
                    paidCalls: 3
                    paidCpa: 4
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/reports/revenue/leads:
    get:
      summary: Revenue leads log (list or pivot)
      description: Paid CPL leads (cost + DQ payout > 0) in a date range, bucketed by lead.createdAt (tenant timezone). With
        pivotBy omitted or 'none' returns a row list (ordered by createdAt desc, capped at 500 with a truncated flag);
        with any other pivotBy returns an aggregated CPL roll-up. Never returns buyer-side columns.
      operationId: getPartnerRevenueLeadsLog
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          description: none (default) returns list rows; day/campaign/sub1..sub5 return aggregated pivot rows.
          schema:
            type: string
            enum:
              - none
              - day
              - campaign
              - sub1
              - sub2
              - sub3
              - sub4
              - sub5
            default: none
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List rows with a truncated flag (pivotBy=none) or aggregated pivot rows (any other pivotBy).
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      description: List row (id, createdAt, offerName, sub1..sub5, cplPayout) when pivotBy=none; otherwise pivot row (key,
                        label, cplPayout, paidLeads).
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        sub1:
                          type: string
                          nullable: true
                        sub2:
                          type: string
                          nullable: true
                        sub3:
                          type: string
                          nullable: true
                        sub4:
                          type: string
                          nullable: true
                        sub5:
                          type: string
                          nullable: true
                        cplPayout:
                          type: number
                        key:
                          type: string
                        label:
                          type: string
                        paidLeads:
                          type: integer
                  truncated:
                    type: boolean
                    description: Present only for the list response (pivotBy=none).
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/partner/reports/revenue/calls:
    get:
      summary: Revenue calls log (list or pivot)
      description: Paid calls (callSale.payout > 0) in a date range, bucketed by callSale.createdAt (tenant timezone). With
        pivotBy omitted or 'none' returns a row list (ordered by createdAt desc, capped at 500 with a truncated flag);
        day/campaign return aggregated payout. sub1..sub5 filters are silently ignored (callSale has no sub columns) and
        sub pivots are rejected at parse time. Never returns buyer-side columns.
      operationId: getPartnerRevenueCallsLog
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          description: none (default) returns list rows; day/campaign return aggregated rows.
          schema:
            type: string
            enum:
              - none
              - day
              - campaign
            default: none
        - name: campaignId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List rows with a truncated flag (pivotBy=none) or aggregated pivot rows (day/campaign).
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      description: List row (id, createdAt, offerName, durationSeconds, payout) when pivotBy=none; otherwise pivot row (key,
                        label, payout, paidCalls).
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        durationSeconds:
                          type: integer
                        payout:
                          type: number
                        key:
                          type: string
                        label:
                          type: string
                        paidCalls:
                          type: integer
                  truncated:
                    type: boolean
                    description: Present only for the list response (pivotBy=none).
        "400":
          description: Validation error (includes sub1..sub5 pivot rejection).
        "401":
          description: Unauthorized
  /api/partner/reports/revenue/acquisitions:
    get:
      summary: Revenue acquisitions (CPA) log (list or pivot)
      description: Unified CPA-converted log merging lead-CPA and call-CPA rows by bucket date (tenant timezone). With pivotBy
        omitted or 'none' returns combined rows sorted by convertedAt desc (capped at 500 with a truncated flag); any
        other pivotBy returns aggregated CPA payout. For sub1..sub5 pivots, call-source rows collapse into a single
        '(call)' bucket. Never returns buyer-side columns.
      operationId: getPartnerRevenueAcquisitionsLog
      tags:
        - "Partner: Reports"
      security:
        - bearerAuth: []
        - sessionCookie: []
      parameters:
        - name: start
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: end
          in: query
          required: true
          description: Must be >= start and <= 365 days after start.
          schema:
            type: string
            format: date
        - name: pivotBy
          in: query
          description: none (default) returns list rows; day/campaign/sub1..sub5 aggregate.
          schema:
            type: string
            enum:
              - none
              - day
              - campaign
              - sub1
              - sub2
              - sub3
              - sub4
              - sub5
            default: none
        - name: source
          in: query
          description: Which CPA sources to include.
          schema:
            type: string
            enum:
              - lead
              - call
              - all
            default: all
        - name: campaignId
          in: query
          schema:
            type: string
        - name: sub1
          in: query
          schema:
            type: string
        - name: sub2
          in: query
          schema:
            type: string
        - name: sub3
          in: query
          schema:
            type: string
        - name: sub4
          in: query
          schema:
            type: string
        - name: sub5
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Combined CPA rows with a truncated flag (pivotBy=none) or aggregated pivot rows (any other pivotBy).
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      description: List row (source, entityId, originalCreatedAt, convertedAt, offerName, sub1..sub5, cpaPayout) when
                        pivotBy=none; otherwise pivot row (key, label, cpaPayout, conversions, leadConversions,
                        callConversions).
                      properties:
                        source:
                          type: string
                          enum:
                            - lead
                            - call
                        entityId:
                          type: string
                        originalCreatedAt:
                          type: string
                          format: date-time
                        convertedAt:
                          type: string
                          format: date-time
                        offerName:
                          type: string
                          nullable: true
                        sub1:
                          type: string
                          nullable: true
                        sub2:
                          type: string
                          nullable: true
                        sub3:
                          type: string
                          nullable: true
                        sub4:
                          type: string
                          nullable: true
                        sub5:
                          type: string
                          nullable: true
                        cpaPayout:
                          type: number
                        key:
                          type: string
                        label:
                          type: string
                        conversions:
                          type: integer
                        leadConversions:
                          type: integer
                        callConversions:
                          type: integer
                  truncated:
                    type: boolean
                    description: Present only for the list response (pivotBy=none).
        "400":
          description: Validation error
        "401":
          description: Unauthorized
  /api/health:
    get:
      operationId: getHealth
      tags:
        - Public
      summary: Liveness probe
      description: >-
        Unauthenticated liveness probe used by uptime monitors, Vercel checks, and the deploy-verification scripts.


        Two-tier by design (security review B6). Anonymous callers get the SIGNAL but not the DETAIL: `200
        {"status":"ok"}` when healthy, `503 {"status":"degraded"}` when the database is unreachable. Callers presenting
        `Authorization: Bearer $CRON_SECRET` additionally receive the component breakdown (`db`, `latencyMs`) for
        triage.


        The status code deliberately still reflects database reachability — this endpoint is a load-bearing deploy gate,
        and a green health check while the DB is down would be a worse failure than the minor disclosure of reporting
        degradation.
      security: []
      responses:
        "200":
          description: Service healthy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ok
                  db:
                    type: boolean
                    description: Internal callers only (Bearer CRON_SECRET).
                  latencyMs:
                    type: integer
                    description: Internal callers only (Bearer CRON_SECRET).
                required:
                  - status
              examples:
                anonymous:
                  summary: Anonymous caller
                  value:
                    status: ok
                internal:
                  summary: Internal monitoring caller
                  value:
                    status: ok
                    db: true
                    latencyMs: 12
        "503":
          description: Service degraded — database unreachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - degraded
                required:
                  - status
  /api/v1/track:
    post:
      operationId: trackMarketingEvent
      tags:
        - Public
      summary: Marketing pageview beacon
      description: >-
        Public fire-and-forget pageview sink for the theleadrouter.com marketing site. Always answers `200
        {"data":{"ok":true}}` — the beacon never sees a 5xx and never reveals whether an event was stored.


        Visitor identity is taken from the middleware-minted `lr_vid`/`lr_sid` COOKIES only; identity fields in the
        request body are ignored, so events cannot be attributed to a spoofed visitor. Requests with missing or
        malformed identity cookies, and requests from recognised bot user agents, are accepted and silently dropped
        rather than stored with nulls.


        Rate-limited per IP.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event:
                  type: string
                  description: Event name (e.g. `pageview`).
                path:
                  type: string
                referrer:
                  type: string
                utm_source:
                  type: string
                utm_medium:
                  type: string
                utm_campaign:
                  type: string
                utm_term:
                  type: string
                utm_content:
                  type: string
      responses:
        "200":
          description: Always returned, whether the event was stored, dropped as a bot hit, or dropped for missing identity cookies.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
  /api/v1/auth/magic-login/request:
    post:
      operationId: requestMagicLogin
      tags:
        - Auth
      summary: Request a passwordless magic-login link
      description: >-
        Public endpoint. If the supplied email matches a user with at least one active membership in an active tenant,
        issues a one-time verification token (15-minute TTL) and emails a magic link.


        ENUMERATION-SAFE: always returns `200 {"data":{"ok":true}}` with a byte-identical body. Unknown email, no active
        membership, and suspended tenant are all silent-reject paths, so the response cannot be used to test whether an
        account exists.


        Rate-limited to 10 requests/hour per email AND 10 requests/hour per IP.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
                redirectTo:
                  type: string
                  description: Optional post-login destination. Normalised server-side to a same-origin relative path — external URLs are
                    rejected, so this is not an open-redirect vector.
      responses:
        "200":
          description: Always returned regardless of whether a link was actually sent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "429":
          description: Rate limit exceeded.
  /api/superadmin/tenants:
    get:
      summary: List tenants
      description: Returns a paginated list of all tenants. Supports search by name/slug and status filtering.
      operationId: listTenants
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: q
          in: query
          description: Search by tenant name or slug (case-insensitive partial match)
          schema:
            type: string
        - name: status
          in: query
          description: Filter by tenant status
          schema:
            type: string
            enum:
              - active
              - suspended
              - inactive
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of tenants
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Acme Corp
                    slug: acme-corp
                    status: active
                    plan: standard
                    domain: null
                    logoUrl: null
                    features: {}
                    settings: {}
                    createdAt: 2025-01-15 10:00:00+00:00
                    updatedAt: 2025-01-15 10:00:00+00:00
                pagination:
                  total: 12
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized - no valid session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a tenant
      description: Creates a new tenant organization. Slug must be unique, lowercase alphanumeric with hyphens.
      operationId: createTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - slug
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                slug:
                  type: string
                  minLength: 1
                  maxLength: 100
                  pattern: ^[a-z0-9-]+$
                plan:
                  type: string
                  enum:
                    - starter
                    - standard
                    - enterprise
                  default: standard
                settings:
                  type: object
                  additionalProperties: true
                  default: {}
                domain:
                  type: string
                  maxLength: 255
                  nullable: true
            example:
              name: Acme Corp
              slug: acme-corp
              plan: standard
              settings: {}
              domain: null
      responses:
        "201":
          description: Tenant created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Tenant"
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Acme Corp
                  slug: acme-corp
                  status: active
                  plan: standard
                  domain: null
                  logoUrl: null
                  features: {}
                  settings: {}
                  createdAt: 2025-01-15 10:00:00+00:00
                  updatedAt: 2025-01-15 10:00:00+00:00
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: VALIDATION_ERROR
                  message: Invalid request data
                  details:
                    fields:
                      slug:
                        - Slug must be lowercase alphanumeric with hyphens
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Duplicate slug
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: DUPLICATE
                  message: Tenant with this slug already exists
  /api/superadmin/tenants/{tenantId}:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant
        schema:
          type: string
          format: uuid
    get:
      summary: Get tenant by ID
      description: Returns a single tenant record by UUID.
      operationId: getTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Tenant details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Tenant"
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Acme Corp
                  slug: acme-corp
                  status: active
                  plan: enterprise
                  domain: acme.example.com
                  logoUrl: null
                  features:
                    telephony: true
                    messaging: false
                  settings: {}
                  createdAt: 2025-01-15 10:00:00+00:00
                  updatedAt: 2025-02-01 14:30:00+00:00
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update tenant
      description: Updates tenant fields. All fields optional. Slug uniqueness enforced if changing.
      operationId: updateTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                slug:
                  type: string
                  minLength: 1
                  maxLength: 100
                  pattern: ^[a-z0-9-]+$
                status:
                  type: string
                  enum:
                    - active
                    - suspended
                    - inactive
                plan:
                  type: string
                  enum:
                    - starter
                    - standard
                    - enterprise
                settings:
                  type: object
                  additionalProperties: true
                domain:
                  type: string
                  maxLength: 255
                  nullable: true
                logoUrl:
                  type: string
                  maxLength: 1024
                  nullable: true
            example:
              name: Acme Corp Renamed
              plan: enterprise
      responses:
        "200":
          description: Tenant updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Tenant"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Duplicate slug
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Partial update tenant
      description: Identical to PUT - accepts partial updates to tenant fields.
      operationId: patchTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                slug:
                  type: string
                  minLength: 1
                  maxLength: 100
                  pattern: ^[a-z0-9-]+$
                status:
                  type: string
                  enum:
                    - active
                    - suspended
                    - inactive
                plan:
                  type: string
                  enum:
                    - starter
                    - standard
                    - enterprise
                settings:
                  type: object
                  additionalProperties: true
                domain:
                  type: string
                  maxLength: 255
                  nullable: true
                logoUrl:
                  type: string
                  maxLength: 1024
                  nullable: true
            example:
              status: suspended
      responses:
        "200":
          description: Tenant updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Tenant"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Duplicate slug
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Soft-delete tenant
      description: Sets tenant status to "inactive". Does not permanently delete data.
      operationId: deleteTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Tenant soft-deleted (status set to inactive)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Tenant"
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Acme Corp
                  slug: acme-corp
                  status: inactive
                  plan: standard
                  createdAt: 2025-01-15 10:00:00+00:00
                  updatedAt: 2025-03-08 12:00:00+00:00
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/impersonate:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant to impersonate
        schema:
          type: string
          format: uuid
    post:
      summary: Impersonate tenant
      description: >
        Starts an impersonation session as the target tenant's admin. Sets the session cookie to the tenant's admin
        context with impersonating=true. Tenant must be active. Uses the tenant's first admin user if available,
        otherwise the superadmin's own user ID.
      operationId: impersonateTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Impersonation session started
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      name:
                        type: string
                      slug:
                        type: string
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  name: Acme Corp
                  slug: acme-corp
        "400":
          description: Cannot impersonate inactive tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: VALIDATION_ERROR
                  message: Cannot impersonate inactive tenant
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/stats:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant
        schema:
          type: string
          format: uuid
    get:
      summary: Get tenant stats
      description: Returns aggregate counts of leads, buyers, partners, and users for a tenant.
      operationId: getTenantStats
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Tenant statistics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      totalLeads:
                        type: integer
                      totalBuyers:
                        type: integer
                      totalPartners:
                        type: integer
                      totalUsers:
                        type: integer
              example:
                data:
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  totalLeads: 4523
                  totalBuyers: 12
                  totalPartners: 8
                  totalUsers: 5
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/features:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant
        schema:
          type: string
          format: uuid
    get:
      summary: Get tenant feature flags
      description: Returns the current feature flags map for a tenant.
      operationId: superadminGetTenantFeatures
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Feature flags map
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: boolean
              example:
                data:
                  telephony: true
                  messaging: false
                  consent: true
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update tenant feature flags
      description: >
        Merges feature flag changes into the tenant's existing flags. Accepts either a single feature toggle or a bulk
        map of features.
      operationId: updateTenantFeaturesSuperadmin
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  description: Single feature toggle
                  required:
                    - feature
                    - enabled
                  properties:
                    feature:
                      type: string
                    enabled:
                      type: boolean
                - type: object
                  description: Bulk feature update
                  required:
                    - features
                  properties:
                    features:
                      type: object
                      additionalProperties:
                        type: boolean
            examples:
              single:
                summary: Toggle a single feature
                value:
                  feature: telephony
                  enabled: true
              bulk:
                summary: Bulk update multiple features
                value:
                  features:
                    telephony: true
                    messaging: false
                    consent: true
      responses:
        "200":
          description: Updated feature flags map (full merged result)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: boolean
              example:
                data:
                  telephony: true
                  messaging: false
                  consent: true
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/users:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant
        schema:
          type: string
          format: uuid
    get:
      summary: List tenant users
      description: Returns a paginated list of users belonging to a specific tenant. Supports search by email, first name, or
        last name.
      operationId: listTenantUsers
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: q
          in: query
          description: Search by email, first name, or last name (case-insensitive partial match)
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of tenant users
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    firstName: Jane
                    lastName: Doe
                    email: jane@acme.com
                    role: admin
                    status: active
                    lastLoginAt: 2025-03-07 18:00:00+00:00
                    createdAt: 2025-01-15 10:00:00+00:00
                pagination:
                  total: 5
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create user in tenant
      description: Creates a new user under a specific tenant. Email must be unique within the tenant.
      operationId: createTenantUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  minLength: 8
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                role:
                  type: string
                  default: admin
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
            example:
              email: newuser@acme.com
              password: secureP@ss123
              firstName: John
              lastName: Smith
              role: admin
      responses:
        "201":
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/UserSummary"
              example:
                data:
                  id: c3d4e5f6-a7b8-9012-cdef-123456789012
                  email: newuser@acme.com
                  firstName: John
                  lastName: Smith
                  role: admin
                  status: active
                  createdAt: 2025-03-08 12:00:00+00:00
        "400":
          description: Validation error (missing email/password, password too short)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: VALIDATION_ERROR
                  message: Password must be at least 8 characters
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Duplicate email in tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: DUPLICATE
                  message: User with this email already exists in tenant
  /api/superadmin/tenants/{tenantId}/users/{userId}:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant
        schema:
          type: string
          format: uuid
      - name: userId
        in: path
        required: true
        description: UUID of the user
        schema:
          type: string
          format: uuid
    get:
      summary: Get tenant user
      description: Returns full details of a user within a specific tenant, including permissions and profile fields.
      operationId: getTenantUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: User details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/UserDetail"
              example:
                data:
                  id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  email: jane@acme.com
                  firstName: Jane
                  lastName: Doe
                  jobTitle: Operations Manager
                  phone: "+15551234567"
                  workPhone: null
                  ext: null
                  role: admin
                  profilePicture: null
                  isOwner: true
                  isAdmin: true
                  dataExport: true
                  massEmails: false
                  notifications: true
                  support: true
                  billing: true
                  newsletters: false
                  apiAccess: true
                  apiSecret: null
                  status: active
                  buyerId: null
                  lastLoginAt: 2025-03-07 18:00:00+00:00
                  createdAt: 2025-01-15 10:00:00+00:00
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found in tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update tenant user
      description: >
        Updates allowed fields on a tenant user. Allowed fields: firstName, lastName, email, jobTitle, phone, workPhone,
        ext, role, isAdmin, dataExport, massEmails, notifications, support, billing, newsletters, apiAccess, status,
        buyerId.
      operationId: updateTenantUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                jobTitle:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                workPhone:
                  type: string
                  nullable: true
                ext:
                  type: string
                  nullable: true
                role:
                  type: string
                isAdmin:
                  type: boolean
                dataExport:
                  type: boolean
                massEmails:
                  type: boolean
                notifications:
                  type: boolean
                support:
                  type: boolean
                billing:
                  type: boolean
                newsletters:
                  type: boolean
                apiAccess:
                  type: boolean
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                buyerId:
                  type: string
                  format: uuid
                  nullable: true
            example:
              firstName: Jane
              lastName: Smith
              role: admin
      responses:
        "200":
          description: User updated (returns full user record)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/UserDetail"
        "400":
          description: No valid fields to update
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found in tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Patch tenant user (action-based)
      description: >
        Performs an action on a tenant user. Supports two actions: `setPassword` (resets user password, min 8 chars) and
        `setStatus` (sets user status to active or inactive).
      operationId: patchTenantUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  description: Reset password
                  required:
                    - action
                    - password
                  properties:
                    action:
                      type: string
                      enum:
                        - setPassword
                    password:
                      type: string
                      minLength: 8
                - type: object
                  description: Set status
                  required:
                    - action
                    - status
                  properties:
                    action:
                      type: string
                      enum:
                        - setStatus
                    status:
                      type: string
                      enum:
                        - active
                        - inactive
            examples:
              setPassword:
                summary: Reset user password
                value:
                  action: setPassword
                  password: newSecureP@ss456
              setStatus:
                summary: Deactivate user
                value:
                  action: setStatus
                  status: inactive
      responses:
        "200":
          description: Action performed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
              example:
                data:
                  success: true
        "400":
          description: Validation error (bad password, invalid status, unknown action)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found in tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Soft-delete tenant user
      description: Sets the user's status to "inactive". Does not permanently delete the record.
      operationId: deleteTenantUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: User soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - inactive
              example:
                data:
                  id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  status: inactive
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found in tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/users:
    get:
      summary: List all users
      description: >
        Returns a paginated list of all users across all tenants. Supports search, filtering by role/status/tenantId,
        and sorting. Includes tenant name via left join.
      operationId: listAllUsers
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: q
          in: query
          description: Search by email, first name, or last name (case-insensitive partial match)
          schema:
            type: string
        - name: role
          in: query
          description: Filter by user role
          schema:
            type: string
        - name: status
          in: query
          description: Filter by user status
          schema:
            type: string
            enum:
              - active
              - inactive
        - name: tenantId
          in: query
          description: Filter by tenant UUID
          schema:
            type: string
            format: uuid
        - name: sort
          in: query
          description: Sort column
          schema:
            type: string
            enum:
              - createdAt
              - email
              - firstName
              - role
              - status
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of users with tenant name
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    email: jane@acme.com
                    firstName: Jane
                    lastName: Doe
                    role: admin
                    status: active
                    lastLoginAt: 2025-03-07 18:00:00+00:00
                    createdAt: 2025-01-15 10:00:00+00:00
                    tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    tenantName: Acme Corp
                pagination:
                  total: 48
                  limit: 20
                  offset: 0
                  hasMore: true
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create user (cross-tenant)
      description: >
        Creates a new user. For superadmin role, tenantId is set to null. For all other roles, tenantId is required.
        Email must be unique within the tenant scope.
      operationId: superadminCreateUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  minLength: 8
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                role:
                  type: string
                  default: admin
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
                tenantId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Required for non-superadmin roles. Set to null for superadmin users.
            example:
              email: super@iscale.com
              password: secureP@ss123
              firstName: Super
              lastName: Admin
              role: superadmin
      responses:
        "201":
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      email:
                        type: string
                      firstName:
                        type: string
                        nullable: true
                      lastName:
                        type: string
                        nullable: true
                      role:
                        type: string
                      status:
                        type: string
                      tenantId:
                        type: string
                        format: uuid
                        nullable: true
                      createdAt:
                        type: string
                        format: date-time
              example:
                data:
                  id: d4e5f6a7-b8c9-0123-def0-123456789abc
                  email: super@iscale.com
                  firstName: Super
                  lastName: Admin
                  role: superadmin
                  status: active
                  tenantId: null
                  createdAt: 2025-03-08 12:00:00+00:00
        "400":
          description: Validation error (missing fields, password too short, tenantId required for non-superadmin)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Duplicate email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: DUPLICATE
                  message: User with this email already exists
  /api/superadmin/users/{userId}:
    parameters:
      - name: userId
        in: path
        required: true
        description: UUID of the user
        schema:
          type: string
          format: uuid
    get:
      summary: Get user by ID
      description: Returns full user details including permissions, profile fields, and tenant association.
      operationId: superadminGetUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: User details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/UserDetail"
              example:
                data:
                  id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  email: jane@acme.com
                  firstName: Jane
                  lastName: Doe
                  jobTitle: Operations Manager
                  phone: "+15551234567"
                  workPhone: null
                  ext: null
                  role: admin
                  profilePicture: null
                  isOwner: true
                  isAdmin: true
                  dataExport: true
                  massEmails: false
                  notifications: true
                  support: true
                  billing: true
                  newsletters: false
                  apiAccess: true
                  apiSecret: null
                  status: active
                  buyerId: null
                  lastLoginAt: 2025-03-07 18:00:00+00:00
                  createdAt: 2025-01-15 10:00:00+00:00
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update user
      description: >
        Updates allowed fields on a user. Allowed fields: firstName, lastName, email, jobTitle, phone, workPhone, ext,
        role, isAdmin, dataExport, massEmails, notifications, support, billing, newsletters, apiAccess, status, buyerId,
        tenantId.
      operationId: superadminUpdateUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                jobTitle:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                workPhone:
                  type: string
                  nullable: true
                ext:
                  type: string
                  nullable: true
                role:
                  type: string
                isAdmin:
                  type: boolean
                dataExport:
                  type: boolean
                massEmails:
                  type: boolean
                notifications:
                  type: boolean
                support:
                  type: boolean
                billing:
                  type: boolean
                newsletters:
                  type: boolean
                apiAccess:
                  type: boolean
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                buyerId:
                  type: string
                  format: uuid
                  nullable: true
                tenantId:
                  type: string
                  format: uuid
                  nullable: true
            example:
              role: superadmin
              tenantId: null
      responses:
        "200":
          description: User updated (returns full user record)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/UserDetail"
        "400":
          description: No valid fields to update
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Patch user (action-based)
      description: >
        Performs an action on a user. Supports two actions: `setPassword` (resets password, min 8 chars) and `setStatus`
        (sets status to active or inactive).
      operationId: patchUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  description: Reset password
                  required:
                    - action
                    - password
                  properties:
                    action:
                      type: string
                      enum:
                        - setPassword
                    password:
                      type: string
                      minLength: 8
                - type: object
                  description: Set status
                  required:
                    - action
                    - status
                  properties:
                    action:
                      type: string
                      enum:
                        - setStatus
                    status:
                      type: string
                      enum:
                        - active
                        - inactive
            examples:
              setPassword:
                summary: Reset user password
                value:
                  action: setPassword
                  password: newSecureP@ss456
              setStatus:
                summary: Activate user
                value:
                  action: setStatus
                  status: active
      responses:
        "200":
          description: Action performed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
              example:
                data:
                  success: true
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Soft-delete user
      description: Sets the user's status to "inactive". Does not permanently delete the record.
      operationId: superadminDeleteUser
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: User soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - inactive
              example:
                data:
                  id: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  status: inactive
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/users/{userId}/login-activity:
    parameters:
      - name: userId
        in: path
        required: true
        description: UUID of the user
        schema:
          type: string
          format: uuid
    get:
      summary: Get user login activity
      description: Returns paginated login activity records for a specific user, ordered by most recent first.
      operationId: getUserLoginActivity
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          description: Max records to return (capped at 100)
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated login activity
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        browser:
                          type: string
                          nullable: true
                        success:
                          type: boolean
                        ipAddress:
                          type: string
                          nullable: true
                        timestamp:
                          type: string
                          format: date-time
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
              example:
                data:
                  - id: e5f6a7b8-c9d0-1234-ef01-234567890abc
                    browser: Chrome 120.0 / macOS
                    success: true
                    ipAddress: 192.168.1.100
                    timestamp: 2025-03-07 18:00:00+00:00
                  - id: f6a7b8c9-d0e1-2345-f012-34567890abcd
                    browser: Safari 17.0 / iOS
                    success: false
                    ipAddress: 10.0.0.5
                    timestamp: 2025-03-06 09:15:00+00:00
                pagination:
                  total: 127
                  limit: 50
                  offset: 0
                  hasMore: true
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/impersonate/exit:
    post:
      summary: Exit impersonation
      description: >
        Ends the current impersonation session and restores the superadmin's original session. Requires that the current
        session has impersonating=true and a valid adminUserId.
      operationId: superadminExitImpersonation
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Impersonation ended, superadmin session restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      redirectTo:
                        type: string
              example:
                data:
                  redirectTo: /superadmin
        "400":
          description: Not currently impersonating
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: VALIDATION_ERROR
                  message: Not currently impersonating
        "401":
          description: Unauthorized - no valid session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Original admin user not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/telephony/credentials:
    get:
      summary: List system telephony credentials
      description: >
        Returns paginated list of system-scope telephony credentials (tenantId=null). API keys and secrets are masked in
        responses. Supports filtering by provider.
      operationId: superadminListTelephonyCredentials
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      parameters:
        - name: provider
          in: query
          description: Filter by telephony provider
          schema:
            type: string
            enum:
              - telnyx
              - twilio
              - bandwidth
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated list of credentials (keys masked)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: f6a7b8c9-d0e1-2345-f012-34567890abcd
                    scope: system
                    tenantId: null
                    provider: telnyx
                    apiKey: KEY_...xyz
                    apiSecret: "***"
                    publicKey: null
                    connectionId: "1234567890"
                    extra: null
                    status: active
                    createdAt: 2025-02-01 10:00:00+00:00
                    updatedAt: 2025-02-15 14:30:00+00:00
                pagination:
                  total: 2
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create or upsert a telephony credential (system, or tenant-scope BYO)
      description: >
        Creates a new system-scope telephony credential. If a system credential for the same provider already exists, it
        is updated instead (upsert). Pass an optional `tenantId` to instead attach a tenant-scope (BYO) credential for
        that tenant — it upserts the tenant's own row for the provider and resolves ahead of the system default. API key
        and secret are encrypted at rest. Returns 201 on create, 200 on upsert/update.
      operationId: createTelephonyCredential
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - provider
                - apiKey
              properties:
                provider:
                  type: string
                  enum:
                    - telnyx
                    - twilio
                    - bandwidth
                    - signalwire
                tenantId:
                  type: string
                  format: uuid
                  nullable: true
                  description: When present, attaches a tenant-scope (BYO) credential for this tenant instead of a system credential.
                apiKey:
                  type: string
                  minLength: 1
                apiSecret:
                  type: string
                  nullable: true
                publicKey:
                  type: string
                  nullable: true
                connectionId:
                  type: string
                  nullable: true
                extra:
                  type: object
                  additionalProperties: true
                  nullable: true
            example:
              provider: telnyx
              apiKey: KEY12345abcdef
              apiSecret: SECRET_xyz789
              connectionId: "1234567890"
      responses:
        "200":
          description: Credential updated (existing provider upserted)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TelephonyCredentialMasked"
        "201":
          description: Credential created (new provider)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TelephonyCredentialMasked"
              example:
                data:
                  id: f6a7b8c9-d0e1-2345-f012-34567890abcd
                  scope: system
                  tenantId: null
                  provider: telnyx
                  apiKey: KEY_...def
                  apiSecret: "***"
                  publicKey: null
                  connectionId: "1234567890"
                  extra: null
                  status: active
                  createdAt: 2025-03-08 12:00:00+00:00
                  updatedAt: 2025-03-08 12:00:00+00:00
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete system credential by query param
      description: >
        Deletes a system-scope telephony credential by ID passed as query parameter. Only system-scope credentials can
        be deleted via this endpoint.
      operationId: deleteTelephonyCredentialByQuery
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: query
          required: true
          description: UUID of the credential to delete
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Credential permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
              example:
                data:
                  id: f6a7b8c9-d0e1-2345-f012-34567890abcd
                  deleted: true
        "400":
          description: Missing id query parameter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - can only delete system-level credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Credential not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/telephony/credentials/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: UUID of the telephony credential
        schema:
          type: string
          format: uuid
    get:
      summary: Get telephony credential
      description: Returns a single system-scope telephony credential by ID. Keys are masked.
      operationId: superadminGetTelephonyCredential
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Credential details (keys masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TelephonyCredentialMasked"
              example:
                data:
                  id: f6a7b8c9-d0e1-2345-f012-34567890abcd
                  scope: system
                  tenantId: null
                  provider: telnyx
                  apiKey: KEY_...def
                  apiSecret: "***"
                  publicKey: null
                  connectionId: "1234567890"
                  extra: null
                  status: active
                  createdAt: 2025-02-01 10:00:00+00:00
                  updatedAt: 2025-02-15 14:30:00+00:00
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Credential not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update telephony credential
      description: Updates fields on a system-scope telephony credential. All fields optional. Credentials re-encrypted on update.
      operationId: superadminUpdateTelephonyCredential
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                apiKey:
                  type: string
                  minLength: 1
                apiSecret:
                  type: string
                  nullable: true
                publicKey:
                  type: string
                  nullable: true
                connectionId:
                  type: string
                  nullable: true
                extra:
                  type: object
                  additionalProperties: true
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
            example:
              apiKey: NEW_KEY_abc123
              status: active
      responses:
        "200":
          description: Credential updated (keys masked in response)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TelephonyCredentialMasked"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Credential not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Partial update telephony credential
      description: Identical to PUT - accepts partial updates to credential fields.
      operationId: patchTelephonyCredential
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                apiKey:
                  type: string
                  minLength: 1
                apiSecret:
                  type: string
                  nullable: true
                publicKey:
                  type: string
                  nullable: true
                connectionId:
                  type: string
                  nullable: true
                extra:
                  type: object
                  additionalProperties: true
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
            example:
              status: inactive
      responses:
        "200":
          description: Credential updated (keys masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TelephonyCredentialMasked"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Credential not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete telephony credential
      description: Permanently deletes a system-scope telephony credential. Only system-scope credentials can be deleted.
      operationId: superadminDeleteTelephonyCredential
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Credential permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
              example:
                data:
                  id: f6a7b8c9-d0e1-2345-f012-34567890abcd
                  deleted: true
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - can only delete system-level credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Credential not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/settings/signup:
    get:
      summary: Get signup settings
      description: Returns the current self-service signup configuration (enabled, approval mode, terms).
      operationId: getSignupSettings
      tags:
        - "Superadmin: Settings"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Signup settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      approvalMode:
                        type: string
                        enum:
                          - auto
                          - manual
                      requireTerms:
                        type: boolean
                      termsUrl:
                        type: string
                        nullable: true
              example:
                data:
                  enabled: true
                  approvalMode: manual
                  requireTerms: true
                  termsUrl: https://example.com/terms
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update signup settings
      description: Updates the self-service signup configuration.
      operationId: updateSignupSettings
      tags:
        - "Superadmin: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                approvalMode:
                  type: string
                  enum:
                    - auto
                    - manual
                requireTerms:
                  type: boolean
                termsUrl:
                  type: string
                  nullable: true
            example:
              enabled: true
              approvalMode: auto
              requireTerms: false
              termsUrl: null
      responses:
        "200":
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
              example:
                data:
                  success: true
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/approve:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant to approve
        schema:
          type: string
          format: uuid
    post:
      summary: Approve pending tenant
      description: Approves a pending tenant signup, setting status to active.
      operationId: approveTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Tenant approved
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - active
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  status: active
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/reject:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant to reject
        schema:
          type: string
          format: uuid
    post:
      summary: Reject pending tenant
      description: Rejects a pending tenant signup, setting status to inactive.
      operationId: rejectTenant
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Optional rejection reason
            example:
              reason: Duplicate account
      responses:
        "200":
          description: Tenant rejected
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - inactive
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  status: inactive
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/agent/sessions:
    get:
      summary: List agent sessions
      operationId: listAgentSessions
      tags:
        - "Superadmin: AI Agent"
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: tenantId
          in: query
          schema:
            type: string
        - name: userId
          in: query
          schema:
            type: string
        - name: lane
          in: query
          schema:
            type: string
        - name: workflowName
          in: query
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Paginated agent sessions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/agent/sessions/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get agent session
      operationId: getAgentSession
      tags:
        - "Superadmin: AI Agent"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Agent session detail
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Session not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/agent/sessions/stats:
    get:
      summary: Get agent session stats
      operationId: getAgentSessionStats
      tags:
        - "Superadmin: AI Agent"
      security:
        - sessionCookie: []
      parameters:
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
        - name: tenantId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Agent session statistics
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/settings/ai-domains:
    get:
      summary: Get AI domains
      operationId: getAiDomains
      tags:
        - "Superadmin: Settings"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: AI domain list
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update AI domains
      operationId: updateAiDomains
      tags:
        - "Superadmin: Settings"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - domains
              properties:
                domains:
                  type: array
                  items:
                    type: string
      responses:
        "200":
          description: AI domains updated
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden - superadmin role required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/providers:
    get:
      summary: List system provider credentials
      description: |
        Returns every `scope=system` `providerCredential` row across the
        platform catalog. Secrets are masked. Tenants only see the subset
        where `shareable=true` via `/api/v1/providers`.
      operationId: superadminListProviderCredentials
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: System credential list (masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProviderCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create a system provider credential
      description: |
        Creates a system catalog row. `scope` is forced to `system` and
        `tenantId` to `null` regardless of the request body. Supports
        `shareable`, `autoEnableForNewTenants`, and `limits` for the catalog
        UX (tenants subscribe via assignments).
      operationId: superadminCreateProviderCredential
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderCredentialCreate"
      responses:
        "201":
          description: System credential created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderCredential"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/providers/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: UUID of the system provider credential.
        schema:
          type: string
          format: uuid
    get:
      summary: Get a single system provider credential
      operationId: superadminGetProviderCredential
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: System credential (masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a system provider credential
      description: |
        Partial update. Unlike the tenant route, `shareable`,
        `autoEnableForNewTenants`, and `limits` are honored here. Provided
        `credentials` keys are re-encrypted; omitted keys preserve existing
        ciphertext.
      operationId: superadminUpdateProviderCredential
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderCredentialUpdate"
      responses:
        "200":
          description: System credential updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderCredential"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a system provider credential
      description: Returns 409 if any tenant still has an assignment referencing this credential.
      operationId: superadminDeleteProviderCredential
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: System credential deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — credential still has active assignments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/providers/{id}/test:
    parameters:
      - name: id
        in: path
        required: true
        description: UUID of the system provider credential to test.
        schema:
          type: string
          format: uuid
    post:
      summary: Send a test SMS via a system credential
      description: |
        Superadmin equivalent of the tenant `POST /api/v1/providers/{id}/test`
        — fires through the system credential directly without requiring an
        assignment. Rate-limited to **10 calls per minute per superadmin**.
        Writes a `deliveryLog` row when the request includes `tenantId` or the
        superadmin currently has an active tenant context.
      operationId: superadminTestProviderCredential
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderTestSendRequest"
      responses:
        "200":
          description: Test send result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderTestSendResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Credential is not configured for SMS sending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded (10/min per superadmin).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/tenants/{tenantId}/assignments:
    parameters:
      - name: tenantId
        in: path
        required: true
        description: UUID of the tenant to inspect.
        schema:
          type: string
          format: uuid
    get:
      summary: List a tenant's provider assignments
      description: |
        Drill-down view used by the superadmin Per-tenant Assignments tab.
        Returns every `providerAssignment` for the tenant with the referenced
        credential's name, provider, scope, shareable, and status inlined.
      operationId: superadminListTenantAssignments
      tags:
        - "Superadmin: Providers"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Assignment list with inlined credential metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      allOf:
                        - $ref: "#/components/schemas/ProviderAssignment"
                        - type: object
                          properties:
                            credential:
                              type: object
                              properties:
                                id:
                                  type: string
                                  format: uuid
                                name:
                                  type: string
                                  nullable: true
                                provider:
                                  $ref: "#/components/schemas/ProviderName"
                                scope:
                                  $ref: "#/components/schemas/ProviderScope"
                                shareable:
                                  type: boolean
                                status:
                                  $ref: "#/components/schemas/ProviderStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing:
    get:
      summary: Get default platform fee
      description: Returns the system-wide default platform fee in basis points. Superadmin only.
      operationId: getSuperadminBilling
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Default platform fee
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      defaultPlatformFeeBps:
                        type: integer
              example:
                data:
                  defaultPlatformFeeBps: 500
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      summary: Set default platform fee
      description: Updates the system-wide default platform fee in basis points (0-10000). Superadmin only.
      operationId: updateSuperadminBilling
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - defaultPlatformFeeBps
              properties:
                defaultPlatformFeeBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
            example:
              defaultPlatformFeeBps: 500
      responses:
        "200":
          description: Updated default platform fee
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      defaultPlatformFeeBps:
                        type: integer
              example:
                data:
                  defaultPlatformFeeBps: 500
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/ai-costs:
    get:
      summary: List AI service costs
      description: Paginated AI-service credit transactions (ai_dialer, ai_agent, enrichment) with a platform-cost/margin
        summary grouped by service. Superadmin only.
      operationId: listSuperadminBillingAiCosts
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: service
          in: query
          description: Filter by service (ai_dialer, ai_agent, enrichment)
          schema:
            type: string
        - name: tenantId
          in: query
          description: Filter by tenant
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          description: Include transactions created on or after this date/time
          schema:
            type: string
        - name: to
          in: query
          description: Include transactions created on or before this date (end-of-day)
          schema:
            type: string
      responses:
        "200":
          description: AI cost transactions with summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    type: object
                    properties:
                      totalPlatformCost:
                        type: integer
                      totalCreditsCharged:
                        type: string
                        description: Decimal credit string (≤4 dp)
                      totalMargin:
                        type: integer
                      byService:
                        type: object
                        additionalProperties: true
                  data:
                    type: array
                    items:
                      type: object
                  pagination:
                    type: object
              example:
                summary:
                  totalPlatformCost: 1240
                  totalCreditsCharged: "3200.5"
                  totalMargin: -1240
                  byService:
                    ai_dialer:
                      cost: 1240
                      credits: "3200.5"
                      margin: -1240
                data:
                  - id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                    tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    tenantName: Acme Corp
                    service: ai_dialer
                    units: 12
                    credits: -320
                    platformCostUsd: 124
                    amountUsd: 0
                    description: AI dialer usage
                    createdAt: 2026-01-15T10:00:00.000Z
                    margin: -124
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/export:
    get:
      summary: Export billing CSV
      description: Downloads a CSV of per-tenant billing (plan, balances, credits purchased/used, revenue, platform cost,
        margin). Superadmin only.
      operationId: exportSuperadminBilling
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - name: billingMode
          in: query
          description: Filter by tenant billing mode
          schema:
            type: string
        - name: planId
          in: query
          description: Filter by subscription plan
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          description: Include usage transactions on or after this date/time
          schema:
            type: string
        - name: to
          in: query
          description: Include usage transactions on or before this date/time
          schema:
            type: string
      responses:
        "200":
          description: CSV file (attachment)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/overview:
    get:
      summary: Billing overview
      description: Platform-wide billing totals (credits purchased, revenue, MRR, platform cost, margin) plus billing health
        from the billing audit (audit issues, webhook status, payment integrity). Superadmin only.
      operationId: getSuperadminBillingOverview
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - name: from
          in: query
          description: Restrict revenue/cost aggregation to transactions on or after this date/time
          schema:
            type: string
      responses:
        "200":
          description: Billing overview and health
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      creditsPurchased:
                        type: string
                        description: Decimal credit string (≤4 dp)
                      revenueUsd:
                        type: integer
                      mrrUsd:
                        type: integer
                      platformCostUsd:
                        type: integer
                      marginUsd:
                        type: integer
                      periodFrom:
                        type: string
                        nullable: true
                      billingHealth:
                        type: object
              example:
                data:
                  creditsPurchased: "12000"
                  revenueUsd: 240000
                  mrrUsd: 9700
                  platformCostUsd: 5400
                  marginUsd: 244300
                  periodFrom: null
                  billingHealth:
                    auditIssues: 0
                    criticalIssues: 0
                    warningIssues: 0
                    failedWebhooks: 0
                    retryableWebhooks: 0
                    processedWebhooks: 128
                    recordedPayments: 42
                    paymentsMissingRequiredFields: 0
                    stuckReservations: 0
                    balanceDriftIssues: 0
                    latestAuditAt: 2026-01-15T10:00:00.000Z
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/service-costs:
    get:
      summary: List service costs
      description: Paginated credit transactions where iSCALE paid a real platform cost, with a total/per-service cost
        summary. Superadmin only.
      operationId: listSuperadminBillingServiceCosts
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: service
          in: query
          schema:
            type: string
        - name: tenantId
          in: query
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          schema:
            type: string
        - name: to
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Service cost transactions with summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    type: object
                    properties:
                      totalCost:
                        type: integer
                      byService:
                        type: object
                        additionalProperties:
                          type: integer
                  data:
                    type: array
                    items:
                      type: object
                  pagination:
                    type: object
              example:
                summary:
                  totalCost: 1240
                  byService:
                    ai_dialer: 1240
                data: []
                pagination:
                  total: 0
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/tenants:
    get:
      summary: List tenant billing
      description: Paginated per-tenant billing rows (plan, credit balances, subscription status, credits used, revenue).
        Superadmin only.
      operationId: listSuperadminBillingTenants
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: billingMode
          in: query
          schema:
            type: string
        - name: planId
          in: query
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          schema:
            type: string
        - name: to
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Paginated tenant billing rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Acme Corp
                    slug: acme-corp
                    billingMode: credits
                    billingExempt: false
                    planId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                    planName: Standard
                    creditBalance: "12000"
                    planCreditBalance: "5000"
                    subscriptionStatus: active
                    creditsUsed: "320"
                    revenueUsd: 24000
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/tenants/{id}/usage:
    get:
      summary: Get tenant service usage
      description: Per-service credit-usage breakdown for a single tenant (credits used, units, platform cost, transaction
        count). Superadmin only.
      operationId: getSuperadminBillingTenantUsage
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          schema:
            type: string
        - name: to
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Tenant service usage
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      tenantName:
                        type: string
                      services:
                        type: array
                        items:
                          type: object
                      totalCreditsUsed:
                        type: number
                        description: May be fractional (decimal credits)
                      totalPlatformCostUsd:
                        type: integer
              example:
                data:
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  tenantName: Acme Corp
                  services:
                    - service: ai_dialer
                      creditsUsed: 320
                      units: 12
                      platformCostUsd: 124
                      transactionCount: 8
                  totalCreditsUsed: 320
                  totalPlatformCostUsd: 124
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/billing/transactions:
    get:
      summary: List credit transactions
      description: Paginated tenant credit transactions across all tenants (type, credits, balanceAfter, service, related
        entity, platform cost, amount). Superadmin only.
      operationId: listSuperadminBillingTransactions
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: type
          in: query
          description: Filter by transaction type (purchase, deduction, platform_deduction, adjustment, ...)
          schema:
            type: string
        - name: service
          in: query
          schema:
            type: string
        - name: tenantId
          in: query
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          schema:
            type: string
        - name: to
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Paginated credit transactions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/usage:
    get:
      summary: Cross-tenant usage stats
      description: Per-tenant aggregate counts (leads, sales, revenue, cost, sessions, users, buyers, partners, campaigns,
        contracts, offers) over a date range. Each row carries the tenant's `currency`; cross-tenant revenue/cost totals
        are returned grouped per currency in `totalsByCurrency` (never summed across currencies). Superadmin only.
      operationId: getSuperadminUsage
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - name: dateFrom
          in: query
          description: Start date (YYYY-MM-DD); defaults to 30 days ago
          schema:
            type: string
            format: date
        - name: dateTo
          in: query
          description: End date (YYYY-MM-DD); defaults to today
          schema:
            type: string
            format: date
        - name: sort
          in: query
          description: Sort field
          schema:
            type: string
            enum:
              - tenantName
              - totalLeads
              - totalSales
              - totalRevenue
              - totalCost
              - totalSessions
              - totalUsers
              - totalBuyers
              - totalPartners
              - totalCampaigns
              - totalContracts
              - totalOffers
            default: totalLeads
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
      responses:
        "200":
          description: Per-tenant usage rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                  totalsByCurrency:
                    type: object
                    description: Per-currency revenue/cost totals (key = lowercase ISO 4217 tenant currency). Cross-tenant money is never
                      summed across currencies.
                    additionalProperties:
                      type: object
                      properties:
                        totalRevenue:
                          type: string
                        totalCost:
                          type: string
                  dateRange:
                    type: object
                    properties:
                      from:
                        type: string
                      to:
                        type: string
              example:
                data:
                  - tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    tenantName: Acme Corp
                    tenantSlug: acme-corp
                    tenantStatus: active
                    currency: usd
                    totalLeads: 1200
                    totalSales: 340
                    totalRevenue: "4200.00"
                    totalCost: "900.00"
                    totalSessions: 88
                    totalUsers: 12
                    totalBuyers: 5
                    totalPartners: 8
                    totalCampaigns: 14
                    totalContracts: 22
                    totalOffers: 9
                totalsByCurrency:
                  usd:
                    totalRevenue: "4200.00"
                    totalCost: "900.00"
                dateRange:
                  from: 2025-12-16
                  to: 2026-01-15
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/credits/adjust:
    post:
      summary: Adjust tenant credits
      description: Manually add or deduct tenant credits with a required description, recorded as an adjustment transaction.
        Superadmin only.
      operationId: adjustSuperadminCredits
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantId
                - credits
                - description
              properties:
                tenantId:
                  type: string
                  format: uuid
                credits:
                  type: string
                  pattern: ^-?\d+(\.\d{1,4})?$
                  description: Non-zero decimal credit string (≤4 dp); positive adds, negative deducts
                description:
                  type: string
                  minLength: 1
            example:
              tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              credits: "500"
              description: Goodwill credit
      responses:
        "200":
          description: Adjustment applied
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      credits:
                        type: string
                        description: Decimal credit string (≤4 dp)
                      balanceAfter:
                        type: string
                        description: Decimal credit string (≤4 dp)
                      creditBalance:
                        type: string
                        description: Decimal credit string (≤4 dp)
              example:
                data:
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  credits: "500"
                  balanceAfter: "12500.5"
                  creditBalance: "12500.5"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/credits/packages:
    get:
      summary: List credit packages
      description: Returns all credit packages ordered by sort order and price. Optional status filter. Superadmin only.
      operationId: listSuperadminCreditPackages
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          description: Filter by package status
          schema:
            type: string
      responses:
        "200":
          description: Credit packages
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create credit package
      description: Creates a credit package. priceUsd is integer cents (min 100). Superadmin only.
      operationId: createSuperadminCreditPackage
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - credits
                - priceUsd
              properties:
                name:
                  type: string
                  minLength: 1
                credits:
                  type: integer
                  minimum: 1
                priceUsd:
                  type: integer
                  minimum: 100
                  description: Price in integer cents
                discountPercent:
                  type: number
                  minimum: 0
                  maximum: 100
                  default: 0
                sortOrder:
                  type: integer
                  default: 0
            example:
              name: 10k Credits
              credits: 10000
              priceUsd: 25000
              discountPercent: 0
              sortOrder: 0
      responses:
        "201":
          description: Credit package created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/credits/packages/{id}:
    patch:
      summary: Update credit package
      description: Updates a credit package (all fields optional). Superadmin only.
      operationId: updateSuperadminCreditPackage
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                credits:
                  type: integer
                  minimum: 1
                priceUsd:
                  type: integer
                  minimum: 100
                discountPercent:
                  type: number
                  minimum: 0
                  maximum: 100
                sortOrder:
                  type: integer
            example:
              priceUsd: 20000
      responses:
        "200":
          description: Updated credit package
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Deactivate credit package
      description: Soft-deletes a credit package by setting its status to inactive. Superadmin only.
      operationId: deleteSuperadminCreditPackage
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Deactivated credit package
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/credits/plans:
    get:
      summary: List subscription plans
      description: Returns all tenant subscription plans ordered by sort order and monthly price. Optional status filter.
        Superadmin only.
      operationId: listSuperadminCreditPlans
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Subscription plans
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create subscription plan
      description: Creates a tenant subscription plan. monthlyPriceUsd is integer cents. Superadmin only.
      operationId: createSuperadminCreditPlan
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - monthlyPriceUsd
                - includedCredits
              properties:
                name:
                  type: string
                  minLength: 1
                monthlyPriceUsd:
                  type: integer
                  minimum: 0
                  description: Monthly price in integer cents
                includedCredits:
                  type: integer
                  minimum: 0
                creditDiscountPercent:
                  type: number
                  minimum: 0
                  maximum: 100
                  default: 0
                unlimitedServices:
                  type: array
                  items:
                    type: string
                  default: []
                entityLimits:
                  type: object
                  additionalProperties:
                    type: integer
                  default: {}
                trialDays:
                  type: integer
                  minimum: 0
                  default: 0
                trialCredits:
                  type: integer
                  minimum: 0
                  default: 0
                requiresCard:
                  type: boolean
                  default: true
                convertsTo:
                  type: string
                  format: uuid
                  nullable: true
                features:
                  type: object
                  additionalProperties: true
                  default: {}
                sortOrder:
                  type: integer
                  default: 0
            example:
              name: Standard
              monthlyPriceUsd: 9700
              includedCredits: 5000
              creditDiscountPercent: 0
              requiresCard: true
              sortOrder: 1
      responses:
        "201":
          description: Subscription plan created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/credits/plans/{id}:
    patch:
      summary: Update subscription plan
      description: Updates a subscription plan (all fields optional). Superadmin only.
      operationId: updateSuperadminCreditPlan
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                monthlyPriceUsd:
                  type: integer
                  minimum: 0
                includedCredits:
                  type: integer
                  minimum: 0
                creditDiscountPercent:
                  type: number
                  minimum: 0
                  maximum: 100
                unlimitedServices:
                  type: array
                  items:
                    type: string
                entityLimits:
                  type: object
                  additionalProperties:
                    type: integer
                trialDays:
                  type: integer
                  minimum: 0
                trialCredits:
                  type: integer
                  minimum: 0
                requiresCard:
                  type: boolean
                convertsTo:
                  type: string
                  format: uuid
                  nullable: true
                features:
                  type: object
                  additionalProperties: true
                sortOrder:
                  type: integer
            example:
              monthlyPriceUsd: 12700
      responses:
        "200":
          description: Updated subscription plan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Deactivate subscription plan
      description: Soft-deletes a subscription plan by setting its status to inactive. Superadmin only.
      operationId: deleteSuperadminCreditPlan
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Deactivated subscription plan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/credits/pricing:
    get:
      summary: Get service credit rates
      description: Returns the per-service credit rate map from system settings (credits.* keys). Superadmin only.
      operationId: getSuperadminCreditPricing
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Service credit rates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: number
              example:
                data:
                  ai_dialer: 5
                  enrichment: 2
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      summary: Update service credit rates
      description: Upserts per-service credit rates (map of service name to non-negative integer rate) and invalidates the
        rate cache. Superadmin only.
      operationId: updateSuperadminCreditPricing
      tags:
        - "Superadmin: Credits"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: integer
                minimum: 0
            example:
              ai_dialer: 5
              enrichment: 2
      responses:
        "200":
          description: Updated rates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: integer
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/compliance:
    get:
      summary: Compliance status
      description: Returns HIPAA and SOC2 control checklists (with live-computed statuses), the BAA vendor register, and
        audit-log retention status. Superadmin only.
      operationId: getSuperadminCompliance
      tags:
        - "Superadmin: Compliance"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Compliance status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      hipaa:
                        type: object
                      soc2:
                        type: object
                      baa:
                        type: object
                      auditStatus:
                        type: object
              example:
                data:
                  hipaa:
                    checks:
                      - id: hipaa-1
                        requirement: PHI encrypted at rest (AES-256-GCM)
                        category: Technical
                        framework: hipaa
                        status: complete
                        details: PHI field-level encryption active.
                    total: 22
                    complete: 20
                    percentage: 91
                  soc2:
                    checks: []
                    total: 11
                    complete: 10
                    percentage: 91
                  baa:
                    vendors: []
                    totalRequired: 3
                    executed: 0
                    percentage: 0
                  auditStatus:
                    totalRecords: 10420
                    oldestRecord: 2025-06-01T00:00:00.000Z
                    newestRecord: 2026-01-15T10:00:00.000Z
                    retentionDays: 365
                    retentionCompliant: true
                    databaseEncryption: true
                    fieldEncryption: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/compliance/access-review:
    get:
      summary: Access review report
      description: Generates a SOC2 access-review report — summary stats plus stale, privileged, and superadmin accounts,
        session integrity, and auth integrity audit. Generating the report writes an audit log entry. Superadmin only.
      operationId: getSuperadminComplianceAccessReview
      tags:
        - "Superadmin: Compliance"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Access review report
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      summary:
                        type: object
                      staleAccounts:
                        type: array
                        items:
                          type: object
                      privilegedAccounts:
                        type: array
                        items:
                          type: object
                      superadminAccounts:
                        type: array
                        items:
                          type: object
                      sessionIntegrity:
                        type: object
                      authIntegrity:
                        type: object
              example:
                data:
                  summary:
                    totalUsers: 42
                    activeUsers: 38
                    staleUsers: 4
                    usersWithout2FA: 12
                    superadminsWithout2FA: 0
                    usersWithPiiAccess: 6
                    readOnlyUsers: 3
                  staleAccounts: []
                  privilegedAccounts: []
                  superadminAccounts: []
                  sessionIntegrity: {}
                  authIntegrity: {}
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/compliance/docs/{slug}:
    get:
      summary: Get compliance document
      description: Returns the markdown content and title of a named compliance document (HIPAA/SOC2 policies) read from disk.
        Superadmin only.
      operationId: getSuperadminComplianceDoc
      tags:
        - "Superadmin: Compliance"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          description: Document slug (e.g. hipaa-privacy-policy, soc2-security-policy)
          schema:
            type: string
      responses:
        "200":
          description: Compliance document
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      slug:
                        type: string
                      filename:
                        type: string
                      title:
                        type: string
                      content:
                        type: string
                      updatedAt:
                        type: string
              example:
                data:
                  slug: hipaa-privacy-policy
                  filename: HIPAA_PRIVACY_POLICY.md
                  title: HIPAA Privacy Policy
                  content: "# HIPAA Privacy Policy..."
                  updatedAt: 2026-04-05
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/compliance/settings:
    get:
      summary: Get compliance floors
      description: Returns the current platform-wide compliance floor settings (session timeout, max concurrent sessions, 2FA
        policy, lockout). Superadmin only.
      operationId: getSuperadminComplianceSettings
      tags:
        - "Superadmin: Compliance"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Compliance floors
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: string
              example:
                data:
                  sessionTimeoutMinutes: "480"
                  maxConcurrentSessions: "5"
                  twoFactorPolicy: required_admins
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      summary: Update compliance floors
      description: Upserts platform-wide compliance floor settings and invalidates the floors cache. All fields optional
        (string values). Superadmin only.
      operationId: updateSuperadminComplianceSettings
      tags:
        - "Superadmin: Compliance"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sessionTimeoutMinutes:
                  type: string
                maxConcurrentSessions:
                  type: string
                twoFactorPolicy:
                  type: string
                lockoutThreshold:
                  type: string
                lockoutDurationMinutes:
                  type: string
            example:
              sessionTimeoutMinutes: "480"
              twoFactorPolicy: required_admins
      responses:
        "200":
          description: Floors updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
              example:
                data:
                  success: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/email/smtp:
    get:
      summary: Get system email provider
      description: Returns the system default email provider config with secrets stripped (passwordConfigured/apiKeyConfigured
        booleans replace raw secrets). Superadmin only.
      operationId: getSuperadminEmailSmtp
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: System email provider
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
              example:
                data:
                  id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  provider: resend
                  emailAddress: noreply@iscale.com
                  fromName: iSCALE
                  passwordConfigured: false
                  apiKeyConfigured: true
                  status: active
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      summary: Create system email provider
      description: Creates the system default email provider. Resend requires an API key; SMTP requires host and username.
        Returns the created row with secrets stripped. Superadmin only.
      operationId: createSuperadminEmailSmtp
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - emailAddress
              properties:
                provider:
                  type: string
                  enum:
                    - resend
                    - smtp
                  default: resend
                emailAddress:
                  type: string
                  minLength: 1
                fromName:
                  type: string
                  nullable: true
                host:
                  type: string
                  nullable: true
                  description: Required for SMTP
                username:
                  type: string
                  nullable: true
                  description: Required for SMTP
                passwordEncrypted:
                  type: string
                  nullable: true
                  description: SMTP password (encrypted at rest)
                apiKeyEncrypted:
                  type: string
                  nullable: true
                  description: Resend API key (encrypted at rest)
                port:
                  type: integer
                  default: 587
                authEnabled:
                  type: boolean
                  default: true
                sslEnabled:
                  type: boolean
                  default: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
            example:
              provider: resend
              emailAddress: noreply@iscale.com
              fromName: iSCALE
              apiKeyEncrypted: re_xxx
      responses:
        "201":
          description: Provider created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A system default email provider already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Update system email provider
      description: Updates the system default email provider (partial). Switching provider clears the other provider's fields.
        Returns the updated row with secrets stripped. Superadmin only.
      operationId: updateSuperadminEmailSmtp
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                provider:
                  type: string
                  enum:
                    - resend
                    - smtp
                emailAddress:
                  type: string
                fromName:
                  type: string
                  nullable: true
                host:
                  type: string
                  nullable: true
                username:
                  type: string
                  nullable: true
                passwordEncrypted:
                  type: string
                  nullable: true
                apiKeyEncrypted:
                  type: string
                  nullable: true
                port:
                  type: integer
                authEnabled:
                  type: boolean
                sslEnabled:
                  type: boolean
                status:
                  type: string
                  enum:
                    - active
                    - inactive
            example:
              fromName: iSCALE Notifications
      responses:
        "200":
          description: Provider updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete system email provider
      description: Deletes the system default email provider. Superadmin only.
      operationId: deleteSuperadminEmailSmtp
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Provider deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/email/smtp/test:
    post:
      summary: Test system email provider
      description: Sends a test email through the system email provider to verify configuration. Superadmin only.
      operationId: testSuperadminEmailSmtp
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - to
              properties:
                to:
                  type: string
                  format: email
            example:
              to: admin@example.com
      responses:
        "200":
          description: Test email sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Email provider test failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/email/templates:
    get:
      summary: List system email templates
      description: Paginated list of system (tenantId null) email templates. Optional category filter (system or lead).
        Superadmin only.
      operationId: listSuperadminEmailTemplates
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: category
          in: query
          schema:
            type: string
            enum:
              - system
              - lead
      responses:
        "200":
          description: Paginated email templates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/email/templates/{slug}:
    get:
      summary: Get system email template
      description: Returns a single system email template by slug. Superadmin only.
      operationId: getSuperadminEmailTemplate
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Email template
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update system email template
      description: Updates the subject, body, and/or from-email of a system email template. Superadmin only.
      operationId: updateSuperadminEmailTemplate
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subject:
                  type: string
                body:
                  type: string
                fromEmail:
                  type: string
                  format: email
                  nullable: true
            example:
              subject: Welcome to iSCALE
              body: <p>Hello {{userName}}</p>
      responses:
        "200":
          description: Updated email template
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/email/templates/{slug}/test:
    post:
      summary: Send test email template
      description: Renders the named system email template with sample merge-field data and sends it to the given address.
        Superadmin only.
      operationId: testSuperadminEmailTemplate
      tags:
        - "Superadmin: Email"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - to
              properties:
                to:
                  type: string
                  format: email
            example:
              to: admin@example.com
      responses:
        "200":
          description: Test email sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Email template test failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/sms/templates:
    get:
      summary: List system SMS templates
      description: Paginated list of system (tenantId null) SMS templates. Optional status filter. Superadmin only.
      operationId: listSuperadminSmsTemplates
      tags:
        - "Superadmin: SMS"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
      responses:
        "200":
          description: Paginated SMS templates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create system SMS template
      description: Creates a system SMS template. Slug must be lowercase kebab-case and unique among system templates; body
        max 1600 chars. Returns the created template (bare object). Superadmin only.
      operationId: createSuperadminSmsTemplate
      tags:
        - "Superadmin: SMS"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - slug
                - body
              properties:
                slug:
                  type: string
                  maxLength: 100
                  pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                description:
                  type: string
                  maxLength: 500
                  nullable: true
                body:
                  type: string
                  minLength: 1
                  maxLength: 1600
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
            example:
              slug: welcome-sms
              name: Welcome SMS
              body: Welcome to iSCALE!
              status: active
      responses:
        "201":
          description: SMS template created
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: An SMS template with this slug already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/sms/templates/{slug}:
    get:
      summary: Get system SMS template
      description: Returns a single system SMS template by slug (bare object). Superadmin only.
      operationId: getSuperadminSmsTemplate
      tags:
        - "Superadmin: SMS"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: SMS template
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update system SMS template
      description: Updates a system SMS template's name, description, body, and/or status (bare object response). Superadmin only.
      operationId: updateSuperadminSmsTemplate
      tags:
        - "Superadmin: SMS"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                description:
                  type: string
                  maxLength: 500
                  nullable: true
                body:
                  type: string
                  minLength: 1
                  maxLength: 1600
                status:
                  type: string
                  enum:
                    - active
                    - inactive
            example:
              body: Updated welcome message
      responses:
        "200":
          description: Updated SMS template
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete system SMS template
      description: Permanently deletes a system SMS template by slug (bare object response). Superadmin only.
      operationId: deleteSuperadminSmsTemplate
      tags:
        - "Superadmin: SMS"
      security:
        - sessionCookie: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Deleted SMS template
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/billing:
    get:
      summary: Get tenant platform fee
      description: Returns the tenant's platform-fee override (if any) and the effective fee resolved against the system
        default. Superadmin only.
      operationId: getSuperadminTenantBilling
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Tenant platform fee
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      platformFeeBps:
                        type: integer
                        nullable: true
                      effectivePlatformFeeBps:
                        type: integer
                      isOverride:
                        type: boolean
              example:
                data:
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  platformFeeBps: 750
                  effectivePlatformFeeBps: 750
                  isOverride: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Set tenant platform fee
      description: Sets or clears the tenant's platform-fee override in basis points (0-10000). Send null to clear and fall
        back to the system default. Superadmin only.
      operationId: updateSuperadminTenantBilling
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - platformFeeBps
              properties:
                platformFeeBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
                  nullable: true
            example:
              platformFeeBps: 750
      responses:
        "200":
          description: Updated tenant platform fee
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/resend-welcome:
    post:
      summary: Resend tenant welcome email
      description: Resends the account-approved welcome email to the tenant's active owner user. Tenant must be active.
        Superadmin only.
      operationId: resendSuperadminTenantWelcome
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Welcome email sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      sent:
                        type: boolean
                      to:
                        type: string
                        format: email
              example:
                data:
                  sent: true
                  to: owner@acme.com
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/users/{userId}/impersonate:
    get:
      summary: Impersonate tenant user
      description: Starts a per-user impersonation session for an admin-role user in an active tenant, sets the lr_impersonate
        cookie, and redirects to /admin. Opened in a new tab from the UI. Superadmin only.
      operationId: impersonateSuperadminTenantUser
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "302":
          description: Redirect to /admin with lr_impersonate cookie set (or back to superadmin on failure)
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/telephony/twilio/senders:
    get:
      summary: List Twilio senders
      description: Proxies the system Twilio account's SMS-capable phone numbers and Messaging Service SIDs for use in sender
        dropdowns. Superadmin only.
      operationId: listSuperadminTwilioSenders
      tags:
        - "Superadmin: Telephony"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Twilio senders
          content:
            application/json:
              schema:
                type: object
                properties:
                  phoneNumbers:
                    type: array
                    items:
                      type: object
                      properties:
                        phoneNumber:
                          type: string
                        friendlyName:
                          type: string
                        smsEnabled:
                          type: boolean
                  messagingServices:
                    type: array
                    items:
                      type: object
                      properties:
                        sid:
                          type: string
                        friendlyName:
                          type: string
              example:
                phoneNumbers:
                  - phoneNumber: "+15555550100"
                    friendlyName: Main line
                    smsEnabled: true
                messagingServices:
                  - sid: MG0123456789abcdef0123456789abcdef
                    friendlyName: Default service
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "502":
          description: Twilio API rejected the credentials or the upstream call failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: No system Twilio credential configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/ai-dialer/config:
    get:
      summary: Get AI dialer config
      description: Returns the system-level ElevenLabs (AI dialer) integration config with secrets masked. Superadmin only.
      operationId: getSuperadminAiDialerConfig
      tags:
        - "Superadmin: Settings"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: AI dialer config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      config:
                        type: object
                        additionalProperties: true
              example:
                data:
                  enabled: true
                  config:
                    isConfigured: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/members/{memberId}:
    patch:
      summary: Activate or deactivate membership
      description: Activates or deactivates an organization membership (sets member status). Superadmin only.
      operationId: updateSuperadminMember
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: memberId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - activate
                    - deactivate
            example:
              action: deactivate
      responses:
        "200":
          description: Membership updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
              example:
                data:
                  id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
                  status: inactive
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/users/{userId}/sessions:
    delete:
      summary: Revoke all user sessions
      description: Deletes all active sessions for a user, forcing re-authentication. Superadmin only.
      operationId: revokeSuperadminUserSessions
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Sessions revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      count:
                        type: integer
              example:
                data:
                  count: 3
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/users/{userId}/sessions/{sessionId}:
    delete:
      summary: Revoke one user session
      description: Deletes a single session belonging to a user. Superadmin only.
      operationId: revokeSuperadminUserSession
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Session revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/affiliates:
    get:
      summary: List platform affiliates
      description: Paginated list of platform affiliates (global rows) with referral terms, balance, next payout date, and
        referred-tenant counts. Superadmin only.
      operationId: listSuperadminAffiliates
      tags:
        - "Superadmin: Affiliates"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated affiliates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
              example:
                data:
                  - id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                    name: Acme Partners
                    email: partners@acme.com
                    referralCode: ACME1234
                    status: active
                    signupFeeCents: 5000
                    recurringFeeCents: 0
                    revShareBps: 0
                    balanceCents: 15000
                    nextPayoutDate: 2026-02-01T00:00:00.000Z
                    createdAt: 2026-01-15T10:00:00.000Z
                    referredTenants: 4
                pagination:
                  total: 1
                  limit: 20
                  offset: 0
                  hasMore: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create platform affiliate
      description: Creates a platform affiliate. Referral code is generated when omitted (8 uppercase alphanumerics) and must
        be unique. Fees are integer cents; revShareBps is basis points (0-10000). Superadmin only.
      operationId: createSuperadminAffiliate
      tags:
        - "Superadmin: Affiliates"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                email:
                  type: string
                  format: email
                  maxLength: 255
                referralCode:
                  type: string
                  pattern: ^[A-Z0-9]{8}$
                  description: Optional; generated when omitted
                signupFeeCents:
                  type: integer
                  minimum: 0
                recurringFeeCents:
                  type: integer
                  minimum: 0
                revShareBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
                notes:
                  type: string
                  maxLength: 2000
            example:
              name: Acme Partners
              email: partners@acme.com
              signupFeeCents: 5000
      responses:
        "201":
          description: Affiliate created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Referral code already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/affiliates/{id}:
    get:
      summary: Get platform affiliate
      description: Returns an affiliate with its referred tenants (capped at 100), exact referred-tenant count, and lifetime
        accrued earnings in cents. Superadmin only.
      operationId: getSuperadminAffiliate
      tags:
        - "Superadmin: Affiliates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Affiliate detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      referredTenants:
                        type: integer
                      totalEarnedCents:
                        type: integer
                      tenants:
                        type: array
                        items:
                          type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      summary: Update platform affiliate
      description: Updates an affiliate's terms, status, postbacks, or notes (all fields optional). Superadmin only.
      operationId: updateSuperadminAffiliate
      tags:
        - "Superadmin: Affiliates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                email:
                  type: string
                  format: email
                  maxLength: 255
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                signupFeeCents:
                  type: integer
                  minimum: 0
                recurringFeeCents:
                  type: integer
                  minimum: 0
                revShareBps:
                  type: integer
                  minimum: 0
                  maximum: 10000
                postbacks:
                  type: array
                  nullable: true
                  items:
                    type: object
                    required:
                      - event
                      - urlTemplate
                      - enabled
                    properties:
                      event:
                        type: string
                        enum:
                          - wizard_started
                          - email_captured
                          - plan_selected
                          - signup_completed
                          - first_payment
                      urlTemplate:
                        type: string
                        minLength: 1
                        maxLength: 2048
                      enabled:
                        type: boolean
                notes:
                  type: string
                  maxLength: 2000
                  nullable: true
            example:
              status: paused
      responses:
        "200":
          description: Updated affiliate
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/affiliates/{id}/payouts:
    get:
      summary: List affiliate payouts
      description: Paginated payout history for a platform affiliate (global payouts, not tenant-scoped). Superadmin only.
      operationId: listSuperadminAffiliatePayouts
      tags:
        - "Superadmin: Affiliates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated affiliate payouts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/affiliates/{id}/postbacks/test:
    post:
      summary: Test affiliate postback
      description: Fires one configured postback event's URL template with deterministic sample macro values (fires even when
        the config is disabled). The affiliate must have a URL template saved for that event. Superadmin only.
      operationId: testSuperadminAffiliatePostback
      tags:
        - "Superadmin: Affiliates"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - event
              properties:
                event:
                  type: string
                  enum:
                    - wizard_started
                    - email_captured
                    - plan_selected
                    - signup_completed
                    - first_payment
            example:
              event: signup_completed
      responses:
        "200":
          description: Postback fired
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      fired:
                        type: boolean
                      url:
                        type: string
                      status:
                        type: string
                      httpStatus:
                        type: integer
                        nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/coupons:
    get:
      summary: List platform coupons
      description: Paginated list of platform coupons ($-off codes on the platform Stripe account). Superadmin only.
      operationId: listSuperadminCoupons
      tags:
        - "Superadmin: Coupons"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated coupons
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create platform coupon
      description: Creates a platform coupon and the matching Stripe coupon + promotion code. amountOffUsd is integer cents;
        code must be unique. Superadmin only.
      operationId: createSuperadminCoupon
      tags:
        - "Superadmin: Coupons"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - code
                - amountOffUsd
                - duration
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                code:
                  type: string
                  minLength: 1
                  maxLength: 100
                amountOffUsd:
                  type: integer
                  minimum: 1
                  description: Amount off in integer cents
                duration:
                  type: string
                  enum:
                    - once
                    - forever
            example:
              name: Launch 20 off
              code: LAUNCH20
              amountOffUsd: 2000
              duration: once
      responses:
        "201":
          description: Coupon created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Coupon code already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/superadmin/coupons/{id}:
    delete:
      summary: Deactivate platform coupon
      description: Deactivates a platform coupon — flips the Stripe promotion code inactive and sets active=false. Never
        hard-deleted (history preserved). Superadmin only.
      operationId: deleteSuperadminCoupon
      tags:
        - "Superadmin: Coupons"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Deactivated coupon
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/marketing-analytics/pivot:
    get:
      summary: Marketing pageview pivot
      description: Marketing-site pageview + funnel pivot (pageviews, unique visitors, wizard starts, signups) bucketed by a
        chosen dimension over an inclusive UTC-day range (max 92-day span). Superadmin only.
      operationId: getSuperadminMarketingPivot
      tags:
        - "Superadmin: Marketing"
      security:
        - sessionCookie: []
      parameters:
        - name: from
          in: query
          required: true
          description: Inclusive start day (YYYY-MM-DD, UTC)
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          description: Inclusive end day (YYYY-MM-DD, UTC); span must not exceed 92 days
          schema:
            type: string
            format: date
        - name: dimension
          in: query
          required: true
          schema:
            type: string
            enum:
              - day
              - path
              - referrerHost
              - browser
              - subId1
              - utm_source
      responses:
        "200":
          description: Pivot rows and totals
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                        pageviews:
                          type: integer
                        uniqueVisitors:
                          type: integer
                        wizardStarts:
                          type: integer
                        signups:
                          type: integer
                  totals:
                    type: object
                    properties:
                      pageviews:
                        type: integer
                      uniqueVisitors:
                        type: integer
                      wizardStarts:
                        type: integer
                      signups:
                        type: integer
              example:
                data:
                  - key: 2026-01-15
                    pageviews: 1200
                    uniqueVisitors: 840
                    wizardStarts: 120
                    signups: 18
                totals:
                  pageviews: 1200
                  uniqueVisitors: 840
                  wizardStarts: 120
                  signups: 18
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/partial-signups:
    get:
      summary: List partial signups
      description: Paginated list of abandoned get-started wizard captures. Defaults to status 'open'; pass status=all for
        every row or status=<value> to filter. Superadmin only.
      operationId: listSuperadminPartialSignups
      tags:
        - "Superadmin: Signups"
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - name: status
          in: query
          description: Filter by status; 'all' returns every row (default 'open')
          schema:
            type: string
            default: open
      responses:
        "200":
          description: Paginated partial signups
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/partial-signups/{id}:
    patch:
      summary: Dismiss partial signup
      description: Soft-flips a partial signup to 'dismissed' (never deleted; still visible via status=dismissed). Superadmin only.
      operationId: dismissSuperadminPartialSignup
      tags:
        - "Superadmin: Signups"
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - dismissed
            example:
              status: dismissed
      responses:
        "200":
          description: Dismissed partial signup
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/billing-conversion:
    post:
      summary: Schedule tenant billing conversion
      description: Sets or clears tenant.billingStartsAt — the dated free→paid conversion. Past dates are allowed (converts on
        the next enforcement cron run). Sending null clears a scheduled conversion. Setting a date notifies the tenant.
        Superadmin only.
      operationId: setSuperadminTenantBillingConversion
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - billingStartsAt
              properties:
                billingStartsAt:
                  type: string
                  format: date-time
                  nullable: true
                  description: ISO 8601 with offset; null clears the schedule
            example:
              billingStartsAt: 2026-03-01T00:00:00.000Z
      responses:
        "200":
          description: Conversion date set or cleared
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      billingStartsAt:
                        type: string
                        format: date-time
                        nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/service-access:
    get:
      summary: Get tenant service access
      description: Returns every known service with its stored access-level override ('default' when no override row exists).
        Superadmin only.
      operationId: getSuperadminTenantServiceAccess
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Service access levels
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      services:
                        type: array
                        items:
                          type: object
                          properties:
                            service:
                              type: string
                            accessLevel:
                              type: string
                              enum:
                                - default
                                - granted
                                - revoked
                                - byo
              example:
                data:
                  services:
                    - service: ai_dialer
                      accessLevel: granted
                    - service: enrichment
                      accessLevel: default
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Set tenant service access
      description: Sets a single service's access-level override for a tenant. accessLevel 'default' removes the override row.
        Returns the full service-access map. Superadmin only.
      operationId: setSuperadminTenantServiceAccess
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - service
                - accessLevel
              properties:
                service:
                  type: string
                  enum:
                    - lead_submit
                    - lead_ping
                    - call_tracking
                    - tracking_number
                    - ai_dialer
                    - ai_agent
                    - ai_spec_parser
                    - ai_token_mapper
                    - sms
                    - email
                    - enrichment
                accessLevel:
                  type: string
                  enum:
                    - default
                    - granted
                    - revoked
                    - byo
            example:
              service: ai_dialer
              accessLevel: granted
      responses:
        "200":
          description: Updated service access map
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      services:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/service-rates:
    get:
      summary: Get tenant service rates
      description: Returns every flat-rate service with its global credit rate, the tenant's override (null when none), and
        the effective rate. Cost-plus services (ai_spec_parser, ai_token_mapper, enrichment, ai_agent) are excluded —
        they have no flat per-unit rate. Superadmin only.
      operationId: getSuperadminTenantServiceRates
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Service rates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        service:
                          type: string
                        globalRate:
                          type: number
                        overrideRate:
                          type: number
                          nullable: true
                        effectiveRate:
                          type: number
              example:
                data:
                  - service: call_tracking
                    globalRate: 10
                    overrideRate: 2.5
                    effectiveRate: 2.5
                  - service: tracking_number
                    globalRate: 100
                    overrideRate: null
                    effectiveRate: 100
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Set tenant service rate
      description: Upserts a single service's per-tenant credit-rate override (fractional rates allowed, 0.1-credit
        granularity). rate null deletes the override so the tenant reverts to the global rate. Returns the full
        service-rates list. Superadmin only.
      operationId: setSuperadminTenantServiceRate
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - service
                - rate
              properties:
                service:
                  type: string
                  enum:
                    - lead_submit
                    - lead_ping
                    - call_tracking
                    - tracking_number
                    - ai_dialer
                    - ai_agent
                    - ai_spec_parser
                    - ai_token_mapper
                    - sms
                    - email
                    - enrichment
                    - api_request
                rate:
                  type: number
                  nullable: true
                  minimum: 0
            example:
              service: call_tracking
              rate: 2.5
      responses:
        "200":
          description: Updated service rates list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/subscription:
    get:
      summary: Get tenant subscription
      description: Returns the tenant's subscription status, plan, current period end, billing-conversion date, and
        enforcement state/deadline. Superadmin only.
      operationId: getSuperadminTenantSubscription
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Tenant subscription status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      hasSubscription:
                        type: boolean
                      subscriptionStatus:
                        type: string
                        nullable: true
                      currentPeriodEnd:
                        type: string
                        format: date-time
                        nullable: true
                      planName:
                        type: string
                        nullable: true
                      monthlyPriceUsd:
                        type: integer
                        nullable: true
                      billingStartsAt:
                        type: string
                        format: date-time
                        nullable: true
                      enforcementState:
                        type: string
                      enforcementDeadline:
                        type: string
                        format: date-time
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/tenants/{tenantId}/subscription/cancel:
    post:
      summary: Cancel tenant subscription
      description: Cancels the tenant's Stripe subscription — at period end by default, or immediately when immediate=true.
        Superadmin only.
      operationId: cancelSuperadminTenantSubscription
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                immediate:
                  type: boolean
                  description: Cancel now instead of at period end
            example:
              immediate: false
      responses:
        "200":
          description: Subscription canceled
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      subscriptionStatus:
                        type: string
                      cancelAtPeriodEnd:
                        type: boolean
                      currentPeriodEnd:
                        type: string
                        format: date-time
                        nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/tenants/{tenantId}/subscription/resume:
    post:
      summary: Resume tenant subscription
      description: Clears a pending cancel-at-period-end on the tenant's Stripe subscription. Superadmin only.
      operationId: resumeSuperadminTenantSubscription
      tags:
        - "Superadmin: Tenants"
      security:
        - sessionCookie: []
      parameters:
        - name: tenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Subscription resumed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      subscriptionStatus:
                        type: string
                      cancelAtPeriodEnd:
                        type: boolean
                      currentPeriodEnd:
                        type: string
                        format: date-time
                        nullable: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/users/{userId}/activity:
    get:
      summary: Get user activity timeline
      description: Paginated unified activity timeline for a user (login, email, and audit events). Superadmin wrapper over
        the same activity query used by the admin surface. Superadmin only.
      operationId: getSuperadminUserActivity
      tags:
        - "Superadmin: Users"
      security:
        - sessionCookie: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: types
          in: query
          description: CSV of activity kinds to include (default all)
          schema:
            type: string
            example: login,email
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated activity timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/superadmin/billing/margins:
    get:
      summary: Modeled margin matrix
      description: Returns the modeled rate-card margin matrix — theoretical profit per service at the list credit rate and at
        each active credit-bundle discount tier, plus the current provider cost rates and margin-floor multiple.
        Forward-looking sanity check ("are bigger bundles still profitable"), distinct from the backward-looking
        actual-cost tabs. Money is integer µUSD; ratios are display-only floats. Superadmin only.
      operationId: getBillingMargins
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Margin matrix, current cost rates, and floor multiple
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    description: Margin matrix (tiers, rows, floorMultiple)
                  costRatesUsd:
                    type: object
                    additionalProperties:
                      type: number
                  floorMultiple:
                    type: number
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/cost-rates:
    put:
      summary: Update provider cost rates
      description: Sets the platform's provider unit costs (USD) and the margin-floor multiple that power the modeled margin
        matrix. Strict allowlist of known provider keys; values are non-negative and may be fractional. Each key is
        stored as a credits.cost.* systemSetting. Superadmin only.
      operationId: updateBillingCostRates
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: number
                minimum: 0
              example:
                signalwire.voice_per_min: 0.0125
                elevenlabs.agent_per_min: 0.08
                marginFloorMultiple: 2
      responses:
        "200":
          description: Persisted cost rates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      type: number
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/superadmin/billing/system-costs:
    get:
      summary: Live system-cost summary
      description: "Live platform-spend rolled up BY TIER for the selected window. Tier 1 (overhead: neon, vercel, resend) and
        Tier 2 (COGS: signalwire, elevenlabs) contribute to totalCents; Tier 3 (byo-excluded: twilio) is reported but
        never billed. Each provider carries its tier, rolled-up cents, config status and the exact missing env var when
        a credential is absent. Optional includeRevenue=true adds gross margin (revenue − cost). Superadmin only."
      operationId: getSuperadminSystemCosts
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      parameters:
        - name: from
          in: query
          description: Inclusive start day (YYYY-MM-DD); defaults to 30 days ago.
          schema:
            type: string
        - name: to
          in: query
          description: Inclusive end day (YYYY-MM-DD); defaults to today.
          schema:
            type: string
        - name: includeRevenue
          in: query
          description: When 'true', include revenueCents and grossMarginCents.
          schema:
            type: string
            enum:
              - "true"
              - "false"
      responses:
        "200":
          description: System-cost summary, trend, per-provider breakdown, and rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    type: object
                    properties:
                      totalCents:
                        type: integer
                        description: tier1Cents + tier2Cents (Tier 3 excluded)
                      byTier:
                        type: object
                        properties:
                          tier1Cents:
                            type: integer
                          tier2Cents:
                            type: integer
                      byoExcluded:
                        type: array
                        items:
                          type: string
                      byProvider:
                        type: array
                        items:
                          type: object
                          properties:
                            provider:
                              type: string
                            costTier:
                              type: integer
                              enum:
                                - 1
                                - 2
                                - 3
                            cents:
                              type: integer
                            configured:
                              type: boolean
                            missingEnv:
                              type: string
                              nullable: true
                  trend:
                    type: array
                    items:
                      type: object
                      properties:
                        periodStart:
                          type: string
                        totalCents:
                          type: integer
                  providers:
                    type: array
                    items:
                      type: object
                  revenue:
                    type: object
                    nullable: true
                    properties:
                      revenueCents:
                        type: integer
                      grossMarginCents:
                        type: integer
                        description: revenue − Tier2 − Tier1
                      tier2GrossMarginCents:
                        type: integer
                        description: revenue − Tier2
                  data:
                    type: array
                    items:
                      type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Add a manual system-cost entry
      description: Records a manual cost overlay (source='manual') for a period a live collector can't price
        (credential_missing / plan_unavailable / rate_unconfigured). Money is integer cents. Superadmin only.
      operationId: createSuperadminSystemCost
      tags:
        - "Superadmin: Billing"
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - provider
                - category
                - periodStart
                - periodEnd
                - amountCents
              properties:
                provider:
                  type: string
                  enum:
                    - neon
                    - vercel
                    - resend
                    - signalwire
                    - elevenlabs
                    - twilio
                category:
                  type: string
                periodStart:
                  type: string
                periodEnd:
                  type: string
                granularity:
                  type: string
                  enum:
                    - daily
                    - monthly
                amountCents:
                  type: integer
                  minimum: 0
                costTier:
                  type: integer
                  enum:
                    - 1
                    - 2
                  description: Optional tier override; defaults from the map. Tier 3 (BYO) is rejected.
                note:
                  type: string
      responses:
        "200":
          description: Created manual cost row
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/auth:
    post:
      operationId: login
      tags:
        - Auth
      summary: Login (deprecated)
      description: Password login has been removed. This endpoint always returns 410. Use magic link, Google, or passkey at
        `/login`.
      responses:
        "410":
          description: Password login removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
    get:
      operationId: getCurrentUser
      tags:
        - Auth
      summary: Get current user
      description: Returns the authenticated user's profile from the session cookie.
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      email:
                        type: string
                      role:
                        type: string
                      tenantId:
                        type: string
                        format: uuid
                        nullable: true
                      isOwner:
                        type: boolean
                      readOnly:
                        type: boolean
                      piiAccess:
                        type: boolean
                      buyerId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerId:
                        type: string
                        format: uuid
                        nullable: true
                      firstName:
                        type: string
                        nullable: true
                      lastName:
                        type: string
                        nullable: true
                      contactId:
                        type: string
                        format: uuid
                        nullable: true
                        description: Only present for portal users (buyer, partner, referrer).
                      permissions:
                        type: object
                        nullable: true
                        description: Only present for portal users (buyer, partner, referrer).
                        properties:
                          permAccounts:
                            type: boolean
                          permUsers:
                            type: boolean
                          permBilling:
                            type: boolean
                          permOffers:
                            type: boolean
                          permReports:
                            type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
    delete:
      operationId: logout
      tags:
        - Auth
      summary: Logout
      description: Clears the session cookie.
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
      security:
        - sessionCookie: []
  /api/v1/auth/api-keys:
    get:
      operationId: listApiKeys
      tags:
        - Auth
      summary: List API keys
      description: Admin sees all keys; buyer sees only their own. Partners are forbidden.
      parameters:
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: List of API keys (keyHash omitted)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createApiKey
      tags:
        - Auth
      summary: Create API key
      description: |
        Creates a new API key. The full key is returned only once in the response.

        Set `readOnly: true` to mint a key that accepts `GET` / `HEAD` / `OPTIONS` only.
        Non-GET requests with a read-only key return `403` with `error.code: READ_ONLY`.

        Set `piiAccess: true` to mint a key that returns unmasked PII on reads. The caller
        minting the key must have `piiAccess` on their own account (or be superadmin),
        otherwise the request is rejected with `403 FORBIDDEN`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                scopes:
                  type: array
                  items:
                    type: string
                buyerId:
                  type: string
                  format: uuid
                  nullable: true
                partnerId:
                  type: string
                  format: uuid
                  nullable: true
                expiresAt:
                  type: string
                  format: date-time
                  nullable: true
                readOnly:
                  type: boolean
                  default: false
                  description: |
                    When true, this key blocks all non-GET requests with `403 READ_ONLY`.
                    GET / HEAD / OPTIONS remain allowed. Session auth is unaffected.
                piiAccess:
                  type: boolean
                  default: false
                  description: |
                    When true, this key returns unmasked PII on reads. Requires the
                    creator to have `piiAccess` on their own account (or be superadmin).
            example:
              name: Production Integration Key
              scopes:
                - leads.read
                - leads.write
              readOnly: false
              piiAccess: false
      responses:
        "200":
          description: API key created (includes full key, `readOnly`, and `piiAccess` flags)
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |
            Forbidden. Returned when `piiAccess: true` is requested but the creator
            does not have PII access on their own account (error code `FORBIDDEN`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/auth/api-keys/{keyId}:
    parameters:
      - name: keyId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getApiKey
      tags:
        - Auth
      summary: Get API key
      description: Returns a single API key by ID (keyHash omitted).
      responses:
        "200":
          description: API key detail
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateApiKey
      tags:
        - Auth
      summary: Update API key
      description: |
        Update an API key. Admin / superadmin only — partners and buyers receive `403`.

        Setting `readOnly` flips the key's access mode. A read-only admin cannot grant
        write access (`readOnly: false`) on any key — privilege-not-greater-than-self.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                status:
                  type: string
                  enum:
                    - active
                    - revoked
                readOnly:
                  type: boolean
                  description: |
                    Flip the key's access mode. `true` blocks all non-GET requests.
                    `false` requires a non-read-only admin/superadmin caller.
            example:
              readOnly: false
      responses:
        "200":
          description: Updated API key
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: revokeApiKey
      tags:
        - Auth
      summary: Revoke API key
      description: Soft-revokes an API key (sets status to revoked).
      responses:
        "200":
          description: Key revoked
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/ping:
    get:
      operationId: authenticatedPing
      tags:
        - Auth
      summary: Authenticated ping
      description: Authenticated connectivity check. Requires a valid API key or session (wrapped in withAuth) and echoes back
        the caller's tenant name — use it to confirm a key works and which tenant it resolves to. For an unauthenticated
        liveness probe use `GET /api/health` instead.
      responses:
        "200":
          description: Authenticated; returns the caller's tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      tenant:
                        type: string
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - apiKey: []
        - sessionCookie: []
  /api/v1/verticals:
    get:
      operationId: listVerticals
      tags:
        - Verticals
      summary: List verticals
      description: Returns a paginated list of verticals (lead categories such as insurance, solar, etc.) sorted by createdAt
        descending.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated vertical list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
    post:
      operationId: createVertical
      tags:
        - Verticals
      summary: Create vertical
      description: Creates a new vertical (lead category).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - slug
              properties:
                name:
                  type: string
                slug:
                  type: string
                verticalGroup:
                  type: string
                  nullable: true
                verticalGroupId:
                  type: string
                  format: uuid
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
                retentionCadence:
                  type: string
                  enum:
                    - weekly
                    - monthly
                  nullable: true
                  description: Default cadence for buyer retention reporting when this vertical is selected.
            example:
              name: Auto Insurance
              slug: auto-insurance
              retentionCadence: weekly
      responses:
        "201":
          description: Vertical created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Vertical with this slug already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/verticals/{verticalId}:
    parameters:
      - name: verticalId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getVertical
      tags:
        - Verticals
      summary: Get vertical
      description: Returns a single vertical by ID with related details.
      responses:
        "200":
          description: Vertical detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateVertical
      tags:
        - Verticals
      summary: Update vertical
      description: Partially update a vertical.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                slug:
                  type: string
                legacyId:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                verticalGroup:
                  type: string
                  nullable: true
                verticalGroupId:
                  type: string
                  format: uuid
                  nullable: true
                retentionCadence:
                  type: string
                  enum:
                    - weekly
                    - monthly
                  nullable: true
                  description: Default cadence for buyer retention reporting when this vertical is selected.
                profileKey:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                notes:
                  type: string
                  nullable: true
                settings:
                  type: object
                  additionalProperties: true
                  nullable: true
                cascadeInactive:
                  type: boolean
                  description: When true and status transitions active→inactive, cascades linked offers/campaigns to inactive.
                scoringModelId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Default lead-quality scoring model for this vertical (Lead Scoring M1). Setting a non-null value validates
                    the model's config against this vertical's intake fields and responds 400 VALIDATION_ERROR (issues
                    in `details`) on a blocking error. The model must belong to the same tenant (404 otherwise). Offers
                    may override per-offer.
            example:
              name: Home Insurance
              retentionCadence: monthly
      responses:
        "200":
          description: Vertical updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteVertical
      tags:
        - Verticals
      summary: Delete vertical
      description: Deletes a vertical if it has no linked offers or campaigns.
      responses:
        "200":
          description: Vertical deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/verticals/{verticalId}/fields:
    parameters:
      - name: verticalId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listVerticalFields
      tags:
        - Verticals
      summary: List vertical fields
      description: Returns all standard and custom fields for this vertical.
      responses:
        "200":
          description: Field list
      security:
        - sessionCookie: []
    post:
      operationId: createVerticalField
      tags:
        - Verticals
      summary: Create vertical field
      description: Adds a field to the vertical. Rejects names that conflict with reserved system fields. PII floor is
        enforced on sensitive field names/types.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - label
                - type
              properties:
                name:
                  type: string
                  description: Must start lowercase; letters, numbers, and underscores only.
                label:
                  type: string
                type:
                  type: string
                  enum:
                    - text
                    - number
                    - boolean
                    - date
                    - list
                    - email
                    - phone
                    - url
                    - textarea
                required:
                  type: boolean
                hidden:
                  type: boolean
                isPii:
                  type: boolean
                options:
                  type: array
                  nullable: true
                  items:
                    oneOf:
                      - type: string
                      - type: object
                        properties:
                          value:
                            type: string
                          label:
                            type: string
                validation:
                  type: object
                  nullable: true
                sortOrder:
                  type: integer
      responses:
        "201":
          description: Field created
        "400":
          $ref: "#/components/responses/ValidationError"
      security:
        - sessionCookie: []
    put:
      operationId: updateVerticalField
      tags:
        - Verticals
      summary: Update vertical field
      description: Updates an existing vertical field by id. PII floor is re-enforced when the name, type, or isPii flag changes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  format: uuid
                name:
                  type: string
                label:
                  type: string
                type:
                  type: string
                  enum:
                    - text
                    - number
                    - boolean
                    - date
                    - list
                    - email
                    - phone
                    - url
                    - textarea
                required:
                  type: boolean
                hidden:
                  type: boolean
                isPii:
                  type: boolean
                options:
                  type: array
                  nullable: true
                  items:
                    oneOf:
                      - type: string
                      - type: object
                        properties:
                          value:
                            type: string
                          label:
                            type: string
                validation:
                  type: object
                  nullable: true
                sortOrder:
                  type: integer
      responses:
        "200":
          description: Field updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteVerticalField
      tags:
        - Verticals
      summary: Delete vertical field
      description: Deletes a vertical field (id passed via the fieldId query parameter) and cascade-removes references to it
        from contract custom filters.
      parameters:
        - name: fieldId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Field deleted
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/verticals/{verticalId}/fields/reorder:
    parameters:
      - name: verticalId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      operationId: reorderVerticalFields
      tags:
        - Verticals
      summary: Reorder vertical fields
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fieldIds
              properties:
                fieldIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
      responses:
        "200":
          description: Fields reordered
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/verticals/{verticalId}/documents:
    parameters:
      - name: verticalId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listVerticalDocuments
      tags:
        - Verticals
      summary: List vertical documents
      description: Returns document templates attached to this vertical.
      responses:
        "200":
          description: Document list
      security:
        - sessionCookie: []
    post:
      operationId: createVerticalDocument
      tags:
        - Verticals
      summary: Add vertical document
      description: Attaches a document to the vertical.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fileName
                - fileUrl
              properties:
                fileName:
                  type: string
                fileUrl:
                  type: string
                  format: uri
                fileType:
                  type: string
                portalVisible:
                  type: boolean
      responses:
        "201":
          description: Document created
        "400":
          $ref: "#/components/responses/ValidationError"
      security:
        - sessionCookie: []
    put:
      operationId: updateVerticalDocument
      tags:
        - Verticals
      summary: Update vertical document
      description: Updates an existing vertical document by id.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  format: uuid
                fileName:
                  type: string
                fileType:
                  type: string
                fileUrl:
                  type: string
                  format: uri
                portalVisible:
                  type: boolean
      responses:
        "200":
          description: Document updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteVerticalDocument
      tags:
        - Verticals
      summary: Delete vertical document
      description: Deletes a vertical document. The document id is passed via the docId query parameter.
      parameters:
        - name: docId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Document deleted
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/partners:
    get:
      operationId: listPartners
      tags:
        - Partners
      summary: List partners
      description: Returns a paginated list of lead-source partners.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - paused
              - inactive
        - name: search
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: verticalId
          in: query
          description: Filter to partners with campaigns linked to offers in this vertical.
          schema:
            type: string
            format: uuid
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - displayId
              - legacyId
              - name
              - status
              - campaignCount
              - leadCount
              - callCount
              - leadRevenue
              - createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated partner list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createPartner
      tags:
        - Partners
      summary: Create partner
      description: Creates a new lead-source partner.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
                website:
                  type: string
                  nullable: true
                legalEntityType:
                  type: string
                  enum:
                    - individual
                    - corporation
                    - llc
                    - partnership
                    - sole_proprietorship
                    - other
                  nullable: true
                taxId:
                  type: string
                  nullable: true
                timezone:
                  type: string
                  nullable: true
                partnerManagerId:
                  type: string
                  format: uuid
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                    - rejected
                  default: active
                defaultPrice:
                  type: number
                  nullable: true
                acceptsDuplicates:
                  type: boolean
                  default: false
                acceptsReturns:
                  type: boolean
                  default: false
                contractStartDate:
                  type: string
                  format: date
                  nullable: true
                contractEndDate:
                  type: string
                  format: date
                  nullable: true
                billingContactName:
                  type: string
                  nullable: true
                billingContactEmail:
                  type: string
                  format: email
                  nullable: true
                billingContactPhone:
                  type: string
                  nullable: true
                paymentTerms:
                  type: string
                  nullable: true
                notes:
                  type: string
                  nullable: true
                paymentCycle:
                  type: string
                  enum:
                    - weekly
                    - bimonthly
                    - monthly
                    - adhoc
                  default: monthly
                paymentThreshold:
                  type: number
                  default: 0
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
            example:
              name: LeadGen Pro
              email: ops@leadgenpro.com
              phone: "+15551234567"
              status: active
      responses:
        "201":
          description: Partner created
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Duplicate - partner with this email already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/partners/{partnerId}:
    parameters:
      - name: partnerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getPartner
      tags:
        - Partners
      summary: Get partner
      description: Returns a single partner by ID.
      responses:
        "200":
          description: Partner detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updatePartner
      tags:
        - Partners
      summary: Update partner
      description: Partially update a partner. When status transitions to `inactive` and `deactivateChildren` is true,
        non-inactive campaigns for this partner are cascaded to inactive.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                _snapshot:
                  type: object
                  additionalProperties: true
                  description: Optional concurrent-edit guard (field-level). Send the page-load values of exactly the fields included in
                    this PATCH; the server responds 409 (code CONFLICT, details.conflictFields) when any submitted
                    field's current value differs from its snapshot value. Omit for legacy behavior (no guard). Never
                    stored.
                name:
                  type: string
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
                website:
                  type: string
                  nullable: true
                legalEntityType:
                  type: string
                  enum:
                    - individual
                    - corporation
                    - llc
                    - partnership
                    - sole_proprietorship
                    - other
                  nullable: true
                taxId:
                  type: string
                  nullable: true
                timezone:
                  type: string
                  nullable: true
                partnerManagerId:
                  type: string
                  format: uuid
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                    - rejected
                defaultPrice:
                  type: number
                  nullable: true
                acceptsDuplicates:
                  type: boolean
                acceptsReturns:
                  type: boolean
                contractStartDate:
                  type: string
                  format: date
                  nullable: true
                contractEndDate:
                  type: string
                  format: date
                  nullable: true
                billingContactName:
                  type: string
                  nullable: true
                billingContactEmail:
                  type: string
                  format: email
                  nullable: true
                billingContactPhone:
                  type: string
                  nullable: true
                paymentTerms:
                  type: string
                  nullable: true
                notes:
                  type: string
                  nullable: true
                paymentCycle:
                  type: string
                  enum:
                    - weekly
                    - bimonthly
                    - monthly
                    - adhoc
                paymentThreshold:
                  type: number
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                deactivateChildren:
                  type: boolean
                  description: When true and status transitions to inactive, cascades non-inactive campaigns to inactive.
            example:
              status: paused
      responses:
        "200":
          description: Partner updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                  deactivatedCampaigns:
                    type: integer
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deletePartner
      tags:
        - Partners
      summary: Delete partner
      description: Soft-deletes a partner by transitioning its status to `inactive`.
      responses:
        "200":
          description: Partner soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "400":
          description: Invalid status transition to inactive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/partners/{partnerId}/posting-key:
    parameters:
      - name: partnerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: regeneratePartnerPostingKey
      tags:
        - Partners
      summary: Regenerate partner posting key
      security:
        - sessionCookie: []
      responses:
        "200":
          description: New posting key generated
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/buyers:
    get:
      operationId: listBuyers
      tags:
        - Buyers
      summary: List buyers
      description: Returns a paginated list of lead buyers.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - pending
              - active
              - inactive
              - rejected
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated buyer list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createBuyer
      tags:
        - Buyers
      summary: Create buyer
      description: Creates a new lead buyer account.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
                website:
                  type: string
                  nullable: true
                legalEntityType:
                  type: string
                  enum:
                    - individual
                    - corporation
                    - llc
                    - partnership
                    - sole_proprietorship
                    - other
                  nullable: true
                taxId:
                  type: string
                  nullable: true
                timezone:
                  type: string
                  nullable: true
                salesRepId:
                  type: string
                  format: uuid
                  nullable: true
                accountManagerId:
                  type: string
                  format: uuid
                  nullable: true
                billingCycle:
                  type: string
                  enum:
                    - prepay
                    - weekly
                    - bimonthly
                    - monthly
                  default: monthly
                brandName:
                  type: string
                  nullable: true
                brandLogoUrl:
                  type: string
                  nullable: true
                brandTcpa:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - inactive
                    - rejected
                  default: active
                pricingModel:
                  type: string
                  enum:
                    - fixed
                    - ping_post
                  default: fixed
                defaultPrice:
                  type: number
                  nullable: true
                creditLimit:
                  type: string
                  nullable: true
                autoRecharge:
                  type: boolean
                autoRechargeAmount:
                  type: string
                  nullable: true
                autoRechargeThreshold:
                  type: string
                  nullable: true
                dedupEnabled:
                  type: boolean
                dedupFields:
                  type: array
                  items:
                    type: string
                dedupWindowDays:
                  type: integer
                  minimum: 0
                postbackKey:
                  type: string
                  nullable: true
                partnerResponseBuyerName:
                  type: string
                  nullable: true
                partnerResponseBuyerPhone:
                  type: string
                  nullable: true
                  description: Must be E.164 when set.
                partnerResponseBuyerId:
                  type: string
                  nullable: true
                partnerResponseRedirectUrl:
                  type: string
                  nullable: true
                  description: http(s) URL template allowing only {{leadId}} and {{partnerId}} tokens.
                partnerResponseBuyerMediaUrl:
                  type: string
                  format: uri
                  nullable: true
                partnerResponseBuyerMediaType:
                  type: string
                  enum:
                    - photo
                    - video
                  nullable: true
                acceptsReturns:
                  type: boolean
                  default: true
                returnWindow:
                  type: integer
                  minimum: 0
                  default: 72
                billingContactName:
                  type: string
                  nullable: true
                billingContactEmail:
                  type: string
                  format: email
                  nullable: true
                billingContactPhone:
                  type: string
                  nullable: true
                paymentTerms:
                  type: string
                  nullable: true
                notes:
                  type: string
                  nullable: true
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
            example:
              name: Acme Insurance
              email: leads@acmeinsurance.com
              pricingModel: fixed
              defaultPrice: 25
              billingCycle: monthly
      responses:
        "201":
          description: Buyer created
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
  /api/v1/buyers/{buyerId}:
    parameters:
      - name: buyerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getBuyer
      tags:
        - Buyers
      summary: Get buyer
      description: Returns a single buyer by ID with balance and contract summary.
      responses:
        "200":
          description: Buyer detail
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateBuyer
      tags:
        - Buyers
      summary: Update buyer
      description: Partially update a buyer. All fields optional.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                company:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
                website:
                  type: string
                  nullable: true
                legalEntityType:
                  type: string
                  nullable: true
                  enum:
                    - individual
                    - corporation
                    - llc
                    - partnership
                    - sole_proprietorship
                    - other
                taxId:
                  type: string
                  nullable: true
                timezone:
                  type: string
                  nullable: true
                salesRepId:
                  type: string
                  format: uuid
                  nullable: true
                accountManagerId:
                  type: string
                  format: uuid
                  nullable: true
                billingCycle:
                  type: string
                  enum:
                    - prepay
                    - weekly
                    - bimonthly
                    - monthly
                brandName:
                  type: string
                  nullable: true
                brandLogoUrl:
                  type: string
                  nullable: true
                brandTcpa:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - inactive
                    - rejected
                pricingModel:
                  type: string
                  enum:
                    - fixed
                    - ping_post
                defaultPrice:
                  type: number
                  nullable: true
                creditLimit:
                  type: string
                  nullable: true
                autoRecharge:
                  type: boolean
                autoRechargeAmount:
                  type: string
                  nullable: true
                autoRechargeThreshold:
                  type: string
                  nullable: true
                dedupEnabled:
                  type: boolean
                dedupFields:
                  type: array
                  items:
                    type: string
                dedupWindowDays:
                  type: integer
                postbackKey:
                  type: string
                  nullable: true
                partnerResponseBuyerName:
                  type: string
                  nullable: true
                partnerResponseBuyerPhone:
                  type: string
                  nullable: true
                  description: E.164 phone number
                partnerResponseBuyerId:
                  type: string
                  nullable: true
                partnerResponseRedirectUrl:
                  type: string
                  nullable: true
                  description: http(s) URL template with only {{leadId}} and {{partnerId}} tokens
                partnerResponseBuyerMediaUrl:
                  type: string
                  format: uri
                  nullable: true
                partnerResponseBuyerMediaType:
                  type: string
                  nullable: true
                  enum:
                    - photo
                    - video
                acceptsReturns:
                  type: boolean
                returnWindow:
                  type: integer
                billingContactName:
                  type: string
                  nullable: true
                billingContactEmail:
                  type: string
                  format: email
                  nullable: true
                billingContactPhone:
                  type: string
                  nullable: true
                paymentTerms:
                  type: string
                  nullable: true
                notes:
                  type: string
                  nullable: true
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                pauseSubscriptions:
                  type: boolean
                  description: Cascade flag (not persisted). When true and the buyer transitions to inactive, pauses the buyer's active
                    subscriptions.
                deactivateChildren:
                  type: boolean
                  description: Cascade flag (not persisted). When true and the buyer transitions to inactive, expires the buyer's
                    non-expired contracts.
            example:
              status: inactive
              pauseSubscriptions: true
      responses:
        "200":
          description: Buyer updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteBuyer
      tags:
        - Buyers
      summary: Delete buyer
      description: Deletes a buyer if no active contracts exist.
      responses:
        "200":
          description: Buyer deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/buyers/{buyerId}/balance:
    parameters:
      - name: buyerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getBuyerBalance
      tags:
        - Buyers
      summary: Get buyer balance
      description: Returns the buyer's current balance and credit limit. Buyers may only read their own balance.
      responses:
        "200":
          description: Buyer balance
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      buyerId:
                        type: string
                        format: uuid
                      balance:
                        type: string
                      creditLimit:
                        type: string
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    post:
      operationId: adjustBuyerBalance
      tags:
        - Buyers
      summary: Adjust buyer balance
      description: Add or deduct funds from a buyer's prepay balance.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amount
              properties:
                amount:
                  type: number
                  description: Positive amount to add to the buyer's balance.
                reference:
                  type: string
                notes:
                  type: string
            example:
              amount: 500
              reference: WIRE-20260315
              notes: Wire transfer received
      responses:
        "200":
          description: Balance adjusted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/buyers/{buyerId}/status:
    parameters:
      - name: buyerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      operationId: updateBuyerStatus
      tags:
        - Buyers
      summary: Update buyer status
      description: Change a buyer's status (active, paused, suspended, inactive).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - inactive
                    - rejected
                pauseSubscriptions:
                  type: boolean
                  description: When true and status transitions to inactive, best-effort pauses the buyer's Stripe subscriptions.
            example:
              status: inactive
      responses:
        "200":
          description: Status updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/buyers/{buyerId}/media:
    parameters:
      - name: buyerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: uploadBuyerMedia
      tags:
        - Buyers
      summary: Upload buyer media
      description: Upload a photo or video for the buyer's partner-response branding. Stored in Cloudflare R2.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Media uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                      mediaType:
                        type: string
                        enum:
                          - photo
                          - video
        "400":
          description: Validation error (unsupported type or too large)
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
    delete:
      operationId: deleteBuyerMedia
      tags:
        - Buyers
      summary: Delete buyer media
      description: Remove the buyer's stored media object and clear the columns.
      responses:
        "200":
          description: Media removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/campaigns:
    get:
      operationId: listCampaigns
      tags:
        - Campaigns
      summary: List campaigns
      description: Returns a paginated list of campaigns (lead intake sources).
      parameters:
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          description: "Single status or comma-separated list of statuses. Allowed values: testing, active, paused, inactive."
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated campaign list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createCampaign
      tags:
        - Campaigns
      summary: Create campaign
      description: Creates a new campaign tied to a partner and offer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerId
                - name
                - offerId
              properties:
                partnerId:
                  type: string
                  format: uuid
                name:
                  type: string
                offerId:
                  type: string
                  format: uuid
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                cplEnabled:
                  type: boolean
                  default: true
                pricingModel:
                  type: string
                  enum:
                    - fixed
                    - revshare
                  default: fixed
                costPerLead:
                  type: string
                  default: "0"
                maximumPayout:
                  type: string
                  nullable: true
                cpaEnabled:
                  type: boolean
                  default: false
                cpaCost:
                  type: string
                  default: "0"
                cpaPricingModel:
                  type: string
                  enum:
                    - fixed
                    - revshare
                  default: fixed
                cpaMaxPayout:
                  type: string
                  nullable: true
                dailyCap:
                  type: integer
                  nullable: true
                weeklyCap:
                  type: integer
                  nullable: true
                monthlyCap:
                  type: integer
                  nullable: true
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                callEnabled:
                  type: boolean
                  default: false
                callTrackingAreaCode:
                  type: string
                  pattern: ^\d{3}$
                  nullable: true
                callDniEnabled:
                  type: boolean
                  default: false
                callDniPoolEnabled:
                  type: boolean
                  default: false
                callDniCaptureEnabled:
                  type: boolean
                  default: false
                callDniReplaceNumbers:
                  type: array
                  items:
                    type: string
                callDniCaptureConfig:
                  type: object
                  additionalProperties: true
                callDniConfig:
                  type: object
                  additionalProperties: true
                callDniNoImpressionPolicy:
                  type: string
                  enum:
                    - route_static
                    - reject
                  default: route_static
                fieldOverrides:
                  type: object
                  nullable: true
                  additionalProperties:
                    type: object
                    properties:
                      required:
                        type: boolean
                      hidden:
                        type: boolean
                intakeFilters:
                  type: array
                  items:
                    type: object
                intakeFailMode:
                  type: string
                  enum:
                    - reject
                    - accept_zero_cost
                  default: reject
                sendBuyerNameInPartnerResponse:
                  type: boolean
                  default: false
                sendBuyerPhoneInPartnerResponse:
                  type: boolean
                  default: false
                sendBuyerIdInPartnerResponse:
                  type: boolean
                  default: false
                sendRedirectUrlInPartnerResponse:
                  type: boolean
                  default: false
                sendBuyerMediaInPartnerResponse:
                  type: boolean
                  default: false
                notes:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - testing
                    - active
                    - paused
                    - inactive
                  default: testing
            example:
              partnerId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              name: LeadGen Pro - Auto Insurance Q1
              offerId: b2c3d4e5-f6a7-8901-bcde-f12345678901
              costPerLead: "5.00"
              dailyCap: 100
      responses:
        "201":
          description: Campaign created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                  autoProvisionedApiKey:
                    type: string
                    description: Present only when a posting key was auto-provisioned for the partner during creation.
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A campaign already exists for this partner and offer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/campaigns/{campaignId}:
    parameters:
      - name: campaignId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getCampaign
      tags:
        - Campaigns
      summary: Get campaign
      description: Returns a single campaign with posting instructions.
      responses:
        "200":
          description: Campaign detail
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateCampaign
      tags:
        - Campaigns
      summary: Update campaign
      description: Partially update a campaign. All fields optional. verticalId is immutable post-creation and cannot be changed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                _snapshot:
                  type: object
                  additionalProperties: true
                  description: Optional concurrent-edit guard (field-level). Send the page-load values of exactly the fields included in
                    this PATCH; the server responds 409 (code CONFLICT, details.conflictFields) when any submitted
                    field's current value differs from its snapshot value. Omit for legacy behavior (no guard). Never
                    stored.
                partnerId:
                  type: string
                  format: uuid
                name:
                  type: string
                offerId:
                  type: string
                  format: uuid
                cplEnabled:
                  type: boolean
                pricingModel:
                  type: string
                  enum:
                    - fixed
                    - revshare
                costPerLead:
                  type: string
                maximumPayout:
                  type: string
                  nullable: true
                cpaEnabled:
                  type: boolean
                cpaCost:
                  type: string
                cpaPricingModel:
                  type: string
                  enum:
                    - fixed
                    - revshare
                cpaMaxPayout:
                  type: string
                  nullable: true
                dailyCap:
                  type: integer
                  nullable: true
                weeklyCap:
                  type: integer
                  nullable: true
                monthlyCap:
                  type: integer
                  nullable: true
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                callEnabled:
                  type: boolean
                callTrackingAreaCode:
                  type: string
                  nullable: true
                  description: 3-digit US area code
                callDniEnabled:
                  type: boolean
                callDniPoolEnabled:
                  type: boolean
                callDniCaptureEnabled:
                  type: boolean
                callDniReplaceNumbers:
                  type: array
                  items:
                    type: string
                callDniCaptureConfig:
                  type: object
                  additionalProperties: true
                callDniConfig:
                  type: object
                  additionalProperties: true
                callDniNoImpressionPolicy:
                  type: string
                  enum:
                    - route_static
                    - reject
                fieldOverrides:
                  type: object
                  nullable: true
                  additionalProperties: true
                intakeFilters:
                  type: array
                  items:
                    type: object
                intakeFailMode:
                  type: string
                  enum:
                    - reject
                    - accept_zero_cost
                sendBuyerNameInPartnerResponse:
                  type: boolean
                sendBuyerPhoneInPartnerResponse:
                  type: boolean
                sendBuyerIdInPartnerResponse:
                  type: boolean
                sendRedirectUrlInPartnerResponse:
                  type: boolean
                sendBuyerMediaInPartnerResponse:
                  type: boolean
                notes:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - testing
                    - active
                    - paused
                    - inactive
            example:
              costPerLead: "7.50"
      responses:
        "200":
          description: Campaign updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteCampaign
      tags:
        - Campaigns
      summary: Delete campaign
      description: Soft-deletes a campaign by transitioning its status to `paused`.
      responses:
        "200":
          description: Campaign deleted
        "400":
          description: Invalid status transition to paused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/campaigns/{campaignId}/fields:
    parameters:
      - name: campaignId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listCampaignFields
      tags:
        - Campaigns
      summary: List campaign fields
      description: Returns the field configuration for this campaign.
      responses:
        "200":
          description: Field config
      security:
        - sessionCookie: []
    post:
      operationId: createCampaignField
      tags:
        - Campaigns
      summary: Create campaign field
      description: Adds a custom field to the campaign. Rejects names that conflict with reserved system fields, the
        campaign's vertical fields, or the parent offer's field settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - label
                - type
              properties:
                name:
                  type: string
                label:
                  type: string
                type:
                  type: string
                required:
                  type: boolean
                options:
                  type: array
                  nullable: true
                  items:
                    type: string
                validation:
                  type: string
                  nullable: true
                sortOrder:
                  type: integer
      responses:
        "201":
          description: Field created
        "400":
          $ref: "#/components/responses/ValidationError"
      security:
        - sessionCookie: []
    put:
      operationId: updateCampaignField
      tags:
        - Campaigns
      summary: Update campaign field
      description: Updates an existing campaign field by id. Renames are rejected when they collide with the parent offer's
        field settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  format: uuid
                name:
                  type: string
                label:
                  type: string
                type:
                  type: string
                required:
                  type: boolean
                options:
                  type: array
                  nullable: true
                  items:
                    type: string
                validation:
                  type: string
                  nullable: true
                sortOrder:
                  type: integer
      responses:
        "200":
          description: Field updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteCampaignField
      tags:
        - Campaigns
      summary: Delete campaign field
      description: Deletes a campaign field. The field id is passed via the fieldId query parameter.
      parameters:
        - name: fieldId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Field deleted
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/campaigns/{campaignId}/fields/reorder:
    parameters:
      - name: campaignId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      operationId: reorderCampaignFields
      tags:
        - Campaigns
      summary: Reorder campaign fields
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fieldIds
              properties:
                fieldIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
      responses:
        "200":
          description: Fields reordered
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/campaigns/{campaignId}/posting:
    parameters:
      - name: campaignId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getCampaignPostingInstructions
      tags:
        - Campaigns
      summary: Get posting instructions
      description: Returns the posting URL, key, and required fields for this campaign.
      responses:
        "200":
          description: Posting instructions
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/contracts:
    get:
      operationId: listContracts
      tags:
        - Contracts
      summary: List contracts
      description: Returns a paginated list of buyer contracts.
      parameters:
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          description: Contract status. Accepts a comma-separated list for multi-status filtering (e.g. active,paused).
          schema:
            type: string
            enum:
              - setup
              - active
              - paused
              - expired
              - archived
        - name: funding
          in: query
          description: Filter by contract funding source.
          schema:
            type: string
            enum:
              - sku
              - standalone
        - name: search
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
          description: Sort column (e.g. name, createdAt, price)
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated contract list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
    post:
      operationId: createContract
      tags:
        - Contracts
      summary: Create contract
      description: Creates a new buyer contract with pricing, filters, caps, and delivery configuration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - buyerId
                - verticalId
                - name
                - price
              properties:
                buyerId:
                  type: string
                  format: uuid
                verticalId:
                  type: string
                  format: uuid
                name:
                  type: string
                price:
                  type: number
                  minimum: 0
                ruleType:
                  type: string
                  enum:
                    - exclusive
                    - multisale
                  default: exclusive
                status:
                  type: string
                  enum:
                    - setup
                    - active
                    - paused
                    - expired
                    - archived
                  default: setup
                pricingModel:
                  type: string
                  enum:
                    - fixed
                    - ping_post
                  default: fixed
                deliveryMethod:
                  type: string
                  enum:
                    - http_post
                    - http_get
                    - email
                    - ftp
                    - google_sheets
                  default: http_post
                deliveryFormat:
                  type: string
                  enum:
                    - json
                    - xml
                    - form
                  default: json
                deliveryUrl:
                  type: string
                  format: uri
                  nullable: true
                priority:
                  type: integer
                  default: 0
                contactId:
                  type: string
                  format: uuid
                  nullable: true
                exclusiveMultiplier:
                  type: number
                  nullable: true
                allowMultiSale:
                  type: boolean
                  default: false
                deliveryEmail:
                  type: string
                  format: email
                  nullable: true
                requiresPing:
                  type: boolean
                  default: false
                pingTimeout:
                  type: integer
                  minimum: 100
                  maximum: 60000
                  default: 30000
                geo:
                  $ref: "#/components/schemas/GeoTarget"
                schedule:
                  $ref: "#/components/schemas/Schedule"
                dailyLeadCap:
                  type: integer
                  nullable: true
                weeklyLeadCap:
                  type: integer
                  nullable: true
                monthlyLeadCap:
                  type: integer
                  nullable: true
                geoFilters:
                  type: object
                  additionalProperties: true
                  nullable: true
                demographicFilters:
                  type: object
                  additionalProperties: true
                  nullable: true
                customFilters:
                  type: array
                  nullable: true
                  items:
                    type: object
                    required:
                      - field
                      - operator
                    properties:
                      field:
                        type: string
                      operator:
                        type: string
                        enum:
                          - equals
                          - not_equals
                          - contains
                          - in
                          - not_in
                          - greater_than
                          - less_than
                          - is_empty
                          - is_not_empty
                      value:
                        nullable: true
                fieldMapping:
                  type: object
                  additionalProperties:
                    type: string
                acceptsReturns:
                  type: boolean
                  default: true
                returnWindow:
                  type: integer
                  default: 72
                deliveryConfig:
                  type: array
                  items:
                    $ref: "#/components/schemas/DeliveryAction"
                intakeCenterId:
                  type: string
                  format: uuid
                  nullable: true
                callEnabled:
                  type: boolean
                  default: false
                callPhoneNumber:
                  type: string
                  nullable: true
                callBackupNumber:
                  type: string
                  nullable: true
                premiumDestinationAck:
                  type: boolean
                  description: Required true when callPhoneNumber / callBackupNumber classifies as a premium-rate (high-cost NPA)
                    destination — otherwise 400 with error code PREMIUM_ACK_REQUIRED. Stamps the per-minute surcharge on
                    the contract. Not persisted. Non-US transfer numbers are rejected regardless of ack.
                callCapMode:
                  type: string
                  enum:
                    - separate
                    - shared
                  default: separate
            example:
              buyerId: c3d4e5f6-a7b8-9012-cdef-123456789012
              verticalId: d4e5f6a7-b8c9-0123-defa-234567890123
              name: Acme Auto Insurance - East Coast
              price: 25
              geo:
                states:
                  - NY
                  - NJ
                  - CT
                  - PA
              dailyLeadCap: 50
              monthlyLeadCap: 1000
      responses:
        "201":
          description: Contract created. When the saved transfer number(s) classify as a premium-rate destination, `data`
            additionally carries `premiumDestination` with the stamped surcharge.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/ContractCreate"
                      - type: object
                        properties:
                          premiumDestination:
                            $ref: "#/components/schemas/PremiumDestination"
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
  /api/v1/contracts/{contractId}:
    parameters:
      - name: contractId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getContract
      tags:
        - Contracts
      summary: Get contract
      description: Returns a single contract by ID with full configuration.
      responses:
        "200":
          description: Contract detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateContract
      tags:
        - Contracts
      summary: Update contract
      description: Partially update a contract's pricing, filters, caps, delivery, or call settings. All fields optional.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                _snapshot:
                  type: object
                  additionalProperties: true
                  description: Optional concurrent-edit guard (field-level). Send the page-load values of exactly the fields included in
                    this PATCH; the server responds 409 (code CONFLICT, details.conflictFields) when any submitted
                    field's current value differs from its snapshot value. Omit for legacy behavior (no guard). Never
                    stored.
                ruleType:
                  type: string
                  enum:
                    - exclusive
                    - multisale
                contactId:
                  type: string
                  format: uuid
                  nullable: true
                buyerId:
                  type: string
                  format: uuid
                verticalId:
                  type: string
                  format: uuid
                name:
                  type: string
                priority:
                  type: integer
                status:
                  type: string
                  enum:
                    - setup
                    - active
                    - paused
                    - expired
                pricingModel:
                  type: string
                  enum:
                    - fixed
                    - ping_post
                price:
                  type: number
                exclusiveMultiplier:
                  description: Freeform multiplier value
                allowMultiSale:
                  type: boolean
                deliveryMethod:
                  type: string
                  enum:
                    - http_post
                    - http_get
                    - email
                    - ftp
                    - google_sheets
                    - sms
                deliveryFormat:
                  type: string
                  enum:
                    - json
                    - xml
                    - form
                deliveryUrl:
                  type: string
                  format: uri
                  nullable: true
                deliveryEmail:
                  type: string
                  format: email
                  nullable: true
                requiresPing:
                  type: boolean
                acceptsReturns:
                  type: boolean
                returnWindow:
                  type: integer
                minQualityScore:
                  type: integer
                  minimum: 1
                  maximum: 10
                  nullable: true
                  description: Minimum lead quality score (1–10) this contract will accept (Lead Scoring M1). When set, a scored lead
                    whose `qualityScore` is below this value is rejected during routing eligibility (trace reason
                    `quality_score_below_minimum`). Unscored leads (`qualityScore` null) always pass the gate. Null
                    disables the gate.
                geo:
                  type: object
                  properties:
                    states:
                      type: array
                      items:
                        type: string
                    zipCodes:
                      type: array
                      items:
                        type: string
                    cities:
                      type: array
                      items:
                        type: string
                    exclude:
                      type: boolean
                schedule:
                  type: object
                  additionalProperties: true
                caps:
                  type: object
                  properties:
                    daily:
                      type: integer
                      nullable: true
                    weekly:
                      type: integer
                      nullable: true
                    monthly:
                      type: integer
                      nullable: true
                    concurrent:
                      type: integer
                      nullable: true
                fieldMapping:
                  type: object
                  additionalProperties:
                    type: string
                geoFilters:
                  description: Freeform geo filter config
                demographicFilters:
                  description: Freeform demographic filter config
                customFilters:
                  type: array
                  nullable: true
                  items:
                    type: object
                    properties:
                      field:
                        type: string
                      operator:
                        type: string
                        enum:
                          - equals
                          - not_equals
                          - contains
                          - in
                          - not_in
                          - greater_than
                          - less_than
                          - is_empty
                          - is_not_empty
                      value:
                        nullable: true
                scheduleTimezone:
                  type: string
                  nullable: true
                scheduleHours:
                  description: Freeform schedule-hours config
                expiresAt:
                  type: string
                  format: date-time
                  nullable: true
                deliveryMethodOverride:
                  type: string
                  nullable: true
                deliveryUrlOverride:
                  type: string
                  nullable: true
                deliveryConfig:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
                phoneFormat:
                  type: string
                  nullable: true
                  enum:
                    - digits10
                    - digits11
                    - e164
                    - formatted
                    - raw
                intakeCenterId:
                  type: string
                  format: uuid
                  nullable: true
                revenueOnAcceptance:
                  type: boolean
                revenueOnCpa:
                  type: boolean
                allowZeroCpaConversion:
                  type: boolean
                  description: Allow a $0 CPA to convert as a real conversion (cost-plus). Off by default; a $0 CPA is otherwise rejected.
                cpaAmount:
                  description: Freeform CPA amount
                cpaWindowDays:
                  type: integer
                  nullable: true
                pricePerLead:
                  description: Freeform decimal price per lead
                dailyLeadCap:
                  description: Freeform daily lead cap
                weeklyLeadCap:
                  description: Freeform weekly lead cap
                monthlyLeadCap:
                  description: Freeform monthly lead cap
                dailySpendCap:
                  description: Freeform daily spend cap
                weeklySpendCap:
                  description: Freeform weekly spend cap
                monthlySpendCap:
                  description: Freeform monthly spend cap
                legacyId:
                  type: string
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                inboundWebhookKey:
                  type: string
                  nullable: true
                pingDeliveryConfig:
                  type: object
                  nullable: true
                  additionalProperties: true
                pingTimeout:
                  type: integer
                minimumBidFloor:
                  description: Freeform minimum bid floor
                  nullable: true
                cpaLookbackDays:
                  type: integer
                  nullable: true
                  enum:
                    - 7
                    - 15
                    - 30
                    - 60
                    - 90
                cpaLookbackExcludeDays:
                  type: integer
                callEnabled:
                  type: boolean
                callPhoneNumber:
                  type: string
                  nullable: true
                  description: E.164 phone number
                callBackupNumber:
                  type: string
                  nullable: true
                  description: E.164 phone number
                premiumDestinationAck:
                  type: boolean
                  description: Required true when a CHANGED callPhoneNumber / callBackupNumber classifies as a premium-rate (high-cost
                    NPA) destination — otherwise 400 with error code PREMIUM_ACK_REQUIRED. Acknowledging stamps the
                    per-minute surcharge; changing back to a normal number clears it. Unchanged numbers need no re-ack.
                    Not persisted. Non-US transfer numbers are rejected regardless of ack.
                premiumDestinationSurchargeCreditsPerMin:
                  type: string
                  nullable: true
                  description: Superadmin only — per-contract premium-destination surcharge override (credits/min, decimal string; null
                    removes it). Silently stripped for any other role (the guard's automatic stamp/clear stays the sole
                    writer for tenant admins). When a superadmin sends it alongside a transfer-number change, the
                    explicit value wins over the guard's stamp/clear.
                callRingTimeout:
                  type: integer
                  nullable: true
                callWhisperMessage:
                  type: string
                  nullable: true
                callWhisperRequired:
                  type: boolean
                recordCallsOverride:
                  type: boolean
                  nullable: true
                callCapMode:
                  type: string
                  enum:
                    - separate
                    - shared
                dailyCallCap:
                  type: integer
                  nullable: true
                weeklyCallCap:
                  type: integer
                  nullable: true
                monthlyCallCap:
                  type: integer
                  nullable: true
                dedupEnabled:
                  type: boolean
                  nullable: true
                dedupWindowDays:
                  type: integer
                  nullable: true
                aiDialerEnabled:
                  type: boolean
                aiDialerAgentId:
                  type: string
                  format: uuid
                  nullable: true
                aiDialerCadenceId:
                  type: string
                  format: uuid
                  nullable: true
                aiDialerDelaySeconds:
                  type: integer
                productId:
                  type: string
                  format: uuid
                  nullable: true
                notes:
                  type: string
                  nullable: true
                confirmDisableCall:
                  type: boolean
                  description: Bypass the LAST_CALL_CONTRACT warning when disabling the last call-enabled contract on a
                    requireCallContracts offer.
                alsoPauseSubscription:
                  type: boolean
                  description: Cascade flag (not persisted). When true and the contract flips active→paused, pauses this contract's
                    buyerSku subscriptions.
                alsoResumeSubscription:
                  type: boolean
                  description: Cascade flag (not persisted). When true and the contract flips paused→active, resumes this contract's
                    buyerSku subscriptions.
            example:
              price: 30
              caps:
                daily: 75
      responses:
        "200":
          description: "Contract updated. On success `data` is the updated contract plus `subscriptionsPaused` /
            `subscriptionsResumed` (integer, present when a pause/resume status was requested) and `cascade` ({ ok,
            reason? }). When the effective transfer number(s) classify premium, `data` also carries `premiumDestination`
            with the stamped surcharge. When disabling the last call-enabled contract on a requireCallContracts offer
            without `confirmDisableCall`, returns `data: null` with a `warning` envelope and applies no change."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    nullable: true
                    properties:
                      premiumDestination:
                        $ref: "#/components/schemas/PremiumDestination"
                      subscriptionsPaused:
                        type: integer
                        description: Inside `data` (present when a pause status was requested).
                      subscriptionsResumed:
                        type: integer
                        description: Inside `data` (present when a resume status was requested).
                      cascade:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          reason:
                            type: string
                  warning:
                    type: object
                    properties:
                      code:
                        type: string
                      offerNames:
                        type: array
                        items:
                          type: string
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteContract
      tags:
        - Contracts
      summary: Delete contract
      description: Deletes a contract.
      responses:
        "200":
          description: Contract deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/contracts/{contractId}/status:
    parameters:
      - name: contractId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      operationId: updateContractStatus
      tags:
        - Contracts
      summary: Update contract status
      description: Change a contract's status.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - setup
                    - active
                    - paused
                    - expired
                alsoPauseSubscription:
                  type: boolean
                  description: Cascade flag (not persisted). When true and the contract flips active→paused, pauses this contract's
                    buyerSku subscriptions.
                alsoResumeSubscription:
                  type: boolean
                  description: Cascade flag (not persisted). When true and the contract flips paused→active, resumes this contract's
                    buyerSku subscriptions.
            example:
              status: paused
      responses:
        "200":
          description: Status updated. On success `data` is the updated contract plus `subscriptionsPaused` /
            `subscriptionsResumed` (integer, present when a pause/resume status was requested) and `cascade` ({ ok,
            reason? }).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                  subscriptionsPaused:
                    type: integer
                  subscriptionsResumed:
                    type: integer
                  cascade:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      reason:
                        type: string
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offers:
    get:
      operationId: listOffers
      tags:
        - Offers
      summary: List offers
      description: Returns a paginated list of offers with campaign/contract counts.
      parameters:
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - paused
              - inactive
              - setup
        - name: visibility
          in: query
          schema:
            type: string
            enum:
              - public
              - private
        - name: contractId
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
          description: Sort column (e.g. name, createdAt, status)
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated offer list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createOffer
      tags:
        - Offers
      summary: Create offer
      description: Creates an offer that links campaigns to contracts with distribution rules.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - verticalId
              properties:
                name:
                  type: string
                verticalId:
                  type: string
                  format: uuid
                description:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                    - setup
                  default: active
                distributionType:
                  type: string
                  enum:
                    - exclusive
                    - multisell
                    - hybrid
                  default: exclusive
                distributionRule:
                  type: string
                  enum:
                    - price
                    - priority
                    - weight
                    - round_robin
                    - scarcity_weighted
                    - quality_tier
                  default: priority
                maxSales:
                  type: integer
                  nullable: true
                visibility:
                  type: string
                  enum:
                    - public
                    - private
                  default: private
                defaultPayoutModel:
                  type: string
                  nullable: true
                defaultPayoutAmount:
                  type: number
                  minimum: 0
                  nullable: true
                cpaPayoutModel:
                  type: string
                  enum:
                    - fixed
                    - revshare
                  nullable: true
                cpaPayoutAmount:
                  type: number
                  minimum: 0
                  nullable: true
                intakeCenterId:
                  type: string
                  format: uuid
                  nullable: true
                callEnabled:
                  type: boolean
                  default: false
                callCallerAffinityMode:
                  type: string
                  enum:
                    - off
                    - same
                    - exclude
                  default: same
                callLeadAffinityMode:
                  type: string
                  enum:
                    - off
                    - same
                    - exclude
                  default: off
                callAffinityWindowDays:
                  type: integer
                  minimum: 1
                  default: 30
                callDistributionType:
                  type: string
                  enum:
                    - sequential
                    - non_sequential
                  default: sequential
                callDistributionRule:
                  type: string
                  enum:
                    - price
                    - priority
                    - weight
                    - round_robin
                    - simultaneous
                  default: priority
                callAffinityFallback:
                  type: string
                  enum:
                    - none
                    - next_match
                  default: none
                requireCallContracts:
                  type: boolean
                  default: false
                fieldSettings:
                  type: object
                  nullable: true
                attributionPolicy:
                  type: object
                  nullable: true
                balanceCheckEnabled:
                  type: boolean
                  nullable: true
                billDqLeads:
                  type: boolean
                  default: true
                  description: When false, DQ delivery still runs but DQ sale price/cost, buyer billing, partner payout, and billable misc
                    costs stay zero.
            example:
              name: Auto Insurance - National
              verticalId: d4e5f6a7-b8c9-0123-defa-234567890123
              distributionType: exclusive
              distributionRule: priority
      responses:
        "201":
          description: Offer created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/OfferCreate"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getOffer
      tags:
        - Offers
      summary: Get offer
      description: >
        Returns an offer with campaign count, contract count, and related metadata. Includes dqDistributionRule,
        billDqLeads, dqCplPayoutModel, dqCplPayoutPercent, dqCpaPayoutModel, dqCpaPayoutPercent when DQ contracts are
        configured.
      responses:
        "200":
          description: Offer detail
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateOffer
      tags:
        - Offers
      summary: Update offer
      description: Partially update an offer's distribution rules, status, or settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                _snapshot:
                  type: object
                  additionalProperties: true
                  description: Optional concurrent-edit guard (field-level). Send the page-load values of exactly the fields included in
                    this PATCH; the server responds 409 (code CONFLICT, details.conflictFields) when any submitted
                    field's current value differs from its snapshot value. Omit for legacy behavior (no guard). Never
                    stored.
                name:
                  type: string
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                    - setup
                distributionType:
                  type: string
                  enum:
                    - exclusive
                    - multisell
                    - hybrid
                distributionRule:
                  type: string
                  enum:
                    - price
                    - priority
                    - weight
                    - round_robin
                    - scarcity_weighted
                    - quality_tier
                dqDistributionRule:
                  type: string
                  enum:
                    - all
                    - round_robin
                  nullable: true
                billDqLeads:
                  type: boolean
                  description: When false, DQ delivery still runs but DQ sale price/cost, buyer billing, partner payout, and billable misc
                    costs stay zero.
                dqCplPayoutModel:
                  type: string
                  enum:
                    - inherit
                    - revshare
                  nullable: true
                dqCplPayoutPercent:
                  type: string
                  nullable: true
                dqCpaPayoutModel:
                  type: string
                  enum:
                    - inherit
                    - revshare
                  nullable: true
                dqCpaPayoutPercent:
                  type: string
                  nullable: true
                scoringModelId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Offer-level override of the vertical's default scoring model (Lead Scoring M1). Setting a non-null value
                    validates the model's config against the offer's vertical intake fields and responds 400
                    VALIDATION_ERROR (issues in `details`) on a blocking error. The model must belong to the same tenant
                    (404 otherwise). Null falls back to the vertical default.
            example:
              distributionRule: weight
      responses:
        "200":
          description: Offer updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteOffer
      tags:
        - Offers
      summary: Delete offer
      description: Deletes an offer and its campaign/contract links.
      responses:
        "200":
          description: Offer deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/campaigns:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listOfferCampaigns
      tags:
        - Offers
      summary: List offer campaigns
      description: Returns campaigns linked to this offer.
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated campaign links
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: linkCampaignToOffer
      tags:
        - Offers
      summary: Link campaign to offer
      description: Associates a campaign with this offer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  type: string
                  format: uuid
            example:
              campaignId: f6a7b8c9-d0e1-2345-fabc-456789012345
      responses:
        "201":
          description: Campaign linked
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Campaign is already linked to this offer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/campaigns/{campaignId}:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: campaignId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: unlinkCampaignFromOffer
      tags:
        - Offers
      summary: Unlink campaign from offer
      description: Removes the campaign-offer association.
      responses:
        "200":
          description: Campaign unlinked
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/contracts:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listOfferContracts
      tags:
        - Offers
      summary: List offer contracts
      description: Returns contracts linked to this offer with weight/priority overrides.
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated contract links
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: linkContractToOffer
      tags:
        - Offers
      summary: Link contract to offer
      description: Associates a contract with this offer, optionally setting weight and priority.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contractId
              properties:
                contractId:
                  type: string
                  format: uuid
                weight:
                  type: integer
                  minimum: 1
                priority:
                  type: integer
                  minimum: 0
            example:
              contractId: c3d4e5f6-a7b8-9012-cdef-123456789012
              weight: 3
              priority: 1
      responses:
        "201":
          description: Contract linked
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Contract is already linked to this offer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/contracts/{contractId}:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: contractId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      operationId: updateOfferContractLink
      tags:
        - Offers
      summary: Update offer-contract link
      description: Update weight or priority on an offer-contract link.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                weight:
                  type: integer
                  minimum: 1
                priority:
                  type: integer
                  minimum: 0
            example:
              weight: 5
      responses:
        "200":
          description: Link updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: unlinkContractFromOffer
      tags:
        - Offers
      summary: Unlink contract from offer
      description: Removes the contract-offer association.
      responses:
        "200":
          description: Contract unlinked
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/dq-contracts:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listOfferDqContracts
      tags:
        - Offers
      summary: List DQ contracts for an offer
      description: Returns DQ contracts linked to this offer with geo/dedup settings.
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated DQ contract links
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: linkDqContractToOffer
      tags:
        - Offers
      summary: Link a DQ contract to an offer
      description: Associates a contract as a DQ contract for this offer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contractId
              properties:
                contractId:
                  type: string
                  format: uuid
                applyGeo:
                  type: boolean
                applyDedup:
                  type: boolean
            example:
              contractId: c3d4e5f6-a7b8-9012-cdef-123456789012
              applyGeo: true
              applyDedup: false
      responses:
        "201":
          description: DQ contract linked
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: DQ contract link already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/dq-contracts/{contractId}:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: contractId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      operationId: updateOfferDqContractLink
      tags:
        - Offers
      summary: Update DQ contract settings
      description: Update geo/dedup settings on a DQ contract link.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                applyGeo:
                  type: boolean
                applyDedup:
                  type: boolean
            example:
              applyGeo: false
      responses:
        "200":
          description: DQ contract link updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: unlinkDqContractFromOffer
      tags:
        - Offers
      summary: Unlink DQ contract from offer
      description: Removes the DQ contract-offer association.
      responses:
        "200":
          description: DQ contract unlinked
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offers/{offerId}/cascade-status:
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getOfferCascadeStatus
      tags:
        - Offers
      summary: Get cascade-disabled campaigns
      description: Returns campaigns that were auto-disabled when this offer was paused.
      responses:
        "200":
          description: List of auto-disabled campaigns
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    post:
      operationId: cascadeOfferStatus
      tags:
        - Offers
      summary: Cascade offer status to campaigns
      description: Disable or restore campaigns linked to this offer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - disable
                    - restore
                campaignIds:
                  type: array
                  items:
                    type: string
                    format: uuid
            example:
              action: disable
      responses:
        "200":
          description: Cascade applied
      security:
        - sessionCookie: []
  /api/v1/leads:
    get:
      operationId: listLeads
      tags:
        - Leads
      summary: List leads
      description: Returns a paginated list of leads with extensive filter options.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - pending
              - matched
              - sold
              - returned
              - rejected
              - scrubbed
              - trashed
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: spawnedFromLeadId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: search
          in: query
          schema:
            type: string
        - name: isTrashed
          in: query
          schema:
            type: boolean
        - name: isTest
          in: query
          schema:
            type: boolean
        - name: isPaid
          in: query
          schema:
            type: boolean
        - name: qualityScoreBand
          in: query
          description: Lead-scoring band filter (e.g. call_immediately, standard, nurture, deprioritize).
          schema:
            type: string
        - name: minQualityScore
          in: query
          description: Keep only leads whose qualityScore is at or above this floor (1-10).
          schema:
            type: integer
            minimum: 1
            maximum: 10
        - name: scoreDegraded
          in: query
          description: When true, keep only leads whose score breakdown carries flags (degraded score).
          schema:
            type: boolean
        - name: pagination
          in: query
          description: Set to "cursor" for keyset (cursor) pagination instead of offset pagination.
          schema:
            type: string
            enum:
              - cursor
        - name: cursor
          in: query
          description: Opaque cursor token from a prior response's nextCursor/prevCursor (cursor pagination only).
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        "200":
          description: Paginated lead list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
    post:
      operationId: createLead
      tags:
        - Leads
      summary: Create lead (admin)
      description: Directly create a lead and route it through the engine. For partner submissions use /api/v1/leads/submit.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - verticalId
                - firstName
                - lastName
                - email
                - phone
                - tcpaConsent
              properties:
                verticalId:
                  type: string
                  format: uuid
                campaignId:
                  type: string
                  format: uuid
                offerId:
                  type: string
                  format: uuid
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                state:
                  type: string
                  maxLength: 2
                zipCode:
                  type: string
                tcpaConsent:
                  type: boolean
                custom:
                  type: object
                isTest:
                  type: boolean
                trustedFormCertUrl:
                  type: string
                  format: uri
                  nullable: true
                jornayaLeadId:
                  type: string
                  nullable: true
                  description: Jornaya LeadiD token (36-char UUID). When the tenant's Jornaya integration is enabled with auto-audit,
                    submitting one triggers a billed LeadiD audit query.
            example:
              verticalId: d4e5f6a7-b8c9-0123-defa-234567890123
              firstName: Jane
              lastName: Smith
              email: jane.smith@example.com
              phone: "+15559876543"
              state: CA
              zipCode: "90210"
              tcpaConsent: true
              custom:
                policyType: term
      responses:
        "201":
          description: Lead created and routed
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
  /api/v1/leads/submit:
    post:
      operationId: submitLead
      tags:
        - Leads
      summary: Submit lead (partner)
      description: Partner lead submission endpoint authenticated via partner API key (Bearer lr_xxx).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  type: string
                  description: Campaign ID (displayId or UUID)
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                state:
                  type: string
                  maxLength: 2
                zipCode:
                  type: string
                tcpaConsent:
                  type: boolean
                custom:
                  type: object
                trustedFormCertUrl:
                  type: string
                  format: uri
                  nullable: true
                jornayaLeadId:
                  type: string
                  nullable: true
                  description: Jornaya LeadiD token (36-char UUID). When the tenant's Jornaya integration is enabled with auto-audit,
                    submitting one triggers a billed LeadiD audit query.
                consentCertUrl:
                  type: string
                  format: uri
                  nullable: true
                pingId:
                  type: string
                  format: uuid
                  description: Optional ping ID from a prior /leads/ping response — skips routing and uses cached matches
            example:
              campaignId: C-1001
              firstName: John
              lastName: Doe
              email: john.doe@example.com
              phone: "+15551112222"
              state: NY
              zipCode: "10001"
              tcpaConsent: true
      responses:
        "200":
          description: Lead accepted and routed
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: string
                    enum:
                      - success
                      - failed
                      - accepted
                      - rejected
                      - duplicate
                      - error
                  leadId:
                    type: string
                    format: uuid
                    nullable: true
                  price:
                    type: number
                    nullable: true
                  msg:
                    type: string
                    nullable: true
                  buyerName:
                    type: string
                    nullable: true
                    description: Present only on sold responses when the campaign enables buyer name in partner responses.
                  buyerPhone:
                    type: string
                    pattern: ^\+[1-9]\d{1,14}$
                    nullable: true
                    description: Present only on sold responses when the campaign enables buyer phone in partner responses.
                  buyerId:
                    type: string
                    nullable: true
                    description: Partner-facing buyer ID. Present only on sold responses when enabled.
                  redirectUrl:
                    type: string
                    nullable: true
                    description: Rendered buyer redirect URL. Present only on sold responses when enabled.
                  redirectType:
                    type: string
                    enum:
                      - calendar
                      - redirect
                    description: "'calendar' when redirectUrl is a recognized scheduling link (Calendly, Cal.com, Google appointments),
                      otherwise 'redirect'. Present only on sold responses with a non-null redirect URL."
                  calendarProvider:
                    type: string
                    enum:
                      - calendly
                      - cal.com
                      - google
                    description: Scheduling provider detected from redirectUrl. Present only when redirectType is 'calendar'.
                  calendarEmbedUrl:
                    type: string
                    description: Iframe-embeddable booking URL so landers can render the buyer's calendar instead of redirecting. Present
                      only when redirectType is 'calendar'.
                  buyerMediaUrl:
                    type: string
                    nullable: true
                    description: Public URL of the winning buyer's photo or video. Present only on sold responses when the campaign enables
                      buyer media in partner responses.
                  buyerMediaType:
                    type: string
                    enum:
                      - photo
                      - video
                    nullable: true
                    description: Whether buyerMediaUrl is a still image or a video. Present only on sold responses when enabled.
                  dqDistributed:
                    type: boolean
                    description: Whether the lead was distributed to DQ contracts
                  dqSaleCount:
                    type: integer
                    description: Number of DQ contract sales
                  dqRevenue:
                    type: string
                    description: Total DQ revenue
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          description: Invalid or missing partner API key
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - apiKey: []
        - partnerApiKey: []
  /api/v1/leads/ping:
    post:
      operationId: pingLead
      tags:
        - Leads
      summary: Ping (check matches)
      description: "Checks which contracts match a lead without committing. Returns match list with prices. Requires Bearer
        API key auth (same as /submit). The offer linked to the campaign must have pingEnabled: true. Offer-level
        pingFieldSettings may enforce additional required fields."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  oneOf:
                    - type: string
                      format: uuid
                    - type: integer
                  description: Campaign UUID or integer displayId
                verticalId:
                  type: string
                  format: uuid
                  description: Optional — resolved from offer if omitted
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                state:
                  type: string
                  maxLength: 2
                zipCode:
                  type: string
                  maxLength: 10
                custom:
                  type: object
                  description: Vertical-specific custom fields
                expiryMinutes:
                  type: integer
                  minimum: 1
                  maximum: 60
                  default: 5
                  description: Minutes until ping expires (default 5)
            example:
              campaignId: C-1001
              state: CA
              zipCode: "90210"
      responses:
        "200":
          description: Matching contracts with prices
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      pingId:
                        type: string
                        format: uuid
                      expiresAt:
                        type: string
                        format: date-time
                      matchCount:
                        type: integer
                      matches:
                        type: array
                        items:
                          type: object
                          properties:
                            price:
                              type: string
                              description: Partner-facing price as decimal string (e.g. "12.50")
                            rank:
                              type: integer
              example:
                data:
                  pingId: a1b2c3d4-0000-1111-2222-333344445555
                  expiresAt: 2026-04-07T12:05:00.000Z
                  matchCount: 2
                  matches:
                    - price: "12.50"
                      rank: 1
                    - price: "10.00"
                      rank: 2
        "400":
          description: Validation error — offer not ping-enabled, missing required ping fields, or invalid body
        "401":
          description: Invalid or missing API key
        "403":
          description: API key lacks posting scope
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Request body exceeds the maximum allowed size
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Campaign temporarily paused — insufficient platform credits
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - apiKey: []
  /api/v1/leads/post:
    post:
      operationId: postLead
      tags:
        - Leads
      summary: Post lead (after ping)
      description: Converts a ping into a committed lead sale. Requires a valid pingId from a prior ping response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - pingId
                - email
                - tcpaConsent
              properties:
                pingId:
                  type: string
                  format: uuid
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                state:
                  type: string
                  maxLength: 2
                zipCode:
                  type: string
                tcpaConsent:
                  type: boolean
                custom:
                  type: object
                acceptedBuyerIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                trustedFormCertUrl:
                  type: string
                  format: uri
                  nullable: true
                jornayaLeadId:
                  type: string
                  nullable: true
                  description: Jornaya LeadiD token (36-char UUID). When the tenant's Jornaya integration is enabled with auto-audit,
                    submitting one triggers a billed LeadiD audit query.
            example:
              pingId: a1b2c3d4-0000-1111-2222-333344445555
              firstName: John
              lastName: Doe
              email: john.doe@example.com
              phone: "+15551112222"
              tcpaConsent: true
      responses:
        "200":
          description: Lead posted and delivered
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      leadId:
                        type: string
                        nullable: true
                      pingId:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - sold
                          - rejected
                      payout:
                        type: string
                        description: Partner payout as decimal string.
                      matchCount:
                        type: integer
                      buyerName:
                        type: string
                        nullable: true
                        description: Present only on sold responses when the campaign enables buyer name in partner responses.
                      buyerPhone:
                        type: string
                        pattern: ^\+[1-9]\d{1,14}$
                        nullable: true
                        description: Present only on sold responses when the campaign enables buyer phone in partner responses.
                      buyerId:
                        type: string
                        nullable: true
                        description: Partner-facing buyer ID. Present only on sold responses when enabled.
                      redirectUrl:
                        type: string
                        nullable: true
                        description: Rendered buyer redirect URL. Present only on sold responses when enabled.
                      redirectType:
                        type: string
                        enum:
                          - calendar
                          - redirect
                        description: "'calendar' when redirectUrl is a recognized scheduling link (Calendly, Cal.com, Google appointments),
                          otherwise 'redirect'. Present only on sold responses with a non-null redirect URL."
                      calendarProvider:
                        type: string
                        enum:
                          - calendly
                          - cal.com
                          - google
                        description: Scheduling provider detected from redirectUrl. Present only when redirectType is 'calendar'.
                      calendarEmbedUrl:
                        type: string
                        description: Iframe-embeddable booking URL so landers can render the buyer's calendar instead of redirecting. Present
                          only when redirectType is 'calendar'.
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Ping already converted or in a non-active state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: Ping has expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/leads/bulk:
    post:
      operationId: bulkLeadAction
      tags:
        - Leads
      summary: Bulk lead action
      description: >
        Perform bulk actions on multiple leads — scrub, trash, toggle test/paid, cherry-pick, or adjust payout.
        Operation is dispatched by request body shape: `{action}` for scrub/trash/toggle, `{contractId}` for
        cherry-pick, `{operation}` for bulk_adjust_*.


        Cherry-pick branch (`{leadIds, contractId, ...}`) accepts the same skip flags as the single-lead `/cherry-pick`
        endpoint: `skipFilters`, `skipCaps`, `skipDedup`, `skipBalanceCheck`, `skipDelivery`, `skipPostbacks`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
            example:
              leadIds:
                - a1b2c3d4-0000-0000-0000-000000000001
                - a1b2c3d4-0000-0000-0000-000000000002
              action: scrub
      responses:
        "200":
          description: Bulk results per lead
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}:
    parameters:
      - name: leadId
        in: path
        required: true
        description: "Lead UUID, or the lead's `shortId` (the 8-character nanoid on the lead record). NOTE: the numeric
          `displayId` is NOT accepted here — a value that is not a well-formed UUID is matched against `shortId` only,
          so a numeric id returns 404. (Clarified after the 2026-07-27 security review, which read the previous \"UUID
          or short ID\" wording as covering displayId and recorded the 404s as a spec/behaviour mismatch.)"
        schema:
          type: string
    get:
      operationId: getLead
      tags:
        - Leads
      summary: Get lead
      description: Returns a lead by UUID or by `shortId` (not `displayId` — see the parameter description). Response includes
        DQ fields when applicable.
      responses:
        "200":
          description: >
            Lead detail. Includes isDqLead, dqRevenue, dqPayout, dqCpaRevenue, dqCpaPayout when the lead has DQ contract
            sales.
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateLead
      tags:
        - Leads
      summary: Update lead
      description: Partially update a lead. All fields optional.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                verticalId:
                  type: string
                  format: uuid
                partnerId:
                  type: string
                  format: uuid
                campaignId:
                  type: string
                  format: uuid
                offerId:
                  type: string
                  format: uuid
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                dateOfBirth:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                tcpaConsent:
                  type: boolean
                consentTimestamp:
                  type: string
                  format: date-time
                consentIp:
                  type: string
                consentText:
                  type: string
                custom:
                  type: object
                  additionalProperties: true
                sub1:
                  type: string
                  nullable: true
                sub2:
                  type: string
                  nullable: true
                sub3:
                  type: string
                  nullable: true
                sub4:
                  type: string
                  nullable: true
                sub5:
                  type: string
                  nullable: true
                sub6:
                  type: string
                  nullable: true
                sub7:
                  type: string
                  nullable: true
                sub8:
                  type: string
                  nullable: true
                sub9:
                  type: string
                  nullable: true
                sub10:
                  type: string
                  nullable: true
                requestId:
                  type: string
                  nullable: true
                isTest:
                  type: boolean
                trustedFormCertUrl:
                  type: string
                  format: uri
                  nullable: true
                jornayaLeadId:
                  type: string
                  nullable: true
                  description: Jornaya LeadiD token (36-char UUID). When the tenant's Jornaya integration is enabled with auto-audit,
                    submitting one triggers a billed LeadiD audit query.
                consentCertUrl:
                  type: string
                  nullable: true
                gclid:
                  type: string
                  nullable: true
                gbraid:
                  type: string
                  nullable: true
                visitorId:
                  type: string
                  nullable: true
                fbclid:
                  type: string
                  nullable: true
                fbc:
                  type: string
                  nullable: true
                fbp:
                  type: string
                  nullable: true
                intakeFiltered:
                  type: boolean
                intakeFilterReasonInternal:
                  type: string
                  nullable: true
                intakePayoutSuppressed:
                  type: boolean
            example:
              phone: "+15559999999"
      responses:
        "200":
          description: Lead updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteLead
      tags:
        - Leads
      summary: Reject lead
      description: Soft-deletes a lead by transitioning it to the rejected status. Returns 400 if the lead's current status
        cannot transition to rejected.
      responses:
        "200":
          description: Lead rejected
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/cherry-pick:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: cherryPickLead
      tags:
        - Leads
      summary: Cherry-pick lead to contract
      description: >
        Manually sell a lead to a specific contract, optionally overriding price and skipping
        filters/caps/dedup/balance/delivery/postbacks.


        - `skipDelivery: true` bypasses the outbound HTTP post to the buyer's delivery endpoint. The `leadSale` (with a
        real `saleId`), `leadDistribution`, balance deduction, cap increment, partner accrual, dedup record, and
        `lead.status → sold` transition still run — so postbacks can attribute conversions back to the saleId on a
        manually-handed-off lead.

        - `skipPostbacks: true` suppresses the `lead.created` and sale webhooks for this cherry pick. Use when re-keying
        a sale that was already notified out-of-band.

        - `buyerLeadId` (only valid when `skipDelivery: true`) writes the buyer's external ID onto
        `leadSale.buyerLeadId` so a postback arriving as `?saleId=<thisValue>` matches the right sale. Use when
        reconciling a lead the buyer already has (e.g. moved manually at the intake center). Rejected with `400
        VALIDATION_ERROR` if any non-`returned` sale already exists for this `(buyerId, buyerLeadId)` — postbacks would
        be ambiguous.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contractId
              properties:
                contractId:
                  type: string
                  format: uuid
                price:
                  type: number
                  minimum: 0
                skipFilters:
                  type: boolean
                skipCaps:
                  type: boolean
                skipDedup:
                  type: boolean
                skipBalanceCheck:
                  type: boolean
                skipDelivery:
                  type: boolean
                  description: Skip the outbound HTTP post to the buyer's delivery endpoint. Sale + distribution still created so
                    postbacks can attribute to saleId.
                skipPostbacks:
                  type: boolean
                  description: Suppress the lead.created and sale webhooks for this cherry pick.
                buyerLeadId:
                  type: string
                  minLength: 1
                  maxLength: 255
                  description: Buyer's external ID for a lead they already have (e.g. moved at the intake center). Stored on
                    leadSale.buyerLeadId for postback attribution. Only allowed when skipDelivery is true. Rejected if
                    duplicate per (buyerId, buyerLeadId).
            example:
              contractId: c3d4e5f6-a7b8-9012-cdef-123456789012
              price: 35
              skipFilters: true
              skipDelivery: true
              skipPostbacks: false
              buyerLeadId: EXT-12345
      responses:
        "200":
          description: Cherry-pick result with delivery status
        "400":
          description: Validation error (e.g. duplicate buyerLeadId for this buyer, or buyerLeadId set without skipDelivery)
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Delivery or SKU reservation failed after the sale was created (leadSale marked failed)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/adjust-payout:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: adjustLeadPayout
      tags:
        - Leads
      summary: Adjust lead payout
      description: Retroactively change the partner payout (leadSale.cost) on a delivered sale. Updates partner balance,
        writes adjustment transaction, fires lead.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - saleId
                - newPrice
                - expectedCurrentAmount
                - reason
              properties:
                saleId:
                  type: string
                  format: uuid
                newPrice:
                  type: number
                  minimum: 0
                expectedCurrentAmount:
                  type: number
                  minimum: 0
                  description: Current cost expected to be on the sale — used for optimistic lock. 409 if mismatch.
                reason:
                  type: string
                  minLength: 1
                allowRefunded:
                  type: boolean
                  default: false
            example:
              saleId: 00000000-0000-0000-0000-000000000000
              newPrice: 30
              expectedCurrentAmount: 25
              reason: Partner make-good on bad leads
      responses:
        "200":
          description: Payout adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch (another admin adjusted first)
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/adjust-revenue:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: adjustLeadRevenue
      tags:
        - Leads
      summary: Adjust lead revenue
      description: Retroactively change the buyer charge (leadSale.price) on a delivered sale. Updates buyer balance, writes
        adjustment transaction, fires lead.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - saleId
                - newPrice
                - reason
                - expectedCurrentAmount
              properties:
                saleId:
                  type: string
                  format: uuid
                newPrice:
                  type: number
                  minimum: 0
                expectedCurrentAmount:
                  type: number
                  minimum: 0
                  description: Current price expected to be on the sale — used for optimistic lock. 409 if mismatch.
                reason:
                  type: string
                  minLength: 1
                allowRefunded:
                  type: boolean
                  default: false
            example:
              saleId: 00000000-0000-0000-0000-000000000000
              newPrice: 35
              expectedCurrentAmount: 40
              reason: Buyer negotiated lower rate post-delivery
      responses:
        "200":
          description: Revenue adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch (another admin adjusted first)
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/adjust-cpa-revenue:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: adjustLeadCpaRevenue
      tags:
        - Leads
      summary: Adjust lead CPA revenue (buyer charge)
      description: Retroactively change the CPA conversion amount (buyer charge) on a converted sale. Updates buyer balance,
        writes adjustment transaction, fires lead.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - saleId
                - newCpaAmount
                - reason
                - expectedCurrentAmount
              properties:
                saleId:
                  type: string
                  format: uuid
                newCpaAmount:
                  type: number
                  minimum: 0
                expectedCurrentAmount:
                  type: number
                  minimum: 0
                  description: Current cpaAmount expected to be on the sale — used for optimistic lock. 409 if mismatch.
                reason:
                  type: string
                  minLength: 1
                allowRefunded:
                  type: boolean
                  default: false
            example:
              saleId: 00000000-0000-0000-0000-000000000000
              newCpaAmount: 50
              expectedCurrentAmount: 40
              reason: CPA revenue renegotiation
      responses:
        "200":
          description: CPA revenue adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/adjust-cpa-payout:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: adjustLeadCpaPayout
      tags:
        - Leads
      summary: Adjust lead CPA payout (partner earn)
      description: Retroactively change the CPA payout (partner earn) on a converted sale. Updates partner balance, writes
        adjustment transaction, fires lead.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - saleId
                - newCpaPayout
                - reason
                - expectedCurrentAmount
              properties:
                saleId:
                  type: string
                  format: uuid
                newCpaPayout:
                  type: number
                  minimum: 0
                expectedCurrentAmount:
                  type: number
                  minimum: 0
                  description: Current cpaPayout expected to be on the sale — used for optimistic lock. 409 if mismatch.
                reason:
                  type: string
                  minLength: 1
                allowRefunded:
                  type: boolean
                  default: false
            example:
              saleId: 00000000-0000-0000-0000-000000000000
              newCpaPayout: 25
              expectedCurrentAmount: 20
              reason: CPA payout renegotiation
      responses:
        "200":
          description: CPA payout adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/calls/rtb/ping:
    post:
      operationId: pingCallRtb
      tags:
        - Calls
      summary: Call RTB ping (availability / bid)
      description: 'Sell-side RTB ping a publisher sends before transferring a live caller. One route, two modes —
        `availability` answers whether an eligible buyer exists right now; `rtb` also quotes the campaign payout with
        conversion terms and an expiry. Requires a Bearer API key with the explicit `calls_rtb` scope. Every BUSINESS
        outcome (accept and reject alike) returns HTTP 200 with a stable `LR-1xx` reasonCode; 4xx is reserved for
        malformed requests and auth failures, and 429 (with Retry-After) for quota. Unknown request fields are accepted
        as free-form publisher tags. Accepted as JSON, as a form-urlencoded body, or as a GET with a query string (see
        the GET operation) — identical semantics in all three. Vendor field spellings are folded onto these names before
        validation (caller_number / CID / caller_id → callerNumber, caller_state → state, caller_zip / zipcode → zip,
        campaign_id → campaignId), a Retreaver `{"rtb": {...}}` envelope is unwrapped, and a field you send explicitly
        always wins over an alias. `mode=availability` performs zero durable writes; `mode=rtb` may mint one
        callRtbReservation freezing the quoted payout. Either way the path is excluded from api_request metering.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - campaignId
              additionalProperties: true
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                mode:
                  type: string
                  enum:
                    - availability
                    - rtb
                  default: availability
                  description: Closed enum — omitted means availability (pre-price-rail partners keep working)
                campaignId:
                  type: string
                  description: Campaign UUID or the campaign's numeric displayId (the id shown in the admin UI), matching
                    /api/v1/leads/ping. Anything that is neither returns 404 — never a 5xx, and never distinguishable
                    from a campaign belonging to another tenant or partner.
                callerNumber:
                  type: string
                  description: Caller ANI (E.164). Missing/anonymous rejects with LR-108
                state:
                  type: string
                  maxLength: 2
                zip:
                  type: string
                  maxLength: 10
              additionalProperties: true
            example:
              mode: rtb
              campaignId: 3b1221a1-2c9f-4742-bd12-db51d9f6d289
              callerNumber: "+16195551234"
              state: CA
              zip: "92101"
      responses:
        "200":
          description: Business outcome — accept or structured reject. The X-LR-RTB-Available response header mirrors `available`
            on every 200.
          content:
            application/json:
              schema: &a1
                type: object
                properties:
                  available:
                    type: boolean
                  payout:
                    type: string
                    nullable: true
                    description: String decimal. Null when not quoted; "0.00" ONLY with LR-111 duplicate_window
                  payoutBasis:
                    type: string
                    nullable: true
                  payoutUnavailable:
                    type: string
                    nullable: true
                    description: Set when available with an unquotable payout type (revshare/raw_call)
                  currency:
                    type: string
                    description: Tenant currency, lowercase (usd, gbp, eur, cad)
                  terms:
                    type: object
                    nullable: true
                    description: Conversion terms the quote is earned under (rtb accepts only)
                  expiresInSec:
                    type: integer
                    nullable: true
                  expiresAt:
                    type: string
                    format: date-time
                    nullable: true
                  reservationId:
                    type: string
                    description: Present on an `rtb` accept that RESERVED the quoted payout. The reservation freezes `payout` + `terms`
                      until `expiresAt`; the call that follows settles at that frozen price. Re-pinging for the SAME
                      caller while it is live returns this same id — never a second reservation at a second price.
                      Absent in `availability` mode, on rejects, and when the payout type is unquotable
                      (revshare/raw_call).
                  sipAddress:
                    type: string
                    description: Dialable per-reservation SIP address (`sip:rtb-<id>@<domain>`). Delivering the call here associates it with
                      the reservation. Absent when the campaign's partner has no active SIP origination endpoint —
                      deliver to the campaign's tracking number instead and the reservation is matched on caller number.
                  reason:
                    type: string
                    nullable: true
                    description: Machine token (no_eligible_buyer, rtb_disabled, ...)
                  reasonCode:
                    type: string
                    nullable: true
                    description: Stable LR-1xx code
                  requestId:
                    type: string
                  bidAmount:
                    type: number
                    description: VENDOR ALIAS of `payout`, as a JSON number (Ringba's Dynamic Bid stage). Additive — `payout` remains the
                      canonical string decimal. Present only when a payout was quoted; 0 on the LR-111 duplicate_window
                      accept.
                  retreaver_payout:
                    type: number
                    description: VENDOR ALIAS of `payout` as a number (Retreaver's stock RTB map).
                  retreaver_seconds:
                    type: integer
                    description: VENDOR ALIAS of `terms.minDurationSec` (Retreaver's conversion timer tag).
                  expireInSeconds:
                    type: integer
                    description: VENDOR ALIAS of `expiresInSec` (Ringba's spelling).
                  expires_at:
                    type: string
                    format: date-time
                    description: VENDOR ALIAS of `expiresAt` (Retreaver's spelling).
                  phoneNumber:
                    type: string
                    description: The campaign's tracking DID in E.164 — the number to transfer the caller to. Present on accepts only, and
                      only when the campaign has a tracking number (absent for SIP-only campaigns). Never carries a SIP
                      address.
                  inbound_number:
                    type: string
                    description: VENDOR ALIAS of `phoneNumber` (Retreaver's stock RTB map).
                  sip_address:
                    type: string
                    description: VENDOR ALIAS of `sipAddress` (Retreaver's spelling).
                  bidId:
                    type: string
                    description: VENDOR ALIAS for Ringba's optional Bid ID stage — the `reservationId` when one was minted, otherwise the
                      `requestId`. Accepts only.
        "400":
          description: Malformed request (invalid JSON, missing campaignId, unknown mode)
        "401":
          description: Missing, invalid, or tenantless API key
        "402":
          description: Tenant has no active subscription (intake locked)
        "403":
          description: Key lacks the calls_rtb scope, or is read-only
        "404":
          description: Campaign not found in the key's tenant
        "413":
          description: Body exceeds the intake size cap
        "429":
          description: Ping quota exceeded — carries Retry-After
      security:
        - bearerAuth: []
    get:
      operationId: pingCallRtbGet
      tags:
        - Calls
      summary: Call RTB ping (query-string form)
      description: The same ping as POST, with the parameters in the query string — the shape Ringba's Ring Tree Target editor
        and Retreaver's standard webhooks default to. Identical auth, identical validation, identical business outcomes
        and response body; `mode=rtb` reserves exactly as the POST does. Unknown query parameters are free-form
        publisher tags, and the same vendor field spellings are accepted.
      parameters:
        - name: mode
          in: query
          schema:
            type: string
            enum:
              - availability
              - rtb
            default: availability
        - name: campaignId
          in: query
          required: true
          schema:
            type: string
          description: Campaign UUID or numeric displayId (also accepted as `campaign_id`).
        - name: callerNumber
          in: query
          schema:
            type: string
          description: Caller ANI (E.164). Also accepted as `caller_number`, `CID`, or `caller_id`.
        - name: state
          in: query
          schema:
            type: string
            maxLength: 2
          description: Also accepted as `caller_state`.
        - name: zip
          in: query
          schema:
            type: string
            maxLength: 10
          description: Also accepted as `caller_zip`, `zipcode`, or `zip_code`.
      responses:
        "200":
          description: Business outcome — accept or structured reject, byte-identical to the POST response. The X-LR-RTB-Available
            response header mirrors `available` on every 200.
          content:
            application/json:
              schema: *a1
        "400":
          description: Missing campaignId, or an unknown mode
        "401":
          description: Missing, invalid, or tenantless API key
        "402":
          description: Tenant has no active subscription (intake locked)
        "403":
          description: Key lacks the calls_rtb scope, or is read-only
        "404":
          description: Campaign not found in the key's tenant
        "429":
          description: Ping quota exceeded — carries Retry-After
      security:
        - bearerAuth: []
  /api/v1/calls/{callId}/adjust-revenue:
    parameters:
      - name: callId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: adjustCallRevenue
      tags:
        - Calls
      summary: Adjust call revenue
      description: Retroactively change the buyer charge (callSale.totalPrice) on a completed call sale. Updates buyer
        balance, writes adjustment transaction, fires call.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - callSaleId
                - newTotalPrice
                - reason
                - expectedCurrentTotalPrice
              properties:
                callSaleId:
                  type: string
                  format: uuid
                newTotalPrice:
                  type: number
                  minimum: 0
                expectedCurrentTotalPrice:
                  type: number
                  minimum: 0
                reason:
                  type: string
                  minLength: 1
                  maxLength: 500
                allowRefunded:
                  type: boolean
            example:
              callSaleId: 00000000-0000-0000-0000-000000000000
              newTotalPrice: 12.5
              expectedCurrentTotalPrice: 15
              reason: Twilio billing reconciliation
      responses:
        "200":
          description: Call revenue adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/calls/{callId}/adjust-payout:
    parameters:
      - name: callId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: adjustCallPayout
      tags:
        - Calls
      summary: Adjust call payout
      description: Retroactively change the partner payout (callSale.payout) on a completed call sale. Updates partner
        balance, writes adjustment transaction, fires call.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - callSaleId
                - newPayout
                - reason
                - expectedCurrentPayout
              properties:
                callSaleId:
                  type: string
                  format: uuid
                newPayout:
                  type: number
                  minimum: 0
                expectedCurrentPayout:
                  type: number
                  minimum: 0
                reason:
                  type: string
                  minLength: 1
                  maxLength: 500
                allowRefunded:
                  type: boolean
            example:
              callSaleId: 00000000-0000-0000-0000-000000000000
              newPayout: 8
              expectedCurrentPayout: 10
              reason: Partner make-good agreed
      responses:
        "200":
          description: Call payout adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/calls/{callId}/adjust-cpa-revenue:
    parameters:
      - name: callId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: adjustCallCpaRevenue
      tags:
        - Calls
      summary: Adjust call CPA revenue (buyer charge)
      description: Retroactively change the CPA conversion amount (buyer charge) on a converted call sale. Updates buyer
        balance, writes adjustment transaction, fires call.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - callSaleId
                - newCpaAmount
                - reason
                - expectedCurrentAmount
              properties:
                callSaleId:
                  type: string
                  format: uuid
                newCpaAmount:
                  type: number
                  minimum: 0
                expectedCurrentAmount:
                  type: number
                  minimum: 0
                reason:
                  type: string
                  minLength: 1
                allowRefunded:
                  type: boolean
                  default: false
            example:
              callSaleId: 00000000-0000-0000-0000-000000000000
              newCpaAmount: 75
              expectedCurrentAmount: 50
              reason: Call CPA revenue correction
      responses:
        "200":
          description: Call CPA revenue adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/calls/{callId}/adjust-cpa-payout:
    parameters:
      - name: callId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: adjustCallCpaPayout
      tags:
        - Calls
      summary: Adjust call CPA payout (partner earn)
      description: Retroactively change the CPA payout (partner earn) on a converted call sale. Updates partner balance,
        writes adjustment transaction, fires call.sale_adjusted webhook. Requires finance feature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - callSaleId
                - newCpaPayout
                - reason
                - expectedCurrentAmount
              properties:
                callSaleId:
                  type: string
                  format: uuid
                newCpaPayout:
                  type: number
                  minimum: 0
                expectedCurrentAmount:
                  type: number
                  minimum: 0
                reason:
                  type: string
                  minLength: 1
                allowRefunded:
                  type: boolean
                  default: false
            example:
              callSaleId: 00000000-0000-0000-0000-000000000000
              newCpaPayout: 40
              expectedCurrentAmount: 25
              reason: Call CPA payout correction
      responses:
        "200":
          description: Call CPA payout adjusted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      delta:
                        type: number
                      buyerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerTransactionId:
                        type: string
                        format: uuid
                        nullable: true
                      creditStatus:
                        type: string
                        enum:
                          - normal
                          - overdraft
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — optimistic lock mismatch
        "500":
          description: Server error
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/distribute:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: distributeLead
      tags:
        - Leads
      summary: Re-distribute lead
      description: Run the routing engine again on an existing lead.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mode
              properties:
                mode:
                  type: string
                  enum:
                    - all
                    - exclude_seen
                    - include_specific
                    - exclude_specific
                contractIds:
                  type: array
                  items:
                    type: string
                    format: uuid
            example:
              mode: exclude_seen
      responses:
        "200":
          description: Distribution results
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/distributions:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: listLeadDistributions
      tags:
        - Leads
      summary: List lead distributions
      description: Returns the distribution waterfall for a lead. Each sale record includes isDqSale boolean.
      parameters:
        - name: decidedBy
          in: query
          description: Filter distributions by who decided them.
          schema:
            type: string
            enum:
              - system
              - buyer
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated distribution list plus ping-match auction rows
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      pingMatches:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/dispositions:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: listLeadDispositions
      tags:
        - Leads
      summary: List lead dispositions
      description: Returns disposition history for a lead.
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated disposition list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/notes:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: listLeadNotes
      tags:
        - Leads
      summary: List lead notes
      responses:
        "200":
          description: Note list
      security:
        - sessionCookie: []
    post:
      operationId: addLeadNote
      tags:
        - Leads
      summary: Add lead note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - content
              properties:
                content:
                  type: string
                  maxLength: 5000
            example:
              content: Buyer confirmed receipt via phone.
      responses:
        "200":
          description: Note added
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/return:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: returnLead
      tags:
        - Leads
      summary: Return lead
      description: Buyer returns a lead within the return window.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
                  enum:
                    - duplicate
                    - bad_data
                    - wrong_vertical
                    - out_of_area
                    - not_interested
                    - other
                notes:
                  type: string
            example:
              reason: duplicate
              notes: Already received this lead yesterday
      responses:
        "200":
          description: Lead returned
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/scrub:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: scrubLead
      tags:
        - Leads
      summary: Scrub lead
      description: Admin scrubs a lead, refunding all buyers.
      responses:
        "200":
          description: Lead scrubbed
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/crm-status:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: getLeadCrmStatuses
      tags:
        - Leads
      summary: Get CRM statuses
      responses:
        "200":
          description: CRM status list
      security:
        - sessionCookie: []
    put:
      operationId: updateLeadCrmStatus
      tags:
        - Leads
      summary: Update CRM status
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - buyerId
                - status
              properties:
                buyerId:
                  type: string
                  format: uuid
                status:
                  type: string
                substatus:
                  type: string
            example:
              buyerId: c3d4e5f6-a7b8-9012-cdef-123456789012
              status: contacted
      responses:
        "200":
          description: CRM status updated
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/leads/{leadId}/sales/{saleId}/void:
    parameters:
      - name: leadId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: saleId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: voidLeadSale
      tags:
        - Leads
      summary: Void a lead sale
      security:
        - sessionCookie: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                scrubPartnerPayout:
                  type: boolean
                  default: false
      responses:
        "200":
          description: Sale voided
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/people:
    get:
      operationId: listPeople
      tags:
        - People
      summary: List people
      description: Paginated people list with fuzzy search.
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: state
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - merged
              - deleted
        - name: tags
          in: query
          schema:
            type: string
        - name: filter
          in: query
          schema:
            type: string
            enum:
              - hasRelated
              - hasLeads
              - hasCalls
              - hasBoth
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - displayId
              - state
              - status
              - leadCount
              - callCount
              - lastActivityAt
              - createdAt
              - updatedAt
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated people list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    post:
      operationId: createPerson
      tags:
        - People
      summary: Create person
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                dateOfBirth:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  maxLength: 2
                  nullable: true
                zipCode:
                  type: string
                  maxLength: 10
                  nullable: true
                country:
                  type: string
                  nullable: true
                tags:
                  type: array
                  items:
                    type: string
                custom:
                  type: object
            example:
              firstName: Sarah
              lastName: Connor
              email: sarah@example.com
              tags:
                - vip
      responses:
        "201":
          description: Person created
      security:
        - sessionCookie: []
  /api/v1/people/by-vertical:
    get:
      operationId: listPeopleByVertical
      tags:
        - People
      summary: List people by vertical
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
      responses:
        "200":
          description: People grouped by vertical
      security:
        - sessionCookie: []
  /api/v1/people/{personId}:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getPerson
      tags:
        - People
      summary: Get person
      responses:
        "200":
          description: Person detail with emails and phones
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updatePerson
      tags:
        - People
      summary: Update person
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  format: email
                  nullable: true
                phone:
                  type: string
                  nullable: true
                dateOfBirth:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
                tags:
                  type: array
                  items:
                    type: string
                custom:
                  type: object
                  additionalProperties: true
                status:
                  type: string
                  enum:
                    - active
                    - merged
                    - deleted
            example:
              tags:
                - vip
      responses:
        "200":
          description: Person updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deletePerson
      tags:
        - People
      summary: Delete person
      description: Soft-deletes a person by transitioning their status to deleted.
      responses:
        "200":
          description: Person deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/people/{personId}/leads:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listPersonLeads
      tags:
        - People
      summary: List person's leads
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated lead list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/people/{personId}/calls:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listPersonCalls
      tags:
        - People
      summary: List person's calls
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated call list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/people/{personId}/sales:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listPersonSales
      tags:
        - People
      summary: List person's sales
      responses:
        "200":
          description: Combined lead and call sales. Lead sales include isDqSale boolean field.
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/people/{personId}/timeline:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getPersonTimeline
      tags:
        - People
      summary: Person timeline
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Timeline events
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/people/{personId}/notes:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listPersonNotes
      tags:
        - People
      summary: List person notes
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated note list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    post:
      operationId: addPersonNote
      tags:
        - People
      summary: Add person note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - content
              properties:
                content:
                  type: string
                  maxLength: 10000
            example:
              content: Called back, confirmed interest.
      responses:
        "201":
          description: Note added
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/people/{personId}/related:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getRelatedPeople
      tags:
        - People
      summary: Get related people
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Paginated related people
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/people/{personId}/merge:
    parameters:
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getPersonMergeSuggestions
      tags:
        - People
      summary: Get merge suggestions
      responses:
        "200":
          description: Merge suggestions
      security:
        - sessionCookie: []
    post:
      operationId: mergePerson
      tags:
        - People
      summary: Merge person
      description: Merge another person into this one (survivor).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mergePersonId
              properties:
                mergePersonId:
                  type: string
                  format: uuid
                reason:
                  type: string
            example:
              mergePersonId: a1b2c3d4-5678-9012-abcd-ef0123456789
              reason: Duplicate contact
      responses:
        "200":
          description: Merge completed
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/calls:
    get:
      operationId: listCalls
      tags:
        - Calls
      summary: List calls
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - ringing
              - ivr
              - routing
              - holding
              - connected
              - completed
              - missed
              - failed
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: callerNumber
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated call list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/v1/calls/{callId}:
    parameters:
      - name: callId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getCall
      tags:
        - Calls
      summary: Get call detail
      description: Returns a call with sales, events, tracking number, and linked lead.
      responses:
        "200":
          description: Call detail
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/calls/webhooks/inbound:
    post:
      operationId: callWebhookInbound
      tags:
        - "Internal: Call Webhooks"
      summary: Inbound call webhook
      description: |
        Primary inbound webhook for telephony providers (Twilio, Telnyx, Bandwidth).
        Receives all call lifecycle events and dispatches by event type: call.initiated,
        dtmf.gathered, bridge.answered, bridge.failed, recording.ready, call.completed.
        Validated via provider webhook signature.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific webhook payload (Twilio, Telnyx, or Bandwidth format)
      responses:
        "200":
          description: Webhook processed — returns provider-specific TwiML/XML or empty JSON
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: object
      security: []
  /api/v1/calls/webhooks/bridge:
    post:
      operationId: callWebhookBridge
      tags:
        - "Internal: Call Webhooks"
      summary: Bridge result webhook
      description: |
        Receives bridge (call transfer) result events from the telephony provider.
        Reports whether the bridged leg was answered, busy, no-answer, or failed.
        Returns call-flow commands (TwiML/Telnyx commands) for next action.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific bridge event payload
      responses:
        "200":
          description: Bridge result processed — returns provider-specific commands
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: object
      security: []
  /api/v1/calls/webhooks/dtmf:
    post:
      operationId: callWebhookDtmf
      tags:
        - "Internal: Call Webhooks"
      summary: DTMF input webhook
      description: |
        Receives DTMF (keypress) gather events from the telephony provider.
        Used for IVR menu input during call routing. Returns call-flow commands
        for the next action based on digits pressed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific DTMF gather payload
      responses:
        "200":
          description: DTMF processed — returns provider-specific commands
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: object
      security: []
  /api/v1/calls/webhooks/whisper:
    post:
      operationId: callWebhookWhisper
      tags:
        - "Internal: Call Webhooks"
      summary: Whisper response webhook
      description: |
        Receives buyer-side whisper DTMF response. Buyer presses 1 to accept the call
        or any other digit to reject. Returns call-flow commands to bridge or reject.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific DTMF gather payload with whisper context
      responses:
        "200":
          description: Whisper response processed — returns provider-specific commands
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: object
      security: []
  /api/v1/calls/webhooks/recording:
    post:
      operationId: callWebhookRecording
      tags:
        - "Internal: Call Webhooks"
      summary: Recording ready webhook
      description: |
        Receives recording-ready events from the telephony provider.
        Stores recording URL and duration on the call record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific recording event payload
      responses:
        "200":
          description: Recording processed
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: object
      security: []
  /api/v1/calls/webhooks/status:
    post:
      operationId: callWebhookStatus
      tags:
        - "Internal: Call Webhooks"
      summary: Call status webhook
      description: |
        Receives call status change events from the telephony provider.
        Handles call completion — updates call duration and final status.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific call status payload
      responses:
        "200":
          description: Status processed
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: object
      security: []
  /api/v1/ivr-configs:
    get:
      operationId: listIvrConfigs
      tags:
        - IVR
      summary: List IVR configs
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
        - name: search
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - name
              - status
              - createdAt
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated IVR config list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
        - apiKey: []
    post:
      operationId: createIvrConfig
      tags:
        - IVR
      summary: Create IVR config
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                greeting:
                  type: string
                  nullable: true
                greetingType:
                  type: string
                  enum:
                    - tts
                    - audio
                  default: tts
                menuOptions:
                  type: array
                  items:
                    type: object
                    required:
                      - digit
                      - label
                      - action
                    properties:
                      digit:
                        type: string
                      label:
                        type: string
                      action:
                        type: string
                      actionTarget:
                        type: string
                timeoutSeconds:
                  type: integer
                  default: 5
                maxRetries:
                  type: integer
                  default: 3
                invalidInputMessage:
                  type: string
                  nullable: true
                timeoutMessage:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
            example:
              name: Main IVR Menu
              greeting: Press 1 for sales, 2 for support.
              greetingType: tts
      responses:
        "201":
          description: IVR config created
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/ivr-configs/{configId}:
    parameters:
      - name: configId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getIvrConfig
      tags:
        - IVR
      summary: Get IVR config
      responses:
        "200":
          description: IVR config detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
    patch:
      operationId: updateIvrConfig
      tags:
        - IVR
      summary: Update IVR config
      description: Partially update an IVR config. All fields optional.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                greeting:
                  type: string
                  nullable: true
                greetingType:
                  type: string
                  enum:
                    - tts
                    - audio
                menuOptions:
                  type: array
                  items:
                    type: object
                    required:
                      - digit
                      - label
                      - action
                    properties:
                      digit:
                        type: string
                      label:
                        type: string
                      action:
                        type: string
                      actionTarget:
                        type: string
                timeoutSeconds:
                  type: integer
                maxRetries:
                  type: integer
                invalidInputMessage:
                  type: string
                  nullable: true
                timeoutMessage:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
      responses:
        "200":
          description: IVR config updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
    delete:
      operationId: deleteIvrConfig
      tags:
        - IVR
      summary: Delete IVR config
      responses:
        "200":
          description: IVR config deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/email-templates:
    get:
      operationId: listEmailTemplates
      tags:
        - Email Templates
      summary: List email templates
      parameters:
        - name: category
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated template list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: createEmailTemplate
      tags:
        - Email Templates
      summary: Create email template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - category
                - name
                - slug
              properties:
                category:
                  type: string
                  enum:
                    - system
                    - lead
                name:
                  type: string
                slug:
                  type: string
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                fromEmail:
                  type: string
                  nullable: true
                subject:
                  type: string
                  nullable: true
                htmlEnabled:
                  type: boolean
                body:
                  type: string
                  nullable: true
                designJson:
                  type: array
                  nullable: true
                  items:
                    type: object
                  description: Optional block tree from TemplateBuilder. NULL means the template uses the raw HTML textarea fallback.
                description:
                  type: string
                  nullable: true
            example:
              category: system
              name: Lead Delivery Notification
              slug: lead-delivery-notification
              subject: "New Lead: {{firstName}} {{lastName}}"
              body: <p>New lead from {{partnerName}}.</p>
      responses:
        "201":
          description: Template created
      security:
        - sessionCookie: []
  /api/v1/email-templates/{templateId}:
    parameters:
      - name: templateId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getEmailTemplate
      tags:
        - Email Templates
      summary: Get email template
      responses:
        "200":
          description: Template detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateEmailTemplate
      tags:
        - Email Templates
      summary: Update email template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                category:
                  type: string
                  enum:
                    - system
                    - lead
                name:
                  type: string
                slug:
                  type: string
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                fromEmail:
                  type: string
                  nullable: true
                subject:
                  type: string
                  nullable: true
                htmlEnabled:
                  type: boolean
                body:
                  type: string
                  nullable: true
                designJson:
                  type: array
                  nullable: true
                  description: Block tree from TemplateBuilder. NULL clears the block editor and falls back to raw HTML.
                  items:
                    type: object
                    additionalProperties: true
                description:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Template updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteEmailTemplate
      tags:
        - Email Templates
      summary: Delete email template
      responses:
        "200":
          description: Template deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/email-templates/{templateId}/test:
    parameters:
      - name: templateId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: sendTestEmail
      tags:
        - Email Templates
      summary: Send test email
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
            example:
              email: test@acme-insurance.com
      responses:
        "200":
          description: Test email sent
      security:
        - sessionCookie: []
  /api/v1/transactions:
    get:
      operationId: listTransactions
      tags:
        - Transactions
      summary: List transactions
      parameters:
        - name: type
          in: query
          schema:
            type: string
            enum:
              - sale
              - refund
              - payment
              - adjustment
              - fee
              - cpa
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated transaction list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
  /api/v1/transactions/{transactionId}:
    parameters:
      - name: transactionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getTransaction
      tags:
        - Transactions
      summary: Get transaction
      responses:
        "200":
          description: Transaction detail
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/payouts:
    get:
      operationId: listPayouts
      tags:
        - Payouts
      summary: List payouts
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - pending
              - processing
              - paid
              - cancelled
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated payout list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: createPayout
      tags:
        - Payouts
      summary: Create payout
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerId
                - amount
                - periodStart
                - periodEnd
              properties:
                partnerId:
                  type: string
                  format: uuid
                amount:
                  type: string
                periodStart:
                  type: string
                  format: date-time
                periodEnd:
                  type: string
                  format: date-time
                paymentMethod:
                  type: string
                notes:
                  type: string
            example:
              partnerId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
              amount: "2500.00"
              periodStart: 2026-03-01 00:00:00+00:00
              periodEnd: 2026-03-15 00:00:00+00:00
      responses:
        "201":
          description: Payout created
      security:
        - sessionCookie: []
  /api/v1/payouts/{payoutId}:
    parameters:
      - name: payoutId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getPayout
      tags:
        - Payouts
      summary: Get payout
      responses:
        "200":
          description: Payout detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updatePayout
      tags:
        - Payouts
      summary: Update payout status
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - pending
                    - processing
                    - paid
                    - cancelled
                paymentReference:
                  type: string
            example:
              status: paid
              paymentReference: ACH-20260315-001
      responses:
        "200":
          description: Payout updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/refunds:
    get:
      operationId: listRefunds
      tags:
        - Refunds
      summary: List refunds
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - pending
              - approved
              - rejected
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated refund list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: requestRefund
      tags:
        - Refunds
      summary: Request refund
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - leadId
                - buyerId
                - amount
                - reason
              properties:
                leadId:
                  type: string
                  description: Lead UUID or the human-facing shortId shown in the leads list; resolved server-side.
                buyerId:
                  type: string
                  format: uuid
                saleId:
                  type: string
                  format: uuid
                  description: Optional. When omitted, the most recent non-returned sale for (lead, buyer) is used. When provided, it must
                    belong to the given lead and buyer.
                amount:
                  type: string
                reason:
                  type: string
                  enum:
                    - duplicate
                    - bad_data
                    - wrong_vertical
                    - out_of_area
                    - not_interested
                    - other
                notes:
                  type: string
                  nullable: true
            example:
              leadId: a1b2c3d4-0000-1111-2222-333344445555
              buyerId: c3d4e5f6-a7b8-9012-cdef-123456789012
              amount: "25.00"
              reason: bad_data
      responses:
        "201":
          description: Refund request created
        "404":
          description: Lead not found, or no matching sale for the lead and buyer
      security:
        - sessionCookie: []
  /api/v1/refunds/{refundId}:
    parameters:
      - name: refundId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getRefund
      tags:
        - Refunds
      summary: Get refund
      responses:
        "200":
          description: Refund detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: processRefund
      tags:
        - Refunds
      summary: Process refund
      description: Approve or reject a pending refund.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - approved
                    - rejected
                adminNotes:
                  type: string
                  nullable: true
                  maxLength: 2000
                  description: Note sent to the buyer with the decision.
            example:
              status: approved
      responses:
        "200":
          description: Refund processed
        "400":
          description: Refund already processed, invalid status transition, or invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/refunds/{refundId}/acknowledge:
    parameters:
      - name: refundId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: acknowledgeRefund
      tags:
        - Refunds
      summary: Acknowledge refund decision
      description: Marks a processed (approved/rejected) refund decision as seen by the requesting buyer. Buyers may only
        acknowledge their own refund; admins may acknowledge any refund in tenant. Idempotent — repeat calls return the
        existing row without changing acknowledgedAt.
      responses:
        "200":
          description: Refund acknowledged (or already acknowledged)
        "400":
          description: Refund is still pending
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/dispositions:
    post:
      operationId: submitDisposition
      tags:
        - Disposition Codes
      summary: Submit inbound disposition
      description: May trigger CPA conversion or auto-return based on disposition code rules.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: One of saleId, callSaleId, or callerPhone is required. Cannot provide both saleId and callSaleId.
              required:
                - disposition
              properties:
                saleId:
                  type: string
                  format: uuid
                callSaleId:
                  type: string
                  format: uuid
                callerPhone:
                  type: string
                  maxLength: 20
                disposition:
                  type: string
                  maxLength: 50
                dispoReason:
                  type: string
                  maxLength: 100
                intakeDispo:
                  type: string
                  maxLength: 100
                comsCount:
                  type: integer
                  minimum: 0
                intakeId:
                  type: string
                  maxLength: 255
                notes:
                  type: string
                  maxLength: 2000
                agentName:
                  type: string
                  maxLength: 100
                callDuration:
                  type: integer
                  minimum: 0
                callbackDate:
                  type: string
                  format: date-time
                dispositionDate:
                  type: string
                  description: Optional event date for this disposition. Accepts an ISO-8601 datetime (with offset, e.g.
                    "2026-06-20T14:30:00Z" or "+05:00") OR a date-only string ("YYYY-MM-DD", resolved to tenant-timezone
                    midnight). When omitted, the disposition is timestamped with the receive time. Future dates are
                    rejected.
                externalId:
                  type: string
                  maxLength: 255
                firstName:
                  type: string
                  maxLength: 100
                lastName:
                  type: string
                  maxLength: 100
                program:
                  type: string
                  maxLength: 100
            example:
              saleId: d4e5f6a7-0000-0000-0000-000000000001
              disposition: sold
      responses:
        "200":
          description: Disposition processed
      security:
        - sessionCookie: []
  /api/v1/tracking-numbers:
    get:
      operationId: listTrackingNumbers
      tags:
        - Tracking Numbers
      summary: List tracking numbers
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - assigned
              - available
              - released
        - name: provider
          in: query
          schema:
            type: string
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: offerId
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          description: Match on phone number or label
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - phoneNumber
              - provider
              - status
              - createdAt
            default: createdAt
        - name: sortDirection
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated tracking number list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: createTrackingNumber
      tags:
        - Tracking Numbers
      summary: Create tracking number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - provider
                - phoneNumber
              properties:
                provider:
                  type: string
                  enum:
                    - twilio
                    - telnyx
                    - bandwidth
                phoneNumber:
                  type: string
                campaignId:
                  type: string
                  format: uuid
                  nullable: true
                offerId:
                  type: string
                  format: uuid
                  nullable: true
                label:
                  type: string
                  nullable: true
                numberType:
                  type: string
                  enum:
                    - local
                    - tollfree
                    - mobile
                  default: local
                ivrConfigId:
                  type: string
                  format: uuid
                  nullable: true
                whisperEnabled:
                  type: boolean
                  default: false
                whisperDefault:
                  type: string
                  nullable: true
                recordCalls:
                  type: boolean
                  default: true
                renewalDate:
                  type: string
                  nullable: true
                purchasedAt:
                  type: string
                  format: date-time
                  nullable: true
                poolManaged:
                  type: boolean
                  default: true
                purpose:
                  type: string
                  enum:
                    - inbound
                    - outbound_ai
                    - transfer_ai
                  default: inbound
                status:
                  type: string
                  enum:
                    - assigned
                    - available
                    - released
                  default: assigned
            example:
              provider: telnyx
              phoneNumber: "+18005551234"
              label: National Auto Insurance
              numberType: tollfree
      responses:
        "201":
          description: Tracking number created
      security:
        - sessionCookie: []
  /api/v1/tracking-numbers/{numberId}:
    parameters:
      - name: numberId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getTrackingNumber
      tags:
        - Tracking Numbers
      summary: Get tracking number
      responses:
        "200":
          description: Tracking number detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateTrackingNumber
      tags:
        - Tracking Numbers
      summary: Update tracking number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                provider:
                  type: string
                  enum:
                    - twilio
                    - telnyx
                    - bandwidth
                    - signalwire
                phoneNumber:
                  type: string
                campaignId:
                  type: string
                  format: uuid
                  nullable: true
                offerId:
                  type: string
                  format: uuid
                  nullable: true
                label:
                  type: string
                  nullable: true
                numberType:
                  type: string
                  enum:
                    - local
                    - tollfree
                    - mobile
                ivrConfigId:
                  type: string
                  format: uuid
                  nullable: true
                whisperEnabled:
                  type: boolean
                whisperDefault:
                  type: string
                  nullable: true
                recordCalls:
                  type: boolean
                renewalDate:
                  type: string
                  nullable: true
                purchasedAt:
                  type: string
                  format: date-time
                  nullable: true
                poolManaged:
                  type: boolean
                purpose:
                  type: string
                  enum:
                    - inbound
                    - outbound_ai
                    - transfer_ai
                status:
                  type: string
                  enum:
                    - assigned
                    - available
                    - released
      responses:
        "200":
          description: Tracking number updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: releaseTrackingNumber
      tags:
        - Tracking Numbers
      summary: Release tracking number
      responses:
        "200":
          description: Number released
      security:
        - sessionCookie: []
  /api/v1/tracking-numbers/{numberId}/purchase:
    parameters:
      - name: numberId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: purchaseTrackingNumber
      tags:
        - Tracking Numbers
      summary: Purchase tracking number
      responses:
        "200":
          description: Number purchased
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          description: Provider rejected the purchase
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/tracking-numbers/search:
    post:
      operationId: searchAvailableNumbers
      tags:
        - Tracking Numbers
      summary: Search available numbers
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - provider
              properties:
                provider:
                  type: string
                  enum:
                    - twilio
                    - telnyx
                    - bandwidth
                areaCode:
                  type: string
                country:
                  type: string
                  default: US
                numberType:
                  type: string
                  enum:
                    - local
                    - tollfree
                    - mobile
                  default: local
                limit:
                  type: integer
                  default: 10
      responses:
        "200":
          description: Available numbers
        "400":
          $ref: "#/components/responses/ValidationError"
        "502":
          description: Provider rejected the search
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/offer-integrations:
    get:
      operationId: listOfferIntegrations
      tags:
        - Integrations
      summary: List offer integrations
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated integration list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: createOfferIntegration
      tags:
        - Integrations
      summary: Create offer integration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                prefix:
                  type: string
                  nullable: true
                relevantTo:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                notes:
                  type: string
                  nullable: true
                config:
                  type: object
                  additionalProperties: true
            example:
              name: Acme CRM Integration
      responses:
        "201":
          description: Integration created
        "400":
          $ref: "#/components/responses/ValidationError"
      security:
        - sessionCookie: []
  /api/v1/offer-integrations/{integrationId}:
    parameters:
      - name: integrationId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getOfferIntegration
      tags:
        - Integrations
      summary: Get offer integration
      responses:
        "200":
          description: Integration detail
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateOfferIntegration
      tags:
        - Integrations
      summary: Update offer integration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                verticalId:
                  type: string
                  format: uuid
                  nullable: true
                prefix:
                  type: string
                  nullable: true
                relevantTo:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - active
                    - paused
                    - inactive
                notes:
                  type: string
                  nullable: true
                config:
                  type: object
                  additionalProperties: true
      responses:
        "200":
          description: Integration updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteOfferIntegration
      tags:
        - Integrations
      summary: Delete offer integration
      responses:
        "200":
          description: Integration deleted
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/offer-integrations/{integrationId}/test:
    parameters:
      - name: integrationId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: testOfferIntegration
      tags:
        - Integrations
      summary: Test integration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
            example:
              firstName: Test
              lastName: Lead
              email: test@example.com
      responses:
        "200":
          description: Test result with raw response
      security:
        - sessionCookie: []
  /api/v1/sessions:
    post:
      operationId: createSession
      tags:
        - Sessions
      summary: Create lead session
      description: Progressive lead capture session. Authenticated via partner API key (Bearer lr_xxx).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  type: string
                  description: Campaign ID (displayId or UUID)
                totalSteps:
                  type: integer
            example:
              campaignId: C-1001
              totalSteps: 3
      responses:
        "201":
          description: Session created with sessionKey
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - partnerApiKey: []
  /api/v1/sessions/{sessionKey}:
    parameters:
      - name: sessionKey
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: getLeadSession
      tags:
        - Sessions
      summary: Get session
      responses:
        "200":
          description: Session detail
        "401":
          $ref: "#/components/responses/Unauthorized"
      security: []
    patch:
      operationId: updateLeadSession
      tags:
        - Sessions
      summary: Update session data
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
                currentStep:
                  type: integer
                status:
                  type: string
                  enum:
                    - completed
            example:
              data:
                firstName: John
                lastName: Doe
              currentStep: 2
      responses:
        "200":
          description: Session updated
      security: []
  /api/v1/sessions/{sessionKey}/submit:
    parameters:
      - name: sessionKey
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: submitSession
      tags:
        - Sessions
      summary: Submit session as lead
      requestBody:
        required: false
        description: Optional lead fields merged directly on top of the accumulated session data (not nested under a `data`
          key). An empty body is allowed — the session's accumulated data is submitted as-is.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Lead submitted from session
      security: []
  /api/v1/sessions/{sessionKey}/coreg:
    parameters:
      - name: sessionKey
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: createCoregLead
      tags:
        - Sessions
      summary: Create co-registration lead
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  type: string
                  format: uuid
                  description: Coreg campaign ID (UUID) to route the accumulated session data through.
                data:
                  type: object
                  description: Optional field overrides merged on top of the accumulated session data.
      responses:
        "201":
          description: Co-reg lead created
      security: []
  /api/v1/settings:
    get:
      operationId: getSettings
      tags:
        - Settings
      summary: Get system settings by category
      parameters:
        - name: category
          in: query
          required: true
          description: Settings category to retrieve (e.g. leadFields, branding)
          schema:
            type: string
        - name: key
          in: query
          description: Optional specific key within the category
          schema:
            type: string
      responses:
        "200":
          description: Settings matching the category/key filter
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
    put:
      operationId: updateSettings
      tags:
        - Settings
      summary: Update settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Settings updated
      security:
        - sessionCookie: []
  /api/v1/settings/integrations:
    get:
      operationId: getIntegrationSettings
      tags:
        - Settings
      summary: Get integration settings
      responses:
        "200":
          description: Integration settings
      security:
        - sessionCookie: []
    put:
      operationId: updateIntegrationSettings
      tags:
        - Settings
      summary: Update integration settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Integration settings updated
        "402":
          description: Payment required (error code BILLING_REQUIRED) — a payment method is required to enable this integration
            (e.g. AI Dialer).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/settings/logos:
    get:
      operationId: listLogos
      tags:
        - Settings
      summary: List branding logos
      description: Returns the tenant's branding settings (logo and favicon).
      responses:
        "200":
          description: Branding settings
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
    post:
      operationId: uploadLogo
      tags:
        - Settings
      summary: Upload logo
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - type
              properties:
                file:
                  type: string
                  format: binary
                type:
                  type: string
                  enum:
                    - logo
                    - favicon
      responses:
        "200":
          description: Logo uploaded
      security:
        - sessionCookie: []
    delete:
      operationId: deleteLogo
      tags:
        - Settings
      summary: Delete logo
      description: Removes the tenant's uploaded logo or favicon, reverting to the default.
      parameters:
        - name: type
          in: query
          required: true
          schema:
            type: string
            enum:
              - logo
              - favicon
      responses:
        "200":
          description: Logo deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/settings/domains:
    get:
      operationId: listDomains
      tags:
        - Settings
      summary: List custom domains
      responses:
        "200":
          description: Domain list
      security:
        - sessionCookie: []
    post:
      operationId: addDomain
      tags:
        - Settings
      summary: Add custom domain
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - domain
              properties:
                domain:
                  type: string
                domainType:
                  type: string
                  default: custom
                  description: Deprecated. Every custom domain serves the full branded app; no behaviour branches on this value. Defaults
                    to 'custom'.
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - inactive
                  default: pending
                isDefault:
                  type: boolean
                  default: false
                sslEnabled:
                  type: boolean
                  default: false
                sslInstalled:
                  type: boolean
                  default: false
            example:
              domain: leads.acme-insurance.com
              domainType: custom
      responses:
        "201":
          description: Domain added
      security:
        - sessionCookie: []
  /api/v1/settings/domains/{domainId}:
    parameters:
      - name: domainId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getDomain
      tags:
        - Settings
      summary: Get custom domain
      responses:
        "200":
          description: Domain detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateDomain
      tags:
        - Settings
      summary: Update custom domain
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                domainType:
                  type: string
                  default: custom
                  description: Deprecated. Every custom domain serves the full branded app; no behaviour branches on this value. Defaults
                    to 'custom'.
                status:
                  type: string
                  enum:
                    - pending
                    - active
                    - inactive
                isDefault:
                  type: boolean
                sslEnabled:
                  type: boolean
                sslInstalled:
                  type: boolean
      responses:
        "200":
          description: Domain updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: removeDomain
      tags:
        - Settings
      summary: Remove custom domain
      responses:
        "200":
          description: Domain removed
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/settings/lists:
    get:
      operationId: listSystemLists
      tags:
        - Settings
      summary: List system lists
      parameters:
        - name: listType
          in: query
          description: Filter lists by type
          schema:
            type: string
      responses:
        "200":
          description: System lists
      security:
        - sessionCookie: []
    post:
      operationId: createSystemList
      tags:
        - Settings
      summary: Create system list
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "201":
          description: List created
      security:
        - sessionCookie: []
  /api/v1/settings/lists/{listId}:
    parameters:
      - name: listId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getSystemList
      tags:
        - Settings
      summary: Get system list
      responses:
        "200":
          description: System list detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateSystemList
      tags:
        - Settings
      summary: Update system list
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: List updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteSystemList
      tags:
        - Settings
      summary: Delete system list
      responses:
        "200":
          description: List deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/settings/signup-questions:
    get:
      operationId: listSignupQuestions
      tags:
        - Settings
      summary: List signup questions
      parameters:
        - name: entityType
          in: query
          description: Filter questions by entity type
          schema:
            type: string
            enum:
              - buyer
              - partner
      responses:
        "200":
          description: Question list
      security:
        - sessionCookie: []
    post:
      operationId: createSignupQuestion
      tags:
        - Settings
      summary: Create signup question
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "201":
          description: Question created
      security:
        - sessionCookie: []
  /api/v1/settings/signup-questions/{questionId}:
    parameters:
      - name: questionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getSignupQuestion
      tags:
        - Settings
      summary: Get signup question
      responses:
        "200":
          description: Signup question detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateSignupQuestion
      tags:
        - Settings
      summary: Update signup question
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Question updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteSignupQuestion
      tags:
        - Settings
      summary: Delete signup question
      responses:
        "200":
          description: Question deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/settings/smtp:
    get:
      operationId: listSmtpAccounts
      tags:
        - Settings
      summary: List SMTP accounts
      responses:
        "200":
          description: SMTP account list
      security:
        - sessionCookie: []
    post:
      operationId: createSmtpAccount
      tags:
        - Settings
      summary: Create SMTP account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - emailAddress
              properties:
                provider:
                  type: string
                  enum:
                    - resend
                    - smtp
                  default: resend
                emailAddress:
                  type: string
                fromName:
                  type: string
                  nullable: true
                host:
                  type: string
                  nullable: true
                username:
                  type: string
                  nullable: true
                passwordEncrypted:
                  type: string
                  nullable: true
                apiKeyEncrypted:
                  type: string
                  nullable: true
                port:
                  type: integer
                  default: 587
                authEnabled:
                  type: boolean
                  default: true
                sslEnabled:
                  type: boolean
                  default: true
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                  default: active
            example:
              provider: resend
              emailAddress: noreply@acme-insurance.com
              apiKeyEncrypted: re_xxxxx
      responses:
        "201":
          description: SMTP account created
      security:
        - sessionCookie: []
  /api/v1/settings/smtp/{smtpId}:
    parameters:
      - name: smtpId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getSmtpAccount
      tags:
        - Settings
      summary: Get SMTP account
      responses:
        "200":
          description: SMTP account (password redacted)
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateSmtpAccount
      tags:
        - Settings
      summary: Update SMTP account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: SMTP account updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteSmtpAccount
      tags:
        - Settings
      summary: Delete SMTP account
      responses:
        "200":
          description: SMTP account deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/settings/tax-rates:
    get:
      operationId: listTaxRates
      tags:
        - Settings
      summary: List tax rates
      responses:
        "200":
          description: Tax rate list
      security:
        - sessionCookie: []
    post:
      operationId: createTaxRate
      tags:
        - Settings
      summary: Create tax rate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - code
                - percentage
              properties:
                name:
                  type: string
                code:
                  type: string
                percentage:
                  type: string
            example:
              name: CA Sales Tax
              code: CA-SALES
              percentage: "0.0725"
      responses:
        "201":
          description: Tax rate created
      security:
        - sessionCookie: []
  /api/v1/settings/tax-rates/{taxRateId}:
    parameters:
      - name: taxRateId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getTaxRate
      tags:
        - Settings
      summary: Get tax rate
      responses:
        "200":
          description: Tax rate detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateTaxRate
      tags:
        - Settings
      summary: Update tax rate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Tax rate updated
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteTaxRate
      tags:
        - Settings
      summary: Delete tax rate
      responses:
        "200":
          description: Tax rate deleted
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/settings/portal:
    get:
      operationId: getPortalSettings
      tags:
        - Settings
      summary: Get portal settings
      security:
        - sessionCookie: []
      parameters:
        - name: entityType
          in: query
          required: true
          schema:
            type: string
            enum:
              - buyer
              - partner
      responses:
        "200":
          description: Portal settings
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      operationId: updatePortalSettings
      tags:
        - Settings
      summary: Update portal settings
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - entityType
              properties:
                entityType:
                  type: string
                  enum:
                    - buyer
                    - partner
                signUpEnabled:
                  type: boolean
                signUpApproval:
                  type: string
                  enum:
                    - manual
                    - auto
                portalAccess:
                  type: boolean
                manageUsers:
                  type: boolean
                headerJs:
                  type: string
                  nullable: true
                footerJs:
                  type: string
                  nullable: true
                termsConditions:
                  type: string
                  nullable: true
                requireTermsAcceptance:
                  type: boolean
      responses:
        "200":
          description: Portal settings updated
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/tenant-settings:
    get:
      operationId: getTenantDisplaySettings
      tags:
        - Settings
      summary: Get tenant display/format preferences
      description: Display/format preferences for the authenticated tenant, plus the tenant's transactional currency
        (lowercase ISO 4217, e.g. `usd`, `gbp`). Money amounts across the API are denominated in the tenant currency
        unless a field is explicitly documented as platform USD.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Tenant display settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      phoneDisplayFormat:
                        type: string
                        nullable: true
                      defaultPhoneFormat:
                        type: string
                        nullable: true
                      defaultTimezone:
                        type: string
                        nullable: true
                      dateFormat:
                        type: string
                        nullable: true
                      timeFormat:
                        type: string
                        nullable: true
                      currency:
                        type: string
                        description: Tenant transactional currency (lowercase ISO 4217).
                        example: usd
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/compliance:
    get:
      operationId: listCompliance
      tags:
        - Compliance
      summary: List compliance entries
      description: Returns block list or data deletion requests.
      parameters:
        - name: resource
          in: query
          required: false
          schema:
            type: string
            enum:
              - block-list
              - data-deletion
            default: block-list
        - name: status
          in: query
          schema:
            type: string
        - name: type
          in: query
          schema:
            type: string
            enum:
              - phone
              - email
        - name: scope
          in: query
          description: Block-list scope filter (block-list resource only).
          schema:
            type: string
            enum:
              - global
              - buyer
              - vertical
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated compliance list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
    post:
      operationId: createComplianceEntry
      tags:
        - Compliance
      summary: Create compliance entry
      parameters:
        - name: resource
          in: query
          required: false
          schema:
            type: string
            enum:
              - block-list
              - data-deletion
            default: block-list
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
            example:
              type: phone
              value: "+15551234567"
              reason: dnc_request
              displayName: Jane Example
      responses:
        "201":
          description: Entry created
      security:
        - sessionCookie: []
    delete:
      operationId: deleteComplianceEntry
      tags:
        - Compliance
      summary: Delete compliance entry
      description: Removes a block-list suppression entry, or marks a data-deletion request completed.
      parameters:
        - name: resource
          in: query
          required: false
          schema:
            type: string
            enum:
              - block-list
              - data-deletion
            default: block-list
        - name: id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Entry deleted or request completed
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/compliance/opt-out:
    post:
      operationId: optOut
      tags:
        - Compliance
      summary: Opt-out (public)
      description: Public endpoint for consumers to suppress their data.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: At least one of email or phone is required.
              properties:
                email:
                  type: string
                  format: email
                phone:
                  type: string
            example:
              phone: "+15559876543"
      responses:
        "200":
          description: Opt-out recorded
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Too many requests — rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/suppression:
    get:
      operationId: listSuppressionEntries
      tags:
        - Compliance
      summary: List suppression entries
      parameters:
        - name: type
          in: query
          schema:
            type: string
            enum:
              - phone
              - email
        - name: scope
          in: query
          schema:
            type: string
            enum:
              - global
              - buyer
              - vertical
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Suppression list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
        - apiKey: []
    post:
      operationId: addSuppressionEntry
      tags:
        - Compliance
      summary: Add suppression entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - value
              properties:
                type:
                  type: string
                  enum:
                    - phone
                    - email
                value:
                  type: string
                scope:
                  type: string
                  enum:
                    - global
                    - buyer
                    - vertical
                  default: global
                scopeId:
                  type: string
                  format: uuid
                reason:
                  type: string
                  enum:
                    - dnc_request
                    - opt_out
                    - bad_data
                    - manual
                  default: manual
                displayName:
                  type: string
                  description: Optional human reference name. Display-only; not used for suppression matching.
                expiresAt:
                  type: string
                  format: date-time
            example:
              type: email
              value: bounced@example.com
              scope: global
              displayName: Jane Example
      responses:
        "201":
          description: Entry added
      security:
        - sessionCookie: []
  /api/v1/suppression/check:
    post:
      operationId: checkSuppression
      tags:
        - Compliance
      summary: Check suppression status
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                phone:
                  type: string
            example:
              phone: "+15551234567"
      responses:
        "200":
          description: Suppression check result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      email:
                        type: boolean
                      phone:
                        type: boolean
      security:
        - sessionCookie: []
  /api/v1/suppression/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: deleteSuppression
      tags:
        - Compliance
      summary: Delete suppression entry
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Suppression entry deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blocked-ips:
    get:
      operationId: listBlockedIps
      tags:
        - Blocked Lists
      summary: List blocked IP ranges
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Blocked IP list
      security:
        - sessionCookie: []
    post:
      operationId: blockIpRange
      tags:
        - Blocked Lists
      summary: Block IP range
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fromIp
                - toIp
              properties:
                fromIp:
                  type: string
                toIp:
                  type: string
                blockReason:
                  type: string
                  nullable: true
                reason:
                  type: string
                  nullable: true
            example:
              fromIp: 192.168.1.0
              toIp: 192.168.1.255
              blockReason: Spam source
      responses:
        "201":
          description: IP range blocked
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/v1/blocked-ips/{rangeId}:
    parameters:
      - name: rangeId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: unblockIpRange
      tags:
        - Blocked Lists
      summary: Unblock IP range
      responses:
        "200":
          description: IP range unblocked
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/blocked-callerids:
    get:
      operationId: listBlockedCallerIds
      tags:
        - Blocked Lists
      summary: List blocked caller IDs
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Blocked caller ID list
      security:
        - sessionCookie: []
    post:
      operationId: blockCallerId
      tags:
        - Blocked Lists
      summary: Block caller ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - value
              properties:
                value:
                  type: string
                reason:
                  type: string
            example:
              value: "+15551234567"
              reason: Robocall
      responses:
        "201":
          description: Caller ID blocked
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/v1/blocked-callerids/{callerIdId}:
    parameters:
      - name: callerIdId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: unblockCallerId
      tags:
        - Blocked Lists
      summary: Unblock caller ID
      responses:
        "200":
          description: Caller ID unblocked
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/blocked-referrers:
    get:
      operationId: listBlockedReferrers
      tags:
        - Blocked Lists
      summary: List blocked referrers
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Blocked referrer list
      security:
        - sessionCookie: []
    post:
      operationId: blockReferrer
      tags:
        - Blocked Lists
      summary: Block referrer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - value
              properties:
                value:
                  type: string
                reason:
                  type: string
            example:
              value: spam-leads.com
              reason: Fraudulent traffic
      responses:
        "201":
          description: Referrer blocked
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/v1/blocked-referrers/{referrerId}:
    parameters:
      - name: referrerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: unblockReferrer
      tags:
        - Blocked Lists
      summary: Unblock referrer
      responses:
        "200":
          description: Referrer unblocked
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/users:
    get:
      operationId: listUsers
      tags:
        - Users
      summary: List users
      description: In tenant context each user also includes tenantRoles — the user's active member roles in the current
        tenant, sorted admin > partner > buyer > referrer. The role field remains the legacy global user role.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - active
              - inactive
              - banned
        - name: role
          in: query
          description: In tenant context, matches users holding an active member role of this value in the tenant
            (member.appRole). In superadmin / platform context, matches the global user.role.
          schema:
            type: string
            enum:
              - admin
              - buyer
              - partner
        - name: q
          in: query
          description: Match on email, first name, or last name
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: User list. Each row includes the legacy global role plus, in tenant context, tenantRoles (string array of
            active tenant member roles).
      security:
        - sessionCookie: []
  /api/v1/users/{userId}:
    parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getUser
      tags:
        - Users
      summary: Get user
      responses:
        "200":
          description: User detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    patch:
      operationId: updateUser
      tags:
        - Users
      summary: Perform a user action
      description: Action-based endpoint. `disable2FA` clears 2FA, `revokeAllSessions` deletes sessions, `ban`/`unban` set the
        banned flag (ban also revokes sessions). `setPassword` is deprecated and returns 410.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - disable2FA
                    - revokeAllSessions
                    - ban
                    - unban
                    - setPassword
                banReason:
                  type: string
                  nullable: true
                  description: Optional reason, used with the `ban` action.
                banExpiresAt:
                  type: string
                  format: date-time
                  nullable: true
                  description: Optional ban expiry, used with the `ban` action.
            example:
              action: ban
              banReason: Fraudulent activity
      responses:
        "200":
          description: Action performed
        "400":
          description: Unknown action.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          description: Password management removed — users log in via magic link.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
    put:
      operationId: replaceUser
      tags:
        - Users
      summary: Update user (full record)
      description: Full-record update from the user-edit form. Privileged and per-tenant flags (permUsers, piiAccess,
        readOnly, isOwner, isAdmin, isPartnerManager, isAccountManager, isSalesRep) are mirrored to the caller's member
        rows and gated by the caller's manage-scope. Assigning the superadmin role requires a superadmin caller.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  nullable: true
                jobTitle:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                workPhone:
                  type: string
                  nullable: true
                ext:
                  type: string
                  nullable: true
                role:
                  type: string
                  nullable: true
                isAdmin:
                  type: boolean
                  nullable: true
                dataExport:
                  type: boolean
                  nullable: true
                piiAccess:
                  type: boolean
                  nullable: true
                readOnly:
                  type: boolean
                  nullable: true
                permUsers:
                  type: boolean
                  nullable: true
                massEmails:
                  type: boolean
                  nullable: true
                notifications:
                  type: boolean
                  nullable: true
                support:
                  type: boolean
                  nullable: true
                billing:
                  type: boolean
                  nullable: true
                newsletters:
                  type: boolean
                  nullable: true
                apiAccess:
                  type: boolean
                  nullable: true
                buyerId:
                  type: string
                  nullable: true
                isPartnerManager:
                  type: boolean
                  nullable: true
                isAccountManager:
                  type: boolean
                  nullable: true
                isSalesRep:
                  type: boolean
                  nullable: true
                legacyId:
                  type: integer
                  nullable: true
                crmId:
                  type: string
                  nullable: true
                isOwner:
                  type: boolean
      responses:
        "200":
          description: User updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteUser
      tags:
        - Users
      summary: Delete user
      responses:
        "200":
          description: User deactivated
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/users/{userId}/accounts:
    parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listUserAccounts
      tags:
        - Users
      summary: List user linked OAuth/SSO accounts
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Linked accounts list
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{userId}/accounts/{accountId}:
    parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: accountId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: unlinkUserAccount
      tags:
        - Users
      summary: Unlink user account
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Account unlinked
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{userId}/sessions:
    parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: listUserSessions
      tags:
        - Users
      summary: List user sessions
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Active sessions list
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: revokeAllUserSessions
      tags:
        - Users
      summary: Revoke all sessions for user
      security:
        - sessionCookie: []
      responses:
        "200":
          description: All sessions revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{userId}/sessions/{sessionId}:
    parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: revokeUserSession
      tags:
        - Users
      summary: Revoke user session
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Session revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/contacts:
    get:
      operationId: listContacts
      tags:
        - Contacts
      summary: List contacts
      parameters:
        - name: entityType
          in: query
          schema:
            type: string
            enum:
              - buyer
              - partner
              - admin
          description: Scope results to a specific entity type (member-based path).
        - name: entityId
          in: query
          schema:
            type: string
            format: uuid
          description: Required when entityType is buyer/partner.
        - name: q
          in: query
          schema:
            type: string
          description: Search by first name, last name, or email.
        - name: status
          in: query
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum:
              - createdAt
              - email
              - firstName
              - lastName
              - lastLoginAt
              - jobTitle
            default: createdAt
        - name: dir
          in: query
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: includeNoMembership
          in: query
          schema:
            type: boolean
            default: false
          description: |
            Global-search path only. When true, surfaces users that have no active
            portal-access member row (pre-Phase-5.5 pile needing reactivation).
            Tenant scope still applies via the current user. Response rows include
            hasPortalAccess=false for the orphans.
      responses:
        "200":
          description: Contact list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        firstName:
                          type: string
                          nullable: true
                        lastName:
                          type: string
                          nullable: true
                        email:
                          type: string
                        jobTitle:
                          type: string
                          nullable: true
                        phone:
                          type: string
                          nullable: true
                        officePhone:
                          type: string
                          nullable: true
                        status:
                          type: string
                        hasPassword:
                          type: boolean
                        hasPortalAccess:
                          type: boolean
                          nullable: true
                          description: True if the user has at least one active portal-access member row in the caller's tenant scope.
                        lastLoginAt:
                          type: string
                          format: date-time
                          nullable: true
                        createdAt:
                          type: string
                          format: date-time
                  pagination:
                    $ref: "#/components/schemas/PaginationMeta"
      security:
        - sessionCookie: []
    post:
      operationId: createContact
      tags:
        - Contacts
      summary: Create contact
      description: |
        CRM person upsert (matched by email within tenant). Supplying entityType +
        entityId is deprecated and returns 410 — use POST /api/v1/invitations instead.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                  maxLength: 30
                dateOfBirth:
                  type: string
                  nullable: true
                address:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                  maxLength: 2
                zipCode:
                  type: string
                  nullable: true
                  maxLength: 10
                country:
                  type: string
                  nullable: true
                properties:
                  type: object
                  additionalProperties: true
                  default: {}
                tags:
                  type: array
                  items:
                    type: string
                  default: []
            example:
              email: john@acme-insurance.com
              firstName: John
              tags:
                - newsletter
      responses:
        "200":
          description: Existing contact updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      type:
                        type: string
                        example: contact
                      tags:
                        type: array
                        items:
                          type: string
        "201":
          description: Contact created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      type:
                        type: string
                        example: contact
                      tags:
                        type: array
                        items:
                          type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "410":
          description: Entity-scoped create is deprecated — use POST /api/v1/invitations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/contacts/{contactId}:
    parameters:
      - name: contactId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getContact
      tags:
        - Contacts
      summary: Get contact
      responses:
        "200":
          description: Contact detail
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    put:
      operationId: updateContact
      tags:
        - Contacts
      summary: Update contact
      description: |
        Profile fields (firstName, lastName, email, jobTitle, phone, officePhone) land on
        the global user row and are only editable when the user belongs to a single tenant.
        Permission fields and status land on the per-tenant member row.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  nullable: true
                lastName:
                  type: string
                  nullable: true
                email:
                  type: string
                  nullable: true
                jobTitle:
                  type: string
                  nullable: true
                phone:
                  type: string
                  nullable: true
                officePhone:
                  type: string
                  nullable: true
                status:
                  type: string
                  nullable: true
                permAccounts:
                  type: boolean
                permUsers:
                  type: boolean
                permBilling:
                  type: boolean
                permOffers:
                  type: boolean
                permReports:
                  type: boolean
      responses:
        "200":
          description: Contact updated
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
    delete:
      operationId: deleteContact
      tags:
        - Contacts
      summary: Delete contact
      responses:
        "200":
          description: Contact deactivated
      security:
        - sessionCookie: []
  /api/v1/contacts/{contactId}/send-magic-link:
    parameters:
      - name: contactId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: sendContactMagicLink
      tags:
        - Contacts
      summary: Send magic-link invitation to an existing contact
      description: |
        Admin-only. Sends a magic-link reactivation invitation to an existing user
        (contactId) for a target buyer or partner entity. The entity must belong
        to the caller's tenant unless the caller is a superadmin.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - entityType
              properties:
                entityType:
                  type: string
                  enum:
                    - buyer
                    - partner
                    - admin
                entityId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Required unless entityType is admin.
                appRole:
                  type: string
                  default: buyer
                  description: App-level role (defaults to buyer).
                permissions:
                  type: object
                  description: Permission flags (e.g. permAccounts, permBilling). Defaults to empty object.
                tenantId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Required only for superadmin callers; ignored for tenant admins.
            example:
              entityType: buyer
              entityId: c3d4e5f6-a7b8-9012-cdef-123456789012
              appRole: admin
              permissions:
                permAccounts: true
                permUsers: false
                permBilling: false
                permOffers: true
                permReports: true
      responses:
        "200":
          description: Magic-link email sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/contacts/bulk-send-magic-link:
    post:
      operationId: bulkSendContactMagicLink
      tags:
        - Contacts
      summary: Bulk send magic-link invitations to existing contacts
      description: |
        Admin-only. Sends magic-link reactivation invitations to up to 50 existing
        users for a target buyer or partner entity. Runs a single entity-tenant
        pre-flight check; per-contact failures are reported in the response body
        and do not abort the batch. Always returns 200 (even with partial failures)
        so the caller can reconcile which contactIds succeeded, were skipped, or
        failed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - contactIds
                - entityType
              properties:
                contactIds:
                  type: array
                  minItems: 1
                  maxItems: 50
                  items:
                    type: string
                    format: uuid
                entityType:
                  type: string
                  enum:
                    - buyer
                    - partner
                    - admin
                entityId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Required unless entityType is admin.
                appRole:
                  type: string
                  default: buyer
                permissions:
                  type: object
                  description: Defaults to empty object.
                tenantId:
                  type: string
                  format: uuid
                  description: Required only for superadmin callers; ignored for tenant admins.
            example:
              contactIds:
                - 11111111-1111-1111-1111-111111111111
                - 22222222-2222-2222-2222-222222222222
              entityType: buyer
              entityId: c3d4e5f6-a7b8-9012-cdef-123456789012
              appRole: admin
              permissions:
                permAccounts: true
                permBilling: false
      responses:
        "200":
          description: Bulk send completed (may include partial failures)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      sent:
                        type: array
                        items:
                          type: string
                          format: uuid
                      failed:
                        type: array
                        items:
                          type: object
                          properties:
                            contactId:
                              type: string
                              format: uuid
                            error:
                              type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/v1/reports:
    get:
      operationId: getReport
      tags:
        - Reports
      summary: Get report
      parameters:
        - name: type
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: dateTo
          in: query
          schema:
            type: string
            format: date-time
        - name: verticalId
          in: query
          schema:
            type: string
            format: uuid
        - name: partnerId
          in: query
          schema:
            type: string
            format: uuid
        - name: buyerId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Report data
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
  /api/v1/audit-logs:
    get:
      operationId: listAuditLogs
      tags:
        - Audit Logs
      summary: List audit logs
      parameters:
        - name: entityType
          in: query
          schema:
            type: string
        - name: entityId
          in: query
          schema:
            type: string
            format: uuid
        - name: action
          in: query
          schema:
            type: string
        - name: userId
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Paginated audit log list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
      security:
        - sessionCookie: []
  /api/v1/login-activity:
    get:
      operationId: listLoginActivity
      tags:
        - Audit Logs
      summary: List login activity
      parameters:
        - name: entityType
          in: query
          schema:
            type: string
        - name: entityId
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Login activity list
      security:
        - sessionCookie: []
  /api/v1/postback:
    get:
      operationId: handlePostback
      tags:
        - Leads
      summary: CPA postback receiver (GET)
      description: Processes a CPA conversion postback. Requires disposition plus either saleId or callSaleId; key is required
        if the buyer has a postbackKey configured.
      parameters:
        - name: disposition
          in: query
          required: true
          description: Disposition code to apply.
          schema:
            type: string
        - name: saleId
          in: query
          description: Lead sale ID (UUID) or buyerLeadId to convert. Provide saleId or callSaleId.
          schema:
            type: string
        - name: callSaleId
          in: query
          description: Call sale ID (UUID) to convert. Provide saleId or callSaleId.
          schema:
            type: string
        - name: key
          in: query
          description: Buyer postback key (required if buyer has postbackKey set)
          schema:
            type: string
        - name: dispoReason
          in: query
          description: Optional free-text disposition reason.
          schema:
            type: string
        - name: intakeDispo
          in: query
          description: Optional intake-center disposition code.
          schema:
            type: string
        - name: comsCount
          in: query
          description: Optional communication/contact attempt count.
          schema:
            type: integer
        - name: buyerLeadId
          in: query
          description: Optional buyer-side lead identifier (alias of intakeId).
          schema:
            type: string
        - name: intakeId
          in: query
          description: Optional intake identifier (used when buyerLeadId is absent).
          schema:
            type: string
        - name: dispositionDate
          in: query
          description: Optional disposition event date. ISO-8601 datetime (with offset) OR date-only (YYYY-MM-DD → tenant-timezone
            midnight). Omitted → receive time. Future rejected.
          schema:
            type: string
      requestBody:
        required: false
        description: "Optional JSON body carrying demographics alongside the disposition. Entirely additive — a missing body, an
          empty body, a non-JSON content-type or malformed JSON all behave exactly as a query-string-only postback. Sale
          identity is always taken from the query string; body fields never redirect the write. Values fill only fields
          that are still empty and never overwrite an existing one. An unrecognised value is recorded as sent rather
          than rejected: the disposition rides the same request, so a demographic problem must never cost you the
          disposition."
        content:
          application/json:
            schema:
              type: object
              properties:
                gender:
                  type: string
                  description: Reported gender. Stored as sent; no fixed value list.
                  example: FEMALE
                dateOfBirth:
                  type: string
                  description: Date of birth, strict ISO YYYY-MM-DD only. Other formats are ignored (and the disposition still succeeds)
                    because a locale-parsed date would silently produce a wrong age band. Stored encrypted; an age band
                    is derived from it as of the lead's creation date.
                  example: 1982-12-21
      responses:
        "200":
          description: Conversion processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      dispositionId:
                        type: string
                        format: uuid
                      cpaTriggered:
                        type: boolean
                      returnTriggered:
                        type: boolean
                      cpaReversed:
                        type: boolean
                        description: |
                          True when this postback caused an existing CPA conversion to be reversed.
                          Driven by the disposition's `triggersReturn=true` AND `returnScope IN (NULL, 'CPA')`
                          AND the sale's `cpaStatus='converted'` at receipt time.
                      unmapped:
                        type: boolean
                      idempotent:
                        type: boolean
                        description: |
                          True when this postback replayed a disposition already recorded under the
                          same auto-generated `externalId` (`pb:{saleId}:{disposition}`), so nothing
                          was created and no CPA or return logic ran.

                          Use THIS to tell a replay from a fresh write. `cpaTriggered` and
                          `returnTriggered` report the sale's existing state, so on a replay of an
                          already-converted sale `cpaTriggered` is true even though this call fired
                          nothing.
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
      security: []
    post:
      operationId: handlePostbackPost
      tags:
        - Leads
      summary: CPA postback receiver (POST)
      description: Same as GET — processes a CPA conversion postback via POST.
      parameters:
        - name: disposition
          in: query
          required: true
          description: Disposition code to apply.
          schema:
            type: string
        - name: saleId
          in: query
          description: Lead sale ID (UUID) or buyerLeadId to convert. Provide saleId or callSaleId.
          schema:
            type: string
        - name: callSaleId
          in: query
          description: Call sale ID (UUID) to convert. Provide saleId or callSaleId.
          schema:
            type: string
        - name: key
          in: query
          description: Buyer postback key (required if buyer has postbackKey set)
          schema:
            type: string
        - name: dispoReason
          in: query
          description: Optional free-text disposition reason.
          schema:
            type: string
        - name: intakeDispo
          in: query
          description: Optional intake-center disposition code.
          schema:
            type: string
        - name: comsCount
          in: query
          description: Optional communication/contact attempt count.
          schema:
            type: integer
        - name: buyerLeadId
          in: query
          description: Optional buyer-side lead identifier (alias of intakeId).
          schema:
            type: string
        - name: intakeId
          in: query
          description: Optional intake identifier (used when buyerLeadId is absent).
          schema:
            type: string
        - name: dispositionDate
          in: query
          description: Optional disposition event date. ISO-8601 datetime (with offset) OR date-only (YYYY-MM-DD → tenant-timezone
            midnight). Omitted → receive time. Future rejected.
          schema:
            type: string
      responses:
        "200":
          description: Conversion processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      dispositionId:
                        type: string
                        format: uuid
                      cpaTriggered:
                        type: boolean
                      returnTriggered:
                        type: boolean
                      cpaReversed:
                        type: boolean
                        description: |
                          True when this postback caused an existing CPA conversion to be reversed.
                          Driven by the disposition's `triggersReturn=true` AND `returnScope IN (NULL, 'CPA')`
                          AND the sale's `cpaStatus='converted'` at receipt time.
                      unmapped:
                        type: boolean
                      idempotent:
                        type: boolean
                        description: |
                          True when this postback replayed a disposition already recorded under the
                          same auto-generated `externalId` (`pb:{saleId}:{disposition}`), so nothing
                          was created and no CPA or return logic ran.

                          Use THIS to tell a replay from a fresh write. `cpaTriggered` and
                          `returnTriggered` report the sale's existing state, so on a replay of an
                          already-converted sale `cpaTriggered` is true even though this call fired
                          nothing.
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
      security: []
  /api/v1/bulk/status:
    post:
      operationId: bulkStatusUpdate
      tags:
        - Leads
      summary: Bulk status update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - entity
                - ids
                - status
              properties:
                entity:
                  type: string
                  enum:
                    - offer
                    - partner
                    - buyer
                    - contract
                    - campaign
                    - user
                ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 500
                status:
                  type: string
                cascadeSubscriptions:
                  type: boolean
                  description: Only honored for entity buyer/contract — cascades Stripe subscription pause/resume with the status change.
            example:
              entity: contract
              ids:
                - c3d4e5f6-a7b8-9012-cdef-123456789012
              status: paused
      responses:
        "200":
          description: Bulk update result
        "400":
          $ref: "#/components/responses/ValidationError"
        "410":
          description: Status changes are not supported for the user entity — use ban/unban or deactivate membership.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
  /api/v1/bulk/account-manager:
    post:
      operationId: bulkAssignAccountManager
      tags:
        - Users
      summary: Bulk assign account manager
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
                - accountManagerId
              properties:
                ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 500
                accountManagerId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "200":
          description: Bulk assignment result
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/bulk/partner-manager:
    post:
      operationId: bulkAssignPartnerManager
      tags:
        - Users
      summary: Bulk assign partner manager
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
                - partnerManagerId
              properties:
                ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 500
                partnerManagerId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "200":
          description: Bulk assignment result
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/bulk/role:
    post:
      operationId: bulkAssignRole
      tags:
        - Users
      summary: Bulk assign role
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
                - role
              properties:
                ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 500
                role:
                  type: string
                  enum:
                    - admin
                    - buyer
                    - partner
      responses:
        "200":
          description: Bulk assignment result
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/bulk/sales-rep:
    post:
      operationId: bulkAssignSalesRep
      tags:
        - Users
      summary: Bulk assign sales rep
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
                - salesRepId
              properties:
                ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 500
                salesRepId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "200":
          description: Bulk assignment result
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/bulk/portal-access:
    post:
      operationId: bulkPortalAccess
      tags:
        - Users
      summary: Deprecated — use status field
      deprecated: true
      security:
        - sessionCookie: []
      responses:
        "410":
          description: Gone - endpoint deprecated
  /api/v1/portal-signup:
    post:
      operationId: portalSignup
      tags:
        - Public
      summary: Portal self-service signup
      security: []
      parameters:
        - name: tenant
          in: query
          description: Tenant slug (resolved from slug or request hostname when omitted).
          schema:
            type: string
        - name: sku
          in: query
          description: SKU slug — triggers buy-with-signup Stripe checkout flow (buyer only).
          schema:
            type: string
        - name: ref
          in: query
          description: Referral attribution code (buyer signups only).
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - entityType
                - firstName
                - lastName
                - email
                - company
                - tcpaConsent
              properties:
                entityType:
                  type: string
                  enum:
                    - buyer
                    - partner
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                emailConfirmation:
                  type: string
                  format: email
                phone:
                  type: string
                  nullable: true
                password:
                  type: string
                  minLength: 8
                company:
                  type: string
                website:
                  type: string
                  nullable: true
                legalEntityType:
                  type: string
                  enum:
                    - individual
                    - corporation
                    - llc
                    - partnership
                    - sole_proprietorship
                    - other
                  nullable: true
                address:
                  type: string
                  nullable: true
                address2:
                  type: string
                  nullable: true
                city:
                  type: string
                  nullable: true
                state:
                  type: string
                  nullable: true
                zipCode:
                  type: string
                  nullable: true
                country:
                  type: string
                  nullable: true
                taxId:
                  type: string
                  nullable: true
                acceptTerms:
                  type: boolean
                tcpaConsent:
                  type: boolean
                  enum:
                    - true
                answers:
                  type: array
                  items:
                    type: object
                    properties:
                      questionId:
                        type: string
                        format: uuid
                      answer:
                        type: string
                referralCode:
                  type: string
                  pattern: ^[A-Z0-9]{8}$
                  nullable: true
      responses:
        "201":
          description: Signup created
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Duplicate - email already registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: Gone - SKU no longer available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/portal-signup/config:
    get:
      operationId: getPortalSignupConfig
      tags:
        - Public
      summary: Get portal signup configuration
      security: []
      parameters:
        - name: entityType
          in: query
          required: true
          schema:
            type: string
            enum:
              - buyer
              - partner
        - name: tenant
          in: query
          description: Tenant slug (resolved from slug or request hostname when omitted).
          schema:
            type: string
      responses:
        "200":
          description: Signup configuration
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/sentry/webhook:
    post:
      operationId: handleSentryWebhook
      tags:
        - Webhooks
      summary: Sentry error webhook (HMAC-verified)
      security: []
      responses:
        "200":
          description: Webhook processed
        "401":
          description: Invalid HMAC signature
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/test-delivery:
    get:
      operationId: testDeliveryGet
      tags:
        - "Admin: Delivery Test"
      summary: Test delivery endpoint (GET)
      description: Dev/staging only - returns 404 in production.
      security: []
      parameters:
        - name: mode
          in: query
          schema:
            type: string
            enum:
              - success
              - fail
              - http_error
              - timeout
              - slow
            default: success
        - name: delay
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Test delivery response
        "404":
          description: Not available in production
        "500":
          description: Simulated error
    post:
      operationId: testDeliveryPost
      tags:
        - "Admin: Delivery Test"
      summary: Test delivery endpoint (POST)
      description: Dev/staging only - returns 404 in production.
      security: []
      parameters:
        - name: mode
          in: query
          schema:
            type: string
            enum:
              - success
              - fail
              - http_error
              - timeout
              - slow
            default: success
        - name: delay
          in: query
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Test delivery response
        "404":
          description: Not available in production
        "500":
          description: Simulated error
  /api/v1/setup/{token}:
    parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: getSetupToken
      tags:
        - Auth
      summary: Consume setup token and return integration guide
      description: Consumes the single-use setup token, provisions an API key, and returns a Markdown integration guide.
      responses:
        "200":
          description: Markdown integration guide (token consumed)
          content:
            text/markdown:
              schema:
                type: string
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          description: Setup token has been used or expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/skills/partner-integration:
    get:
      operationId: partnerIntegrationSkill
      tags:
        - Settings
      summary: Partner integration assistant
      parameters:
        - name: campaignId
          in: query
          required: true
          description: Campaign to generate integration instructions for
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Integration instructions
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
  /api/v1/skills/setup-vertical-campaign:
    get:
      operationId: setupVerticalCampaignSkill
      tags:
        - Settings
      summary: Vertical + campaign setup assistant
      responses:
        "200":
          description: Setup instructions
      security:
        - sessionCookie: []
  /api/v1/signup:
    post:
      operationId: signup
      tags:
        - Auth
      summary: Self-service tenant signup
      description: Creates a new tenant and admin user. May auto-approve or require manual approval depending on signup settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - companyName
                - slug
                - plan
                - verticals
                - preferredChat
                - firstName
                - lastName
                - email
                - phone
                - tcpaConsent
              properties:
                companyName:
                  type: string
                slug:
                  type: string
                plan:
                  type: string
                  enum:
                    - starter
                    - standard
                    - enterprise
                verticals:
                  type: array
                  items:
                    type: string
                preferredChat:
                  type: string
                  enum:
                    - slack
                    - telegram
                    - discord
                    - email
                    - other
                preferredChatHandle:
                  type: string
                  maxLength: 120
                  description: How to reach the user on the chosen chat channel. Required for slack/telegram/discord, optional for other,
                    ignored for email.
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                  format: email
                phone:
                  type: string
                  minLength: 7
                  maxLength: 20
                password:
                  type: string
                  minLength: 8
                tcpaConsent:
                  type: boolean
                  enum:
                    - true
                authMethod:
                  type: string
                  enum:
                    - email
                    - google
                  default: email
                features:
                  type: object
                  additionalProperties:
                    type: boolean
                currency:
                  type: string
                  enum:
                    - usd
                    - gbp
                    - eur
                    - cad
                  default: usd
                  description: Tenant billing currency. Non-usd values are accepted only when the deployment has non-USD tenants enabled;
                    otherwise the request fails with VALIDATION_ERROR.
            example:
              companyName: Acme Insurance
              slug: acme-insurance
              plan: starter
              verticals:
                - Insurance
              preferredChat: slack
              firstName: Jane
              lastName: Doe
              email: admin@acme-insurance.com
              phone: "6195551234"
              preferredChatHandle: jane@acme-insurance.com
              tcpaConsent: true
      responses:
        "201":
          description: Tenant created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      userId:
                        type: string
                        format: uuid
                      autoApproved:
                        type: boolean
                      pendingApproval:
                        type: boolean
                      accountLinked:
                        type: boolean
                        description: True when the email already had a login — the new tenant was linked to the existing user (no session
                          minted; a sign-in email is sent).
                      authMethod:
                        type: string
                        enum:
                          - email
                          - google
              example:
                data:
                  tenantId: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  userId: b2c3d4e5-f6a7-8901-bcde-f12345678901
                  autoApproved: false
                  pendingApproval: true
                  authMethod: email
        "400":
          $ref: "#/components/responses/ValidationError"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Slug already taken (an existing email links the account instead of rejecting)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Too many signup attempts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/signup/check-slug:
    get:
      operationId: checkSlugAvailability
      tags:
        - Auth
      summary: Check slug availability
      description: Returns whether a tenant slug is available for registration.
      parameters:
        - name: slug
          in: query
          required: true
          description: Slug to check
          schema:
            type: string
      responses:
        "200":
          description: Slug availability result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      slug:
                        type: string
                      available:
                        type: boolean
              example:
                data:
                  slug: acme-insurance
                  available: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "429":
          description: Too many requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/signup/config:
    get:
      operationId: getSignupConfig
      tags:
        - Auth
      summary: Public signup configuration
      description: Returns whether self-service signup is enabled and terms requirements. No auth required.
      responses:
        "200":
          description: Signup configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      requireTerms:
                        type: boolean
                      termsUrl:
                        type: string
                        nullable: true
              example:
                data:
                  enabled: true
                  requireTerms: true
                  termsUrl: https://example.com/terms
      security: []
  /api/v1/signup/validate-promo:
    post:
      operationId: validateSignupPromoCode
      tags:
        - Auth
      summary: Validate a signup promo code
      description: Checks a customer-entered promo code against the platform coupons. An unknown or inactive code (or a Stripe
        lookup failure) is a normal 200 with valid=false — invalid promo never blocks signup. Stripe
        promotion-code/coupon ids are never returned. Rate-limited per IP like signup.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
                  minLength: 1
                  maxLength: 64
            example:
              code: CONF50
      responses:
        "200":
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      valid:
                        type: boolean
                      description:
                        type: string
                        description: Human-readable discount summary (only when valid).
                      amountOffUsd:
                        type: integer
                        description: Discount in integer cents (only when valid).
                      duration:
                        type: string
                        enum:
                          - once
                          - forever
              example:
                data:
                  valid: true
                  description: $50 off your first month
                  amountOffUsd: 5000
                  duration: once
        "400":
          $ref: "#/components/responses/ValidationError"
        "429":
          description: Too many attempts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/public/campaigns/{id}/specs:
    parameters:
      - name: id
        in: path
        required: true
        description: Campaign UUID or numeric display ID
        schema:
          type: string
    get:
      summary: Get public posting spec
      description: Returns the posting spec for a campaign via spec token. Rate limited to 60 req/min per IP.
      operationId: getPublicCampaignSpecs
      tags:
        - "Partner: Campaigns"
      security: []
      parameters:
        - name: token
          in: query
          required: true
          description: Campaign spec token
          schema:
            type: string
            format: uuid
        - name: format
          in: query
          description: "'text' returns plain-text markdown"
          schema:
            type: string
            enum:
              - json
              - text
      responses:
        "200":
          description: Campaign posting spec
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      campaignName:
                        type: string
                      campaignDisplayId:
                        type: integer
                      verticalName:
                        type: string
                        nullable: true
                      postingSpec:
                        type: object
            text/plain:
              schema:
                type: string
        "400":
          description: Token parameter required
        "404":
          description: Campaign not found or invalid token
        "429":
          description: Rate limit exceeded
  /api/v1/public/referrer-signup:
    post:
      operationId: publicReferrerSignup
      tags:
        - Public
      summary: Self-signup as a referrer (tenant-scoped, public)
      description: |
        Resolves the tenant from the request host and creates a referrer with
        status='pending'. Captures TCPA consent. Sends magic-link to applicant
        and notification email to admins. Returns 412 if the tenant has no
        Stripe Connect account configured.
      parameters:
        - name: tenant
          in: query
          required: false
          schema:
            type: string
          description: Optional tenant slug override (otherwise resolved by Host header).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - firstName
                - lastName
                - email
                - phone
                - acceptTcpa
              properties:
                firstName:
                  type: string
                  minLength: 1
                  maxLength: 100
                lastName:
                  type: string
                  minLength: 1
                  maxLength: 100
                email:
                  type: string
                  format: email
                phone:
                  type: string
                  minLength: 7
                  maxLength: 20
                company:
                  type: string
                  nullable: true
                payoutMethodPreference:
                  type: string
                  nullable: true
                acceptTcpa:
                  type: boolean
                  enum:
                    - true
      responses:
        "201":
          description: Referrer created
          content:
            application/json:
              example:
                data:
                  ok: true
                  referrerId: 11111111-2222-3333-4444-555555555555
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Duplicate email for this tenant
        "412":
          description: Precondition failed — tenant has no Stripe Connect account (details.code=STRIPE_NOT_CONNECTED) or the
            referrer program is disabled (details.code=REFERRER_PROGRAM_DISABLED).
        "429":
          description: Rate limited
  /api/v1/providers:
    get:
      summary: List provider credentials (tenant view)
      description: |
        Returns this tenant's own credentials PLUS any shareable system catalog
        rows (`scope=system && shareable=true`). Secret values in `credentials`
        are masked. Superadmins (no active tenant) see every credential.
      operationId: listProviderCredentials
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Credential list (masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProviderCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create a tenant provider credential
      description: |
        Creates a `scope=tenant` credential under the caller's tenant. `scope`
        is forced to `tenant` and `tenantId` to the caller — system rows must
        be created via the superadmin route. Every value in `credentials` is
        encrypted with AES-256-GCM before insert. `capabilities` MUST be a
        subset of the provider's defaults.
      operationId: createProviderCredential
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderCredentialCreate"
      responses:
        "201":
          description: Credential created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderCredential"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — tenant route cannot create scope=system rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/providers/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: UUID of the provider credential.
        schema:
          type: string
          format: uuid
    get:
      summary: Get a single provider credential
      description: |
        Returns the credential if owned by the caller's tenant OR if it is a
        shareable system row. 404 otherwise. Secrets are masked.
      operationId: getProviderCredential
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Credential (masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a tenant provider credential
      description: |
        Partial update on a `scope=tenant` row. System rows are read-only on
        this route. Provided `credentials` keys are re-encrypted; omitted keys
        preserve existing ciphertext. `shareable`, `autoEnableForNewTenants`,
        and `limits` are ignored on tenant rows (superadmin-only).
      operationId: updateProviderCredential
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderCredentialUpdate"
      responses:
        "200":
          description: Credential updated (masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderCredential"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — system credentials are read-only on this route.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a tenant provider credential
      description: |
        Deletes a `scope=tenant` credential. Returns 409 if any
        `providerAssignment` still references this credential — remove the
        assignments first. System rows cannot be deleted via this route.
      operationId: deleteProviderCredential
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Credential deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — system credentials cannot be deleted via the tenant route.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict — credential still has active assignments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/providers/{id}/assignments:
    parameters:
      - name: id
        in: path
        required: true
        description: UUID of the provider credential.
        schema:
          type: string
          format: uuid
    get:
      summary: List assignments for a credential
      description: |
        Returns this tenant's `providerAssignment` rows that reference the
        given credential. The credential must be owned by the tenant OR be a
        shareable system row.
      operationId: listProviderAssignments
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Assignment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProviderAssignment"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      summary: Create an assignment
      description: |
        Creates a new `providerAssignment` row binding this tenant to the
        credential for a single capability. `capability` MUST be in the
        credential's declared capabilities. `overrides` are validated against
        the per-provider allowlist (anything outside the allowlist returns
        `VALIDATION_ERROR`); secret-named keys are always rejected. When
        `isDefault=true`, any existing default for the same `(tenantId,
        capability)` is atomically demoted in the same transaction.
      operationId: createProviderAssignment
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderAssignmentCreate"
      responses:
        "201":
          description: Assignment created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderAssignment"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — credential is not owned by this tenant and is not a shareable system row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/providers/assignments/{assignmentId}:
    parameters:
      - name: assignmentId
        in: path
        required: true
        description: UUID of the assignment row.
        schema:
          type: string
          format: uuid
    put:
      summary: Update an assignment
      description: |
        Partial update on a `providerAssignment` row. Toggling `isDefault=true`
        atomically demotes any other active default for the same
        `(tenantId, capability)`. `overrides` are revalidated against the
        per-provider allowlist and the credential's `limits.maxDailySendLimit`
        cap.
      operationId: updateProviderAssignment
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderAssignmentUpdate"
      responses:
        "200":
          description: Assignment updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderAssignment"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Forbidden — cross-tenant credential reference, or referenced system credential is no longer shareable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete an assignment
      description: Removes the `providerAssignment` row.
      operationId: deleteProviderAssignment
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Assignment deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/providers/{id}/test:
    parameters:
      - name: id
        in: path
        required: true
        description: UUID of the provider credential to test.
        schema:
          type: string
          format: uuid
    post:
      summary: Send a test SMS via this credential
      description: |
        Fires a real SMS through the credential's resolved provider and writes
        a `deliveryLog` row with `isTest=true`. Rate-limited to **10 calls per
        minute per user**. Capability defaults to the first sms_* capability on
        the credential when not specified. Email test-sends are supported for
        `email_messaging` credentials — pass an email address as `to`. Voice
        test-sends are not yet implemented (returns 501).
      operationId: testProviderCredential
      tags:
        - "Admin: Providers"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderTestSendRequest"
      responses:
        "200":
          description: Test send result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ProviderTestSendResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Credential is not configured for SMS sending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded (10/min per user).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Voice test-send not yet implemented for this capability.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/me:
    get:
      operationId: getAuthMe
      tags:
        - Auth
      summary: Get real session identity
      description: |
        Resolves the authenticated user's real identity from the session cookie.
        Unlike `GET /api/v1/auth`, this always reports the underlying user
        (used by integration flows to resolve `tenantId` without coupling to the
        legacy auth shape). Superadmin impersonation is skipped when the request
        referer is a `/superadmin` page. Portal users report `entityType` as their
        `role` and resolve `buyerId`/`partnerId` from `entityId`.
      responses:
        "200":
          description: Current session identity
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      userId:
                        type: string
                        format: uuid
                      email:
                        type: string
                      role:
                        type: string
                      tenantId:
                        type: string
                        format: uuid
                        nullable: true
                      isOwner:
                        type: boolean
                      readOnly:
                        type: boolean
                      piiAccess:
                        type: boolean
                      buyerId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerId:
                        type: string
                        format: uuid
                        nullable: true
                      firstName:
                        type: string
                        nullable: true
                      lastName:
                        type: string
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
  /api/v1/auth/organizations:
    get:
      operationId: listAuthOrganizations
      tags:
        - Auth
      summary: List my organizations
      description: |
        Lists the organizations (tenants) the current user is an active member of.
        Org display name/slug/logo are sourced from the `tenant` table (single
        source of truth). Superadmins get a synthetic `Platform (Superadmin)`
        entry with `organizationId: null` prepended to the list.
      responses:
        "200":
          description: Organizations the user belongs to
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      organizations:
                        type: array
                        items:
                          type: object
                          properties:
                            memberId:
                              type: string
                              format: uuid
                              nullable: true
                            organizationId:
                              type: string
                              format: uuid
                              nullable: true
                            orgName:
                              type: string
                            orgSlug:
                              type: string
                            orgLogo:
                              type: string
                              nullable: true
                            role:
                              type: string
                            status:
                              type: string
                      currentTenantId:
                        type: string
                        format: uuid
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/auth/profile:
    get:
      operationId: getAuthProfile
      tags:
        - Auth
      summary: Get my profile
      description: |
        Returns the authenticated user's own profile, augmented with the live
        session context (role, tenantId, buyerId, partnerId, permissions) and 2FA
        state (grace logins remaining, whether 2FA is required for the caller's role).
      responses:
        "200":
          description: Current user profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      email:
                        type: string
                      firstName:
                        type: string
                        nullable: true
                      lastName:
                        type: string
                        nullable: true
                      phone:
                        type: string
                        nullable: true
                      workPhone:
                        type: string
                        nullable: true
                      ext:
                        type: string
                        nullable: true
                      jobTitle:
                        type: string
                        nullable: true
                      role:
                        type: string
                      tenantId:
                        type: string
                        format: uuid
                        nullable: true
                      buyerId:
                        type: string
                        format: uuid
                        nullable: true
                      partnerId:
                        type: string
                        format: uuid
                        nullable: true
                      contactId:
                        type: string
                        format: uuid
                        nullable: true
                      permissions:
                        type: object
                        additionalProperties: true
                        nullable: true
                      twoFactorEnabled:
                        type: boolean
                      twoFactorLoginsRemaining:
                        type: integer
                        nullable: true
                      twoFactorRequiredForMyRole:
                        type: boolean
                      impersonating:
                        type: boolean
                      lastLoginAt:
                        type: string
                        format: date-time
                        nullable: true
                      createdAt:
                        type: string
                        format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
    put:
      operationId: updateAuthProfile
      tags:
        - Auth
      summary: Update my profile
      description: Updates the authenticated user's own profile fields. All fields optional.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  minLength: 1
                lastName:
                  type: string
                  minLength: 1
                phone:
                  type: string
                workPhone:
                  type: string
                ext:
                  type: string
                jobTitle:
                  type: string
      responses:
        "200":
          description: Profile updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      success:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
      security:
        - sessionCookie: []
        - apiKey: []
    patch:
      operationId: changeAuthPassword
      tags:
        - Auth
      summary: Change password (deprecated)
      description: |
        Deprecated. Password management was removed — users authenticate via magic
        link. Always returns `410 Gone` with error code `DEPRECATED`.
      deprecated: true
      responses:
        "410":
          description: Endpoint removed (passwordless auth)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: object
                    properties:
                      code:
                        type: string
                        example: DEPRECATED
                      message:
                        type: string
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/call-billing-rules:
    get:
      operationId: listCallBillingRules
      tags:
        - Call Billing Rules
      summary: List call billing rules
      description: |
        Lists polymorphic call billing rules (multi-tier call payout settings),
        ordered by priority desc then creation desc. Admin only.
      parameters:
        - name: parentType
          in: query
          schema:
            type: string
            enum:
              - offer
              - campaign
              - contract
          description: Filter by parent entity type
        - name: parentId
          in: query
          schema:
            type: string
            format: uuid
          description: Filter by parent entity id
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated call billing rules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
        - apiKey: []
    post:
      operationId: createCallBillingRule
      tags:
        - Call Billing Rules
      summary: Create call billing rule
      description: |
        Creates a call billing rule under an offer, campaign, or contract. The
        parent entity must exist within the caller's tenant. Conditional field
        requirements are enforced (e.g. `callRevsharePercent` required when
        `callPayoutType=revshare`; `callMinDurationSec` required when
        `callConversionType=length`; `callDuplicateWindowSec` required when
        `callDuplicatePayouts=window`). `callConversionType=contract_billed`
        (payout fires when the buyer contract billed the call) is valid on
        campaign parents only — 400 on offer/contract parents.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - parentType
                - parentId
                - name
                - callPayoutType
                - callConversionType
              properties:
                parentType:
                  type: string
                  enum:
                    - offer
                    - campaign
                    - contract
                parentId:
                  type: string
                  format: uuid
                priority:
                  type: integer
                  default: 0
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                tagMatch:
                  type: object
                  additionalProperties:
                    type: string
                  nullable: true
                dispositionMatch:
                  type: array
                  items:
                    type: string
                  nullable: true
                callPayoutType:
                  type: string
                  enum:
                    - fixed
                    - revshare
                    - raw_call
                callPayoutAmount:
                  type: string
                  nullable: true
                callRevsharePercent:
                  type: string
                  nullable: true
                callMaxPayoutPerCall:
                  type: string
                  nullable: true
                callConversionType:
                  type: string
                  enum:
                    - length
                    - connected
                    - dialed
                    - postback
                    - contract_billed
                callMinDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                callDurationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                  default: connect
                callDuplicatePayouts:
                  type: string
                  enum:
                    - allow
                    - block
                    - window
                  default: allow
                callDuplicateWindowSec:
                  type: integer
                  minimum: 0
                  nullable: true
      responses:
        "201":
          description: Rule created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/CallBillingRule"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/call-billing-rules/{ruleId}:
    parameters:
      - name: ruleId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Call billing rule UUID
    get:
      operationId: getCallBillingRule
      tags:
        - Call Billing Rules
      summary: Get call billing rule
      responses:
        "200":
          description: Call billing rule
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/CallBillingRule"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
    patch:
      operationId: updateCallBillingRule
      tags:
        - Call Billing Rules
      summary: Update call billing rule
      description: |
        Partial update. The merged (existing + patch) config is re-validated
        against the same conditional rules as creation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                priority:
                  type: integer
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                tagMatch:
                  type: object
                  additionalProperties:
                    type: string
                  nullable: true
                dispositionMatch:
                  type: array
                  items:
                    type: string
                  nullable: true
                callPayoutType:
                  type: string
                  enum:
                    - fixed
                    - revshare
                    - raw_call
                callPayoutAmount:
                  type: string
                  nullable: true
                callRevsharePercent:
                  type: string
                  nullable: true
                callMaxPayoutPerCall:
                  type: string
                  nullable: true
                callConversionType:
                  type: string
                  enum:
                    - length
                    - connected
                    - dialed
                    - postback
                    - contract_billed
                callMinDurationSec:
                  type: integer
                  minimum: 0
                  nullable: true
                callDurationAnchor:
                  type: string
                  enum:
                    - incoming
                    - dial
                    - connect
                callDuplicatePayouts:
                  type: string
                  enum:
                    - allow
                    - block
                    - window
                callDuplicateWindowSec:
                  type: integer
                  minimum: 0
                  nullable: true
      responses:
        "200":
          description: Rule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/CallBillingRule"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
    delete:
      operationId: deleteCallBillingRule
      tags:
        - Call Billing Rules
      summary: Delete call billing rule
      responses:
        "200":
          description: Rule deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/calls/{callId}/distributions:
    get:
      operationId: listCallDistributions
      tags:
        - Calls
      summary: List call distributions
      description: |
        Returns the call's distribution waterfall — each `callDistribution` row
        joined (LATERAL, by nearest timestamp) to its matching `callSale`.
        Ordered by rank asc (nulls last) then creation desc.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Call UUID
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated call distributions
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            callId:
                              type: string
                              format: uuid
                            contractId:
                              type: string
                              format: uuid
                            contractName:
                              type: string
                              nullable: true
                            buyerId:
                              type: string
                              format: uuid
                            offerId:
                              type: string
                              format: uuid
                              nullable: true
                            disposition:
                              type: string
                            dispositionReason:
                              type: string
                              nullable: true
                            filterResults:
                              type: object
                              additionalProperties: true
                              nullable: true
                            price:
                              type: string
                              nullable: true
                            rank:
                              type: integer
                              nullable: true
                            saleId:
                              type: string
                              format: uuid
                              nullable: true
                            saleStatus:
                              type: string
                              nullable: true
                            attemptOrder:
                              type: integer
                              nullable: true
                            saleConnectPrice:
                              type: string
                              nullable: true
                            saleTotalPrice:
                              type: string
                              nullable: true
                            createdAt:
                              type: string
                              format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/invitations:
    get:
      operationId: listInvitations
      tags:
        - Invitations
      summary: List invitations
      description: |
        Lists invitations scoped to the caller's tenant, optionally filtered by
        status (default `pending`), entityType, and entityId. Admin only.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            default: pending
          description: Invitation status filter
        - name: entityType
          in: query
          schema:
            type: string
            enum:
              - admin
              - buyer
              - partner
          description: Filter by invited entity type
        - name: entityId
          in: query
          schema:
            type: string
          description: Filter by invited entity id
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: Paginated invitations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - sessionCookie: []
        - apiKey: []
    post:
      operationId: createInvitation
      tags:
        - Invitations
      summary: Create (or resend) an invitation
      description: |
        Invites a user into the tenant with an app role and permission set.
        Callers cannot grant permissions they do not themselves hold (superadmins
        and tenant owners bypass). Role-scope gating limits which app roles a
        caller may invite. If a pending invitation already exists for the email,
        the request collapses into a resend (`resend: true`, status `200`);
        otherwise a new invitation is created (status `201`). Superadmins
        (tenantId=null) inviting a buyer/partner resolve the target tenant from
        the entity; inviting a tenant `admin` requires an active tenant context.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - entityType
                - appRole
                - permAccounts
                - permUsers
                - permBilling
                - permOffers
                - permReports
              properties:
                email:
                  type: string
                  format: email
                entityType:
                  type: string
                  enum:
                    - admin
                    - buyer
                    - partner
                entityId:
                  type: string
                  nullable: true
                  description: Required when entityType is not `admin`.
                appRole:
                  type: string
                  enum:
                    - admin
                    - buyer
                    - partner
                permAccounts:
                  type: boolean
                permUsers:
                  type: boolean
                permBilling:
                  type: boolean
                permOffers:
                  type: boolean
                permReports:
                  type: boolean
                role:
                  type: string
                  enum:
                    - member
                    - admin
                    - owner
      responses:
        "200":
          description: Existing pending invitation resent
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
                  resend:
                    type: boolean
                    example: true
        "201":
          description: Invitation created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
                  resend:
                    type: boolean
                    example: false
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Email is already a member, or a pending invitation already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Invitation rate limit exceeded
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/invitations/{id}/resend:
    parameters:
      - $ref: "#/components/parameters/pathId"
    patch:
      operationId: resendInvitation
      tags:
        - Invitations
      summary: Resend an invitation
      description: |
        Bumps the invitation's `expiresAt`/`lastSentAt` and re-sends the email.
        Permissions, role, and entity are never rewritten (anti-tamper).
        Tenant-scoped lookup returns 404 for wrong-tenant or non-pending rows.
      responses:
        "200":
          description: Invitation resent
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/invitations/{id}/revoke:
    parameters:
      - $ref: "#/components/parameters/pathId"
    patch:
      operationId: revokeInvitation
      tags:
        - Invitations
      summary: Revoke an invitation
      description: |
        Marks the invitation revoked. Tenant-scoped lookup returns 404 for
        wrong-tenant rows.
      responses:
        "200":
          description: Invitation revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      revoked:
                        type: boolean
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
  /api/v1/leads/bulk-import:
    post:
      operationId: bulkImportLeads
      tags:
        - Leads
      summary: Bulk import leads (JSON)
      description: |
        Imports up to 1000 leads in a single request. Authenticated with an API
        key (Bearer `lr_xxx`) that has the `posting` scope; read-only keys are
        rejected. Leads are inserted as `pending` at `$0` cost/revenue and are
        not routed. Rows are deduplicated by `externalId` (within the batch and
        against existing leads for the tenant). Invalid rows and rows with an
        unknown `verticalId` are counted as failures with per-row error messages;
        the request still returns `200`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - leads
              properties:
                leads:
                  type: array
                  minItems: 1
                  maxItems: 1000
                  items:
                    type: object
                    required:
                      - firstName
                      - lastName
                      - email
                      - phone
                      - verticalId
                      - tcpaConsent
                    properties:
                      firstName:
                        type: string
                        minLength: 1
                      lastName:
                        type: string
                        minLength: 1
                      email:
                        type: string
                        format: email
                      phone:
                        type: string
                        minLength: 10
                      verticalId:
                        type: string
                        format: uuid
                      tcpaConsent:
                        type: boolean
                        description: Accepts boolean or the strings `true`/`1`/`yes`.
                      dateOfBirth:
                        type: string
                        nullable: true
                      address:
                        type: string
                        nullable: true
                      city:
                        type: string
                        nullable: true
                      state:
                        type: string
                        maxLength: 2
                        nullable: true
                      zipCode:
                        type: string
                        maxLength: 10
                        nullable: true
                      country:
                        type: string
                        default: US
                      externalId:
                        type: string
                        nullable: true
                        description: Used for deduplication.
                      originalCreatedAt:
                        type: string
                        nullable: true
                        description: Overrides the imported lead's createdAt.
                      trustedFormCertUrl:
                        type: string
                        format: uri
                        nullable: true
                      jornayaLeadId:
                        type: string
                        nullable: true
                        description: Jornaya LeadiD token (36-char UUID). When the tenant's Jornaya integration is enabled with auto-audit,
                          importing one triggers a billed LeadiD audit query.
                      consentCertUrl:
                        type: string
                        format: uri
                        nullable: true
                      custom:
                        type: object
                        additionalProperties: true
                    additionalProperties: true
      responses:
        "200":
          description: Import result summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  imported:
                    type: integer
                  skipped:
                    type: integer
                  failed:
                    type: integer
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        row:
                          type: integer
                        message:
                          type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - partnerApiKey: []
  /api/v1/leads/bulk-import/csv:
    post:
      operationId: bulkImportLeadsCsv
      tags:
        - Leads
      summary: Bulk import leads (CSV upload)
      description: |
        Imports leads from a CSV file uploaded as `multipart/form-data` (field
        name `file`). The first CSV row must be camelCase headers matching the
        JSON import fields; `tcpaConsent` is coerced from `true`/`1`/`yes`.
        Max file size 10MB, max 1000 data rows. Authenticated with an API key
        (Bearer `lr_xxx`) that has the `posting` scope; read-only keys are
        rejected. Returns the same import-summary shape as the JSON route.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV file (first row = camelCase headers).
      responses:
        "200":
          description: Import result summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  imported:
                    type: integer
                  skipped:
                    type: integer
                  failed:
                    type: integer
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        row:
                          type: integer
                        message:
                          type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
      security:
        - partnerApiKey: []
  /api/v1/public/checkout/sku:
    post:
      operationId: publicCheckoutSku
      tags:
        - Public
      summary: Start a public SKU checkout
      description: |
        Unauthenticated. Creates a Stripe Connect checkout session for a prepaid
        SKU on the tenant resolved from the request host (or `?tenant=` slug).
        The SKU is bound 1:1 to a vertical — `verticalIds` must contain exactly
        that vertical. Optional `?ref=`/`lr_ref` cookie referral attribution and
        `?code=` promo code are forwarded to Stripe. When `firstName` +
        `tcpaConsent:true` are supplied (capture mode), `lastName` is also
        required and a `pending_payment` buyer is pre-created (returns `buyerId`
        + `checkoutSessionId`). Rate limited to 10 requests/min/IP.
      parameters:
        - name: tenant
          in: query
          schema:
            type: string
          description: Tenant slug (falls back to host-based resolution).
        - name: ref
          in: query
          schema:
            type: string
          description: Referral code for attribution.
        - name: code
          in: query
          schema:
            type: string
          description: Promo code forwarded to Stripe checkout.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - skuSlug
                - verticalIds
                - email
              properties:
                skuSlug:
                  type: string
                  minLength: 1
                  maxLength: 64
                verticalIds:
                  type: array
                  items:
                    type: string
                  minItems: 1
                  maxItems: 10
                email:
                  type: string
                  format: email
                firstName:
                  type: string
                  minLength: 1
                  maxLength: 100
                lastName:
                  type: string
                  maxLength: 100
                  description: Required in capture mode (when firstName + tcpaConsent supplied).
                company:
                  type: string
                  maxLength: 255
                phone:
                  type: string
                  maxLength: 20
                tcpaConsent:
                  type: boolean
                  description: When present must be true; enables buyer pre-creation.
      responses:
        "200":
          description: Checkout session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: Stripe checkout URL to redirect to.
                      buyerId:
                        type: string
                        format: uuid
                        description: Only present in capture mode.
                      checkoutSessionId:
                        type: string
                        description: Only present in capture mode.
        "400":
          description: Validation error, or the merchant cannot accept payments yet (CONNECT_NOT_READY)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          description: SKU promo window has closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: A buyer with this email is already active (BUYER_EXISTS_ACTIVE)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/public/checkout/topup:
    post:
      operationId: publicCheckoutTopup
      tags:
        - Public
      summary: Start a public balance top-up checkout
      description: |
        Unauthenticated. Creates a Stripe Connect checkout session to add funds
        to a balance for the tenant resolved from the request host (custom domain
        only). `amountCents` must be an integer within the configured min/max
        top-up bounds. Rate limited to 10 requests/min/IP.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amountCents
                - email
              properties:
                amountCents:
                  type: integer
                  description: Amount in cents; bounded by configured min/max top-up limits.
                email:
                  type: string
                  format: email
      responses:
        "200":
          description: Checkout session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: Stripe checkout URL to redirect to.
        "400":
          description: Validation error, or the merchant cannot accept payments yet (CONNECT_NOT_READY)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/public/recordings/{token}:
    get:
      operationId: getSignedRecording
      tags:
        - Public
      summary: Stream a call recording via a signed URL
      description: |
        Redeems a short-lived capability token minted by
        `POST /api/admin/calls/{id}/recording/signed-url` and streams the audio.

        Deliberately unauthenticated — the token IS the credential, so no session
        cookie or API key is sent or accepted. This exists so a bulk download
        script or an `<audio>` element can fetch recordings without being handed
        an `admin`-scoped API key, which would open the entire admin surface.

        The token names exactly one call in one tenant and expires after
        `Config.recordingSignedUrlTtlSeconds` (15 min). There is no revocation
        list — the TTL is the revocation window, so a leaked URL stays valid until
        it expires.

        Responses are intentionally uninformative: `401` covers malformed,
        tampered and expired tokens alike, and `404` covers "no such call", "wrong
        tenant" and "recording already purged by retention". Neither confirms
        whether a given call exists.
      security: []
      parameters:
        - name: token
          in: path
          required: true
          description: Signed capability token (`base64url(payload).base64url(hmac)`)
          schema:
            type: string
      responses:
        "200":
          description: "Audio stream. `Content-Type` echoes the upstream recording (typically `audio/mpeg`), `Content-Disposition:
            inline`, and `Cache-Control` is `private` with max-age clamped to the token TTL so a cached copy cannot
            outlive the capability that fetched it."
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "401":
          description: Token missing, malformed, tampered with, or expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Recording not found, not in the token's tenant, or deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/public/dsr:
    post:
      operationId: submitDataSubjectRequest
      tags:
        - Public
      summary: Submit a data subject request (DSR)
      description: |
        Unauthenticated privacy request (GDPR/CCPA). Creates a
        `dataDeletionRequest` for the tenant resolved from the request host and
        notifies tenant admins. `do_not_sell` maps to CCPA; all other types map
        to GDPR. If the tenant cannot be resolved the request is still recorded
        (visible to superadmins only). Rate limited to 5 requests/hour/IP.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - requestType
                - email
              properties:
                requestType:
                  type: string
                  enum:
                    - access
                    - delete
                    - correct
                    - do_not_sell
                email:
                  type: string
                  format: email
                firstName:
                  type: string
                  maxLength: 100
                lastName:
                  type: string
                  maxLength: 100
                details:
                  type: string
                  maxLength: 2000
      responses:
        "201":
          description: Request submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      message:
                        type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/public/skus:
    get:
      operationId: listPublicSkus
      tags:
        - Public
      summary: List public SKUs
      description: |
        Unauthenticated. Lists active, currently-available prepaid SKUs for the
        tenant resolved from the request host (or `?tenant=` slug), ordered by
        sort order. SKUs past their `availableUntil` date are excluded. Rate
        limited to 60 requests/min/IP.
      parameters:
        - name: tenant
          in: query
          schema:
            type: string
          description: Tenant slug (falls back to host-based resolution).
        - name: productType
          in: query
          schema:
            type: string
            enum:
              - lead
              - call
          description: Filter by product type (lead SKUs include those with no product type).
      responses:
        "200":
          description: Available SKUs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        slug:
                          type: string
                        name:
                          type: string
                        tagline:
                          type: string
                          nullable: true
                        badge:
                          type: string
                          nullable: true
                        priceCents:
                          type: integer
                        leadCount:
                          type: integer
                          nullable: true
                        bonusLeads:
                          type: integer
                          nullable: true
                        verticalId:
                          type: string
                          format: uuid
                          nullable: true
                        isSubscription:
                          type: boolean
                        billingInterval:
                          type: string
                          nullable: true
                        trialLeads:
                          type: integer
                          nullable: true
                        trialDays:
                          type: integer
                          nullable: true
                        availableUntil:
                          type: string
                          nullable: true
                        availableUntilDisplay:
                          type: string
                          nullable: true
                        perLeadCostCents:
                          type: integer
                        totalLeads:
                          type: integer
                        productType:
                          type: string
                        unitType:
                          type: string
                        unitLabel:
                          type: string
                        unitLabelPlural:
                          type: string
                        unitCount:
                          type: integer
                        unitsTotal:
                          type: integer
                        perUnitCostCents:
                          type: integer
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/public/skus/{slug}:
    get:
      operationId: getPublicSku
      tags:
        - Public
      summary: Get a public SKU by slug
      description: |
        Unauthenticated. Returns a single active SKU by slug for the tenant
        resolved from the request host (or `?tenant=` slug). Returns 410 if the
        SKU's availability window has closed. Rate limited to 60 requests/min/IP.
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
          description: SKU slug
        - name: tenant
          in: query
          schema:
            type: string
          description: Tenant slug (falls back to host-based resolution).
      responses:
        "200":
          description: SKU detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      slug:
                        type: string
                      name:
                        type: string
                      tagline:
                        type: string
                        nullable: true
                      badge:
                        type: string
                        nullable: true
                      priceCents:
                        type: integer
                      leadCount:
                        type: integer
                        nullable: true
                      bonusLeads:
                        type: integer
                        nullable: true
                      verticalId:
                        type: string
                        format: uuid
                        nullable: true
                      isSubscription:
                        type: boolean
                      billingInterval:
                        type: string
                        nullable: true
                      trialLeads:
                        type: integer
                        nullable: true
                      trialDays:
                        type: integer
                        nullable: true
                      availableUntil:
                        type: string
                        nullable: true
                      availableUntilDisplay:
                        type: string
                        nullable: true
                      perLeadCostCents:
                        type: integer
                      totalLeads:
                        type: integer
                      productType:
                        type: string
                      unitType:
                        type: string
                      unitLabel:
                        type: string
                      unitLabelPlural:
                        type: string
                      unitCount:
                        type: integer
                      unitsTotal:
                        type: integer
                      perUnitCostCents:
                        type: integer
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          description: SKU no longer available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
  /api/v1/utils/zip-lookup:
    get:
      operationId: zipLookup
      tags:
        - Public
      summary: Look up city/state by ZIP code
      description: |
        Unauthenticated utility. Returns the city and state for a US 5-digit ZIP
        code. Non-numeric characters are stripped; the first 5 digits are used.
      parameters:
        - name: zip
          in: query
          required: true
          schema:
            type: string
          description: US ZIP code (5 digits).
      responses:
        "200":
          description: ZIP location
          content:
            application/json:
              schema:
                type: object
                properties:
                  city:
                    type: string
                  state:
                    type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "404":
          $ref: "#/components/responses/NotFound"
      security: []
  /api/v1/verticals/{verticalId}/cascade-preview:
    get:
      operationId: getVerticalCascadePreview
      tags:
        - Verticals
      summary: Preview vertical deactivation cascade
      description: |
        Returns counts of active offers, campaigns, and contracts that would be
        flipped to inactive if this vertical were deactivated with
        `cascadeInactive=true`. Powers the confirmation modal in the verticals
        admin UI. Admin only.
      parameters:
        - name: verticalId
          in: path
          required: true
          schema:
            type: string
          description: Vertical UUID (or resolvable vertical identifier).
      responses:
        "200":
          description: Cascade counts
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      offers:
                        type: integer
                      campaigns:
                        type: integer
                      contracts:
                        type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
      security:
        - sessionCookie: []
        - apiKey: []
tags:
  - name: "Admin: AI Dialer"
    description: AI dialer agents, calls, and campaigns (admin)
  - name: "Admin: Alerts"
    description: Operational alert configuration (admin)
  - name: "Admin: Approvals"
    description: Approval queue management (admin)
  - name: "Admin: Bid Signals"
    description: Real-time bid signal log (admin)
  - name: "Admin: Billing"
    description: Billing audit, reconciliation, repair, and Stripe events (admin)
  - name: "Admin: Buyer SKUs"
    description: Buyer SKU inventory, credits, refunds, and subscriptions (admin)
  - name: "Admin: Consent"
    description: Consent record management (admin)
  - name: "Admin: Contracts"
    description: Contract management (admin)
  - name: "Admin: Coupons"
    description: Coupon and promotion code management (admin)
  - name: "Admin: Credits"
    description: Tenant credit balance, packages, and subscription plans (admin)
  - name: "Admin: Dedup"
    description: Deduplication log (admin)
  - name: "Admin: Delivery"
    description: Delivery spec import (admin)
  - name: "Admin: Delivery Log"
    description: Lead delivery log and redelivery (admin)
  - name: "Admin: Delivery Test"
    description: Delivery endpoint testing (admin)
  - name: "Admin: Disposition Log"
    description: Disposition log (admin)
  - name: "Admin: Distribution Log"
    description: Lead distribution waterfall log (admin)
  - name: "Admin: Engagements"
    description: Lead engagement tracking (admin)
  - name: "Admin: Enrollments"
    description: Enrollment requests, setup tokens, and impersonation (admin)
  - name: "Admin: Events"
    description: System event log (admin)
  - name: "Admin: Export"
    description: CSV entity export (admin)
  - name: "Admin: Intake Centers"
    description: Intake center configuration (admin)
  - name: "Admin: Messaging Log"
    description: Messaging (email/SMS) log (admin)
  - name: "Admin: Notification Subscriptions"
    description: Notification subscription management (admin)
  - name: "Admin: Notifications"
    description: Notification delivery and history (admin)
  - name: "Admin: Number Pool"
    description: Tracking number pool management (admin)
  - name: "Admin: Optimization"
    description: Optimization suggestions (admin)
  - name: "Admin: Organizations"
    description: Organization management (admin)
  - name: "Admin: Ping Log"
    description: Ping request log (admin)
  - name: "Admin: Postback Log"
    description: Postback (CPA) log (admin)
  - name: "Admin: Posting Log"
    description: Lead posting log (admin)
  - name: "Admin: Products"
    description: Product catalog management (admin)
  - name: "Admin: Providers"
    description: Tenant provider (SMS) credentials and assignments (admin)
  - name: "Admin: Referrers"
    description: Referrer management (admin)
  - name: "Admin: Report Folders"
    description: Report folder organization (admin)
  - name: "Admin: SKUs"
    description: SKU catalog management and Stripe sync (admin)
  - name: "Admin: SMS Templates"
    description: SMS template management (admin)
  - name: "Admin: Saved Reports"
    description: Saved report management (admin)
  - name: "Admin: Scoring Models"
    description: Lead quality scoring model authoring — versions, promotion, and pre-attach validation (admin)
  - name: "Admin: Security"
    description: IP rules, security settings, and active sessions (admin)
  - name: "Admin: Signups"
    description: Pending portal signup approvals (admin)
  - name: "Admin: Subscriptions"
    description: Subscription buyer SKU listing (admin)
  - name: "Admin: Telephony"
    description: Telephony provider configuration (admin)
  - name: "Admin: Vertical Groups"
    description: Vertical group management (admin)
  - name: "Admin: Webhook Log"
    description: Webhook event delivery log (admin)
  - name: "Admin: Wizards"
    description: Buyer onboarding wizard authoring (admin)
  - name: Audit Logs
    description: System audit log access
  - name: Auth
    description: Authentication, sessions, and API key management
  - name: Blocked Lists
    description: Blocked phone/email lists
  - name: Buyers
    description: Lead buyers and their configuration
  - name: Call Billing Rules
    description: Call billing rule configuration
  - name: Calls
    description: Call records and call tracking
  - name: Campaigns
    description: Partner campaigns for lead submission
  - name: Compliance
    description: TCPA consent and compliance management
  - name: "Consent: Accounts"
    description: Consent account provisioning (consent)
  - name: "Consent: Certificates"
    description: TCPA consent certificate lifecycle (consent)
  - name: Contacts
    description: Contact list management
  - name: Contracts
    description: Buyer contracts with filters, caps, and pricing
  - name: Cron
  - name: Disposition Codes
    description: Lead disposition code definitions
  - name: Email Templates
    description: Email template management
  - name: IVR
    description: Interactive voice response trees
  - name: Integrations
    description: Third-party integrations and delivery endpoints
  - name: "Internal: Call Webhooks"
  - name: Invitations
    description: User invitation management
  - name: Leads
    description: Lead records and lifecycle management
  - name: "Messaging: Analytics"
    description: Campaign, flow, and deliverability analytics (messaging)
  - name: "Messaging: Campaigns"
    description: Email/SMS campaign management (messaging)
  - name: "Messaging: Contacts"
    description: Contact records, tags, and timelines (messaging)
  - name: "Messaging: Flows"
    description: Automation flow builder and steps (messaging)
  - name: "Messaging: Flows API"
    description: "External API: flow triggering (messaging v1)"
  - name: "Messaging: Lists"
    description: "External API: contact lists (messaging v1)"
  - name: "Messaging: Messages"
    description: Individual message records (messaging)
  - name: "Messaging: Profiles"
    description: "External API: contact profiles (messaging v1)"
  - name: "Messaging: Providers"
    description: Sending provider configuration (messaging)
  - name: "Messaging: Segments"
    description: Contact segmentation (messaging)
  - name: "Messaging: Send"
    description: "External API: transactional send (messaging v1)"
  - name: "Messaging: Settings"
    description: Messaging workspace settings and API keys (messaging)
  - name: "Messaging: Suppression"
    description: Suppression list management (messaging)
  - name: "Messaging: Templates"
    description: Email/SMS template management (messaging)
  - name: Offers
    description: Offers linking campaigns to contracts with distribution rules
  - name: Onboarding
    description: Tenant onboarding checklist status and progress
  - name: "Partner: Campaigns"
    description: "Campaign management (partner portal, role: partner)"
  - name: "Partner: Consent"
    description: Consent management (partner portal)
  - name: "Partner: Engagements"
    description: "Engagement tracking (partner portal, role: partner)"
  - name: "Partner: Leads"
    description: "Lead access (partner portal, role: partner)"
  - name: "Partner: Offers"
    description: "Offer visibility (partner portal, role: partner)"
  - name: "Partner: Payouts"
    description: "Payout history (partner portal, role: partner)"
  - name: "Partner: People"
    description: "People records (partner portal, role: partner)"
  - name: "Partner: Posting Log"
    description: "Posting log (partner portal, role: partner)"
  - name: "Partner: Reports"
    description: "Reporting and analytics (partner portal, role: partner)"
  - name: "Partner: Settings"
    description: "Partner settings and API key management (partner portal, role: partner)"
  - name: "Partner: Users"
    description: "Partner portal user management (partner portal, role: partner)"
  - name: "Partner: Webhooks"
    description: "Webhook configuration (partner portal, role: partner)"
  - name: Partners
    description: Lead source partners
  - name: Payouts
    description: Partner payout management
  - name: People
    description: People (contacts) extracted from leads
  - name: "Portal: API Keys"
    description: API key management (buyer portal)
  - name: "Portal: Agent Connect"
    description: Live call taking — availability toggle, webhook subscriptions, event feed, and disposition (buyer portal)
  - name: "Portal: Billing"
    description: Prepaid balance and credit summary (buyer portal)
  - name: "Portal: Calls"
    description: Revenue-bearing calls (buyer portal)
  - name: "Portal: Contracts"
    description: Contract self-service pause/resume and updates (buyer portal)
  - name: "Portal: Delivery Log"
    description: Delivery log (buyer portal)
  - name: "Portal: Discovery"
    description: Buyer identity and available endpoints (buyer portal)
  - name: "Portal: Engagements"
    description: Engagement tracking (buyer portal)
  - name: "Portal: People"
    description: People records (buyer portal)
  - name: "Portal: Posting Log"
    description: Posting log (buyer portal)
  - name: "Portal: SKUs & Checkout"
    description: SKU catalog and Stripe checkout (buyer portal)
  - name: "Portal: Settings"
    description: Buyer self-service settings — lead-completion partner-response phone, redirect URL, and media (buyer portal)
  - name: "Portal: Subscriptions"
    description: Subscription pause/resume (buyer portal)
  - name: "Portal: Users"
    description: Buyer portal user management (buyer portal)
  - name: "Portal: Wizard"
    description: Buyer onboarding wizard (buyer portal)
  - name: Public
    description: Public unauthenticated endpoints (signup, checkout, DSR, ZIP lookup, DNI impressions)
  - name: Referrer Portal
    description: "Referrer self-service portal — referrals, commissions, payouts (role: referrer)"
  - name: Refunds
    description: Lead refund processing
  - name: Reports
    description: Reporting and analytics
  - name: Search
  - name: Sessions
    description: Active session management
  - name: Settings
    description: System settings and configuration
  - name: "Superadmin: AI Agent"
    description: Agent session monitoring (superadmin)
  - name: "Superadmin: Affiliates"
    description: Platform affiliate program — affiliates, payouts, and postbacks (superadmin)
  - name: "Superadmin: Billing"
    description: Platform billing overview, service/AI costs, transactions, and per-tenant billing (superadmin)
  - name: "Superadmin: Compliance"
    description: HIPAA/SOC2 compliance status, access review, docs, and floors (superadmin)
  - name: "Superadmin: Coupons"
    description: Platform coupon codes on the platform Stripe account (superadmin)
  - name: "Superadmin: Credits"
    description: Tenant credit packages, subscription plans, pricing, and manual adjustments (superadmin)
  - name: "Superadmin: Email"
    description: System email provider and email template management (superadmin)
  - name: "Superadmin: Marketing"
    description: Marketing-site pageview and funnel analytics (superadmin)
  - name: "Superadmin: Providers"
    description: System provider credentials and tenant assignments (superadmin)
  - name: "Superadmin: SMS"
    description: System SMS template management (superadmin)
  - name: "Superadmin: Settings"
    description: Global signup, AI domain, and AI dialer settings (superadmin)
  - name: "Superadmin: Signups"
    description: Abandoned get-started wizard captures (superadmin)
  - name: "Superadmin: Telephony"
    description: Global telephony management (superadmin)
  - name: "Superadmin: Tenants"
    description: Multi-tenant management (superadmin)
  - name: "Superadmin: Users"
    description: Cross-tenant user management (superadmin)
  - name: Tracking Numbers
    description: Phone tracking number management
  - name: Transactions
    description: Financial transactions and billing
  - name: Users
    description: User account management
  - name: Verticals
    description: Lead verticals (categories) and custom field definitions
  - name: Webhooks
    description: Webhook configuration and event delivery
x-tagGroups:
  - name: Getting Started
    tags:
      - Auth
      - Public
      - Onboarding
  - name: Admin
    tags:
      - Verticals
      - Partners
      - Buyers
      - Campaigns
      - Contracts
      - Offers
      - Leads
      - People
      - Calls
      - IVR
      - Email Templates
      - Transactions
      - Payouts
      - Refunds
      - Integrations
      - Delivery Methods
      - Tracking Numbers
      - Webhooks
      - Sessions
      - Settings
      - Compliance
      - Disposition Codes
      - Users
      - Contacts
      - Blocked Lists
      - Reports
      - Audit Logs
      - Invitations
      - Call Billing Rules
  - name: Admin Operations
    tags:
      - "Admin: Vertical Groups"
      - "Admin: Intake Centers"
      - "Admin: Number Pool"
      - "Admin: Telephony"
      - "Admin: Disposition Codes"
      - "Admin: Engagements"
      - "Admin: Consent"
      - "Admin: Posting Log"
      - "Admin: Organizations"
      - "Admin: Contracts"
      - "Admin: Events"
      - "Admin: Delivery Test"
      - "Admin: AI Dialer"
      - "Admin: Alerts"
      - "Admin: Approvals"
      - "Admin: Bid Signals"
      - "Admin: Billing"
      - "Admin: Buyer SKUs"
      - "Admin: Coupons"
      - "Admin: Credits"
      - "Admin: Dedup"
      - "Admin: Delivery"
      - "Admin: Delivery Log"
      - "Admin: Disposition Log"
      - "Admin: Distribution Log"
      - "Admin: Enrollments"
      - "Admin: Export"
      - "Admin: Messaging Log"
      - "Admin: Notification Subscriptions"
      - "Admin: Notifications"
      - "Admin: Optimization"
      - "Admin: Ping Log"
      - "Admin: Postback Log"
      - "Admin: Products"
      - "Admin: Providers"
      - "Admin: Referrers"
      - "Admin: Report Folders"
      - "Admin: SKUs"
      - "Admin: Scoring Models"
      - "Admin: SMS Templates"
      - "Admin: Saved Reports"
      - "Admin: Security"
      - "Admin: Signups"
      - "Admin: Subscriptions"
      - "Admin: Webhook Log"
      - "Admin: Wizards"
  - name: Buyer
    tags:
      - "Portal: People"
      - "Portal: Posting Log"
      - "Portal: Engagements"
      - "Portal: Agent Connect"
      - "Portal: API Keys"
      - "Portal: Billing"
      - "Portal: Calls"
      - "Portal: Contracts"
      - "Portal: Delivery Log"
      - "Portal: Discovery"
      - "Portal: SKUs & Checkout"
      - "Portal: Settings"
      - "Portal: Subscriptions"
      - "Portal: Users"
      - "Portal: Wizard"
  - name: Partner
    tags:
      - "Partner: Campaigns"
      - "Partner: Leads"
      - "Partner: Offers"
      - "Partner: Payouts"
      - "Partner: Settings"
      - "Partner: Consent"
      - "Partner: Engagements"
      - "Partner: People"
      - "Partner: Posting Log"
      - "Partner: Reports"
      - "Partner: Users"
      - "Partner: Webhooks"
      - Referrer Portal
  - name: Messaging
    tags:
      - "Messaging: Analytics"
      - "Messaging: Campaigns"
      - "Messaging: Contacts"
      - "Messaging: Flows"
      - "Messaging: Segments"
      - "Messaging: Messages"
      - "Messaging: Templates"
      - "Messaging: Providers"
      - "Messaging: Suppression"
      - "Messaging: Settings"
      - "Messaging: Profiles"
      - "Messaging: Lists"
      - "Messaging: Flows API"
      - "Messaging: Send"
  - name: Consent
    tags:
      - "Consent: Certificates"
      - "Consent: Accounts"
  - name: Superadmin
    tags:
      - "Superadmin: Tenants"
      - "Superadmin: Users"
      - "Superadmin: Telephony"
      - "Superadmin: AI Agent"
      - "Superadmin: Settings"
      - "Superadmin: Providers"
      - "Superadmin: Billing"
      - "Superadmin: Credits"
      - "Superadmin: Compliance"
      - "Superadmin: Email"
      - "Superadmin: SMS"
      - "Superadmin: Affiliates"
      - "Superadmin: Coupons"
      - "Superadmin: Marketing"
      - "Superadmin: Signups"
